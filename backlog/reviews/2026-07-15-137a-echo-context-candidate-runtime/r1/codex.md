---
item_id: "2026-07-15-137a-echo-context-candidate-runtime"
round: 1
reviewer: "codex"
artifact_sha: "de3c249f8a586b2723616f010d6aab2586629744"
completed_at: '2026-07-17T20:56:14Z'
review_protocol: 2
review_mode: "full"
verdict: "proceed_after_patches"
findings:
  - severity: "high"
    mechanism: "candidate entrypoint and execution environment closure"
    origin: "original"
    where: "AC1, AC3, and AC4 — runtime, seed, stage, and smoke interfaces"
    finding: "The spec names entrypoints but never pins their executable commands, accepted flags, config-file location, derived token/lease/database/log paths, ready and liveness FD numbers and directions, exit codes, or stdout/stderr byte limits. Patch the ACs and Tests with one exact invocation contract for each entrypoint, including the only permitted environment inputs and failure-before-mutation ordering."
  - severity: "high"
    mechanism: "candidate root topology and ownership"
    origin: "original"
    where: "AC1, AC4, and AC5 — candidate-root topology"
    finding: "AC1 requires one closed candidate root containing every runtime-related path, while AC4 creates and copies a stage into another disposable root and AC5 refers to plural stage/state roots. Define the exact directory topology and containment relationship, which root the config names, which members are immutable versus writable, and the ownership, mode, validation, and cleanup rules for each root."
  - severity: "high"
    mechanism: "authentication before application body consumption"
    origin: "original"
    where: "AC2 — authorization and capture-disabled before body read"
    finding: "The literal requirement that rejection occur before any body byte is read is not implementable or observable at the Node HTTP transport boundary because bytes may already be buffered with the headers. Define the enforceable invariant as no application body consumption, parsing, listener attachment, or storage work before raw-header and Host validation; pin the exact accepted Host grammar; and require raw-socket tests that withhold or stream an unbounded body while proving an immediate rejection and zero application consumption."
  - severity: "high"
    mechanism: "generated stage inventory and executed-byte binding"
    origin: "original"
    where: "AC4 and files_to_modify — diagnostic inventory and executed-byte binding"
    finding: "The inventory must bind the candidate target SHA/tree, yet provenance/candidate-runtime.v1.json is also listed as a tracked target file, leaving an impossible or ambiguous self-reference. Specify that head-bound inventory is generated only after the target commit into the untracked stage, define its canonical digest, reject a dirty or mismatched source tree, and verify the destination copy's complete member set, modes, and hashes immediately before execution so the reviewed head is bound to the bytes actually run."
  - severity: "medium"
    mechanism: "candidate SQLite writer lease acquisition"
    origin: "original"
    where: "AC1 and tests/runtime/composition.test.ts — SQLite writer lease"
    finding: "An exclusive transaction alone does not guarantee that a contender exits rather than waiting and starting after the winner stops. Pin the lease database pragmas, zero busy timeout, exact transaction statement and acquisition order, typed contention exit, and test that a loser exits before main-database or socket access and cannot resume after the winner releases the lease."
  - severity: "medium"
    mechanism: "parent-liveness orphan cleanup and external observation"
    origin: "original"
    where: "AC4 and tests/candidate/lifecycle.test.ts — harness-SIGKILL orphan proof"
    finding: "After the harness is killed it cannot perform the required absence checks, and the spec does not identify an observer or prevent that observer from inheriting the liveness writer. Define the external observer process, FD inheritance map, authoritative start-time source, shutdown deadline, PID-reuse-safe identity checks, and failure cleanup used to prove child, listener, database handles, and lease absence."
  - severity: "medium"
    mechanism: "process-tree-scoped repository and network denial"
    origin: "original"
    where: "AC4, AC5 steps 8–9, and tests/candidate/repo-free.test.ts"
    finding: "Environment scrubbing and static fences do not prove that the runtime made no repository fallback, package-manager/network access, or connection to the three forbidden ports. Add a concrete process-tree-scoped observation or denial mechanism, state its platform prerequisites, and define the captured events and assertions; system-wide port checks would be racy and could attribute unrelated host activity to the candidate."
---
