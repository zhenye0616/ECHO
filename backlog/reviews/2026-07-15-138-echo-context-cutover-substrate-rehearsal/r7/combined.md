---
item_id: 2026-07-15-138-echo-context-cutover-substrate-rehearsal
round: 7
combined_at: '2026-07-16T05:39:19Z'
codex_response: codex.md
cursor_response: null
codex-ops_response: codex-ops.md
claude_response: null
patch_commit_sha: null
next_round: 8
combined_verdict: proceed_after_patches
escalated_to_founder: false
---

# Combined findings

Reframe gate: fired (broad-window rule — findings 1-6 and 8 target mechanisms introduced or last reshaped by prior-round `spec-r*-patches` commits 9c37bd8c/8d863930/677c585a/8de4ff00/777c6f49, well over the 2-finding threshold). Fresh-context investigator ran (codex exec, read-only, session 019f6971-2334-7002-a262-48cc045e97bd). Verdict: `kind: propagation_completion` — R7 exposes incomplete propagation of the founder-locked R6 mechanisms across phase, bootstrap, deployment, CAS, interval, and test owners; removal would break item 139's producer contract and local wording would preserve cross-section contradictions. Diagnostic check applied: every phase/action, resume, bootstrap, CAS, and manifest-handoff cell must have exactly one outcome, owner, commit point, recovery rule, and test — the r7 patches below add those tables rather than more scattered prose. Investigator recommendation adopted without override (matches founder instruction for this tick: accept all findings, fold independent-audit residuals, dispatch R8, no promotion/build). Coalescing per investigator: #1+#2 → one exhaustive transition/full-mode/rollback-ordering table; #3+#4 → creator/contender lock bootstrap + one named shared flock dependency; #5 → manifest→receipt→bind→execute/resume trusted composition; #6+#8 → one durable per-target CAS protocol with a named Darwin primitive or explicit narrowing; #7 → exact (C0,C1]. No new phase, no second lock, no alternate deployer, no replacement transaction mechanism.

Strategist independent-audit residuals folded into the same r7 patch set (founder-directed, beyond reviewer rows): (R-a) production controller producer surface — literal manifest-bound `execute`/`resume` entrypoints with exact argv, separately authenticated approval input, no npm/checkout/env fallback, same single `authority.lock`, `no ungated or bypass entrypoint` guarantee replacing `no live-capable mode`, execute/execute + execute/resume + resume/resume contention tests, item 138 still never invokes live adapters; (R-b) phase-aware resume re-proof matrix (source_fenced does not require a completed backup; rolled_back proves restored old authority without a zero-old-writer proof; generation-bound G2 rules retaining the G1 rolled_back projection until G2 prepared atomically replaces it); (R-c) trusted deployment composed as one crash-replayable transaction (pre-extraction bootstrap asset, payload publication, installed receipt, identity binding, evidence-dir provisioning; immutable hash/version payload subtree vs declared mutable metadata subtree); (R-d) lock/temp numeric pins (deadline/backoff/exit code) and strict interrupted-temp validation (uid, 0600, nlink=1, same-fs, bounded size, canonical schema, predecessor hash/generation/phase binding; invalid temp untouched + fail closed).

## Convergent findings

| # | Severity | Source | Where (primary) | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|

## Divergent findings (single-reviewer or non-overlapping primary `where`)

| # | Severity | Source | Where | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| 1 | HIGH | codex | AC1/AC2, packet lines 194-206 | accepted — patched (spec-r7-patches) | AC2's full-mode rule replaced by an exhaustive per-phase permit/reject table: absent/planned/recutover_prepared permit old-full start with no authorization; rolled_back permits only with the exact durable rollback authorization; source_fenced/backed_up/migrated/prepared/active reject. Rollback-authorization predicate defined; planned/recutover_prepared start-versus-transition race tests added. |
| 2 | HIGH | codex | AC1/AC7, packet lines 194 and 244-246 | accepted — patched (spec-r7-patches) | AC1's closed graph gains the active → rolled_back edge as an in-generation transition. Commit ordering defined: rollback interval exports, client restoration, and launchd service-control before-image restoration are durably complete before the rolled_back flip commits; old-full readiness is proven after the flip through the restored job. Full mode at rolled_back requires those restorations durable (they precede the flip by construction). Kill-plus-competing-start test immediately after the flip added. |
| 3 | HIGH | codex | AC1 authority-lock contract, packet line 196 | accepted — patched (spec-r7-patches) | Creator/contender bootstrap rule + sole-lock exemption: creating the empty `authority.lock` itself is the one permitted pre-acquisition root write (idempotent, O_CREAT non-exclusive so racers converge on one inode); the timeout zero-write guarantee is narrowed to exclude exactly that creation. Multiprocess creator-loser test added (creator loses the flock race, times out, exits with the pinned code having written nothing but the empty lock file). |
| 4 | MEDIUM | codex | AC1 authority-lock and descriptor-relative filesystem mechanism, packet line 196 | accepted — patched (spec-r7-patches) | The shared flock provider is named: pinned `fs-ext` dependency (exact version + sha512 integrity recorded identically in both package-lock.json files; npm `fs-ext`, upstream baudehlo/node-fs-ext; API surface pinned to `flock`/`flockSync(fd, 'exnb')`), prebuilt at candidate build for Darwin x64 against the pinned Node ABI and packaged as a prebuilt member. AC5's native-member rejection is reconciled with a sole allowlist: exactly that member at its recorded path + manifest hash. Test proves both packaged binaries load flock from that member and contend the identical inode. |
| 5 | HIGH | codex | AC2/AC5 installed identity and deployment entrypoints, packet lines 206 and 228 | accepted — patched (spec-r7-patches) | Trusted deployment recomposed: the candidate publishes a separate deterministic pre-extraction bootstrap asset with exact argv carrying authenticated manifest/approval/archive/prefix/authority-root; it verifies its own trust, archive hash, and member safety before staging, with no host Node/npm/checkout. Payload publication, installed receipt (exact schema, carries verified archive SHA-256/package identity/version), identity binding, and evidence-dir provisioning compose as one crash-replayable transaction with replay table + kill matrix; descriptor handoff to the fence defined; immutable payload in the hash/version subtree, host-specific identity in the declared mutable metadata subtree. Committed source owners named for both entrypoints. |
| 6 | HIGH | codex | AC4 client-transform transaction, packet lines 218-222 | accepted — patched (spec-r7-patches) | Concrete primitives named or narrowed: create-type targets use `open(2)` `O_CREAT\|O_EXCL` + `renamex_np(2)` `RENAME_EXCL` (real no-clobber Darwin primitives); replace-type targets are explicitly narrowed — Darwin has no compare-and-rename, so the never-overwrite promise covers foreign changes durable before the under-lock descriptor-pinned final validation, and the validate→rename window is minimized, not claimed prevented. Full journal/stage/commit ordering specified (same-dir exclusive temp, metadata apply, file fsync, rename, parent fsync, under-lock reconciliation) plus barrier-controlled foreign rename/chmod/byte races and commit-boundary kill tests. |
| 7 | MEDIUM | codex | AC3 versus AC7 rollback interval, packet lines 214 and 244 | accepted — patched (spec-r7-patches) | AC3 now uses exactly the half-open interval (C0,C1]; idempotency state, open deadlines, and checkpoints are cut at C1; replay imports no row or state above C1, with a boundary test asserting it. |
| 8 | MEDIUM | codex-ops | AC4 — per-target CAS transaction; Tests — client-adapters.test.ts | accepted — patched (spec-r7-patches) | Folded into the same durable CAS protocol as #6: in-place writes are forbidden, so a kill can never leave a controller-created third image at the target — the target is always exactly before-image or after-image; interrupted same-directory exclusive temps are reconciled under the lock (never classified as foreign drift). Kill tests around write/fsync/rename/parent-fsync for apply, rollback, and recutover added to client-adapters.test.ts. |

## Convergence call

needs R8 — focus_hints: verify the r7 propagation-completion patches (all eight r7 findings accepted plus founder-directed independent-audit residuals): exhaustive transition/full-mode/resume tables, active → rolled_back edge and rollback ordering, creator/contender lock bootstrap with named fs-ext dependency and sole native allowlist, trusted bootstrap deployment composition with installed receipt, production execute/resume entrypoints under approval binding, per-target metadata CAS transition table with named Darwin primitives or explicit narrowing, phase-aware resume matrix, and AC3 (C0,C1] alignment.

