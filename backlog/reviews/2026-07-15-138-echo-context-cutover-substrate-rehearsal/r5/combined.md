---
item_id: 2026-07-15-138-echo-context-cutover-substrate-rehearsal
round: 5
combined_at: '2026-07-16T04:06:16Z'
codex_response: codex.md
cursor_response: null
codex-ops_response: codex-ops.md
claude_response: null
patch_commit_sha: null
next_round: 6
combined_verdict: proceed_after_patches
escalated_to_founder: false
---

# Combined findings

Reframe gate: fired — all 8 findings target r4-patch-introduced mechanism (broad lookback window: 9c37bd8c spec-r2-patches, 8d863930 spec-r3-patches, 677c585a spec-r4-patches; every r5 `where` falls inside 677c585a's additions: authority.lock bootstrap, counterpart-manifest handoff, preprovisioned sink, pre-trust write-nothing absolute, candidate lifecycle, quiescence machinery). Fresh-context investigator (codex exec, read-only) returned `kind: structural_cut`: r5 exposes defects in r4-added overlays — counterpart handoff, selectable candidate outputs, ambient npm zero-write, durable fence sink — not missing original contracts; remove those surfaces and narrowly complete only the load-bearing authority serialization. Diagnostic check applied per row: a cut passes only if its state/behavior/owner disappear while AC8/item-139 still prove paired identity and AC1/AC2 still prove one authority; rows needing replacement machinery were treated as propagation completion instead. Strategist validated against the spec contract: item 139 pins identity solely from AC8's read-back landed SHAs (AC5 build-time cross-binding was redundant and cyclic — codex and codex-ops independently proved the first build cannot know the counterpart identity), while `authority.lock` is load-bearing (r4 reviewers required the fence serialization), so its bootstrap gap, root binding, and bounded waits are completed, not removed. Patches landed as 8de4ff00fa4f2db760a87fb856fbb5512f744a4b (spec-r5-patches). Investigator risk noted: if the founder requires cross-bound candidate manifests or contract-grade restart-visible fence evidence as trust artifacts, those cuts must be reverted to propagation completion — r6 reviewers should flag if so.

## Convergent findings

| # | Severity | Source | Where (primary) | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| 1 | HIGH | both (convergent on `AC5 — cross-repository identity handoff`) | AC5 — cross-repository identity handoff | accepted — mechanism dropped (structural cut) | 8de4ff00 — counterpart-manifest choreography removed instead of adding the orchestration command + counterpart object-database inputs both reviewers showed it needs. Manifests are now self-bound (own repo {SHA, tree} only, verified against own git objects; tampered-SHA/tree negative test); the authoritative cross-repo pairing is AC8's paired migration record + read-back landed SHAs, which item 139 alone pins. Removal matrix — state_removed: four-identity tuple in manifests, `--counterpart-manifest` input; behavior_removed: build-time counterpart verification, cross-manifest consistency check, swapped-counterpart test; owners_removed: build-workflow cross-repo ordering owner, Project_echo build's counterpart input surface; tests_removed_or_changed: four-identity assertion → self-bound tamper test (absence asserted); remaining_invariants: AC8 pairing (pre-existing, not compensating machinery) + self-bound manifest integrity. |

## Divergent findings (single-reviewer or non-overlapping primary `where`)

| # | Severity | Source | Where | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| 1 | MEDIUM | codex | AC1 — initialize/resume root-state contract | accepted — patched (propagation completion; treated as one with #2) | 8de4ff00 — single recognized lock-only bootstrap state added: a root whose only entry is a valid `authority.lock` (regular non-symlink file, descriptor-relative validation) resumes initialization under that lock; kill tests at every bootstrap boundary (before lock, lock-to-first-record window, each record replacement). |
| 2 | HIGH | codex-ops | AC1 — initialization/resume grammar and authority-record commit | accepted — patched (treated as one with #1) | 8de4ff00 — same lock-only bootstrap state; every canonical record commit is temp-write/fsync/atomic-rename/parent-fsync durable and performed while `authority.lock` is held. |
| 3 | HIGH | codex-ops | AC2 — shared authority-root binding | accepted — patched (propagation completion) | 8de4ff00 — authority-root identity bound at package-install time into an immutable installed identity record (same install step that provisions the log location; fixture installer in rehearsal); fence resolves the root only from that record and rejects missing/malformed record, mismatched or aliased root, and any env/flag override before any PID/data-dir/SQLite/socket mutation; cross-process alternate-root fixture required. |
| 4 | MEDIUM | codex-ops | AC5 — candidate output and verification lifecycle | accepted — simplified within the AC5 cut | 8de4ff00 — fresh per-run staging directory + atomic manifest/archive publication as the final step + failed/interrupted build leaves no publishable manifest, folded into the rewritten AC5. The descriptor-validated no-follow staging/quarantine/concurrent-invocation machinery was not adopted: the surface shrank with the counterpart cut, and build commands remain artifact-output-only rehearsal tools, not authority-bearing paths. |
| 5 | MEDIUM | codex-ops | AC1 — advertised npm rehearsal command | accepted — promise narrowed | 8de4ff00 — zero-write promise scoped to controller-owned writes (npm/Node launcher cache/log behavior is outside the controller's authority, as the reviewer proved it must be); sentinel temp/cache/log matrix asserts absence of controller-created entries around the advertised `npm run rehearse` invocation itself. |
| 6 | MEDIUM | codex-ops | AC1/AC2 — bounded authority and quiescence operations | accepted — patched (propagation completion) | 8de4ff00 — every lock acquisition, service-control op, process stop, and zero-writer verification is deadline-bounded; timeout/stuck/unkillable → no phase commit, safe lock release, redacted durable failure record, non-zero exit; stuck-lock, failing-fake-launchd, unkillable-process fixtures. |
| 7 | MEDIUM | codex-ops | AC2 — preprovisioned fence-evidence sink | accepted — mechanism dropped (limiter contract cut; evidence demoted to best-effort) | 8de4ff00 — rejection is unconditional and fail-closed regardless of sink state (missing/unwritable/full sink still rejects with zero startup mutation); relaunch bound enforced solely by start-job neutralization. Removal matrix — state_removed: bounded-retention/coalescing limiter state and sink-health contract; behavior_removed: sink-enforced cross-process relaunch bounding, evidence as contract-bearing durability; owners_removed: sink module's limiter role; tests_removed_or_changed: retention/coalescing assertions → sink-failure variants asserting rejection + zero mutation (absence), evidence best-effort-when-writable; remaining_invariants: unconditional fail-closed rejection + neutralization bound (both pre-existing r4 requirements, not new compensating machinery). |

## Convergence call

needs R6 — focus_hints: verify the r5 structural-cut + completion patches at 8de4ff00fa4f2db760a87fb856fbb5512f744a4b: (1) AC5 self-bound manifests — no counterpart argument/checkout/tuple remains anywhere; own-git-objects verification + tampered-SHA/tree negative test; AC8 read-back landed SHAs stay the sole cross-repo identity authority for item 139; fresh per-run staging + atomic publish + no publishable manifest on failure. (2) AC1 lock-only bootstrap state — exactly one admitted shape, validated descriptor-relative; durable record commits while lock held; bootstrap-boundary kill matrix. (3) AC2 installed authority-root identity record — fence rejects missing/mismatched/aliased/override roots pre-mutation; cross-process alternate-root fixture. (4) Bounded fail-closed waits — no phase commit on timeout, safe lock release, durable failure record. (5) Fence evidence best-effort — rejection unconditional under sink failure, relaunch bound by neutralization alone. (6) AC1 zero-write promise scoped to controller-owned writes around the advertised npm invocation. Check the cuts introduce no new unnamed mechanism, no second lock, no revived counterpart choreography or sink-health contract.

