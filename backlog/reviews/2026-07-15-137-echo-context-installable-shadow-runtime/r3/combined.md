---
item_id: 2026-07-15-137-echo-context-installable-shadow-runtime
round: 3
combined_at: '2026-07-16T03:10:59Z'
codex_response: codex.md
cursor_response: null
codex-ops_response: codex-ops.md
claude_response: null
patch_commit_sha: 65b9a0af5a4ab21a34ad71d6258c8e231427a180
next_round: 4
combined_verdict: divergent
escalated_to_founder: true
---

# Combined findings

**Divergent verdicts** — codex='pushback', codex-ops='proceed_after_patches' cross the `{proceed*, pushback}` boundary; founder escalation per §Out of Scope #7.

**Founder resolution (2026-07-15, verbatim "yes keep and let them resume"):** keep the current architecture for items 137 and 138; accept ALL r3 findings as required spec patches; continue review only, no build. `combined_verdict: divergent` and `escalated_to_founder: true` above remain unchanged as historical facts; this disposition executes the founder's delegated resolution.

Reframe gate: TRIGGERED — all 13 findings target mechanisms introduced/completed by the r2 patch commit `8e73045f` (≥2 threshold met). Fresh-context codex investigator (read-only, this tick) returned **kind: propagation_completion** — the r2 patch added founder-confirmed safety mechanisms as localized prose without carrying them through ownership, schemas, lifecycle atomicity, diagnostics, and tests; removal would reopen the original defects, and local text edits would leave cross-AC contradictions. Diagnostic check applied per the investigator: every patched row fills a missing owner/primitive/source-of-truth cell for an already-required mechanism (consolidated contracts: rows 1+9 release FSM, 2 bootstrap, 3+7+8 writer lock, 4+11 bounded logs, 5+10+12 layout resolver/candidate isolation, 6+13 architecture truth); no alternative mechanism was added, the authority schema stays exactly prepared|active|rolled_back, the writer lock stays distinct from item 138 cutover authority/execution coordination, and no residual deployer or cutover phase enters item 137. No disposition uses removal language, so the removal proof matrix does not fire.


## Convergent findings

| # | Severity | Source | Where (primary) | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|

## Divergent findings (single-reviewer or non-overlapping primary `where`)

| # | Severity | Source | Where | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| 1 | HIGH | codex | AC4 release ownership and AC7 founder execute checkpoint | accepted — patched (founder-delegated) | 65b9a0af — builder authority ends at candidate-only handoff; phase two is founder-owned tools/release-runtime.mjs stage\|resume over a durable FSM (sealed→built→checkpointed→smoked→approved→published→installed→recorded); stage builds once into owned staging, checkpoints exact hashes, re-runs the smoke on release bytes, then PAUSES for a separate release approval bound to (landed source SHA, version, runtime artifact SHA-256, bootstrap hash, asset-set manifest hash); merge approval never equals release approval; a second release build for the same SHA+version is a hard refusal. |
| 2 | HIGH | codex | AC4, AC5, and AC7 installer bootstrap | accepted — patched (founder-delegated) | 65b9a0af — one independently published POSIX-sh bootstrap outside the runtime tgz (echo-context-bootstrap-0.1.0-dev.137.1.sh), size/hash/mode/version bound in the manifest and approval tuple; exact repo-free invocation added to AC5; verifies its own approved hash, host arch/Rosetta, runtime checksum/manifest/SBOM, and complete member path/type/mode/link inventory BEFORE extraction; bundled absolute Node probe + bundled CLI only after staged extraction; no host Node, checkout, npm, compiler, or alternate artifact; explicitly not item 138's residual deployer. |
| 3 | HIGH | codex | AC1 single-writer lease and tests/runtime/composition.test.ts | accepted — patched (founder-delegated) | 65b9a0af — exclusion is the kernel primitive, not metadata: flock(2) LOCK_EX\|LOCK_NB on the 0600 regular-file <home>/writer.lock (per-layout, resolver-derived), held process-lifetime from before SQLite open until after close, kernel-released on any exit incl. SIGKILL; owner metadata diagnostic-only, atomically durable, replaceable only after acquiring the lock; new tests/runtime/writer-lock.test.ts runs barrier multiprocess, simultaneous stale reclaim, holder-exit, PID-reuse, and SIGKILL tests proving exactly one storage opener. |
| 4 | HIGH | codex | AC5 bounded logs and AC6 log-path diagnosis | accepted — patched (founder-delegated) | 65b9a0af — install/start-only truncation replaced by an always-on bundled supervisor sink; exact numeric caps (5 MiB per chunk, ≤4 chunks per stream, 64 MiB aggregate, oldest-chunk deletion on overflow); launchd StandardOut/ErrorPath cannot bypass (supervisor last-resort files truncated per spawn and counted in the cap); 0700 directory, 0600 regular non-link files, ownership checks; real-launchd crash-loop/long-output tests exceed caps with no CLI reinvocation; doctor independently recomputes effective caps from on-disk chunk inventory. |
| 5 | HIGH | codex | AC3, AC5, and AC7 clean-home isolation | accepted — patched (founder-delegated) | 65b9a0af — src/install/layout.ts is the single closed resolver for support/state/log/release/shim/plist roots, label, port, and launchd GUI domain, precedence flag > config > shadow default, no ECHO/HOME fallback; candidate mode puts all mutable paths under one disposable root, rejects founder/live paths and canonical labels/ports, uses run-unique com.echo.context.candidate.<run-id> label and a collision-reserved port; every install/launchctl action routes through the resolver; tests/install/layout.test.ts added. |
| 6 | MEDIUM | codex | AC5 architecture preflight, AC6 doctor output, and related tests | accepted — patched (founder-delegated) | 65b9a0af — named bounded probes with typed exit mappings: sysctlbyname hw.optional.arm64 + uname -m (physical host), sysctl.proc_translated (translation state), 5-second arch -x86_64 /usr/bin/true (Rosetta availability), staged-node Mach-O header (bundled binary arch), 10-second bundled-node -p process.arch (live probe); stable doctor fields physical_host_architecture / bundled_runtime_architecture / translation_required / translation_available / process_translated / architecture_verdict; mutation-sentinel tests prove pre-extraction failure leaves no state and bundled-node failure precedes plist/service creation. |
| 7 | MEDIUM | codex | AC1 and AC6 lease/doctor truth | accepted — patched (founder-delegated) | 65b9a0af — AC6 gains the six-state writer-lock truth table (expected launchd-correlated holder healthy; refused duplicate starter leaves incumbent healthy with refusal record surfaced; live foreign holder unhealthy; stale metadata + unlocked file reclaimable; running-without-lock fails; stopped-with-live-holder fails); the durable refusal record is named (<home>/writer.last-refusal.json), bounded ≤4096 bytes, 0600, versioned schema, atomically replaced on refusal and atomically cleared on next successful acquisition. |
| 8 | HIGH | codex-ops | AC1 — single-writer lease and AC6 doctor evidence | accepted — patched (founder-delegated) | 65b9a0af — same kernel-lock contract as row 3: process-lifetime atomic flock(2) exclusion; crash-safe atomic metadata replacement permitted only after lock acquisition (check-then-reclaim race is structurally impossible — the loser's flock fails regardless of metadata); bounded owned last-refusal record gives the losing process durable doctor-inspectable evidence; barrier-based concurrent, PID-reuse, and stale-reclaim tests prove exactly one storage opener. |
| 9 | HIGH | codex-ops | AC4 phase two and AC7 private-release/install sequence | accepted — patched (founder-delegated) | 65b9a0af — build-exactly-once is now resumable: durable FSM journal keyed by landed SHA + version in the founder-owned staging root with atomic phase checkpoints of tgz/manifest/checksum/SBOM/bootstrap hashes; resume verifies and reuses the exact staged and partially uploaded bytes — matching remote assets no-op, missing assets upload from staging, mismatch/tamper refuses; never rebuilds, retags, or replaces; no reliance on the ephemeral detached clone after checkpointing. |
| 10 | HIGH | codex-ops | AC7 — repo-free clean-home lifecycle and timeout cleanup | accepted — patched (founder-delegated) | 65b9a0af — smoke runs in candidate mode with a run-unique label and collision-reserved port (never 39478/38478/38479), so the launchd namespace and TCP port are isolated, not just filesystem prefixes; cleanup is an unconditional finally path (bootout run-unique label, kill survivors, prove reserved listener gone, ownership-fenced deletion under the disposable root only); bounded redacted failure evidence persists before cleanup; before/after sentinels prove canonical com.echo.context labels, ports 39478/38478/38479, and real Application Support/secret roots untouched. |
| 11 | MEDIUM | codex-ops | AC5–AC6 — launchd lifecycle and bounded logs | accepted — patched (founder-delegated) | 65b9a0af — plist pins RunAtLoad true, KeepAlive true, ThrottleInterval 10 seconds (no silent down-state or unthrottled hot loop); logging via the lifetime-compatible bundled supervisor sink of row 4 with exact 0700/0600 modes and pre-created installer-owned paths; real-launchd long-running-output and repeated-crash tests; doctor independently verifies effective caps. |
| 12 | MEDIUM | codex-ops | AC3 and Tests — configuration-derived support-root isolation | accepted — patched (founder-delegated) | 65b9a0af — AC3 resolution now explicitly routes through the AC5 resolver (same code path in candidate and real installs); a deny-write or before/after audit proves every candidate write stays beneath the disposable prefixes and the real Application Support and secret roots remain byte-for-byte unchanged; resolver tests for temporary and founder paths added in tests/install/layout.test.ts. |
| 13 | MEDIUM | codex-ops | AC5–AC6 and Tests — architecture/Rosetta preflight ordering | accepted — patched (founder-delegated) | 65b9a0af — mutation-sentinel architecture matrix added to Tests: incompatible host or unavailable Rosetta fails before extraction leaving no extracted/config/secret/plist/launchd-job state; a failing bundled-Node architecture probe occurs after staged extraction but before any plist, service, config, or secret mutation; doctor fields distinguish Intel-native, Apple-Silicon-translated, and translation-missing states. |

## Convergence call

needs R4 — founder-delegated disposition (2026-07-15 "yes keep and let them resume": keep the architecture, accept all 13 r3 findings as required spec patches, continue review only, no build) accepts every finding as propagation completion in `65b9a0af`; R4 verifies the six completed contracts — founder-owned release FSM with tuple-bound approval and rebuild-free resume, published bootstrap asset, flock(2) writer lock with refusal record and truth table, always-on bounded log sink with launchd pins, single layout resolver with candidate-mode label/port isolation and finally cleanup, and named architecture probes with mutation-sentinel ordering — and that no patch added mechanism beyond these completions.

