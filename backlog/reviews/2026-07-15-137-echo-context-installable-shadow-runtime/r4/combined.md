---
item_id: 2026-07-15-137-echo-context-installable-shadow-runtime
round: 4
combined_at: '2026-07-16T05:38:19Z'
codex_response: codex.md
cursor_response: null
codex-ops_response: codex-ops.md
claude_response: null
patch_commit_sha: null
next_round: null
combined_verdict: proceed_after_patches
escalated_to_founder: false
---

# Combined findings

Reframe gate: TRIGGERED — all 13 findings target mechanisms introduced by the prior patch commits `8e73045f` (r2) and `65b9a0af` (r3) (≥2 threshold met). Fresh-context codex investigator (read-only, this tick) returned **kind: structural_cut** scoped to the launchd last-resort file mechanism (rows 5+12) with the remaining 11 rows dispositioned as propagation completion: "Rows 5 and 12 target the r3-added launchd fallback-file truncation/counting mechanism, not the original bounded-logging invariant. Open launchd descriptors make deeper truncation rules the same flawed seam; /dev/null removes it while preserving the internal bounded sink." Diagnostic check applied before patching: the cut passes only if fallback-file state, behavior, owners, accounting, and tests are removed, both plist output paths target /dev/null, no replacement fallback stream exists, and real-launchd tests prove only the rotating sink grows while a separate bounded last-exit record remains durable — the r4 patch satisfies each clause (matrix below). Founder direction this round: accept every reviewer finding, incorporate two independent-audit residuals that no single row names in full — (i) the exact owner-sidecar contract and (ii) a durable failure-evidence destination outside the disposable root — patch spec/task-state only, no promotion/build/claim, dispatch r5 to codex+codex-ops.

Removal proof matrix (rows 5+12 — launchd last-resort stdout/stderr files):
- state_removed: the two supervisor last-resort regular files under /Users/zhenye/Library/Logs/echo-context/ and their byte-count entries in the 64 MiB aggregate accounting; no fallback log file exists on disk.
- behavior_removed: spawn-time truncation of those files, counting them inside the aggregate cap, and any launchd-descriptor-fed retained-file growth; StandardOutPath/StandardErrorPath now point at /dev/null, non-growing by construction.
- owners_removed: the supervisor's truncation/counting responsibility for launchd-owned descriptors and doctor's ownership/mode checks over the last-resort files (replaced by absence checks plus last-exit-record verification).
- tests_removed_or_changed: tests/install/launchd.test.ts last-resort truncation/counting assertions deleted; replaced by absence assertions plus sustained-supervisor-output, sink-failure, and pre-sink crash-loop tests under real launchd.
- remaining_invariants: exactly one bounded rotating internal sink (5 MiB/chunk, ≤4 chunks/stream, 64 MiB aggregate) plus a separate bounded durable 0600 last-exit record surfaced by doctor. The last-exit record is a new bounded compensating diagnostic — founder-directed and investigator-endorsed as "narrow mechanism drop plus separate last-exit propagation" — that records exit metadata only; it does not preserve the removed stream-capture behavior under a new name.

## Convergent findings

| # | Severity | Source | Where (primary) | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|

## Divergent findings (single-reviewer or non-overlapping primary `where`)

| # | Severity | Source | Where | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| 1 | HIGH | codex | AC1 writer lock / AC5 supervisor / AC6 doctor truth table | accepted — patched | spec-r4-patches (this tick) — group 4: explicit supervisor/runtime split; launchd owns the supervisor, the supervisor spawns and verifies exactly one direct runtime child (executable realpath under the owned release, matching artifact hash), and that child alone binds the listener, opens SQLite, and holds writer.lock; status/doctor gain distinct supervisor_pid/runtime_pid identities with start times, executable realpaths, and artifact hashes; AC6 truth table re-keyed to the verified child of the launchd-reported supervisor as the expected lock holder. |
| 2 | HIGH | codex | AC5 layout resolver and bootstrap installation sequence | accepted — patched | spec-r4-patches (this tick) — group 3: the bootstrap verifies and extracts only into an explicit self-created 0700 safe staging directory and makes no other path decision; the bundled CLI, through the single src/install/layout.ts resolver, owns the sole final atomic install into the resolved immutable release root; literal closed candidate argv (--candidate-root plus run-unique --label/--port) covers every root/label/port/domain/bootstrap write, with deny-write tests. |
| 3 | HIGH | codex | AC4 release FSM approval and durable resume contract | accepted — patched | spec-r4-patches (this tick) — group 1: exact release-journal.v1 contract (0600 file in 0700 staging dir, atomic temp+rename with file and parent-directory fsync, write-ahead intent records, monotonic compare-and-swap transitions); the approval ingress is exclusively `resume --approve <approval-file>` — an independently created owner-checked 0600 file binding the exact tuple plus operator, timestamp, and single-use nonce; direct journal mutation and merge metadata never authorize; every crash window resolves to verify-and-adopt or hard refusal, never a second build. |
| 4 | HIGH | codex | AC4 asset-set manifest / AC5 exact bootstrap invocation | accepted — patched | spec-r4-patches (this tick) — group 2: non-recursive canonical release-set manifest/digest (sorted per-asset name/size/SHA-256 records covering every published asset and never itself); the founder-owned repo-free runner verifies the downloaded bootstrap and release-set manifest against the approval-bound hashes checkpointed in the release journal — a trust anchor external to all downloaded assets — before /bin/sh runs; coherent whole-set substitution tamper test added. |
| 5 | MEDIUM | codex | AC5 launchd StandardOutPath/StandardErrorPath retention | accepted — mechanism dropped | spec-r4-patches (this tick) — group 6 structural cut per the removal proof matrix above: StandardOut/ErrorPath route to /dev/null; last-resort files, spawn-time truncation, and aggregate counting removed entirely; diagnostics = one bounded rotating internal sink + separate bounded durable last-exit record; sustained-supervisor-output, sink-failure, pre-sink crash-loop, absence, and cap-invariant tests. |
| 6 | MEDIUM | codex | AC5 candidate port reservation / AC7 parallel cleanup proof | accepted — patched | spec-r4-patches (this tick) — group 3: reservation-to-runtime handoff defined as a bounded close-bind verification protocol — close the reservation, start the job, require the runtime child to bind within a fixed deadline; any intervening claimant is a typed port-stolen failure that triggers unconditional cleanup without touching canonical state; competing-binder test added. |
| 7 | HIGH | codex-ops | AC4 release-FSM paragraph; tests/install/release-fsm.test.ts | accepted — patched | spec-r4-patches (this tick) — group 1 (same contract as row 3): one nonblocking kernel lock (flock LOCK_EX\|LOCK_NB on a per-landed-SHA+version fsm.lock) held from before journal read across build, upload, install, record, and their checkpoints; write-ahead intent states and CAS transitions close every side-effect/checkpoint crash window via verify-and-adopt or hard refusal; concurrent stage/stage, stage/resume, resume/resume barrier tests plus kill-at-every-boundary tests prove exactly one build and one mutator. |
| 8 | HIGH | codex-ops | AC4 paragraph beginning Ownership is two-phase and explicit | accepted — patched | spec-r4-patches (this tick) — group 1: explicit post-smoke approval input (`resume --approve` with an independently created owner-checked 0600 approval file binding exact tuple, operator, timestamp, single-use nonce consumed by the journal); journal edits and merge metadata never authorize progression; absent, malformed, stale, replayed, pre-smoke, and wrong-tuple approval negatives specified in tests/install/release-fsm.test.ts. |
| 9 | HIGH | codex-ops | AC4 asset-set contract; AC5 repo-free bootstrap invocation; tests/install/bootstrap.test.ts | accepted — patched | spec-r4-patches (this tick) — group 2: trusted verification precedes execution — the runner verifies bootstrap+release-set manifest against journal-checkpointed approved hashes, then executes the verified protected staged bytes (0700 copy or already-open descriptor, defeating verify/use swaps); pinned absolute system tools/safe PATH, umask 077, environment allowlist stripping NODE_OPTIONS/DYLD_*; whole-set substitution, swap-race, hostile-PATH/env, and missing-utility tests; final cache acceptance covers the exact bootstrap+release-set-manifest+tgz+runtime-manifest+checksum+SBOM set with all hashes/sizes/modes and no extras. |
| 10 | HIGH | codex-ops | AC1 shutdown and writer lock; AC5 launchd supervisor; AC6 writer-lock truth table; AC7 lifecycle tests | accepted — patched | spec-r4-patches (this tick) — group 4 (same contract as row 1) plus lifecycle semantics: parent-death detection in the child, one-child and sole-restart authority (launchd restarts only the supervisor, the supervisor restarts only its child), signal forwarding, bounded TERM-to-KILL escalation, reaping, close-on-exec lock descriptor, exact start/stop/restart/disable launchctl sequences; real-launchd tests kill or hang either process and prove child, listener, and lock convergence before restart. |
| 11 | HIGH | codex-ops | AC5 install idempotence and extraction; AC7 unconditional cleanup; tests/integration/shadow-install.test.ts | accepted — patched | spec-r4-patches (this tick) — group 7: install is a crash transaction — durable 0600 install-intent record inside a fresh 0700 owned transaction directory before the first real-path mutation, checkpointed provisional ownership manifest, atomic release/config/plist commit, no launchd load/start before the ownership-manifest commit, and adopt/rollback/refuse semantics on rerun; kill-at-every-boundary tests prove no untracked bytes, no loaded job without a committed manifest, and no real-path mutation that manifest-driven uninstall cannot remove. |
| 12 | MEDIUM | codex-ops | AC5 bounded supervisor logs; AC6 doctor; tests/install/launchd.test.ts | accepted — mechanism dropped + last-exit propagation | spec-r4-patches (this tick) — group 6 structural cut (same as row 5) plus the separate bounded durable last-exit record: 0600 versioned-schema file ≤4096 bytes atomically replaced by the supervisor on every child exit (status/signal, timestamps, restart count) and surfaced by doctor even under sink failure or pre-sink crash; supervisor-output, sink-failure, and pre-sink crash-loop tests under real launchd without CLI reinvocation. |
| 13 | MEDIUM | codex-ops | AC6 status and doctor; tests/cli/doctor.test.ts | accepted — patched | spec-r4-patches (this tick) — group 9: stable exit-code enum (0 healthy, 1 unhealthy, 2 usage error, 3 not installed, 4 probe timeout, 5 internal error); exactly one schema-valid JSON document on stdout for every operational outcome; credential-free diagnostics on stderr only; bounded probes; subprocess tests assert exact stdout bytes and exit status. |

Independent-audit residuals folded into the same spec-r4-patches commit (no single reviewer row names them in full): group 5 — exact owner-sidecar contract (<home>/writer.owner.json, versioned writer-owner.v1 schema, ≤4096 bytes, 0600 current-user regular non-link file, atomic temp+rename with file and parent-directory fsync only while the kernel lock is held, fields including lock device/inode; loser never writes, normal close clears, crash-stale replaced only after acquisition, boundary/corruption tests); group 8 — durable failure evidence written to a caller-supplied destination outside the disposable root (versioned redacted bounded schema, 0700 directory / 0600 regular files, atomic+fsync, survives cleanup, no secret/path leak).

## Convergence call

needs R5 — focus_hints: verify the nine r4 patch groups (release-FSM lock/journal/intent/CAS/approval ingress; asset-set trust anchor + hardened invocation environment + full-set cache acceptance; bootstrap staging + CLI-owned final install + closed candidate argv + close-bind port handoff; supervisor/runtime split with lifecycle semantics; owner-sidecar exact contract; /dev/null structural cut + last-exit record; install crash transaction; durable failure-evidence destination; status/doctor exit-code and stdout-purity contract) and confirm no alternative mechanism beyond the founder-directed cut was added, the authority schema stays exactly prepared|active|rolled_back, no source_fenced or item-138 deployer/controller scope entered, and candidate labels/ports never touch com.echo.context or 39478/38478/38479.

