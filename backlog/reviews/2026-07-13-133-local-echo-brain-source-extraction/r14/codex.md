---
item_id: "2026-07-13-133-local-echo-brain-source-extraction"
round: 14
reviewer: "codex"
artifact_sha: "58870d8c6dca1ed230cd3af8f9262cd36bc1087c"
completed_at: '2026-07-14T02:51:42Z'
verdict: "pushback"
findings:
  - severity: "high"
    where: "AC5 — bootstrap publisher"
    finding: "The Node-core-only `bootstrap-publisher.mjs` must perform descriptor-relative `openat` operations, but Node 22 core exposes no dirfd-relative open/link/unlink API; the native publisher is compiled only afterward. The spec also requires every command to run through `process-watch` without defining how that watcher, bootstrap process, or clang child exists before supervision. Define an implementable, hash-pinned root-of-trust bootstrap and explicit initial supervision boundary."
  - severity: "high"
    where: "AC5 — publisher state machine; AC8 — final seal"
    finding: "The publisher fsyncs file bytes before `fchmod(0400)` and never fsyncs the inode after the mode change, so a crash can preserve a linked final file without its read-only mode. The final root `0500` transition likewise lacks a durability barrier. Require `fchmod` before the final file fsync, fsync after the root chmod, and test the relevant crash boundaries."
  - severity: "high"
    where: "AC5 — process-watch quiescence"
    finding: "Periodic `KERN_PROC_ALL` polling plus point-in-time `lsof` snapshots cannot prove all descendants exited: a child can double-fork or create a new session between polls, remain resource-silent during the clean scan, then write later. Cleanup can also kill an observed survivor and still permit acceptance. Require enforceable containment or complete lifecycle observation, make any post-leader survivor/writer/listener a sticky failure, and add a delayed-write escaped-child fixture."
  - severity: "medium"
    where: "AC5 and AC7 — verification-plan namespace roster"
    finding: "The claimed exact roster is not enumerated by stable ID, order, kind, repetition ID, and output owner. `fetch` has no unambiguous kind, the required operator test suites have no legal row, and AC8 invokes undefined reviewer namespaces while validation rejects extra namespaces and rows. Add a normative builder-and-reviewer roster table and schedule every required test explicitly."
  - severity: "medium"
    where: "AC7 — template token vocabulary"
    finding: "The complete token vocabulary has no target-commit-time token, yet baseline, hostile, and rebuild must render `SOURCE_DATE_EPOCH=<target-commit-time>`. The committed plan cannot contain that value unless commit time is predetermined by an unstated protocol. Add a captured token derived from the verified target commit or prescribe and verify a fixed commit timestamp before plan creation."
  - severity: "high"
    where: "AC3 and AC7 — raw-source audit and parity dispositions"
    finding: "No deterministic closure algorithm or approved disposition map is specified. A builder can omit or mark production files `excluded`, replace them with authored files, or rewrite whole blobs while the audit faithfully validates the submitted manifests and replay. Pin roots and traversal rules plus exact exclusion/rewrite allowlists, forbid authored replacements for excluded behavior, and add omission and disposition-evasion fixtures."
  - severity: "high"
    where: "AC8 — Project_echo handoff and attempt-seal ordering"
    finding: "The Project_echo migration record is committed before the sole push but must contain the final attempt-seal digest. That seal covers every descendant, including the later push intent, streams, result, and quiescence evidence, and the record may not be edited afterward. This is circular and unbuildable; separate pre-push and post-push sealed envelopes or authorize a second exactly bound handoff commit."
  - severity: "medium"
    where: "AC8 — independent-review shared-target checks"
    finding: "Reviewer evidence binds HEAD and tree but does not require the complete AC1 shared-target cleanliness check. The same HEAD/tree can coexist with untracked or ignored files, extra filesystem nodes, altered refs/remotes/config, or a dirty index. Require each reviewer to rerun and bind the minimal-environment status, no-follow walk, refs/remotes/alternates/promisor, and fsck checks before and after private verification."
  - severity: "medium"
    where: "AC8 — exact Project_echo SHA/ref handoff"
    finding: "The destination remains unconstrained as `<full-ref>`, and comparing trees does not prove that the handoff commit has the expected parent. Derive the literal full ref from the populated claim branch, capture expected parent from the clean baseline HEAD, require exactly one matching parent, and bind endpoint hash, commit, tree, and full ref in post-commit handoff evidence."
  - severity: "medium"
    where: "AC8 — request-bound reviewer evidence"
    finding: "Create-once reviewer metadata binds selected request scalars but not the canonical request path and bytes, requested-reviewer roster, or evidence linkage from the eventual response. Bind the request SHA-256, exact `spec_commit_sha`, roster and membership, then require the response to identify its reviewer root and final evidence-seal digest; reject reuse across request hash, round, or reviewer."
---
