---
item_id: "2026-07-13-133-local-echo-brain-source-extraction"
round: 11
reviewer: "codex"
artifact_sha: "b6095d0265b6a6fce2386cd20d98e9965a65359d"
completed_at: '2026-07-14T01:19:46Z'
verdict: "proceed_after_patches"
findings:
  - severity: "high"
    where: "AC3 and AC7 provenance audit"
    finding: "Provenance dispositions are not enforceable: `copied` and `relocated` never require destination bytes to equal source bytes, while `rewritten` depends on an undefined `source-plan row`. Define the committed plan path/schema, exact from/to literals and occurrence counts, and enforce disposition-specific path/hash and deterministic-transform invariants in both `check:provenance` and the operator audit."
  - severity: "medium"
    where: "AC3 provenance universe"
    finding: "`every newly created target file` includes mutable `.git/**` and transient files, whereas AC7 derives the audited universe from committed target HEAD. Define the destination universe as regular file blobs in committed HEAD, explicitly exclude Git metadata and untracked install/build outputs, and require the independent audit to enumerate that identical universe."
  - severity: "high"
    where: "AC5 push lifecycle and AC8 failed-stop logging"
    finding: "The post-handoff failure path is inconsistent: AC5 freezes the handoff commit/ref before push and permits reconciliation to retry only that exact commit, but AC8 requires every failed stop, including a failed or ambiguous push, to append state to Project_echo agent-run and agent-notes files. Define a lifecycle that durably retains those post-commit failures without silently creating an unpublished commit, either by keeping them receipt/evidence-root-only or by specifying an explicit pre-acceptance rebind and new-commit protocol."
  - severity: "high"
    where: "AC5 receipt writer handoff and remote-ref reconciliation"
    finding: "Expected-generation temp-write-and-rename is crash-atomic but not a compare-and-swap: an unfenced builder and reconciler can both derive generation N+1 and overwrite monotonic state. Require proven builder quiescence plus a write-once ownership epoch, or a serialized CAS/lock that rejects stale generation/writer; also bind the remote identity in the receipt and use a create-only or expected-OID push so an existing ref or changed remote cannot be mutated before the equality check."
  - severity: "medium"
    where: "AC5 receipt evidence model and Tests"
    finding: "The receipt specifies only a current command intent/result, so successive updates do not retain a hash-bound history of argv, cwd, environment/profile, exit, and logs needed by AC8's exact-command record and independent review. Define immutable per-command records or a monotonic command ledger bound from the receipt, plus a concrete schema/transition validator and failure-injection tests for stale generations, skipped phases, write-once fields, and phase/acceptance coupling."
  - severity: "medium"
    where: "files_to_modify; AC1, AC5, and AC7 path containment"
    finding: "AC1 authorizes creation of missing `.echo-migration-evidence` and `133` parents that are not themselves covered by `.../133/**`, and verifier clone/output roots are only described as unique absent roots. Add the parent paths to `files_to_modify` or make them fail-closed preconditions, bind every scratch/clone/output root beneath the attempt root, and name the retained parent-chain record and exact no-follow component, owner/mode, device/inode, and post-mkdir comparison procedure."
  - severity: "medium"
    where: "AC6; Risks behavior-drift mitigation; Tests test-parity entry"
    finding: "The Risks and Tests sections still call the eight-file proof `transformed-hash parity`, contradicting AC6's copied, same-path, byte-identical contract. Replace both residual phrases with source-blob/content-hash equality and byte-comparison language so builder and reviewer apply one parity rule."
  - severity: "medium"
    where: "AC7 verification phases"
    finding: "Only the two `npm ci` commands are exact; the remaining dependency, boundary, provenance, parity, typecheck, lint, product/e2e, artifact-install, smoke/config/selftest, source-independence, probe, and audit steps lack canonical argv, cwd, sandbox profile, and output bindings. Add an ordered command matrix or one retained runner that invokes the absolute npm CLI with explicit offline cache/config and hook controls for every script and defines baseline-versus-hostile probe fixtures and expected denial evidence."
  - severity: "medium"
    where: "AC8 independent artifact review"
    finding: "Independent review rebuilds and compares an artifact without defining success as byte-identical SHA-256 or as equality under a specified normalized manifest. Bind the rebuilt artifact to target HEAD/tree and lockfile with one explicit comparison rule and rerunnable command; otherwise a materially different rebuild can satisfy the prose comparison."
---
