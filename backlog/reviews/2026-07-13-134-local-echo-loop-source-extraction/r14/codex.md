---
item_id: "2026-07-13-134-local-echo-loop-source-extraction"
round: 14
reviewer: "codex"
artifact_sha: "58870d8c6dca1ed230cd3af8f9262cd36bc1087c"
completed_at: '2026-07-14T02:58:40Z'
verdict: "proceed_after_patches"
findings:
  - severity: "medium"
    where: "AC2 — source-universe bootstrap"
    finding: "The seed command combines `--literal-pathspecs` with `tsconfig.*.json`, so Git treats that token literally and omits variant configs. Workspace-pattern manifests outside the seed roots likewise require enumeration before exact lookup. Specify a pinned-tree enumerate-then-filter algorithm, canonical ordering and binding-context serialization, ambiguity/cycle failures, and a fixture whose alias is discoverable only through a variant tsconfig or workspace manifest."
  - severity: "medium"
    where: "AC2 — final-HEAD dependency reconciliation"
    finding: "Requiring every import and dynamic read to map to a dependency-plan row incorrectly includes repository-local edges, while dependency-plan rows are limited to npm-managed and toolchain-managed dependencies. Partition local edges to exactly one committed target blob and inventory row, npm imports and CLIs to npm rows, and interpreters or helpers to toolchain rows; require scanning captured HEAD blobs rather than mutable checkout bytes."
  - severity: "high"
    where: "AC3 — emitted-event and invocation uniqueness"
    finding: "The public `emitCoordEvent` API can apparently emit `role.invoked`, making generic uniqueness `(actor,kind,correlationId)` conflict with invocation uniqueness when one role and correlation invoke different tasks. Reserve `role.invoked` to `invokeRole`, define the generic index as excluding invocation events, validate deterministic invocation/event projection, and test public-emitter rejection plus same-role/same-correlation/different-task calls."
  - severity: "medium"
    where: "AC3 — PENDING publisher failure contract"
    finding: "The synchronous publisher is called bounded without a retry count, SQLite busy timeout, or transaction ordering for its promised durable attempt/error row. Specify the publication budget, rollback behavior, separate durable error recording while the invocation remains PENDING, and the exact condition producing `PUBLISH_FAILED`; add busy-exhaustion and failure-then-public-retry fixtures."
  - severity: "medium"
    where: "AC3 — sidecar-free SQLite initialization"
    finding: "Only UUID-temp sidecars are checked. If `state/coord.sqlite` is absent but stale `coord.sqlite-wal`, `-shm`, or `-journal` remains, `linkat` can publish beside those files and the final reopen may consume or recreate sidecar state. Fail closed on final-name sidecars immediately before linking and after all handles close, require `journal_mode` to report `delete`, retain/report stale files, and add stale-final-sidecar fixtures."
  - severity: "high"
    where: "AC5 — watcher claim and Git ref update"
    finding: "The SQLite claim and terminal record cannot be atomic with the Git ref update. A crash can leave a claim without a ref change or a changed ref without terminal state, while all later claimants are forbidden to write. Define durable PREPARED data containing expected old SHA, new SHA, and action digest; use an exact Git CAS; reconcile old/new/divergent ref states before terminalization or durable escalation; and test crashes on both sides of the CAS."
  - severity: "medium"
    where: "AC7 — npm lifecycle denial"
    finding: "The package-manifest audit does not define whether it covers the root, every planned cached dependency, pre/post wrappers, lock signals, or npm's implicit `binding.gyp`/node-gyp install behavior, while the target intentionally retains explicitly invoked package scripts. Define forbidden automatic/install hooks versus allowed explicit scripts, audit the complete planned closure before both installs, and require transitive-postinstall and implicit-node-gyp fixtures to fail closed with unchanged sentinels."
  - severity: "high"
    where: "AC7 — bounded process cleanup"
    finding: "A fresh process group plus TERM/KILL cannot prove no survivors when a child changes session or process group, and `waits/reaps` has no terminal supervisor deadline after the two grace periods. Require enforceable descendant containment or escaped-descendant discovery, a hard completion deadline with a durable survivor outcome, and fixtures for a session-escaping child and inherited listener."
  - severity: "medium"
    where: "AC7 — abnormal-exit diagnostics"
    finding: "The required create-new diagnostic has no normative path, versioned schema, maximum encoded size, exit-code mapping, or behavior when creation or fsync fails. Specify a bounded artifact location and schema beneath the attempt root, atomic publication and fallback rules, and tests for diagnostic-write failure and repeated-signal persistence."
  - severity: "high"
    where: "AC7 — direct/npm verification-result equality"
    finding: "Direct and npm routes necessarily differ in launcher argv, `--out` values, npm-injected environment, timing, and outer stdout, yet the schema requires literal argv/environment and raw-stream hashes while equality ignores only route, output path, and monotonic/PID fields. Separate route-envelope evidence from a route-independent sanitized workload projection, enumerate the exact compared and ignored fields, and test that npm lifecycle environment and banners neither cause false inequality nor hide workload differences."
---
