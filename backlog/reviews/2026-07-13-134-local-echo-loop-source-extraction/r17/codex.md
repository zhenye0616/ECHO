---
item_id: "2026-07-13-134-local-echo-loop-source-extraction"
round: 17
reviewer: "codex"
artifact_sha: "e1115daee4ad389bca1bed9b10a43e76df534c19"
completed_at: '2026-07-14T04:35:26Z'
verdict: "pushback"
findings:
  - severity: "medium"
    where: "frontmatter requested_reviewers; AC5"
    finding: "The proposal requests `codex` and `codex-ops`, which does not provide the cross-vendor review that AC5 claims to preserve. Add a non-Codex reviewer such as `cursor` and run a fresh review round at the resulting spec SHA; `codex-ops` may remain the separate post-build reviewer."
  - severity: "medium"
    where: "frontmatter files_to_modify/spec_refs; AC2"
    finding: "The source policy is only path-referenced and remains builder-modifiable, while no reviewed blob OID or SHA-256 is fixed in the spec. A builder could change the policy and then produce internally consistent attestations over unreviewed bytes. Seal the exact policy blob/hash in the proposal, include its bytes in review input, remove it from builder-mutable scope after convergence, and require raw-object loading by the sealed OID."
  - severity: "medium"
    where: "AC5; AC8"
    finding: "AC5 preserves content-only reviewer bindings and founder checkpoints, but AC8 directs the `codex-ops` reviewer itself to check out, commit, and push a feature branch without a stated founder authorization. It also says the reviewer adds only the review record while making that child the pending-review head, leaving the existing `head_sha` and handoff metadata stale. Define a wrapper- or operator-owned publication workflow, its founder approval point, exact expected-old CAS, and the authoritative pending-review metadata/update rule, or keep the review artifact off the feature branch."
  - severity: "medium"
    where: "AC5 watcher state machine"
    finding: "The declared transition graph permits `APPLYING` only to `APPLIED` or `ESCALATED`, but recovery later requires `APPLYING -> APPROVED` when the endpoint still equals expected-old. Choose one recovery state and specify the guarded SQLite CAS, owner-token/lease handling, retained approval token, and fixture assertion for that transition."
  - severity: "medium"
    where: "AC5 watcher project configuration"
    finding: "The fixed endpoint and repository identity come from an unnamed `reviewed project config`; no path, schema, canonicalization rule, or reviewed hash is specified. Name and seal that configuration, define endpoint normalization and allowed transport, add it to the relevant modification/reference scope, and make substitution fixtures use the sealed digest."
  - severity: "medium"
    where: "AC3 readiness-marker publication"
    finding: "The database publication primitive is concrete, but the load-bearing `coord.ready` step merely says it is published atomically. Specify same-directory exclusive temp creation, canonical byte encoding including lossless device/inode representation, file fsync, no-clobber publication, EEXIST reconciliation, directory fsync, cleanup, and descriptor-based consumer matching so the two-contender and crash claims are implementable."
  - severity: "medium"
    where: "AC3 invokeRole publication budget"
    finding: "Clamping SQLite busy timeouts and sleeps cannot guarantee return by exactly 2,000ms because a synchronous SQLite transaction or filesystem operation may itself overrun the remaining budget; fake-clock tests do not prove that wall-clock bound. Either use an interruptible worker mechanism or narrow the contract to bounded configured waits, no new operation after deadline, and an explicit scheduler/I/O tolerance with real-clock tests."
  - severity: "medium"
    where: "AC7 locked installation"
    finding: "The ordered verification argv is specified, but the preceding lock-authorized cache fill and offline installs have no exact commands, flags, integrity inputs, or ownership. Add their absolute npm argv and ordering, require lifecycle suppression such as `--ignore-scripts`, require offline consumption from integrity-checked lock entries, and record these steps in provenance for both independent routes."
  - severity: "medium"
    where: "Out of Scope; AC1; AC8"
    finding: "The global prohibition on touching real repositories or remotes contradicts creation of the real target repository and AC8's Project_echo feature-branch push. Scope that prohibition explicitly to fixtures/tests and enumerate the authorized pinned source reads, target creation, and feature-branch CAS push."
---
