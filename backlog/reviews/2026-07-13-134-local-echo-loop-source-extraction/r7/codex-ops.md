---
item_id: "2026-07-13-134-local-echo-loop-source-extraction"
round: 7
reviewer: "codex-ops"
artifact_sha: "a4a4e1255143c8338bcfcfa123c0f59d5d7b1582"
completed_at: '2026-07-13T23:33:19Z'
verdict: "proceed_after_patches"
findings:
  - severity: "high"
    where: "AC1 — target publication/discard; AC8 — verify-handoff"
    finding: "A successful target rename followed by parent-fsync failure, or later record-CAS rejection after source-ref drift, permanently wedges the workflow: the target exists, discard requires target absence, fresh publication cannot win RENAME_EXCL, and handoff rejects unrelated descendants. Specify exact-target reconciliation or durable archival, bounded record-CAS recovery, and failpoint tests for both windows."
  - severity: "high"
    where: "AC1 — child gate, supervision, and discard"
    finding: "SIGKILL or a supervisor crash after gate release can leave an exact live child running indefinitely; later invocations never signal it and discard must refuse it. Require a child-side supervisor-liveness lease or another bounded self-termination mechanism, durable operator evidence, and a test that kills the supervisor after work begins."
  - severity: "high"
    where: "AC1 — publish-record"
    finding: "The record sequence does not define how exact record bytes reach the bound worktree. update-ref does not update worktree files, while updating only the real index leaves the worktree dirty; writing before CAS creates an unhandled dirty pre-CAS window. Specify a crash-recoverable state machine covering durable record-file materialization, temporary-index object creation, ref CAS, real-index locking, clean-worktree proof, and retries for every exact pre/post-CAS state."
  - severity: "medium"
    where: "AC1 — claim election, discard, and target publication durability"
    finding: "Each RENAME_EXCL crosses distinct source and destination directories, but the spec requires only a singular parent fsync. Require and test fsync of both parent directories after every claim, archive, and target-publication rename, including recovery when either fsync fails."
  - severity: "high"
    where: "AC1, AC2, and AC7 — trusted bootstrap and runtime closure"
    finding: "The documented entrypoint resolves node through ambient PATH and executes the mutable worktree echo-loop.mjs before either can validate the committed blob or runtime manifest. A poisoned PATH or dirty entrypoint therefore runs before preflight. Require a trusted bootstrap using absolute, hash-verified runtime and pinned script bytes, followed by sealed absolute child launches, with hostile top-level PATH and dirty-entrypoint tests."
  - severity: "medium"
    where: "AC1 — run identity and filesystem paths"
    finding: "Run ID validation is unspecified even though the value is interpolated into new and archive paths. Require a bounded ASCII grammar, traversal rejection, canonical no-symlink containment for every derived path, and exact full-hash validation of the requested source commit before filesystem mutation."
  - severity: "high"
    where: "AC3 — initialization intent publication"
    finding: "Direct O_EXCL creation exposes an empty or partially written ownership marker to simultaneous openers, allowing a live winner to be misclassified as malformed or stale. Publish an already-complete file-fsynced marker with an atomic no-replace operation and parent fsync, and add a contender barrier before final marker publication."
  - severity: "high"
    where: "AC3 — initialization crash recovery"
    finding: "A crash after schema COMMIT but before marker unlink or directory fsync leaves a complete schema with a dead ownership marker, yet the spec defines stale conversion only for an uninitialized schema. Define single-winner cleanup for a positively stale marker plus the exact completed schema, fail closed for partial or unexpected schemas, and test every post-commit cleanup boundary."
  - severity: "high"
    where: "AC6 — fixture Git isolation"
    finding: "Private HOME and global-config settings do not neutralize inherited GIT_DIR, GIT_WORK_TREE, index/object/config injection, askpass, SSH, proxy, or alternate-object variables; protocol.file is also scheme-wide while containment is checked only before push. Run every fixture child from an explicit allowlisted environment and containment-check all resolved Git directories, object stores, and file-transport operands immediately before every clone, fetch, pull, submodule, and push operation."
---
