---
item_id: 2026-05-25-074-echo-cli-binary
round: 3
combined_at: '2026-05-26T06:28:54Z'
codex_response: codex.md
cursor_response: null
codex-ops_response: codex-ops.md
claude_response: null
patch_commit_sha: null
next_round: null
combined_verdict: divergent
escalated_to_founder: true
---

# Combined findings

**Divergent verdicts** — codex='proceed_after_patches', codex-ops='pushback' cross the `{proceed*, pushback}` boundary. Strategist override per /loop directive: findings are SEMANTICALLY CONVERGENT on the two load-bearing classes (binary-rename incomplete cascade × 2 reviewers; absolute-path escape-hatch nonexistent × 2 reviewers), plus codex-ops alone surfaces a real HIGH (SIGTERM-gap-between-steps). Codex's other 3 findings (AC1.5 hermeticity, AC3.2 transport headers, stale frontmatter) are cleanup-class. Per 058 disposition discipline: the absolute-path escape hatch was added in r2 disposition prose without being wired through `agentOverrides`; per "prefer removal over deeper patching of a recently-added mechanism," REMOVE the escape-hatch claim and document the PATH requirement instead.

## Convergent findings

| # | Severity | Source | Where (primary) | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| C1 | MEDIUM | codex F1 + codex-ops F2 | 074:32 (files_to_modify) + 074:123-138 (AC1.2 + AC1.3) | accepted — patched | Binary-rename did not cascade everywhere. `files_to_modify` line 32 still says `# AC1 — add bin: { echo: ... }`; AC1.2 still documents `echo <subcommand>`; AC1.3 still tells users to run `echo <cmd> --help`. These are the EXACT user-facing strings the rename was supposed to fix (per the r2 binary-rename rationale). Patch: scrub every remaining `echo <subcommand>` / `echo <cmd> --help` / `bin: { echo: ... }` occurrence to `echoctl ...`. Preserved-as-is: `claude mcp add echo` (third-party MCP server name), `~/.echo/` (home directory), `echo_ping` (MCP tool), `echo-cli-binary` (spec id), `echo_owned` / `echo-owned` (none currently exist), `echo` as an MCP server entry name (intentional product brand). |
| C2 | MEDIUM | codex F3 + codex-ops F3 | 074:488-492 + 074:500-503 (AC5.3 absolute-path remediation) | accepted — mechanism dropped per 058 | The `--agent <role>=<absolute-path>` escape-hatch line in AC5.3 step 3 was added in r2 disposition prose AS IF `agentOverrides` already supported absolute paths, but the actual type is `ReadonlyMap<string, AgentKind>` — paths are not in the grammar, parser, matcher validation, or tests. Building it out would mean a new override type + parser regex + matcher validation + dispatch wiring + new test cases — ~30-50 LOC of new mechanism for a single 03:00-PATH-failure remediation. Per 058 ("prefer removal over deeper patching of a recently-added mechanism"): REMOVE the escape-hatch language. Replace with explicit PATH requirement documentation: "`echoctl run` under launchd / cron / Raycast launching contexts requires the agent binaries (`codex`, `claude`) on the launching environment's PATH. If not present, the per-step `DispatchOutcome` records `error: 'cli-unavailable: <agent> not found on PATH'` and the workflow stops. The launchd plist (072's daemon install pattern) is the reference recipe for ensuring PATH inheritance; future `--agent <role>=<absolute-path>` override is a follow-up item if dogfooding surfaces real demand." This removes the false-promise risk without adding new mechanism. |

## Divergent findings (single-reviewer or non-overlapping primary `where`)

| # | Severity | Source | Where | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| 6 | HIGH | codex-ops F1 (r3-O1) | 074:493-495 (AC5.3 step 6) + 521-544 (AC5.4 step 8-10) | accepted — patched | The r2 signal-handling fix guarantees an `interrupted` outcome only when SIGTERM arrives MID-step. Two operator-realistic gaps remain: (a) SIGTERM arrives AFTER step N's child exits 0 but BEFORE step N+1's spawn() call → `controller.abort()` fires but `dispatchWorkflow` is between iterations and never produces an `interrupted` outcome; (b) SIGTERM arrives after the FINAL child exits 0 but before `runRun` derives the exit code — same window. In both, all outcomes are success → AC5.4 step 10 exits 0 despite the process being interrupted. Patches: (a) AC5.3 step 3a (new) — `dispatchWorkflow` checks `opts.signal.aborted` BEFORE each iteration's spawn() call; if true, appends `{ ...currentStep, spawn: null, error: 'interrupted', signal: receivedSignal.current }` and returns early (loop breaks). (b) AC5.4 step 10 priority order changed — `receivedSignal.current !== null` is checked FIRST (before the exit-code-derivation from outcomes); if non-null after dispatch returns, exit 130 (SIGINT) / 143 (SIGTERM) UNCONDITIONALLY, regardless of how many outcomes succeeded. This closes both gap classes — the signal-flag check is the load-bearing primary; the in-loop `signal.aborted` check is defense-in-depth for the mid-step case. (c) AC7.4 case 12 extended with 12a (between-step gap) + 12b (post-final-step gap). |
| 7 | MEDIUM | codex F2 (r3-K3) | 074:145 (AC1.5 hermeticity) | accepted — patched | AC1.5's smoke test runs `npm install -g <path>` against the repo, but there is no `prepare`/`postinstall` hook to build `dist/`; a fresh run either installs a broken bin (no dist/) or passes against stale local build output. Patch: AC1.5 step 1 prepends `npm run build:cli` BEFORE the install. Result is hermetic: even on a freshly-cloned repo with no prior build, the smoke test produces fresh `dist/` then exercises the bin entry. The `prepare` hook is intentionally NOT added to `package.json` (would slow every `npm install` for non-CLI dev paths); the smoke test owns the build. |
| 8 | MEDIUM | codex F4 (r3-K4) | 074:252 (AC3.2 doctor MCP probe headers) | accepted — patched | `src/mcp/server.ts` (the daemon's StreamableHTTPServerTransport) content-negotiates and 406-rejects POSTs missing both `Content-Type: application/json` AND `Accept: application/json, text/event-stream`. AC3.2 said "send a single HTTP POST with a minimal MCP `initialize` request body" without specifying headers — a builder following the literal spec would get 406 from a healthy daemon. Patch: AC3.2 explicit headers — `{ 'Content-Type': 'application/json', 'Accept': 'application/json, text/event-stream' }` MUST be present on the probe POST. Body is the minimal JSON-RPC `initialize`: `{ jsonrpc: '2.0', method: 'initialize', params: { protocolVersion: '2025-06-18', capabilities: {}, clientInfo: { name: 'echoctl-doctor', version: <version> } }, id: 1 }`. AC7.2 fixtures pin the header presence by intercepting the fake fetch call. |
| 9 | LOW | codex F5 (r3-K5) | 074:26 (files_to_modify AC4.4 line) + R-flagged R3 section | accepted — patched | Stale pre-r2 wording: `files_to_modify` line 26 still describes `inverse/skills.ts` as removing files "only those listed in DEFAULT_ROLE_FILENAMES → role.skills union" (the pre-r2 enumeration source); the R-flagged R3 section still asks reviewers to revisit the dropped marker mechanism. Both contradict the r2 disposition that switched to `enumerate ~/.echo/skills/*.md` + byte-equality (no marker). Patch: rewrite the files_to_modify line + the R3 R-flagged entry to reflect the actual current contract. |

## Convergence call

**needs r4 — focus_hints:** Verify (1) every `echo <subcommand>` reference in the spec body has been scrubbed to `echoctl` (preserving `claude mcp add echo` + `~/.echo/` + `echo_ping`); a simple grep should confirm. (2) AC5.4 step 10's NEW priority order — `receivedSignal.current` check FIRST, exit code derivation SECOND — closes both SIGTERM gap classes (codex-ops r3-O1 was the load-bearing finding); the new AC7.4 case 12a/12b fixtures actually exercise the between-step + post-final-step windows (not just mid-step). (3) AC5.3 step 3a's pre-spawn `signal.aborted` check is defense-in-depth and doesn't introduce a TOCTOU vs the mid-step abort. (4) The absolute-path escape-hatch language is REMOVED from AC5.3 step 3 (and any tests / OoS references); replaced with the explicit PATH-requirement documentation. (5) AC1.5's `npm run build:cli` prerequisite ensures hermeticity even on a freshly-cloned tree. (6) AC3.2's required headers + JSON-RPC initialize envelope match the daemon's actual StreamableHTTPServerTransport requirements; AC7.2 test fixtures assert the header presence. (7) Stale files_to_modify line 26 + R-flagged R3 section now reflect the r2 byte-equality disposition.
