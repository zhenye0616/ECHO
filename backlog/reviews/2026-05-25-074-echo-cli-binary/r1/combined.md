---
item_id: 2026-05-25-074-echo-cli-binary
round: 1
combined_at: '2026-05-26T05:49:31Z'
codex_response: codex.md
cursor_response: null
codex-ops_response: codex-ops.md
claude_response: null
patch_commit_sha: null
next_round: 2
combined_verdict: divergent
escalated_to_founder: true
---

# Combined findings

**Divergent verdicts** — codex='pushback', codex-ops='proceed_after_patches' cross the `{proceed*, pushback}` boundary. Strategist override per /loop directive: the divergence is verdict-label-only; findings are SEMANTICALLY CONVERGENT (both reviewers cite the same capabilities-never-populated root cause as their H1). Dispositioning autonomously and dispatching r2 for verification; the founder retains the merge gate at the end of the convergence cycle.

## Convergent findings

| # | Severity | Source | Where (primary) | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| C1 | HIGH | codex F1 + codex-ops F1 | 074:338 / 074:103,338-359 + 073:426-430,668 + assets/echo-roles/builder.toml:3,6-8 | accepted — patched | Both reviewers find that `OnboardedAgentProfile.capabilities` is never populated (073's `wire()` sets `[]` per `wire.ts:127-164`; 070 schema only requires `string[]`), so the default builder/strategist roles whose `requires.capabilities` are non-empty would make every `echo run` fail with `no-onboarded-agent` or `capability-mismatch`. codex-ops adds the related concern that the dispatcher hardcodes `--sandbox read-only` while default roles require write capabilities. Patches: (1) NEW AC2.5 adds a static `AGENT_CAPABILITIES_BY_KIND` constant in `src/cli/commands/init.ts` (codex=`['fs.read','fs.write','git.read','git.write','network','mcp.echo.read','mcp.echo.write']`, claude-code=same, cursor=`['mcp.echo.read']`) and `runInit` mutates `onboarding.json` after `wizard.wire()` to populate `OnboardedAgentProfile.capabilities` from this map BEFORE probe — keeping 073 unchanged. (2) AC5.3 patched: spawn's sandbox flag is derived from `role.sandbox` (`'workspace-write'` → `codex exec --sandbox workspace-write`; `'read-only'` → `--sandbox read-only`). (3) NEW AC7.4 case 11: end-to-end run with a workflow whose step requires `workspace-write` + `fs.write` succeeds via codex; the spawn receives `--sandbox workspace-write`. (4) Test extensions in AC7.1 / AC7.7 pin the capability-write step + the sandbox-mapping branch. |

## Divergent findings (single-reviewer or non-overlapping primary `where`)

| # | Severity | Source | Where | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| 2 | HIGH | codex | 074:165 | accepted — patched | AC2.2's `import { version } from '../../../package.json' assert { type: 'json' }` fails on Node 22 (`assert` rejected; JSON modules expose default export, not named `version`). Patch: replace with `JSON.parse(readFileSync(new URL('../../../package.json', import.meta.url), 'utf8')).version`. Portable across Node versions, no `assert`/`with` syntax dependency, no test fragility around future JSON-modules spec changes. |
| 7 | HIGH | codex-ops | 074:155-160,431-435,457-468 | accepted — patched | `runInit` must fail closed on `!process.stdin.isTTY` BEFORE any `wizard.*` call, NOT rely on `makeTtyPrompt`'s default-fallback under the hood (which would let a piped `echo init` proceed silently into `wizard.wire()` and mutate `~/.codex` / `~/.claude` / `~/.cursor` / `~/.echo`). Patches: (1) AC2.1 prepends a non-TTY guard: if `!process.stdin.isTTY && !opts.yes` (no `--yes` exists today for init; reserved field for future), exit 2 with "non-interactive — `echo init` requires a TTY (pipe-driven onboarding is a future item)" BEFORE any wizard call. (2) AC7.1 case 10 patched to assert ZERO wizard methods invoked (use a spy on the `wizardFactory` injection). (3) Companion follow-up logged in After-Completion for a future `--answer-file` non-interactive path if dogfooding demands it. |
| 8 | HIGH | codex-ops | 074:272-275,481-491,531-535 | accepted — patched | `removeCodexMcpEntry`'s string-level table-elision currently stops at "next `[mcp_servers.<next>]` header or EOF" — wrong: real `~/.codex/config.toml` puts unrelated tables (`[profiles.work]`, `[model]`, `[tools]`) immediately after the ECHO block, and the spec would delete the rest of the file. Patches: (1) AC4.3 elision rule rewritten: stop at the next TOML table OR array-of-tables header of ANY name (regex `^\s*\[\[?[^\]]+\]\]?\s*(#.*)?$`), with CRLF + leading-comment tolerance, and PRESERVE everything from that header onward byte-for-byte. (2) AC7.5 inverse-codex-config.test.ts gains a fixture pinning: `[mcp_servers.echo]` followed by `[profiles.work]` → ECHO block removed, `[profiles.work]` survives byte-for-byte. CRLF + no-trailing-newline + BOM variants pinned in three sub-cases. |
| 3 | MEDIUM | codex | 074:302 | accepted — patched | AC5.1's TOML example used `version = 1`; the validation text + loader interface dropped the field. Patches: (1) Rename to `schema_version = 1` to mirror 070's `OnboardingState.schema_version` pattern (single project-wide vocabulary). (2) `Workflow` interface exposes `readonly schemaVersion: 1` (typed; not silently discarded). (3) AC7.6 explicitly covers the `schema_version` round-trip + the mismatch-throws case. |
| 4 | MEDIUM | codex | 074:200 | accepted — patched | Doctor rollup was contradictory. Resolution: `'degraded'` iff (`pidLockHeld === true` AND `mcpReachable === false`) — the recoverable stale-lock shape; `'broken'` iff (`pidLockHeld === false` AND `mcpReachable === false`) — the daemon-completely-gone shape, OR `echoHome.exists === false` OR `schemaVersion === 'mismatch'`. Patches: (1) AC3.6 rewritten with the explicit truth table. (2) AC7.2 case 2 reworded to "PID lock present + MCP unreachable → `degraded`"; NEW case 2b "PID lock absent + MCP unreachable → `broken`". |
| 5 | MEDIUM | codex | 074:289 | accepted — mechanism dropped | The `<!-- echo-owned-skill -->` first-line marker would push existing skills' frontmatter `---` delimiter off line 1 (skills/process-backlog.md, skills/review-pending.md, etc. all open with YAML frontmatter), breaking metadata parsers. Per 058 disposition-discipline: prefer removal over deeper patching of a recently-added mechanism. Patches: (1) AC4.4 reworked to use **byte-equality against `~/.echo/skills/<skill>.md`** as the ownership proof — no marker needed, no 072 change required. For each candidate `~/.claude/commands/<skill>.md`: read both files; if byte-equal → delete; if differ → skip with `reason: 'user-modified'`; if `~/.echo/skills/<skill>.md` missing → skip with `reason: 'source-missing'`. (2) R4 (paired 072 marker addition) removed from the risk section. (3) Definition of done item 5 (about the marker) removed. |
| 9 | MEDIUM | codex-ops | 074:107,373-390,411-418,493-504 | accepted — patched | `--project` is resolved but never passed to the spawn. Patches: (1) AC5.3 `spawn(cmd, args, { cwd: projectRoot, env: process.env })` is now explicit; the absence was an unintentional omission. (2) AC7.4 case 9 patched: run from git-rootless cwd with `--project /fixture/repo`, fake spawn must receive `opts.cwd === '/fixture/repo'`. |
| 10 | MEDIUM | codex-ops | 074:188-200,470-479; src/daemon/lifecycle.ts:53-55 | accepted — patched | `echo doctor` was specced to stat `<dataDir>/echo.pid` but the daemon writes `<dataDir>/daemon.pid` (`src/daemon/lifecycle.ts:55`). Patches: (1) AC3.2 corrected to `daemon.pid`. (2) AC7.2 fixture renamed accordingly. (3) Out-of-scope item 11 added: don't add an `acquirePidLockPath()` export to the daemon module (the constant `'daemon.pid'` is duplicated in 074's doctor; if it ever changes in the daemon, the daemon item that changes it owns updating both sites). |

## Convergence call

**needs r2 — focus_hints:** Verify the C1 capability-population + sandbox-mapping patch (AC2.5 + AC5.3); the AC2.2 fs.readFileSync replacement is portable across Node 22+; AC2.1's non-TTY-fail-closed guard fires BEFORE any wizard side effect; AC4.3's elision rule's "next-header-of-any-name" regex handles CRLF + BOM + no-trailing-newline; AC4.4's byte-equality ownership check + the dropped 072 marker addition; the doctor rollup truth table is unambiguous; `cwd: projectRoot` passed to all spawns; workflow `schema_version = 1` is consistent across schema + interface + tests.
