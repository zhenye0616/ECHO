---
item_id: 2026-05-25-074-echo-cli-binary
round: 2
combined_at: '2026-05-26T06:11:02Z'
codex_response: codex.md
cursor_response: null
codex-ops_response: codex-ops.md
claude_response: null
patch_commit_sha: null
next_round: null
combined_verdict: pushback
escalated_to_founder: false
---

# Combined findings

Both reviewers converged on `pushback` (no boundary cross, no escalation). 8 findings, 4 HIGH + 4 MED. Three findings target r1 patches (per 058 disposition discipline check: K2 + K3 + K4 are r1-patch-introduced; K2 keeps the patch and fixes the data flow because removal would re-open codex-ops r1 F1; K3 + K4 are precise corrections to the r1 patch, not removals — the r1 mechanism shape is correct, the sub-detail was wrong). The other 5 findings target original-spec mechanism (O1 binary-name + O2 signal handling + O3 PATH + O4 uninstall-exit + K1 skillsRoot) — all real patches.

## Convergent findings

| # | Severity | Source | Where (primary) | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|

## Divergent findings (single-reviewer or non-overlapping primary `where`)

| # | Severity | Source | Where | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| 1 | HIGH | codex F1 (K1) | 074:483 (AC5.4 + AC4.4) | accepted — patched | 071's `loadRolesFromDir(rolesDir)` walks upward from `sourcePath` looking for `package.json + skills/`. When `rolesDir = ECHO_HOME_PATHS.roles` (i.e., `~/.echo/roles/`), no such ancestor exists — every installed `echo run` role-load would fail before matching. Patch: every 074 call site to `loadRolesFromDir` MUST pass `{ skillsRoot: ECHO_HOME_PATHS.skills, assertDefaults: true }`. The `~/.echo/skills/` directory exists by 072's `populateEchoSkills` AC4.1 contract. Sites: AC5.4 step 5 + AC4.4's role-skill enumeration. AC7.4 + AC7.7 tests rewritten to use a tmp `ECHO_HOME` tree (real `~/.echo/skills/` + `~/.echo/roles/`), NOT in-repo assets. |
| 2 | HIGH | codex F2 (K2) | 074:437 (AC5.3 step 2 + AC5.4 step 8) | accepted — patched | `dispatchWorkflow()`'s signature accepts only `workflow` + `matches`, but AC5.3 step 2 (added in r1 for sandbox mapping) requires looking up `Role.sandbox`. `AgentMatch` carries `role: string` only — the builder cannot satisfy AC5.3 without inventing an unreviewed side channel or reloading roles. Patch: enrich `AgentMatch` with `resolvedSandbox?: 'read-only' \| 'workspace-write'` (filled by the matcher when `reason: 'matched'`; undefined otherwise). The dispatcher reads `match.resolvedSandbox` to derive the spawn flag. No `roles` array passed to dispatcher; data flows through `AgentMatch`. AC7.4 case 11 + AC7.7 case 7 updated to assert the matcher populates `resolvedSandbox` from the picked role. |
| 3 | MEDIUM | codex F3 (K3) | 074:357 (AC4.4 skillNames enumeration) | accepted — patched | 072's `syncClaudeSkills` copies EVERY `.md` from `~/.echo/skills/` to `~/.claude/commands/`, not just the union of `role.skills`. At the pinned SHA, `~/.echo/skills/review-queue-cursor.md` is copied but no default role references it — `echo uninstall` would leave that file orphaned in `~/.claude/commands/`. Patch: AC4.4's `skillNames` is now derived from `enumerate ~/.echo/skills/*.md` (the source-of-truth for what ECHO actually copied), NOT from `role.skills` union. The byte-equality ownership proof is unchanged; only the enumeration source moves. This also removes 074's dependency on a valid `loadRolesFromDir` for the uninstall path (one less failure mode). AC7.5 / AC7.3 case 9 + new case 11 cover `review-queue-cursor.md` removal. |
| 4 | MEDIUM | codex F4 (K4) | 074:423 (AC5.2 vs AC7.7 case 8) | accepted — patched | AC5.2's non-override path returned `no-onboarded-agent` whenever the capability-filtered set was empty, but AC7.7 case 8 (added in r1) asserted `capability-mismatch` for a profile with `capabilities: []`. The two definitions contradict. Patch: explicit reason taxonomy — (a) `no-onboarded-agent` ⟺ ZERO profiles in `onboarded[]` for this AgentKind (the user never wired any agent capable of this role's kind); (b) `capability-mismatch` ⟺ profiles exist in `onboarded[]` but NONE has `capabilities ⊇ requires.capabilities` (wired but under-permissioned); (c) `role-unknown` ⟺ `step.role` not found in `roles[]`. AC5.2 + AC7.7 case 8 rewritten to match. |
| 5 | HIGH | codex-ops F1 (O1) | 074:113-127 (binary name `echo`) | accepted — patched | `echo` is a POSIX shell builtin (bash + zsh + dash). `npm link` puts `~/.npm-prefix/bin/echo` on PATH but the shell intercepts `echo init` as the builtin and prints `"init\n"` instead of running the CLI. This is a production-blocking command-reachability bug. Patch: **rename the binary from `echo` to `echoctl`** (follows the `kubectl`/`systemctl`/`launchctl` pattern; distinctive; not a shell builtin; correctly reads as "ECHO control"). Cascading updates: AC1.1 `bin: { echoctl: './dist/cli/index.js' }`; every inline `echo init`/`echo doctor`/`echo uninstall`/`echo run` reference; remediation copy in AC2.4 (e.g. "run `echoctl doctor` to re-probe"); After-Completion section notes that the decision-archive's `/usr/local/bin/echo` reference + wiki page should follow this rename post-shipment. The product name "ECHO" is unchanged; only the binary verb moves. NEW AC1.5 smoke test added to pin the bin entry actually reaches `dist/cli/index.js` from a real shell — `bash -c 'echoctl --version'` against an npm-linked install. |
| 6 | HIGH | codex-ops F2 (O2) | 074:131-134 (AC1.3 exit codes) + 430-486 (AC5.3 + AC5.4) | accepted — patched | AC1 promised SIGINT/SIGTERM = exit 130/143; AC5.3 had `signal.aborted` returning the partial outcome array; AC5.4 exited 0 if all RETURNED outcomes had `exitCode 0`. The race: SIGTERM after step 1 succeeded but before step 2 records → `echo run` could report success for a partial workflow. Patch: (a) AC5.3 step 6 rewritten — on `signal.aborted` mid-step, the dispatcher kills the child, appends an `{ ...partial, error: 'interrupted', signal: 'SIGINT' \| 'SIGTERM', timedOut: false }` outcome to the array, AND returns early. (b) AC5.4 step 8 rewritten — `runRun` checks for ANY outcome with `error === 'interrupted'`; if present, exit 130 (SIGINT) or 143 (SIGTERM) regardless of prior step exit codes. (c) NEW AC5.4 step 9 — `runRun` installs `process.on('SIGINT')` + `process.on('SIGTERM')` handlers BEFORE calling `dispatchWorkflow` that abort an `AbortController` passed via `signal`. (d) AC7.4 case 12 (new) — fixture: SIGTERM delivered after step 1 succeeds but before step 2 spawns → exit 143, outcomes array contains step 1 success + step 2 interrupted, NOT exit 0. |
| 7 | MEDIUM | codex-ops F3 (O3) | 074:454-456 (AC5.3 spawn env) | accepted — patched (minimal) | Bare `codex` / `claude` in `spawn()` with `env: process.env` works in interactive shells but fails under launchd / cron / Raycast / minimal-PATH callers — `ENOENT` on the binary. Patch: AC5.3 step 3 — when spawn throws `ENOENT` for the picked agent's binary, the `DispatchOutcome` records `error: 'cli-unavailable: <agent> not found on PATH'` (mirrors 073's probe failure mapping) and the dispatcher STOPS (per AC5.3 step 5 — non-zero exit halts the workflow). Per 058 disposition discipline: do NOT add binary-path persistence in onboarding.json (scope creep) and do NOT normalize PATH (Heisenbug under different distributions); the explicit error message + the per-step actionable outcome is sufficient. Documented limitation: `echoctl run` under launchd/cron requires the agent binaries to be on the launching env's PATH. Future `--agent <role>=<absolute-path>` override (already in AC5.4 grammar) is the user's escape hatch. AC7.4 case 13 (new) — fixture: spawn ENOENT → `DispatchOutcome.error: 'cli-unavailable: ...'`, exit 1, no orphan child. |
| 8 | MEDIUM | codex-ops F4 (O4) | 074:280-292 (AC4 uninstall exit code + --purge-state gate) | accepted — patched | `echo uninstall` was specced to exit 0 even on partial adapter failures, AND `--purge-state` would still remove `~/.echo/` — leaving `~/.codex` / `~/.claude` / `~/.cursor` polluted with ECHO refs while automation sees exit 0. Patch: (a) AC4.1 exit semantics rewritten — `runUninstall` exits 0 only if every per-agent adapter result is `removed` or `noop`; exits 1 if ANY result is `conflict` or `error`. (b) `--purge-state` rewritten — runs ONLY when the inverse-cleanup phase reported zero conflicts/errors. If `--purge-state` is requested AND the cleanup phase had failures, the second prompt is REPLACED with a refusal message naming the un-cleaned files and exit code 1. (c) NEW `--force-purge` flag — overrides the safety gate for the "I really want to nuke, I'll clean ~/.codex manually" case; surfaces a stern warning + the residual-config file list before proceeding. (d) AC7.3 cases 11 + 12 (new) — fixture for partial-failure exit 1 + --purge-state-blocked-by-conflict refusal. |

## Convergence call

**needs r3 — focus_hints:** Verify (1) `echoctl` rename cascades cleanly across all CLI references + AC1.5 smoke test would actually catch a future bin-collision regression; (2) AC5.3's `signal.aborted → interrupted outcome → exit 130/143` chain leaves no path for `runRun` to exit 0 on partial workflow; (3) `AgentMatch.resolvedSandbox` carries the role sandbox value end-to-end with no other code path needing access to the full `Role` from the dispatcher (per K2 disposition); (4) AC4.4's `enumerate ~/.echo/skills/*.md` enumeration correctly handles the case where `~/.echo/skills/` is missing or empty (degenerate uninstall — should be `noop`-class, not error); (5) `loadRolesFromDir` is now invoked uniformly with `{ skillsRoot: ECHO_HOME_PATHS.skills, assertDefaults: true }` at BOTH sites (AC5.4 + AC4.4); (6) the matcher's K4 reason taxonomy (`no-onboarded-agent` vs `capability-mismatch` distinction) is unambiguous + tests cover both branches; (7) O3's spawn-ENOENT path captures the failure as an actionable `DispatchOutcome`, not a crash; (8) O4's `--purge-state` safety gate + `--force-purge` override are unambiguous about which conflicts block which.
