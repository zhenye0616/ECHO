---
item_id: "2026-07-15-136-echo-context-canonical-repository-release-substrate"
round: 2
reviewer: "claude"
artifact_sha: "a3d83d7d8eae4d67854a0c57fe429d7dc808f79c"
completed_at: '2026-07-15T22:36:49Z'
verdict: "proceed"
findings:
  - severity: "nit"
    where: "AC3 fresh-clone script list ('drives only these named scripts plus git fsck') vs Tests fresh-clone enumeration"
    finding: "AC3's named-script list includes test:operator and the sentence 'The scripted fresh-clone acceptance run is tools/fresh-clone-acceptance.sh and drives only these named scripts plus git fsck' could be over-read as 'drives ALL of them', which would make the sibling-free acceptance run fail by design (test:operator requires ECHO_SOURCE_GIT_DIR and fails clearly without it). The natural whitelist reading ('its command surface is limited to these scripts') is coherent, and the Tests section enumerates the exact subset it actually runs (npm ci, typecheck, lint, CI Vitest, inventory, authority, artifact, git fsck, secret scan — no test:operator), so a builder has an unambiguous specification. Non-blocking; no patch required — recording only that the focus-hint (4) consistency sweep surfaced this residual reading and that the Tests enumeration resolves it."
---

# claude review — 2026-07-15-136 r2 (conceptual / architectural-drift lens)

Reviewed `backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.md` at `a3d83d7d8eae4d67854a0c57fe429d7dc808f79c`, verifying the r1 patch commit `ad53c6c7` against the six focus-hint areas plus a fresh conceptual pass.

## Verdict rationale

All eight r1 dispositions are faithfully realized in the patched text, the six focus-hint verifications pass, and no new conceptual, scope, or authority drift was introduced by the patches. One nit recorded for the round record; it needs no patch. Proceed.

## Focus-hint verification

1. **AC6 build-once separation — verified.** The unprivileged `build-artifact` job (contents: read) checks out the landed SHA as a fresh detached clone, verifies extraction-baseline ancestry, builds exactly once, and persists the three AC5 files as an immutable workflow artifact identified by run ID, artifact ID, and SHA-256 digest. Founder approval happens *after* the build over the complete tuple (source SHA, tree, version, artifact SHA-256, lock hash, manifest hash, run ID, artifact ID) through the `source-release` protected environment with sole reviewer zhenye0616. `publish-release` downloads, re-hashes against the approved digest, and publishes without rebuilding. AC5 now correctly frames double-build as a determinism *test* property while the release path builds once — the r1 tension between "build twice" and "build exactly once" is resolved, not papered over.
2. **Release authorization/atomicity — verified.** `workflow_dispatch` with explicit source-SHA/version/expected-hash inputs; hard failure unless the SHA equals canonical main HEAD; concurrency-group serialization; pre-publication re-read of repository ID, owner, and private visibility; rejection of any pre-existing same-name tag/release/asset with no clobber; post-upload readback of the annotated tag object and asset hashes against the approved tuple. Each irreversible external mutation is now bound to readback evidence, consistent with the Risks section's own standard.
3. **AC1 bootstrap pin + AC4 equivalence — verified.** The bootstrap scan pins gitleaks to an exact release version and binary SHA-256 (both recorded in the migration record), runs non-shallow across every ref with redaction, and reports paths/rule-IDs only. AC4 requires `tools/secret-scan.sh` to encode the equivalent scanner, version pin, configuration, and invocation, and `tests/governance/workflow-policy.test.ts` asserts pre-push/post-push command equivalence. The "same contract" language closes the r1 gap where the bootstrap and committed scans could silently diverge.
4. **Named scripts/job IDs/protection fields — internally consistent.** The AC3 script names (`typecheck`, `lint`, `test:ci`, `test:operator`, `verify:inventory`, `verify:authority`, `verify:artifact`, `scan:secrets`), the AC4 job IDs (`quality-macos`, `quality-ubuntu`, `secret-scan`, `build-artifact`, `publish-release`), the AC4 required-status-check names, and the Tests-section assertions all reference the same identifiers with no orphans or mismatches. The required checks (`quality-macos`, `quality-ubuntu`, `secret-scan`) exactly match the declared job IDs of the two non-release workflows, and the release-workflow jobs are correctly *not* required branch checks (they gate the release path, not merges). One residual wording nit recorded in frontmatter — non-blocking.
5. **Sidecar manifest + tar determinism — complete and non-self-referential.** Exactly three sidecar outputs; the manifest is never a member of the archive it describes, so binding the artifact size/hash inside the manifest creates no circularity (the manifest's own hash lives one level up, in the AC6 approval tuple and release record). The tar contract is fully pinned: single `echo-context-<version>/` root, sorted Git-tracked paths, Git modes 0644/0755, uid/gid 0 with numeric owners, committer-timestamp mtimes, gzip without name/timestamp metadata, canonical-JSON manifest bytes. Nothing in the format depends on the machine or wall clock that builds it.
6. **Actor bindings — sealed.** AC1: the founder creates the repository and operates the initial main push; the builder prepares commands and verifies readback only, with explicit "no reading of this spec authorizes a builder identity to push to either main" language. AC4: main protection is configured by the founder or a founder-approved named operator recorded in the migration record. AC6: the founder merges the target PR; publication is executed by the guarded workflow under founder environment approval, never a builder identity. I could construct no reading of the patched text that permits a builder push to echo-context/main or Project_echo main as a source mutation. (The builder's ordinary backlog state-transition commits on Project_echo main remain authorized by the standing pipeline protocol in `backlog/README.md`/CLAUDE.md, which this spec neither grants nor needs to grant — no conflict.)

## Fresh conceptual pass on the patch deltas

- **Split-authority attribution.** The corrected `spec_refs` annotation (two-organization-repository topology, controls-preservation rule, "predates this split and is not its authority") and the new "Why this spec exists" sentence attribute the third-repository split to the 2026-07-15 successor-execution decision and item 135's founder-reconciled merge. This matches the G2 record's actual content and cures the r1 misattribution exactly as dispositioned.
- **Personal-account deviation.** AC1 now records zhenye0616 personal hosting as an accepted, explicit deviation from the G2 organization-repository shape, keeps the later organization transfer under the G2 controls-preservation gate, and requires the AC6 migration record to state the deviation. The deviation is now visible instead of silent — the r1 concern is resolved.
- **No patch-induced scope growth.** The patches added named identifiers, actor bindings, and format pinning — constraint tightening, not new mechanism or new external surface. Out of Scope, Risks, and the 137/138/139 boundary are unchanged and still honest. The hosting-tier risk ("controls may not be enforceable... builder stops for a founder decision and does not claim source-release completion") is item-wide and covers both the AC4 branch-protection fields and the AC6 protected-environment requirement, so the known GitHub-tier limitation on private-repo protections has an explicit stop path rather than a silent-weakening path.
- **DEV preservation intact.** Version `0.1.0-dev.136.1`, `maturity DEV`, `installable false`, and the authority flags survive the patches unchanged at every layer (authority record, manifest, tests, README, release record).

## Cross-check against my r1 findings

r1 combined findings 6 (actor binding — medium), 7 (G2 annotation — low), and 8 (personal-account deviation — low) are each patched precisely as the disposition table describes; no partial application, no drift between the disposition rationale and the landed text.
