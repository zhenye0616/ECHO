---
item_id: 2026-07-15-136-echo-context-canonical-repository-release-substrate
round: 6
combined_at: '2026-07-16T03:57:02Z'
codex_response: codex.md
cursor_response: null
codex-ops_response: codex-ops.md
claude_response: null
patch_commit_sha: d309cdebc804c2fd5aa924f14d4d7496358b0a0a
next_round: null
combined_verdict: proceed_after_patches
escalated_to_founder: false
---

# Combined findings

Reframe gate: TRIGGERED — findings 1-6 and 11-13 target mechanisms introduced or reshaped by prior `spec-r*-patches` commits (r5's 3d74d33b: `run_attempt == 1` rerun rejection, single lock-hash dispatch input, manifest-hash-once wording, fixed release identity/flags; r3's 9997f073: inner/outer artifact-ID/digest handoff), so the mandatory fresh-context investigator ran (`codex exec --sandbox read-only`). Verdict: `propagation_completion` — NOT a structural cut and NOT patch-on-patch drift. R5 already removed the custom recovery transaction; what r6 targets are the retained, load-bearing guards whose producer→carrier→consumer→observable contracts were left incomplete against real GitHub Actions/API semantics (job-level `if` can only skip, never fail; `make_latest` is a request parameter not persisted in readbacks; annotated tag refs peel through a tag object; artifact IDs exist only post-upload and need run-ownership/expiry checks). Cutting these guards would reopen prior HIGH findings; the correct move is completing each contract with a supported observable and a negative fixture. Diagnostic check applied: each retained value was traced producer→carrier→consumer→observable — every one has a fail-closed observable (rerun-guard job failed conclusion; version equality vs package.json blob at approved SHA; recomputed canonical-JSON manifest hash; ref→tag-object→peeled-commit readback; latest-release lookup; raw-archive digest before extraction) and none could be deleted without weakening an invariant, so no field was cut and no marker/retry/cleanup/ledger/pre-dispatch build reappears. Findings 7-10 target older AC1/AC4 governance surface (trigger contracts, scan history completeness, scanner platform pinning, environment-control readback) — same completion treatment. Investigator risk accepted: if the hosting tier cannot expose an enforceable observable for any field, the spec now stops for founder disposition rather than absorbing another prose patch. Grouped dispositions: 1+12 (rerun rejection), 2+11 (version binding), 4+5+13 (release identity), 3 (terminology), 6 (artifact handoff), 7-10 (AC4/AC1 propagation). Patch commit: d309cdebc804c2fd5aa924f14d4d7496358b0a0a (`spec-r6-patches`).

## Convergent findings

| # | Severity | Source | Where (primary) | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|

## Divergent findings (single-reviewer or non-overlapping primary `where`)

| # | Severity | Source | Where | Disposition | Patch SHA / rationale |
|---|---|---|---|---|---|
| 1 | HIGH | codex | AC6 — run_attempt guard; Tests — rerun-rejection fixture | accepted — patched (grouped with #12): "rejected before any job step executes" replaced with layered observable rejection — a dedicated first rerun-guard job (empty permissions, no checkout/build/upload/environment/publication step) fails whenever `run_attempt != 1`, and every other job needs a successful rerun-guard plus its own `run_attempt == 1` condition, so reruns yield a failed conclusion (never all-skipped green) with zero external effect; fixtures updated | d309cdeb |
| 2 | MEDIUM | codex | AC6 — workflow_dispatch inputs and build-artifact job; Tests — dispatch-input fixtures | accepted — patched (grouped with #11): build-artifact validates every dispatch input before building and fails unless the version input exactly equals package.json's version in the committed tree at the approved source SHA; only that verified version feeds every tuple field, filename, tag, and release name; malformed/wrong-version fixtures fail before any upload, approval, or external write | d309cdeb |
| 3 | MEDIUM | codex | AC6 — manifest-hash lifecycle | accepted — patched (terminology-only): the authoritative manifest hash is *first derived* by the single build inside build-artifact (never dispatch-supplied, never substituted); every downstream verifier (publish-release, fresh-clone `verify:artifact`) independently recomputes the canonical-JSON SHA-256 and compares — the "computed exactly once" / re-hash conflict is resolved | d309cdeb |
| 4 | MEDIUM | codex | AC6 — draft creation, annotated-tag readback, and final postcondition | accepted — patched (grouped with #13): draft created with `target_commitish` = approved source SHA (testable target before the tag exists); draft-creation readback proves no implicit tag; tag verification reads ref → tag object → peeled commit against the approved SHA and expected annotation; lightweight-tag and wrong-peeled-target fixtures added | d309cdeb |
| 5 | MEDIUM | codex | AC6 — release publish flags and release-identity fixtures | accepted — patched (grouped with #13): publish update sends the API's string-valued `make_latest: "false"`; readbacks verify `draft: false` + `prerelease: true` directly; not-latest proven via a supported semantic observable (latest-release lookup must not resolve to this release) since `make_latest` is not persisted in readbacks; fixtures aligned | d309cdeb |
| 6 | MEDIUM | codex | AC6 — workflow-artifact ID and digest handoff | accepted — patched: outer fields exported via explicit step-to-job output mappings; publish-release gets `actions: read`, downloads via the Actions artifact API or a commit-SHA-pinned action by exact ID, verifies run ownership/name/expiry, hashes raw archive bytes against the exported digest before safe-path extraction; wrong-ID/wrong-run/expired/digest-mismatch fixtures added | d309cdeb |
| 7 | HIGH | codex-ops | AC4 — Add least-privilege CI and enforceable repository controls | accepted — patched: ci.yml and secret-scan.yml run on every PR targeting main and every push to main with no path filters or omitting conditions; check-run names pinned exactly to the job IDs; workflow-policy tests assert the trigger/name contract so protection can't stay pending or accept a wrong-SHA check | d309cdeb |
| 8 | HIGH | codex-ops | AC1/AC4 — full-history secret scanning | accepted — patched: hosted scan checks out with fetch-depth 0 + complete tag/ref fetch and runs a fail-closed preflight that fails on a shallow or ref-incomplete checkout; governance fixture proves incomplete history fails | d309cdeb |
| 9 | MEDIUM | codex-ops | AC1/AC3/AC4 — secret-scan executable and exit semantics | accepted — patched: single binary digest replaced with a per-platform (macOS + Ubuntu) SHA-256 digest map recorded in the migration record; unsupported OS/arch or digest mismatch fails before scanning; scan exits nonzero on leaks and on scanner-infrastructure failure with no redaction pipeline masking the exit status | d309cdeb |
| 10 | HIGH | codex-ops | AC6 — protected source-release environment | accepted — patched: environment is proven, not named — API readback recorded in the migration record must show reviewer set exactly {zhenye0616}, prevent-self-review deliberately disabled (founder dispatches and approves; deviation recorded), no admin bypass, main-only deployment-branch policy; unenforceable/unexposable fields stop for founder disposition | d309cdeb |
| 11 | HIGH | codex-ops | AC6 — workflow-dispatch inputs and approval tuple | accepted — same patch as #2: exact version equality across dispatch input, package.json at approved SHA, manifest, asset names, tuple, tag, and release name, enforced before upload/approval with zero publication writes on failure | d309cdeb |
| 12 | MEDIUM | codex-ops | AC6 — run_attempt rerun rejection | accepted — same patch as #1: observable rejected state = rerun-guard job's failed conclusion failing the run; every substantive job independently gated on `run_attempt == 1`; full-workflow and failed-job rerun fixtures prove zero checkout, upload, approval, or repository mutation | d309cdeb |
| 13 | MEDIUM | codex-ops | AC6 — release API identity and flag readback | accepted — same patch as #4/#5: draft `target_commitish` = approved source SHA, string-valued `make_latest: "false"` on the wire, authoritative not-latest observable via latest-release lookup; wrong-target and contradictory-latest (latest-lookup-resolves) fixtures added | d309cdeb |

## Convergence call

needs R7 — focus_hints: verify the r6 propagation-completion patches: layered rerun rejection (permissions-empty rerun-guard job that fails on `run_attempt != 1`, every other job needs the guard + its own `run_attempt == 1` condition — failed conclusion, never all-skipped green); dispatch version input bound to package.json's version at the approved source SHA before any upload/approval; manifest hash derived once in build-artifact with every verifier recomputing canonical-JSON SHA-256; release identity (draft `target_commitish` = approved source SHA, no implicit tag at draft, ref→tag-object→peeled-commit readback, string `make_latest: "false"` on the wire, not-latest via latest-release lookup); artifact handoff (step-to-job outputs, `actions: read`, API/pinned-action download by exact ID, run-ownership/name/expiry checks, raw-archive digest before extraction); AC4/AC1 governance (exact PR+push-to-main trigger contract with pinned check names, fetch-depth-0 + fail-closed shallow preflight, per-platform gitleaks digest map, unmaskable nonzero scan exits); protected-environment API readback (reviewer set exactly zhenye0616, prevent-self-review deliberately disabled, no admin bypass, main-only) with fail-closed stop on unenforceable tiers; Tests bullet alignment for all of the above.

