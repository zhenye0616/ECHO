---
item_id: "2026-07-15-136-echo-context-canonical-repository-release-substrate"
round: 3
reviewer: "claude"
artifact_sha: "9cc29b1493659f8b3cbb433633232448aad3ae2c"
completed_at: '2026-07-15T22:53:13Z'
verdict: "proceed"
findings: []
---

# claude review — 2026-07-15-136 r3 (conceptual / architectural-drift lens)

Reviewed `backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.md` at `9cc29b1493659f8b3cbb433633232448aad3ae2c`, verifying the r2 patch commit `0f05a7ce` against the five focus-hint areas plus a fresh conceptual pass over the patched AC3/AC6/Tests text.

## Verdict rationale

All four r2 dispositions (plus the row-5 nit resolution) are faithfully realized in the patched text, all five focus-hint verifications pass, and the patches are pure completions of the existing r1 mechanisms — no new job, asset, tool, or external surface was introduced. No conceptual, scope, authority, or cross-item drift found. Proceed with zero findings.

## Focus-hint verification

1. **AC6 dispatch guard — verified.** The run now fails unless `github.ref` is `refs/heads/main`, `github.sha` equals the source-SHA input, AND both equal the canonical main HEAD re-read through the hosting API at run start — a triple agreement that binds the executed workflow definition, the dispatched ref, and the reviewed landed SHA to one object. The source-release protected environment's deployment-branch policy is restricted to main, read back through the API, and recorded in the migration record. This closes the r2 row-1 gap (a dispatch against a non-main ref could previously execute an unreviewed workflow definition) without altering the workflow/job shape, and it is coherent with the pre-existing "only a fresh detached clone of that landed main SHA may enter the release gate" sentence — the guards are the enforcement of that sentence, not a parallel mechanism.
2. **Tuple handoff — verified.** build-artifact now (a) fails on any mismatch between computed lock/manifest hashes and the dispatch-input expected values, (b) writes the complete eight-field tuple to the run summary AND to a machine-readable tuple record persisted inside the same workflow artifact — explicitly a workflow-only record, not a fourth release asset, so the AC5 three-asset contract survives intact — and (c) exports every tuple field as job outputs. publish-release downloads by the exact artifact ID exported from build-artifact and revalidates every tuple field against the approved record. The stated rationale ("a protected-environment approval does not itself encode what was approved") is exactly right: the approval is now bound to immutable bytes rather than to a human's memory of a run summary. The chain producer (build-artifact) → carrier (immutable artifact + job outputs) → approver (founder over the presented tuple) → consumer (publish-release revalidation) is closed with no unbound edge.
3. **Publication staging — verified.** The pre-first-write API re-read now includes canonical main HEAD (fails unless the approved source SHA still equals main HEAD — a deliberate conservative choice that invalidates a pending approval if main advances; correct for a single-artifact DEV release). Publication is draft-staged: all three assets upload to an unpublished draft and each uploaded asset's hash is verified against the approved tuple before the annotated tag or published release becomes visible — the externally visible state transition happens only after verification. Failure-after-write behavior is complete in both directions: same-run cleanup of own draft/partial uploads where the API permits, and any surviving partial state is recorded and stops subsequent release runs for founder disposition. This resolves the r2 row-3 dead-end (a failed run's partial tag/asset would previously trip the no-clobber rule on every retry with no recovery path) via the repo's standing pattern for irreversible external mutations: founder-gated disposition, never silent adoption or silent overwrite.
4. **Fresh-clone two-mode contract — consistent across AC3/AC6/Tests.** AC3 defines the full argument contract: `--mode=source --source-sha <sha>` builds the artifact locally via tools/build-source-artifact.mjs (so `verify:artifact` always has a defined input — closing the r2 row-4 undefined-input gap), and `--mode=release --source-sha <sha> --version <version> --archive <path> --checksum <path> --manifest <path>` verifies the three downloaded AC5 assets instead of building. Missing or extra arguments fail with usage text rather than skipping a check; `test:operator` is never invoked by either mode — which also textually retires my r2 nit (the "drives only these named scripts" over-reading) exactly as the row-5 disposition claimed. AC6's fresh-clone paragraph invokes `tools/fresh-clone-acceptance.sh --mode=release` with the downloaded asset paths as explicit arguments, and the Tests bullet runs both modes plus the negative missing-mode / missing-arg / checksum-manifest-mismatch cases. The three sections name the same modes, the same arguments, and the same failure semantics; no orphan or contradiction.
5. **No new mechanism — verified against the r1→r2 diff.** The diff touches exactly three regions (AC3 script contract, AC6 release paragraphs, Tests bullets). The workflow roster (ci.yml, secret-scan.yml, source-release.yml), job IDs (quality-macos, quality-ubuntu, secret-scan, build-artifact, publish-release), three-asset release shape, and tool roster are unchanged; the tuple record lives inside the existing workflow artifact, the draft release is a staging state of the existing publication step, and the two-mode contract parameterizes the existing acceptance script using the already-specced AC5 builder. This is constraint completion, not scaffolding growth — the strategist-drift check (prefer removal over deeper patching) is satisfied here because the r2 reframe-gate investigation established that these mechanisms are load-bearing r1 controls whose removal would reopen accepted r1 HIGH findings, and the patches add edges to those controls rather than new mechanism beside them.

## Fresh conceptual pass on the patch deltas

- **Authority boundaries unchanged.** The patches touch only release-path mechanics. The authority split (echo-context/main = source + source-artifact authority; Project_echo = installed runtime + live-state authority), DEV maturity, installable:false, the 137 tuple handoff, and the Out of Scope section are byte-unchanged from the r2-reviewed state. No patch grants any new external mutation surface; every external write remains founder-gated with readback.
- **Actor bindings survive the patches.** The staged-publication and tuple-record language keeps publication inside the guarded workflow under founder environment approval; the partial-state stop routes to founder disposition, not to builder discretion. No reading of the new text authorizes a builder identity to push, tag, publish, or adopt partial state.
- **Internal-asset scope guard intact.** Nothing in the r2 deltas serves anything beyond the 136–139 successor-repository lane; no Team-product maturity movement, no client-facing surface, no live-state read. The narrow-lane authorization (2026-07-15 successor-repository execution decision) still covers every external mutation the spec orders.
- **Risks section still honest.** The pre-existing hosting-tier stop path ("controls may not be enforceable... builder stops for a founder decision") covers the r2 investigator's flagged residual — if GitHub environment/artifact/draft-release semantics cannot support the immutable-tuple or draft-stage contract as specced, the spec already fails closed to founder disposition rather than silently weakening. No prose addition needed.

## Cross-check against r2 findings

r2 rows 1–4 (codex, MEDIUM) are each patched precisely as the disposition table describes — no partial application, no drift between disposition rationale and landed text. Row 5 (my r2 nit) is resolved by the row-4 command-surface rewrite with no standalone patch, exactly as dispositioned.
