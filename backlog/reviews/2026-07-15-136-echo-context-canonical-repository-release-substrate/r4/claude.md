---
item_id: "2026-07-15-136-echo-context-canonical-repository-release-substrate"
round: 4
reviewer: "claude"
artifact_sha: "9997f07362d9fa7849c4069642019c536657ff77"
completed_at: '2026-07-15T23:13:27Z'
verdict: "proceed"
findings: []
---

# claude review — 2026-07-15-136 r4 (conceptual / architectural-drift lens)

Reviewed `backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.md` at `9997f07362d9fa7849c4069642019c536657ff77`, verifying the r3 patch commit (same SHA) against the five focus-hint areas plus a fresh conceptual pass over the patched AC3/AC5/AC6/Tests text.

## Verdict rationale

All four r3 dispositions are faithfully realized in the patched text, all five focus-hint verifications pass, and the r3 combined's `propagation_completion` framing is confirmed by the landed diff: every delta completes an existing r1/r2 control at an edge platform ordering or interruption semantics forces open — none adds mechanism beside those controls. Hash vocabulary is now globally consistent with exactly one occurrence of unqualified "artifact SHA-256" in the whole artifact, and that occurrence is the prohibition sentence itself. No conceptual, scope, authority, or cross-item drift found. Proceed with zero findings.

## Focus-hint verification

1. **Inner/outer tuple layering — verified.** AC6 now states the causal reason for the split ("the hosting platform assigns a workflow artifact's ID only after its upload completes") and layers the record accordingly: the pre-upload seven-field inner tuple (source SHA, source tree, version, source-archive SHA-256, lock hash, manifest hash, run ID) is written to a machine-readable record persisted inside the workflow artifact alongside the three AC5 files — explicitly a workflow-only record, not a fourth release asset — and the two outer fields (workflow artifact ID, workflow-artifact digest) are captured post-upload to the run summary with every inner and outer field exported as job outputs. Founder approval is over exactly the presented nine-field tuple (7 + 2 = 9; arithmetic and field rosters agree at every mention). publish-release downloads by the exact exported artifact ID, verifies the artifact bytes against the exported workflow-artifact digest, re-hashes each contained file, and revalidates every inner- and outer-tuple field against the approved record. The producer → carrier → approver → consumer chain that r2/r3 built is now closed at the one edge the platform's ID-after-upload ordering had left unbound, and the AC5 three-release-asset contract survives intact (AC5 "exactly three sidecar files"; AC6 "uploads all three assets"; fresh clone "downloads the three private prerelease assets").
2. **AC3 command surface — verified.** The script roster now names `build:artifact` (node tools/build-source-artifact.mjs) with an exact argument contract (`--source-sha <sha>`, `--out <dir>`, version derived from the package.json version field in the committed tree at that source SHA — coherent with AC5's committed-objects-never-working-tree rule), gives `verify:artifact` its own exact three-argument interface, and pins the acceptance script's command surface to exactly `npm ci`, the named package.json scripts, and `git fsck --full` with "it invokes no other executable." Source mode runs `build:artifact` into a temporary gitignored directory inside the clone (defined input for `verify:artifact`); release mode "never invokes `build:artifact`" — build-once preserved on the release path. The argument grammar failure set is widened to unknown/missing/duplicate/wrong-mode, and the Tests bullet carries the matching negative cases plus the two behavioral non-invocation assertions (release mode never builds; neither mode invokes `test:operator`), so both surface bans are test-pinned rather than prose-only.
3. **Publication staging — verified.** The concurrency group is now explicitly non-cancelling ("queued runs wait, and an in-progress release run is never auto-cancelled by a newer dispatch"), closing the interruption vector where a newer dispatch could orphan a mid-publication run. Run-owned identifiers (draft release ID, uploaded asset IDs, tag name) are logged at creation time, so external writes stay attributable even after cancellation or runner loss. The pre-first-write preflight now lists ALL tags, releases (draft or published), and assets and stops for founder disposition on any release-namespace state of any version not attributed to a completed prior release in the migration record — the detection burden moves from the dying run (which can never guarantee cleanup) to the next run (which always can check), and the text says exactly that. Same-run cleanup is retained as best-effort only ("where the API permits... which same-run cleanup can never guarantee to reach"). The recovery posture stays the repo's standing pattern for irreversible external mutations: founder-gated disposition, never silent adoption, never silent overwrite.
4. **Hash naming — verified globally.** AC5 defines the four distinct names (source-archive SHA-256 = digest of the .tgz, bound by the .sha256 sidecar; manifest hash = SHA-256 of the manifest's canonical-JSON bytes; lock hash = SHA-256 of the package-lock.json blob at the source SHA; workflow-artifact digest = digest of the uploaded CI artifact). A full-text sweep finds every hash mention in AC5/AC6/handoff/migration-record/Tests uses one of the four qualified names; the only unqualified "artifact SHA-256" in the artifact is the sentence prohibiting it, and the only other bare SHA-256 mentions are the AC1 gitleaks binary pin and commit SHAs (both correctly out of vocabulary scope). The dispatch-input expected-hash producers are named with exact commands (lock hash via `git cat-file blob <sha>:package-lock.json | shasum -a 256`; manifest hash via a local `build:artifact` run against a fresh detached clone of the landed SHA), and the build-once tension is resolved correctly by grounding the local computation in AC5's test-proven determinism — a verification computation, not a second release build; the text states "the release artifact itself is still built exactly once, inside build-artifact." The 137 handoff is clarified as the six-field immutable content tuple, with run ID and the outer workflow-artifact fields explicitly demoted to release-run provenance — the right conceptual cut, since item 137 consumes content identity, not run provenance.
5. **No new mechanism — verified against the r2→r3 diff.** The diff is 8 changed lines across exactly the four dispositioned regions (AC3 script contract, AC5 hash vocabulary, AC6 release paragraphs, Tests bullets). The workflow roster, five job IDs, three-asset release shape, protected-environment gate, and tool roster are all unchanged; the only new named surface is the `build:artifact` npm script, which wraps the pre-existing AC5 builder rather than introducing a new tool. Splitting an existing tuple into layers, qualifying an existing concurrency group, extending an existing preflight, and naming existing hashes are constraint completions, not scaffolding growth — the strategist-drift check (prefer removal over deeper patching) is satisfied because the r3 reframe-gate investigator already established these mechanisms are load-bearing r1 controls, and this diff adds edges to them, not mechanism beside them.

## Fresh conceptual pass on the patch deltas

- **Authority boundaries unchanged.** The r3 deltas touch only release-path mechanics and vocabulary. The authority split (echo-context/main = source + source-artifact authority; Project_echo = installed runtime + live-state authority), DEV maturity, installable:false, and the Out of Scope section are byte-unchanged. No delta grants any new external mutation surface; every external write remains founder-gated with readback, and the narrow-lane authorization (2026-07-15 successor-repository execution decision) still covers everything the spec orders.
- **Actor bindings survive.** The dispatching founder/operator's expected-hash computation is a read-only local computation recorded in the migration record; publication stays inside the guarded workflow under founder environment approval; partial-state stops route to founder disposition, not builder discretion. No new text authorizes a builder identity to push, tag, publish, or adopt partial state.
- **Cross-item coherence.** The six-field content tuple matches what item 137's exact-artifact contract needs and excludes what it must not depend on (run-scoped identifiers that die with workflow-artifact retention). The `build:artifact` script strengthens rather than weakens the 137 seam — the dispatch-input manifest-hash producer and the workflow build now share one named entry point.
- **Out of Scope honesty holds.** The new script surface could tempt "while we're here" packaging conveniences, but AC5's non-installable constraints plus the existing "no npm/public publication... no generic release framework" bullet already forbid the adjacencies; no addition needed.

## Cross-check against r3 findings

r3 rows 1–4 (codex: 1 HIGH, 3 MEDIUM) are each patched precisely as the disposition table describes — no partial application, no drift between disposition rationale and landed text, and no residue of the pre-split eight-field tuple language anywhere in the artifact.
