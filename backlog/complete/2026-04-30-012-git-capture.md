---
id: 2026-04-30-012-git-capture
title: Git capture surface (commits via refs watch)
status: ready
priority: HIGH
estimate: 1d
created: 2026-04-30
spec_refs:
  - wiki/entities/capture-gate.md
  - wiki/entities/storage.md
  - wiki/sources/capture-allowlist.md
blocked_by:
  - 2026-04-30-006-capture-pipeline
  - 2026-04-30-007-daemon-entry
  - 2026-04-30-008-sqlite-storage
acceptance:
  - "`startGitWatcher(repoPaths, storage): Promise<{stop}>` exported from `src/capture/surfaces/git-watcher.ts`"
  - "Watches `.git/refs/heads/` and `.git/HEAD` of each allowlisted repo via chokidar; on change, runs `git log` to find new commits"
  - "Polling fallback: every 30s, runs `git rev-parse HEAD` per repo as a safety net for missed events"
  - "Per-repo state: in-memory `Map<repo_path, last_seen_sha>`, backfilled from storage on boot via `storage.query({source: 'git:<repo>', limit: 1, ...})`"
  - "Emits one `CandidateEvent` per new commit, in chronological order"
  - "Content shape: `COMMIT <short_sha>: <subject>\\n\\n<body>\\n\\n--- DIFF ---\\n<unified_diff>`"
  - "source: `git:<absolute_repo_path>`"
  - "metadata: `{ sha, parent_sha, author, files_changed, additions, deletions }`"
  - "`CAPTURED_SOURCES` extended with new `git_repos` category — exact-match list of repo paths"
  - "Initial `git_repos` entry: `~/Desktop/Project_echo/`"
  - "Allowlist predicate `isAllowedRepo(path): boolean` added to `sources.ts` with the same defensive shape as the others (returns false on malformed input, expands `~`)"
  - "Source kind `git` registered in the gate's source-prefix map (this DOES require touching the gate; it's a deliberate extension since git is a new source kind, not an FS event)"
  - "Tests cover: parsing commits from a tempdir git repo; multiple commits on multiple branches; rebase produces commits as new SHAs (we don't dedup by tree); polling fallback runs"
  - "Lag: new commit → CaptureEvent in storage within 1s (verify in agent_notes)"
  - "`npm run test`, `npm run lint`, `npm run typecheck` clean"
files_to_modify:
  - src/capture/surfaces/git-watcher.ts
  - src/capture/sources.ts
  - src/capture/gate.ts
  - src/daemon/index.ts
  - tests/capture/surfaces/git-watcher.test.ts

claimed_by: "Mac.attlocal.net-zhenye"
claimed_at: "2026-04-30T22:08:00Z"
branch: "agent/012-git-capture"
worktree: "Project_echo--012-git-capture"
head_sha: "09f6b1ba938af60a810f26508f89ee43bc9c4678"
pr_url: ""
agent_notes: |
  All acceptance criteria met. 119/119 tests pass; lint and typecheck clean.

  Implementation summary:
  - src/capture/surfaces/git-watcher.ts (new): chokidar watches .git/refs/heads/**
    and .git/HEAD per repo; 30s polling fallback; per-repo last_seen_sha cache
    backfilled from storage on boot; commit enumeration via `git log --reverse`
    with %H/%P/%aI/%an/%s/%b fields delimited by 0x1F + 0x1E records (safe for
    multi-line commit bodies); diffs >100 KB truncated with marker.
  - src/capture/sources.ts: new git_repos category (initial entry
    "~/Desktop/Project_echo/"); _isAllowedRepoIn / isAllowedRepo predicates
    (defensive on malformed input, expand ~, exact-match on normalized form).
  - src/capture/gate.ts: 'git' source kind registered; new 'unknown_repo'
    rejection reason added to RejectionReason union.
  - src/daemon/index.ts: registers startGitWatcher AFTER startLifecycle
    completes so SIGINT/SIGTERM can shut down cleanly even mid-backfill.
  - tests/capture/surfaces/git-watcher.test.ts: 12 tests covering backfill,
    resumption from last_seen_sha, polling fallback (with FS watch disabled),
    100KB diff truncation, gate accept/reject of git: sources, exact-match
    isAllowedRepo, ECHO_GIT_BACKFILL_COMMITS env override, fs-watching
    catches a fresh commit with poll interval set high enough that only FS
    events plausibly trigger capture.
  - package.json: added chokidar ^5.0.0 dependency.

  Notable design choices (not pre-specified, called out for review):
  1. startGitWatcher returns immediately after FS watchers + poll interval are
     set up; the initial backfill runs in the background. stop() awaits any
     in-flight refreshRepo via an inFlight Set + Promise.allSettled. This was
     necessary so SIGINT during backfill (~50 commits, several seconds of git
     subprocesses) still produces a clean lifecycle shutdown — without this,
     the SIGINT lifecycle test timed out at 20s.
  2. Added a non-spec'd third optional parameter `options: GitWatcherOptions`
     to startGitWatcher with `pollIntervalMs` and `enableFsWatch`. Production
     daemon does not pass either (uses 30 000 ms / true). Tests use these to
     get fast / deterministic coverage of the polling path. Spec signature
     `startGitWatcher(repoPaths, storage)` remains compatible — third param
     is optional.
  3. Spec mentions storage `{limit: 1, sort: 'timestamp DESC'}` for last-seen
     discovery, but the Storage interface (item 008) has no `sort` field.
     Worked around by querying all events for the source and picking the
     entry with maximum timestamp. Cost is linear-scan in MemoryStorage and
     equivalent in SqliteStorage; for the expected per-repo event count
     (~hundreds) this is fine. If sort becomes needed, add to QueryFilter
     in a follow-up item rather than an ad-hoc workaround here.
  4. Branch was forked from main BEFORE 009 merged. Therefore my changes to
     src/capture/sources.ts, src/daemon/index.ts, and package.json all
     overlap textually with 009's changes (which now sit at 87657fc on main
     in pending_review). Founder will need to resolve conflicts on merge —
     specifically, fs_paths and git_repos both belong in CAPTURED_SOURCES,
     and the daemon's onShutdown should chain stop() calls in
     fs-watcher → git-watcher → sqlite order. chokidar versions match (^5.0.0).

  LAG VERIFICATION (founder, manual): see acceptance — measure
  `commit → CaptureEvent` lag over 5 trials in Project_echo with the daemon
  running. Target: ≤1s median. Test scaffolding (waitForCount with 50 ms poll)
  shows in-test latency from commit to processCandidate is well under 1 s on
  a tempdir repo, but founder confirmation against the real Project_echo repo
  is required before the wiki entity page is written.

  Out-of-scope items observed but not implemented (per spec):
  - Working-tree state, branch/tag operations, remote ops, submodules, GitHub
    API integration, beyond-backfill history, cross-repo dedup, squash detection,
    additional repos beyond Project_echo, non-git VCS.
review_notes: |
  Merged on 2026-04-30 via founder reconciliation:
  - sources.ts: auto-merged cleanly — kept main's fs_paths AND added 012's
    git_repos. expandTilde stays exported (main's value won the line conflict).
  - daemon/index.ts: hand-merged. Both watchers register BEFORE startLifecycle
    (matches 009's pattern, closes the SIGINT-during-init race the reviewer
    flagged). onShutdown chain: gitWatcher.stop() → fsWatcher.stop() → dispose().
  - package.json: no conflict (both branches added chokidar ^5.0.0).
  - Pre-merge nit applied: pollHandle.unref() in git-watcher.ts so the 30s
    timer doesn't pin the event loop in test harnesses.

  132/132 tests pass; lint and typecheck clean post-merge.

  Follow-up items deferred (non-blocking):
  - Add `sort` field to QueryFilter so discoverLastSeen can drop the
    linear-scan workaround.
  - Founder lag verification on real Project_echo repo (target ≤1s) before
    the wiki page is written.
  - Object.freeze() the CAPTURED_SOURCES.* arrays once tests no longer mutate.
---

# Git capture surface (commits via refs watch)

## What

A new capture surface that watches the user's local git repositories for new commits. Each commit becomes one `CaptureEvent` with content = commit message + unified diff. Independent of the chat extractors; both surfaces feed the same storage.

```ts
// src/capture/surfaces/git-watcher.ts
export async function startGitWatcher(
  repoPaths: string[],
  storage: Storage,
): Promise<{ stop: () => Promise<void> }>;
```

Detection:

1. **Real-time:** for each repo, watch `.git/refs/heads/*` and `.git/HEAD` via chokidar. When any of these changes, the head moved (commit, branch switch, rebase). Run `git log <new_head> ^<last_seen_sha> --reverse --format=...` to enumerate new commits.
2. **Polling fallback:** every 30s, run `git rev-parse HEAD` per repo. If different from `last_seen_sha`, kick off the same enumerate-and-emit logic. Catches any missed FS events.

Content per commit:

```
COMMIT <abbrev_sha>: <subject line>

<body, if any>

--- DIFF ---
<unified diff against parent — git diff <parent>..<sha>>
```

Truncate the diff at a reasonable size (e.g., 100 KB) with a `[truncated]` marker — large refactors shouldn't blow up storage in a single event.

## Why

The user's product call: separate "what was discussed" (chat extractors) from "what actually changed" (git). Git is the canonical record of code change — assistants sometimes claim to have edited files they didn't, or partially edit. Git records reality.

This separation also keeps each capture surface simple: chat extractors don't have to detect / parse / extract diffs. Git capture doesn't have to understand chat. Both produce identically-shaped envelopes; the MCP retrieval tool joins them by timestamp at query time.

A new `git_repos` allowlist category is added (option A from the strategist conversation): explicit, reviewable, scoped per-repo. Initial entry: `~/Desktop/Project_echo/` itself, since that's where the founder is dogfooding. Future repos land in their own per-repo PRs.

## Acceptance Criteria

- [ ] `src/capture/surfaces/git-watcher.ts` exports `startGitWatcher(repoPaths, storage)` returning `{ stop }`
- [ ] Real-time watch via chokidar on `.git/refs/heads/**` and `.git/HEAD` per repo (recursive on the refs/heads directory; chokidar handles file create/modify/delete)
- [ ] Polling fallback: `setInterval` every 30s, `git rev-parse HEAD` per repo, compare to in-memory last-seen-sha, trigger enumerate-and-emit if different
- [ ] **Commit enumeration:** when head moves, run `git log <new_head> ^<last_seen_sha> --reverse --format='%H%x09%P%x09%aI%x09%an%x09%s%x09%b'` to get new commits in chronological order. Use `child_process.execFile` to avoid shell injection. (`%x09` = TAB delimiter.)
- [ ] For each new commit:
  - Run `git show <sha> --no-color --format='' --stat` to get files-changed/additions/deletions
  - Run `git diff <parent>..<sha> --no-color` for the unified diff
  - Truncate diff at 100 KB with `\\n[diff truncated at 100 KB; full diff at <sha>]\\n`
  - Build CandidateEvent and `processCandidate(event, storage)`
  - On success: update `last_seen_sha` for that repo
- [ ] **Initial commit handling:** for repos with no prior storage events (`last_seen_sha = undefined`), capture the most recent N commits (default 50) as backfill on first boot, NOT the entire repo history. Configurable via env `ECHO_GIT_BACKFILL_COMMITS` (default 50).
- [ ] **Rebase handling:** rebases produce new SHAs. We capture them as if they were new commits. The original commits remain in storage; this is intentional (append-only audit trail). Acceptable duplicate of content because SHAs differ.
- [ ] On boot, daemon backfills `last_seen_sha` per repo by querying storage for `source: 'git:<repo>', limit: 1, sort: 'timestamp DESC'`
- [ ] `src/capture/sources.ts` updated:
  - Add `git_repos: ['~/Desktop/Project_echo/']` to `CAPTURED_SOURCES` (typed `as const`)
  - Add `isAllowedRepo(repoPath): boolean` predicate using `_isAllowedRepoIn` (mirror the pattern of the other predicates: defensive, tilde-expands, exact-match — repos must match exactly, not by prefix)
- [ ] `src/capture/gate.ts` updated:
  - Add `'git'` to `SOURCE_KIND_TO_PREDICATE` and `SOURCE_KIND_TO_REJECTION` maps
  - Add `'unknown_repo'` to `RejectionReason` (extends the union; existing rejection reasons unchanged)
  - Tests updated: a `git:<allowed>` source accepts; `git:<unknown>` rejects with `unknown_repo`
- [ ] Daemon registers `startGitWatcher` on boot, calls `stop()` on shutdown — same lifecycle pattern as item 009
- [ ] Tests in `tests/capture/surfaces/git-watcher.test.ts`:
  - Create temp dir, `git init`, make 3 commits → assert 3 events emitted in order
  - Make 2 more commits → assert only the 2 new ones emitted (resumption from last_seen_sha)
  - Polling fallback: simulate a missed FS event by writing directly to `.git/refs/heads/main` without triggering chokidar; assert next polling cycle catches it
  - Diff truncation: create a commit with a 200 KB diff; assert the stored content is truncated with the marker
  - Allowlist enforcement: a `git:` source with an unknown path rejects via the gate
  - Backfill: temp repo with 5 commits; first run captures all 5 (backfill < 50)
- [ ] Lag verification (founder, recorded in `agent_notes`): commit a real change in Project_echo; measure time-to-event-in-storage; median ≤1s over 5 trials
- [ ] `npm run test`, `npm run lint`, `npm run typecheck` clean

## Out of Scope (Don't Drift)

- **Working-tree state capture** — uncommitted changes are not captured. V1.5+ if useful. Most useful work crosses a commit boundary anyway.
- **Branch / tag operations standalone** — we capture commits, not "user switched to branch X." Branch state is implicit in commit metadata.
- **Remote operations** — pushes/fetches/pulls are not captured. The local commit is the canonical event.
- **Submodules** — V1.5+
- **GitHub integration** (PR comments, issue references) — separate Wave 4 item via the GitHub API
- **Pre-existing commits beyond the backfill window (default 50)** — older history is intentionally not captured. The user dogfoods forward.
- **Cross-repo dedup** (e.g., a commit cherry-picked across repos) — different SHAs, different `source` strings, treated as distinct events
- **Squash detection** — squashed commits are new commits with new SHAs; we capture them as such
- **Adding repos beyond Project_echo** — per-repo additions are separate items so each gets reviewed
- **Non-git VCS** — Mercurial, Pijul, etc. are out

## After Completion (Strategist Notes)

This item is meaningful: it's the second capture surface and the *first non-FS one*. The wiki updates should reflect that:

1. Create `wiki/entities/git-capture.md` documenting:
   - The refs-watching mechanism (chokidar + polling fallback)
   - The CandidateEvent shape for commits
   - The `git_repos` allowlist category and rationale
   - Diff truncation policy (100 KB cap)
   - Lag characteristics from `agent_notes`
   - Cross-references to [[capture-pipeline]], [[storage]], [[capture-allowlist]]
2. Update `wiki/sources/capture-allowlist.md`:
   - Document the `git_repos` category alongside `apps`, `domains`, `fs_paths`, `apis` (now five categories)
   - Note the per-repo addition convention
3. Update `wiki/concepts/sandboxed-capture.md`:
   - Add the new `unknown_repo` rejection reason to the stable codes list
   - Note the gate now handles a fifth source kind
4. Update `wiki/entities/capture-gate.md`:
   - Add `'git'` to the source-kind table
   - Add `unknown_repo` to the stable rejection codes table
5. Add the rationale for "diff source = git, not chat" to the Spec Authoring Lessons section in `backlog/README.md` so future capture-surface specs honor the separation
6. Update manifest + index
