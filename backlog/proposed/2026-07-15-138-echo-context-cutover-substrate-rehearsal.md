---
id: 2026-07-15-138-echo-context-cutover-substrate-rehearsal
title: "Deterministic echo-context cutover substrate and full-cycle rehearsal"
status: proposed
priority: HIGH
estimate: 6d
created: 2026-07-15
blocked_by:
  - 2026-07-15-137b-echo-context-real-shadow-install
task_state_ref: 2026-07-15-138-echo-context-cutover-substrate-rehearsal
requested_reviewers: ["codex", "codex-ops"]
files_to_modify:
  - /Users/zhenye/Desktop/echo-context/package.json # controller build/rehearsal entrypoints
  - /Users/zhenye/Desktop/echo-context/package-lock.json # exact controller dependency closure
  - /Users/zhenye/Desktop/echo-context/src/cutover/** # NEW phase machine, backup, migration, client, authority, rollback, and recutover
  - /Users/zhenye/Desktop/echo-context/schemas/** # NEW plan/checkpoint/backup/rollback/rehearsal schemas; consume 137 authority schema unchanged
  - /Users/zhenye/Desktop/echo-context/provenance/** # controller inventory and predecessor/runtime bindings
  - /Users/zhenye/Desktop/echo-context/tools/** # deterministic controller build/verify and guarded rehearsal
  - /Users/zhenye/Desktop/echo-context/tests/cutover/** # NEW mutation-boundary and full-cycle fixture tests
  - /Users/zhenye/Desktop/echo-context/docs/context-cutover.md # reviewed operator contract for item 139
  - package.json # residual/fenced-full package build and version identity
  - package-lock.json # exact Project_echo residual/rollback closure
  - src/daemon/index.ts # full-daemon authority fence before PID/DB/socket mutation
  - src/daemon/lifecycle.ts # owns pre-open side effects (data-dir mkdirSync, PID-file write); AC2's fence must execute before these, so fence hooks/reordering land here
  - src/daemon/legacy-residual.ts # NEW product/loop-only composition root
  - src/daemon/context-authority.ts # NEW canonical authority-record validator
  - src/mcp/server.ts # explicit context/full/residual roster profiles
  - src/storage/context-service.ts # NEW authenticated client constrained by the 137 consumer contract
  - src/storage/coord-store.ts # NEW residual coordination authority store/import
  - src/storage/coord-mirror.ts # NEW transactional outbox and idempotent context observation mirror
  - src/cli/commands/daemon.ts # packaged residual and fenced rollback-full definitions
  - src/cli/commands/init.ts # split endpoint/client naming defaults
  - src/cli/commands/selftest.ts # two-endpoint fixture verification
  - src/cli/inverse/cursor-config.ts # reversible split-client transform
  - src/echo-home/adapters/** # pure Codex/Claude/Cursor two-endpoint transforms
  - src/brain/brain.ts # explicit authenticated context endpoint caller
  - src/surfaces/ceo-slack-responder/responder.ts # explicit authenticated context/residual caller classification
  - skills/** # canonical explicit context-versus-residual endpoint callers
  - .claude/commands/** # generated Claude adapters from canonical skills
  - tools/install-echo-codex-skills.sh # rendered Codex adapter endpoint/auth contract
  - tools/coord-status.sh # residual endpoint/auth caller
  - tools/review-queue/** # residual coordination endpoint/auth callers
  - tools/context-cutover/** # NEW Project_echo-side artifact and rehearsal helpers
  - tests/daemon/** # residual composition and pre-open full-fence tests
  - tests/mcp/** # exact residual roster and descriptor-continuity tests
  - tests/storage/** # context service, coord import, outbox, and mirror tests
  - tests/echo-home/** # lossless client transform fixtures
  - tests/integration/context-cutover-rehearsal.test.ts # full old-to-new-to-old-to-new ceremony under scratch roots
  - raw/internal/migrations/2026-07-15-138-echo-context-cutover-rehearsal.md # NEW redacted cross-repo rehearsal record
  - raw/internal/agent-runs/** # workflow-owned failure/completion run log
  - backlog/task-state/2026-07-15-138-echo-context-cutover-substrate-rehearsal/** # workflow continuity pointers
  - backlog/proposed/2026-07-15-138-echo-context-cutover-substrate-rehearsal.md # proposal and review revisions
  - backlog/ready/2026-07-15-138-echo-context-cutover-substrate-rehearsal.md # watcher-owned promotion target
  - backlog/claimed/2026-07-15-138-echo-context-cutover-substrate-rehearsal.md # workflow claim target
  - backlog/pending_review/2026-07-15-138-echo-context-cutover-substrate-rehearsal.md # workflow handoff target
  - backlog/complete/2026-07-15-138-echo-context-cutover-substrate-rehearsal.md # AC8 evidence-only readback commit: landed-SHA frontmatter update after both landings
  - docs/BACKLOG.md # generated stage-derived index
spec_refs:
  - backlog/complete/2026-07-15-137a-echo-context-candidate-runtime.md # exact runtime/candidate contract and target source lineage
  - backlog/complete/2026-07-15-137b-echo-context-real-shadow-install.md # exact artifact, authority schema, consumer contract, receipt, and shadow proof
  - raw/internal/decisions/2026-07-17-echo-context-137-two-pass-scope-reset.md # predecessor split and stale-review boundary
  - backlog/complete/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # canonical target source/release authority
  - backlog/complete/2026-07-13-135-local-echo-context-source-extraction.md # context roster/state/capture boundary
  - backlog/complete/2026-07-13-134-local-echo-loop-source-extraction.md # coordination authority/store semantics
  - backlog/complete/2026-07-13-133-local-echo-brain-source-extraction.md # product ownership remains separate
  - raw/internal/decisions/2026-07-15-echo-context-successor-repository-execution.md # cross-repo worktree/review/landing protocol
  - raw/internal/decisions/2026-07-12-g2-terminal-dispositions-and-repository-topology.md # per-repo authority checkpoint
  - src/daemon/index.ts # current mixed composition/startup order
  - src/mcp/tools/coord-emit.ts # wait/search descriptor that the mirror must preserve
  - src/coord/deadlines.ts # full-ledger reconstruction and watermark semantics
  - src/echo-home/adapters/codex-config.ts # current single echo client key
  - src/echo-home/adapters/claude-code-mcp.ts # current single echo client key
  - src/echo-home/adapters/cursor-config.ts # current single echo client key
claimed_by: ""
claimed_at: ""
branch: ""
worktree: ""
head_sha: ""
pr_url: ""
agent_notes: ""
review_notes: ""
target_repo: "/Users/zhenye/Desktop/echo-context"
target_remote: "https://github.com/zhenye0616/echo-context"
target_branch: ""
target_worktree: ""
target_head_sha: ""
target_pr_url: ""
target_landed_sha: ""
project_landed_sha: ""
---

# Deterministic echo-context cutover substrate and full-cycle rehearsal

## Why this spec exists

Items 137a and 137b prove one exact echo-context runtime first as a disposable candidate and then as an authenticated, capture-off installed shadow. Live cutover still needs a Project_echo residual, an old-full authority fence, populated-state and coordination migration, client transforms, and rollback/recutover. Combining that source work with real credential/state/launchd mutation would execute unreviewed code and violate the normal backlog handoff.

This gate builds and independently reviews all cutover substrate in both canonical repositories, then exercises the complete ceremony against representative temporary state and fake service controls. It ends with authority:false and no founder-machine mutation. Item 139 alone builds the final operator/residual artifacts from the read-back landed SHAs and executes the live switch.

This internal substrate exists because unified machine context across Codex, Claude Code, and Cursor is required by ECHO's development and Team-product delivery workflow. It is not a parallel commercial product or maturity advancement.

## Acceptance Criteria

### AC1 — Implement one closed, replayable phase machine behind a hard mutation boundary

Add a controller with closed phases planned, source_fenced, backed_up, migrated, prepared, active, rolled_back, and recutover_prepared, admitting exactly two phase sequences plus one in-generation rollback edge: the initial generation planned → source_fenced → backed_up → migrated → prepared → active, the rollback edge active → rolled_back, and a fresh recutover generation rolled_back → recutover_prepared → source_fenced → backed_up → migrated → prepared → active. One exhaustive per-phase table is normative for the whole item — every phase names its sole permitted next transition, its old-full-mode admission, and its resume re-proof set, with no cell left implicit: planned → source_fenced (full mode permitted, no authorization; resume replays the journal only, with no fence or backup re-proof); source_fenced → backed_up (full mode rejected; resume re-proves the fence, zero-writer state, and source identity/cut/digest — a completed backup is not required, and an authenticated partial backup is reconciled or discarded, never trusted); backed_up → migrated (rejected; adds the authenticated complete backup plus scratch restore); migrated → prepared (rejected; adds migration output digests); prepared → active (rejected; adds projection, service, client-CAS, and readiness re-validation); active → rolled_back (rejected; resume proves the new authority holds with exactly one context writer); rolled_back → recutover_prepared (full mode permitted only with the exact durable rollback authorization defined in AC2; resume proves the restored old authority — restored start jobs, clients, quarantine — and does not require zero old writers, because the old stack is authoritative again); recutover_prepared → source_fenced (full mode permitted, no authorization; resume replays the journal only and re-validates the G2 plan). An absent canonical generation permits full mode and admits only initialization. Resume re-proofs are generation-bound: a G2 resume validates against the G2 plan while the G1 rolled_back projection is retained until G2's prepared commit atomically replaces it. Committing active → rolled_back is ordered: the rollback interval exports, client and quarantine restoration, and AC7's launchd service-control before-image restoration are durably journaled complete before the rolled_back flip commits, and old-full readiness is proven after the flip through the restored start job before rollback is declared complete. source_fenced is a controller phase only, not a new item-137 context-authority.v1 state: the item-137 authority projection is absent before prepared and remains sealed to exactly prepared/active/rolled_back afterward. One canonical transaction record on one filesystem is the commit point; immutable configs and non-authoritative projection records reference its generation/hash. Every filesystem, SQLite, service-control, client-config, clock, secret, process, port, and artifact operation is behind an injected interface with precondition/result journaling; durable journaling begins only after the root descriptor is trusted and the per-root authority lock is held. Crash replay either finishes the current idempotent step or restores its protected before image; it never advances two authorities.

The only mutation-capable command this item ever executes outside fixture tests is the rehearsal entrypoint (AC5's `candidates:build`/`candidates:verify` are artifact-output-only commands classified there, and AC5's approval-gated production execute/resume entrypoints run in this item only inside fixture tests against rehearsal roots and fake adapters): package script `rehearse` in /Users/zhenye/Desktop/echo-context/package.json, run as `npm run rehearse -- --root <path>` with the echo-context checkout as working directory, writing only under the supplied root, exiting 0 only on a completed ceremony and non-zero on every failure. The canonical transaction record and its commit temporary are the exact reserved root-top-level names `transaction.json` and `transaction.json.tmp`. A `--root` naming a nonexistent path or empty directory initializes a new rehearsal; a root whose only entry is a valid `authority.lock` (exactly that name, a regular non-symlink file validated descriptor-relative) is the lock-only bootstrap state — the crash window between lock creation and the first planned-record commit — and resumes initialization under that lock; a root containing a valid `transaction.json` resumes crash replay of that rehearsal; a root containing `authority.lock` plus only a `transaction.json.tmp` (interrupted first planned commit), or a valid `transaction.json` plus a `transaction.json.tmp` (interrupted replacement), is the strictly validated interrupted-commit state: the temporary is never trusted as authority because the atomic rename is the sole commit point, and reconciliation — under the held lock, descriptor-relative — validates the temporary exactly before touching it: the exact reserved name, regular non-symlink type, owner uid equal to the invoking euid, mode 0600, link count 1, the same filesystem device as the root, size within a 1 MiB bound, a canonically parseable record schema, and a predecessor binding naming the hash/generation/phase of the record it was replacing; only a temporary passing every check is unlinked so resume proceeds from `transaction.json` when present or from lock-only initialization when absent, while a temporary failing any check is left untouched and the run fails closed with redacted durable evidence; any other root content is rejected. Every canonical transaction record commit is durable (write to `transaction.json.tmp`, fsync, atomic rename onto `transaction.json`, parent-directory fsync) and performed while `authority.lock` is held; interrupted temp-record reconciliation applies at every commit boundary including the first planned commit, and kill tests cover every bootstrap and commit boundary: before lock creation, between lock creation and the first planned-record commit, after temp creation, after temp write, after temp fsync, immediately before and immediately after the rename, and across each record replacement, for both the first record and every later replacement. Root validation is descriptor-relative and refuses symlinked components. One stable per-root authority lock — the sibling lock file `authority.lock` at the root top level, opened descriptor-relative with no-follow semantics and created if absent, never atomically replaced or deleted for the life of the root — prevents two rehearse processes from binding the same root, is the same lock object that serializes AC2's authority-fence decision, and is the one machine-wide execute/resume lock item 139 consumes: no second lock object exists in either package. Its interprocess exclusion primitive is BSD `flock(2)` `LOCK_EX|LOCK_NB` on that descriptor, provided through one named shared dependency: `fs-ext` (npm `fs-ext`, upstream baudehlo/node-fs-ext), pinned at one exact version with its sha512 integrity recorded identically in both package-lock.json files, its consumed API surface pinned to `flock`/`flockSync(fd, 'exnb')`, prebuilt at candidate build for Darwin x64 against the pinned Node ABI, and packaged as the sole allowlisted prebuilt native member at a recorded path and manifest-bound hash (AC5's native-member rejection admits exactly that member, and a test proves both packaged binaries load flock from it and contend the identical inode); acquisition polls at a fixed 100 ms interval under a 30-second monotonic-clock deadline (never wall clock) and exits with code 75 on timeout; after acquisition the held descriptor is re-`fstat`ed and its device/inode compared against a fresh descriptor-relative no-follow open of the same name to detect replacement; the descriptor is opened close-on-exec, never inherited by spawned children, held for the entire critical section, and closed — releasing the lock, including via kernel cleanup on crash — on every exit path. Real multiprocess tests prove both packaged binaries contend the identical inode, exclusion never overlaps, and a contender against a stuck holder exits with code 75 within the 30-second deadline. Lock bootstrap follows a creator/contender rule: the lock file is opened `O_CREAT` (non-exclusive) so racing processes converge on one inode, and creating the empty sole `authority.lock` is the one exempted pre-acquisition root write — idempotent, never deleted, and explicitly excluded from the timeout zero-write guarantee. A lock-acquisition timeout therefore writes nothing under the root beyond that sole-lock exemption — a redacted reason on stderr and exit code 75 are its only other outputs, consistent with durable journaling being permitted only while the lock is held; a multiprocess creator-loser test proves the process that created the lock file, lost the flock race, and timed out exits with the pinned code having written nothing but the empty lock file; failures after acquisition journal their redacted record under the root while the lock is still held, then release it. The canonical transaction record itself is never a lock target because it is absent in allowed startup states and atomically replaced at commit points. A production mutation guard rejects the real HOME, /Users/zhenye/Library, ~/.echo*, real client configs, launchctl GUI domains, ports 38478/38479, non-fixture processes, and any path outside the supplied root before file/socket/spawn mutation. Guard rejections and root-validation failures that occur before the root is trusted write nothing controller-owned anywhere — no durable journal entry, record, temporary file, cache write, or secondary log created by controller code in any location: they emit a redacted reason on stderr and exit non-zero with zero controller-originated filesystem mutation — the exit code plus captured stderr is the unattended-run contract. The zero-write promise is scoped to controller-owned writes: the npm/Node launcher's own cache and log behavior is outside the controller's authority, and the sentinel temp/cache/log matrix asserts the absence of controller-created entries around the advertised `npm run rehearse` invocation itself. After the root is trusted, every unrecoverable replay stop writes a redacted phase/error record under the root and exits non-zero. The rehearse command is permanently root-scoped and fake-service-only. The shipped guarantee is no ungated or bypass entrypoint: every mutation-capable entrypoint in the controller archive is gated — rehearse by the production mutation guard permanently, and AC5's execute/resume production entrypoints by their separately authenticated approval input that only item 139's reviewed authorization path can produce — with no flag, environment override, or unlisted entrypoint bypassing either gate; item 138 never invokes the execute/resume entrypoints against live adapters, and live mutation becomes possible only through item 139's separately reviewed exact-artifact/execute authorization path. The controller consumes item 137b's completed context-authority schema unchanged and cannot create a live record without item 139's exact artifact/execute authorization.

Committing source_fenced is the controller's source-fence step, executed while holding `authority.lock` (through the fake service control and fixture processes in rehearsal): it verifies the installed authority-root identity record and the exact fenced package identity; neutralizes every old-full direct start path; boots out and persistently disables each old start job, relocating its plist into the protected generation snapshot; stops the mixed daemon/shadow processes; and proves zero matching jobs, PIDs, and listeners and zero writers on the source context DB, WAL, SHM, and allowlisted sidecars. The source_fenced record binds the durable fence identity, the source identity/cut/digest, and the service-control readback. No checkpoint, backup, migration, client transform, prepare-final, or prepared-service start may execute before source_fenced commits, and a no-source-mutation-before-source_fenced test proves it. Every resume reacquires the same `authority.lock` and applies exactly its phase's re-proof set from AC1's per-phase table — in particular a source_fenced resume re-proves the fence, zero-writer state, and source identity/cut/digest without requiring a completed backup (an authenticated partial backup is reconciled or discarded, never trusted), while backed_up-or-later pre-prepared resumes additionally re-validate the authenticated backup plus a scratch restore before any reuse of it; a foreign restart of a neutralized job or process, unjournaled physical state, source drift, or backup mismatch fails closed with redacted durable evidence under the root and no phase advance.

### AC2 — Package an explicit residual and fence every supported old-full start path

Add src/daemon/legacy-residual.ts as a separate root. It starts no generic fs/git/Granola/Claude/Codex/Cursor capture, context enrichment/storage, or eight context MCP registrars. Its exact seven-tool roster is propose_decision, pending_decisions, get_role_state, list_task_states, coord_emit, coord_status, and coord_invoke. Repository-backed task-state, pending-decision, and reviewer-invocation data intentionally remain Project_echo-owned; runtime code is package-installed and repo-independent except for explicit repository data arguments.

Product meeting/brief/intake/health consumers access context only through src/storage/context-service.ts, whose operations must be a subset of item 137b's sealed product-context-consumer-contract.v1.json. Local-versus-HTTP conformance pins ordering, filters, caps, IDs, errors, and restart behavior. Any new/uncovered operation fails the residual build and requires a new reviewed context runtime; no fallback opens either context DB.

The checksum-bound Project_echo package contains both the residual entrypoint and a rollback-full entrypoint whose authority fence executes before PID lock, data-dir creation, SQLite open, workers, or socket bind. The canonical authority-root identity the fence consults is bound at package-install time into an immutable installed identity record: the exact file `share/echo/installed-authority-root.v1.json` under the installed package prefix (in rehearsal, under the scratch-root fixture install prefix), one canonically serialized JSON document carrying schema id `installed-authority-root.v1`, the symlink-free canonical absolute root path, the root's filesystem identity (device and inode), the package name/version/archive SHA-256, a bound-at timestamp, and an integrity SHA-256 over the record's other fields; owned by the installing user, mode 0444, written via temp write, fsync, atomic rename, and parent-directory fsync by the atomic identity-binding step `echo-bind-authority-root --prefix <install-prefix> --root <path>`, which AC5's deployment bootstrap executes as one journaled step of its single crash-replayable install transaction — the same transaction that preprovisions the evidence log location; in rehearsal, invoked only by the fixture installer under the scratch root. The fence resolves the authority root only from that record, verifies it descriptor-pinned — open the root with no-follow semantics, fstat, match both the recorded canonical path and device/inode, and hold that descriptor through `authority.lock` acquisition and every subsequent authority read — and rejects a missing or malformed record, an integrity-hash mismatch, a resolved root that mismatches or aliases the bound identity (any differing canonical path or device/inode), or any environment/flag-supplied root override, before any PID/data-dir/SQLite/socket mutation — so a stale configuration or alternate root can never acquire a different `authority.lock`, observe an absent generation, and start full mode after activation, and a root replacement between path resolution and lock open can never swap the fence's target; a cross-process alternate-root fixture proves it, and symlinked-root, alternate-path-spelling, parent-traversal, and root-replacement-race fixtures prove the descriptor-pinned binding. It replaces/neutralizes every supported global echoctl daemon-start path during the future live ceremony. Full-mode admission follows exactly AC1's per-phase table: an absent, planned, or recutover_prepared canonical generation permits full mode with no authorization — before source_fenced commits the source is unfenced and an old-full restart must never be blocked; source_fenced, backed_up, migrated, prepared, and active reject full mode before mutation; a rolled_back generation permits it only against the exact durable rollback authorization, defined as the committed rolled_back record tuple — generation, phase, and the restoration digests that AC1's ordering guarantees exist only after the rollback interval exports, client/quarantine restoration, and service-control before-image restoration are durably complete — validated by the fence from the canonical transaction record while holding `authority.lock`. Start-versus-transition race fixtures at planned and recutover_prepared prove the pre-fence permit in both race orders, and a kill immediately after the rolled_back flip followed by a competing old-full start proves the authorization is durably readable. The fence decision is serialized with controller phase transitions through AC1's stable per-root authority lock (`authority.lock`, a never-replaced sibling of the canonical transaction record that exists independently of it and is created descriptor-relative no-follow when absent — never a lock on the record's own inode, which is absent in allowed startup states and atomically replaced at commit points): the rollback-full startup path and every controller transition acquire it before reading any authority state, recheck generation state while holding it, and complete the entire authority-bearing startup handoff (PID lock, data-dir creation, SQLite open, socket bind) or the phase commit before release, so an old-full start can never observe absent or rolled_back, pause, and then mutate PID, database, or socket after the controller commits prepared or active. Before committing source_fenced — and re-verifying before committing prepared or active — while still holding the lock, the controller both neutralizes every supported old start job (plist bootout/disable through the fake service control in rehearsal) so a stale KeepAlive plist cannot relaunch the fenced daemon in a loop, and stops and verifies quiescent — zero remaining writers — every already-started full process, so a full process that completed startup and released the lock before the transition cannot keep writing after it. Every authority-lock acquisition, service-control operation, process stop, and zero-writer verification is deadline-bounded: on timeout or a stuck/unkillable target the actor never commits the pending phase, releases the lock safely, persists a redacted failure record (the controller under its root; the fence via its rejection path), and exits non-zero; stuck-lock, failing-fake-launchd, and unkillable-process fixtures prove bounded fail-closed exit with no partial authority transition. Fence rejections emit a redacted reason and exit non-zero unconditionally; evidence is additionally written best-effort to the packaged daemon's preprovisioned log location (created at package install time, never by the pre-open fence itself), and no evidence-write outcome can gate, delay, or permit startup — a missing, unwritable, or full sink still rejects with zero startup mutation. The relaunch bound is enforced solely by start-job neutralization, with no in-memory limiter state and no sink-dependent limiter. Direct command, old plist, and stale-config fixtures prove the old full daemon cannot reclaim authority; barrier-controlled concurrent start-versus-activation fixtures cover every supported start path, including absent-record and atomic-record-replacement cases while a paused contender holds a stale view, both race orders, and a post-commit write attempt by an already-started full process; a fake-launchd KeepAlive fixture launches a fresh process per attempt and proves repeated relaunch attempts terminate bounded through start-job neutralization, with best-effort rejection evidence visible after restart when the sink is writable and sink-failure variants (missing/unwritable/full sink) still rejecting with zero startup mutation.

### AC3 — Preserve coordination authority and the existing wait/search promise

Residual coord.sqlite is canonical coordination authority. Offline import preserves all pre-cutover coord IDs, bytes, append order, replay watermark, idempotency state, open deadlines, and coord_status snapshot. New coord emission commits the canonical event and an outbox row in one transaction; loss after either write is impossible.

The mirror worker sends a stable-ID generic coord:* observation through item 137b's authenticated idempotent append-with-caller-ID service operation. Identical repeats succeed without a duplicate. A differing-ID collision or retry exhaustion stops the mirror and persists a terminal error state — the last error plus the current watermark — in residual coord.sqlite, where coord_status/health reads surface it and it remains visible across restart; the failure can never look healthy while observations silently stop advancing. Bounded retry/restart reconciliation drains gaps and records a watermark. Deadline/status/invoke paths read only residual coord.sqlite; context search_memories and wait_for_new_turns may see the mirrored observations, keeping coord_emit's existing descriptor truthful. Mirror lag never changes coordination authority or loses canonical writes.

Rollback imports into the restored old ledger exactly the half-open interval (C0,C1] of residual coordination rows — the same bounds AC7 persists (C0 established under the pre-flip freeze, C1 under the rollback freeze); idempotency state, open deadlines, and checkpoints are cut at C1, and replay imports no row or state above C1, with a boundary test asserting it. Recutover rebuilds residual authority from the new old-ledger cut and reconciles context mirrors without duplicates.

### AC4 — Make all endpoint/client transforms pure, lossless, and classified

Add parse-plan-apply-undo transforms for Codex TOML, Claude JSON, Cursor JSON, canonical skills, rendered Claude/Codex adapters, CLI/selftest/inverse config, brain, Slack responder, review queue, and operator tools. Retain client key echo for context at 38478 and add echo-project-residual at 38479. Replace ambiguous ECHO_MCP_URL/ECHO_DAEMON_PORT use with explicit ECHO_CONTEXT_MCP_URL or ECHO_RESIDUAL_MCP_URL plus protected auth references.

Every current 38478/echo caller is classified as context, residual, or both in a committed manifest. Unknown/ambiguous callers fail. Transforms preserve unrelated settings and exact before bytes for undo, reject duplicate/unsupported auth or format drift, and never reveal header values. This item uses committed fixture configs and temporary installed-skill roots only; no real client or global skill path is read or written.

The controller owns one journaled per-target compare-and-swap transaction that governs apply, abort recovery, resume, rollback, interrupted rollback, and recutover of every client/endpoint transform, including create-type (before-image = absent) and delete-type (after-image = absent) targets. It is metadata-aware: each journaled target binds presence, descriptor-relative parent and target identity (device/inode), the exact before bytes/hash and protected metadata (file type, uid/gid, mode), the planned after-image, and the owning generation. One normative per-target transition table governs every operation, with each cell having exactly one outcome: current state = before-image → transition to the after-image on apply, and no-op on rollback of a never-applied target; current state = after-image → no-op on re-apply when the journal already records the apply (idempotent), and restore-to-before-image on rollback; current state matching neither journaled image — byte, metadata, presence, or identity drift by any foreign writer, with a metadata-only difference treated as drift on equal terms with a byte difference — → the target is never overwritten or deleted, is recorded as durable secret-free manual-recovery evidence, and blocks activation and the pending phase commit, while every other target's completed durable transition is retained; interrupted rollback resumes replay the journal and land every target in exactly one of those three cells. Replacement mechanics are fixed: same-directory exclusive temporary (`open(2)` `O_CREAT|O_EXCL`, mode 0600), planned metadata applied to the temporary, file fsync, atomic rename onto the target, parent-directory fsync, and under-lock interrupted-temporary reconciliation — in-place target writes are forbidden, so a kill can never leave a controller-created third image at a target path. Create-type targets commit via `renamex_np(2)` `RENAME_EXCL`, a real no-clobber Darwin primitive. For replace-type targets the never-overwrite promise is explicitly narrowed: Darwin provides no compare-and-rename primitive, so the guarantee covers every foreign change durable before the under-lock, descriptor-pinned final validation immediately preceding the commit rename; a foreign write racing inside that validate→rename window is minimized by that ordering, not claimed prevented. The item-137b projection retains only sealed aggregate hashes of transform state, never per-target bytes or metadata.

### AC5 — Build deterministic controller and Project_echo package candidates

Add deterministic build/verify workflows for an operator-only echo-context cutover-controller archive and a Darwin x64 Project_echo residual/rollback-full package. The workflows are named package scripts created by this item — `candidates:build` and `candidates:verify` in /Users/zhenye/Desktop/echo-context/package.json (controller archive) and in Project_echo's package.json backed by tools/context-cutover/** (residual/full package) — each run from its repository root, reading only committed inputs, and writing only under its repository's build/output directory. Both `candidates:build` scripts are artifact-output-only commands: they write only under their repository's build/output directory and never invoke service-control, client-config, or state adapters; `candidates:verify` is read-only apart from scratch extraction under that same build/output directory, and a command-surface test asserts this separation from the rehearse entrypoint. Candidate manifests are self-bound: each `candidates:build` verifies its own clean tree and embeds only its own repository's {SHA, tree} identity — there is no counterpart-manifest argument, no counterpart checkout or object-database input, and no cross-manifest identity tuple at build time. The authoritative cross-repository pairing is AC8's paired migration record and read-back landed SHAs, which item 139 alone pins; each manifest's bound SHA must equal the build checkout's HEAD, its tree is derived from that SHA rather than independently supplied, and the archive provenance binds both; `candidates:verify` validates that binding against its own repository's git objects only, rejecting any other valid in-repository commit/tree pair, and tamper negative tests that edit the manifest's SHA or tree — including substituting a different valid commit/tree pair — must fail it. Each build stages into a fresh per-run staging directory on the same filesystem as the final publication area under its repository's build/output directory; the completed candidate is one immutable versioned directory containing exactly the manifest and archive — plus, for the Project_echo candidate, the separate pre-extraction deployment bootstrap asset below, whose SHA-256 the manifest records — every file and the directory fully fsynced, and the sole commit point is one atomic same-filesystem directory rename of that staging directory to its immutable final versioned path followed by parent-directory fsync. Discovery recognizes only completed final-path directories and ignores staging and orphaned paths; concurrent builds use distinct per-run staging names and a rename loser aborts without disturbing the winner. A build that fails or is killed before the rename leaves no publishable manifest — that guarantee is scoped to pre-commit failures; a kill immediately after the rename leaves a complete valid published pair whose recovery plus `candidates:verify` counts as success. Kill tests cover immediately before and immediately after the rename, plus concurrent-build publication. Manifests bind their own repository's canonical source SHA/tree input, item 137a's exact runtime/candidate lineage and item 137b's exact installed-runtime artifacts, locks/SBOMs, bundled/runtime Node decisions, platform/architecture, entrypoints, schemas, inventories/modes, consumer/caller manifests, and authority_active_at_release false. Every archive and toolchain input is pinned deterministic — canonical entry order, fixed entry timestamps, fixed uid/gid and modes, fixed compression metadata, canonical manifest serialization, and locale/timezone-independent tooling — and a determinism test builds twice from two independent clean checkouts of the same SHA and requires byte-identical manifest and archive hashes. Archives contain no source repository path, credential, state, client config, mutable cache, ungated mutation-capable entrypoint, mutation-guard bypass, or environment-override hook, and reject traversal/symlink/hardlink/manifest drift and every native member except the sole allowlisted fs-ext prebuilt at its recorded path and manifest-bound hash; a named extracted-archive inspection test asserts the no-bypass properties on the exact controller archive handed to item 139 and enumerates the complete command surface — rehearse, candidates:build, candidates:verify, the production execute and resume entrypoints, and the deployment bootstrap — proving every mutation-capable entrypoint is gated and no other executable entrypoint exists.

The Project_echo candidate manifest contains the literal field `deployment_entrypoint` whose value names the candidate's separate deterministic pre-extraction deployment bootstrap asset and its exact argv: `bootstrap/echo-deploy-bootstrap --manifest <manifest-path> --approval <approval-record-path> --archive <archive-path> --prefix <install-prefix> --root <authority-root>`. The bootstrap is a self-contained deterministic executable published beside the manifest and archive in the versioned candidate directory (committed source owner: tools/context-cutover/bootstrap/** in Project_echo), requiring no host Node, npm, or repository checkout. Before staging anything it verifies its trust chain — the authenticated approval record binds the manifest hash and the manifest binds the bootstrap's own SHA-256 and the archive SHA-256 — then verifies the archive hash and member safety (traversal/symlink/hardlink rejection and the sole-allowlist native rule) against the manifest. Installation then composes four steps as one crash-replayable journaled transaction: payload publication (approved archive bytes verbatim only — no lifecycle or build scripts, no dependency resolution, no byte mutation, no activation — into the immutable hash/version payload subtree `<prefix>/versions/<version>-<archive-sha256>/`), the installed receipt (exact schema `installed-receipt.v1`: package name/version, archive SHA-256, manifest hash, bootstrap hash, payload subtree path, bound-at timestamp, and an integrity SHA-256 over its other fields; mode 0444, committed via temp write, fsync, atomic rename, parent fsync), the installed authority-root identity binding (writing AC2's `installed-authority-root.v1.json` exactly as specified there, as one journaled step of this same transaction), and evidence-directory provisioning. The immutable payload lives only in the hash/version subtree; every host-specific mutable record — receipt, identity record, evidence directory, and the deployment journal itself — lives in the declared mutable metadata subtree `share/echo/`. A replay table defines each step as idempotent (fully-installed detection via hash readback; a partial payload is discarded and re-staged; records recommit via their temp/rename protocol) and a kill matrix covers every step boundary. Descriptor handoff: AC2's fence receives the receipt and identity record through descriptor-pinned no-follow opens, fstat-matched and held from verification through `authority.lock` acquisition. After install the bootstrap hash-reads the installed bytes back against the manifest. This composition is the producer contract item 139 consumes by name for residual and rollback-full deployment. Tests exercise it in rehearsal roots only and reject a missing or mismatched manifest, approval, archive, or receipt binding.

The controller candidate manifest contains the literal fields `execute_entrypoint` and `resume_entrypoint` naming the production execute and resume entrypoints and their exact argv: `bin/echo-cutover-execute --approval <approval-record-path>` and `bin/echo-cutover-resume --approval <approval-record-path>` (committed source owner: src/cutover/** in echo-context). The approval record is one canonically serialized, separately authenticated input — integrity SHA-256 over its fields plus the authorization binding only item 139's reviewed authorization path can produce — carrying the landed target and Project_echo SHAs and trees, the controller/runtime/residual manifest and archive hashes, the plan hash, the generation, the canonical authority root, the install prefix, and the mode (execute or resume). The entrypoints accept no other operational argv and no environment fallback, and never invoke npm, a repository checkout, or a host toolchain; every operational parameter comes from the validated approval record. Execute initializes only a fresh transaction — it rejects a root containing any existing `transaction.json`; resume resumes only the exact journal tuple (generation, plan hash, phase) named in the approval and rejects everything else; both acquire the same single `authority.lock` before any mutation. In this item both entrypoints are built and exercised exclusively against rehearsal roots and fake adapters under fixture approvals — item 138 never invokes live adapters. Contention tests cover execute/execute, execute/resume, and resume/resume on one root: single-owner exclusion through the one lock, with the loser exiting non-zero with no root mutation beyond the sole-lock exemption, plus an approval rejection matrix (missing, tampered, wrong-mode, wrong-generation, wrong-root, and wrong-tuple approvals all fail closed with zero mutation).

Rehearsal builds from reviewed feature heads are test candidates only. Following the successor-repository protocol, the target branch lands first, then the Project_echo branch. Item 139 alone builds/releases the execute artifacts from fresh detached clones of both read-back canonical landed SHAs; no feature-head artifact may reach live paths.

### AC6 — Rehearse populated backup, migration, coordination import, and preexisting-home quarantine

Generate a deterministic representative legacy database/WAL/sidecar set with null metadata, binary embeddings, every source class, product-derived events, and at least 1,000 ordered coord events including open deadlines/idempotency rows. The fake mixed service quiesces; SQLite backup API writes an authenticated encrypted bundle; wrong key/tamper/partial/WAL drift fails; a scratch restore is mandatory.

Migrate only a restored copy into an absent 0700 final context root, translate the explicit nonsecret capture sidecar allowlist, and import the complete coord ledger into residual. Versioned length-prefixed logical digests prove row IDs/bytes/order/count/max-rowid/per-source parity, sidecar schemas, integrity, populated eight-tool hashes, and coord reconstruction equivalence.

The planner explicitly handles the founder Mac's known preexisting ~/.echo-context scaffold: only the exact inventoried shape of empty adapters/roles/skills/workflows plus state/onboarding.json and state/projects.json may be atomically moved whole into protected generation quarantine after hash/type/mode/owner recheck. It is never merged into new state or deleted. Unknown entries, DBs, nonempty support dirs, symlinks, mount/owner/mode change, or post-plan hash drift fail. Rehearsal proves rollback restores the exact scaffold and recutover quarantines it again.

### AC7 — Prove lossless cutover, rollback, and new-generation recutover

Run the exact candidate controller/package/runtime under a temporary HOME, ephemeral ports, and fake launchd: old full to prepared split to active generation G1. Assert exact eight-plus-seven rosters, one context writer, residual remote-context conformance, populated history, coord mirror convergence, and no repository dependency.

After G1, inject events from all six context sources, a product-derived append, coord/deadline writes, client rewiring, service restarts, and failures before/after every journal checkpoint. Every context and coordination high-water cut is taken under an explicit writer freeze: the controller quiesces the context and coordination writers, drains the coord outbox, verifies the final high-water marks while the freeze is held, and commits the cut and the authority flip in one canonical-transaction-record step before writers resume — no context or coordination write can land after its snapshot but before the transition and be absent from both authorities. The activation baselines W0 and C0 are established under that freeze before the initial authority flip; the rollback upper bounds W1 and C1 are taken under the rollback freeze; all four bounds persist in the canonical transaction record. Rollback exports exactly the half-open intervals (W0,W1] of target context rows and (C0,C1] of residual coordination rows, plus current checkpoints; restores an old-full staging copy plus exact clients/quarantine; idempotently restores the exact captured launchd service-control before-image — enable/load/KeepAlive state and plist bytes/location — of every start job neutralized at source_fenced, and proves old-full readiness through the restored job (never a direct test-only start) before rollback completes; and proves zero loss/duplicates. Per AC1's ordering, every restoration above is durably journaled complete before the rolled_back flip commits, and old-full readiness is proven only after the flip through the restored job. Tests cover rows exactly on each of the four interval boundaries, crash replay immediately before and after each flip, and a kill immediately after the rolled_back flip followed by a competing old-full start.

While rolled back, inject additional old-full context and coord events. Recutover follows the fresh-generation phase sequence rolled_back → recutover_prepared → source_fenced → backed_up → migrated → prepared → active: it creates new generation G2 with a fresh cut taken under the same writer-freeze protocol, backup, migrated context DB, rebuilt residual coord DB, client plan, and authority record, and its source_fenced step neutralizes the restored old start jobs again; it never reactivates G1. After G2, all pre-cutover, G1-era, and rollback-era events appear exactly once, coord status matches, mirrors converge, and failures always resume to exactly old-authoritative or new-authoritative.

### AC8 — Land reviewed substrate and emit a redacted go/no-go handoff

Independent reviewers inspect both exact repository heads, run all target and Project_echo suites, and validate the paired migration record. The founder separately approves target-main and Project_echo-main landings per the successor-repository landing protocol; canonical readback fills both landed SHAs and proves per-repository reviewed-to-landed tree equality — each landed SHA's tree equals the tree the independent reviewers inspected — or the landed tree requires re-review before completion, with a landed-tree drift fixture proving the failure path. The readback values are recorded in a subsequent evidence-only commit — never inside the landing commit itself — into this item's completed frontmatter (`target_landed_sha`, `project_landed_sha`) and the migration record; that frontmatter at origin/main, cross-checked against the migration record and verified reachable from each canonical remote, is the sole SHA authority item 139 pins. No live artifact is released and no user path/service/credential/config is touched.

raw/internal/migrations/2026-07-15-138-echo-context-cutover-rehearsal.md binds landed SHAs/trees, candidate-build hashes, phase/crash matrix, fixture logical digests/counts, rosters, coord mirror lag/reconstruction, client/caller classification, mutation-guard sentinel before/after hashes, and verdict without content/secrets. Completion says cutover substrate ready; runtime authority remains Project_echo and item 139 is the only live gate.

## Out of Scope (Don't Drift)

- No read/write of live ~/.echo*, ~/Library, ~/.codex, ~/.claude, ~/.cursor, global installed skills/packages, LaunchAgents, listeners, credentials, or databases.
- No port 38478/38479 bind, real launchctl operation, Slack token rotation/send, live shadow rebind, live client rewiring, live state migration, live authority activation, live rollback, or freeze.
- No rebuild/patch of item 137b's installed runtime and no live use of a feature-head or 137a candidate stage.
- No new context MCP tool/semantic, storage redesign, embeddings/backfill, product feature, echo-brain/echo-loop install, or Team-product graduation.
- No generic multi-repo/migration framework, public release, other platform, Fleet, cloud, or cross-machine sync.
- No wiki edit or deletion of any source/target/quarantine/backup state.

## Risks

- A fixture-only rehearsal can miss launchd/filesystem races. Production adapters are narrow and phase-injected; item 139 repeats every preflight and stops rather than patching.
- Coord mirroring can be mistaken for authority. The residual store is the only canonical read path for deadlines; the context copy is named and tested as an observation.
- Product consumers can exceed the sealed service contract. Static closure and build failure prevent runtime fallback or silent drift.
- Client formats and installed adapters can drift. Pure transforms preserve before bytes, classify every caller, and fail on unknown formats.
- Cross-repo artifacts can bind the wrong heads. Candidate evidence is non-live; item 139 builds only from completed canonical landed SHAs.
- Rollback/recutover can reorder or duplicate rows. Stable IDs, explicit W/C cuts, binary digests, collision aborts, and G2 tests close the path.
- The preexisting home can be mistaken for live context. Its exact whole-root quarantine is separate and never merged.

## Tests

- /Users/zhenye/Desktop/echo-context/tests/cutover/phase-machine.test.ts and mutation-guard.test.ts cover journal replay, both closed phase sequences with source_fenced ordering (including the no-source-mutation-before-source_fenced proof), zero real-path/process/socket access, initialize-versus-resume root semantics including the lock-only bootstrap state, the interrupted-commit temp-record reconciliation states, and the bootstrap/commit-boundary kill matrix at temp creation/write/fsync and both sides of the rename for the first planned commit and every replacement, per-root exclusive locking via real multiprocess flock contention through the named fs-ext member with stuck-holder pinned-deadline/exit-code-75 exits, the creator-loser bootstrap race (nothing written but the empty sole lock file), and lock-acquisition-timeout stderr-only zero-root-write-beyond-sole-lock behavior, the exhaustive per-phase transition/full-mode/resume table (every cell asserted, including the active → rolled_back edge and generation-bound G2 re-proofs retaining the G1 rolled_back projection until G2 prepared replaces it), strict interrupted-temp validation (uid/mode/nlink/device/size/schema/predecessor binding; an invalid temp left untouched with fail-closed exit), the production execute/resume entrypoints under fixture approvals — fresh-transaction-only execute, exact-journal-tuple-only resume, the approval rejection matrix, and execute/execute, execute/resume, and resume/resume contention through the one lock —, a table-driven root-rejection matrix (forbidden, symlinked, unwritable, and invalid-content roots each exit non-zero with zero controller-originated filesystem mutation — asserted against sentinel temporary, cache, and log locations around the advertised `npm run rehearse` invocation as well as the root — and no journal or record written), the self-bound manifest-versus-own-git-objects assertion with tampered-SHA/tree and substituted-valid-commit/tree `candidates:verify` failures, HEAD-equality/derived-tree binding, fresh-staging single-directory-rename publication with kill points immediately before/after the rename and concurrent-build publication, the two-independent-clean-checkout byte-identical determinism build, the rehearse-versus-candidates command-surface separation, and extracted-controller-archive inspection rejecting live adapters, guard-disable flags, or environment bypasses.
- /Users/zhenye/Desktop/echo-context/tests/cutover/backup-migrate.test.ts covers WAL backup, encryption/tamper, restore, logical parity, sidecars, coord import, and quarantine.
- /Users/zhenye/Desktop/echo-context/tests/cutover/client-adapters.test.ts covers lossless Codex/Claude/Cursor and installed-skill transforms with unsupported-format/auth failures, plus the journaled per-target metadata-aware CAS transaction across apply, abort recovery, resume, rollback, interrupted rollback, and recutover, asserting every cell of AC4's transition table including create-type and delete-type targets: byte-only and metadata-only foreign-drift targets at initial cutover, rollback, and recutover are left untouched, recorded as secret-free manual-recovery evidence, and block the pending phase commit while completed sibling transitions persist; barrier-controlled foreign rename, chmod, and byte-write races; `RENAME_EXCL` no-clobber create commits; and kill tests at every replacement boundary (temp create, metadata apply, file fsync, rename, parent fsync) for apply, rollback, and recutover — no in-place write ever leaves a controller-created third image.
- /Users/zhenye/Desktop/echo-context/tests/cutover/rollback-recutover.test.ts and crash-resume.test.ts prove W0/C0 and W1/C1 establishment, rows exactly on each interval boundary, exact (W0,W1] and (C0,C1] export equality, writer-freeze cut races and crash replay immediately before/after every cut/flip boundary, exact launchd service-control before-image restoration durably journaled complete before the rolled_back flip commits with old-full readiness proven through the restored job only after the flip, the post-flip kill-plus-competing-start case, the (C0,C1] no-row-or-state-above-C1 import assertion, recutover re-neutralization, G1 rollback, rollback-era writes, fresh G2, one authority, and every phase-aware resume re-proof from AC1's table — source_fenced without a completed backup (authenticated partial backup reconciled or discarded), backed_up-or-later with authenticated backup plus scratch restore, active one-writer proof, rolled_back restored-old-authority proof without a zero-old-writer requirement — with foreign-restart, unjournaled-physical-state, source-drift, DB/WAL/SHM/sidecar-drift, and backup-mismatch fail-closed variants.
- tests/daemon/legacy-residual.test.ts and authority-fence.test.ts prove residual exclusions, pre-open rejection of every full start path, and start-versus-activation serialization through the stable per-root authority lock — absent-record and atomic-record-replacement barriers with a paused contender, both race orders, post-commit write rejection for an already-started full process — plus execute/resume contention through the same single machine-wide lock, bounded KeepAlive relaunch termination under fake launchd with a fresh process per attempt (bounded by start-job neutralization; missing/unwritable/full-sink variants still reject with zero startup mutation, best-effort evidence visible across restart when writable), deadline-bounded stuck-lock/failing-launchd/unkillable-process fail-closed exits, installed-identity-record schema/integrity/atomic-binding fixtures with symlinked-root, alternate-path-spelling, parent-traversal, and root-replacement-race rejection, alternate-root/override rejection before any mutation, the per-phase full-mode admission table (planned and recutover_prepared start-versus-transition races in both orders; rolled_back admission only against the durable rollback-authorization tuple; every fenced phase rejecting), and the `deployment_entrypoint`-named bootstrap composition in rehearsal roots: trust-chain and member-safety verification before staging, the four-step crash-replayable install transaction with its replay table and kill matrix at every step boundary, hash/version payload-subtree versus mutable share/echo metadata-subtree separation, installed-receipt schema/integrity fixtures, descriptor-pinned receipt/identity handoff to the fence, installed-byte hash readback, and missing/mismatched manifest/approval/archive/receipt-binding rejection.
- tests/mcp/legacy-residual-roster.test.ts proves exact authenticated seven; existing target tests prove exact eight; union/classification equals the prior 15.
- tests/storage/context-service.test.ts proves the sealed consumer subset; coord-continuity-mirror.test.ts proves import, deadline snapshot, atomic outbox, retry, stable-ID idempotency, wait/search visibility, and the durable collision/retry-exhaustion terminal state surfacing through coord_status/health across restart.
- tests/echo-home/adapters/split-mcp-config.test.ts and caller-classification.test.ts cover every active/generated endpoint consumer.
- tests/integration/context-cutover-rehearsal.test.ts runs the full isolated old-to-G1-to-old-to-G2 ceremony with all source/product/coord deltas and crash points.
- Both repository typecheck/lint/full suites, the `candidates:build`/`candidates:verify` package scripts, runtime inventory, tools/sync-skills.sh --check, tools/install-echo-codex-skills.sh --dry-run, git diff --check, and mutation sentinels remain green.

## After Completion (Strategist Notes)

- Update no wiki page; no live authority changed.
- Promote item 139 only when both canonical landed SHAs and the redacted rehearsal verdict are present.
- Item 139 owns exact-artifact build/release, credentials, live backup/migration, client/installed-skill wiring, activation, rollback/recutover, acceptance, and context-plane freeze.
