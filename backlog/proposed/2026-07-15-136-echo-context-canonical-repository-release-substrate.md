---
id: 2026-07-15-136-echo-context-canonical-repository-release-substrate
title: "Canonical echo-context repository, self-contained CI, and source-artifact authority"
status: proposed
priority: HIGH
estimate: 3d
created: 2026-07-15
blocked_by:
  - 2026-07-13-135-local-echo-context-source-extraction
task_state_ref: 2026-07-15-136-echo-context-canonical-repository-release-substrate
requested_reviewers: ["codex", "codex-ops"]
files_to_modify:
  - /Users/zhenye/Desktop/echo-context/.github/workflows/** # NEW least-privilege CI, secret scan, and push-triggered read-only source-release build workflow
  - /Users/zhenye/Desktop/echo-context/package.json # prerelease identity and self-contained operator scripts
  - /Users/zhenye/Desktop/echo-context/package-lock.json # exact dependency graph for the source artifact
  - /Users/zhenye/Desktop/echo-context/README.md # replace extraction-era no-remote/awaiting-review status
  - /Users/zhenye/Desktop/echo-context/LICENSE # exact reviewed MIT license for standalone distribution
  - /Users/zhenye/Desktop/echo-context/.gitignore # ignore build/artifact/cache/log/state outputs
  - /Users/zhenye/Desktop/echo-context/AGENTS.md # target-repo internal-asset and review/build-once instructions
  - /Users/zhenye/Desktop/echo-context/tsconfig.json # commit the reviewed standalone compiler configuration
  - /Users/zhenye/Desktop/echo-context/eslint.config.mjs # commit the reviewed standalone lint configuration
  - /Users/zhenye/Desktop/echo-context/vitest.*.config.ts # separate source-independent CI from operator replay
  - /Users/zhenye/Desktop/echo-context/provenance/** # NEW baseline/repository authority plus successor runtime-inventory.v2.json (item-135 v1 bytes immutable)
  - /Users/zhenye/Desktop/echo-context/schemas/** # NEW source-artifact and authority schemas
  - /Users/zhenye/Desktop/echo-context/tools/** # repository checks, deterministic source build, verification, and secret scan
  - /Users/zhenye/Desktop/echo-context/tests/migration/** # bind extraction proof to the frozen baseline instead of evolving HEAD
  - /Users/zhenye/Desktop/echo-context/tests/operator/** # explicit Project_echo raw-object replay, excluded from hosted CI
  - /Users/zhenye/Desktop/echo-context/tests/governance/** # NEW authority, artifact, and workflow-policy tests
  - raw/internal/migrations/2026-07-15-136-echo-context-repository-bootstrap.md # NEW redacted authority/release record
  - raw/internal/migrations/2026-07-15-136-echo-context-implementation-review.json # NEW independent-reviewer-owned actor/run and reviewed-head evidence
  - raw/internal/migrations/2026-07-15-136-echo-context-target-main-execution-plan.json # NEW coordinator-owned canonical target-main plan bytes
  - raw/internal/migrations/2026-07-15-136-echo-context-target-main-delegated-authorization.json # NEW coordinator-owned exact target-main authorization
  - raw/internal/migrations/2026-07-15-136-echo-context-source-release-publication-plan.json # NEW coordinator-owned canonical local-publication plan bytes
  - raw/internal/migrations/2026-07-15-136-echo-context-source-release-delegated-authorization.json # NEW coordinator-owned machine-verifiable release authorization
  - tools/echo-context-reviewed-operation-launcher.mjs # NEW Project_echo-owned authenticated loader for the reviewed operation envelope
  - tools/echo-context-operation-envelope.mjs # NEW Project_echo-owned outer land/collect/publish provenance, workspace, deadline, and cleanup state machine
  - tests/echo-context/operation-envelope.test.ts # NEW executable tests for the reviewed launcher and outer coordinator envelope
  - raw/internal/agent-runs/** # workflow-owned failure/completion run log
  - backlog/task-state/2026-07-15-136-echo-context-canonical-repository-release-substrate/** # workflow continuity pointers
  - backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # proposal and review revisions
  - backlog/ready/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # watcher-owned promotion target
  - backlog/claimed/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # workflow claim target
  - backlog/pending_review/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # workflow handoff target
  - docs/BACKLOG.md # generated stage-derived index
spec_refs:
  - backlog/complete/2026-07-13-135-local-echo-context-source-extraction.md # exact reviewed extraction and later-authority boundary
  - raw/internal/migrations/2026-07-13-135-echo-context.md # accepted source/parity evidence and target identity
  - raw/internal/migrations/2026-07-13-135-echo-context-review.md # independent acceptance record
  - raw/internal/decisions/2026-07-12-g2-terminal-dispositions-and-repository-topology.md # two-organization-repository topology + controls-preservation rule; predates this split and is not its authority
  - raw/internal/decisions/2026-07-15-echo-context-successor-repository-execution.md # founder-authorized cross-repo execution/review protocol
  - raw/internal/decisions/2026-07-16-echo-context-sequential-program-delegated-authority.md # scoped coordinator authority and immutable-operation record contract
  - raw/internal/migrations/2026-07-15-136-echo-context-canonical-repository-release-substrate-repository-create-6a2e8326-delegated-approval.md # immutable once-only repository creation authorization/evidence
  - raw/internal/migrations/2026-07-15-136-echo-context-canonical-repository-release-substrate-baseline-main-push-472e7bf5-delegated-approval.md # immutable once-only baseline push authorization/evidence
  - backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.review.md # cycle-one implementation rejection and accepted fix contract
  - raw/internal/decisions/2026-07-11-commercial-focus-team-product-carve.md # context remains an internal asset
  - /Users/zhenye/Desktop/echo-context/provenance/target-only-policy.v1.json # extraction-era exact-HEAD/finality constraint
  - /Users/zhenye/Desktop/echo-context/provenance/runtime-inventory.v1.json # frozen item-135 executable/dependency closure; superseded for current validation by runtime-inventory.v2.json
  - /Users/zhenye/Desktop/echo-context/tests/migration/object-closure.test.ts # one-branch/no-remote extraction invariant to historicize
  - /Users/zhenye/Desktop/echo-context/tests/migration/parity-matrix.test.ts # source-dependent proof that hosted CI cannot assume
  - /Users/zhenye/Desktop/echo-context/tests/migration/context-tool-evidence.test.ts # source replay seam and sealed eight-tool evidence
claimed_by: ""
claimed_at: ""
branch: ""
worktree: ""
head_sha: ""
pr_url: ""
agent_notes: ""
review_notes: |
  Implementation cycle 1 rejected — redo before merge.
  Reviewed Project_echo head: 25b833332bb22ec79700fcdf31b9c9f20eea79f5.
  Reviewed echo-context head: 145868a67a85dbb651faed457ee4001370c0fad0.
  Immutable review evidence: Project_echo commit 77c5e0c3f7f64c0bda1d99439d8d35eca54551ee,
  review-artifact blob 1cd16c1f31dab5c40ad76b70716846a09c98d845.
  AC4/AC6 require fresh specification convergence; neither head is authorized
  for target-main merge or release.
target_repo: ""
target_remote: ""
target_branch: ""
target_worktree: ""
target_head_sha: ""
target_pr_url: ""
target_landed_sha: ""
project_landed_sha: ""
---

# Canonical echo-context repository, self-contained CI, and source-artifact authority

## Why this spec exists

Item 135 proved that the local echo-context extraction is source-independent and behaviorally faithful, but it deliberately ended with one migration branch, no remote, no install, and no runtime authority. Its extraction-finality tests also bind several checks to the then-current HEAD and two operator proofs default to Project_echo Git objects, so blindly publishing that directory would produce a repository that cannot safely evolve or validate itself in hosted CI.

This first gate makes echo-context/main the canonical source authority without confusing source publication with installation or live-state authority. It preserves the exact reviewed extraction and completed private-repository bootstrap as immutable history, gives clean clones a self-contained quality gate, and seals one explicitly non-installable source artifact for item 137. Project_echo remains the installed runtime and state authority throughout this item. The third-repository split executed here is authorized by the 2026-07-15 successor-repository execution decision and item 135's founder-reconciled merge; the G2 topology record defined two organization repositories and kept the context layer inside echo-dev-platform until split triggers fired, so it is cited for its controls-preservation rule, not as the split authority.

This internal-asset work is permitted by the scope guard because unified machine context across Codex, Claude Code, and Cursor is required by the current ECHO development workflow and Team-product delivery. It is not a parallel commercial roadmap or a second product.

## Acceptance Criteria

### AC1 — Bind the completed exact baseline publication as immutable history

This bootstrap is already complete and is an immutable prerequisite, not a retryable builder step. The single repository-creation operation was authorized at Project_echo commit `e1ec8f74ae812e5df0983cd11a9a0827a9aefb67`; the single baseline-main publication was authorized at Project_echo commit `ece86049e8bfca567f03e7cdfbb8ee1d0b45fd05`. Authenticated readback established private repository `zhenye0616/echo-context`, numeric repository ID `1302541575`, node ID `R_kgDOTaM1Bw`, default branch `main`, and exact baseline commit `0cf7b006eba665c0bf55e82ff04da70f19f01ebb` / tree `70c5cf8352652b3c4c1dce68cd1a5e40d44e4b05`. The creation response was exactly one HTTP 201, the baseline push was exactly one create-only empty-expect leased porcelain `* [new branch]` result, and authenticated readback matched the captured repository identities and exact main SHA. No later implementation, reframe, or recovery path may repeat, adopt, delete, rewrite, or reinterpret either operation.

The builder implements `tools/repository-bootstrap-gate.mjs` as an injected, executable repository-bootstrap gate model that verifies the frozen evidence contract without touching GitHub: expected founder identity; negative name lookup treated as non-authoritative; exactly one create call whose only success is a direct HTTP 201 carrying the stable repository ID/node ID; authenticated owner/name/private readbacks; fully paginated empty branch results plus exhaustive zero-ref `git ls-remote`; the exact empty-expect `git push --porcelain --force-with-lease=refs/heads/main:` command; `* [new branch]` created-by-this-run proof; and exact post-push readback. Redirect, non-201, lost response, wrong identity/visibility, ID drift, incomplete pagination, any foreign ref, pre-existing main even at the identical OID, missing/malformed porcelain, ambiguous push, or failed post-readback each fail with no retry, adoption, cleanup, or later write. Tests consume recorded fixtures only; they never execute repository creation or baseline publication again.

The completed bootstrap full-ref secret scan remains bound to the committed `tools/secret-scan-contract.json` by SHA-256 `b186d99d61f774a6fbf6f16849c7aeb21618d90f79d3f7da4398d88d95925453`. The committed scanner must enforce the same exact gitleaks version/per-platform digest/invocation contract, remain non-shallow and complete-ref, redact secret values, and fail on leaks or scanner infrastructure errors. Personal-account hosting is the accepted current owner topology; the revised AC4 states its exact enforcement limit rather than pretending the unavailable private-repository controls exist.

### AC2 — Convert extraction-finality into explicit successor provenance

Add provenance/extraction-baseline.v1.json, provenance/repository-authority.v1.json, their schemas, and tools/check-repository-authority.mjs. The baseline binds the accepted commit/tree, the 190-path mode/blob/content inventory, item-135 evidence hashes, and independent-review record. The authority record binds canonical repository/default branch, source_authority echo-context/main, artifact_authority versioned-source-artifact, extraction baseline ancestry, runtime_authority false, state_authority false, installed false, and maturity DEV.

Refactor tests/migration/object-closure.test.ts, tests/migration/committed-source-only.test.ts, and every target-only/extraction check that currently treats evolving HEAD as the final 190-file acceptance object. Those tests must inspect the frozen baseline through Git objects while successor tests require that baseline to be an ancestor of every releasable source SHA. Existing item-135 provenance bytes remain immutable historical proof; successor manifests reference rather than rewrite them. Because AC3 adds successor executables while provenance/runtime-inventory.v1.json is part of that frozen evidence, the regenerated runtime inventory is a successor file: provenance/runtime-inventory.v2.json, produced by a committed generator in tools/** over the successor tree's executable/dependency closure. runtime-inventory.v1.json remains byte-identical; `verify:inventory`, its generator, and every test that validates the current inventory bind to v2, and a governance test asserts both v1 byte identity (against its recorded hash) and v2 validity for the successor tree.

README.md must state the exact split: echo-context/main owns source after this item; Project_echo still owns the active daemon, live state, client endpoint, and rollback. It must not say the repository is installed, authoritative at runtime, qualified, or a commercial product.

Add LICENSE as a byte-identical reviewed copy of Project_echo's MIT license; .gitignore covering at least node_modules, dist/build, artifacts, caches, temporary files, logs, and mutable state; and a target AGENTS.md that locks the internal-asset boundary, main-branch review rule, exact-artifact release rule, no-live-state default, and Project_echo coordination-record requirement. These are successor files covered by provenance and the source artifact, not retroactive changes to the frozen extraction baseline.

### AC3 — Make a sibling-free clean clone fully self-testing

Commit the exact standalone `tsconfig.json` and `eslint.config.mjs` configurations used in the accepted item-135 private-clone review. Add explicit `vitest.ci.config.ts` and `vitest.operator.config.ts`. `package.json` defines the exact scripts the gate, CI, and fresh-clone run share: `typecheck`, `lint`, `test:ci` (`vitest run --config vitest.ci.config.ts`), `test:operator` (`vitest run --config vitest.operator.config.ts`), `verify:inventory`, `verify:authority`, `verify:artifact` (`node tools/verify-source-artifact.mjs`), `build:artifact` (`node tools/build-source-artifact.mjs`), and `scan:secrets` (`tools/secret-scan.sh`). `build:artifact` accepts exactly `--source-sha <sha>` and `--out <dir>`, derives the version from the `package.json` version at that committed source SHA, writes the three AC5 files into the given gitignored output directory, and emits the built manifest's canonical-JSON SHA-256 as its exact final stdout line `manifest_hash=<hex>`; the workflow maps that value to its `manifest_hash` job output. `verify:artifact` accepts exactly `--archive <path>`, `--checksum <path>`, `--manifest <path>`, and `--expected-manifest-hash <hex>`, recomputes the manifest's canonical-JSON SHA-256, and fails unless it equals the supplied expected value.

The scripted acceptance entrypoint is `tools/fresh-clone-acceptance.sh`, a committed shell bootstrap for the single Node 22 verifier/orchestrator `tools/fresh-clone-verifier.mjs`. Every invocation begins with the exact common prefix `--node-bin <NODE> --npm-bin <NPM> --git-bin <GIT> --git-version <GIT_VERSION> --sandbox-home <HOME>`, followed by the selected mode's exact arguments below. `NODE`, `NPM`, `GIT`, and `HOME` must be canonical absolute paths supplied by the reviewed CI/coordinator plan; `HOME` and `HOME/tmp` must already be caller-owned mode-0700 directories. The wrapper parses only that fixed prefix with shell builtins, rejects missing/duplicate/relative/newline-bearing values, and its only external process is `exec /usr/bin/env -i` of exact `NODE` and the committed verifier with the common prefix and remaining arguments unchanged. It never searches inherited `PATH` or runs a caller-selected shell command.

Before Node starts, the wrapper constructs the complete environment allowlist: exact `HOME`, `TMPDIR=HOME/tmp`, `PATH=<real NODE directory>:<real NPM directory>:<real GIT directory>:/usr/bin:/bin`, `LANG=C`, `LC_ALL=C`, `TZ=UTC`, `CI=1`, `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_TERMINAL_PROMPT=0`, `NPM_CONFIG_USERCONFIG=/dev/null`, and `NPM_CONFIG_CACHE=HOME/.npm-cache`; no other inherited variable crosses. The verifier rebuilds that same allowlist for every child, with only step-specific nonsecret additions. Thus `NODE_OPTIONS`, `NODE_PATH`, every unapproved `GIT_*` redirect such as `GIT_DIR`, `GIT_WORK_TREE`, `GIT_OBJECT_DIRECTORY`, `GIT_ALTERNATE_OBJECT_DIRECTORIES`, and `GIT_CONFIG_*`, every inherited `npm_config_*`/`NPM_CONFIG_*`, proxy variables, credential variables, and all `ECHO_*` state are absent.

Before the mode trace, the verifier requires `realpath(process.execPath) == realpath(NODE)`, `process.version == v22.22.1`, canonical executable regular-file `NPM` and `GIT`, and exact clone-root `cwd`. It then runs the common two-child prelude exactly once under the controlled environment: absolute `NPM` with `["--version"]` must return exactly `10.9.4`, then absolute `GIT` with `["--version"]` must return exactly `git version <GIT_VERSION>`; CR, NUL, extra lines/bytes, another version, or another executable fails before install. The verifier alone parses mode arguments, validates identity, creates/removes its source-mode temporary directory through Node filesystem APIs, and spawns children from the clone root. Every child spawn is the recorded absolute executable plus argv array with `shell:false`; a shell string, `shell:true`, unlisted executable/script, or any missing, extra, reordered, or duplicated argv element/invocation fails before spawn. There is no generic `npm run <script>` allowlist entry.

After validating the common prefix and selected mode's arguments, the verifier stores the caller's full 40-hex `--source-sha` as immutable expected SHA `S` for the whole run. Each cleanliness boundary is the same two-child pair, in this order: absolute `GIT` with argv `["status","--porcelain=v1","--untracked-files=all"]` must exit zero with empty stdout, immediately followed by absolute `GIT` with argv `["rev-parse","HEAD"]`, which must exit zero and whose normalized full-SHA stdout must equal `S`. Putting the HEAD assertion last closes a persistent clean retarget between the two boundary commands; a retarget after a nonfinal pair is caught at the next pair, and the final pair ends with the HEAD assertion. The pair runs exactly three times in either mode: before `npm ci`, immediately after `npm ci`, and at the end after every mode check and any source-owned cleanup/absence assertion. Thus status count is three and `rev-parse` count is three; neither is once-only.

After the exact common prefix, source mode accepts exactly `--mode=source --source-sha <S>`. Its sole Node-internal setup step occurs after child step 10 and before child step 11: create the single gitignored temporary directory inside the clone and record its exact canonical path as `T`. Let `A`, `C`, and `M` be the archive, checksum, and manifest paths deterministically derived beneath `T`; and let `H` be the validated `manifest_hash=<hex>` carrier parsed from step 11. Its complete mode trace, following the common prelude, is:

| Ordinal | Executable | Exact argv |
|---|---|---|
| 1 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 2 | `<GIT>` | `["rev-parse","HEAD"]` |
| 3 | `<NPM>` | `["ci"]` |
| 4 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 5 | `<GIT>` | `["rev-parse","HEAD"]` |
| 6 | `<NPM>` | `["run","typecheck"]` |
| 7 | `<NPM>` | `["run","lint"]` |
| 8 | `<NPM>` | `["run","test:ci"]` |
| 9 | `<NPM>` | `["run","verify:inventory"]` |
| 10 | `<NPM>` | `["run","verify:authority"]` |
| 11 | `<NPM>` | `["run","build:artifact","--","--source-sha",S,"--out",T]` |
| 12 | `<NPM>` | `["run","verify:artifact","--","--archive",A,"--checksum",C,"--manifest",M,"--expected-manifest-hash",H]` |
| 13 | `<GIT>` | `["fsck","--full"]` |
| 14 | `<NPM>` | `["run","scan:secrets"]` |
| 15 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 16 | `<GIT>` | `["rev-parse","HEAD"]` |

Between child steps 14 and 15, source mode performs one verifier-internal cleanup step through Node APIs: it attempts to remove only the exact recorded `T`, then applies `lstat` to that exact path and requires `ENOENT`. A removal error, any remaining entry at `T` (including a gitignored directory), or any `lstat` result other than `ENOENT` fails before child step 15 can establish success. The verifier never derives a cleanup target from mutable child output and never removes a user/repository path or any path other than recorded `T`.

After the exact common prefix, release mode accepts exactly `--mode=release --source-sha <S> --version <version> --archive <RA> --checksum <RC> --manifest <RM> --expected-manifest-hash <RH>`, where the paths and hash are caller supplied from the delegated exact authorization tuple. It creates no verifier-owned temporary directory, never invokes `build:artifact`, and has this complete mode trace following the common prelude:

| Ordinal | Executable | Exact argv |
|---|---|---|
| 1 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 2 | `<GIT>` | `["rev-parse","HEAD"]` |
| 3 | `<NPM>` | `["ci"]` |
| 4 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 5 | `<GIT>` | `["rev-parse","HEAD"]` |
| 6 | `<NPM>` | `["run","typecheck"]` |
| 7 | `<NPM>` | `["run","lint"]` |
| 8 | `<NPM>` | `["run","test:ci"]` |
| 9 | `<NPM>` | `["run","verify:inventory"]` |
| 10 | `<NPM>` | `["run","verify:authority"]` |
| 11 | `<NPM>` | `["run","verify:artifact","--","--archive",RA,"--checksum",RC,"--manifest",RM,"--expected-manifest-hash",RH]` |
| 12 | `<GIT>` | `["fsck","--full"]` |
| 13 | `<NPM>` | `["run","scan:secrets"]` |
| 14 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 15 | `<GIT>` | `["rev-parse","HEAD"]` |

Each common-prelude row and every table row other than the repeated boundary commands runs exactly once. Source build/verify counts are 1/1; release build/verify counts are 0/1. `test:operator` remains independently runnable but is explicitly rejected in both verifier modes. Gitignored `node_modules` and source `T` do not themselves dirty the status probe, so source cleanup is proven by the separate exact-path `ENOENT` assertion. Any staged, unstaged, or nonignored-untracked entry, persistent clean retarget to a different HEAD between boundaries, install/check-time mutation, wrong executable/version/environment/count/order/vector, or failed check stops acceptance. These boundary probes target persistent accidental mutation; the spec makes no claim against adversarial transient retarget-and-restore or mutate-and-restore between probes.

Release mode's `S` and version are transcribed from the delegated exact authorization tuple and are never ignorable: after `verify:artifact` authenticates the manifest, the verifier fails unless `S` equals the manifest source commit and all three HEAD boundary reads, and version equals both the authenticated manifest version and the committed `package.json` version. Source mode consumes `H` only from the exact build carrier; release mode consumes `RH` only from the approved durable record, never by recomputing an approval value from the downloaded manifest. Unknown, missing, duplicate, or wrong-mode arguments fail with usage text rather than skipping a check.

`tools/fresh-clone-verifier.mjs` exports one production `runFreshClone(argv, deps)` state machine and keeps the shell as the fixed scrub-and-exec bootstrap above. Spawn, environment, executable-realpath, filesystem, temporary-directory, and cleanup operations are injected so executable tests exercise the real transition logic; production adapters remain the only operational path. The expected child plan in tests is a literal independent oracle derived from the common prelude and AC3 tables, never the production trace passed back as its own expected value. Cleanup errors, surviving owned temporary paths, carrier tampering, poisoned inherited state, and non-owned-sentinel mutation fail without being masked by a `finally` path.

Move only the raw Project_echo Git-object recomputation portions of `tests/migration/parity-matrix.test.ts` and `tests/migration/context-tool-evidence.test.ts` into `tests/operator/source-parity.test.ts`; the operator suite requires an explicit `ECHO_SOURCE_GIT_DIR` and pinned source SHA and never supplies a founder-path default.

A fresh no-local clone in a temporary HOME, with Project_echo and sibling repositories absent and all ECHO_* variables cleared, must pass npm ci from the exact lock, typecheck, lint, the complete source-independent CI suite, runtime-inventory validation (against provenance/runtime-inventory.v2.json), repository-authority validation, context-tool roster/parity fixtures, and service parity. There may be no unexpected skip caused by missing Project_echo. Hosted CI must never read /Users/zhenye/Desktop, ~/.echo, ~/.echo-context, credentials, or a live database.

The operator replay remains independently runnable against the pinned item-135 source objects and must still reproduce the accepted source inventory and parity aggregate. It is a release-review check, not a hosted-CI dependency.

### AC4 — Add least-privilege CI and an exact-SHA evidence-carrying landing gate

Add `.github/workflows/ci.yml` (job IDs `quality-macos`, `quality-ubuntu`), `.github/workflows/secret-scan.yml` (job ID `secret-scan`), and `.github/workflows/source-release-build.yml` (job ID `build-artifact`) plus `tools/secret-scan.sh`, injected `tools/target-main-landing-gate.mjs`, and workflow-policy tests. CI and secret scan run on every pull request targeting `main` and every push to `main`, with no omitting path filter or trigger/job condition; the read-only source build runs only on a push to `main` and has no manual, API-initiated, callable, scheduled, or other trigger. All third-party actions are immutable-commit pinned; every checkout sets `persist-credentials: false`; every workflow and job has no permission beyond `contents: read`; and no Actions workflow, reusable workflow, or script other than the single local production `tools/release-publication-controller.mjs` may create a tag, release, or release asset. Every GitHub REST request in AC4/AC6 and both canonical plans pins `Accept: application/vnd.github+json` plus `X-GitHub-Api-Version: 2026-03-10`; upload requests additionally pin their exact content type, binary downloads pin their exact Accept value, and a missing/different version or media-type header fails before use. Node 22.22.1 and npm 10.9.4 are pinned on macOS and Ubuntu. The hosted secret scan enforces complete non-shallow refs and the AC1 contract file; actual top-level leak, scanner-failure, pipeline-masking, unsupported/digest-mismatched binary, incomplete-ref, and fake-secret non-disclosure fixtures must prove nonzero failure without printing secret values.

The current personal/private Free repository cannot provide server-enforced branch protection, administrator enforcement, required PR approval, or a private protected-environment reviewer, and the sole GitHub identity cannot independently approve its own PR. This specification deletes those impossible claims. It does not claim to prevent a malicious or compromised sole repository owner from bypassing, force-updating, deleting, or publishing manually. The enforceable threat boundary is instead exact Git object identity, immutable Project_echo authorization evidence, authenticated destination readback, create/update compare-and-swap, and fail-closed refusal on any drift or ambiguity. Private visibility remains mandatory; making the repository public or silently pretending unavailable controls exist is forbidden.

For pull-request events, `H` is exactly `github.event.pull_request.head.sha`, never `github.sha` or `refs/pull/*/merge`. Each of the three required jobs checks out that immutable `H`, asserts both `git rev-parse HEAD == H` and `HEAD^{tree}` equals authenticated feature-ref readback, and rejects a merge ref, synthetic merge commit, mutable branch substitution, or mismatched tree. After its substantive checks, each job writes one canonical JSON carrier under `schemas/hosted-check-evidence.v1.schema.json` and uploads it under the exact name `echo-context-<job-id>-evidence-<github.run_id>-<github.run_attempt>`. The carrier binds repository ID/node ID, constant workflow path, run ID/attempt, event, PR head ref/SHA, checked-out SHA/tree, logical job ID, toolchain, and completed command list; it does not claim context-unavailable numeric workflow or server-job IDs. Authenticated, fully paginated API readback independently supplies and binds numeric workflow ID/path, carrier artifact ID/name/digest/expiry, server job ID/name/status/conclusion, server job `check_run_url`, linked check-run ID/name/head SHA/status/conclusion/App ID, and run status/conclusion. Every carrier-artifact download uses the exact-ID API plus the AC6 one-302, signed-host-allowlist, credential-stripping, no-second-redirect contract; its raw ZIP SHA-256 must equal the API `sha256:<hex>` digest before safe extraction. A carrier is evidence only when its exact run, server job, and linked check run are successful, the check-run head is `H`, and its downloaded bytes validate and match the API identities. Push-to-main carriers use the pushed commit as both event and checked-out SHA. Missing, duplicate, expired, malformed, stale-SHA, wrong-tree, wrong-workflow, wrong-event, wrong-run/job/check, failed, wrong-app, synthetic-merge, hostile-redirect, or unreadable evidence fails closed.

After a fresh builder completes cycle two, a different fresh agent performs independent implementation review. Let `P_H` be the exact Project_echo implementation head; it contains `tools/echo-context-reviewed-operation-launcher.mjs`, `tools/echo-context-operation-envelope.mjs`, and their tests, while `H` is the exact echo-context feature head. The reviewer owns the exact canonical JSON bytes at `raw/internal/migrations/2026-07-15-136-echo-context-implementation-review.json`, validated by `schemas/implementation-review-evidence.v1.schema.json`; the record binds the exact builder actor ID/run ID, reviewer actor ID/run ID, `P_H`, both Project_echo entrypoint paths/blob OIDs/SHA-256/static-import closures, `H`/tree, every reviewed hosted carrier identity, finding dispositions, and verdict `merge_ready`. The reviewer commits only that record plus its permitted reviewer run log as earlier additive Project_echo commit `R`; the delegated coordinator pushes `R`, then credentialless public readback proves `P_H` and `R` reachable from Project_echo main with `P_H` an ancestor of `R`, and captures the exact record/launcher/envelope paths, Git blob OIDs, byte SHA-256s, schema-valid canonical bytes, and identities. The record does not embed its own containing commit and there is no second fresh-review artifact. The later landing plan/authorization commit binds `P_H`, `R`, and those captured identities. The landing gate normalizes and independently recomputes `builder_actor_id != reviewer_actor_id` and `builder_run_id != reviewer_run_id`; it never trusts a stored boolean. Equality, a missing run identity, a substituted head/tree/launcher/envelope, same-commit or stale/cycle-one review evidence, or any verdict other than `merge_ready` blocks landing.

The unavoidable local trust anchor is the persistent coordinator plus absolute reviewed Git and Node executables, not a repository module falsely claiming to authenticate itself before it runs. Before each operation, that coordinator compares the exact launcher blob/import closure at `P_H` from reviewed local Project_echo Git objects with host-pinned credentialless public readback, materializes only those exact bytes into a new mode-0700 coordinator-owned launcher root, and invokes `runReviewedOperation(input,deps)` from `P_H:tools/echo-context-reviewed-operation-launcher.mjs` with a scrubbed environment. The launcher authenticates the separate envelope path/blob/SHA-256/import closure at `P_H` through the same public adapter before dynamically importing it and calling `runOperationEnvelope(input,deps)`. After the launcher process and every descendant are closed/reaped, the coordinator's outer host `finally` removes only that recorded launcher root with the same primary-error/attached-cleanup-error and non-owned-sentinel rules as the operation root. The Project_echo operation suite models this exact prelaunch materialization/spawn/cleanup contract. No test or record claims that the launcher proves its own pre-execution authenticity; the implementation-review record, public readback, exact-object materialization evidence, and explicit coordinator trust boundary are the authority.

The envelope exposes only modes `land`, `collect`, and `publish`; `land` additionally requires exact phase `evidence` or `write`, while the other modes reject a phase. It creates one invocation-scoped mode-0700 root and records its canonical realpath before any target repository code runs. For either land phase, it creates from reviewed local Git objects a fresh config-isolated detached clone at `H`; proves clean status, `HEAD=H`, `HEAD^{tree}=H^{tree}`, `git fsck --full`, and byte identity for `tools/target-main-landing-gate.mjs`, its complete static-import/schema closure, and `provenance/runtime-inventory.v2.json` against Git objects at `H`; and only then imports the target module. That module exports exactly read-only `collectLandingEvidence(input,deps)` and mutating `runLanding(input,deps)`. The evidence phase may receive only the invocation's authenticated read-only target adapter; the write phase may receive its authenticated write adapter only after public plan/authorization verification. No target controller clones, authenticates, or cleans its outer workspace. A stale/tampered launcher/envelope/entrypoint/import/schema/inventory, dirty tree, wrong cwd, or wrong executable/version yields zero target-controller token exposure and zero API/write.

For every mode, the Project_echo envelope—not target code—owns partial setup, root/clone creation, child-process groups, and the outer cleanup transition. Its single outer `finally` first closes/aborts every still-owned stream or pipe, TERM/KILL/reaps every still-owned process group under the AC4 deadline policy, and then removes only the recorded root without following a child-selected path. Cleanup failure turns otherwise successful work into failure; an earlier primary error stays primary with a sanitized cleanup error attached. The contract covers failures before target code starts and after it returns, including partial root creation, clone, provenance, authorization, controller, cancellation, and cleanup, while a non-owned sentinel remains byte-identical. Target controllers close their own inner transports and children before return but never remove or claim ownership of the envelope root. The plan's `cleanup:false` refers only to remote destination objects and never disables mandatory outer local cleanup.

In the first fresh `land/evidence` invocation, `collectLandingEvidence` binds actor login `zhenye0616`, numeric user ID `73834646`, node ID `MDQ6VXNlcjczODM0NjQ2`, repository ID `1302541575`, node ID `R_kgDOTaM1Bw`, owner/name/private/default-branch/canonical-URL identity, repository object format exactly `sha1`, `main=B`, feature ref `H`, both commit objects and committer epochs, and the target-main observation interval. It requires both `git merge-base --is-ancestor <frozen-baseline> H` and `git merge-base --is-ancestor B H`; a feature head that does not contain current main must first become a new descendant `H'` through an additive merge/commit and receive fresh hosted evidence plus fresh independent implementation review. Fully paginated authenticated workflow-run, job, and artifact readback selects the exact successful CI run containing `quality-macos` and `quality-ubuntu` plus the exact successful secret-scan run at `H`, all produced by the same discovered GitHub Actions App ID and bound to their downloaded canonical carriers. The function has structurally no target mutation dependency, repeats repository/main/feature/evidence readback immediately before return, and returns only a schema-bounded nonsecret tuple. Then its inner resources close, its invocation credential is zeroed, and the outer root is removed; no root, token, adapter, file descriptor, Git object, or mutable path crosses to the write phase.

From that returned tuple, the coordinator deterministically constructs the exact raw merge-commit body: headers are only `tree <H-tree>`, `parent <B>`, `parent <H>`, `author ECHO Merge Coordinator <noreply@echo.local> <merge-epoch> +0000`, and `committer ECHO Merge Coordinator <noreply@echo.local> <merge-epoch> +0000` in that order, followed by one blank LF and exact message `Merge item 136 echo-context canonical source` plus one final LF. `merge-epoch` is the decimal value `max(B-committer-epoch,H-committer-epoch)+1`; no signature, mergetag, encoding, extra header, CR, or mutable clock input is allowed. Under the authenticated `sha1` object format it computes the raw-body SHA-256 and Git commit OID `M` as SHA-1 of `commit <byte-length>\0<body>`. Another object format blocks before plan construction. Because authenticated `B` is an ancestor of reviewed `H`, the authorized tree is exactly `H^{tree}`; missing ancestry or any request for conflict resolution/tree synthesis is new implementation work requiring a reviewed descendant rather than different merge bytes.

Before the target-main write, `raw/internal/migrations/2026-07-15-136-echo-context-target-main-execution-plan.json`, validated by `schemas/target-main-execution-plan.v1.schema.json`, serializes as canonical JSON (UTF-8, lexicographically sorted object keys, no insignificant whitespace, one trailing LF). Its own bytes carry every non-self-referential immutable input: authenticated actor login/user ID/node ID and credential provenance; repository ID/node ID/owner/name/visibility/default branch, object format `sha1`, and literal canonical token-free HTTPS URL; `B`, `H`, `H` tree, both committer epochs, merge-epoch, exact raw merge-body base64/byte-length/SHA-256, `M`, and ordered parents; builder/reviewer actor and run identities; `P_H` plus launcher and operation-envelope paths/blob OIDs/SHA-256/import closures; review commit `R` plus implementation-review path/blob/SHA-256; every workflow/run/job/check/carrier ID/name/path/head/status/conclusion/digest; Actions App ID; the complete ordered evidence-phase and write-phase requests; absolute executable/version, clean-clone/cwd/tree/controller/import/schema/inventory assertions; exhaustive allowed Git config and scrubbed environment; sole direct-URL push argv; expected porcelain grammar; finite per-operation deadlines, outer cleanup, and termination policy; ordered post-write readbacks; backup/rollback identities; one-attempt counters; and literal `retry:false`, `adopt:false`, `cleanup:false`, and `rewrite:false`. Its SHA-256 is recomputed from those exact path bytes. The immutable single-use Project_echo record `raw/internal/migrations/2026-07-15-136-echo-context-target-main-delegated-authorization.json`, validated by `schemas/project-echo-delegated-operation-authorization.v1.schema.json`, independently duplicates and binds those identities plus the exact plan path/schema/hash, intended operation `target_main_fast_forward`, backup identity `refs/heads/main=B`, and rollback identity `no_history_rewrite_new_reviewed_descendant_only`; neither file embeds its own containing commit or hash.

After the independently pushed/read-back review commit `R`, the target-main plan and authorization are committed and pushed together as later additive Project_echo commit `P_L` before the target write. Host-pinned credentialless Project_echo repository/main readback must prove `R` and `P_L` reachable from main, `R` an ancestor of `P_L`, the exact review path at `R`, and both plan/authorization paths at `P_L`, each with bound Git blob OID, byte SHA-256, schema-valid canonical bytes, and cross-file identities. Same-commit review/plan bytes or a missing, mutable-only, mismatched, credential-bearing, or unpushed record gives zero target write.

The coordinator then starts a separate fresh `land/write` invocation through the same authenticated `P_H` launcher/envelope chain. After the new clone/controller provenance gate and credentialless public verification of `R`, `P_L`, review, plan, and authorization, `runLanding` receives that invocation's one target adapter. It repeats actor/repository/main/feature/ancestry/workflow/job/check/carrier evidence and requires byte identity with the evidence tuple; any `main != B`, ref/evidence drift, or stale identity blocks. From plan bytes alone it decodes the raw merge body, validates every fixed header/epoch/parent/tree/message rule and body SHA-256, runs absolute Git `hash-object -t commit -w --stdin`, requires the returned OID equal `M`, and re-reads `git cat-file commit M` byte-for-byte before push. Thus the authorized merge object is deterministically rematerialized in the second clone; no first-invocation object or mutable file crosses phases.

The only permitted target-main write is one absolute-Git invocation whose semantic argv is `push --porcelain --no-verify --no-follow-tags --force-with-lease=refs/heads/main:<B> https://github.com/zhenye0616/echo-context.git <M>:refs/heads/main`; exactly one structurally parsed fast-forward row for the requested destination ref succeeds. It runs from the fresh write-phase clone with system/global config disabled, exhaustive local config restricted to the reviewed clone entries, `core.hooksPath=/dev/null`, `push.followTags=false`, `push.pushOption` empty, `protocol.allow=never` plus `protocol.https.allow=always`, proxy variables/config removed, URL rewrites/remote helpers rejected, and the anonymous credential pipe as the only authentication seam. No remote alias is resolved. Any hook execution, another ref, missing/additional/malformed row, drift, failed/lost/ambiguous response, or failed readback permits read-only reconciliation only—no retry, adoption, remote cleanup, rewrite, or substitution. Authenticated readback must prove repository identity and `main=M`. Hosted CI, secret scan, and the read-only source build must then pass at exact `M` with new exact run/job/carrier identities; a failure is repaired by a new descendant feature branch, fresh independent review, and new additive records, never by rewriting `main`. The migration record pins both authorizations, exact evidence/write readbacks, raw merge-body identities, merge SHA/tree/parents, builder/reviewer identities, all pre/post hosted evidence identities, launcher/envelope evidence, and exact sanitized porcelain proof.

CI and workflows may not checkout or fetch Project_echo, use a founder-local absolute path, consume live user state, publish to a public registry, or create a tag/release/asset. Project_echo authorization is consumed only by local coordinator/controllers through the credential-isolated public adapter defined in AC6; every authenticated local AC4/AC6 private-target read/push/API call uses the same reviewed private-target adapter and anonymous-pipe credential transport defined there. Mutable Project_echo branch bytes are never approval authority.

Every local AC4/AC6 HTTP operation, response-body/binary stream, keyring lookup, and Git/gh child has a monotonic start and fixed hard deadline; outer workflow waiting never substitutes for a per-request deadline. The maximum classes are exact and may be narrowed but never widened by a plan: 30 seconds for `gh auth status`/`gh auth token` and each JSON metadata/readback request including its full body; 120 seconds for a Git push or release mutation request including its response body; 300 seconds for clone/fetch/fsck, workflow-artifact or release-asset binary transfer, and local archive verification/extraction. A binary response is additionally capped at its authenticated expected size, or the authenticated API size for the outer ZIP before extraction. HTTP expiry aborts the request, closes/discards the stream/socket, and waits for terminal abort before another step. Each child starts in its own process group; on expiry or controller cancellation the coordinator sends group TERM, waits the fixed five-second grace, sends group KILL if needed, and reaps the whole group before token teardown or exit. A deadline after any write may have crossed the transport boundary is ambiguous: only bounded read-only destination reconciliation may follow, and only after the write owner is confirmed dead. No retry, replacement child, later write, or detached/orphan transport is allowed.

### AC5 — Build one deterministic, explicitly non-installable source artifact

Set the first successor version to 0.1.0-dev.136.1 and add schemas/source-artifact-manifest.v1.schema.json, tools/build-source-artifact.mjs, tools/verify-source-artifact.mjs, and tests/governance/source-artifact.test.ts. The builder consumes committed Git objects at a reviewed source SHA, never mutable working-tree bytes, and emits exactly three sidecar files: echo-context-<version>-source.tgz, echo-context-<version>-source.tgz.sha256 (coreutils sha256sum format), and echo-context-<version>-source.manifest.json. The manifest is a sidecar and is never a member of the archive it describes — no self-referential size/hash. The manifest serializes as canonical JSON (UTF-8, sorted keys, LF, single trailing newline). The archive root is the single directory echo-context-<version>/ containing exactly the Git-tracked paths at the source SHA in sorted path order, modes taken from Git (0644/0755), uid/gid 0 with numeric owners, member mtimes fixed to the source commit's committer timestamp, and gzip written without name/timestamp metadata.

The manifest binds version, source commit/tree, extraction baseline, lock hash, exact path/mode/blob/content inventory, tar member order/modes/timestamps, source-archive size and source-archive SHA-256, and the literal classifications artifact_kind source, installable false, runtime_authority false, state_authority false, maturity DEV. Hash names are distinct and used consistently everywhere this spec, its records, and its tests mention them: the **source-archive SHA-256** is the digest of echo-context-<version>-source.tgz and is the value the .sha256 sidecar binds; the **manifest hash** is the SHA-256 of the manifest's canonical-JSON bytes; the **lock hash** is the SHA-256 of the package-lock.json blob at the source SHA; the **workflow-artifact digest** (AC6) is the digest of the uploaded CI workflow artifact that carries the three files plus the tuple record. No unqualified "artifact SHA-256" appears in any record, summary, or test. The archive contains source and verification material only; it has no compiled service, LaunchAgent, client config, credential, live state, or install script.

Two builds from the same Git objects and pinned toolchain must be byte-identical — a determinism property proven in tests; the release path itself builds exactly once per AC6. Dirty-byte substitution, wrong commit/tree/version, missing/extra/reordered member, unsafe archive path, mode/timestamp drift, checksum mismatch, baseline non-ancestry, or changing any authority/installability flag fails verification.

### AC6 — Seal one private source prerelease without granting runtime authority

All successor changes remain on the frozen echo-context feature branch while this specification is reviewed. After fresh convergence, a fresh Codex builder repairs that branch without rebasing or rewriting cycle-one history, and a different fresh independent reviewer evaluates the corrected exact Project_echo and echo-context heads. AC4's evidence-carrying landing gate then lands exact merge commit `M` and verifies the three required quality/security jobs plus the read-only source build at `M`. Only exact Git objects from authenticated canonical `main=M` may build or publish. No implementation head reviewed in cycle one is merge or release authority.

Release has one read-only remote build caused by the authorized target-main push, one local read-only `collect` invocation that produces the evidence used to authorize publication, and one later local `publish` invocation that owns every release mutation; there is no write-capable remote publish workflow, manually/API-triggered build, or target authorization ref. `.github/workflows/source-release-build.yml` has only `contents: read`, sole trigger `push` restricted to branch `main`, concurrency group `echo-context-source-build-${{ github.sha }}` with `cancel-in-progress:false`, run name `echo-context-source-build-${{ github.sha }}`, and sole job `build-artifact`. It accepts no workflow inputs and has no manual/API, callable, schedule, tag, pull-request, or other trigger. It fails unless `github.event_name == push`, `github.ref == refs/heads/main`, checkout/HEAD equal `github.sha`, committed version/lock identities validate at that SHA, and `github.run_attempt == 1`. It has a terminal `always()` rerun sentinel, no environment, no target write permission, no Project_echo access, and no tag/release API or publication-controller invocation.

There is no API-created build event, nonce, or discovery owner. After authenticated target readback proves `main=M`, the coordinator starts `collect` through the same exact-object-authenticated `P_H` launcher/envelope chain. Before target token/API access, the launcher credentiallessly reauthenticates public `P_L` plus the exact target-main plan/authorization paths, blobs, hashes, and ancestry. The envelope creates a fresh root and config-isolated detached clone from reviewed local `H` objects, requires `M` absent from the source object database, decodes the authorized raw merge body from the authenticated target-main plan, revalidates object format/base64/length/body SHA-256/fixed fields, runs absolute Git `hash-object -t commit -w --stdin`, requires OID `M`, and re-reads the body byte-for-byte. It then detaches at that locally rematerialized `M` and proves clean status, `HEAD=M`, `HEAD^{tree}=M^{tree}=H^{tree}`, `git fsck --full`, and byte identity for `tools/release-publication-controller.mjs`, its complete static-import/schema closure, `tools/verify-source-release-authorization.mjs`, `tools/git-askpass-fd.mjs`, `tools/github-download-host-policy.v1.json`, and `provenance/runtime-inventory.v2.json` against Git objects at `M`. A target remote clone/fetch, target API call, or credential lookup before this gate is forbidden. Only after the gate does the envelope acquire the invocation's validated target adapter and give it to exported read-only `collectPublicationEvidence(input,deps)` from the authenticated controller.

Inside that read-only state machine, the target adapter authenticates `GET /repos/zhenye0616/echo-context/actions/workflows/source-release-build.yml` as one positive numeric workflow ID whose path is exactly `.github/workflows/source-release-build.yml` and state is `active`. Starting from the recorded target-main push interval, it performs at most 121 fully paginated read-only selections at monotonic offsets 0, 15, 30, …, 1800 seconds. A candidate matches only that numeric workflow ID, run path `.github/workflows/source-release-build.yml@main`, `display_title == echo-context-source-build-<M>`, event `push`, branch/ref `main`, head SHA `M`, run attempt 1, and server `created_at` within the authorized push interval widened by 60 seconds before and 180 seconds after. Every selection requires zero or one candidate; a second candidate is immediate permanent ambiguity. Once one appears, exact-ID GET and job/artifact endpoints must keep that same run identity through `completed/success` and exactly one server job named `build-artifact` with head `M` and `completed/success`. After download/verification and immediately before returning the plan-construction evidence, it repeats the complete fully paginated uniqueness and exact-ID run/job/artifact readback; drift, a later duplicate, or a rerun invalidates the result. Zero candidates at the outer deadline, duplicate, rerun, wrong workflow/event/ref/head/time/title, malformed readback, timeout, failed, or cancelled run fails release authority. Neither the envelope nor the state machine creates, reruns, cancels, or substitutes a build or performs any target mutation; correction requires a new reviewed descendant/main push and fresh records.

The build checks out exact `M` with `persist-credentials:false`, proves extraction-baseline ancestry, asserts actual HEAD/tree, and invokes `build:artifact` exactly once. Before upload it writes canonical `source-release-inner-tuple.v1.json` containing source SHA, source tree, version, source-archive SHA-256, lock hash, manifest hash, and build run ID, plus canonical `source-release-build-evidence.v1.json` containing repository ID/node ID, constant workflow path, event, ref, head SHA/tree, run ID/attempt, logical job ID, toolchain, completed commands, and authority flags; neither record invents context-unavailable numeric workflow or server-job IDs. It uploads exactly one workflow artifact named `echo-context-<version>-release-<M>` containing only those two records and the three AC5 files. Authenticated API readback independently adds exact numeric workflow ID/path, server job ID/name/status/conclusion, workflow artifact ID/name/digest/expiry, and the inner evidence-record path/blob SHA-256. No lookup by latest or by name alone is permitted.

The exact-artifact-ID download endpoint is called with manual redirect handling and must return exactly one HTTP 302. Its `Location` must be an absolute HTTPS URL with no userinfo, fragment, or non-443 port and a host accepted by committed `tools/github-download-host-policy.v1.json`, whose only case-normalized, DNS-label-aware suffix rules are `.actions.githubusercontent.com`, `.githubusercontent.com`, `.blob.core.windows.net`, and `.amazonaws.com`; string lookalikes such as `evilamazonaws.com` do not match. The one redirected GET sends no Authorization, Cookie, GitHub API-version header, or other credential; only the signed URL and nonsecret binary Accept/User-Agent headers cross hosts. It must return direct HTTP 200 with no second redirect. The signed URL/query is held in memory only and never persisted or logged. Artifact metadata must carry digest `sha256:<64-lowercase-hex>`; the raw unextracted ZIP body is hashed before extraction and must equal that exact hex. The completed successful run, successful job, evidence carrier, and exact artifact yield the approved build tuple.

Within the same `collect` invocation, `collectPublicationEvidence` downloads the raw ZIP by exact run and artifact IDs into the envelope-owned paths, verifies ownership/name/expiry/API digest and raw bytes before safe extraction, validates the two canonical records and exactly three release files, and independently runs `verify:artifact` against the carried manifest hash without rebuilding. It returns only the nonsecret, schema-bounded evidence tuple and hashes to the Project_echo coordinator; the invocation then closes its target resources, zeroes its credential Buffer, and the outer envelope removes its root. The coordinator uses that returned evidence—never a mutable path or secret—to construct the annotated-tag body as these exact UTF-8 bytes with LF separators and one final LF, where `<epoch>` is the decimal committer timestamp of `M` and the timezone is fixed `+0000`:

```text
object <M>
type commit
tag v<version>
tagger ECHO Release Coordinator <noreply@echo.local> <epoch> +0000

echo-context source release
version: <version>
source-sha: <M>
```

Before authorization, the coordinator computes and records both SHA-256 of those raw tag-body bytes and the Git tag-object OID obtained by hashing the canonical `tag <byte-length>\0<body>` object; it also records object target `M`, type `commit`, tag name, tagger name/email/epoch/timezone, and the exact annotation. No tag field is selected after authorization.

Only after `collect` succeeds does the coordinator construct canonical JSON `raw/internal/migrations/2026-07-15-136-echo-context-source-release-publication-plan.json`, validated by `schemas/source-release-publication-plan.v1.schema.json`, using the same canonical byte rules as AC4. Its own bytes carry every non-self-referential immutable input: item/spec/seal/delegation identities; exact `P_H` plus the reviewed-operation-launcher and operation-envelope paths/blob OIDs/SHA-256/import closures used for both `collect` and `publish`; builder/reviewer actor/run identities and reviewed heads; authenticated GitHub actor login/user ID/node ID plus GitHub CLI keyring/OAuth/HTTPS credential provenance and exact observed nonsecret scope set; target repository ID/node ID/owner/name/visibility/canonical token-free URL plus `M`/tree; version; complete source/checksum/lock/manifest/raw-ZIP/inner-carrier tuple and hashes returned by `collect`; exact unique push workflow/run/job/artifact/evidence fields; exact tag body/tagger/byte-hash/object-OID/peeled-source fields; release and percent-encoded asset names; authority flags; fixed Project_echo authorization path; credential-boundary declarations; clean launch closure, scrubbed environment/config, owned-workspace paths, and finite deadline/termination table; the complete ordered artifact checks and target preflight; local tag-object materialization/verification; sole direct-URL tag push; every exact REST method/host/endpoint/header/body; response grammar; ordered readback; final postcondition; intended operation and backup/rollback identities; one-attempt counters; and literal `retry:false`, `adopt:false`, `cleanup:false`, and `automatic_reentry:false`. Its SHA-256 is recomputed from those exact path bytes. The immutable Project_echo record `raw/internal/migrations/2026-07-15-136-echo-context-source-release-delegated-authorization.json`, validated by `schemas/project-echo-delegated-operation-authorization.v1.schema.json`, independently duplicates and binds those identities plus the exact plan path/schema/hash, intended operation `publish_private_source_prerelease`, backup identity `destination_namespace_empty_preflight`, rollback identity `none_automatic_fail_stop`, and literal ambiguity policy; neither file embeds its own containing commit or hash. The plan and authorization are committed and pushed together as an additive public Project_echo commit, and the resulting containing commit plus both exact path/blob/schema/hashes and public-main ancestry readback are pinned externally before any target release write.

Server-generated release and asset IDs and signed download URLs are not invented before authorization. The canonical plan defines typed, single-assignment state variables `release.id`, `release.upload_url`, `asset.archive.id`, `asset.checksum.id`, and `asset.manifest.id`, plus an ephemeral `download.location`; for each it names the sole prior unambiguous response field that may assign it, the schema/range constraint, the exact downstream endpoint-template substitutions, and the readback that must authenticate the assignment before the next mutation. A variable cannot be prefilled, reassigned, derived by name/latest, persisted as approval authority, or consumed before its defining response and readback. The plan hash binds these templates and derivation rules; the post-operation migration record binds the concrete captured IDs, while signed URLs are never recorded.

The local controller invocation pins the exact Project_echo authorization commit/path/blob hash and plan path/blob hash. Its dedicated public-ECHO adapter may call only host-pinned `https://api.github.com` endpoints for `zhenye0616/ECHO`, sends no `Authorization`, `Cookie`, proxy credential, Git credential-helper, askpass, `http.extraheader`, target token, or other secret, rejects every redirect and host/scheme change, proves the supplied authorization commit is an ancestor of public Project_echo main, and reads the exact path bytes at that commit. It constructs an explicit clean public-read environment with credential/token variables removed and system/global/local Git credentials, helpers, askpass, and extraheaders disabled rather than inheriting process state.

The separate private-target adapter uses the founder's existing GitHub CLI keyring credential without creating or changing credential state. Only after the current invocation's target-clone provenance gate (and, for `land/write` or `publish`, credentialless public authorization verification), with `GH_TOKEN`, `GITHUB_TOKEN`, and equivalent inherited overrides removed, it runs `gh auth status --hostname github.com` and requires active account `zhenye0616`, keyring storage, OAuth token class, and HTTPS Git protocol. That same `land/evidence`, `land/write`, `collect`, or `publish` invocation then independently executes `gh auth token --hostname github.com` exactly once with the secret absent from argv/environment and accepts stdout only as one nonempty token byte sequence followed by exactly one LF; CR, NUL, an empty value, extra line, missing LF, or any extra byte fails. It retains that one nonlogged in-memory Buffer for that invocation and uses the same Buffer for authenticated API `GET /user`, which must return login `zhenye0616`, user ID `73834646`, and node ID `MDQ6VXNlcjczODM0NjQ2`, while the normalized `X-OAuth-Scopes` set must equal `gist,read:org,repo,workflow`. No Buffer, credential-derived object, or private adapter crosses between invocations. A second acquisition within one invocation, different identity, storage source, protocol, missing/extra scope, absent scope header, or auth error gives zero target-controller access/write. No token bytes or token hash enter a record.

The invocation's single validated Buffer never reaches the public adapter. In-process HTTPS places it only in the initial target API request's Authorization header; a signed-download redirect never receives it. Git HTTPS authentication uses committed `tools/git-askpass-fd.mjs`: for each exact Git operation the controller copies the retained Buffer into a fresh anonymous inherited pipe, passes only its descriptor number plus `GIT_ASKPASS_REQUIRE=force` and `GIT_TERMINAL_PROMPT=0` to that Git child, disables every other credential/helper/extraheader/config/proxy/rewrite source, and uses the literal token-free canonical URL. The helper returns fixed username `x-access-token` without consuming the pipe and reads the token only for the password prompt. Each Git call gets one pipe; on completion/failure the controller closes both ends and removes its per-call environment while retaining the validated master Buffer for later exact target calls. In the invocation's single final teardown—after all HTTP streams and child groups are closed/reaped—it overwrites that Buffer and persists no token, helper state, config, file, or URL. Secret bytes may appear only inside an initial target Authorization header or anonymous credential pipe, never argv, ordinary environment values, Git config, filesystem, plan/record, diagnostics, redirected download, or the public adapter. Keyring/account rotation fixtures after acquisition must prove no second fetch/substitution; redirects and upper/lowercase proxy overrides must fail before transport. The operation creates no new credential or signing-key state. Mutable Project_echo branch content is not authority; only the pinned commit/path/hash/schema bytes are.

After the additive plan/authorization commit is publicly readable, the coordinator starts a separate `publish` invocation through the same exact-object-authenticated `P_H` launcher/envelope chain; no root, clone, file descriptor, stream, child, token, signed URL, or mutable object crosses from `collect`. Before target token/API access, the launcher credentiallessly authenticates both public authorization layers: `P_L` with the target-main plan/authorization that seals raw `M`, and the later source-publication plan/authorization commit. The envelope creates a new mode-0700 root with exact `clone`, `download`, and `extract` children, again starts from reviewed local `H` with `M` absent, rematerializes/re-reads exact `M` solely from the authenticated target-main plan, detaches at `M`, and repeats the clean/tree/fsck/controller/import/schema/inventory gate. It then invokes the committed source-release authorization verifier from that clone against the pinned public record/plan. Only after both authorization layers and provenance pass does this invocation acquire its one validated target credential Buffer and provide the target adapter to the authenticated controller. Before its first write, the controller reauthenticates canonical `main=M`; reruns the complete bounded unique workflow/run/job/artifact selection; redownloads by the authorized exact IDs; and revalidates workflow-artifact ZIP digest, safe extraction, carrier bytes, all three AC5 files, and every nonsecret field against the plan. Any duplicate/rerun, identity/hash drift, or failure blocks before mutation. The fresh download, rather than any path or byte retained from `collect`, is the sole publication input.

`tools/release-publication-controller.mjs` exports exactly the read-only `collectPublicationEvidence(input,deps)` and the mutating `runPublication(input,deps)` state machines. Both are invoked only through the authenticated Project_echo operation envelope and the single production adapter set; `collectPublicationEvidence` has structurally zero target-mutation capability, while `runPublication` alone owns every AC6 target mutation. HTTP, Git subprocess, filesystem reads, diagnostics, clock, credential-isolated public/target adapters, and hard-loss checkpoints are injected so tests execute these production state machines; test adapters never form another selection, download, or mutation implementation. Each target state machine closes its own streams, pipes, and process groups before return; the envelope's outer `finally` alone removes the root and covers partial setup/clone/provenance/authorization/controller/cleanup failure. Before its first write, `runPublication` verifies the exact Project_echo authorization and plan, repository identity, `main=M`, source/artifact/tag identities, and a fully paginated empty target publication namespace: no tag ref, draft/published release, or release asset. Any foreign or apparently attributable entry, authentication error, malformed response, redirect, pagination loop/truncation, or readback ambiguity blocks.

The fixed write order is exact local tag-object materialization/verification → annotated-tag create-only push → exact tag readback → draft release POST → exact draft readback → archive upload/readback → checksum upload/readback → manifest upload/readback → publish PATCH → exact post-publish readback. Local materialization uses the authorized raw body, verifies its byte SHA-256 and `git hash-object -t tag -w --stdin` result equal the authorized immutable tag-object OID, and re-reads that OID byte-for-byte before push. It creates no mutable local tag ref. The sole tag write is one absolute-Git invocation whose semantic argv is `push --porcelain --no-verify --no-follow-tags --force-with-lease=refs/tags/v<version>: https://github.com/zhenye0616/echo-context.git <TAG_OBJECT_OID>:refs/tags/v<version>`, under the same exhaustive config/environment isolation as the main push. Exactly one structurally parsed porcelain row with flag `*`, destination `refs/tags/v<version>`, and summary `[new tag]` succeeds; hook execution, follow-tag publication, up-to-date, another flag/ref/summary, local-ref/remote-alias substitution, or any additional/malformed row fails. The sanitized proof contains only flag, destination ref, and captured tag-object OID.

Each remote mutation is attempted exactly once with transport retries disabled. Draft creation accepts only direct HTTP 201 with parseable body carrying a newly captured numeric release ID, exact tag/name/`draft:true`/`prerelease:true`, and exact release-scoped `upload_url` template `https://uploads.github.com/repos/zhenye0616/echo-context/releases/<release-id>/assets{?name,label}`. Each asset upload expands that captured template to `https://uploads.github.com/repos/zhenye0616/echo-context/releases/<release-id>/assets?name=<exact-percent-encoded-name>` and accepts only direct HTTP 201 whose parseable body carries a new numeric asset ID plus exact decoded name, size, and `state: uploaded`; no nonexistent asset-response `release_id` field is assumed. The controller then fully paginates `GET https://api.github.com/repos/zhenye0616/echo-context/releases/<release-id>/assets`, requires the exact captured ID exactly once under that release, and fetches exact JSON metadata through `GET https://api.github.com/repos/zhenye0616/echo-context/releases/assets/<asset-id>`.

The separate binary GET to that same exact-ID endpoint uses `Accept: application/octet-stream`, manual redirects, and accepts either direct HTTP 200 bytes or exactly one HTTP 302 satisfying the same signed-download policy as the Actions artifact. A redirected request sends no Authorization, Cookie, or GitHub API header and must terminate in direct HTTP 200 with no second redirect; only the terminal body is hashed. This exact metadata-plus-binary sequence runs before the next write and again in the final postcondition. Publish accepts only direct HTTP 200 with parseable body carrying the same release ID/tag/name and `draft:false`/`prerelease:true`; it sends string `make_latest: "false"`. A malformed or mismatched nominal-success body, hostile Location, credential-bearing redirected request, or unexpected redirect/status is ambiguous even if later destination state looks correct.

The controller retains the captured release record and all three `{id, name, size, sha256, expected_bytes, upload_endpoint}` asset records. After every unambiguous response it requires exact authenticated response-plus-readback identity before the next write. Immediately before success, it completely paginates tags, releases, and the captured release's assets; proves exact tag-object OID/body/peeled `M`, exact release ID/tag/name/flags and not-latest observable; requires the asset-ID set to equal the three captured IDs; and re-fetches exact metadata and re-downloads/re-hashes byte-identical content for each captured ID. Matching names or bytes under a different release/asset ID fail; same-content delete/recreate, ID drift, extra/missing/foreign objects, changed bytes, changed flags, or a different tag object fails.

Destination namespace readback is the sole durable publication authority; Actions state, run logs, plan markers, and diagnostics are evidence only. Each next write requires both an unambiguously successful response and exact destination readback of the prior write. If the local controller survives a timeout, lost/ambiguous response, malformed success, or failed/ambiguous readback, it may perform read-only reconciliation only and then exits nonzero with zero retry, repeat write, delete, remote cleanup, adoption, or later mutation in that invocation; mandatory local owned-root cleanup still runs. Process death, cancellation, or host loss ends the only write owner, so no remote/downstream job can continue publication and no post-mortem log guarantee exists. Tag-only, tag-plus-draft, partial-assets, and apparently-complete namespaces mechanically block every cold re-entry. Any later manual disposition is a separate exact authorized operation; this item provides none.

There is intentionally no separate durable attempt/consumption marker. If an invocation ends before any target object exists—or a first-write transport failure leaves an authenticated empty namespace—a cold process cannot mechanically distinguish that attempt from an authorization never executed. Under the accepted trusted single-coordinator/single-owner threat boundary, coordinator policy forbids replaying the same build/plan/authorization and requires a new reviewed descendant/main push, unique push build, plan, and Project_echo authorization, but this empty-namespace cold-replay prohibition is not claimed as server-enforced. The controller still exposes no retry/re-entry/adoption command and never launches a second invocation automatically. Adding a durable marker solely to claim stronger replay prevention would violate the founder-approved destination-as-sole-publication-authority design and requires a future reviewed architecture change.

A fresh no-local clone downloads the three private prerelease assets by exact captured IDs, verifies archive/checksum/manifest bytes and the approved content tuple, and runs the AC3 release-mode trace with the externally approved manifest hash. `raw/internal/migrations/2026-07-15-136-echo-context-repository-bootstrap.md` records the canonical URL/repository IDs, baseline and final main SHAs/trees, implementation-review commit/path/blob/hash and landing-plan identities, exact pre/post hosted run/job/carrier identities, unique read-only push-build workflow/run/job/artifact identities, Project_echo authorization commit/path/hash, publication-plan path/hash, tag-body hash/tag-object OID/tagger fields, local controller actor/run identity, tag/release/asset IDs, complete content tuple, fresh-clone and owned-cleanup evidence, builder/reviewer identities, and `authority:false` / `installed:false`, without secrets or expiring URLs.

The handoff to item 137 is the immutable content tuple source SHA + source tree + version + source-archive SHA-256 + lock hash + manifest hash; the build run/job/artifact and local publication fields are provenance recorded in the migration record, not mutable selectors in the 137 contract. Item 137 must consume these exact landed outputs and separately review any later-release generalization; item 136's empty-namespace controller is not automatic authorization for another release. At completion, echo-context owns source and source-artifact authority only; Project_echo remains installed runtime and live-state authority, and no machine client points at the new repository or artifact.

## Out of Scope (Don't Drift)

- No LaunchAgent, machine-wide/per-user install, service composition root, MCP client registration, port, bearer-token, or loopback-exposure decision.
- No live database, capture checkpoint, onboarding state, project config, credential, or client-config read/copy/migration.
- No active daemon switch, dual-run, rollback ceremony, Project_echo context freeze, or runtime/state authority transfer.
- No npm/public package publication, public repository visibility, auto-update channel, source signing program, or generic release framework.
- No context-tool, capture, storage, retrieval, schema, clustering, enrichment, or embedding behavior change.
- No Project_echo deletion/rename/deprecation and no echo-brain or echo-loop repository/install work.
- No Team-product maturity advancement; this internal source release remains DEV.

## Risks

- Extraction evidence could be weakened by rebasing it onto evolving HEAD. The frozen-baseline plus ancestry contract is the fallback; existing evidence bytes are never rewritten.
- Hosted CI could appear green only because the founder machine supplies Project_echo or state. AC3 removes implicit paths and proves a clean sibling-free clone.
- Publishing history could expose a secret. Pre/post full-history scans block the private push/release without printing values.
- Native better-sqlite3 behavior can differ across hosted runners. CI pins Node/npm and uses macOS plus Ubuntu; item 137 separately owns the exact Darwin runtime closure.
- The current private personal Free topology cannot stop a malicious or compromised sole owner from bypassing policy. AC4/AC6 make that limitation explicit and instead fail closed on stale identity, unreviewed bytes, missing immutable Project_echo authorization, compare-and-swap drift, duplicate exact-`M` push builds, or ambiguous destination state; paid server enforcement remains a future topology upgrade, not an item-136 claim.
- A source archive could be mistaken for an installable runtime. Its filename, manifest, tests, README, and release record all state installable:false and authority:false.
- Repository creation and baseline publication are already-completed immutable history and may never repeat. Target-main landing is mechanically single-use through its exact `B` lease. Local tag/release publication is single-attempt per controller invocation and bound to exact objects and canonical plan bytes; nonempty destination state mechanically blocks replay, while empty-namespace cold replay remains the explicit trusted-coordinator policy limitation in AC6.
- The first-release local controller contract is intentionally not reusable for a populated namespace. Item 137 owns the separately reviewed later-release generalization; absence of that propagation blocks its publication rather than widening item 136 again.

## Tests

- tests/governance/repository-authority.test.ts verifies the exact frozen baseline, successor ancestry, canonical authority fields, immutable item-135 evidence hashes (including byte identity of provenance/runtime-inventory.v1.json against its recorded hash), validity of the successor provenance/runtime-inventory.v2.json for the successor tree, that `verify:inventory` and its generator bind to v2, and negative wrong-remote/wrong-baseline fixtures.
- tests/echo-context/operation-envelope.test.ts exercises Project_echo's production `runReviewedOperation(input,deps)` and `runOperationEnvelope(input,deps)` using only Project_echo-owned generated temporary Git fixtures whose committed harmless target modules expose the required export names; it never imports echo-context, needs a sibling checkout, or fetches the private target. The test explicitly models the external trust anchor: exact launcher bytes are materialized from a pinned fixture `P_H` Git object and matched to credentialless public-readback bytes before Node starts; the launcher itself is not credited with self-authentication. It proves the coordinator host `finally` reaps the launcher process tree and removes the distinct launcher-materialization root on pre-spawn, launch, operation, cancellation, and cleanup failure. It then proves the launcher authenticates envelope path/blob/hash/import closure before import; every `land/evidence`, `land/write`, `collect`, and `publish` invocation creates a distinct operation root/fresh fixture-target clone; and exactly one named target export is invoked after its gate. `collect`/`publish` fixtures start with local `H` and provably absent `M`, authenticate public `P_L` without target credentials, rematerialize/re-read exact `M` from sealed plan bytes, detach at `M`, and prove clean/tree/fsck/controller/import/schema/inventory before any target token/API/remote clone/fetch. Partial root/setup/clone/rematerialization/provenance/authorization/child/cancellation/TERM-resistant-child/cleanup fixtures prove stream closure, process-group TERM/KILL/reap, exact-root removal, primary-error preservation with attached sanitized cleanup failure, and byte-identical unowned sentinels for both root layers. Wrong mode/phase, stale/tampered launcher/envelope/plan/raw merge body, wrong executable/version/cwd/tree, target access before the gate, target-owned root removal, reuse of a root/token/object between land phases or between `collect`/`publish`, or more than one target invocation fails. Actual cross-repository composition is authenticated at operation time and recorded in plan/readback evidence; neither repository's CI depends on the other.
- tests/governance/source-artifact.test.ts builds twice, compares bytes, validates the manifest/inventory, and rejects dirty, missing, extra, reordered, unsafe-path, checksum, version, tree, and authority-flag mutations.
- tests/governance/workflow-policy.test.ts parses YAML structurally through a declared pinned parser dependency and mutation fixtures, not source-string self-assertions. It proves immutable action pins; `persist-credentials:false`; exact Node/npm; CI/secret triggers and required job IDs on every PR/push to main; pull-request checkout from `github.event.pull_request.head.sha` rather than `github.sha`/merge refs; in-job HEAD/tree assertions; exact canonical carrier generation/upload; least privilege; no environment; one read-only source-build workflow triggered only by a main push, SHA-bound run/concurrency names, attempt-one and terminal rerun sentinel, and no workflow inputs/manual/API/callable/scheduled trigger; and absence of every remote write-capable publish workflow, tag/release API call, reusable writer, or alternate release-mutation owner. Malformed graph, permissions, needs, trigger, action pin, rerun, concurrency, checkout, carrier, artifact, synthetic-merge, or alternate-writer variants each fail.
- tests/governance/landing-gate.test.ts directly executes echo-context's exported read-only `collectLandingEvidence(input,deps)` and mutating `runLanding(input,deps)` with injected authenticated adapters; it never imports Project_echo and does not retest outer clone/launcher/envelope provenance. Wrong actor login/user ID/node ID or credential provenance, repository ID/node ID/owner/name/visibility/default branch/canonical URL, baseline SHA, feature SHA/tree, failure of frozen-baseline→H or B→H ancestry, presence of a mutation-capable evidence dependency, builder/reviewer actor or run identity, identity equality, review commit `R` ordering/path/blob/hash/verdict, same-commit or stale/cycle-one review evidence, missing/failed/stale-SHA/wrong-app/duplicate-selected/unreadable workflow run/job/carrier artifact, canonical plan bytes/hash, delegated authorization, raw merge-body/base64/length/hash/header/parent/tree/epoch/message/OID mismatch, direct-URL push argv/lease/porcelain, or post-push readback fails. Poisoned `origin`, `insteadOf`, proxy, remote-helper, executable pre-push hook, `push.followTags=true`, and orphan child each prove zero unintended ref/target write. A fixture where H omits newer B requires a reviewed descendant rather than alternate merge bytes. Main/evidence drift and every lost/ambiguous/deadline push outcome allow read-only reconciliation only after process-group termination, with exactly one attempt and no retry/adoption/rewrite. A valid two-phase fixture proves the first call returns only schema-bounded nonsecret evidence; a separate second call rematerializes exact `M` solely from plan bytes, re-reads its raw commit body, emits one direct canonical fast-forward porcelain row, and authenticates `main=M` without any first-phase mutable object.
- tests/governance/repository-bootstrap-gate.test.ts executes the production injected AC1 model over recorded fixtures without live writes. It covers expected `GET /user`, non-authoritative negative lookup, single POST-create direct HTTP 201, stable repository ID/node ID, fully paginated empty branch list, exhaustive zero-ref `ls-remote`, exact create-only main-push argv/porcelain, and post-readback. Redirect/non-201/lost/malformed response, wrong identity/visibility, ID drift, later-page foreign ref, identical pre-existing main, missing `--porcelain`, up-to-date/multirow output, ambiguous push, and failed post-readback each assert zero later write, retry, adoption, or cleanup.
- tests/governance/source-release-build-selection.test.ts runs echo-context's production read-only `collectPublicationEvidence(input,deps)` directly with injected authenticated read-only target adapters and a fake monotonic/UTC clock; it neither imports nor locates Project_echo, and the AC3 clean-clone/hosted-CI contract remains sibling-free. It binds the authenticated numeric workflow ID/path/active state, target-main push interval, 60/180-second skew, and at-most-121 selection/1800-second outer schedule. Visibility at every boundary, later-page unique match, exact-ID stability, exact run path/title/`push` event/ref/head/attempt, one successful `build-artifact` job, exact artifact/evidence identities, exact-ID download/verification, repeated pre-return uniqueness, and schema-bounded nonsecret return pass; zero/duplicate/later-duplicate candidates, rerun, wrong workflow/event/ref/head/time/title, pagination loop/truncation, timeout, malformed/auth failure, failed/cancelled run, presence of a mutation-capable dependency, and any mutation attempt fail permanently with no tag/release/repository-ref write. A separate `runPublication` fixture repeats the same unique selection and exact-ID download against the authorized collect tuple and rejects every mismatch before the first write. Project_echo's operation-envelope suite owns the independent generic provenance-before-invocation composition proof; no cross-repository test runner exists or is required.
- tests/governance/release-authorization.test.ts validates review commit `R`, the exact canonical Project_echo publication plan/authorization commits and paths, their schemas/hashes, complete build tuple, unique push workflow/run/job/evidence identities, exact target/Project_echo source SHAs, exact tag-body bytes/SHA-256/object OID/tagger fields, source/version/artifact/authority fields, intended operation, and fail-stop backup/rollback identities. Missing/tampered/wrong public record commit/path/blob, non-ancestor or same-commit review/plan evidence, noncanonical plan, source/version/run/job/artifact/digest/repository/tag/authority/plan mismatch, `run_attempt>1`, or identity substitution blocks before target mutation. Fixtures prove zero repeated mutation within one invocation, zero automatic second invocation, and mechanical cold blocking for every nonempty namespace; an empty-namespace cold-start fixture documents rather than falsely rejects the explicit trusted-coordinator replay limitation. Carrier fixtures reject artifact retrieval by name/latest, wrong run/ID/name/expiry/API or raw-ZIP digest, unsafe extraction, wrong tuple, rebuild-on-publish, and any private/credentialed Project_echo dependency. Repository-policy fixtures also require absence of `source-release-approval.v1.json`, every `release-authorizations/*` ref mechanism, and every remote build/publication mutation owner.
- The same release-authorization suite executes the production public-ECHO/private-target adapters and proves host/scheme pinning, redirect rejection, public commit ancestry/exact-path byte read, and complete credential isolation. A sentinel target token, Authorization/Cookie/proxy header, credential-helper, askpass, `http.extraheader`, token-bearing argv/env/config/file, or diagnostic leak fails; the known sentinel must be absent from captured public requests, subprocesses, stdout, and stderr. Target authentication is accepted only inside exact private-target calls. Negative fixtures cover inherited token overrides, malformed token stdout, a second token acquisition, keyring/account rotation after acquisition, wrong active account/user ID/node ID, non-keyring storage, non-HTTPS Git protocol, missing/extra OAuth scope, absent scope header, auth failure, proxy/rewrite/redirect substitution, and failure to zero the one retained Buffer after every stream/group is closed.
- tests/governance/github-publication-adapters.test.ts executes the production target REST, Link-pagination, binary-download, credential-pipe, deadline, process-group, and Git-porcelain adapters against protocol-faithful transcripts rather than the state-machine fake. It proves every exact API-version/media-type header; ordinary-REST redirect/malformed-body rejection; exact `api.github.com` versus `uploads.github.com` host/path/method/header/body routing; percent encoding; multi-page RFC 8288 `Link` traversal and loop/cross-host/truncation rejection; Actions-artifact exact one-302 flow; release-asset direct-200 and one-302 flows; DNS-label-aware signed-host allowlist matching; HTTPS/no-userinfo/no-fragment/port rules; zero Authorization/Cookie/API header on the redirected request; no second redirect; signed-URL nonlogging; exact-ID `Accept: application/octet-stream` bytes; protocol-accurate release/asset response parsing without `release_id`; target-token presence only in the initial target Authorization/anonymous pipe; helper teardown; direct canonical URL; `--no-verify`/`--no-follow-tags`; and exact stdout/stderr parsing of one Git porcelain row. Hostile Location, disallowed suffix lookalike, redirected credential, never-resolving HTTP/body stream, orphan child, TERM-resistant child, timeout after write, executable hook, follow-tags, alias/rewrite/proxy/helper, malformed status/header/JSON/link/binary/porcelain, and additional-row fixtures fail before any later write.
- tests/governance/release-publication-controller.test.ts drives exported `runPublication(input,deps)` against one stateful fake destination and the production transition logic after the outer-envelope provenance and authorization fixtures have supplied authenticated dependencies. At pre-write unique-build revalidation/tag push/draft creation/each of three uploads/publish it injects timeout before/after application, lost response, nonaccepted status, malformed/mismatched success body, readback transport/auth/pagination/malformed/identity/byte failure, controller-process death, host/system loss, cancellation, total diagnostic-log loss, and inner download/extraction failure. Every surviving failure asserts one attempt, controller-owned process-group/stream closure before return, read-only-only reconciliation, nonzero exit, and zero later retry/repeat/delete/remote-cleanup/adoption/write; every hard-loss fixture asserts no later write owner and accepts failed or cancelled terminal state without post-mortem-log claims. Partial root/setup/clone/provenance/outer-cleanup and unowned-sentinel assertions belong only to tests/echo-context/operation-envelope.test.ts.
- The same controller suite covers canonical tag-body construction and byte/OID revalidation, immutable-OID porcelain proof, absence of mutable local tag refs, draft/tag ordering, fixed serial asset order, exact release/asset IDs, protocol-faithful response bodies, release-scoped asset endpoint plus fully paginated exact-ID enumeration, metadata/download byte binding, not-latest semantics, multi-page/loop/truncated/auth-failed namespace reads, and foreign objects. Re-entry fixtures from tag-only, tag+draft, partial-assets, apparently-complete, or any other nonempty namespace must stop before a write with zero retry, adoption, cleanup, or continuation. Final-postcondition races delete/recreate each asset with identical name and either identical or changed bytes after its first readback; success requires the original three captured asset IDs plus final exact-ID metadata/download byte hashes, exact release ID/name/tag/flags, exact tag object, and no extra namespace object.
- Secret-scan executable fixtures invoke the actual top-level script/result path with shallow/incomplete refs, unsupported/digest-mismatched binaries, scanner infrastructure failure, injected leak, and pipeline masking; each is nonzero. A known fake literal never appears in stdout/stderr. The bootstrap record's scanner-contract SHA-256 must equal the committed contract digest, and complete-ref scans cover every reachable repository ref.
- AC1 repository-creation fixtures additionally prove `GET /user` must authenticate the expected founder account, a negative name lookup — including HTTP 404 — is not durable absence or created-by-this-run authority, only the direct single-attempt `POST /user/repos` HTTP 201 response may establish the captured repository `id`/`node_id`, every later readback must return those same IDs, and any redirect, non-201 status, lost response, ID drift, nonempty branch list, or foreign ref yields zero initial push.
- tests/operator/source-parity.test.ts, with explicit source Git dir/SHA, recomputes the item-135 source inventory and eight-tool parity aggregate; absence of those inputs fails clearly rather than skipping.
- tests/migration/object-closure.test.ts and tests/migration/committed-source-only.test.ts prove the 190-file extraction baseline stays closed while successor HEAD may add reviewed files.
- Existing tests/api/context-only-roster.test.ts and tests/integration/context-service.test.ts keep the exact eight-tool and service semantics unchanged.
- A scripted fresh-clone acceptance run executes exported production `runFreshClone(argv, deps)` through injected spawn/environment/executable/filesystem/temp adapters for both exact AC3 traces with no sibling repository or inherited `ECHO_*` state. Tests derive expected steps from a literal immutable oracle independent of the production trace—passing one production object as both actual and expected is forbidden. Tests assert the shell parses only the exact common prefix with builtins and execs absolute `/usr/bin/env -i` plus the supplied canonical Node; the verifier authenticates Node/npm/Git realpaths and versions; every child uses the recorded absolute executable, `shell:false`, clone-root cwd, and exact allowlisted environment; the common prelude runs once; and source/release trace arrays equal the AC3 tables entry-for-entry. Poisoned/minimal `PATH`, fake earlier binaries, `NODE_OPTIONS`, `NODE_PATH`, `GIT_DIR`, `GIT_WORK_TREE`, `GIT_CONFIG_*`, object-directory/alternate-object variables, upper/lowercase npm/proxy config, credential variables, wrong HOME ownership/mode, CR/NUL/multiline version output, and wrong executable/version must fail before install. Allowlist fixtures reject `test:operator`, another script/executable, shell string/`shell:true`, missing/extra/reordered/duplicated argv or invocation, wrong-mode invocation, and any source build in release mode before spawn. Cleanliness fixtures reject dirty index/worktree/nonignored untracked state, lifecycle/check-time mutation, and persistent clean retargets. Source cleanup fixtures preserve the primary error, require exact `T` absence, and protect a non-owned sentinel; release mode creates/removes no `T`. Existing checksum/manifest/carrier/source/version fixtures remain.

## After Completion (Strategist Notes)

- Update no wiki page: repository source authority is internal shipped topology, not a new user-facing capability.
- Mark the item-135 follow-up's repository/default-path portion complete only for source publication; installation, vocabulary closure, auth, rehearsal, and live authority remain with items 137, 138, and the two authorized successors that will replace frozen item 139 after the 138 exit gate.
- Feed the exact AC6 tuple into item 137 without rebuilding or substituting a mutable branch.
- Require item 137's own reviewed workflow/governance scope before its successor prerelease; do not reuse item 136's empty-namespace-only local publication controller against populated state.
- Keep Project_echo as the context runtime/state authority and pinned migration source while item 138 lands the rehearsed substrate; only the authorized live-cutover-and-reversibility successor created after the 138 exit gate may complete the founder cutover.
