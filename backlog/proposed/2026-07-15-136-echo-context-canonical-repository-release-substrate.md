---
id: 2026-07-15-136-echo-context-canonical-repository-release-substrate
title: "Canonical echo-context repository, self-contained CI, and source-artifact authority"
status: proposed
priority: HIGH
estimate: 3d
created: 2026-07-15
blocked_by:
  - 2026-07-13-135-local-echo-context-source-extraction
task_state_ref: 2026-07-15-136-echo-context-canonical-repository-release-substrate
requested_reviewers: ["codex", "codex-ops"]
files_to_modify:
  - /Users/zhenye/Desktop/echo-context/.github/workflows/** # NEW least-privilege CI, secret scan, and push-triggered read-only source-release build workflow
  - /Users/zhenye/Desktop/echo-context/package.json # prerelease identity and self-contained operator scripts
  - /Users/zhenye/Desktop/echo-context/package-lock.json # exact dependency graph for the source artifact
  - /Users/zhenye/Desktop/echo-context/README.md # replace extraction-era no-remote/awaiting-review status
  - /Users/zhenye/Desktop/echo-context/LICENSE # exact reviewed MIT license for standalone distribution
  - /Users/zhenye/Desktop/echo-context/.gitignore # ignore build/artifact/cache/log/state outputs
  - /Users/zhenye/Desktop/echo-context/AGENTS.md # target-repo internal-asset and review/build-once instructions
  - /Users/zhenye/Desktop/echo-context/tsconfig.json # commit the reviewed standalone compiler configuration
  - /Users/zhenye/Desktop/echo-context/eslint.config.mjs # commit the reviewed standalone lint configuration
  - /Users/zhenye/Desktop/echo-context/vitest.*.config.ts # separate source-independent CI from operator replay
  - /Users/zhenye/Desktop/echo-context/provenance/** # NEW baseline/repository authority plus successor runtime-inventory.v2.json (item-135 v1 bytes immutable)
  - /Users/zhenye/Desktop/echo-context/schemas/** # NEW source-artifact and authority schemas
  - /Users/zhenye/Desktop/echo-context/tools/** # repository checks, deterministic source build, verification, and secret scan
  - /Users/zhenye/Desktop/echo-context/tests/migration/** # bind extraction proof to the frozen baseline instead of evolving HEAD
  - /Users/zhenye/Desktop/echo-context/tests/operator/** # explicit Project_echo raw-object replay, excluded from hosted CI
  - /Users/zhenye/Desktop/echo-context/tests/governance/** # NEW authority, artifact, and workflow-policy tests
  - /Users/zhenye/Desktop/echo-context/tests/governance/fresh-clone-acceptance.test.ts # exact wrapper/verifier/deadline/cleanup production-state-machine fixtures
  - raw/internal/migrations/2026-07-15-136-echo-context-repository-bootstrap.md # NEW redacted authority/release record
  - raw/internal/migrations/2026-07-15-136-echo-context-implementation-review.json # NEW independent-reviewer-owned actor/run and reviewed-head evidence
  - raw/internal/migrations/2026-07-15-136-echo-context-target-main-execution-plan.json # NEW coordinator-owned canonical target-main plan bytes
  - raw/internal/migrations/2026-07-15-136-echo-context-target-main-delegated-authorization.json # NEW coordinator-owned exact target-main authorization
  - raw/internal/migrations/2026-07-15-136-echo-context-source-release-publication-plan.json # NEW coordinator-owned canonical local-publication plan bytes
  - raw/internal/migrations/2026-07-15-136-echo-context-source-release-delegated-authorization.json # NEW coordinator-owned machine-verifiable release authorization
  - schemas/echo-context-operation-rpc.v1.schema.json # NEW Project_echo-owned finite worker-to-host capability protocol
  - tools/echo-context-operation-host.mjs # NEW Project_echo-owned invocation supervisor, credential broker, transport owner, and cleanup owner
  - tools/echo-context-git-askpass-fd.mjs # NEW Project_echo-owned authenticated anonymous-FD Git credential helper used by the operation host
  - tools/echo-context-remove-owned-root.mjs # NEW Project_echo-owned bounded direct cleanup child used by host and coordinator
  - tools/echo-context-reviewed-operation-launcher.mjs # NEW Project_echo-owned authenticated loader for the reviewed operation envelope
  - tools/echo-context-operation-envelope.mjs # NEW Project_echo-owned outer land/collect/publish provenance, workspace, deadline, and cleanup state machine
  - tools/echo-context-project-authority-commit-publisher.mjs # NEW Project_echo-owned CAS publisher for R, P_L, and source-publication authority commits
  - tests/echo-context/operation-envelope.test.ts # NEW executable tests for the reviewed operation host, launcher worker, and envelope
  - tests/echo-context/operation-transports.test.ts # NEW Project-owned production REST/Git/gh/askpass/deadline transport tests
  - tests/echo-context/project-authority-commit-publisher.test.ts # NEW executable CAS and exact-readback tests for Project_echo authority commits
  - raw/internal/agent-runs/** # workflow-owned failure/completion run log
  - backlog/task-state/2026-07-15-136-echo-context-canonical-repository-release-substrate/** # workflow continuity pointers
  - backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # proposal and review revisions
  - backlog/ready/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # watcher-owned promotion target
  - backlog/claimed/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # workflow claim target
  - backlog/pending_review/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # workflow handoff target
  - docs/BACKLOG.md # generated stage-derived index
spec_refs:
  - backlog/complete/2026-07-13-135-local-echo-context-source-extraction.md # exact reviewed extraction and later-authority boundary
  - raw/internal/migrations/2026-07-13-135-echo-context.md # accepted source/parity evidence and target identity
  - raw/internal/migrations/2026-07-13-135-echo-context-review.md # independent acceptance record
  - raw/internal/decisions/2026-07-12-g2-terminal-dispositions-and-repository-topology.md # two-organization-repository topology + controls-preservation rule; predates this split and is not its authority
  - raw/internal/decisions/2026-07-15-echo-context-successor-repository-execution.md # founder-authorized cross-repo execution/review protocol
  - raw/internal/decisions/2026-07-16-echo-context-sequential-program-delegated-authority.md # scoped coordinator authority and immutable-operation record contract
  - raw/internal/migrations/2026-07-15-136-echo-context-canonical-repository-release-substrate-repository-create-6a2e8326-delegated-approval.md # immutable once-only repository creation authorization/evidence
  - raw/internal/migrations/2026-07-15-136-echo-context-canonical-repository-release-substrate-baseline-main-push-472e7bf5-delegated-approval.md # immutable once-only baseline push authorization/evidence
  - backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.review.md # cycle-one implementation rejection and accepted fix contract
  - raw/internal/decisions/2026-07-11-commercial-focus-team-product-carve.md # context remains an internal asset
  - /Users/zhenye/Desktop/echo-context/provenance/target-only-policy.v1.json # extraction-era exact-HEAD/finality constraint
  - /Users/zhenye/Desktop/echo-context/provenance/runtime-inventory.v1.json # frozen item-135 executable/dependency closure; superseded for current validation by runtime-inventory.v2.json
  - /Users/zhenye/Desktop/echo-context/tests/migration/object-closure.test.ts # one-branch/no-remote extraction invariant to historicize
  - /Users/zhenye/Desktop/echo-context/tests/migration/parity-matrix.test.ts # source-dependent proof that hosted CI cannot assume
  - /Users/zhenye/Desktop/echo-context/tests/migration/context-tool-evidence.test.ts # source replay seam and sealed eight-tool evidence
claimed_by: ""
claimed_at: ""
branch: ""
worktree: ""
head_sha: ""
pr_url: ""
agent_notes: ""
review_notes: |
  Implementation cycle 1 rejected — redo before merge.
  Reviewed Project_echo head: 25b833332bb22ec79700fcdf31b9c9f20eea79f5.
  Reviewed echo-context head: 145868a67a85dbb651faed457ee4001370c0fad0.
  Immutable review evidence: Project_echo commit 77c5e0c3f7f64c0bda1d99439d8d35eca54551ee,
  review-artifact blob 1cd16c1f31dab5c40ad76b70716846a09c98d845.
  AC4/AC6 require fresh specification convergence; neither head is authorized
  for target-main merge or release.
target_repo: ""
target_remote: ""
target_branch: ""
target_worktree: ""
target_head_sha: ""
target_pr_url: ""
target_landed_sha: ""
project_landed_sha: ""
---

# Canonical echo-context repository, self-contained CI, and source-artifact authority

## Why this spec exists

Item 135 proved that the local echo-context extraction is source-independent and behaviorally faithful, but it deliberately ended with one migration branch, no remote, no install, and no runtime authority. Its extraction-finality tests also bind several checks to the then-current HEAD and two operator proofs default to Project_echo Git objects, so blindly publishing that directory would produce a repository that cannot safely evolve or validate itself in hosted CI.

This first gate makes echo-context/main the canonical source authority without confusing source publication with installation or live-state authority. It preserves the exact reviewed extraction and completed private-repository bootstrap as immutable history, gives clean clones a self-contained quality gate, and seals one explicitly non-installable source artifact for item 137. Project_echo remains the installed runtime and state authority throughout this item. The third-repository split executed here is authorized by the 2026-07-15 successor-repository execution decision and item 135's founder-reconciled merge; the G2 topology record defined two organization repositories and kept the context layer inside echo-dev-platform until split triggers fired, so it is cited for its controls-preservation rule, not as the split authority.

This internal-asset work is permitted by the scope guard because unified machine context across Codex, Claude Code, and Cursor is required by the current ECHO development workflow and Team-product delivery. It is not a parallel commercial roadmap or a second product.

## Acceptance Criteria

### AC1 — Bind the completed exact baseline publication as immutable history

This bootstrap is already complete and is an immutable prerequisite, not a retryable builder step. The single repository-creation operation was authorized at Project_echo commit `e1ec8f74ae812e5df0983cd11a9a0827a9aefb67`; the single baseline-main publication was authorized at Project_echo commit `ece86049e8bfca567f03e7cdfbb8ee1d0b45fd05`. Authenticated readback established private repository `zhenye0616/echo-context`, numeric repository ID `1302541575`, node ID `R_kgDOTaM1Bw`, default branch `main`, and exact baseline commit `0cf7b006eba665c0bf55e82ff04da70f19f01ebb` / tree `70c5cf8352652b3c4c1dce68cd1a5e40d44e4b05`. The creation response was exactly one HTTP 201, the baseline push was exactly one create-only empty-expect leased porcelain `* [new branch]` result, and authenticated readback matched the captured repository identities and exact main SHA. No later implementation, reframe, or recovery path may repeat, adopt, delete, rewrite, or reinterpret either operation.

The builder implements `tools/repository-bootstrap-gate.mjs` as an injected, executable repository-bootstrap gate model that verifies the frozen evidence contract without touching GitHub: expected founder identity; negative name lookup treated as non-authoritative; exactly one create call whose only success is a direct HTTP 201 carrying the stable repository ID/node ID; authenticated owner/name/private readbacks; fully paginated empty branch results plus exhaustive zero-ref `git ls-remote`; the exact empty-expect `git push --porcelain --force-with-lease=refs/heads/main:` command; `* [new branch]` created-by-this-run proof; and exact post-push readback. Redirect, non-201, lost response, wrong identity/visibility, ID drift, incomplete pagination, any foreign ref, pre-existing main even at the identical OID, missing/malformed porcelain, ambiguous push, or failed post-readback each fail with no retry, adoption, cleanup, or later write. Tests consume recorded fixtures only; they never execute repository creation or baseline publication again.

The completed bootstrap full-ref secret scan remains bound to the committed `tools/secret-scan-contract.json` by SHA-256 `b186d99d61f774a6fbf6f16849c7aeb21618d90f79d3f7da4398d88d95925453`. The committed scanner must enforce the same exact gitleaks version/per-platform digest/invocation contract, remain non-shallow and complete-ref, redact secret values, and fail on leaks or scanner infrastructure errors. Personal-account hosting is the accepted current owner topology; the revised AC4 states its exact enforcement limit rather than pretending the unavailable private-repository controls exist.

### AC2 — Convert extraction-finality into explicit successor provenance

Add provenance/extraction-baseline.v1.json, provenance/repository-authority.v1.json, their schemas, and tools/check-repository-authority.mjs. The baseline binds the accepted commit/tree, the 190-path mode/blob/content inventory, item-135 evidence hashes, and independent-review record. The authority record binds canonical repository/default branch, source_authority echo-context/main, artifact_authority versioned-source-artifact, extraction baseline ancestry, runtime_authority false, state_authority false, installed false, and maturity DEV.

Refactor tests/migration/object-closure.test.ts, tests/migration/committed-source-only.test.ts, and every target-only/extraction check that currently treats evolving HEAD as the final 190-file acceptance object. Those tests must inspect the frozen baseline through Git objects while successor tests require that baseline to be an ancestor of every releasable source SHA. Existing item-135 provenance bytes remain immutable historical proof; successor manifests reference rather than rewrite them. Because AC3 adds successor executables while provenance/runtime-inventory.v1.json is part of that frozen evidence, the regenerated runtime inventory is a successor file: provenance/runtime-inventory.v2.json, produced by a committed generator in tools/** over the successor tree's executable/dependency closure. runtime-inventory.v1.json remains byte-identical; `verify:inventory`, its generator, and every test that validates the current inventory bind to v2, and a governance test asserts both v1 byte identity (against its recorded hash) and v2 validity for the successor tree.

README.md must state the exact split: echo-context/main owns source after this item; Project_echo still owns the active daemon, live state, client endpoint, and rollback. It must not say the repository is installed, authoritative at runtime, qualified, or a commercial product.

Add LICENSE as a byte-identical reviewed copy of Project_echo's MIT license; .gitignore covering at least node_modules, dist/build, artifacts, caches, temporary files, logs, and mutable state; and a target AGENTS.md that locks the internal-asset boundary, main-branch review rule, exact-artifact release rule, no-live-state default, and Project_echo coordination-record requirement. These are successor files covered by provenance and the source artifact, not retroactive changes to the frozen extraction baseline.

### AC3 — Make a sibling-free clean clone fully self-testing

Commit the exact standalone `tsconfig.json` and `eslint.config.mjs` configurations used in the accepted item-135 private-clone review. Add explicit `vitest.ci.config.ts` and `vitest.operator.config.ts`. `package.json` defines the exact scripts the gate, CI, and fresh-clone run share: `typecheck`, `lint`, `test:ci` (`vitest run --config vitest.ci.config.ts`), `test:operator` (`vitest run --config vitest.operator.config.ts`), `verify:inventory`, `verify:authority`, `verify:artifact` (`node tools/verify-source-artifact.mjs`), `build:artifact` (`node tools/build-source-artifact.mjs`), and `scan:secrets` (`tools/secret-scan.sh`). `build:artifact` accepts exactly `--source-sha <sha>` and `--out <dir>`, derives the version from the `package.json` version at that committed source SHA, writes the three AC5 files into the given gitignored output directory, and emits the built manifest's canonical-JSON SHA-256 as its exact final stdout line `manifest_hash=<hex>`; the workflow maps that value to its `manifest_hash` job output. `verify:artifact` accepts exactly `--archive <path>`, `--checksum <path>`, `--manifest <path>`, and `--expected-manifest-hash <hex>`, recomputes the manifest's canonical-JSON SHA-256, and fails unless it equals the supplied expected value.

The scripted acceptance entrypoint is `tools/fresh-clone-acceptance.sh`, a committed shell bootstrap for the single Node 22 verifier/orchestrator `tools/fresh-clone-verifier.mjs`. It must be invoked by its canonical absolute, regular, nonsymlink path `<CLONE>/tools/fresh-clone-acceptance.sh` while physical `PWD` is exact canonical `<CLONE>`. Before starting Node, shell builtins only (`case`, parameter expansion, `test`, `cd -P`, `pwd -P`) require absolute nonsymlink `$0`, derive and physically canonicalize its exact `tools` directory, require that directory's parent equal physical `PWD`, and select only the regular nonsymlink sibling `<CLONE>/tools/fresh-clone-verifier.mjs`. Relative invocation, a symlink component, wrong cwd, missing/substituted sibling, or a decoy under caller cwd fails before any external process.

Every invocation then begins with the exact common prefix `--node-bin <NODE> --npm-bin <NPM> --git-bin <GIT> --git-version <GIT_VERSION> --sandbox-home <HOME>`, followed by the selected mode's exact arguments below. `NODE`, `NPM`, `GIT`, and `HOME` must be canonical absolute paths supplied by the reviewed CI/coordinator plan; `HOME` and `HOME/tmp` must already be caller-owned mode-0700 directories. The wrapper parses only that fixed prefix with shell builtins, rejects missing/duplicate/relative/newline-bearing values, and its only external process is `exec /usr/bin/env -i` of exact `NODE` and the authenticated absolute verifier sibling with the common prefix and remaining arguments unchanged. It never searches inherited `PATH` or runs a caller-selected shell command.

Before Node starts, the wrapper constructs the complete environment allowlist: exact `HOME`, `TMPDIR=HOME/tmp`, `PATH=<real NODE directory>:<real NPM directory>:<real GIT directory>:/usr/bin:/bin`, `LANG=C`, `LC_ALL=C`, `TZ=UTC`, `CI=1`, `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_TERMINAL_PROMPT=0`, `NPM_CONFIG_USERCONFIG=/dev/null`, and `NPM_CONFIG_CACHE=HOME/.npm-cache`; no other inherited variable crosses. The verifier rebuilds that same allowlist for every child, with only step-specific nonsecret additions. Thus `NODE_OPTIONS`, `NODE_PATH`, every unapproved `GIT_*` redirect such as `GIT_DIR`, `GIT_WORK_TREE`, `GIT_OBJECT_DIRECTORY`, `GIT_ALTERNATE_OBJECT_DIRECTORIES`, and `GIT_CONFIG_*`, every inherited `npm_config_*`/`NPM_CONFIG_*`, proxy variables, credential variables, and all `ECHO_*` state are absent.

The verifier module and its complete static-import closure must be side-effect-free during module evaluation. The aggregate clock starts at verifier entry, and the first action inside exported `runFreshClone` requires `realpath(process.execPath) == realpath(NODE)`, `process.version == v22.22.1`, canonical executable regular-file `NPM` and `GIT`, `realpath(fileURLToPath(import.meta.url))` equal the exact absolute verifier sibling supplied by the wrapper, and physical `cwd` equal that verifier's two-level parent `<CLONE>` before any filesystem mutation, network access, or child spawn. Static analysis and import-time sentinel fixtures enforce the side-effect-free closure. The verifier then runs the common two-child prelude exactly once under the controlled environment: absolute `NPM` with `["--version"]` must return exactly `10.9.4`, then absolute `GIT` with `["--version"]` must return exactly `git version <GIT_VERSION>`; CR, NUL, extra lines/bytes, another version, or another executable fails before install. The verifier alone parses mode arguments, validates identity, records its source-mode temporary directory, and spawns children from the clone root. Every child spawn is the recorded absolute executable plus argv array with `shell:false` and its own process group; a shell string, `shell:true`, unlisted executable/script, or any missing, extra, reordered, or duplicated argv element/invocation fails before spawn. There is no generic `npm run <script>` allowlist entry.

After validating the common prefix and selected mode's arguments, the verifier stores the caller's full 40-hex `--source-sha` as immutable expected SHA `S` for the whole run. Each cleanliness boundary is the same two-child pair, in this order: absolute `GIT` with argv `["status","--porcelain=v1","--untracked-files=all"]` must exit zero with empty stdout, immediately followed by absolute `GIT` with argv `["rev-parse","HEAD"]`, which must exit zero and whose normalized full-SHA stdout must equal `S`. Putting the HEAD assertion last closes a persistent clean retarget between the two boundary commands; a retarget after a nonfinal pair is caught at the next pair, and the final pair ends with the HEAD assertion. The pair runs exactly three times in either mode: before `npm ci`, immediately after `npm ci`, and at the end after every mode check and any source-owned cleanup/absence assertion. Thus status count is three and `rev-parse` count is three; neither is once-only.

After the exact common prefix, source mode accepts exactly `--mode=source --source-sha <S>`. Its sole Node-internal setup step occurs after child step 10 and before child step 11: create the single gitignored temporary directory inside the clone and record its exact canonical path as `T`. Let `A`, `C`, and `M` be the archive, checksum, and manifest paths deterministically derived beneath `T`; and let `H` be the validated `manifest_hash=<hex>` carrier parsed from step 11. Its complete mode trace, following the common prelude, is:

| Ordinal | Executable | Exact argv |
|---|---|---|
| 1 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 2 | `<GIT>` | `["rev-parse","HEAD"]` |
| 3 | `<NPM>` | `["ci"]` |
| 4 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 5 | `<GIT>` | `["rev-parse","HEAD"]` |
| 6 | `<NPM>` | `["run","typecheck"]` |
| 7 | `<NPM>` | `["run","lint"]` |
| 8 | `<NPM>` | `["run","test:ci"]` |
| 9 | `<NPM>` | `["run","verify:inventory"]` |
| 10 | `<NPM>` | `["run","verify:authority"]` |
| 11 | `<NPM>` | `["run","build:artifact","--","--source-sha",S,"--out",T]` |
| 12 | `<NPM>` | `["run","verify:artifact","--","--archive",A,"--checksum",C,"--manifest",M,"--expected-manifest-hash",H]` |
| 13 | `<GIT>` | `["fsck","--full"]` |
| 14 | `<NPM>` | `["run","scan:secrets"]` |
| 15 | `<NODE>` | `["<CLONE>/tools/fresh-clone-cleanup.mjs","--owned-root",T]` |
| 16 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 17 | `<GIT>` | `["rev-parse","HEAD"]` |

On the success path, child step 15 is the verifier-owned cleanup transition. Creation of `T` also creates one single-assignment cleanup state `not_started`; the sole transition is `not_started -> running -> completed|failed`, and no state may return to `not_started`. The surrounding `finally` invokes that transition if and only if `T` exists and state remains `not_started`; it never starts a second helper after a running, completed, or failed attempt. The transition first terminates/reaps any active child group and closes its streams, authenticates the canonical absolute regular nonsymlink side-effect-free sibling `tools/fresh-clone-cleanup.mjs`, marks `running`, and starts exact `<NODE>` with argv `["<CLONE>/tools/fresh-clone-cleanup.mjs","--owned-root",T]` as a direct process-group leader. The helper accepts only the verifier-recorded canonical nonsymlink directory `T` beneath the clone's single gitignored acceptance-temp parent and recursively removes only that root. The verifier marks `completed` only after direct-child reap, stream closure, PGID absence, and parent-side `lstat(T)==ENOENT`; timeout/error, surviving PGID, any remaining entry at `T`, or any other `lstat` result marks `failed` and is terminal without retry. A prior primary error remains primary with sanitized cleanup failure attached. Only `completed` may proceed to child steps 16–17; their failure does not respawn cleanup because the root is already absent. The verifier never derives a cleanup target from mutable child output and never removes a user/repository path or any path other than recorded `T`.

After the exact common prefix, release mode accepts exactly `--mode=release --source-sha <S> --version <version> --archive <RA> --checksum <RC> --manifest <RM> --expected-manifest-hash <RH>`, where the paths and hash are caller supplied from the delegated exact authorization tuple. It creates no verifier-owned temporary directory, never invokes `build:artifact`, and has this complete mode trace following the common prelude:

| Ordinal | Executable | Exact argv |
|---|---|---|
| 1 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 2 | `<GIT>` | `["rev-parse","HEAD"]` |
| 3 | `<NPM>` | `["ci"]` |
| 4 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 5 | `<GIT>` | `["rev-parse","HEAD"]` |
| 6 | `<NPM>` | `["run","typecheck"]` |
| 7 | `<NPM>` | `["run","lint"]` |
| 8 | `<NPM>` | `["run","test:ci"]` |
| 9 | `<NPM>` | `["run","verify:inventory"]` |
| 10 | `<NPM>` | `["run","verify:authority"]` |
| 11 | `<NPM>` | `["run","verify:artifact","--","--archive",RA,"--checksum",RC,"--manifest",RM,"--expected-manifest-hash",RH]` |
| 12 | `<GIT>` | `["fsck","--full"]` |
| 13 | `<NPM>` | `["run","scan:secrets"]` |
| 14 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 15 | `<GIT>` | `["rev-parse","HEAD"]` |

Each common-prelude row and every table row other than the repeated boundary commands runs exactly once. Source build/verify counts are 1/1; release build/verify counts are 0/1. `test:operator` remains independently runnable but is explicitly rejected in both verifier modes. Gitignored `node_modules` and source `T` do not themselves dirty the status probe, so source cleanup is proven by the separate exact-path `ENOENT` assertion. Any staged, unstaged, or nonignored-untracked entry, persistent clean retarget to a different HEAD between boundaries, install/check-time mutation, wrong executable/version/environment/count/order/vector, or failed check stops acceptance. These boundary probes target persistent accidental mutation; the spec makes no claim against adversarial transient retarget-and-restore or mutate-and-restore between probes.

Release mode's `S` and version are transcribed from the delegated exact authorization tuple and are never ignorable: after `verify:artifact` authenticates the manifest, the verifier fails unless `S` equals the manifest source commit and all three HEAD boundary reads, and version equals both the authenticated manifest version and the committed `package.json` version. Source mode consumes `H` only from the exact build carrier; release mode consumes `RH` only from the approved durable record, never by recomputing an approval value from the downloaded manifest. Unknown, missing, duplicate, or wrong-mode arguments fail with usage text rather than skipping a check.

The AC3 deadline constants are exact and not caller-selectable: 30 seconds for each version/status/rev-parse child; 600 seconds for `npm ci`; 900 seconds each for `test:ci` and `scan:secrets`; 300 seconds for each remaining typecheck/lint/inventory/authority/build/artifact-verification/fsck child; 30 seconds for the cleanup child plus parent absence readback; and 3700 seconds total from entry into `runFreshClone` before identity checks through final HEAD read or failed cleanup. The verifier fixes `aggregate_end=entry+3700s` and `prefinal_cutoff=aggregate_end-120s`. Every source step through 14, every release step through 13, and every earlier failure/cancellation path must finish child execution by `prefinal_cutoff-10s` and terminal settlement by `prefinal_cutoff`; each effective execution deadline is the earlier of its class limit and that boundary. The final 120 seconds are reserved and nonborrowable: source cleanup step 15, status step 16, and HEAD step 17 each receive a consecutive maximum 40-second envelope (30-second execution/readback plus ten-second termination/reap/stream settlement); release status step 14 and HEAD step 15 receive the first two such envelopes and leave the final 40 seconds as guard. On a prefinal source failure after `T` creation, the same first reserved envelope runs the sole cleanup transition and the remaining reserve is guard. Timeout or cancellation closes stdin, sends TERM to the child PGID, waits exactly five seconds, sends KILL if the PGID remains, then allows at most five seconds for direct-child exit plus stdout/stderr closure and verifies no process remains addressable in that PGID. No next step starts before terminal settlement. A never-settling lightweight Git child, cleanup child, stream, or filesystem result is fatal within the same complete wall bound and cannot be reported as success.

`tools/fresh-clone-verifier.mjs` exports one production `runFreshClone(argv, deps)` state machine and keeps the shell as the fixed scrub-and-exec bootstrap above; `tools/fresh-clone-cleanup.mjs` is its sole production cleanup child. Spawn, clock, cancellation, environment, executable-realpath, filesystem, temporary-directory, and cleanup-child operations are injected so executable tests exercise the real transition logic; production adapters remain the only operational path. The expected child plan in tests is a literal independent oracle derived from the common prelude and AC3 tables, never the production trace passed back as its own expected value. Cleanup errors, surviving cleanup child/PGID/owned temporary paths, carrier tampering, poisoned inherited state, and non-owned-sentinel mutation fail without being masked by the `finally` path.

Move only the raw Project_echo Git-object recomputation portions of `tests/migration/parity-matrix.test.ts` and `tests/migration/context-tool-evidence.test.ts` into `tests/operator/source-parity.test.ts`; the operator suite requires an explicit `ECHO_SOURCE_GIT_DIR` and pinned source SHA and never supplies a founder-path default.

A fresh no-local clone in a temporary HOME, with Project_echo and sibling repositories absent and all ECHO_* variables cleared, must pass npm ci from the exact lock, typecheck, lint, the complete source-independent CI suite, runtime-inventory validation (against provenance/runtime-inventory.v2.json), repository-authority validation, context-tool roster/parity fixtures, and service parity. There may be no unexpected skip caused by missing Project_echo. Hosted CI must never read /Users/zhenye/Desktop, ~/.echo, ~/.echo-context, credentials, or a live database.

The operator replay remains independently runnable against the pinned item-135 source objects and must still reproduce the accepted source inventory and parity aggregate. It is a release-review check, not a hosted-CI dependency.

### AC4 — Add least-privilege CI and an exact-SHA evidence-carrying landing gate

Add `.github/workflows/ci.yml` (job IDs `quality-macos`, `quality-ubuntu`), `.github/workflows/secret-scan.yml` (job ID `secret-scan`), and `.github/workflows/source-release-build.yml` (job ID `build-artifact`) plus `tools/secret-scan.sh`, injected `tools/target-main-landing-gate.mjs`, and workflow-policy tests. CI and secret scan run on every pull request targeting `main` and every push to `main`, with no omitting path filter or trigger/job condition; the read-only source build runs only on a push to `main` and has no manual, API-initiated, callable, scheduled, or other trigger. All third-party actions are immutable-commit pinned; every checkout sets `persist-credentials:false` and `fetch-depth:0`; every workflow and job has no permission beyond `contents: read`; and no Actions workflow, reusable workflow, or script other than the single local production `tools/release-publication-controller.mjs` may create a tag, release, or release asset. Every GitHub REST request in AC4/AC6 and both canonical plans pins `Accept: application/vnd.github+json` plus `X-GitHub-Api-Version: 2026-03-10`; upload requests additionally pin their exact content type, binary downloads pin their exact Accept value, and a missing/different version or media-type header fails before use. Node 22.22.1 and npm 10.9.4 are pinned on macOS and Ubuntu. The hosted secret scan enforces complete non-shallow refs and the AC1 contract file; actual top-level leak, scanner-failure, pipeline-masking, unsupported/digest-mismatched binary, incomplete-ref, and fake-secret non-disclosure fixtures must prove nonzero failure without printing secret values.

Each quality job has `timeout-minutes:65` and uses the AC3 source-mode entrypoint as its sole substantive verification trace. Let `S_JOB` be exactly `github.event.pull_request.head.sha` for a pull-request event and `github.sha` for a main push. After checkout at exact `S_JOB`, the job proves full history contains the extraction baseline, resolves/canonicalizes the pinned absolute Node/npm/Git paths and exact Git version, creates one nonsymlink runner-owned mode-0700 sandbox `HOME_JOB` plus mode-0700 `HOME_JOB/tmp`, and invokes exactly once:

`<canonical-github-workspace>/tools/fresh-clone-acceptance.sh --node-bin <NODE> --npm-bin <NPM> --git-bin <GIT> --git-version <GIT_VERSION> --sandbox-home <HOME_JOB> --mode=source --source-sha <S_JOB>`

The workflow has no parallel hand-written npm/check trace that could drift from AC3. An `if:always()` cleanup step removes only recorded `HOME_JOB`, requires absence, and fails the job on cleanup failure. The carrier is generated only after the entrypoint succeeds and records the exact vector/tool identities. Structural tests reject shallow checkout, mutable/event-wrong SHA, relative wrapper/tool path, missing/wrong-mode/reordered argv, non-0700 or symlink HOME/tmp, insufficient job timeout, missing cleanup, or an alternate substantive trace.

The current personal/private Free repository cannot provide server-enforced branch protection, administrator enforcement, required PR approval, or a private protected-environment reviewer, and the sole GitHub identity cannot independently approve its own PR. This specification deletes those impossible claims. It does not claim to prevent a malicious or compromised sole repository owner from bypassing, force-updating, deleting, or publishing manually. The enforceable threat boundary is instead exact Git object identity, immutable Project_echo authorization evidence, authenticated destination readback, create/update compare-and-swap, and fail-closed refusal on any drift or ambiguity. Private visibility remains mandatory; making the repository public or silently pretending unavailable controls exist is forbidden.

For pull-request events, `H` is exactly `github.event.pull_request.head.sha`, never `github.sha` or `refs/pull/*/merge`. Each of the three required jobs checks out that immutable `H`, asserts both `git rev-parse HEAD == H` and `HEAD^{tree}` equals authenticated feature-ref readback, and rejects a merge ref, synthetic merge commit, mutable branch substitution, or mismatched tree. After its substantive checks, each job writes one canonical JSON carrier under `schemas/hosted-check-evidence.v1.schema.json` and uploads it under the exact name `echo-context-<job-id>-evidence-<github.run_id>-<github.run_attempt>`. The carrier binds repository ID/node ID, constant workflow path, run ID/attempt, event, PR head ref/SHA, checked-out SHA/tree, logical job ID, toolchain, and completed command list; it does not claim context-unavailable numeric workflow or server-job IDs. Authenticated, fully paginated API readback independently supplies and binds numeric workflow ID/path, carrier artifact ID/name/digest/expiry, server job ID/name/status/conclusion, server job `check_run_url`, linked check-run ID/name/head SHA/status/conclusion/App ID, and run status/conclusion. Every carrier-artifact download uses the exact-ID API plus the AC6 one-302, signed-host-allowlist, credential-stripping, no-second-redirect contract; its raw ZIP SHA-256 must equal the API `sha256:<hex>` digest before safe extraction. A carrier is evidence only when its exact run, server job, and linked check run are successful, the check-run head is `H`, and its downloaded bytes validate and match the API identities. Push-to-main carriers use the pushed commit as both event and checked-out SHA. Missing, duplicate, expired, malformed, stale-SHA, wrong-tree, wrong-workflow, wrong-event, wrong-run/job/check, failed, wrong-app, synthetic-merge, hostile-redirect, or unreadable evidence fails closed.

After a fresh builder completes cycle two, a different fresh agent performs independent implementation review. Let `P_H` be the exact Project_echo implementation head; it contains `tools/echo-context-operation-host.mjs`, `tools/echo-context-git-askpass-fd.mjs`, `tools/echo-context-remove-owned-root.mjs`, `tools/echo-context-reviewed-operation-launcher.mjs`, `tools/echo-context-operation-envelope.mjs`, `tools/echo-context-project-authority-commit-publisher.mjs`, `schemas/echo-context-operation-rpc.v1.schema.json`, and their tests, while `H` is the exact echo-context feature head. The reviewer owns the exact canonical JSON bytes at `raw/internal/migrations/2026-07-15-136-echo-context-implementation-review.json`, validated by `schemas/implementation-review-evidence.v1.schema.json`; the record binds the exact builder actor ID/run ID, reviewer actor ID/run ID, `P_H`, all six Project_echo executable/entrypoint paths/blob OIDs/SHA-256/static-import closures plus the RPC schema identity, `H`/tree, every reviewed hosted carrier identity, finding dispositions, and verdict `merge_ready`. The reviewer commits only that record plus its permitted reviewer run log as earlier additive Project_echo commit `R`. The record does not embed its own containing commit and there is no second fresh-review artifact. The later landing plan/authorization commit binds `P_H`, `R`, and every captured identity. The landing gate normalizes and independently recomputes `builder_actor_id != reviewer_actor_id` and `builder_run_id != reviewer_run_id`; it never trusts a stored boolean. Equality, a missing run identity, a substituted head/tree/host/askpass/cleanup/launcher/envelope/publisher/schema, same-commit or stale/cycle-one review evidence, or any verdict other than `merge_ready` blocks landing.

The unavoidable local trust anchor is the persistent coordinator plus canonical absolute nonsymlink reviewed Node, Git, and GitHub CLI executables and exact versions, not a repository module claiming to authenticate itself before it runs. The coordinator supervisor clock starts before any public read, root creation, or materialization and gives that entire prelaunch phase an exact 120-second limit. It reads the exact operation-host and owned-root-cleanup-helper blobs/import closures at `P_H` from reviewed local Project_echo Git objects, compares them with host-pinned credentialless public readback, and retains the exact cleanup-helper bytes in memory before creating a root. It then creates one mode-0700 coordinator root, owns it immediately, materializes only authenticated host closure bytes, creates an anonymous liveness pipe whose coordinator-held write end is never written and whose host-only read end is nonblocking/read-only, binds its own exact decimal PID as the host's expected parent, and starts the host as its direct child/process-group leader with scrubbed environment, exact Node/argv, and that read end. The coordinator retains the sole write end. Only after successful spawn and an initial `process.ppid == expected_parent_pid` plus liveness-`EAGAIN` check does the host's selected aggregate clock start. The host revalidates the bound executable realpaths, regular-file modes, and version outputs before any credential access. No test or record credits the host with self-authentication. The reviewed host is invocation-scoped, not a daemon or LaunchAgent. It creates/records the operation root, authenticates the Project-owned askpass and owned-root-cleanup helpers plus launcher/envelope/publisher/RPC-schema closures before use, and starts its worker over a separate anonymous inherited sequenced IPC channel.

The coordinator owns only that host child, its streams, the liveness-pipe write end, and the materialization root. Its complete monotonic supervisor bound is `120-second prelaunch + selected host aggregate + 120-second outer reserve`. The liveness read end is explicitly inherited only by the host; launcher, Git, gh, askpass, cleanup, and every other child get it closed before exec. On every coordinator-detected non-normal path after a host exists—including cancellation, protocol/terminal-frame error, stream error, or host aggregate expiry—the outer `finally` first closes the liveness write end and continues draining bounded host streams for at most 55 seconds so the surviving reviewed host can observe EOF, poison, settle its separate child groups/HTTP/pipes, zero, remove its operation root, and exit nonzero. Only if the direct host remains live or its streams remain open after that cooperative window does the coordinator send TERM to the direct host PGID, wait five seconds, send KILL if needed, and allow five final seconds for direct-child reap, stream closure, and host-PGID absence. It never removes materialized bytes while a host is live. On every path after root creation—including public-read, partial-materialization, and host-spawn failure—it then starts exact reviewed Node as a direct process-group leader with argv `["--input-type=module","-","--owned-root",<coordinator-root>]`, streams the retained authenticated `P_H:tools/echo-context-remove-owned-root.mjs` bytes to stdin, closes stdin, and allows 30 seconds for normal completion followed, if needed, by TERM/five-second grace/KILL and five final seconds for direct-child reap/stream closure/PGID-absence proof and parent `lstat(ENOENT)`. The 55-second cooperative window, ten-second escalation, 40-second coordinator-cleanup envelope, and 15-second scheduling guard fit wholly inside the 120-second outer reserve. Normal completion first requires authenticated host result, host exit, stream closure, and then coordinator closure of the write end. A never-settling cleanup child or surviving root is fatal. Abrupt coordinator death cannot guarantee coordinator-root cleanup or logging, but kernel closure of its sole liveness write end fences the surviving host as specified below. Because separately grouped grandchildren cannot be reaped after abrupt host loss, that path makes no operation-root, credential-zeroing, child-absence, or post-mortem-log guarantee; it guarantees only that no surviving coordinator/launcher/controller state-machine capability can issue a successor transition. Any potentially crossed write remains ambiguous destination state. Success is impossible until both host and coordinator roots are absent.

The host's coordinator-facing stdout is exactly one four-byte big-endian length plus strict UTF-8 canonical JSON terminal-result frame of `1..65536` bytes, followed by stdout EOF and no other byte. The schema binds operation ID, `project_authority` class/phase or target mode/phase, terminal status, nonsecret result/evidence identity, cleanup status, and root-absence result. Stderr is sanitized diagnostic UTF-8 capped at 65,536 bytes and may never contain a token, signed URL, credential pipe content, or raw secret-bearing child output. Coordinator success requires one schema-valid matching `status:success` frame, host exit code zero, stdout EOF, bounded sanitized stderr, host stream closure, and subsequent coordinator-root absence. Missing/zero-length/duplicate/truncated/oversized/mismatched/trailing stdout, invalid UTF-8/schema, success-looking output with nonzero exit, zero exit without success, stderr cap/secret failure, or result-then-hang is non-normal and follows host termination before cleanup.

The operation host is the sole OS/resource broker. It continuously monitors the coordinator-liveness read end. At initial startup and synchronously immediately before every resource-creating or mutation-capable transport dispatch—including the final spawn/write boundary for each Git, gh, or HTTP mutation—it requires both `process.ppid == expected_parent_pid` and one nonblocking liveness read returning `EAGAIN`; EOF, any byte, another result, or a changed parent poisons before dispatch. Coordinator death immediately after that last check is the sole permitted race and classifies only that already-dispatched transport as possibly in flight. EOF or parent change before normal host exit atomically poisons the invocation, permits at most that one already in-flight transport, rejects all later RPC, performs the same abort/TERM/KILL/reap/zero/operation-root cleanup as abnormal launcher loss, emits no success, and exits nonzero. Every operation kind—including all Project publisher/reconciler classes and every target envelope mode—runs inside the same launcher topology: the launcher is the host's recorded direct child, inherits the host PGID, is never detached or a process-group leader, and is statically forbidden to spawn; only Git/gh/owned-root-cleanup children use separate PGIDs. The host alone executes GitHub CLI auth, retains and zeroes the invocation's one credential Buffer, opens credential-bearing target HTTP transports, creates anonymous askpass pipes, and spawns every Git/gh/cleanup child as its direct child and a process-group leader. Before acknowledging such a spawn it records the direct-child handle, PID/PGID, streams, operation class, and monotonic deadline. A Git/gh/cleanup RPC does not resolve normally until the direct group leader is reaped, stdout/stderr and every pipe are closed, and the PGID is absent; a lingering helper/descendant is fatal and poisons later requests. The launcher worker, envelope, Project publisher, target landing controller, and target publication controller receive only finite plan-bound RPC capabilities; static AST tests forbid them from importing/invoking `node:child_process`, opening credential-bearing target HTTP, obtaining token/signed-URL bytes, or submitting arbitrary executable/argv/env/cwd/URL/method/header values. RPC carries a host-issued invocation ID and strictly increasing sequence with at most one transport request in flight; malformed, duplicate, reordered, unbound, or post-poison requests fail. The logical controllers alone choose state transitions; the host never becomes a second landing/publication state machine.

The anonymous IPC wire is not trusted before framing and schema validation. Every request and response is exactly one four-byte big-endian unsigned length followed by strict UTF-8 canonical JSON, with length `1..65536`; the receiver reads the prefix into a fixed four-byte buffer, rejects an over-cap length before allocating a payload, holds at most one payload frame, and permits at most 10,000 total frames per invocation. A duplicate-key-rejecting decoder enforces maximum nesting depth 32, maximum string length 8,192 bytes, maximum array length 1,000, and maximum 256 object keys before the RPC schema/method/capability checks. Binary bodies, token bytes, signed URLs, and arbitrary diagnostic text never cross IPC; only schema-bounded nonsecret identities, hashes, finite request descriptors, and results do. Zero-length, malformed UTF-8, duplicate key, oversized/deep/wide value, partial-prefix/payload EOF, trailing bytes, coalesced extra frame while one request is in flight, response without request, or frame-count overflow atomically poisons before dispatch and triggers normal host teardown.

The sole normal worker completion sequence is one schema-authenticated terminal result, host closure of request intake, host-directed launcher shutdown, direct launcher exit/reap plus IPC/stdout/stderr closure, and only then host resource settlement/zeroing/cleanup and result return. Launcher exit or IPC EOF before that terminal result—even exit zero—launcher hang after the result, SIGKILL, protocol violation, worker deadline, or cancellation is abnormal and atomically poisons the invocation. The host rejects every later request, aborts and settles live HTTP, closes request bodies/credential pipes/stdin, sends TERM then KILL to registered separate child PGIDs, reaps their direct leaders, proves PGID absence, and signals the nonspawning launcher by direct PID with the same five-second TERM/KILL/reap/stream sequence. Only then does it overwrite the credential Buffer and run bounded cleanup of recorded roots. It performs no readback or reconciliation after abnormal worker loss; a write that may have crossed is reported `ambiguous_write` for later separate destination readback. Cleanup failure turns success into failure; a prior primary error remains primary with sanitized cleanup error attached; a non-owned sentinel remains byte-identical. The executable guarantee covers launcher/controller loss while the reviewed host survives. Abrupt host/kernel/power loss has no cleanup or post-mortem-log guarantee; an already in-flight mutation may remain ambiguous, but no surviving coordinator/launcher/controller state-machine capability can issue a successor transition.

The host accepts exactly two top-level operation kinds. `project_authority` requires one class `implementation_review`, `target_main_authority`, or `source_publication_authority` plus phase `publish` or `reconcile`. Publish phase authenticates the class-specific publisher bytes/candidate binding and starts only `publishProjectAuthorityCommit` as its worker with the finite Project-only public-read and single leased-push capability set. Reconcile phase is allowed only in a fresh host after an ambiguous prior publish, binds the exact delegation/class/`E`/`C`/candidate paths and hashes, starts `reconcileProjectAuthorityCommit` with credentialless public-read capability only, and has structurally zero credential acquisition, Git spawn, or mutation method. Neither phase imports the target envelope or exposes any echo-context repository/API capability. `target` starts the launcher/envelope; the envelope exposes only modes `land`, `collect`, and `publish`, with exact `land` phase `evidence` or `write` and no phase for the others. For either land phase, the host creates from reviewed local Git objects a fresh config-isolated detached clone at `H`; the worker proves clean status, `HEAD=H`, `HEAD^{tree}=H^{tree}`, `git fsck --full`, and byte identity for `tools/target-main-landing-gate.mjs`, its complete static-import/schema closure, and `provenance/runtime-inventory.v2.json` against Git objects at `H`; and only then imports the target module. That module exports exactly read-only `collectLandingEvidence(input,deps)` and mutating `runLanding(input,deps)`. The evidence phase receives only read-only broker capabilities; the write phase receives its plan-bound write capabilities only after public plan/authorization verification. Target code never clones, authenticates, spawns, holds credentials, or cleans an outer root. A stale/tampered host/askpass/launcher/envelope/publisher/entrypoint/import/schema/inventory, dirty tree, wrong cwd, or wrong executable/version yields zero credential acquisition, API, or write. The plan's `cleanup:false` refers only to remote destination objects and never disables mandatory host-owned local cleanup.

One Project_echo-owned module `tools/echo-context-project-authority-commit-publisher.mjs` exports the mutating `publishProjectAuthorityCommit(input,deps)` and read-only `reconcileProjectAuthorityCommit(input,deps)` state machines, each executed only under its matching `project_authority` host phase. Publish supports exactly three authority classes: `implementation_review` ending at `R`, `target_main_authority` ending at `P_L`, and `source_publication_authority` ending at `P_S`. For each, it binds authenticated expected public main `E`, exact candidate tip `C`, candidate commit range/tree, permitted changed paths and exact path/blob/SHA-256 bytes, canonical URL `https://github.com/zhenye0616/ECHO.git`, absolute Git/version, one-attempt counter, and the common broker/deadline/credential contract. After class/provenance validation, the publish host performs its exact Project-only no-plaintext-config, user-pinned keyring acquisition, host-owned identity/scope gate, owns the Project Git askpass transport, and exposes no target endpoint or target repository capability. The publisher requires a new clean isolated config-free worktree rooted at `E`; `C` must descend directly through only the bound additive range, with no merge and no path/byte outside the class allowlist. `P_L` and `P_S` each have exactly parent `E`; the bootstrap `R` range may also contain the independently reviewed Project implementation commits through `P_H`, but every commit/path/blob in `E..R` is bound by the independent review record.

The sole bootstrap exception to public `P_H` prelaunch authentication is that first `implementation_review` publication: before `P_H` is reachable from public main, the persistent coordinator binds the local host/askpass/owned-root-cleanup/publisher/launcher/envelope/schema bytes and static closures at exact `P_H` to the fresh independent review response, then uses those reviewed local bytes only for the `E..R` CAS publication and bounded cleanup. No echo-context target credential/API/write is permitted in that bootstrap invocation; the Project_echo CAS push uses the reviewed Project-owned askpass helper. Exact public readback of `R`, `P_H`, every reviewed path/blob, and ancestry is mandatory before any later operation may use `P_H`; a failed or ambiguous bootstrap never grants target authority.

The authority publisher never pulls, fetches-and-retries, rebases, autostashes, cherry-picks, merges, or substitutes a base. Its sole write is one broker-owned absolute-Git command with semantic argv `-c http.followRedirects=false push --porcelain --no-verify --no-follow-tags --force-with-lease=refs/heads/main:<E> https://github.com/zhenye0616/ECHO.git <C>:refs/heads/main`, under the same no-hook/no-follow-tags/config/proxy/rewrite/helper isolation as target pushes. Exactly one structurally parsed fast-forward porcelain row succeeds. After a nominal response, credentialless public readback must prove `main=C`, expected parent/range/ancestry, and every allowed exact path/blob/hash before the commit is usable. A lost/ambiguous response poisons and ends the publish host with zero downstream target access. Under the scoped delegation, a separate fresh reconcile host then deterministically dispositions only exact public `main=C` plus exact expected parent/range/ancestry/path/blob/hash as `applied_exact`; authenticated destination readback itself is the durable authority, so no fourth commit or recursively published marker is introduced. That exact reconciliation evidence is later copied into the next canonical plan or final bootstrap record, but mutable logging is not a gate. `main=E`, any other SHA, unreadable state, path/blob drift, stale lease, or malformed/multirow porcelain is terminal for candidate `C`, with zero retry or target access.

In the first fresh `land/evidence` invocation, `collectLandingEvidence` binds actor login `zhenye0616`, numeric user ID `73834646`, node ID `MDQ6VXNlcjczODM0NjQ2`, repository ID `1302541575`, node ID `R_kgDOTaM1Bw`, owner/name/private/default-branch/canonical-URL identity, repository object format exactly `sha1`, `main=B`, feature ref `H`, both commit objects and committer epochs, and the target-main observation interval. It requires both `git merge-base --is-ancestor <frozen-baseline> H` and `git merge-base --is-ancestor B H`; a feature head that does not contain current main must first become a new descendant `H'` through an additive merge/commit and receive fresh hosted evidence plus fresh independent implementation review. Fully paginated authenticated workflow-run, job, and artifact readback selects the exact successful CI run containing `quality-macos` and `quality-ubuntu` plus the exact successful secret-scan run at `H`, all produced by the same discovered GitHub Actions App ID and bound to their downloaded canonical carriers. The function has structurally no target mutation dependency, repeats repository/main/feature/evidence readback immediately before return, and returns only a schema-bounded nonsecret tuple. After worker return, the host closes every target resource, reaps direct group leaders and proves PGIDs absent, zeroes its invocation credential, removes its operation root, and only then returns the tuple to the coordinator; no root, token, adapter, file descriptor, Git object, or mutable path crosses to the write phase.

From that returned tuple, the coordinator deterministically constructs the exact raw merge-commit body: headers are only `tree <H-tree>`, `parent <B>`, `parent <H>`, `author ECHO Merge Coordinator <noreply@echo.local> <merge-epoch> +0000`, and `committer ECHO Merge Coordinator <noreply@echo.local> <merge-epoch> +0000` in that order, followed by one blank LF and exact message `Merge item 136 echo-context canonical source` plus one final LF. `merge-epoch` is the decimal value `max(B-committer-epoch,H-committer-epoch)+1`; no signature, mergetag, encoding, extra header, CR, or mutable clock input is allowed. Under the authenticated `sha1` object format it computes the raw-body SHA-256 and Git commit OID `M` as SHA-1 of `commit <byte-length>\0<body>`. Another object format blocks before plan construction. Because authenticated `B` is an ancestor of reviewed `H`, the authorized tree is exactly `H^{tree}`; missing ancestry or any request for conflict resolution/tree synthesis is new implementation work requiring a reviewed descendant rather than different merge bytes.

Before the target-main write, `raw/internal/migrations/2026-07-15-136-echo-context-target-main-execution-plan.json`, validated by `schemas/target-main-execution-plan.v1.schema.json`, serializes as canonical JSON (UTF-8, lexicographically sorted object keys, no insignificant whitespace, one trailing LF). Its own bytes carry every non-self-referential immutable input: authenticated actor login/user ID/node ID and credential provenance; repository ID/node ID/owner/name/visibility/default branch, object format `sha1`, and literal canonical token-free HTTPS URL; `B`, `H`, `H` tree, both committer epochs, merge-epoch, exact raw merge-body base64/byte-length/SHA-256, `M`, and ordered parents; builder/reviewer actor and run identities; `P_H` plus operation-host, Project-owned askpass helper, owned-root-cleanup helper, launcher, operation-envelope, Project authority publisher, and operation-RPC-schema paths/blob OIDs/SHA-256/static-import closures; review commit `R` plus implementation-review path/blob/SHA-256; every workflow/run/job/check/carrier ID/name/path/head/status/conclusion/digest; Actions App ID; the complete ordered evidence-phase and write-phase requests; the finite framed-RPC method/capability matrix, invocation ID/sequence rules, host-owned operation-root/credential/HTTP/askpass/direct-child/PGID inventory, and worker prohibition on credential-bearing transport or process creation; canonical absolute nonsymlink Node/Git/GitHub-CLI paths and exact versions, clean-clone/cwd/tree/controller/import/schema/inventory assertions; exhaustive allowed Git config and scrubbed environment; sole direct-URL push argv; expected porcelain grammar; the complete generated deadline inventory, aggregate limits, coordinator/host cleanup, termination, and poison policy; ordered post-write readbacks; backup/rollback identities; one-attempt counters; and literal `retry:false`, `adopt:false`, `cleanup:false`, and `rewrite:false`. Its SHA-256 is recomputed from those exact path bytes. The immutable single-use Project_echo record `raw/internal/migrations/2026-07-15-136-echo-context-target-main-delegated-authorization.json`, validated by `schemas/project-echo-delegated-operation-authorization.v1.schema.json`, independently duplicates and binds those identities plus the exact plan path/schema/hash, intended operation `target_main_fast_forward`, backup identity `refs/heads/main=B`, and rollback identity `no_history_rewrite_new_reviewed_descendant_only`; neither file embeds its own containing commit or hash.

The independent reviewer first supplies the candidate additive range ending at `R`; the operation host publishes it only through `publishProjectAuthorityCommit` class `implementation_review`. After exact public readback of `R`, the coordinator supplies a separate additive candidate ending at `P_L` containing only the target-main plan and authorization; the same state machine publishes it as class `target_main_authority`. Host-pinned credentialless Project_echo repository/main readback must prove `R` and `P_L` reachable from main, `R` an ancestor of `P_L`, the exact review path at `R`, and both plan/authorization paths at `P_L`, each with bound Git blob OID, byte SHA-256, schema-valid canonical bytes, cross-file identities, exact CAS-publisher input/output/porcelain proof, and no unbound path in either candidate range. Same-commit review/plan bytes or a missing, mutable-only, mismatched, credential-bearing, unpushed, non-CAS, or ambiguously published record gives zero target write.

The coordinator then starts a separate fresh authenticated `P_H` operation host in `land/write` mode. After the host-backed new-clone/controller provenance gate and credentialless public verification of `R`, `P_L`, review, plan, and authorization, `runLanding` receives only that invocation's finite plan-bound target RPC. It repeats actor/repository/main/feature/ancestry/workflow/job/check/carrier evidence and requires byte identity with the evidence tuple; any `main != B`, ref/evidence drift, or stale identity blocks. From plan bytes alone it decodes the raw merge body, validates every fixed header/epoch/parent/tree/message rule and body SHA-256, requests absolute Git `hash-object -t commit -w --stdin`, requires the returned OID equal `M`, and re-reads `git cat-file commit M` byte-for-byte. Thus the authorized merge object is deterministically rematerialized in the second clone; no first-invocation object, credential, descriptor, or mutable file crosses phases.

The only permitted target-main write is one broker-owned absolute-Git invocation whose semantic argv is `-c http.followRedirects=false push --porcelain --no-verify --no-follow-tags --force-with-lease=refs/heads/main:<B> https://github.com/zhenye0616/echo-context.git <M>:refs/heads/main`; exactly one structurally parsed fast-forward row for the requested destination ref succeeds. It runs from the fresh write-phase clone with system/global config disabled, exhaustive local config restricted to the reviewed clone entries, `core.hooksPath=/dev/null`, `push.followTags=false`, `push.pushOption` empty, `protocol.allow=never` plus `protocol.https.allow=always`, proxy variables/config removed, URL rewrites/remote helpers rejected, and the anonymous credential pipe as the only authentication seam. No remote alias is resolved. Any hook execution, redirect, another ref, missing/additional/malformed row, drift, failed/lost/ambiguous response, or failed readback permits read-only reconciliation only—no retry, adoption, remote cleanup, rewrite, or substitution. Authenticated readback must prove repository identity and `main=M`. Hosted CI, secret scan, and the read-only source build must then pass at exact `M` with new exact run/job/carrier identities; a failure is repaired by a new descendant feature branch, fresh independent review, and new additive records, never by rewriting `main`. The migration record pins both authorizations, exact evidence/write readbacks, raw merge-body identities, merge SHA/tree/parents, builder/reviewer identities, all pre/post hosted evidence identities, Node/Git/GitHub-CLI identities, host/askpass/cleanup/launcher/envelope/publisher/framed-RPC evidence, coordinator/host teardown evidence, and exact sanitized porcelain proof.

CI and workflows may not checkout or fetch Project_echo, use a founder-local absolute path, consume live user state, publish to a public registry, or create a tag/release/asset. Project_echo authorization is consumed only by local coordinator/controllers through the credential-isolated public adapter defined in AC6; every authenticated local AC4/AC6 private-target read/push/API call uses the same reviewed private-target adapter and anonymous-pipe credential transport defined there. Mutable Project_echo branch bytes are never approval authority.

Every operation appears exactly once in a generated/tested deadline inventory shared by the host and canonical plans; an unmapped or caller-overridden operation fails governance. Exact class limits are: 30 seconds for GitHub CLI auth, each JSON request including its full body, filesystem metadata/small-object hashing, config inspection, and every lightweight local Git child (`status`, `rev-parse`, `merge-base`, `hash-object`, `cat-file`, `show`, `ls-tree`, `ls-remote`); 120 seconds for each Git push or release mutation including its response body; and 300 seconds for clone/fetch/fsck, workflow-artifact or release-asset binary transfer, archive/binary hashing, verification, or extraction. A binary response is also capped at its authenticated expected/API size. Aggregate invocation limits are exact: 300 seconds for each Project authority publish or reconcile, 900 for `land/evidence`, 900 for `land/write`, 2400 for `collect` including the 1800-second workflow wait, and 1800 for `publish`. Each aggregate starts at host entry before root creation/provenance/authentication and ends only after logical completion or failure, transport and child settlement, credential zeroing, root removal/absence, terminal-frame drain/EOF, normal launcher reap, and host exit. Its final 50 seconds are reserved: no new logical request may start after `aggregate_end-50s`; up to 45 seconds is reserved for host teardown/root cleanup and the final five seconds only for canonical terminal-frame write/drain, stdout EOF, stream closure, and normal host exit. Every request's effective deadline is the earlier of its class deadline and the logical-work boundary.

Expiry aborts the HTTP request and allows five seconds for stream/socket settlement. Child expiry closes stdin, sends TERM to its PGID, waits five seconds, sends KILL if needed, then allows five seconds for the broker to reap its direct group leader, close stdout/stderr/pipes, and prove PGID absence. Worker exit uses the same sequence concurrently across registered resources. Exact owned-root recursive removal runs only through a host-owned direct cleanup worker with a 30-second limit and final absence check; the complete outer cleanup transition is capped at the reserved 45 seconds. Credential zeroing follows target transport/child settlement and precedes root removal. Timeout, never-settling adapter, surviving PGID/root, or cleanup error is fatal; no promise is awaited after the broker has made its underlying owner terminal. A deadline after a write may have crossed is ambiguous: a surviving logical controller may use its already-defined bounded read-only reconciliation only after transport quiescence, while launcher loss permits none. No retry, replacement child, later write after poison, or detached/orphan transport is allowed. The coordinator supervisor adds at most the separately specified 120 seconds beyond the host aggregate: it gives liveness-EOF poison priority over escalation, escalates only a wedged host after the 55-second cooperative window, then removes the coordinator root within the remaining partition; it never converts host loss into success.

### AC5 — Build one deterministic, explicitly non-installable source artifact

Set the first successor version to 0.1.0-dev.136.1 and add schemas/source-artifact-manifest.v1.schema.json, tools/build-source-artifact.mjs, tools/verify-source-artifact.mjs, and tests/governance/source-artifact.test.ts. The builder consumes committed Git objects at a reviewed source SHA, never mutable working-tree bytes, and emits exactly three sidecar files: echo-context-<version>-source.tgz, echo-context-<version>-source.tgz.sha256 (coreutils sha256sum format), and echo-context-<version>-source.manifest.json. The manifest is a sidecar and is never a member of the archive it describes — no self-referential size/hash. The manifest serializes as canonical JSON (UTF-8, sorted keys, LF, single trailing newline). The archive root is the single directory echo-context-<version>/ containing exactly the Git-tracked paths at the source SHA in sorted path order, modes taken from Git (0644/0755), uid/gid 0 with numeric owners, member mtimes fixed to the source commit's committer timestamp, and gzip written without name/timestamp metadata.

The manifest binds version, source commit/tree, extraction baseline, lock hash, exact path/mode/blob/content inventory, tar member order/modes/timestamps, source-archive size and source-archive SHA-256, and the literal classifications artifact_kind source, installable false, runtime_authority false, state_authority false, maturity DEV. Hash names are distinct and used consistently everywhere this spec, its records, and its tests mention them: the **source-archive SHA-256** is the digest of echo-context-<version>-source.tgz and is the value the .sha256 sidecar binds; the **manifest hash** is the SHA-256 of the manifest's canonical-JSON bytes; the **lock hash** is the SHA-256 of the package-lock.json blob at the source SHA; the **workflow-artifact digest** (AC6) is the digest of the uploaded CI workflow artifact that carries the three files plus the tuple record. No unqualified "artifact SHA-256" appears in any record, summary, or test. The archive contains source and verification material only; it has no compiled service, LaunchAgent, client config, credential, live state, or install script.

Two builds from the same Git objects and pinned toolchain must be byte-identical — a determinism property proven in tests; the release path itself builds exactly once per AC6. Dirty-byte substitution, wrong commit/tree/version, missing/extra/reordered member, unsafe archive path, mode/timestamp drift, checksum mismatch, baseline non-ancestry, or changing any authority/installability flag fails verification.

### AC6 — Seal one private source prerelease without granting runtime authority

All successor changes remain on the frozen echo-context feature branch while this specification is reviewed. After fresh convergence, a fresh Codex builder repairs that branch without rebasing or rewriting cycle-one history, and a different fresh independent reviewer evaluates the corrected exact Project_echo and echo-context heads. AC4's evidence-carrying landing gate then lands exact merge commit `M` and verifies the three required quality/security jobs plus the read-only source build at `M`. Only exact Git objects from authenticated canonical `main=M` may build or publish. No implementation head reviewed in cycle one is merge or release authority.

Release has one read-only remote build caused by the authorized target-main push, one local read-only `collect` invocation that produces the evidence used to authorize publication, and one later local `publish` invocation that owns every release mutation; there is no write-capable remote publish workflow, manually/API-triggered build, or target authorization ref. `.github/workflows/source-release-build.yml` has only `contents: read`, sole trigger `push` restricted to branch `main`, concurrency group `echo-context-source-build-${{ github.sha }}` with `cancel-in-progress:false`, run name `echo-context-source-build-${{ github.sha }}`, and sole job `build-artifact`. It accepts no workflow inputs and has no manual/API, callable, schedule, tag, pull-request, or other trigger. It fails unless `github.event_name == push`, `github.ref == refs/heads/main`, checkout/HEAD equal `github.sha`, committed version/lock identities validate at that SHA, and `github.run_attempt == 1`. It has a terminal `always()` rerun sentinel, no environment, no target write permission, no Project_echo access, and no tag/release API or publication-controller invocation.

There is no API-created build event, nonce, or discovery owner. After authenticated target readback proves `main=M`, the coordinator starts a fresh `collect` operation host at exact authenticated `P_H`; that host starts the launcher worker and envelope over the finite RPC channel. Before target token/API access, the worker credentiallessly requests public `P_L` plus the exact target-main plan/authorization paths, blobs, hashes, and ancestry through the host's public-read capability. The envelope requests a fresh root and config-isolated detached clone through the host, requires `M` absent from the source object database, decodes the authorized raw merge body from the authenticated target-main plan, revalidates object format/base64/length/body SHA-256/fixed fields, requests absolute Git `hash-object -t commit -w --stdin`, requires OID `M`, and re-reads the body byte-for-byte. It then detaches at that locally rematerialized `M` and proves clean status, `HEAD=M`, `HEAD^{tree}=M^{tree}=H^{tree}`, `git fsck --full`, and byte identity for `tools/release-publication-controller.mjs`, its complete static-import/schema closure, `tools/verify-source-release-authorization.mjs`, `tools/github-download-host-policy.v1.json`, and `provenance/runtime-inventory.v2.json` against Git objects at `M`. A target remote clone/fetch, target API call, credential lookup, worker-owned child spawn, or credential-bearing child spawn before this gate is forbidden; the host's exact precredential local-Git provenance children are the only allowed spawns. Only after the gate does the host acquire and retain the invocation's validated target credential and expose the schema-bounded read-only target RPC required by `collectPublicationEvidence(input,deps)`; no worker or target controller receives a token, signed URL, pipe descriptor, arbitrary endpoint, arbitrary command, or process-spawn capability.

Inside that read-only state machine, the target adapter authenticates `GET /repos/zhenye0616/echo-context/actions/workflows/source-release-build.yml` as one positive numeric workflow ID whose workflow-metadata path is exactly `.github/workflows/source-release-build.yml` and state is `active`. Starting from the recorded target-main push interval, it performs at most 121 read-only selections at monotonic offsets 0, 15, 30, …, 1800 seconds. Each selection queries the numeric-workflow runs endpoint with `head_sha=M` as the sole server-side selector, fully paginates the entire returned set through the authenticated `total_count`, and fails rather than truncating if pagination is inconsistent, loops, or reaches GitHub's 1,000-result cap. Before applying workflow path, title, event, branch, attempt, status, or time predicates, the total `head_sha=M` set must contain zero or one run; two or more are immediate permanent ambiguity even when only one lies inside the widened push interval. Zero may continue polling. The sole run must then have the authenticated numeric workflow ID, API workflow-run path exactly `.github/workflows/source-release-build.yml@main`, `display_title == echo-context-source-build-<M>`, event `push`, `head_branch == main`, head SHA `M`, run attempt 1, and server `created_at` within the authorized push interval widened by 60 seconds before and 180 seconds after. The `@main` run-path suffix is distinct from the plain workflow-metadata path and is required by a raw current-API fixture; plain or other-suffix run paths fail. Once one appears, exact-ID GET and job/artifact endpoints must keep that same run identity through `completed/success` and exactly one server job named `build-artifact` with head `M` and `completed/success`. After download/verification and immediately before `collect` returns plan-construction evidence, it repeats the complete global `head_sha=M` uniqueness and exact-ID run/job/artifact readback. During `publish`, that same complete global uniqueness plus exact-ID workflow/run/job/artifact identity, attempt, status, conclusion, head, name, digest, and expiry readback is coupled to every repository-namespace boundary: it runs immediately before each predecessor scan `E`/`N0`…`N4`, immediately after each successor scan `N0`…`N5`, and once more after the final byte-level `N5` postcondition. Either half of a boundary drifting prevents the next write; drift after a write poisons every successor and the operation fails. A later duplicate, rerun, replaced job/artifact, or exact-ID drift is permanent ambiguity even when the repository namespace still matches. Zero candidates at the outer deadline, duplicate, rerun, wrong workflow/event/branch/head/time/title/path, malformed readback, timeout, failed, or cancelled run fails release authority. Neither the envelope nor the state machine creates, reruns, cancels, or substitutes a build or performs any target mutation; correction requires a new reviewed descendant/main push and fresh records.

The build checks out exact `M` with `persist-credentials:false`, proves extraction-baseline ancestry, asserts actual HEAD/tree, and invokes `build:artifact` exactly once. Before upload it writes canonical `source-release-inner-tuple.v1.json` containing source SHA, source tree, version, source-archive SHA-256, lock hash, manifest hash, and build run ID, plus canonical `source-release-build-evidence.v1.json` containing repository ID/node ID, constant workflow path, event, ref, head SHA/tree, run ID/attempt, logical job ID, toolchain, completed commands, and authority flags; neither record invents context-unavailable numeric workflow or server-job IDs. It uploads exactly one workflow artifact named `echo-context-<version>-release-<M>` containing only those two records and the three AC5 files. Authenticated API readback independently adds exact numeric workflow ID/path, server job ID/name/status/conclusion, workflow artifact ID/name/digest/expiry, and the inner evidence-record path/blob SHA-256. No lookup by latest or by name alone is permitted.

The exact-artifact-ID download endpoint is called with manual redirect handling and must return exactly one HTTP 302. Its `Location` must be an absolute HTTPS URL with no userinfo, fragment, or non-443 port and a host accepted by committed `tools/github-download-host-policy.v1.json`, whose only case-normalized, DNS-label-aware suffix rules are `.actions.githubusercontent.com`, `.githubusercontent.com`, `.blob.core.windows.net`, and `.amazonaws.com`; string lookalikes such as `evilamazonaws.com` do not match. The one redirected GET sends no Authorization, Cookie, GitHub API-version header, or other credential; only the signed URL and nonsecret binary Accept/User-Agent headers cross hosts. It must return direct HTTP 200 with no second redirect. The signed URL/query is held in memory only and never persisted or logged. Artifact metadata must carry digest `sha256:<64-lowercase-hex>`; the raw unextracted ZIP body is hashed before extraction and must equal that exact hex. The completed successful run, successful job, evidence carrier, and exact artifact yield the approved build tuple.

Within the same `collect` invocation, `collectPublicationEvidence` requests download of the raw ZIP by exact run and artifact IDs into host-owned paths, verifies ownership/name/expiry/API digest and raw bytes before safe extraction, validates the two canonical records and exactly three release files, and independently requests `verify:artifact` against the carried manifest hash without rebuilding. It returns only the nonsecret, schema-bounded evidence tuple and hashes to the host. After worker return, the host settles target transports, reaps every direct group leader and proves PGIDs absent, zeroes the credential Buffer, removes its exact operation root, and only then returns that tuple to the Project_echo coordinator. The coordinator uses the evidence—never a mutable path or secret—to construct the annotated-tag body as these exact UTF-8 bytes with LF separators and one final LF, where `<epoch>` is the decimal committer timestamp of `M` and the timezone is fixed `+0000`:

```text
object <M>
type commit
tag v<version>
tagger ECHO Release Coordinator <noreply@echo.local> <epoch> +0000

echo-context source release
version: <version>
source-sha: <M>
```

Before authorization, the coordinator computes and records both SHA-256 of those raw tag-body bytes and the Git tag-object OID obtained by hashing the canonical `tag <byte-length>\0<body>` object; it also records object target `M`, type `commit`, tag name, tagger name/email/epoch/timezone, and the exact annotation. No tag field is selected after authorization.

Only after `collect` succeeds does the coordinator construct canonical JSON `raw/internal/migrations/2026-07-15-136-echo-context-source-release-publication-plan.json`, validated by `schemas/source-release-publication-plan.v1.schema.json`, using the same canonical byte rules as AC4. Its own bytes carry every non-self-referential immutable input: item/spec/seal/delegation identities; exact `P_H` plus the operation-host, Project-owned askpass helper, owned-root-cleanup helper, reviewed-operation-launcher, operation-envelope, Project authority publisher, and operation-RPC-schema paths/blob OIDs/SHA-256/static-import closures used for `collect` and `publish`; builder/reviewer actor/run identities and reviewed heads; authenticated GitHub actor login/user ID/node ID plus exact no-plaintext GitHub CLI config identity, user-pinned keyring-selection provenance, direct-HTTPS credential boundary, and observed nonsecret scope set; canonical absolute nonsymlink Node/Git/GitHub-CLI paths and exact versions; target repository ID/node ID/owner/name/visibility/canonical token-free URL plus `M`/tree; version; complete source/checksum/lock/manifest/raw-ZIP/inner-carrier tuple and hashes returned by `collect`; exact global-unique push workflow/run/job/artifact/evidence fields; exact tag body/tagger/byte-hash/object-OID/peeled-source fields; release and percent-encoded asset names; authority flags; fixed Project_echo authorization path; credential-boundary declarations; framed broker capability matrix and sequence rules; clean launch closure, scrubbed environment/config, owned-workspace paths, host-owned resource inventory, and complete generated deadline/coordinator/host-termination table; the complete ordered artifact checks and target preflight; local tag-object materialization/verification; sole direct-URL tag push; every exact REST method/host/endpoint/header/body; response grammar; empty snapshot `E` and namespace checkpoints `N0` through `N5`; ordered readback; final postcondition; intended operation and backup/rollback identities; one-attempt counters; and literal `retry:false`, `adopt:false`, `cleanup:false`, and `automatic_reentry:false`. Its SHA-256 is recomputed from those exact path bytes. The immutable Project_echo record `raw/internal/migrations/2026-07-15-136-echo-context-source-release-delegated-authorization.json`, validated by `schemas/project-echo-delegated-operation-authorization.v1.schema.json`, independently duplicates and binds those identities plus the exact plan path/schema/hash, intended operation `publish_private_source_prerelease`, backup identity `destination_namespace_empty_preflight`, rollback identity `none_automatic_fail_stop`, and literal ambiguity policy; neither file embeds its own containing commit or hash. The coordinator supplies the exact additive candidate ending at `P_S`, containing only the plan and authorization, to `publishProjectAuthorityCommit` class `source_publication_authority`. Exact public-main CAS porcelain, ancestry, containing commit, candidate range, both path/blob/schema/hashes, and credentialless readback must be pinned before any target release write; a lost/ambiguous Project push blocks until the separate read-only reconciliation contract is satisfied.

Server-generated release and asset IDs and signed download URLs are not invented before authorization. The canonical plan defines typed, single-assignment state variables `release.id`, `release.upload_url`, `asset.archive.id`, `asset.checksum.id`, and `asset.manifest.id`, plus an ephemeral `download.location`; for each it names the sole prior unambiguous response field that may assign it, the schema/range constraint, the exact downstream endpoint-template substitutions, and the readback that must authenticate the assignment before the next mutation. A variable cannot be prefilled, reassigned, derived by name/latest, persisted as approval authority, or consumed before its defining response and readback. The plan hash binds these templates and derivation rules; the post-operation migration record binds the concrete captured IDs, while signed URLs are never recorded.

The local controller invocation pins the exact Project_echo authorization commit/path/blob hash and plan path/blob hash. Its dedicated public-ECHO adapter may call only host-pinned `https://api.github.com` endpoints for `zhenye0616/ECHO`, sends no `Authorization`, `Cookie`, proxy credential, Git credential-helper, askpass, `http.extraheader`, target token, or other secret, rejects every redirect and host/scheme change, proves the supplied authorization commit is an ancestor of public Project_echo main, and reads the exact path bytes at that commit. It constructs an explicit clean public-read environment with credential/token variables removed and system/global/local Git credentials, helpers, askpass, and extraheaders disabled rather than inheriting process state.

The operation host's Project/target private transport uses the founder's existing GitHub CLI keyring credential without creating or changing credential state. Only after the applicable Project-class or target-clone provenance gate (and, for `land/write` or `publish`, credentialless public authorization verification), the host validates the plan-bound canonical absolute nonsymlink GitHub CLI config directory and opens `hosts.yml` once with no-follow semantics. Every path component is owned by founder UID `501` and not group/world writable; pre/post-read `fstat` on the same descriptor must match device, inode, owner, exact mode `0600`, size at most 16,384 bytes, and nanosecond mtime/ctime. Its strict UTF-8/LF byte parser accepts only four- and eight-space indentation under one top-level `github.com` mapping, nonsecret scalar `git_protocol`/`user` fields, and a `users` mapping whose login keys use `^[A-Za-z0-9](?:[A-Za-z0-9-]{0,37}[A-Za-z0-9])?$`, contain no `--`, and have empty values. The `users` mapping must contain `zhenye0616`; the active `user` and configured Git protocol are not authority. Tabs, CR/NUL/BOM, comments, quotes, anchors, aliases, tags, merge/flow/multiline syntax, duplicate or unknown keys, another host, any nonempty `users.<login>` value, and any plaintext token/credential field fail. After parsing, the host creates a mode-0700 `gh-config-snapshot` beneath its owned operation root, exclusively creates mode-0600 `hosts.yml`, writes only the exact validated nonsecret bytes, closes and reopens it no-follow, and requires owner/mode/size plus byte-for-byte/SHA-256 readback. The live config path is never reopened or passed to a child; swap, truncate, rewrite, or account-map change after the descriptor read cannot alter this invocation. The snapshot is the sole `GH_CONFIG_DIR` and is removed with the operation root.

Before child spawn the host removes `GH_TOKEN`, `GITHUB_TOKEN`, `GH_ENTERPRISE_TOKEN`, `GITHUB_ENTERPRISE_TOKEN`, `GH_HOST`, inherited `GH_CONFIG_DIR`, `XDG_CONFIG_HOME`, ambient PATH selection, and every upper/lowercase proxy override, then supplies only the authenticated snapshot directory as `GH_CONFIG_DIR` alongside the common scrubbed child environment. It runs the plan-bound canonical absolute nonsymlink `GH` with exact argv `["auth","token","--hostname","github.com","--user","zhenye0616"]` exactly once as a registered direct process-group leader; no `auth status`, `auth switch`, other GH subcommand, or second token command is allowed. The pinned GitHub CLI binary/path/version and this exact documented local token subcommand are the trusted keyring-acquisition boundary; the host does not claim OS-level observation or denial of sockets opened internally by that trusted executable.

The GH child's stdout is a private pipe to the host, accumulated only as raw Buffers under a 4,096-byte hard cap, never decoded to a string or logged. It accepts one nonempty bearer byte sequence followed by exactly one LF: every pre-LF byte must be ASCII alphanumeric or one of `-._~+/`, with `=` allowed only as a trailing suffix; whitespace, CR, NUL, colon, an empty value, extra line, missing LF, cap overflow, or any extra byte fails. Every raw stream chunk and intermediate accumulator is overwritten on every success/failure/cancellation path immediately after the single master Buffer is created or acquisition aborts. The host alone retains that one nonlogged master Buffer for the invocation and uses it in the sole credentialed identity check: a direct, no-redirect host-owned `GET https://api.github.com/user` must return login `zhenye0616`, user ID `73834646`, and node ID `MDQ6VXNlcjczODM0NjQ2`, while the normalized `X-OAuth-Scopes` set must equal `gist,read:org,repo,workflow`. A Project-authority host receives only the Project repository push capability; a target host receives only its plan-bound target capabilities. Every Git operation independently uses the literal canonical HTTPS repository URL, so active-account and configured-protocol state cannot redirect it. The launcher and controllers receive only the authenticated nonsecret identity/scope tuple and typed RPC results. No Buffer, credential-derived object, private transport, descriptor, or signed URL crosses into a worker or between invocations. A second acquisition, wrong executable/path/version/mode/config/snapshot, plaintext config secret, malformed bearer bytes, different identity, missing/extra scope, absent scope header, redirect, or auth error gives zero controller access/write. No token bytes or token hash enter a record.

The invocation's single validated Buffer never reaches the public adapter or any worker. Host-owned in-process HTTPS places it only in the direct `/user` identity check and later plan-bound target API Authorization headers; a signed-download redirect never receives it. Git HTTPS authentication uses Project_echo's committed `tools/echo-context-git-askpass-fd.mjs`: before each exact Project or target Git operation the host authenticates the helper as the canonical absolute nonsymlink path at `P_H`, with exact blob bytes/static-import closure, pinned absolute Node, and scrubbed `PATH`; copies the retained Buffer into a fresh anonymous inherited token pipe; creates a second anonymous nonsecret receipt pipe; and starts Git as its own direct process-group leader. During the `R` bootstrap those exact local helper bytes are bound by the independent review and then proven by public `P_H` readback before any target access. The Git child receives only `GIT_ASKPASS=<authenticated-absolute-helper>`, `ECHO_GIT_ASKPASS_FD=<decimal-token-read-fd>`, `ECHO_GIT_ASKPASS_RECEIPT_FD=<decimal-receipt-write-fd>`, and `GIT_TERMINAL_PROMPT=0`; `GIT_ASKPASS_REQUIRE`, `SSH_ASKPASS`, and `SSH_ASKPASS_REQUIRE` are absent. The host rejects inherited or configured `core.askPass`, credential helpers, `http.extraheader`, proxy variables/config, URL rewrites, remote helpers, and any alternate askpass source before spawn, uses the literal token-free canonical URL, and supplies command-scoped `-c http.followRedirects=false`. Each helper process writes exactly one nonsecret receipt line naming its recognized prompt class. The username invocation writes `username\n`, returns fixed `x-access-token`, and does not read the token pipe. The password invocation first consumes the token through EOF, writes only those bytes plus LF to its private stdout credential pipe connected to Git, and only after that write succeeds emits `password\n` on the receipt pipe. Before accepting Git completion, the host requires receipt bytes exactly `username\npassword\n` followed by EOF and token-pipe EOF; a missing/substituted/repeated/unknown/out-of-order prompt, second token read, extra receipt, redirect, helper-byte/path drift, or inherited descriptor fails and poisons later writes. Each Git call gets fresh token and receipt pipes; on completion/failure the host closes all ends and removes its per-call environment while retaining the master Buffer for later exact calls. In the invocation's single final teardown—after all HTTP streams and direct child group leaders are reaped and every PGID is absent—the host overwrites that Buffer and persists no token, helper state, config, file, or URL. Secret bytes may exist only in the pre-existing OS keyring; the exact user-pinned `gh auth token` child memory; its private stdout-to-host acquisition pipe and host raw/master Buffers; authenticated target API Authorization headers/HTTPS process memory; the anonymous host-to-helper token pipe; authenticated helper/Git/libcurl process memory; and the private helper-stdout-to-Git credential pipe. The sole GH child is a registered direct group leader whose streams/private pipe close, whose leader is reaped, and whose PGID is proven absent before the host proceeds. Secret bytes may never enter the validated config file, argv, ordinary environment values, receipt bytes, Git config, filesystem, plan/record, diagnostics, redirected download, public adapter, IPC, launcher, controller, coordinator terminal stdout, or any externally captured stdout/stderr/log. Keyring/account rotation fixtures after acquisition prove no second fetch/substitution; redirects and upper/lowercase proxy overrides fail before transport. The operation creates no new credential or signing-key state. Mutable Project_echo branch content is not authority; only the pinned commit/path/hash/schema bytes are.

After the `P_S` plan/authorization commit is publicly readable, the coordinator starts a separate exact-object-authenticated `P_H` operation host in `publish` mode; no root, clone, file descriptor, stream, child, token, signed URL, or mutable object crosses from `collect`. Before target token/API access, its launcher worker requests credentialless authentication of both public authorization layers: `P_L` with the target-main plan/authorization that seals raw `M`, and `P_S` with the source-publication plan/authorization. Through plan-bound host RPC the envelope obtains a new mode-0700 root with exact `clone`, `download`, and `extract` children, again starts from reviewed local `H` with `M` absent, rematerializes/re-reads exact `M` solely from the authenticated target-main plan, detaches at `M`, and repeats the clean/tree/fsck/controller/import/schema/inventory gate. It then invokes the committed source-release authorization verifier from that clone against the pinned public record/plan. Only after both authorization layers and provenance pass does the host acquire and retain its one validated target credential Buffer and expose the finite target RPC to the authenticated controller. Before its first write, the controller requests canonical `main=M` readback; repeats complete global `head_sha=M` workflow uniqueness; redownloads by the authorized exact IDs; and revalidates workflow-artifact ZIP digest, safe extraction, carrier bytes, all three AC5 files, and every nonsecret field against the plan. Any duplicate/rerun, identity/hash drift, or failure blocks before mutation. The fresh download, rather than any path or byte retained from `collect`, is the sole publication input.

`tools/release-publication-controller.mjs` exports exactly the read-only `collectPublicationEvidence(input,deps)` and the mutating `runPublication(input,deps)` logical state machines. Both are invoked only as launcher workers through the authenticated Project_echo operation host and finite schema-bound RPC; `collectPublicationEvidence` receives structurally zero mutation methods, while `runPublication` receives only the exact ordered AC6 mutation methods. The workers/controllers cannot import `node:child_process`, open credential-bearing HTTP, access the token/pipe/signed URL, select arbitrary endpoints, or request arbitrary commands. The host owns every HTTP request, Git/gh/cleanup direct child, pipe, stream, process group, credential, operation root, deadline, poison transition, and teardown; tests inject host transports beneath these production state machines and never form another selection, download, or mutation implementation. Controller return or worker death cannot release the host: its outer `finally` settles/terminates all resources, zeroes credentials, and removes the root across partial setup/clone/provenance/authorization/controller/cleanup failure. Before its first write, `runPublication` verifies the exact Project_echo authorization and plan, repository identity, `main=M`, source/artifact/tag identities, repeats global workflow uniqueness, and requests empty namespace snapshot `E`: fully paginated repository tags, all releases, and every release's assets prove that the entire repository publication namespace has zero tag ref, release, or release asset. Any object, authentication error, malformed response, redirect, pagination loop/truncation, cap, or readback ambiguity blocks.

Because this is the repository's first release, the governed publication namespace is the entire repository tag/release/asset namespace, not a version-filtered prefix. A namespace scan fully paginates all repository tags and releases and every release's assets, rejects loops/truncation/the 1,000-result cap, and records exact object IDs, names, flags, tag OIDs, endpoints, sizes, and byte identities; filtering away an unrelated object or checking only the captured object is forbidden. The empty preflight snapshot is `E`: zero tags, zero releases, and therefore zero assets. Exact post-write snapshots are `N0` the sole exact tag; `N1` that tag plus the sole captured draft prerelease; `N2` plus only the archive asset; `N3` plus archive and checksum assets; `N4` plus all three assets while still draft; and `N5` the same exact IDs/bytes with the sole release published as a prerelease and not latest. Immediately before each of the six mutations the host must perform the coupled global-build/exact-ID check and rescan/match the exact predecessor (`E`, then `N0`…`N4`); immediately after each unambiguous response it must rescan/match the exact successor (`N0`…`N5`) and repeat the coupled build check before any later write. Final success additionally repeats build identity after the complete byte-level `N5` postcondition.

The fixed write order is exact local tag-object materialization/verification → annotated-tag create-only push → draft release POST → archive upload → checksum upload → manifest upload → publish PATCH, with the required predecessor/successor namespace scans bracketing every mutation. Local materialization uses the authorized raw body, verifies its byte SHA-256 and `git hash-object -t tag -w --stdin` result equal the authorized immutable tag-object OID, and re-reads that OID byte-for-byte before push. It creates no mutable local tag ref. The sole tag write is one broker-owned absolute-Git invocation whose semantic argv is `-c http.followRedirects=false push --porcelain --no-verify --no-follow-tags --force-with-lease=refs/tags/v<version>: https://github.com/zhenye0616/echo-context.git <TAG_OBJECT_OID>:refs/tags/v<version>`, under the same exhaustive config/environment isolation as the main push. Exactly one structurally parsed porcelain row with flag `*`, destination `refs/tags/v<version>`, and summary `[new tag]` succeeds; hook execution, redirect, follow-tag publication, up-to-date, another flag/ref/summary, local-ref/remote-alias substitution, or any additional/malformed row fails. The sanitized proof contains only flag, destination ref, and captured tag-object OID.

Each remote mutation is attempted exactly once with transport retries disabled. Draft creation accepts only direct HTTP 201 with parseable body carrying a newly captured numeric release ID, exact tag/name/`draft:true`/`prerelease:true`, and exact release-scoped `upload_url` template `https://uploads.github.com/repos/zhenye0616/echo-context/releases/<release-id>/assets{?name,label}`. Each asset upload expands that captured template to `https://uploads.github.com/repos/zhenye0616/echo-context/releases/<release-id>/assets?name=<exact-percent-encoded-name>` and accepts only direct HTTP 201 whose parseable body carries a new numeric asset ID plus exact decoded name, size, and `state: uploaded`; no nonexistent asset-response `release_id` field is assumed. Each successor namespace scan fully enumerates the exact captured ID once under that release and fetches exact JSON metadata through `GET https://api.github.com/repos/zhenye0616/echo-context/releases/assets/<asset-id>`, while also proving the absence of every extra tag, release, or asset anywhere else in the repository.

The separate binary GET to that same exact-ID endpoint uses `Accept: application/octet-stream`, manual redirects, and accepts either direct HTTP 200 bytes or exactly one HTTP 302 satisfying the same signed-download policy as the Actions artifact. A redirected request sends no Authorization, Cookie, or GitHub API header and must terminate in direct HTTP 200 with no second redirect; only the terminal body is hashed. This exact metadata-plus-binary sequence runs before the next write and again in the final postcondition. Publish accepts only direct HTTP 200 with parseable body carrying the same release ID/tag/name and `draft:false`/`prerelease:true`; it sends string `make_latest: "false"`. A malformed or mismatched nominal-success body, hostile Location, credential-bearing redirected request, or unexpected redirect/status is ambiguous even if later destination state looks correct.

The controller retains the captured release record and all three `{id, name, size, sha256, expected_bytes, upload_endpoint}` asset records. Every successor scan requires exact authenticated response-plus-readback identity before the next write. `N5` completely paginates the full repository namespace; proves exact tag-object OID/body/peeled `M`, exact release ID/tag/name/flags and not-latest observable; requires the asset-ID set to equal the three captured IDs; and re-fetches exact metadata and re-downloads/re-hashes byte-identical content for each captured ID. Matching names or bytes under a different release/asset ID fail; same-content delete/recreate, ID drift, any extra/missing/foreign object, changed bytes, changed flags, or a different tag object fails.

Destination namespace readback is the sole durable publication authority; Actions state, run logs, plan markers, and diagnostics are evidence only. Each next write requires both an unambiguously successful response and exact full-namespace successor readback of the prior write. Drift observed before the host begins a mutation yields zero write. A concurrent drift after the predecessor scan may race only with the already in-flight mutation; the mandatory successor scan then fails, poisons the invocation, and permits no subsequent write. If the controller and host survive a timeout, lost/ambiguous response, malformed success, or failed/ambiguous readback, they may perform bounded read-only reconciliation only and then exit nonzero with zero retry, repeat write, delete, remote cleanup, adoption, or later mutation; mandatory local owned-root cleanup still runs. Unexpected launcher/worker death or IPC EOF poisons the host, which rejects new requests, aborts target HTTP, closes credential pipes, TERM/KILLs its direct child process groups, reaps each direct group leader, proves every PGID absent, zeroes the token, and removes its owned root; there is no later write or reconciliation from that invocation. If the operation-host process itself or the machine/kernel dies, cleanup and post-mortem logging cannot be guaranteed and an already in-flight mutation may remain ambiguous, but no surviving coordinator/launcher/controller state-machine capability can issue a successor transition or automatically re-enter. Tag-only, tag-plus-draft, partial-assets, and apparently-complete namespaces mechanically block every cold re-entry. Any later manual disposition is a separate exact authorized operation; this item provides none.

There is intentionally no separate durable attempt/consumption marker. If an invocation ends before any target object exists—or a first-write transport failure leaves an authenticated empty namespace—a cold process cannot mechanically distinguish that attempt from an authorization never executed. Under the accepted trusted single-coordinator/single-owner threat boundary, coordinator policy forbids replaying the same build/plan/authorization and requires a new reviewed descendant/main push, unique push build, plan, and Project_echo authorization, but this empty-namespace cold-replay prohibition is not claimed as server-enforced. The controller still exposes no retry/re-entry/adoption command and never launches a second invocation automatically. Adding a durable marker solely to claim stronger replay prevention would violate the founder-approved destination-as-sole-publication-authority design and requires a future reviewed architecture change.

A fresh no-local clone downloads the three private prerelease assets by exact captured IDs, verifies archive/checksum/manifest bytes and the approved content tuple, and runs the AC3 release-mode trace with the externally approved manifest hash. `raw/internal/migrations/2026-07-15-136-echo-context-repository-bootstrap.md` records the canonical URL/repository IDs, baseline and final main SHAs/trees, implementation-review commit/path/blob/hash and landing-plan identities, exact pre/post hosted run/job/carrier identities, unique read-only push-build workflow/run/job/artifact identities, Project_echo authorization commit/path/hash, publication-plan path/hash, tag-body hash/tag-object OID/tagger fields, local controller actor/run identity, tag/release/asset IDs, complete content tuple, fresh-clone and owned-cleanup evidence, builder/reviewer identities, and `authority:false` / `installed:false`, without secrets or expiring URLs.

The handoff to item 137 is the immutable content tuple source SHA + source tree + version + source-archive SHA-256 + lock hash + manifest hash; the build run/job/artifact and local publication fields are provenance recorded in the migration record, not mutable selectors in the 137 contract. Item 137 must consume these exact landed outputs and separately review any later-release generalization; item 136's empty-namespace controller is not automatic authorization for another release. At completion, echo-context owns source and source-artifact authority only; Project_echo remains installed runtime and live-state authority, and no machine client points at the new repository or artifact.

## Out of Scope (Don't Drift)

- No LaunchAgent, machine-wide/per-user install, service composition root, MCP client registration, port, bearer-token, or loopback-exposure decision.
- No live database, capture checkpoint, onboarding state, project config, credential, or client-config read/copy/migration.
- No active daemon switch, dual-run, rollback ceremony, Project_echo context freeze, or runtime/state authority transfer.
- No npm/public package publication, public repository visibility, auto-update channel, source signing program, or generic release framework.
- No context-tool, capture, storage, retrieval, schema, clustering, enrichment, or embedding behavior change.
- No Project_echo deletion/rename/deprecation and no echo-brain or echo-loop repository/install work.
- No Team-product maturity advancement; this internal source release remains DEV.

## Risks

- Extraction evidence could be weakened by rebasing it onto evolving HEAD. The frozen-baseline plus ancestry contract is the fallback; existing evidence bytes are never rewritten.
- Hosted CI could appear green only because the founder machine supplies Project_echo or state. AC3 removes implicit paths and proves a clean sibling-free clone.
- Publishing history could expose a secret. Pre/post full-history scans block the private push/release without printing values.
- Native better-sqlite3 behavior can differ across hosted runners. CI pins Node/npm and uses macOS plus Ubuntu; item 137 separately owns the exact Darwin runtime closure.
- The current private personal Free topology cannot stop a malicious or compromised sole owner from bypassing policy. AC4/AC6 make that limitation explicit and instead fail closed on stale identity, unreviewed bytes, missing immutable Project_echo authorization, compare-and-swap drift, duplicate exact-`M` push builds, or ambiguous destination state; paid server enforcement remains a future topology upgrade, not an item-136 claim.
- A source archive could be mistaken for an installable runtime. Its filename, manifest, tests, README, and release record all state installable:false and authority:false.
- Repository creation and baseline publication are already-completed immutable history and may never repeat. Target-main landing is mechanically single-use through its exact `B` lease. Local tag/release publication is single-attempt per controller invocation and bound to exact objects and canonical plan bytes; nonempty destination state mechanically blocks replay, while empty-namespace cold replay remains the explicit trusted-coordinator policy limitation in AC6.
- The first-release local controller contract is intentionally not reusable for a populated namespace. Item 137 owns the separately reviewed later-release generalization; absence of that propagation blocks its publication rather than widening item 136 again.

## Tests

- tests/governance/repository-authority.test.ts verifies the exact frozen baseline, successor ancestry, canonical authority fields, immutable item-135 evidence hashes (including byte identity of provenance/runtime-inventory.v1.json against its recorded hash), validity of the successor provenance/runtime-inventory.v2.json for the successor tree, that `verify:inventory` and its generator bind to v2, and negative wrong-remote/wrong-baseline fixtures.
- tests/echo-context/operation-envelope.test.ts executes Project_echo's production `runOperationHost(input,deps)`, Project-owned askpass helper, launcher worker, and `runOperationEnvelope(input,deps)` using only Project_echo-owned generated Git fixtures; it never imports echo-context, needs a sibling checkout, or fetches the private target. It models the external trust anchor by materializing exact host/helper bytes/static closures from pinned fixture `P_H` Git objects and matching credentialless public-readback bytes before Node starts; no repository module is credited with self-authentication. Static AST/capability fixtures prove the launcher, envelope, Project publisher, landing controller, and publication controller cannot import/invoke `node:child_process`, open credential-bearing HTTP, obtain token/signed-URL/pipe bytes, or submit arbitrary executable/argv/env/cwd/URL/method/header values. RPC fixtures enforce `project_authority` publish/reconcile and target mode/phase tables, one invocation ID, strictly increasing sequence, one transport request in flight, and poison rejection. Every `land/evidence`, `land/write`, `collect`, and `publish` invocation gets a distinct host-owned operation root/fresh fixture-target clone and exactly one gated target export. `collect`/`publish` start with local `H` and absent `M`, authenticate public `P_L`/`P_S` without target credentials, rematerialize/re-read exact `M`, and prove clean/tree/fsck/controller/import/schema/inventory before credential acquisition. A real-process fixture SIGKILLs the launcher while a host-spawned TERM-resistant Git writer and descendant are active; the surviving host must reject later RPC, abort HTTP, close askpass pipes, TERM/KILL the PGID, reap its direct group leader, prove the PGID absent, close streams, zero the token, remove exact owned roots, preserve an unowned sentinel, and prove no delayed-write sentinel appears. Partial setup/clone/rematerialization/provenance/authorization/cancellation/deadline/cleanup fixtures cover the same terminal ordering and primary-error preservation. Coordinator fixtures cover normal host exit, a wedged/killed host, exact supervisor deadline, host child reap/stream closure, coordinator-root absence, and non-success. One cancellation fixture proves closing liveness lets a cooperative host finish poison/resource teardown before the coordinator acts; a distinct wedged-host fixture proves no TERM occurs before 55 seconds and exact TERM/KILL/reap plus coordinator cleanup still finishes inside the 120-second outer reserve. Real-process fixtures SIGKILL the coordinator both immediately before a successor mutation dispatch and while a host-owned writer is active; they prove expected-parent mismatch or liveness EOF poisons, the liveness FD is absent from launcher/Git/gh/askpass/cleanup children, no delayed-successor sentinel appears, and a surviving host performs its bounded operation-root/resource teardown and emits no success. The honest boundary leaves the dead coordinator's own root/log cleanup and the sole immediately-post-check in-flight write explicitly unguaranteed. Wrong operation kind/mode/phase, stale/tampered host/helper/launcher/envelope/publisher/RPC schema/plan/raw merge body, wrong executable/version/cwd/tree, target access before the gate, worker root removal, resource reuse between invocations, or more than one target invocation fails.
- The same operation-host suite runs each of the three `project_authority` publish classes and all three fresh reconcile cases through production `runOperationHost`, proving bootstrap-local `P_H` closure binding, Project-only credential/askpass capability, zero target capability, and exact normal terminal-result→launcher-exit/reap→host-teardown ordering. Early zero exit, terminal-result-then-hang, wrong launcher topology, public-read/partial-materialization/host-spawn failure, never-settling coordinator cleanup, and cleanup-helper drift fail within the supervisor bound. Raw-wire fuzz feeds zero/oversized/partial/coalesced frames, invalid UTF-8, duplicate keys, excessive depth/string/array/object width, response-without-request, and >10,000 frames, proving rejection before dispatch/allocation beyond the cap and bounded poison teardown.
- Coordinator terminal-result fixtures cover zero exit without a frame, duplicate/truncated/oversized/mismatched/trailing frames, invalid UTF-8/schema, nonzero exit with success-looking output, result then live-host hang, stderr overflow, and secret-bearing diagnostics. Only one matching capped success frame plus exit zero, EOF/stream closure, bounded sanitized stderr, host teardown, and both root-absence results may pass.
- tests/echo-context/operation-transports.test.ts executes the Project-owned production REST, Link-pagination, binary-download, credential, askpass, Git/gh, deadline, cleanup-worker, and porcelain adapters against protocol-faithful request descriptors and transcripts; it imports no echo-context module. It proves exact API/media headers, host/path/method routing, bounded pagination, redirect credential stripping/allowlist enforcement, raw-byte caps, exact-ID binary transfer, Project/target capability separation, and protocol-accurate response parsing. Credential fixtures enforce canonical owner/mode/nonsymlink config paths, same-descriptor pre/post metadata, the capped strict no-plaintext `hosts.yml` grammar, exclusive host-owned snapshot creation/readback, exact user-pinned token argv against only that snapshot, private stdout framing, the 4,096-byte cap and bearer byte grammar without string decode, direct no-redirect host-owned `/user` identity/scope verification, and ordered zeroing of every raw/intermediate/master Buffer. Askpass fixtures require authenticated `P_H` helper bytes/path, exact decimal token/receipt FDs, absent alternatives, receipt sequence `username\npassword\n` plus EOF, username without token read, password consuming token to EOF, command-scoped redirect denial, canonical URL, and one porcelain row; missing/repeated/reordered/unknown prompts or extra receipts fail. Every normal Git/gh RPC withholds completion until direct-leader reap, streams/pipes close, and PGID absence. Never-settling JSON/body stream, lightweight local Git child, TERM-resistant helper/descendant, and cleanup worker fixtures prove bounded abort/TERM/KILL/reap, zeroing before exact-root removal, fatal surviving PGID/root, and zero later operation.
- The operation-transport executable fixtures bind canonical absolute nonsymlink Node/Git/GitHub-CLI paths and exact version output before credential access; allow only `gh --version` plus exact `gh auth token --hostname github.com --user zhenye0616`; and structurally reject `auth status`, `auth switch`, any other GH subcommand, and a second token invocation. Symlink/wrong-owner/permissive/alternate config, metadata change during descriptor read, oversized/invalid UTF-8, duplicate/unknown/host/token YAML fields, missing selected user, nonempty user entry, inherited token/config/host/XDG/proxy override, snapshot path/mode/byte drift, poisoned PATH, GH decoy, wrong executable/version, malformed bearer bytes, or spawn before executable/config/snapshot validation yields zero controller access or write. Validate-to-spawn fixtures swap, truncate, rewrite with plaintext, and change active/selected account mappings in the live file; the exact validated snapshot remains the only bytes the GH child can read, and an invalid pre-snapshot race fails. Fixtures with an unrelated active account/account switch and both OAuth- and PAT-shaped safe bearer tokens prove active state and token class are deliberately non-authoritative; only the exact pinned user's token followed by matching host-owned `/user` identity/scopes may pass. Tests treat the pinned GH binary/version plus exact token argv as the explicit trusted child boundary and make no unsupported per-process socket-observation claim.
- tests/echo-context/project-authority-commit-publisher.test.ts executes production `publishProjectAuthorityCommit(input,deps)` and `reconcileProjectAuthorityCommit(input,deps)` for `implementation_review`, `target_main_authority`, and `source_publication_authority` through their exact host phases. It proves exact `E`/candidate range/path/blob/hash allowlists, the reviewed `P_H` bootstrap exception, clean isolated worktree, Project-only credential capability, direct canonical URL, command-scoped redirect denial, one leased porcelain row, credentialless public ancestry/path readback, and no target access during bootstrap. Wrong parent/range/tree/path/bytes, merge, dirty worktree, unbound run log, stale lease, redirect, hook/helper/proxy/rewrite, malformed/multirow porcelain, or readback drift produces one attempt, zero retry/rebase/pull/adoption, and no downstream target authority. Ambiguous publish always ends; only a fresh mutation-incapable reconcile host seeing exact public `C` and every bound byte returns deterministic `applied_exact`, while `E`/other/unreadable state is terminal and no fourth marker commit exists.
- tests/governance/source-artifact.test.ts builds twice, compares bytes, validates the manifest/inventory, and rejects dirty, missing, extra, reordered, unsafe-path, checksum, version, tree, and authority-flag mutations.
- tests/governance/workflow-policy.test.ts parses YAML structurally through a declared pinned parser dependency and mutation fixtures, not source-string self-assertions. It proves immutable action pins; every checkout has `persist-credentials:false` and `fetch-depth:0`; exact Node/npm/Git; both quality jobs execute the exact AC3 source-mode vector once from canonical absolute paths under fresh mode-0700 HOME/tmp, a 65-minute job limit, and an `always()` exact-root cleanup; CI/secret triggers and required job IDs on every PR/push to main; pull-request checkout from `github.event.pull_request.head.sha` rather than `github.sha`/merge refs; in-job HEAD/tree assertions; exact canonical carrier generation/upload; least privilege; no environment; one read-only source-build workflow triggered only by a main push, SHA-bound run/concurrency names, attempt-one and terminal rerun sentinel, and no workflow inputs/manual/API/callable/scheduled trigger; and absence of every remote write-capable publish workflow, tag/release API call, reusable writer, alternate substantive check trace, or alternate release-mutation owner. Shallow checkout, relative path, wrong/missing/reordered vector, non-0700/symlink root, insufficient deadline, missing cleanup, malformed graph/permissions/needs/trigger/action pin/rerun/concurrency/carrier/artifact/synthetic-merge, or alternate writer each fails.
- tests/governance/landing-gate.test.ts directly executes echo-context's exported read-only `collectLandingEvidence(input,deps)` and mutating `runLanding(input,deps)` with finite injected broker capabilities; it never imports Project_echo, spawns a process, holds credentials, or retests outer host/clone provenance. Wrong actor login/user ID/node ID or credential provenance, repository ID/node ID/owner/name/visibility/default branch/canonical URL, baseline SHA, feature SHA/tree, failure of frozen-baseline→H or B→H ancestry, presence of a mutation-capable evidence dependency, builder/reviewer actor or run identity, identity equality, review commit `R` ordering/path/blob/hash/verdict, same-commit or stale/cycle-one review evidence, missing/failed/stale-SHA/wrong-app/duplicate-selected/unreadable workflow run/job/carrier artifact, canonical plan bytes/hash, delegated authorization, raw merge-body/base64/length/hash/header/parent/tree/epoch/message/OID mismatch, broker-bound direct-URL push argv/lease/porcelain, or post-push readback fails. Poisoned `origin`, `insteadOf`, proxy, remote-helper, executable pre-push hook, `push.followTags=true`, redirect, and arbitrary-command capability each prove zero unintended ref/target write. A fixture where H omits newer B requires a reviewed descendant rather than alternate merge bytes. Main/evidence drift and every lost/ambiguous/deadline push outcome allow read-only reconciliation only after host transport settlement, with exactly one attempt and no retry/adoption/rewrite. A valid two-phase fixture proves the first call returns only schema-bounded nonsecret evidence; a separate second call rematerializes exact `M` solely from plan bytes, re-reads its raw commit body, requests one direct canonical fast-forward porcelain push, and authenticates `main=M` without any first-phase mutable object.
- tests/governance/repository-bootstrap-gate.test.ts executes the production injected AC1 model over recorded fixtures without live writes. It covers expected `GET /user`, non-authoritative negative lookup, single POST-create direct HTTP 201, stable repository ID/node ID, fully paginated empty branch list, exhaustive zero-ref `ls-remote`, exact create-only main-push argv/porcelain, and post-readback. Redirect/non-201/lost/malformed response, wrong identity/visibility, ID drift, later-page foreign ref, identical pre-existing main, missing `--porcelain`, up-to-date/multirow output, ambiguous push, and failed post-readback each assert zero later write, retry, adoption, or cleanup.
- tests/governance/source-release-build-selection.test.ts runs echo-context's production read-only `collectPublicationEvidence(input,deps)` directly with injected authenticated read-only target adapters and a fake monotonic/UTC clock; it neither imports nor locates Project_echo, and the AC3 clean-clone/hosted-CI contract remains sibling-free. It binds the authenticated numeric workflow ID/plain metadata path/active state, target-main push interval, 60/180-second skew, and at-most-121 selection/1800-second outer schedule. Each poll queries only `head_sha=M`, fully paginates the entire set against `total_count`, and requires global cardinality zero or one before applying any time/title/event/path predicate. Raw current-API fixtures accept run path `.github/workflows/source-release-build.yml@main` and reject the plain metadata path or any other suffix. Visibility at every boundary, later-page unique match, exact-ID stability, exact title/`push` event/`head_branch`/head/attempt, one successful `build-artifact` job, exact artifact/evidence identities, exact-ID download/verification, repeated pre-return global uniqueness, and schema-bounded nonsecret return pass. A duplicate outside the authorized interval, a second run that otherwise fails predicates, 1,000-cap/inconsistent `total_count`, rerun, wrong workflow/event/branch/head/time/title/path, pagination loop/truncation, timeout, malformed/auth failure, failed/cancelled run, mutation-capable collect dependency, or any collect mutation fails permanently with no tag/release/repository-ref write. Separate `runPublication` fixtures pair full global uniqueness and exact-ID workflow/run/job/artifact stability with every pre-mutation predecessor scan, every post-mutation successor scan, and the final byte-level `N5` postcondition; injected duplicate/rerun/job/artifact/digest/expiry drift between each transition permits at most the already in-flight write and zero successor. Project_echo's operation-host suite owns the independent generic provenance-before-invocation composition proof; no cross-repository test runner exists or is required.
- tests/governance/release-authorization.test.ts validates review commit `R`, exact `P_H` host/askpass/cleanup/launcher/envelope/publisher/framed-RPC identities, the exact CAS-published `P_L`/`P_S` plan/authorization commits and paths, their schemas/hashes, complete build tuple, globally unique push workflow/run/job/evidence identities, exact target/Project_echo source SHAs, exact tag-body bytes/SHA-256/object OID/tagger fields, source/version/artifact/authority fields, intended operation, and fail-stop backup/rollback identities. Missing/tampered/wrong public record commit/path/blob, non-ancestor or same-commit review/plan evidence, noncanonical plan, executable path/version, source/version/run/job/artifact/digest/repository/tag/authority/plan mismatch, `run_attempt>1`, or identity substitution blocks before target mutation. Fixtures prove zero repeated mutation within one invocation, zero automatic second invocation, and mechanical cold blocking for every nonempty repository tag/release/asset namespace; an empty-namespace cold-start fixture documents rather than falsely rejects the explicit trusted-coordinator replay limitation. Carrier fixtures reject artifact retrieval by name/latest, wrong run/ID/name/expiry/API or raw-ZIP digest, unsafe extraction, wrong tuple, rebuild-on-publish, and any private/credentialed Project_echo dependency. Repository-policy fixtures also require absence of `source-release-approval.v1.json`, every `release-authorizations/*` ref mechanism, and every remote build/publication mutation owner.
- The same release-authorization suite supplies pure protocol fixtures proving public-ECHO versus private-target host/scheme/capability separation and complete credential isolation without importing Project transports. A sentinel target token, Authorization/Cookie/proxy header, credential-helper, alternate askpass, `http.extraheader`, token-bearing argv/ordinary env/config/file/worker message, or diagnostic leak fails; the known sentinel must be absent from public requests, IPC/workers, subprocess metadata, diagnostic/captured external stdout/stderr, and logs, while the explicitly private GH-to-host acquisition and helper-to-Git credential pipes are modeled only by the Project transport suite. Negative fixtures cover inherited token/config overrides, plaintext config token, malformed token carrier, a second acquisition request, rotation, wrong user/identity/scope, proxy/rewrite/redirect substitution, and invalid zeroing order.
- tests/governance/github-publication-adapters.test.ts is narrowed to echo-context's pure, noncredentialed protocol/request-descriptor and response-parser layer used by the logical landing/publication controllers. It imports no Project_echo code, opens no socket, spawns no process, and never receives a token, pipe, signed URL, arbitrary endpoint, or arbitrary command. Protocol-faithful fixtures prove every exact API-version/media-type descriptor; `api.github.com` versus `uploads.github.com` route selection; percent encoding; RFC 8288 pagination descriptors; Actions/release binary redirect policy descriptors; exact-ID requests; release/asset response parsing without `release_id`; tag/main push semantic descriptors; and rejection of malformed status/header/JSON/link/porcelain or capability escalation. Project_echo's `tests/echo-context/operation-transports.test.ts` alone proves the production credential/process/HTTP execution of those finite descriptors.
- tests/governance/release-publication-controller.test.ts drives exported `runPublication(input,deps)` against one stateful fake destination and the production logical transition table after the operation-host provenance and authorization fixtures supply finite authenticated RPC dependencies. At every coupled global-build/exact-ID plus predecessor/successor namespace boundary, tag push, draft creation, each of three uploads, publish, and the final postcondition it injects duplicate/rerun/job/artifact drift, namespace drift, timeout before/after application, lost response, nonaccepted status, malformed/mismatched success body, readback transport/auth/pagination/cap/malformed/identity/byte failure, worker death, host/system loss, cancellation, total diagnostic-log loss, and inner download/extraction failure. Every surviving-host failure asserts one attempt, host transport settlement, read-only-only reconciliation where allowed, nonzero exit, and zero later retry/repeat/delete/remote-cleanup/adoption/write; launcher-loss fixtures require poison/termination/zero/cleanup with no reconciliation, while host/kernel-loss fixtures assert only no surviving state-machine capability for a successor transition and make no operation-root/child/zeroing/log guarantee. Partial root/setup/clone/provenance/outer-cleanup and unowned-sentinel assertions live in tests/echo-context/operation-envelope.test.ts.
- The same controller suite covers canonical tag-body construction and byte/OID revalidation, immutable-OID porcelain proof, absence of mutable local tag refs, fixed serial write order, exact release/asset IDs, protocol-faithful response bodies, metadata/download byte binding, not-latest semantics, complete repository-wide namespace snapshots, and stable exact workflow/run/job/artifact authority throughout. It fully paginates all `head_sha=M` runs plus all tags/releases and every release's assets before and after every mutation, matches exact build identity with predecessor `E`/`N0`…`N4` and successor `N0`…`N5`, repeats build identity after final byte-level `N5`, rejects cap/loop/truncation/auth failure and any duplicate/rerun/replaced artifact/unrelated-version/foreign object, and proves drift before mutation yields zero write while a race after precheck permits at most the already in-flight mutation and zero successor write. Re-entry from tag-only, tag+draft, each partial-assets state, apparently complete, or any other nonempty repository namespace stops before a write. Delete/recreate with identical bytes but changed ID, foreign release placement, extra/missing objects, changed bytes/flags, or different tag object fails; success requires the original captured IDs, exact build tuple, and exact `N5` bytes.
- Secret-scan executable fixtures invoke the actual top-level script/result path with shallow/incomplete refs, unsupported/digest-mismatched binaries, scanner infrastructure failure, injected leak, and pipeline masking; each is nonzero. A known fake literal never appears in stdout/stderr. The bootstrap record's scanner-contract SHA-256 must equal the committed contract digest, and complete-ref scans cover every reachable repository ref.
- AC1 repository-creation fixtures additionally prove `GET /user` must authenticate the expected founder account, a negative name lookup — including HTTP 404 — is not durable absence or created-by-this-run authority, only the direct single-attempt `POST /user/repos` HTTP 201 response may establish the captured repository `id`/`node_id`, every later readback must return those same IDs, and any redirect, non-201 status, lost response, ID drift, nonempty branch list, or foreign ref yields zero initial push.
- tests/operator/source-parity.test.ts, with explicit source Git dir/SHA, recomputes the item-135 source inventory and eight-tool parity aggregate; absence of those inputs fails clearly rather than skipping.
- tests/migration/object-closure.test.ts and tests/migration/committed-source-only.test.ts prove the 190-file extraction baseline stays closed while successor HEAD may add reviewed files.
- Existing tests/api/context-only-roster.test.ts and tests/integration/context-service.test.ts keep the exact eight-tool and service semantics unchanged.
- `/Users/zhenye/Desktop/echo-context/tests/governance/fresh-clone-acceptance.test.ts` executes exported production `runFreshClone(argv, deps)` and the exact cleanup child seam through injected spawn/environment/executable/filesystem/temp adapters for both AC3 traces with no sibling repository or inherited `ECHO_*` state. Tests derive expected steps from a literal immutable oracle independent of the production trace—passing one production object as both actual and expected is forbidden. Static/import-time fixtures prove the verifier, cleanup module, and their import closures are side-effect-free; the first `runFreshClone` action authenticates `import.meta.url`, physical cwd, Node/npm/Git paths and versions before mutation/network/spawn. Shell fixtures require canonical absolute nonsymlink wrapper-to-sibling resolution and reject relative/symlink/wrong-cwd invocation plus a hostile same-name decoy. Every child uses the recorded absolute executable, `shell:false`, its own process group, clone-root cwd, exact environment/vector, and deadline; the source oracle includes exact cleanup row 15. Poisoned PATH/config/proxy/credential variables, wrong HOME mode, malformed versions, alternate scripts, wrong vectors/count/order, dirty/retargeted trees, and release-mode source builds fail before the affected step. Boundary-expiry fixtures make a prefinal child consume its effective deadline and settlement, then prove the sole cleanup plus final envelopes remain inside the 3,700-second entry-to-exit bound. Failures at source steps 16 and 17 prove completed cleanup is not invoked again; cleanup-step timeout/failure also proves one attempt with state `failed`, no retry, and fatal surviving root. Never-settling and TERM-resistant npm, lightweight Git metadata, fsck, scan, stream, and cleanup-child fixtures prove TERM/five-second KILL/direct-child reap/stream closure/PGID absence, no next step, parent `lstat(ENOENT)` after successful cleanup, primary-error preservation, and byte-identical non-owned sentinel; release mode creates/removes no `T`. Existing checksum/manifest/carrier/source/version fixtures remain.

## After Completion (Strategist Notes)

- Update no wiki page: repository source authority is internal shipped topology, not a new user-facing capability.
- Mark the item-135 follow-up's repository/default-path portion complete only for source publication; installation, vocabulary closure, auth, rehearsal, and live authority remain with items 137, 138, and the two authorized successors that will replace frozen item 139 after the 138 exit gate.
- Feed the exact AC6 tuple into item 137 without rebuilding or substituting a mutable branch.
- Require item 137's own reviewed workflow/governance scope before its successor prerelease; do not reuse item 136's empty-namespace-only local publication controller against populated state.
- Keep Project_echo as the context runtime/state authority and pinned migration source while item 138 lands the rehearsed substrate; only the authorized live-cutover-and-reversibility successor created after the 138 exit gate may complete the founder cutover.
