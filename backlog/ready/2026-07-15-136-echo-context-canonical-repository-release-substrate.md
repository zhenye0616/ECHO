---
id: 2026-07-15-136-echo-context-canonical-repository-release-substrate
title: "Canonical echo-context repository, self-testing clean clone, and tuple-sealed source artifact"
status: proposed
priority: HIGH
estimate: 1d
created: 2026-07-15
blocked_by:
  - 2026-07-13-135-local-echo-context-source-extraction
task_state_ref: 2026-07-15-136-echo-context-canonical-repository-release-substrate
requested_reviewers: ["codex", "codex-ops"]
ready_content_sha: a1570370f26201be2e2390dbc94407cce5ee2e65b76843ca6b787c8d20d7e5ca
files_to_modify:
  - /Users/zhenye/Desktop/echo-context/.github/workflows/** # DELETE cycle-one hosted workflows; hosted CI is deferred to item 140 and no workflow file may remain
  - /Users/zhenye/Desktop/echo-context/package.json # prerelease identity and self-contained operator scripts
  - /Users/zhenye/Desktop/echo-context/package-lock.json # exact dependency graph for the source artifact
  - /Users/zhenye/Desktop/echo-context/README.md # replace extraction-era no-remote/awaiting-review status
  - /Users/zhenye/Desktop/echo-context/LICENSE # exact reviewed MIT license for standalone distribution
  - /Users/zhenye/Desktop/echo-context/.gitignore # ignore build/artifact/cache/log/state outputs
  - /Users/zhenye/Desktop/echo-context/AGENTS.md # target-repo internal-asset and review/build-once instructions
  - /Users/zhenye/Desktop/echo-context/tsconfig.json # commit the reviewed standalone compiler configuration
  - /Users/zhenye/Desktop/echo-context/eslint.config.mjs # commit the reviewed standalone lint configuration
  - /Users/zhenye/Desktop/echo-context/vitest.*.config.ts # separate source-independent CI suite from operator replay
  - /Users/zhenye/Desktop/echo-context/provenance/** # NEW baseline/repository authority plus successor runtime-inventory.v2.json (item-135 v1 bytes immutable)
  - /Users/zhenye/Desktop/echo-context/schemas/** # NEW source-artifact schema (hosted-evidence and plan/authorization schemas are item-140 scope)
  - /Users/zhenye/Desktop/echo-context/tools/** # repository checks, deterministic source build, verification, secret scan, fresh-clone acceptance; DELETE release-publication and operation-host tooling
  - /Users/zhenye/Desktop/echo-context/tests/migration/** # bind extraction proof to the frozen baseline instead of evolving HEAD
  - /Users/zhenye/Desktop/echo-context/tests/operator/** # explicit Project_echo raw-object replay, excluded from the CI suite
  - /Users/zhenye/Desktop/echo-context/tests/governance/** # authority, artifact, bootstrap-gate, and no-hosted-surface tests; DELETE workflow-policy/landing/publication suites
  - /Users/zhenye/Desktop/echo-context/tests/governance/fresh-clone-acceptance.test.ts # exact wrapper/verifier/deadline/cleanup production-state-machine fixtures
  - raw/internal/migrations/2026-07-15-136-echo-context-repository-bootstrap.md # NEW redacted authority/landing/tuple record
  - raw/internal/migrations/2026-07-15-136-echo-context-implementation-review.md # NEW independent-reviewer-owned review record (builder/reviewer identities, reviewed head, verdict)
  - raw/internal/migrations/2026-07-15-136-echo-context-canonical-repository-release-substrate-target-main-landing-*-delegated-approval.md # COORDINATOR-ONLY immutable single-use landing authorization; never builder-authored
  - raw/internal/agent-runs/** # workflow-owned failure/completion run log
  - backlog/task-state/2026-07-15-136-echo-context-canonical-repository-release-substrate/** # workflow continuity pointers
  - backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # proposal and review revisions
  - backlog/ready/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # watcher-owned promotion target
  - backlog/claimed/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # workflow claim target
  - backlog/pending_review/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # workflow handoff target
  - backlog/complete/2026-07-15-136-echo-context-canonical-repository-release-substrate.md # coordinator-owned completion-evidence move after tuple verification
  - docs/BACKLOG.md # generated stage-derived index
spec_refs:
  - backlog/complete/2026-07-13-135-local-echo-context-source-extraction.md # exact reviewed extraction and later-authority boundary
  - raw/internal/migrations/2026-07-13-135-echo-context.md # accepted source/parity evidence and target identity
  - raw/internal/migrations/2026-07-13-135-echo-context-review.md # independent acceptance record
  - raw/internal/decisions/2026-07-15-echo-context-successor-repository-execution.md # founder-authorized cross-repo execution/review protocol
  - raw/internal/decisions/2026-07-16-echo-context-sequential-program-delegated-authority.md # scoped coordinator authority and immutable-operation record contract
  - raw/internal/decisions/2026-07-16-136-defer-github-hosted-gates.md # founder decision deferring all hosted gates/release to item 140
  - backlog/inbox/2026-07-16-140-echo-context-hosted-ci-and-release-governance.md # parked successor scope for hosted CI/protection/release
  - raw/internal/migrations/2026-07-15-136-echo-context-canonical-repository-release-substrate-repository-create-6a2e8326-delegated-approval.md # immutable once-only repository creation authorization/evidence
  - raw/internal/migrations/2026-07-15-136-echo-context-canonical-repository-release-substrate-baseline-main-push-472e7bf5-delegated-approval.md # immutable once-only baseline push authorization/evidence
  - backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.review.md # cycle-one implementation rejection and accepted fix contract
  - raw/internal/decisions/2026-07-11-commercial-focus-team-product-carve.md # context remains an internal asset
  - /Users/zhenye/Desktop/echo-context/provenance/target-only-policy.v1.json # extraction-era exact-HEAD/finality constraint
  - /Users/zhenye/Desktop/echo-context/provenance/runtime-inventory.v1.json # frozen item-135 executable/dependency closure; superseded for current validation by runtime-inventory.v2.json
  - /Users/zhenye/Desktop/echo-context/tests/migration/object-closure.test.ts # one-branch/no-remote extraction invariant to historicize
  - /Users/zhenye/Desktop/echo-context/tests/migration/parity-matrix.test.ts # source-dependent proof the CI suite cannot assume
  - /Users/zhenye/Desktop/echo-context/tests/migration/context-tool-evidence.test.ts # source replay seam and sealed eight-tool evidence
claimed_by: ""
claimed_at: ""
branch: ""
worktree: ""
head_sha: ""
pr_url: ""
agent_notes: ""
review_notes: |
  Implementation cycle 1 rejected — redo before merge.
  Reviewed Project_echo head: 25b833332bb22ec79700fcdf31b9c9f20eea79f5.
  Reviewed echo-context head: 145868a67a85dbb651faed457ee4001370c0fad0.
  Immutable review evidence: Project_echo commit 77c5e0c3f7f64c0bda1d99439d8d35eca54551ee,
  review-artifact blob 1cd16c1f31dab5c40ad76b70716846a09c98d845.
  The 2026-07-16 founder decision then deferred all hosted gates/release to item 140;
  cycle two implements this slimmed spec against the frozen cycle-one branch.
  The first independent cycle-two implementation review rejected exact target head
  358fb4da774287b6c55d287a46d53b5aff033e87. The same builder preserved both
  histories and repaired only the bounded findings. Independent re-review must bind
  Project_echo head 5b99d896e9103e0047c31d19fc574d7eea92abc5 and echo-context
  head 1a91750e5b9ce9db49e9c893f9974b318f12f38a; no acceptance is asserted here,
  and the reviewer-owned implementation-review record remains unwritten.
  The second independent cycle-two review rejected those exact heads with three AC3
  medium blockers: late child completion, repeated-signal cancellation, and an
  unimplementable hard-return promise for non-returning synchronous filesystem calls.
  The persistent coordinator returned the item to proposed for the smallest truthful
  scope correction and exact-SHA cross-vendor review; no failed evidence is waived.
target_repo: "/Users/zhenye/Desktop/echo-context"
target_remote: "https://github.com/zhenye0616/echo-context.git"
target_branch: "agent/echo-context-canonical-repository-release-substrate"
target_worktree: "/Users/zhenye/Desktop/echo-context--echo-context-canonical-repository-release-substrate"
target_head_sha: "1a91750e5b9ce9db49e9c893f9974b318f12f38a"
target_pr_url: "https://github.com/zhenye0616/echo-context/pull/1"
target_landed_sha: ""
project_landed_sha: ""
---

# Canonical echo-context repository, self-testing clean clone, and tuple-sealed source artifact

## Why this spec exists

Item 135 proved that the local echo-context extraction is source-independent and behaviorally faithful, but it deliberately ended with one migration branch, no remote, no install, and no runtime authority. Its extraction-finality tests also bind several checks to the then-current HEAD and two operator proofs default to Project_echo Git objects, so blindly publishing that directory would produce a repository that cannot safely evolve or validate itself from a clean clone.

This first gate makes echo-context/main the canonical source authority without confusing source publication with installation or live-state authority. It preserves the exact reviewed extraction and completed private-repository bootstrap as immutable history, gives clean clones a self-contained quality gate, and seals one explicitly non-installable source artifact for item 137. Project_echo remains the installed runtime and state authority throughout this item.

Per the 2026-07-16 founder decision (`raw/internal/decisions/2026-07-16-136-defer-github-hosted-gates.md`), this item makes **no GitHub Actions, hosted-CI, branch-protection, protected-environment, check-run-evidence, tag, GitHub Release, or release-asset claim**. Nothing in this item is client-facing; all hosted enforcement and hosted release publication is parked in item 140 (`backlog/inbox/`) pending the founder's org/plan-tier decision. Earlier hosted designs remain reviewable at spec SHAs `f130ba6f` (r14 GitHub-native) and `98250a76` (r18 delegated-authorization) as historical evidence.

This internal-asset work is permitted by the scope guard because unified machine context across Codex, Claude Code, and Cursor is required by the current ECHO development workflow and Team-product delivery. It is not a parallel commercial roadmap or a second product.

## Acceptance Criteria

### AC1 — Bind the completed exact baseline publication as immutable history

This bootstrap is already complete and is an immutable prerequisite, not a retryable builder step. The single repository-creation operation was authorized at Project_echo commit `e1ec8f74ae812e5df0983cd11a9a0827a9aefb67`; the single baseline-main publication was authorized at Project_echo commit `ece86049e8bfca567f03e7cdfbb8ee1d0b45fd05`. Authenticated readback established private repository `zhenye0616/echo-context`, numeric repository ID `1302541575`, node ID `R_kgDOTaM1Bw`, default branch `main`, and exact baseline commit `0cf7b006eba665c0bf55e82ff04da70f19f01ebb` / tree `70c5cf8352652b3c4c1dce68cd1a5e40d44e4b05`. The creation response was exactly one HTTP 201, the baseline push was exactly one create-only empty-expect leased porcelain `* [new branch]` result, and authenticated readback matched the captured repository identities and exact main SHA. No later implementation, reframe, or recovery path may repeat, adopt, delete, rewrite, or reinterpret either operation.

The builder implements `tools/repository-bootstrap-gate.mjs` as an injected, executable repository-bootstrap gate model that verifies the frozen evidence contract without touching GitHub: expected founder identity; negative name lookup treated as non-authoritative; exactly one create call whose only success is a direct HTTP 201 carrying the stable repository ID/node ID; authenticated owner/name/private readbacks; fully paginated empty branch results plus exhaustive zero-ref `git ls-remote`; the exact empty-expect `git push --porcelain --force-with-lease=refs/heads/main:` command; `* [new branch]` created-by-this-run proof; and exact post-push readback. Redirect, non-201, lost response, wrong identity/visibility, ID drift, incomplete pagination, any foreign ref, pre-existing main even at the identical OID, missing/malformed porcelain, ambiguous push, or failed post-readback each fail with no retry, adoption, cleanup, or later write. Tests consume recorded fixtures only; they never execute repository creation or baseline publication again.

The completed bootstrap full-ref secret scan remains bound to the committed `tools/secret-scan-contract.json` by SHA-256 `b186d99d61f774a6fbf6f16849c7aeb21618d90f79d3f7da4398d88d95925453`. The committed scanner must enforce the same exact gitleaks version/per-platform digest/invocation contract, remain non-shallow and complete-ref, redact secret values, and fail on leaks or scanner infrastructure errors. Personal-account hosting is the accepted current owner topology; AC4 states its exact enforcement limit rather than pretending the unavailable private-repository controls exist.

### AC2 — Convert extraction-finality into explicit successor provenance

Add provenance/extraction-baseline.v1.json, provenance/repository-authority.v1.json, their schemas, and tools/check-repository-authority.mjs. The baseline binds the accepted commit/tree, the 190-path mode/blob/content inventory, item-135 evidence hashes, and independent-review record. The authority record binds canonical repository/default branch, source_authority echo-context/main, artifact_authority versioned-source-artifact, extraction baseline ancestry, runtime_authority false, state_authority false, installed false, and maturity DEV.

Refactor tests/migration/object-closure.test.ts, tests/migration/committed-source-only.test.ts, and every target-only/extraction check that currently treats evolving HEAD as the final 190-file acceptance object. Those tests must inspect the frozen baseline through Git objects while successor tests require that baseline to be an ancestor of every releasable source SHA. Existing item-135 provenance bytes remain immutable historical proof; successor manifests reference rather than rewrite them. Because AC3 adds successor executables while provenance/runtime-inventory.v1.json is part of that frozen evidence, the regenerated runtime inventory is a successor file: provenance/runtime-inventory.v2.json, produced by a committed generator in tools/** over the successor tree's executable/dependency closure. runtime-inventory.v1.json remains byte-identical; `verify:inventory`, its generator, and every test that validates the current inventory bind to v2, and a governance test asserts both v1 byte identity (against its recorded hash) and v2 validity for the successor tree.

README.md must state the exact split: echo-context/main owns source after this item; Project_echo still owns the active daemon, live state, client endpoint, and rollback. It must not say the repository is installed, authoritative at runtime, qualified, or a commercial product.

Add LICENSE as a byte-identical reviewed copy of Project_echo's MIT license; .gitignore covering at least node_modules, dist/build, artifacts, caches, temporary files, logs, and mutable state; and a target AGENTS.md that locks the internal-asset boundary, main-branch review rule, exact source-tuple handoff/build-once rule, no-live-state default, and Project_echo coordination-record requirement. These are successor files covered by provenance and the source artifact, not retroactive changes to the frozen extraction baseline.

### AC3 — Make a sibling-free clean clone fully self-testing

Commit the exact standalone `tsconfig.json` and `eslint.config.mjs` configurations used in the accepted item-135 private-clone review. Add explicit `vitest.ci.config.ts` and `vitest.operator.config.ts`. `package.json` defines the exact scripts the acceptance and fresh-clone run share: `typecheck`, `lint`, `test:ci` (`vitest run --config vitest.ci.config.ts`), `test:operator` (`vitest run --config vitest.operator.config.ts`), `verify:inventory`, `verify:authority`, `verify:artifact` (`node tools/verify-source-artifact.mjs`), `build:artifact` (`node tools/build-source-artifact.mjs`), and `scan:secrets` (`tools/secret-scan.sh`). `build:artifact` accepts exactly `--source-sha <sha>` and `--out <dir>`, derives the version from the `package.json` version at that committed source SHA, writes the three AC5 files into the given gitignored output directory, and emits the built manifest's canonical-JSON SHA-256 as its exact final stdout line `manifest_hash=<hex>`. `verify:artifact` accepts exactly `--archive <path>`, `--checksum <path>`, `--manifest <path>`, and `--expected-manifest-hash <hex>`, recomputes the manifest's canonical-JSON SHA-256, and fails unless it equals the supplied expected value.

The scripted acceptance entrypoint is `tools/fresh-clone-acceptance.sh`, a committed shell bootstrap for the single Node 22 verifier/orchestrator `tools/fresh-clone-verifier.mjs`. It must be invoked by its canonical absolute, regular, nonsymlink path `<CLONE>/tools/fresh-clone-acceptance.sh` while physical `PWD` is exact canonical `<CLONE>`. Before starting Node, shell builtins only (`case`, parameter expansion, `test`, `cd -P`, `pwd -P`) require absolute nonsymlink `$0`, derive and physically canonicalize its exact `tools` directory, require that directory's parent equal physical `PWD`, and select only the regular nonsymlink sibling `<CLONE>/tools/fresh-clone-verifier.mjs`. Relative invocation, a symlink component, wrong cwd, missing/substituted sibling, or a decoy under caller cwd fails before any external process.

Every invocation then begins with the exact common prefix `--node-bin <NODE> --npm-bin <NPM> --git-bin <GIT> --git-version <GIT_VERSION> --sandbox-home <HOME>`, followed by the sole accepted source arguments below. `NODE`, `NPM`, `GIT`, and `HOME` must be canonical absolute paths supplied by the operator invocation record; `HOME` and `HOME/tmp` must already be caller-owned mode-0700 directories. The wrapper parses only that fixed prefix with shell builtins, rejects missing/duplicate/relative/newline-bearing values, and its only external process is `exec /usr/bin/env -i` of exact `NODE` and the authenticated absolute verifier sibling with the common prefix and remaining arguments unchanged. It never searches inherited `PATH` or runs a caller-selected shell command.

Before Node starts, the wrapper constructs the complete environment allowlist: exact `HOME`, `TMPDIR=HOME/tmp`, `PATH=<real NODE directory>:<real NPM directory>:<real GIT directory>:/usr/bin:/bin`, `LANG=C`, `LC_ALL=C`, `TZ=UTC`, `CI=1`, `GIT_CONFIG_NOSYSTEM=1`, `GIT_CONFIG_GLOBAL=/dev/null`, `GIT_TERMINAL_PROMPT=0`, `NPM_CONFIG_USERCONFIG=/dev/null`, and `NPM_CONFIG_CACHE=HOME/.npm-cache`; no other inherited variable crosses. The verifier rebuilds that same allowlist for every child, with only step-specific nonsecret additions. Thus `NODE_OPTIONS`, `NODE_PATH`, every unapproved `GIT_*` redirect such as `GIT_DIR`, `GIT_WORK_TREE`, `GIT_OBJECT_DIRECTORY`, `GIT_ALTERNATE_OBJECT_DIRECTORIES`, and `GIT_CONFIG_*`, every inherited `npm_config_*`/`NPM_CONFIG_*`, proxy variables, credential variables, and all `ECHO_*` state are absent.

The verifier module and its complete static-import closure must be side-effect-free during module evaluation. The aggregate clock starts at verifier entry, and the first action inside exported `runFreshClone` requires `realpath(process.execPath) == realpath(NODE)`, `process.version == v22.22.1`, canonical executable regular-file `NPM` and `GIT`, `realpath(fileURLToPath(import.meta.url))` equal the exact absolute verifier sibling supplied by the wrapper, and physical `cwd` equal that verifier's two-level parent `<CLONE>` before any filesystem mutation, network access, or child spawn. Static analysis and import-time sentinel fixtures enforce the side-effect-free closure. The verifier then runs the common two-child prelude exactly once under the controlled environment: absolute `NPM` with `["--version"]` must return exactly `10.9.4`, then absolute `GIT` with `["--version"]` must return exactly `git version <GIT_VERSION>`; CR, NUL, extra lines/bytes, another version, or another executable fails before install. The verifier alone parses the source arguments, validates identity, records its temporary directory, and spawns children from the clone root. Every child spawn is the recorded absolute executable plus argv array with `shell:false` and its own process group; a shell string, `shell:true`, unlisted executable/script, or any missing, extra, reordered, or duplicated argv element/invocation fails before spawn. There is no generic `npm run <script>` allowlist entry.

After validating the common prefix and source arguments, the verifier stores the caller's full 40-hex `--source-sha` as immutable expected SHA `S` for the whole run. Each cleanliness boundary is the same two-child pair, in this order: absolute `GIT` with argv `["status","--porcelain=v1","--untracked-files=all"]` must exit zero with empty stdout, immediately followed by absolute `GIT` with argv `["rev-parse","HEAD"]`, which must exit zero and whose normalized full-SHA stdout must equal `S`. Putting the HEAD assertion last closes a persistent clean retarget between the two boundary commands; a retarget after a nonfinal pair is caught at the next pair, and the final pair ends with the HEAD assertion. The pair runs exactly three times: before `npm ci`, immediately after `npm ci`, and at the end after every source check and cleanup/absence assertion. Thus status count is three and `rev-parse` count is three; neither is once-only.

After the exact common prefix, the verifier accepts exactly `--mode=source --source-sha <S>`; no other mode exists in item 136. Its sole Node-internal setup step occurs after child step 10 and before child step 11: create the single gitignored temporary directory inside the clone and record its exact canonical path as `T`. Let `A`, `C`, and `M` be the archive, checksum, and manifest paths deterministically derived beneath `T`; and let `H` be the validated `manifest_hash=<hex>` carrier parsed from step 11. Its complete trace, following the common prelude, is:

| Ordinal | Executable | Exact argv |
|---|---|---|
| 1 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 2 | `<GIT>` | `["rev-parse","HEAD"]` |
| 3 | `<NPM>` | `["ci"]` |
| 4 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 5 | `<GIT>` | `["rev-parse","HEAD"]` |
| 6 | `<NPM>` | `["run","typecheck"]` |
| 7 | `<NPM>` | `["run","lint"]` |
| 8 | `<NPM>` | `["run","test:ci"]` |
| 9 | `<NPM>` | `["run","verify:inventory"]` |
| 10 | `<NPM>` | `["run","verify:authority"]` |
| 11 | `<NPM>` | `["run","build:artifact","--","--source-sha",S,"--out",T]` |
| 12 | `<NPM>` | `["run","verify:artifact","--","--archive",A,"--checksum",C,"--manifest",M,"--expected-manifest-hash",H]` |
| 13 | `<GIT>` | `["fsck","--full"]` |
| 14 | `<NPM>` | `["run","scan:secrets"]` |
| 15 | `<NODE>` | `["<CLONE>/tools/fresh-clone-cleanup.mjs","--owned-root",T]` |
| 16 | `<GIT>` | `["status","--porcelain=v1","--untracked-files=all"]` |
| 17 | `<GIT>` | `["rev-parse","HEAD"]` |

On the success path, child step 15 is the verifier-owned cleanup transition. Creation of `T` also creates one single-assignment cleanup state `not_started`; the sole transition is `not_started -> running -> completed|failed`, and no state may return to `not_started`. The surrounding `finally` invokes that transition if and only if `T` exists and state remains `not_started`; it never starts a second helper after a running, completed, or failed attempt. The transition's first synchronous, non-fallible state action is the compare-and-set `not_started -> running`, before active-child settlement, stream closure, helper authentication, filesystem inspection, or spawn. It then terminates/reaps any active child group and closes its streams, authenticates the canonical absolute regular nonsymlink side-effect-free sibling `tools/fresh-clone-cleanup.mjs`, and starts exact `<NODE>` with argv `["<CLONE>/tools/fresh-clone-cleanup.mjs","--owned-root",T]` as a direct process-group leader. Any later error—including pre-helper child settlement or helper authentication—sets `failed` and is terminal, so `finally` cannot start a second transition or helper. The helper accepts only the verifier-recorded canonical nonsymlink directory `T` beneath the clone's single gitignored acceptance-temp parent and recursively removes only that root. The verifier marks `completed` only after direct-child reap, stream closure, PGID absence, and parent-side `lstat(T)==ENOENT`; timeout/error, surviving PGID, any remaining entry at `T`, or any other `lstat` result marks `failed` and is terminal without retry. A prior primary error remains primary with sanitized cleanup failure attached. Only `completed` may proceed to child steps 16–17; their failure does not respawn cleanup because the root is already absent. The verifier never derives a cleanup target from mutable child output and never removes a user/repository path or any path other than recorded `T`.

Each common-prelude row and every table row other than the repeated boundary commands runs exactly once. Build/verify counts are 1/1. `test:operator` remains independently runnable but is explicitly rejected by the verifier. Gitignored `node_modules` and `T` do not themselves dirty the status probe, so cleanup is proven by the separate exact-path `ENOENT` assertion. The verifier consumes `H` only from the exact build carrier. Unknown, missing, duplicate, or non-source arguments fail with usage text rather than skipping a check. Any staged, unstaged, or nonignored-untracked entry, persistent clean retarget to a different HEAD between boundaries, install/check-time mutation, wrong executable/version/environment/count/order/vector, or failed check stops acceptance. These boundary probes target persistent accidental mutation; the spec makes no claim against adversarial transient retarget-and-restore or mutate-and-restore between probes.

The AC3 deadline constants are exact and not caller-selectable: 30 seconds for each version/status/rev-parse child; 600 seconds for `npm ci`; 900 seconds each for `test:ci` and `scan:secrets`; 300 seconds for each remaining typecheck/lint/inventory/authority/build/artifact-verification/fsck child; 30 seconds for the cleanup child plus parent absence readback; and a 3700-second orchestration deadline from entry into `runFreshClone` before identity checks through final HEAD read or failed cleanup. The verifier fixes `aggregate_end=entry+3700s` and `prefinal_cutoff=aggregate_end-120s`. Every step through 14 and every earlier failure/cancellation path must either complete on time or trigger cancellation no later than `prefinal_cutoff`; each effective execution deadline is the earlier of its class limit and that boundary. The final 120 seconds are reserved and nonborrowable: cleanup step 15, status step 16, and HEAD step 17 each receive consecutive 40-second orchestration envelopes (30 seconds for execution/readback plus ten seconds to initiate termination/reap/stream settlement). On a prefinal failure after `T` creation, the first reserved envelope starts the sole cleanup transition and the remaining reserve is guard. Timeout or cancellation closes stdin, sends TERM to the child PGID, waits exactly five seconds, sends KILL if the PGID remains, then waits for direct-child exit plus stdout/stderr closure and verifies no process remains addressable in that PGID. SIGINT/SIGTERM handlers remain installed, idempotent, and non-defaulting until terminal cleanup/settlement and explicit disposal; repeated same or different signals only reaffirm the first cancellation. No timeout/cancellation failure may be reported and no next step may start before terminal settlement; completion after its execution deadline is late and fails even if it arrives inside the settlement reserve.

The 3700-second value is an orchestration deadline under responsive kernel calls, not a hard verifier-return or process-exit bound. The managed-spawn state machine has exactly two immutable terminal shapes. A **pre-spawn failure** is a synchronous spawn throw or an asynchronous spawn error for which no positive integer PID was ever assigned: before reporting failure it records the error, proves that no PID/PGID exists, and observes closure of every stdout/stderr stream that was materialized (an uncreated stream is explicitly absent); it never waits for a nonexistent exit event or signals a nonexistent group. It uses the same execution-plus-settlement orchestration envelope, destroys/closes any materialized streams when that envelope expires, and still withholds failure if close has not yet been observed. A **spawned child** has a positive integer PID: every outcome—success, nonzero exit, signal, spawn/stream error after PID assignment, timeout, or cancellation—requires direct-child exit, stdout/stderr closure, and PGID absence before the verifier may report it or advance. If an otherwise ordinary completion leaves its PGID alive, the verifier applies the same idempotent TERM/five-second/KILL ceremony and remains pending until absence is proved; this is required even before `T` exists, when no cleanup transition would otherwise run. Neither terminal shape permits a later step before its proof completes.

Verifier-local synchronous filesystem calls are checked fail-closed against the monotonic deadline immediately before and after they return. Node 22 supplies no abortable `realpath`/`lstat`/`access`/`mkdir`/`mkdtemp`/`chmod`/`rm` primitive that can prove both bounded return and no later mutation: a call that never returns, or a cleanup PGID that cannot be proved absent, may keep the invocation pending past `aggregate_end`. It is never treated as success, followed by another step, retried, or abandoned while mutation remains possible. Item 136 adds no supervisor, worker, watchdog, controller, or extra production child to change that boundary.

`tools/fresh-clone-verifier.mjs` exports one production `runFreshClone(argv, deps)` state machine and keeps the shell as the fixed scrub-and-exec bootstrap above; `tools/fresh-clone-cleanup.mjs` is its sole production cleanup child. Spawn, clock, cancellation, environment, executable-realpath, filesystem, temporary-directory, and cleanup-child operations are injected so executable tests exercise the real transition logic; production adapters remain the only operational path. The expected child plan in tests is a literal independent oracle derived from the common prelude and AC3 tables, never the production trace passed back as its own expected value. Cleanup errors, surviving cleanup child/PGID/owned temporary paths, carrier tampering, poisoned inherited state, and non-owned-sentinel mutation fail without being masked by the `finally` path.

Move only the raw Project_echo Git-object recomputation portions of `tests/migration/parity-matrix.test.ts` and `tests/migration/context-tool-evidence.test.ts` into `tests/operator/source-parity.test.ts`; the operator suite requires an explicit `ECHO_SOURCE_GIT_DIR` and pinned source SHA and never supplies a founder-path default.

A fresh no-local clone in a temporary HOME, with Project_echo and sibling repositories absent and all ECHO_* variables cleared, must pass npm ci from the exact lock, typecheck, lint, the complete source-independent CI suite, runtime-inventory validation (against provenance/runtime-inventory.v2.json), repository-authority validation, context-tool roster/parity fixtures, and service parity. There may be no unexpected skip caused by missing Project_echo. The acceptance must never read /Users/zhenye/Desktop siblings, ~/.echo, ~/.echo-context, credentials, or a live database.

The operator replay remains independently runnable against the pinned item-135 source objects and must still reproduce the accepted source inventory and parity aggregate. It is an independent operator check, not a clean-clone dependency.

### AC4 — Land reviewed successor work on echo-context main through independent review and a recorded authorization (no hosted gates)

Hosted CI, server-enforced branch protection, protected environments, check-run evidence, and every GitHub Actions workflow are out of this item's scope, deferred to item 140. Cycle two **deletes** the cycle-one `.github/workflows/**` files from the feature branch; the landed tree must contain no workflow file, and the no-hosted-surface governance test proves it. The current personal/private Free repository cannot provide server-enforced branch protection, administrator enforcement, required PR approval, or a private protected-environment reviewer, and the sole GitHub identity cannot independently approve its own PR. This specification does not pretend otherwise and does not claim to prevent a malicious or compromised sole repository owner from bypassing policy, force-updating, deleting, or publishing manually. The enforceable boundary is process-level: independent review recorded in Project_echo, a committed/pushed/read-back authorization record before the single external write, exact Git object identity, create/update compare-and-swap leases, authenticated readback, and fail-closed refusal on any drift or ambiguity. Private visibility remains mandatory; making the repository public or silently pretending unavailable controls exist is forbidden.

**Quality gate (local, dual-run).** All successor changes remain on the frozen cycle-one echo-context feature branch; the cycle-two builder repairs that branch without rebasing or rewriting cycle-one history. Let `H` be the exact cycle-two echo-context feature head. The builder runs the AC3 source-mode acceptance at `H` in a fresh no-local clone under a temporary HOME on the founder Mac and records the invocation, toolchain paths/versions, and result in the run log. The independent reviewer then **independently re-runs** the same AC3 source-mode acceptance at `H` in their own fresh clone; a review without that re-run is invalid.

**Independent implementation review.** After the builder completes, a different fresh agent reviews the exact head `H` against this spec and writes `raw/internal/migrations/2026-07-15-136-echo-context-implementation-review.md`, committed and pushed to Project_echo main by the reviewer before any landing step. The record binds: builder actor ID and run ID; reviewer actor ID and run ID; exact `H` and `H^{tree}`; both AC3 acceptance results (builder-run and reviewer-run, with toolchain identities); finding dispositions; and verdict `merge_ready`. The landing step independently recomputes `builder_actor_id != reviewer_actor_id` and `builder_run_id != reviewer_run_id` from the record — never a stored boolean. Equality, a missing identity, a stale or cycle-one head, a missing reviewer acceptance run, or any verdict other than `merge_ready` blocks landing.

**Construct the exact merge object before approval.** In a fresh config-isolated detached clone of echo-context (system/global config disabled, hooks disabled through `core.hooksPath=/dev/null`, literal canonical URL `https://github.com/zhenye0616/echo-context.git`, and the founder's existing GitHub credential used exactly as in the landed bootstrap operations), authenticated `GET /user` must return exact login `zhenye0616`; `GET /repos/zhenye0616/echo-context` must return repository ID `1302541575`, node ID `R_kgDOTaM1Bw`, `full_name=zhenye0616/echo-context`, `private=true`, and `default_branch=main`; and authenticated `GET /repos/zhenye0616/echo-context/git/ref/heads/main` must return exact `B`, where `B` is baseline `0cf7b006eba665c0bf55e82ff04da70f19f01ebb`. The coordinator fetches the feature ref, requires its tip to be exact reviewed `H`, requires `B` to be an ancestor of `H`, checks clean status and `git fsck --full`, and checks out `B`. It chooses one ISO-8601 `merge_timestamp`, records it before spawn, and creates the no-fast-forward merge with only the additional environment `GIT_AUTHOR_NAME=ECHO Coordinator`, `GIT_AUTHOR_EMAIL=echo-coordinator@users.noreply.github.com`, `GIT_COMMITTER_NAME=ECHO Coordinator`, `GIT_COMMITTER_EMAIL=echo-coordinator@users.noreply.github.com`, and both `GIT_AUTHOR_DATE` and `GIT_COMMITTER_DATE` equal to that timestamp; semantic argv is `-c core.hooksPath=/dev/null -c commit.gpgSign=false merge --no-ff --no-edit -m "Merge item 136 echo-context canonical source" <H>`. Let the resulting local object be `M`. Before authorization, require ordered parents exactly `[B,H]`, `M^{tree}=H^{tree}`, the exact fixed message and metadata, no `gpgsig` header, and clean status. Literal `M` authenticates the complete commit bytes; no later reconstruction is permitted.

**Landing authorization record.** Only after `M` exists and passes those checks, the coordinator creates the immutable single-use record `raw/internal/migrations/2026-07-15-136-echo-context-canonical-repository-release-substrate-target-main-landing-<approval-id>-delegated-approval.md`, following the exact pattern of the two landed bootstrap approvals. This path is coordinator-only despite being listed in `files_to_modify`; the builder must not create it. The record binds: the delegation decision path and landed SHA; the deferral decision path and landed SHA; item ID, exact reviewed spec SHA, and ready seal; the implementation-review record path and its Project_echo commit; exact `B`, `H`, `H^{tree}`, literal `M`, `M^{tree}`, ordered parents `[B,H]`, fixed merge message, merge timestamp, exact nonsecret author/committer environment, merge argv, and sanitized `git cat-file commit M` evidence; the fully literal push argv with `B` and `M` substituted; coordinator identity, approval timestamp, unique approval ID; and the fail-closed ambiguity policy (`retry:false`, `adopt:false`, `rewrite:false`). The record is committed and pushed to Project_echo main and read back from `origin/main` before the target write. Reuse, omission, stale identity, or a value change invalidates it.

**The single target write.** After authorization readback, the same serialized coordinator session repeats the three authenticated reads above and requires the same login/repository fields and `main=B`; it also revalidates local `H`, literal `M`, the bound author/committer metadata, ordered parents/tree/message/absence of signature, clean status, and the approval bytes. It then invokes exactly one push with semantic argv `-c http.followRedirects=false push --porcelain --no-verify --no-follow-tags --force-with-lease=refs/heads/main:<B> https://github.com/zhenye0616/echo-context.git <M>:refs/heads/main`. Exactly one structurally parsed fast-forward porcelain row for `refs/heads/main` succeeds, and the sanitized porcelain proof is retained for the migration record. Authenticated post-readback repeats `GET /user`, `GET /repos/zhenye0616/echo-context`, and `GET /repos/zhenye0616/echo-context/git/ref/heads/main`, requiring the same login/repository fields and now `main=M`. Any identity drift, hook execution, redirect, other ref, missing/additional/malformed row, non-fast-forward, lost or ambiguous response, or failed readback permits read-only inspection only — no retry, second push, force-update, adoption, rewrite, or remote cleanup. The founder-locked topology has one persistent coordinator and one active covered item; that process boundary does not claim to detect an adversarial concurrent actor, and item 136 adds no cross-machine attempt ledger, runner, credential helper, or operation host.

### AC5 — Build one deterministic, explicitly non-installable source artifact

Set the first successor version to 0.1.0-dev.136.1 and add schemas/source-artifact-manifest.v1.schema.json, tools/build-source-artifact.mjs, tools/verify-source-artifact.mjs, and tests/governance/source-artifact.test.ts. The builder consumes committed Git objects at a reviewed source SHA, never mutable working-tree bytes, and emits exactly three sidecar files: echo-context-<version>-source.tgz, echo-context-<version>-source.tgz.sha256 (coreutils sha256sum format), and echo-context-<version>-source.manifest.json. The manifest is a sidecar and is never a member of the archive it describes — no self-referential size/hash. The manifest serializes as canonical JSON (UTF-8, sorted keys, LF, single trailing newline). The archive root is the single directory echo-context-<version>/ containing exactly the Git-tracked paths at the source SHA in sorted path order, modes taken from Git (0644/0755), uid/gid 0 with numeric owners, member mtimes fixed to the source commit's committer timestamp, and gzip written without name/timestamp metadata.

The manifest binds version, source commit/tree, extraction baseline, lock hash, exact path/mode/blob/content inventory, tar member order/modes/timestamps, source-archive size and source-archive SHA-256, and the literal classifications artifact_kind source, installable false, runtime_authority false, state_authority false, maturity DEV. Hash names are distinct and used consistently everywhere this spec, its records, and its tests mention them: the **source-archive SHA-256** is the digest of echo-context-<version>-source.tgz and is the value the .sha256 sidecar binds; the **manifest hash** is the SHA-256 of the manifest's canonical-JSON bytes; the **lock hash** is the SHA-256 of the package-lock.json blob at the source SHA. No unqualified "artifact SHA-256" appears in any record, summary, or test. The archive contains source and verification material only; it has no compiled service, LaunchAgent, client config, credential, live state, or install script.

Two builds from the same Git objects and pinned toolchain must be byte-identical — a determinism property proven in tests and re-proven at the AC6 seal. Dirty-byte substitution, wrong commit/tree/version, missing/extra/reordered member, unsafe archive path, mode/timestamp drift, checksum mismatch, baseline non-ancestry, or changing any authority/installability flag fails verification.

### AC6 — Seal the source artifact by recorded content tuple (no hosted release)

No Git tag, GitHub Release, release asset, or any other echo-context mutation beyond the AC4 main push is created by this item; hosted release publication is item-140 scope. The seal is the **recorded content tuple**, and determinism (AC5) makes rebuild-from-`M` the distribution mechanism: no artifact bytes are hosted, committed, or handed over as files.

After the AC4 readback proves `main=M`, the coordinator performs the seal in a fresh no-local clone at exact `M` under a temporary HOME: run `build:artifact --source-sha <M> --out <dir1>` and independently `build:artifact --source-sha <M> --out <dir2>`, require the three files byte-identical across the two builds, and run `verify:artifact` against each build's emitted `manifest_hash` carrier. The authenticated manifests provide the six-field content tuple — source SHA `M`, source tree, version, source-archive SHA-256, lock hash, manifest hash — which must be identical across both builds and is recorded with the exact commands and toolchain identities. This dual build plus verification is the complete item-136 seal; AC3 does not expose or compare a second tuple, and item 137 is the independent rebuild consumer. Any byte difference, hash mismatch, malformed manifest, or verification failure blocks completion; `M` is never mutated and no automatic retry, adoption, or second target push is allowed.

If the seal fails after `main=M`, the coordinator must first append a sanitized failure entry to the existing item run log under `raw/internal/agent-runs/**`, commit and push it to Project_echo, and read it back before any later disposition. The entry binds `M`, the failed phase, exact command and toolchain identities, exit/timeout disposition or mismatched field, and terminal `no automatic retry or adoption`; no new failure schema, controller, or hosted workflow is introduced.

Only after the seal passes does the coordinator generate `raw/internal/migrations/2026-07-15-136-echo-context-repository-bootstrap.md`. It records the canonical URL and repository IDs; baseline SHA/tree; implementation-review record path and Project_echo commit; landing approval ID/record path and its Project_echo commit; exact `M`/tree and the sanitized porcelain proof; the complete six-field content tuple; the dual-build determinism and verification evidence; builder/reviewer identities; `source_authority: echo-context/main`; `artifact_authority: versioned-source-artifact`; `runtime_authority:false`; `state_authority:false`; and `installed:false` — without secrets. Completed-item frontmatter sets `target_landed_sha=M`; `project_landed_sha` is the Project_echo commit SHA that landed the migration record, written into the separate completion-move commit (a commit never names itself). The completion move (item to `backlog/complete/`, regenerated `docs/BACKLOG.md`, task-state finalization, completion run log) is one Project_echo commit after the migration record lands.

The handoff to item 137 is: exact `M`, the six-field content tuple, and the migration-record path plus its Project_echo commit. Item 137 consumes exact `M`, rebuilds via `build:artifact`, and verifies against the tuple before any install work; item 136's seal is not automatic authorization for any later release or version. At completion, echo-context owns source and source-artifact authority only; Project_echo remains installed runtime and live-state authority, and no machine client points at the new repository or artifact.

## Out of Scope (Don't Drift)

- No GitHub Actions workflow, hosted CI, branch protection, ruleset, protected environment, check-run evidence, tag, GitHub Release, or release asset — all item-140 scope (`backlog/inbox/`).
- No operation-host/launcher/envelope/askpass/authority-publisher machinery; the r15–r18 designs are historical evidence, not scope.
- No multi-coordinator attempt-consumption ledger, hardened landing runner, pinned credential helper, or replacement execution controller; the locked single-coordinator topology plus the exact object/lease/approval/readback gate is the item-136 boundary.
- No external supervisor, Worker-thread watchdog, or additional cleanup/process-control child solely to force a hard return from a non-returning kernel filesystem call; AC3 instead fails closed without success, advance, retry, or abandonment.
- No LaunchAgent, machine-wide/per-user install, service composition root, MCP client registration, port, bearer-token, or loopback-exposure decision.
- No live database, capture checkpoint, onboarding state, project config, credential, or client-config read/copy/migration.
- No active daemon switch, dual-run, rollback ceremony, Project_echo context freeze, or runtime/state authority transfer.
- No npm/public package publication, public repository visibility, auto-update channel, source signing program, or generic release framework.
- No context-tool, capture, storage, retrieval, schema, clustering, enrichment, or embedding behavior change.
- No Project_echo deletion/rename/deprecation and no echo-brain or echo-loop repository/install work.
- No Team-product maturity advancement; this internal source seal remains DEV.

## Risks

- Extraction evidence could be weakened by rebasing it onto evolving HEAD. The frozen-baseline plus ancestry contract is the fallback; existing evidence bytes are never rewritten.
- With hosted CI deferred, a regression could land on echo-context main if the local acceptance is skipped. Mitigation: the AC3 acceptance is mandatory twice (builder and reviewer, independently recorded) before any landing, and item 140 restores server-side enforcement later.
- Hosted-runner cross-platform coverage (Ubuntu) is deferred with the workflows; until item 140, the acceptance proves macOS only. Item 137 separately owns the exact Darwin runtime closure, which is the only platform anything currently consumes.
- The acceptance could appear green only because the founder machine supplies Project_echo or state. AC3 removes implicit paths and proves a clean sibling-free clone under a temporary HOME.
- A local filesystem or kernel call can become non-responsive, so the 3,700-second value cannot be both a literal process-exit bound and a no-late-mutation proof inside the fixed single-process verifier. AC3 keeps every numeric orchestration deadline and fails closed after each returning call, while a non-returning call leaves the invocation pending and never accepted.
- Publishing history could expose a secret. The AC1-bound scanner remains in the acceptance trace and blocks on leaks without printing values.
- The current private personal Free topology cannot stop a malicious or compromised sole owner from bypassing policy. AC4 makes that limitation explicit and fails closed on drift and ambiguity; server enforcement is item-140 scope.
- A source archive could be mistaken for an installable runtime. Its filename and manifest identify a source artifact with `installable:false`, `runtime_authority:false`, and `state_authority:false`; the authority and migration records separately bind `source_authority: echo-context/main`, `artifact_authority: versioned-source-artifact`, and `installed:false`.
- Repository creation and baseline publication are already-completed immutable history and may never repeat. The target-main landing is mechanically single-use through its exact `B` lease and single-use approval record.

## Tests

- tests/governance/repository-authority.test.ts verifies the exact frozen baseline, successor ancestry, canonical authority fields, immutable item-135 evidence hashes (including byte identity of provenance/runtime-inventory.v1.json against its recorded hash), validity of the successor provenance/runtime-inventory.v2.json for the successor tree, that `verify:inventory` and its generator bind to v2, and negative wrong-remote/wrong-baseline fixtures.
- tests/governance/source-artifact.test.ts builds twice, compares bytes, validates the manifest/inventory, and rejects dirty, missing, extra, reordered, unsafe-path, checksum, version, tree, and authority-flag mutations.
- tests/governance/repository-bootstrap-gate.test.ts executes the production injected AC1 model over recorded fixtures without live writes. It covers expected `GET /user`, non-authoritative negative lookup, single POST-create direct HTTP 201, stable repository ID/node ID, fully paginated empty branch list, exhaustive zero-ref `ls-remote`, exact create-only main-push argv/porcelain, and post-readback. Redirect/non-201/lost/malformed response, wrong identity/visibility, ID drift, later-page foreign ref, identical pre-existing main, missing `--porcelain`, up-to-date/multirow output, ambiguous push, and failed post-readback each assert zero later write, retry, adoption, or cleanup.
- tests/governance/no-hosted-surface.test.ts proves the successor tree contains no `.github/workflows/**` entry, no release-publication or operation-host tooling path, and no schema for hosted evidence or plan/authorization records; it exists to make the item-140 deferral mechanically visible and to block silent reintroduction before that item's review.
- Secret-scan executable fixtures invoke the actual top-level script/result path with shallow/incomplete refs, unsupported/digest-mismatched binaries, scanner infrastructure failure, injected leak, and pipeline masking; each is nonzero. A known fake literal never appears in stdout/stderr. The bootstrap record's scanner-contract SHA-256 must equal the committed contract digest, and complete-ref scans cover every reachable repository ref.
- tests/operator/source-parity.test.ts, with explicit source Git dir/SHA, recomputes the item-135 source inventory and eight-tool parity aggregate; absence of those inputs fails clearly rather than skipping.
- tests/migration/object-closure.test.ts and tests/migration/committed-source-only.test.ts prove the 190-file extraction baseline stays closed while successor HEAD may add reviewed files.
- Existing tests/api/context-only-roster.test.ts and tests/integration/context-service.test.ts keep the exact eight-tool and service semantics unchanged.
- `/Users/zhenye/Desktop/echo-context/tests/governance/fresh-clone-acceptance.test.ts` executes production `runFreshClone(argv,deps)` and cleanup through injected adapters for the single AC3 source trace without siblings or inherited `ECHO_*`. A literal independent oracle covers exact steps, side-effect-free imports, wrapper/path/executable authentication, scrubbed environment, process groups, deadlines, cleanliness, and the source argument vector; any other mode is rejected. Cleanup fixtures assert the first state action is `not_started→running` before fallible active-child settlement or helper authentication; failures at each pre-helper point set `failed`, spawn no helper where appropriate, and cannot cause a second transition/spawn in `finally`. Boundary-expiry, exact-on-time versus late completion, repeated SIGINT/SIGTERM, synchronous and asynchronous pre-spawn errors with no PID/PGID, a pre-`T` nonzero direct exit with a surviving descendant, steps 16/17, cleanup timeout/failure, never-settling/TERM-resistant children/streams, PGID absence, exact `ENOENT`, primary-error preservation, and non-owned sentinel tests prove the 3,700-second orchestration deadline plus fail-closed no-success/no-advance behavior under controllable adapters. Pre-spawn fixtures prove no nonexistent exit wait or group signal occurs and no failure is reported before every materialized stream closes; the pre-`T` spawned-child fixture proves the verifier applies the same TERM/KILL ceremony and withholds the ordinary failure until the descendant PGID is absent. Injected never-Promise fixtures do not claim kernel-request cancellation or a hard verifier-return/process-exit bound; synchronous filesystem fixtures instead prove before/after monotonic checks reject a late return.
- AC4's landing protocol is procedural coordinator work, not production code: its contract is enforced by the single-use approval record, the recorded exact commands, the retained porcelain proof, and the migration record — reviewed as evidence, not executed as fixtures. The AC1 bootstrap-gate suite already models the shared leased-porcelain-push grammar.

## After Completion (Strategist Notes)

- Update no wiki page: repository source authority is internal shipped topology, not a new user-facing capability.
- Mark the item-135 follow-up's repository/default-path portion complete only for source publication; installation, vocabulary closure, auth, rehearsal, and live authority remain with items 137, 138, and the two authorized successors that will replace frozen item 139 after the 138 exit gate.
- Feed exact `M` plus the six-field content tuple into item 137; 137 rebuilds at `M` and verifies against the tuple — never a mutable branch or a copied artifact file.
- Item 140 (`backlog/inbox/`) owns hosted CI, server-enforced protection, and GitHub Release publication; its activation is a founder org/plan-tier decision.
- Keep Project_echo as the context runtime/state authority and pinned migration source while item 138 lands the rehearsed substrate; only the authorized live-cutover-and-reversibility successor created after the 138 exit gate may complete the founder cutover.
