---
item_id: 2026-05-17-061-doc-leanout
verdict: merge with founder fixups
reviewed_at: 2026-05-18T07:30:00Z
test_counts: { passed: 0, failed: 0, note: "docs-only diff; no executable tests" }
---

## Verdict

`merge with founder fixups`. All seven ACs are functionally satisfied: HTML twins purged, journal renamed + sharded, skip-rule added, archive directory + canonical stub created, `tools/sync-skills.sh --check` clean, review-queue-setup rewritten, STATUS.md deleted, TZ-bug followup landed. Drift is minimal and largely defensible. The single substantive issue is that the spec's literal AC2 DoD grep (`grep -rn "mcp-interactions-journal\.md" --include="*.md"`) still finds 131 bare references outside the archive — historical pointers in completed-item bodies, `_followups.md`, and tool READMEs. The agent flagged this as the "caveat" in `agent_notes`. Whether to treat as Met or Partial is a founder call; the lower-risk interpretation is "those refs now correctly point at the archive file (renamed, history preserved), no bulk path-rewrite needed in this spec."

## Pre-merge fixups

- [ ] Reconcile the **live-checkout's uncommitted `raw/internal/dogfooding/mcp-interactions-journal.md` modification** before `merge --no-ff` — the file is renamed on the branch, so the working-tree edit will collide. Either commit the new entry into the May shard on this branch before merge, or stash + reapply post-merge as a May-shard append. Do not let auto-resolve win.
- [ ] Founder ack on **`AGENTS.md` parallel edit** — not enumerated in `files_to_modify` but the agent mirrored AC2 (canonical journal path → monthly shard) and AC3 (zero-MCP-call skip-rule) into AGENTS.md. Defensible (keeps cross-AI-binding peer in sync; the alternative silently breaks Codex journaling), but counts as scope drift. Accept as-is, or split to a separate commit before merge.
- [ ] Founder ack on **three bonus `_followups.md` bullets at lines 831–833** (`atom_too_large_for_wire`, `search_memories` paraphrase brittleness, builder selector convergence-not-enforced). Spec OoS #10 explicitly forbids adding entries beyond the AC7 TZ-paragraph. Observations are valuable but exceed spec; accept as documented, or move to a separate "additional friction notes from 061 drafting" commit.

## Expected merge conflicts

- `raw/internal/dogfooding/mcp-interactions-journal.md` — branch renamed via `git mv` to `mcp-interactions-journal-archive-through-2026-05-17.md`; live checkout has uncommitted modifications. Resolution: stash/commit live edits, then re-route any new entries into `mcp-interactions-journal-2026-05.md` post-merge.
- `CLAUDE.md` — main has had no edits since branch base (`d62a977`). Clean merge expected; flagged here only because the diff is large.
- `backlog/_followups.md` — branch appends one new section at lines 828–833. No competing edits on main since branch base. Clean.
- No other anticipated conflicts; `git log $(git merge-base main HEAD)..main --oneline` shows only review commits on main since branch cut.

## Follow-up items (defer, do not block merge)

- Bulk path-rewrite of the **131 bare `mcp-interactions-journal.md` references in `.md` files** → archive-suffixed path. Safe `sed`-style change. File as a small followup spec; gated on whether founder wants AC2's grep-check to literally pass zero, or accepts the current "those refs point at the (renamed) file" reading.
- Clean up stale `docs/STATUS.md` references in `backlog/_followups.md:143-144` (active todos pointing at a now-deleted file). One-line edit; not in any AC.
- Confirm intent of empty `.claude/worktrees/` directory — AC5 required removing the stale `laughing-shaw-952357/` subdir (done); if the parent dir should also go, file a 1-line followup.
- TZ-offset bug (AC7): revisit if a second AI client hits the same empty-window failure. One occurrence → followup observation (now landed). Two occurrences → spec it.
- Watch next 3 merge-and-cleanup cycles for whether the stub-and-archive pattern feels right; if so, bulk-migrate the remaining 57 `backlog/complete/` items via a followup spec.

## Acceptance status

- **AC1 — Met.** `find raw/internal -name '*.html' | grep -v journal-style.html` empty. `.gitignore` has the two new lines. CLAUDE.md "HTML twin" subsection rewritten; pandoc one-liner preserved. 13 HTML files deleted.
- **AC2 — Met with caveat.** Rename via `git mv` (history preserved). Fresh `mcp-interactions-journal-2026-05.md` has full preamble + template + rotation rule + two carried-forward Raycast dogfooding entries. CLAUDE.md path refs updated. Caveat: 131 bare-name references remain in other `.md` files (see Bugs/risks B1 in detail below).
- **AC3 — Met.** CLAUDE.md skip-rule paragraph present verbatim from the AC, including the "expected-but-failed MCP call IS journaled" exception.
- **AC4 — Met.** `backlog/archive/README.md` exists (16 lines). Item 001 is a 12-line stub with all required frontmatter fields. Original 107-line body lives at `backlog/archive/shipped/2026-04/2026-04-30-001-repo-bootstrap.md`. `skills/merge-and-cleanup.md` has the new "Wiki promotion (post-merge)" step at L387–390. `tools/sync-skills.sh --check` → `OK`.
- **AC5 — Met (out-of-band).** `git worktree list` no longer shows `laughing-shaw-952357`. `git branch | grep -i shaw` empty. `.claude/worktrees/` directory present but empty.
- **AC6 — Met.** `docs/review-queue-setup.md` launchd section rewritten with ephemeral-worktree approach + canonical `codex exec` recipe. `docs/STATUS.md` deleted (−136 lines).
- **AC7 — Met.** TZ-offset section appended to `backlog/_followups.md` with Repro / Suspected fix size / Leverage paragraphs. (Three additional bonus bullets — see Drift fixups #3.)
- **DoD size delta** — observed `raw=2.3M` (was 4.0M, −1.7M; spec optimistically expected −3M, actual is correct because deleted HTML totaled the 1.5M journal HTML + ~1.5M across the 12 other twins). Other dirs roughly flat as expected.

## Bugs/risks

- **B1.** AC2's literal DoD grep is not met — 131 references to bare `mcp-interactions-journal.md` in `.md` files outside the archive. Examples: `backlog/_followups.md:22,253,258,339`, `backlog/complete/2026-05-08-025-mcp-best-practices.md:82,95`, `tools/raycast-echo/README.md:14`, `tools/review-queue/schemas/README.md:25`. Two interpretations: (a) those refs correctly point at the renamed archive file (filesystem rename, history preserved) → Met; (b) refs should be updated to the archive-suffixed path → followup spec. Recommend (a) + non-blocking followup.
- **B2.** Stale `docs/STATUS.md` refs in `backlog/_followups.md:143-144` are active todos pointing at the deleted file. Historical refs in `backlog/complete/*` are acceptable archive content; active todos are not. Non-blocking; clean up post-merge.
- **B3.** Live checkout has uncommitted `M raw/internal/dogfooding/mcp-interactions-journal.md` and `M tools/raycast-echo/src/search-context.tsx`. The first will collide with the rename on merge — must reconcile pre-merge (see Pre-merge fixups #1).

## Open questions for founder

None blocking. Two ack-style decisions in the pre-merge fixups: (a) accept AGENTS.md parallel edit as-is, (b) accept the three bonus `_followups.md` bullets as documented, or split each to a separate commit.
