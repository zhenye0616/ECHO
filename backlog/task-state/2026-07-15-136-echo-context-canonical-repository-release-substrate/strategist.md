## current_thesis

Implementation cycle one was independently rejected at Project_echo head `25b833332bb22ec79700fcdf31b9c9f20eea79f5` and echo-context head `145868a67a85dbb651faed457ee4001370c0fad0` / tree `44ae95b77cd2298cd25b915f283b07bd7423100e`. The exact rejection artifact is sealed at Project_echo commit `77c5e0c3f7f64c0bda1d99439d8d35eca54551ee`, blob `1cd16c1f31dab5c40ad76b70716846a09c98d845`. R17 returned `proceed_after_patches` plus `pushback`; the delegated strategist accepts findings 1–9 and rejects only the proposed plain workflow-run path because GitHub's current API returns `.github/workflows/source-release-build.yml@main`. The structural cut adds one OS-parent operation host, Project CAS publish/reconcile phases, coordinator/host lifecycle bounds, exact hosted source-mode wiring and cleanup child, global `head_sha=M` run uniqueness, repository-wide exact namespace snapshots, two-pipe askpass/readback hardening, and a complete deadline inventory. R18 must verify these exact patched bytes; both implementation branches remain frozen until fresh same-SHA convergence, a new content seal, and a fresh Codex builder claim.

## locked_decisions

- Item 135 baseline commit `0cf7b006eba665c0bf55e82ff04da70f19f01ebb` / tree `70c5cf8352652b3c4c1dce68cd1a5e40d44e4b05` and all extraction evidence remain immutable.
- Repository creation and baseline-main publication are completed once-only history authorized at Project_echo commits `e1ec8f74ae812e5df0983cd11a9a0827a9aefb67` and `ece86049e8bfca567f03e7cdfbb8ee1d0b45fd05`; repository ID `1302541575`, node ID `R_kgDOTaM1Bw`, private visibility, and baseline main may never be recreated, adopted, or rewritten.
- R14 remains immutable historical convergence at spec commit `f130ba6fd89bd598a06e7603b700fb0f66c6dd54` and old seal `42d2d266660453fc204b6cd3ddaed3b41768c410e1e007ebca9465e60022833e`; that seal is invalidated for claimability by the post-build reframe, not rewritten.
- Cycle-one implementation proved the extraction, source parity, deterministic non-installable artifact, authority boundary, and hosted CI direction; its exact heads are evidence only and carry no merge/release authority.
- Cycle-one review accepted every substantive high/medium finding: executable bootstrap/recovery/fault fixtures; injected production controller/fresh-clone seams; exact mutation-response validation; tag porcelain evidence; final captured release/asset ID and byte readback; and structural workflow-policy tests.
- The private personal Free topology cannot server-enforce branch protection, administrator enforcement, required PR approval, or a private environment reviewer. The spec states this limitation and does not claim protection from a malicious/compromised sole owner.
- Target-main landing uses two fresh `land` phases: read-only `collectLandingEvidence` returns authenticated `B`/reviewed `H`/hosted evidence, then the coordinator seals deterministic raw merge-body bytes and `M`; after the plan/authorization land, `runLanding` starts from a new root/token, rematerializes exact `M` from plan bytes, rechecks evidence, and owns the sole leased porcelain fast-forward push.
- Landing additionally requires both frozen-baseline→H and B→H ancestry; a feature head that omits current main must become a new reviewed descendant before M can exist.
- Release is one read-only push-triggered Actions build at exact landed `M`, one provenance-gated local read-only `collect` invocation, then one separately provenance-gated exact-plan local `publish` invocation. No remote job has write scope, and no authorization ref, off-main approval object, manual/API-created build event, nonce, or build-mutation owner exists.
- Workflow metadata uses plain path `.github/workflows/source-release-build.yml`, while workflow-run readback must use current-API path `.github/workflows/source-release-build.yml@main`. Each selection filters server-side only by `head_sha=M`, fully paginates the total set, and requires global cardinality zero or one before any time/title/event/path predicate; a second run anywhere is permanent ambiguity. Publish couples this global uniqueness plus exact-ID run/job/artifact stability to every predecessor/successor namespace boundary and the final post-N5 check.
- The local publisher fetches the pinned Project_echo authorization only through a host-pinned credentialless public adapter. Target authentication is isolated to exact private-target operations; no new credential/signing state is introduced.
- GitHub REST is pinned to API version `2026-03-10`; all Actions/release binary downloads use one controlled signed-host redirect with credentials stripped.
- Project/target authentication uses the existing `zhenye0616` GitHub CLI keyring credential and exact current scope set without creating state. Plans bind canonical absolute nonsymlink Node/Git/GitHub-CLI paths/versions plus a UID-501/mode-0600 strict no-plaintext GH config before access. The host reads one no-follow descriptor, parses exact bytes, and supplies only a readback-authenticated mode-0600 copy in its owned mode-0700 snapshot directory to close validate/use races. `gh auth status` is removed because it owns an unbrokered credentialed network check; the only allowed acquisition is exact `gh auth token --hostname github.com --user zhenye0616`, followed by the host-owned direct no-redirect `/user` identity/scope gate. The pinned GH binary/version and exact token subcommand are the trusted child boundary; no unsupported per-process socket-denial claim remains. Active account, configured Git protocol, and OAuth-versus-PAT class are deliberately non-authoritative. The host alone acquires private raw token stdout under a 4,096-byte bearer-grammar cap, zeroes every chunk/accumulator, retains one master Buffer, owns HTTPS plus anonymous token/receipt pipes, and zeroes only after GH/Git children and transports settle, direct group leaders are reaped, and PGIDs are absent. Workers receive only framed, bounded, nonsecret typed RPC results.
- The independent reviewer owns the sole canonical implementation-review JSON in earlier additive commit `R`; later plan/authorization commit `P_L` binds `R`/path/blob/hash, with no same-commit self-reference.
- Canonical Project_echo implementation-review, target-main plan/authorization, and source-publication plan/authorization files own every builder/reviewer, run/job/carrier, object, hash, operation, response, readback, and retry-policy identity.
- Main/tag/Project authority pushes use the literal canonical token-free HTTPS URL, command-scoped `http.followRedirects=false`, exact one-ref lease/refspec, `--no-verify`, `--no-follow-tags`, exhaustive config/environment isolation, authenticated absolute askpass with token and nonsecret receipt FDs, exact username/password receipt sequence, and structural porcelain/readback. Mutable `origin`, hooks, follow-tags, redirects, URL rewrites, proxy overrides, alternate helpers/askpass, or remote aliases are forbidden.
- Exact reviewed Project_echo head `P_H` owns the operation host, anonymous-FD askpass helper, streamed owned-root cleanup helper, launcher, envelope, Project authority publisher/reconciler, finite framed RPC schema, and tests. The persistent coordinator is the external trust anchor; its bound is exact 120-second prelaunch + host aggregate + 120-second reserve, it owns/terminates the direct host, and its outer finally runs the authenticated cleanup helper on every partial path. A never-written anonymous liveness pipe plus bound expected parent PID is host-only and noninherited; startup and every resource/write dispatch require live PPID plus nonblocking `EAGAIN`. On detected failure the coordinator closes liveness and gives the host 55 seconds for self-teardown before exact TERM/KILL escalation, preserving host-owned cleanup of separate child groups. The host owns the nonspawning launcher direct child, operation roots, credential, HTTP, Git/gh/cleanup direct-child groups, poison, zeroing, and teardown. Only one authenticated terminal result followed by normal launcher reap can succeed; early exit/hang poisons. Workers/controllers cannot spawn or hold credential-bearing transport.
- One Project `project_authority` host kind publishes the only three authority classes—review `R`, target-main plan `P_L`, and source-publication plan `P_S`—with exact CAS. An ambiguous publish ends; a separate fresh mutation-incapable reconcile phase may deterministically accept only exact public `C` plus every bound ancestry/path/blob/hash under the existing delegation. Destination readback itself is durable authority, avoiding a recursive fourth marker commit.
- The target release controller exports read-only `collectPublicationEvidence` and mutating `runPublication`. Both `collect` and `publish` start from local reviewed `H`, credentiallessly authenticate `P_L`, rematerialize exact `M` from sealed raw plan bytes before target access, and then use distinct roots/tokens. `collect` returns only nonsecret build evidence; `publish` repeats selection/download/verification and alone may mutate the destination.
- Every filesystem/hash/JSON/HTTP/keyring/Git/gh/transfer/extraction/cleanup operation appears once in a generated inventory with exact per-operation, aggregate, settlement, TERM/KILL/reap, PGID-absence, and root-removal limits; missing or caller-overridden entries fail governance. AC3 reserves its final 120 seconds for cleanup/final boundaries, and `T` cleanup is one single-assignment `not_started -> running -> completed|failed` transition so final status/HEAD failure cannot invoke it twice.
- The reviewed host owns each operation root and every live resource; launcher exit/IPC EOF or coordinator-liveness EOF/PPID drift atomically poisons, aborts, TERM/KILLs, reaps direct group leaders, proves PGIDs absent, zeroes, and cleans with no reconciliation or later write. Coordinator death immediately after the synchronous pre-dispatch check leaves at most that single transport in flight; the surviving host fences every successor, but the dead coordinator's own root/log cleanup is honestly unguaranteed. Abrupt host/kernel/power loss has no operation-root/child/zeroing/log guarantee and may leave the in-flight mutation ambiguous. Plan `cleanup:false` means remote destination objects only.
- The full annotated-tag body, tagger fields, raw-byte SHA-256, and immutable Git tag-object OID are authorization inputs. Asset uploads bind captured release-scoped endpoints plus exact-ID enumeration/metadata/download, not a nonexistent response `release_id`.
- Destination namespace authenticated readback remains the sole durable publication authority. Because this is the first release, full scans require the entire repository namespace empty at `E` and equal to exact all-object states `N0`…`N5` before/after every mutation, each coupled to the unchanged globally unique exact build tuple; an unrelated object, duplicate/rerun, or run/job/artifact drift blocks. Drift before a mutation gives zero write; a post-precheck race can affect only the already in-flight mutation and then poisons all successors. No run log, retry, adoption, automatic cleanup, or authority expansion is allowed.
- Nonempty destination state mechanically blocks cold replay. With no separate consumption marker, an empty-namespace cold process cannot distinguish a prior pre-write attempt; a new reviewed descendant/main push, unique build, plan, and authorization are trusted-coordinator policy, not falsely claimed server enforcement.
- The production publication controller retains exact release/asset IDs and approved bytes, validates every mutation response body, emits sanitized tag porcelain proof, and repeats exact-ID metadata/download verification at the final exhaustive namespace boundary.
- Version `0.1.0-dev.136.1` remains source-only, deterministic, `installable:false`, `runtime_authority:false`, `state_authority:false`, maturity DEV.
- Founder delegation at `raw/internal/decisions/2026-07-16-echo-context-sequential-program-delegated-authority.md` authorizes the persistent coordinator but does not waive exact-SHA convergence, fresh builder/reviewer independence, immutable operation records, tests, or readback.
- Items 137–139 remain frozen horizon/risk evidence until item 136 fully lands, releases, verifies, and pins exact outputs.

## open_questions

- None requiring founder input. R17 is dispositioned under the scoped delegation; R18 must verify the operation-host/finite-RPC cut, Project CAS publish/reconcile phases, coordinator/host lifecycle, exact CI source-mode vector and cleanup child, secure wrapper/two-pipe askpass paths, global workflow uniqueness with `@main` run-path fixture, repository-wide namespace checkpoints, and generated deadline/cleanup inventory on the patched same SHA.
- Any R18 finding accepted into another spec edit requires R19+ same-SHA verification before fresh promotion.

## dont_touch

- Do not edit either cycle-one implementation branch, merge target main, create/update an authorization ref, trigger/rerun a build, tag, publish, install, change credentials, or mutate live state while specification review is active.
- Do not alter item-135 provenance, R1–R14 review artifacts, the cycle-one rejection sidecar bytes, items 137–139, echo-brain, echo-loop, or unrelated user/agent work.
- Do not install a daemon, touch LaunchAgents/clients/database/config, switch authority, or advance maturity during item 136.

## canonical_anchors

- spec: backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.md
- rejected_review: backlog/proposed/2026-07-15-136-echo-context-canonical-repository-release-substrate.review.md
- reviews: backlog/reviews/2026-07-15-136-echo-context-canonical-repository-release-substrate/
- r15_combined: backlog/reviews/2026-07-15-136-echo-context-canonical-repository-release-substrate/r15/combined.md
- r16_combined: backlog/reviews/2026-07-15-136-echo-context-canonical-repository-release-substrate/r16/combined.md
- r17_combined: backlog/reviews/2026-07-15-136-echo-context-canonical-repository-release-substrate/r17/combined.md
- delegation: raw/internal/decisions/2026-07-16-echo-context-sequential-program-delegated-authority.md
