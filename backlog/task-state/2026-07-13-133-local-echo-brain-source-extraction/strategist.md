## current_thesis

Extract the reviewed item-132 product closure into a fully independent local `echo-brain` repository before FOUNDER LIVE. Use one attended deterministic run; prove parity and source independence before any remote or authority transfer.

## locked_decisions

- `echo-brain` is the complete client-facing Team decision product, not merely a model adapter.
- Source is pinned to `Project_echo@2971310441b69735cbe759293abd8c4d044bf347`; dirty journals and `.agents/` are excluded.
- Final target is `/Users/zhenye/Desktop/echo-brain`; it is a real local Git repository on a migration branch with no remote.
- Lifecycle is `ABSENT -> RUNNING -> PUBLISHED | FAILED`; there is no automatic resume, stale-owner takeover, artifact lock, or checkpoint reuse.
- Atomic target-specific state-directory creation elects one run. The active supervisor owns its child process group; later invocations never signal recorded process identifiers.
- Failure recovery is explicit `discard`: refuse a final target or possibly-live process, archive state/staging/record/cache/output without deletion, then start a fresh pinned extraction.
- Migration record publication precedes no-replace target publication. Target + byte-identical record + committed candidate identity are the durable published fact.
- Copy only the item-132 product closure plus source/test/build necessities; record every copy, relocation, and rewrite with hashes.
- No symlink, submodule, workspace, `file:`, Git, tarball-path, or runtime import may reach Project_echo or sibling repos.
- The new repo owns its package, lockfile, config, tests, build, artifact, state paths, and qualification evidence.
- Node `22.22.1` and npm `10.9.4` are hard preflights; committed `package-lock.json` is required.
- The pinned source has exactly eight `tests/product` files; transformed hashes are computed before isolation and verified standalone.
- Direct dependencies derive from final bare imports plus fixed dev tools and exact source-lock versions; missing, extra, or ranged entries fail.
- Pre-isolation acquisition builds an integrity-manifested run cache; all candidate install/build/test work then runs offline under sanitized environment and OS sandbox.
- Artifact build is invoked exactly once in a unique run output; a failed build is archived and rebuilt only in a fresh extraction.
- `verify-handoff` derives canonical state/record/target paths and binds the original control blobs despite the record-only evidence commit.
- Project_echo remains backup and migration authority; candidate authority is false and maturity remains DEV.

## open_questions

- R6 by independent `codex` and `codex-ops` bindings must confirm that removing automatic recovery closes R5 without weakening publication, parity, sandbox, or handoff guarantees.
- A later authority-transfer item decides remote creation, branch protection, and old-path freeze.

## dont_touch

- Do not create a target remote, publish anything, or advance maturity.
- Do not implement rank 2/rank 3 features, decision-card redesign, org context, launchd, or delivery.
- Do not modify/delete/freeze current product paths, wiki pages, real state, credentials, or holdout-131.
- Do not touch `/Users/zhenye/Desktop/echo-loop` or `/Users/zhenye/Desktop/echo-context`.

## canonical_anchors

- decision: raw/internal/decisions/2026-07-13-one-shot-local-extraction-lifecycle.md
- spec: backlog/proposed/2026-07-13-133-local-echo-brain-source-extraction.md
- reviews: backlog/reviews/2026-07-13-133-local-echo-brain-source-extraction/
