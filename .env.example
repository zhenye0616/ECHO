# ECHO local secrets — template. Copy to `.env` and fill in real values.
# `.env` is gitignored; this `.env.example` is committed as documentation.

# Granola public API key (Business plan). Format: grn_...
# Used by the api:granola capture poller (backlog item 104).
GRANOLA_API_KEY=
