OpenAI Codex v0.135.0
--------
workdir: /Users/zhenye/Desktop/Project_echo--install-profile-split
model: gpt-5.5
provider: openai
approval: never
sandbox: workspace-write [workdir, /tmp, $TMPDIR]
reasoning effort: xhigh
reasoning summaries: none
session id: 019e893d-7cfc-7cb1-861c-aec3d0e8fce1
--------
user
You are an independent ECHO code reviewer for one pending_review item. This is a read-only review: do not edit source, backlog items, sidecars, wiki, docs/BACKLOG, or git branches. You may run verification commands in the feature worktree only.

Where things are:
- Item file: /Users/zhenye/Desktop/Project_echo/backlog/pending_review/2026-06-02-084-install-profile-split.md
- Worktree: /Users/zhenye/Desktop/Project_echo--install-profile-split
- Branch: agent/install-profile-split
- Expected head_sha: f144bb4c0a8e15371c61ea8b410da77e32ed8f4a
- Project CLAUDE.md: /Users/zhenye/Desktop/Project_echo/CLAUDE.md
- Main repo root: /Users/zhenye/Desktop/Project_echo

Spec refs to read before judging acceptance:
- /Users/zhenye/Desktop/Project_echo/backlog/{ready,pending_review,complete}/2026-06-01-083-init-registers-claude-code-mcp.md
- /Users/zhenye/Desktop/Project_echo--install-profile-split/src/echo-home/adapters/skill-sync.ts
- /Users/zhenye/Desktop/Project_echo--install-profile-split/src/echo-home/adapter-sync.ts
- /Users/zhenye/Desktop/Project_echo--install-profile-split/src/echo-home/adapters/role-sync.ts
- /Users/zhenye/Desktop/Project_echo--install-profile-split/src/echo-home/adapters/workflow-sync.ts
- /Users/zhenye/Desktop/Project_echo--install-profile-split/src/echo-home/paths.ts
- /Users/zhenye/Desktop/Project_echo--install-profile-split/src/cli/commands/init.ts
- /Users/zhenye/Desktop/Project_echo/raw/internal/decisions/2026-05-25-echo-pro-paid-coord-layer-design.md
- /Users/zhenye/Desktop/Project_echo/backlog/{ready,pending_review,complete}/2026-05-26-076-packaged-echoctl-install-boundary.md

GROUND-TRUTH CHECK FIRST, before anything else:
Run exactly this check in the worktree:

cd "/Users/zhenye/Desktop/Project_echo--install-profile-split"
ACTUAL=$(git rev-parse HEAD)
EXPECTED="f144bb4c0a8e15371c61ea8b410da77e32ed8f4a"
if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "WORKTREE DRIFTED: expected $EXPECTED, found $ACTUAL"
  produce a review with verdict `block` and only this open question for the founder: "Worktree HEAD ($ACTUAL) does not match recorded head_sha ($EXPECTED). Founder must reconcile (re-push agent's work, force the worktree to the recorded SHA, or update the recorded head_sha) before review can proceed."
  Do NOT run tests. Do NOT speculate about the diff.
fi

If HEAD matches, review only the item at the recorded SHA. Read the item, CLAUDE.md, and listed spec refs. Check:
1. Acceptance-criteria coverage: every AC and relevant body checklist item, table format, Met/Partial/Not Met + one-line evidence.
2. Drift: use `git diff main...agent/install-profile-split` from the feature worktree/main refs and compare to the item's Out of Scope section, especially AC8 no files outside files_to_modify.
3. Design-choice judgments: every judgment call or decision in agent_notes/spec that the agent flagged, judged stand/redo with reasoning.
4. Code quality: concurrency, error handling, security, schema/backcompat, CLI behavior, test quality.
5. Cross-cut conflicts: compare current main versions for files_to_modify and predict merge conflicts.
6. Bugs/risks with file:line refs.

Verification to run inside /Users/zhenye/Desktop/Project_echo--install-profile-split after HEAD check passes:
- npm test
- npm run lint
- npm run typecheck
Report observed results, not agent_notes claims.

Return markdown under 1000 words using EXACTLY these level-2 section headings, all present:
## Verdict
First line must contain exactly one of: merge as-is | merge with founder fixups | redo before merge | block. Then one concise paragraph explaining why.

## Acceptance status
Use a markdown table covering AC1-AC8 and any important body checklist/provenance obligations.

## Drift findings
State touched files vs files_to_modify and any out-of-scope behavior.

## Design-choice judgments
Judge J1-J7 / locked choices and agent_notes choices as stand or redo.

## Bugs/risks
List findings with file:line references. If none, say none found.

## Merge-conflict preview
List expected conflicts with files and recommended resolution strategy. If none expected, say none expected.

## Suggested fixups
Split into `Pre-merge punch list` and `Non-blocking follow-ups`. Use bullets. If none, say none.

## Test counts observed
List exact commands and observed pass/fail counts or output summaries.

codex
I’ll first verify the worktree HEAD exactly as requested. If it matches, I’ll stay in reviewer mode and only run/read what’s needed for the review.
exec
/bin/zsh -lc 'cd "/Users/zhenye/Desktop/Project_echo--install-profile-split"
ACTUAL=$(git rev-parse HEAD)
EXPECTED="f144bb4c0a8e15371c61ea8b410da77e32ed8f4a"
if [ "$ACTUAL" != "$EXPECTED" ]; then
  echo "WORKTREE DRIFTED: expected $EXPECTED, found $ACTUAL"
fi' in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 199ms:
codex
HEAD matched the recorded SHA, so I’m proceeding with the review at `f144bb4`. I’m reading the governing instructions, the pending item, and the referenced implementation files before running tests.
exec
/bin/zsh -lc "sed -n '1,220p' '/Users/zhenye/.codex/skills/ECHO:review-pending/SKILL.md'" in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 187ms:
---
name: 'ECHO:review-pending'
description: 'Review every item in backlog/pending_review/ in parallel. Spawns one code-reviewer subagent per item; synthesizes a per-item verdict + fixup list + merge-conflict preview; writes a sidecar review plan next to each item; stops for human validation. Read-only EXCEPT for committing+pushing the per-item review sidecars (the skill''s deliverable) — closes the /review-pending → /merge-and-cleanup handoff gap per 045 AC6.'
metadata:
  short-description: 'Review every item in backlog/pending_review/ in parallel. Spawns one'
---

You are reviewing the agent's pending work for the founder. The founder runs this command in the morning (or whenever pending_review has items) to get a structured verdict before deciding what to merge.

This command is **read-only** in the sense that matters for merge planning: it does not move items between stages, modify item frontmatter, or run git operations beyond committing+pushing the per-item review sidecars (the skill's deliverable). Sidecars become tracked artifacts on `origin/main` so the downstream `/merge-and-cleanup` skill's pre-flight clean-tree check passes without manual founder intervention. Its primary output is human-readable summaries plus the sidecar review plans themselves.

## Inputs

- Optional positional argument: a specific item id (e.g., `012`, `2026-04-30-012`, or full filename `2026-04-30-012-git-capture.md`).
- If omitted: reviews **every** item in `backlog/pending_review/`.

Resolve the input to a list of full paths under `backlog/pending_review/`.

## Step A — Pre-flight

```bash
cd ~/Desktop/Project_echo
git fetch origin main --quiet

ITEMS=()
if [ -n "${1:-}" ]; then
  # match by suffix — accepts "012", "2026-04-30-012", or full filename
  match=$(ls backlog/pending_review/*"$1"*.md 2>/dev/null | head -1)
  [ -z "$match" ] && { echo "no match in pending_review/ for: $1"; exit 1; }
  ITEMS+=("$match")
else
  for f in backlog/pending_review/*.md; do
    [ -e "$f" ] && ITEMS+=("$f")
  done
fi

[ ${#ITEMS[@]} -eq 0 ] && { echo "nothing to review — pending_review/ is empty"; exit 0; }
echo "Reviewing ${#ITEMS[@]} item(s): ${ITEMS[*]##*/}"
```

## Step B — Dispatch one code-review process per item, in parallel

For each item, dispatch one independent code-review process **concurrently** using your binding's subagent dispatch primitive. Each process must run with a self-contained prompt that does not depend on conversation context — context-shared dispatch defeats parallelism and pollutes per-item evidence.

Concrete dispatch mechanisms differ by binding (see "Binding-specific notes" sections below). The contract those mechanisms must satisfy:

1. Per-item isolation — each process operates on exactly one pending_review item; its writeable surface is scoped to that item's existing per-item worktree, not the main repo or other items' worktrees.
2. Parallelism — all processes start before any completes.
3. Capture-and-survive — each process emits its review into a discrete, durably-addressable artifact (file or returned content). The orchestrator must be able to inspect the artifact even if the process exited non-zero.
4. Concurrency cap — fan-out is bounded (N ≤ 4 on the founder's machine) to avoid CPU saturation when many items are pending.

### Per-item prompt template

The prompt must include all of:

- **Where things are.** Absolute path to the item file, absolute path to the worktree, the branch name + head_sha (read these from frontmatter), the spec_refs list, and the project's CLAUDE.md location.
- **Ground-truth check (FIRST, before anything else).** The worktree's HEAD must match the item's recorded `head_sha`. If it doesn't, the verification below would be meaningless — tests against drifted state are false-green by construction. Run:
  ```bash
  cd "$WORKTREE"
  ACTUAL=$(git rev-parse HEAD)
  if [ "$ACTUAL" != "$EXPECTED_HEAD_SHA" ]; then
    echo "WORKTREE DRIFTED: expected $EXPECTED_HEAD_SHA, found $ACTUAL"
    exit 1
  fi
  ```
  If HEADs disagree, abort this item's review with verdict `block` and a single open question for the founder: *"Worktree HEAD ($ACTUAL) does not match recorded head_sha ($EXPECTED). Founder must reconcile (re-push agent's work, force the worktree to the recorded SHA, or update the recorded head_sha) before review can proceed."* Do NOT run tests. Do NOT speculate about the diff. Drift detection is a load-bearing failure mode.
- **What to check** (only after ground-truth check passes). A structured rubric:
  1. Acceptance-criteria coverage — every bullet in `acceptance:` and the body checklist, table format, Met/Partial/Not Met + one-line evidence.
  2. Drift — `git diff main...agent/<slug>` from the project root vs. the body's "Out of Scope (Don't Drift)".
  3. Design-choice judgments — every choice the agent flagged in `agent_notes`, judged stand/redo with reasoning.
  4. Code quality — concurrency, error handling, security where applicable.
  5. Cross-cut conflicts — what this branch will collide with on merge to current main (read main's versions of files in `files_to_modify` to predict).
  6. Bugs/risks with `file:line` refs.
- **Verification.** Run `npm test`, `npm run lint`, `npm run typecheck` inside the worktree (which we just confirmed is at the recorded head_sha). Don't trust agent_notes' test counts at face value — re-run, report observed.
- **What to return.** A structured review (markdown) with sections — exact headings required so the orchestrator can extract them by regex match: **`Verdict`** (one of `merge as-is` | `merge with founder fixups` | `redo before merge` | `block`), **`Acceptance status`**, **`Drift findings`**, **`Design-choice judgments`**, **`Bugs/risks`**, **`Merge-conflict preview`**, **`Suggested fixups`** (split into pre-merge punch list + non-blocking follow-ups), **`Test counts observed`**. Aim for under 1000 words.

Dispatch all per-item processes concurrently — do not serialize them.

## Binding-specific notes — Claude Code

When this skill executes inside a Claude Code session:

- Dispatch primitive: spawn one `superpowers:code-reviewer` subagent per item via the `Agent` tool, with all calls in a single message so subagents run in parallel.
- Each subagent runs in Claude Code's tool-use sandbox; `npm test`, `npm run lint`, `npm run typecheck` run inside its respective worktree directly.
- Per-item review markdown is returned as the subagent's final message; the orchestrator (the parent Claude Code session) consumes it directly from the tool result — no temp files are needed.
- Synced adapter: `.claude/commands/review-pending.md` (byte-identical to canonical `skills/review-pending.md`).

## Binding-specific notes — codex

When this skill executes inside a codex CLI session, the protocol body above (Step A, the per-item prompt template, Step C–E) is unchanged; codex's dispatch primitive is what differs from Claude Code's. This section is operational guidance specific to that primitive.

### Codex skill install

- `tools/install-echo-codex-skills.sh` renders this canonical skill into `~/.codex/skills/ECHO:review-pending/SKILL.md` with Codex-shaped frontmatter. Re-run that installer after editing canonical `skills/*.md`; there is no repo-side Codex adapter snapshot.

### Sandbox semantics (per-child)

- Each per-item child runs as `codex exec --sandbox workspace-write -C "$WORKTREE" --output-last-message "$RUN_DIR/<item-id>.review.md" - < "$PROMPT"`.
- `-C "$WORKTREE"` scopes the child's CWD AND its workspace-write surface to its own per-item worktree at `$HOME/Desktop/Project_echo--<slug>/`, NOT the main repo. The per-item worktree is the disjoint write surface each child needs in order to run `npm install` / `npm test` / `npm run lint` / `npm run typecheck` without racing other children. Parallel children CANNOT race each other's writes because worktree paths are disjoint.
- The main repo (`~/Desktop/Project_echo/`) is OUTSIDE each child's workspace-write surface; per-item sidecar writes happen in the single-threaded orchestrator path (see below), not from children.

### Run-directory + per-child output isolation

- Orchestrator allocates a per-run isolated directory ONCE at fan-out start: `RUN_DIR=$(mktemp -d -t echo-review-pending-XXXXX)` (uses `${TMPDIR:-/tmp}` when `TMPDIR` is unset). Two concurrent `/review-pending` invocations each get their own `RUN_DIR` so per-child output files cannot collide across orchestrators.
- Each child writes:
  - The final response → `$RUN_DIR/<item-id>.review.md` via `--output-last-message` (NOT parsed from stdout — see below).
  - Stdout → `$RUN_DIR/<item-id>.stdout` (diagnostics only).
  - Stderr → `$RUN_DIR/<item-id>.stderr` (diagnostics only).
  - Exit code → `$RUN_DIR/<item-id>.rc`.
- Cleanup is scoped to `RUN_DIR` only: `trap 'rm -rf "$RUN_DIR"' EXIT`. Never a broad glob; another in-flight orchestrator's `RUN_DIR` must be untouched.

### Subprocess wrapping under `set -euo pipefail`

Each per-item child invocation is wrapped so codex's non-zero exit does not terminate the orchestrator before all outputs are durable:

```bash
(
  set +e
  codex exec --sandbox workspace-write -C "$WORKTREE" \
    --output-last-message "$RUN_DIR/$ITEM_ID.review.md" \
    - < "$PROMPT" > "$RUN_DIR/$ITEM_ID.stdout" 2> "$RUN_DIR/$ITEM_ID.stderr"
  echo $? > "$RUN_DIR/$ITEM_ID.rc"
) &
PIDS+=( $! )
```

Parent drains: `for pid in "${PIDS[@]}"; do wait "$pid" || true; done`. The `|| true` prevents `set -e` in the orchestrator from killing it mid-drain when a child's `wait` returns non-zero.

### Why `--output-last-message`, not stdout parsing

codex CLI v0.130.0's stdout interleaves banner + workdir/model metadata + the echoed user prompt + token summary BEFORE the final answer. The per-item prompt contains the same review-section heading names (`Verdict`, `Drift findings`, etc.) as the desired output, so regex extraction from raw stdout would match prompt text as if it were the child's review and silently produce wrong reviews. `--output-last-message` writes ONLY the final response to the named file. Orchestrator parses `<item-id>.review.md` — never `<item-id>.stdout`. Stdout + stderr are kept only as diagnostics, surfaced when a parse fails or `rc≠0`.

### Concurrency cap

Fan-out is bounded by a counting-semaphore gate (`(( running < N ))`) before each child spawn; N ≤ 4 on the founder's machine. Unbounded `&` would saturate CPU on a 10-item review.

### Sidecar write happens in the orchestrator path

Children never write the sidecar. The orchestrator (single-threaded for that step) parses `<item-id>.review.md`, derives `verdict` + structured sections, then writes `backlog/pending_review/<id>.review.md` in the MAIN repo, commits, and pushes via `tools/review-queue/push-with-retry.sh` (same as the Claude Code path). Main-repo writes never race because exactly one orchestrator process performs them.

### Required section headings

Orchestrator extracts the following section headings (level-2 Markdown, `## <heading>`) from each child's `<item-id>.review.md`:

- `Verdict`
- `Acceptance status`
- `Drift findings`
- `Design-choice judgments`
- `Bugs/risks`
- `Merge-conflict preview`
- `Suggested fixups`
- `Test counts observed`

These match what the Claude Code path's `superpowers:code-reviewer` subagent already produces, so sidecar synthesis (Step C) is binding-agnostic.

### Parse-failure semantics — durable evidence preservation

If the child's review file is missing, OR `rc ≠ 0`, OR any required section heading is absent from the file, the orchestrator MUST:

1. **Preserve evidence first** — copy the child's `<item-id>.{stdout,stderr,review.md}` triple to `raw/internal/queue-errors/<ISO-ts>-review-pending-<item-id>/` BEFORE the `RUN_DIR` cleanup trap fires. This gives the operator a durable pointer to inspect what the child actually produced.
2. **Log a queue-errors row** that names the specific missing section headings + `rc` + the first 2KB of stderr inline. This is durable in `queue-errors.md` and visible at next-run pre-flight.
3. **Do not silently drop or substitute defaults.** A child whose review failed parsing is treated as a missing review; the founder gets a "verdict: block (parse failure)" sidecar with a pointer to the preserved evidence directory.

### Dogfooding journaling

Per `CLAUDE.md` "Dogfooding journal discipline", every ECHO MCP call made during a `/review-pending` run is journaled in-the-moment to `raw/internal/dogfooding/mcp-interactions-journal.md` with the 6-field template. The codex orchestrator journals its own calls directly; the codex children journal theirs in-the-moment if they make any (typically they don't — children operate on the worktree filesystem, not MCP).

## Step C — Synthesize and write per-item sidecar plans

For each returned review, write a sidecar file at `backlog/pending_review/<id>.review.md` (next to the item file). This is what `/merge-and-cleanup` will consume.

### Sidecar format

```markdown
---
item_id: 2026-04-30-012-git-capture
verdict: merge with founder fixups   # merge as-is | merge with founder fixups | redo before merge | block
reviewed_at: 2026-04-30T22:30:00Z
test_counts: { passed: 132, failed: 0 }
---

## Verdict
<one paragraph>

## Pre-merge fixups
- [ ] <fixup 1 — file:line — one-sentence rationale>
- [ ] <fixup 2 — ...>

## Expected merge conflicts
- `<file>` — <recommended resolution strategy in one sentence>
- `<file>` — ...

## Follow-up items (defer, do not block merge)
- <item description>
- ...

## Open questions for founder
<only if verdict is "block" — list the specific decisions blocking merge>
```

The human will read this, optionally edit it (uncheck fixups they want to defer, edit resolution strategy, add notes), then invoke `/merge-and-cleanup`.

### Sidecar commit + push (045 AC6)

After ALL sidecars are written, commit + push each one atomically. The sidecar IS a complete review artifact and benefits from atomic git history; the founder can amend in place via a follow-up commit if edits are needed.

```bash
# SIDECARS is the list of paths written above (one per item).
for SIDECAR in "${SIDECARS[@]}"; do
  SIDECAR_BASE=$(basename "$SIDECAR" .review.md)
  git add "$SIDECAR"
  git commit -m "review: $SIDECAR_BASE" "$SIDECAR"
  tools/review-queue/push-with-retry.sh "review: $SIDECAR_BASE"
done
```

`push-with-retry.sh` is used in place of `git push origin main || true`. The bare-push-with-swallow pattern would produce a local-only sidecar commit on auth loss, network outage, or rejected push, leaving the strategist's `/merge-and-cleanup` to pass pre-flight locally while the next operator or machine sees no review artifact on origin. `push-with-retry.sh` performs bounded retries, logs to `queue-errors.md` on terminal failure (existing 039+041 contract), and surfaces the failure non-zero — making any push gap visible at /review-pending exit time, not silently at /merge-and-cleanup time.

The helper is invoked **inside the SIDECARS loop**, once per sidecar, using the per-sidecar base name. This is unambiguous for multi-item /review-pending invocations and produces one push-with-retry per review (matches the per-reviewer-response pattern used by `commit-reviewer-response.sh`).


codex
I’m using the `ECHO:review-pending` review rubric, but not its sidecar/commit steps because your prompt explicitly makes this a read-only single-item review. Next I’m mapping the spec to the branch diff so the acceptance table is based on evidence, not agent notes.
exec
/bin/zsh -lc "sed -n '1,260p' '/Users/zhenye/Desktop/Project_echo/CLAUDE.md'" in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc "sed -n '1,280p' '/Users/zhenye/Desktop/Project_echo/backlog/pending_review/2026-06-02-084-install-profile-split.md'" in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc "ls '/Users/zhenye/Desktop/Project_echo'/backlog/{ready,pending_review,complete}/2026-05-26-076-packaged-echoctl-install-boundary.md" in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git status --short --branch' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git diff --name-status main...agent/install-profile-split' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc "ls '/Users/zhenye/Desktop/Project_echo'/backlog/{ready,pending_review,complete}/2026-06-01-083-init-registers-claude-code-mcp.md" in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 210ms:
# CLAUDE.md — ECHO Project Working Instructions

## Project Mission

ECHO is the cross-platform context layer for AI-era knowledge work. The product makes every AI surface smarter by unifying context across the user's tools, lives invisibly via browser extension + MCP server + hotkey overlay, and never becomes a destination app.

**Brand promise:** *"We don't make AI smarter. We make every AI smarter about you."*

## How to Use This Repo

This is both a **decision archive** (the wiki) and a **build coordination system** (the backlog + agent runs). Two important rules govern how the two halves stay honest:

- **The product wiki (`wiki/`) is lagging documentation of shipped reality, not aspirational spec.** A page exists for X only after X has been built, reviewed, and merged. Until then, X's spec lives inside its `backlog/ready/<id>.md` item.
- **Operating-model files** (this file, `docs/AGENT_INSTRUCTIONS.md`, `backlog/README.md`, `.claude/commands/process-backlog.md`) update *immediately* when the operating model changes. They have no shipping milestone and are not product decisions.

### When making strategic decisions

1. **Search existing wiki + backlog first.** A shipped wiki page (`wiki/product/`, `wiki/principles/`, `wiki/architecture/`, `wiki/capture/`, `wiki/surfaces/`, etc.) or an in-flight backlog item often already captures the principle. Reuse before creating.
2. **Cite cross-project wisdom.** The `yc-wiki` (`~/Desktop/yc/yc-wiki/`) is the authoritative source for startup strategy frameworks. Reference its concept pages by `[[link]]` when applying them.
3. **Capture new decisions as backlog items, not wiki pages.** The full spec — reasoning, alternatives considered, final call, acceptance criteria — lives inside `backlog/ready/<id>.md`. The strategist does **not** write to `wiki/` at decision time. Wiki pages are written *after* the item lands in `backlog/complete/`, and only then.
4. **Background reasoning** that doesn't correspond to an actionable build item lands in `raw/internal/decisions/`.
5. **The manifest** is updated only when (and only when) a wiki page is actually created post-shipment.

### When researching precedents

- Drop research notes into `raw/external/precedents/` (Wispr Flow, 1Password, Plaid patterns, etc.)
- Drop competitor scans into `raw/external/competitor-scans/`
- Synthesize patterns into `wiki/principles/` only after multiple raw sources point the same direction

### When running validation experiments

- Each user interview gets a markdown file in `raw/internal/interviews/`
- Aggregate signals into `wiki/research/` after 5+ interviews
- Concierge experiment notes also go in `raw/internal/interviews/`

## Folder Taxonomy

The wiki is restructured (2026-05-01) into eight folders that mirror the substrate's left-edge / middle / right-edge architecture and the `interface-layers` L1/L3/L5 vocabulary. See `raw/internal/decisions/2026-05-01-wiki-restructure-proposal.md` for the diagnosis and reasoning.

| Folder | Purpose | Examples |
|---|---|---|
| `product/` | Strategic "what" — locked V1 scope, target cohort, brand decisions | v1-spec, bundle-decision, target-cohort-indie-ai-builders |
| `principles/` | Active commitments + disciplines (design, brand, scope) | felt-not-seen, compose-not-capture, drift-prevention |
| `architecture/` | The durable middle — substrate components + canonical models | system-architecture, capture-gate, storage, interface-layers |
| `capture/` | Layer 1 capture surfaces (substrate's left edge) | fs-watcher, cursor-extractor, claude-code-extractor |
| `capture/per-app/` | Field-level reference for each connected app | cursor-collected-data |
| `surfaces/` | Layer 3 + Layer 5 — what users + AI clients touch | mcp-server, hotkey-overlay, audit-page, browser-extension |
| `research/` | Validation work | wedge-vs-thesis-validation, validation-experiments |
| `operating-model/` | Process meta — wave retros, drift audits | wave-1-2-3-retrospective |

Each page carries a `status: shipped | planned` frontmatter field. `shipped` documents reality that exists; `planned` is a V1 commitment specced but not yet built (today: hotkey-overlay, audit-page).

## Filename + Link Conventions

- **Filenames:** kebab-case, globally unique across folders. Prefix with `src-` if a source would collide with a concept/entity.
- **Wikilinks:** `[[link]]` format. Use the filename only — no folder prefix, no `.md` extension.
- **Topic taxonomy:** see `.manifest-schema.json`. Reuse existing topics before creating new ones.

## Cross-Project References

When citing the YC wiki:
```markdown
Per [[ai-moats-debate|the moats analysis]] in the YC wiki, brand is a clearly-stated obsession...
```

When citing the AIE / ECHO research wiki (architecture, memory system internals):
```markdown
Per [[append-only-ledger]] in the Claude wiki, the storage substrate is...
```

For external wiki references that don't exist locally as wikilinks, use markdown links with the full path:
```markdown
See [Aravind Srinivas on agentic search](/Users/zhenye/Desktop/yc/yc-wiki/sources/aravind-agentic-search.md).
```

## V1 Scope Reminder (Tape Above Desk)

- **Cohort:** Indie AI builders / dev founders
- **Bundle:** Cursor + Claude Code + GitHub + Slack + web AI extension
- **Form:** Browser extension + MCP server + hotkey overlay (no destination app)
- **Pricing:** $25/mo
- **Layers:** L1 (passive ingestion) + L3 (summoned, Q&A + assembly via clipboard+launch) + minimal L5 (audit)
- **Cut from V1:** Email, Linear, Notion, meeting transcripts, Zoom, calendar — all V1.5+
- **Cut layers:** L2 (ambient), L4 (conversational), all autonomous agent action — all V2+
- **Definition of done:** killer demo works in founder's daily workflow with no hand-staging; ≥3/5 randomly-selected indie AI builders ask "when can I pay?"

## Naming

Working name: **ECHO**. Hard rename deadline: before public Show HN launch (week 10). After that, name is permanent.

---

## Operating Mode: Coordination System

This repo coordinates three roles. **Multiple builder agents may run in parallel** — each works inside its own git worktree on its own feature branch.

1. **Strategist (Claude in conversation with founder)** — produces design decisions; specs them as `backlog/ready/<id>.md` items; does **not** write to `wiki/` until items ship; **may also review and prep merges** for items in `pending_review/` (see "Reviewer independence rule" below)
2. **Builder agents (autonomous, parallelizable)** — claim items from `backlog/ready/`, work in isolated worktrees, move items through the pipeline; **never review or merge their own work**
3. **Founder** — gives final approval at the two irreversible moments: (a) signing off on substantive conflict resolutions surfaced by reviewer, (b) `git push origin main`. Also handles review + merge directly when no strategist or independent reviewer is available, and asks the strategist to update the wiki post-shipment.

### Cross-tool protocol lives in `skills/` (not `.claude/commands/`)

The slash-command skills that drive ECHO's multi-agent workflow — `process-backlog`, `process-backlog-batch`, `review-pending`, `merge-and-cleanup`, `review-queue-codex`, `review-queue-cursor`, `review-queue-codex-ops`, `review-queue-watch` — are **the cross-tool collaboration protocol**, not Claude-Code-specific helpers. They define the grammar by which multiple AI clients (Claude Code as strategist, Cursor's Claude as builder, Codex as headless reviewer, etc.) coordinate as peers.

- **Canonical source of truth:** `skills/<name>.md` — vendor-neutral, ECHO-namespaced.
- **Claude Code adapter:** `.claude/commands/<name>.md` — derived real-file copy, maintained by `tools/sync-skills.sh`. **Do not hand-edit `.claude/commands/<name>.md`**; the sync script overwrites it. Edit the canonical `skills/<name>.md` and re-run `tools/sync-skills.sh` (or `tools/sync-skills.sh --check` to verify identity post-edit).
- **Future client adapters** (Cursor's Claude, Codex, web ChatGPT, etc.): add their own directories alongside `.claude/commands/`. The sync script will extend to copy to each adapter directory. For non-filesystem clients (OpenAI's GPT, web ChatGPT, etc.), a future MCP tool `echo_skill(name)` will return canonical content from `skills/` on demand.

Why this matters: Anthropic structurally can't be vendor-neutral because they ARE a vendor. ECHO's wedge is hosting the *protocol* that lets Claude AND non-Claude tools work as peers. Putting the protocol under `.claude/` would perpetuate the "Claude owns the workflow" framing the wedge is designed to undo. See `raw/internal/decisions/2026-05-13-echo-skills-are-the-cross-tool-protocol.md` for the full reasoning.

### Reviewer independence rule

The reviewer-and-merger of any item must be **a different role/agent than the builder** that wrote the code. Acceptable reviewers, in preference order:

1. **Strategist** — has full design context, often more than founder for technical items
2. **A second builder agent** (not the one that built the item) — independent eyes, no spec-author conflict-of-interest
3. **Founder** — fallback when neither of the above is available, or whenever founder wants to review directly

Self-review is the bad version. The agent that drifted into wrong scope can't see its own drift. Independence is the structural check that makes the pipeline trustworthy. Whoever reviews must (a) read the diff against acceptance criteria, (b) prep `review_notes` and any reconciliation diff for conflicts, (c) pause for founder green-light at substantive-conflict and `push-to-main` checkpoints. The reviewer never skips those two checkpoints; everything else they handle end-to-end.

### Pipeline

```
backlog/ready/  →  backlog/claimed/  →  backlog/pending_review/  →  backlog/complete/
                                                                         │
                                                                         ▼
                                                            strategist updates wiki/
```

### Strategist Responsibilities (this Claude conversation)

After any strategic conversation that lands an actionable decision:

1. **Create a `backlog/ready/<id>.md` item** — full spec lives here (this is the authoritative spec until the item ships). Include an "After Completion (Strategist Notes)" section noting which wiki pages should be created/updated post-shipment.
2. **Add a row to `docs/BACKLOG.md`'s Ready table.**
3. **Do NOT touch `wiki/`.** Wiki edits happen only after items land in `complete/`.

When the founder reports items have moved to `complete/`, the strategist's *next* job is to read those items' "After Completion" sections and promote the now-shipped decisions to `wiki/` — landing each page in the appropriate folder (product/, principles/, architecture/, capture/, capture/per-app/, surfaces/, research/, or operating-model/), then updating `.manifest.json` and regenerating `wiki/index.md` via `tools/wiki_index.py`.

### Builder Agent Responsibilities

When a builder agent runs:

1. **Pull `main`**, then **atomically claim** an item: a single commit on `main` that moves the file `ready/ → claimed/` and sets `claimed_by`, `claimed_at`, `branch` in frontmatter. Push immediately. If push is rejected, another agent won — pick the next ready item.
2. **Create the worktree** at `~/Desktop/Project_echo--<slug>/` on a fresh `agent/<slug>` branch.
3. **Read all `spec_refs`** in the item before writing code.
4. **Implement to acceptance criteria only.** No scope expansion (per `drift-prevention` rules).
5. **Log work** in `raw/internal/agent-runs/<date>-<item-id>.md`.
6. **If uncertainty arises that requires founder input** — STOP, move item to `pending_review/` with the question in `agent_notes`. Do not guess.
7. **When acceptance criteria pass** — push the feature branch, then in the main repo on `main` move the item to `pending_review/` with `agent_notes` summary, `head_sha`, and (if applicable) `pr_url`.
8. **One item per run.** Do not pick up a second.

The agent operates across **two directories**: backlog state changes happen in the main repo on `main` (so all agents share consistent backlog state); code work happens inside the worktree on the feature branch. The slash command handles directory switching.

### Dogfooding journal discipline (every AI client)

**Every ECHO MCP call must be logged to the current month's shard at `raw/internal/dogfooding/mcp-interactions-journal-YYYY-MM.md` in the moment** — currently `raw/internal/dogfooding/mcp-interactions-journal-2026-06.md`. Do not append to the frozen historical archive (`raw/internal/dogfooding/mcp-interactions-journal-archive-through-2026-05-17.md`). This applies equally to Claude Code, Codex, Cursor's Claude, agent runs, and any other AI client invoking the MCP server. The journal is cross-tool and cross-item; monthly shards are the canonical log. It is the input that decides V1.5+ backlog priorities; aspirational end-of-week entries are useless, lossy in-the-moment entries are gold.

**What counts:** any `mcp__echo__*` or `mcp__echo-memory__*` invocation — `get_recent_work_context`, `search_memories`, `echo_ping`, `memory_*`, etc. Log even 0-match / error responses; those are the highest-signal entries.

**Skip-rule for zero-MCP-call entries.** If a reviewer tick (or any AI-client invocation) reads files / runs git / runs scripts but makes **zero `mcp__echo__*` calls**, do NOT journal it. The journal's signal is MCP-call discipline + surprising failures; mechanical activity without ECHO retrieval is not journal-worthy. Operational artifacts (review responses, commit messages, agent-run logs) already capture that work. Exception: a reviewer tick that *expected to* make an MCP call but *failed to* (sandbox error, transport error, etc.) IS journaled — that's a surprising failure.

**Required entry shape** (the template lives in the journal preamble; copy it verbatim):

```
### YYYY-MM-DD HH:MM PDT — <one-line context>
- **Trigger:** what the user (or AI client) just did that called the tool
- **Query inputs:** since=…  until=…  artifact_hint=…  (or other args)
- **Returned:** N clusters, M atoms; top cluster: "<label or none>"; rank_reasons: […]
- **Sources:** source_breakdown={…} for trace; OR per-match source-prefix list for search; OR the specific jsonl/git/fs paths the atoms came from. ALWAYS shown — a reader must be able to tell which capture surfaces contributed and which were silently absent.
- **Verdict:** ✅ right / 🟡 partial / ❌ wrong
- **Note:** observation — what felt right or off
- **Conjecture:** (optional) what algorithm/config change might help — observations only, don't design fixes here
```

The **Sources** field is non-optional. Source-volume bias and silent omission (e.g., a window whose git rows all got dropped by Bug A's text-compare WHERE clause) are the most-recurring failure modes; without explicit per-call source attribution, future readers can't tell whether a 1-cluster trace response is "right and narrow" or "wrong because all git rows got dropped." Codex started doing this organically at 2026-05-08 00:46 PDT and earlier; the discipline is now project-wide.

**Don't design fixes in the journal.** Observations only. Backlog items come from end-of-window synthesis. If you find yourself drafting a fix, stop — the journal's "lossy in the moment" honesty is what makes it useful for backlog planning later.

**Journal-by-proxy for read-only consultees (046 AC6).** A read-only consultee (e.g., `codex exec --sandbox read-only`, a subagent without write capability, or any future binding that lacks repo-write) MAY call ECHO MCP only if it immediately reports `tool name / inputs / returned shape / sources / verdict / note` to its orchestrator in the same turn. The orchestrator MUST journal the call in the same turn, attributed to the consultee — e.g. `Source agent: codex strategist (consulting; orchestrator-journaled by claude)`. The in-the-moment rule is NOT weakened: the consultee's report and the orchestrator's journal entry are part of the same turn, not deferred. Worked example: the journal entry at `2026-05-13 16:45 PDT — closed-loop event` (codex strategist reads prior codex strategist via ECHO; claude orchestrator journals the call) demonstrates the shape. Cross-referenced in `skills/role-typed-task-state.md` so non-Claude bindings see the same rule.

**HTML twins are no longer committed.** Local regeneration is optional — anyone who wants a styled view can run the pandoc one-liner against the current month's journal shard and view the generated HTML locally. The MD shard is the canonical authoritative format. One-liner:

```
pandoc -s --metadata title="ECHO MCP interactions journal (cross-tool, cross-AI)" --toc --toc-depth=3 \
  -H raw/internal/dogfooding/journal-style.html \
  raw/internal/dogfooding/mcp-interactions-journal-2026-05.md \
  -o raw/internal/dogfooding/mcp-interactions-journal-2026-05.html
```

Do not commit regenerated HTML twins. On the first MCP-call journal append of each new calendar month, create a fresh `raw/internal/dogfooding/mcp-interactions-journal-YYYY-MM.md` shard with the same preamble template and update this current-shard pointer.

### Drift Prevention Applies to Agents Too

Agents are more dangerous than the founder for drift, because they don't have the founder's gut. Three safeguards:

1. Every backlog item has an explicit "Out of Scope (Don't Drift)" section
2. Agents must read `spec_refs` before any code is written
3. Sandbox is enforced in code (capture-gate pattern), not by policy

If an agent finds itself wanting to do something not in acceptance criteria: STOP, log the temptation in `raw/internal/decisions/` as a drift-event, fill `agent_notes` with the question, push branch, move item to `pending_review/`.

### Strategist drift — patching deeper instead of removing

The "agents drift via scope expansion" failure mode has a strategist-side twin during review rounds: adding mechanism in response to a finding, then watching the next round find bugs in *the mechanism the patch added*. Each round's diff grows; the spec accumulates premature optimizations and observability scaffolding that the load-bearing core doesn't need.

The fix is dispositioning discipline, not better patches. See `skills/review-queue-watch.md` "Disposition discipline — prefer removal over deeper patching when findings target a recent-round patch" for the check the strategist applies mid-tick. Concrete worked examples from 057a r4 and r6 are in that skill.

See [`backlog/README.md`](./backlog/README.md) for the full system documentation including atomic-claim and worktree mechanics.

 succeeded in 215ms:
---
id: 2026-06-02-084-install-profile-split
title: "`echoctl init` install-profile split — customer-default vs dogfood-opt-in (stop the coord-surface leak onto customer machines)"
status: ready
priority: HIGH
estimate: 1-1.5d
created: 2026-06-02
blocked_by:
  - 2026-06-01-083-init-registers-claude-code-mcp
task_state_ref: 2026-06-02-084-install-profile-split
requested_reviewers: ["codex", "codex-ops"]
files_to_modify:
  - src/cli/commands/init.ts                       # AC1 — add `--profile customer|dogfood` flag + answer-file `profile` field; thread the resolved profile into wire/skill-sync; persist it. SHARED SEAM with 083 (init.ts) → this item is blocked_by 083; build after 083 merges.
  - src/echo-home/adapters/skill-sync.ts           # AC2 — filter the first-hop copy (assets/echo-skills/ → ~/.echo/skills/) by each skill's `audience` frontmatter against the active profile, so dogfood-only skills never land on a customer machine.
  - src/echo-home/adapter-sync.ts                  # AC3 — syncAll threads the profile to skill-sync + gates role/workflow sync (customer skips, represented as a successful no-op so computeOverallOk stays true).
  - tests/echo-home/adapter-sync.test.ts           # AC7 — customer-skip is a successful no-op (not undefined→failure); dogfood preserves today's role/workflow behavior.
  - src/echo-home/paths.ts                         # AC4 — add `profile: "customer" | "dogfood"` to the onboarding.json schema (+ Ajv validator); default-absent tolerated for pre-084 installs.
  - src/echo-home/wizard/wire.ts                   # AC1 — accept + forward the profile to syncAll (the wizard already owns the wire orchestration).
  - src/echo-home/wizard/run-wizard.ts             # AC1 — the public `Wizard.wire()` API init.ts calls is declared here; its `Pick<WireOpts,...>` must accept + forward `profile` to wireAgents() (else passing profile from init.ts is a typecheck failure). codex r4 F1.
  - src/cli/commands/doctor.ts                     # AC5 — doctor reports the active profile (JSON/report-model side).
  - src/cli/io/render.ts                           # AC5 — render the profile in `renderDoctorReport()` text output (codex/codex-ops r3: text path is silent otherwise + AC8 forbids touching it if unlisted).
  - assets/echo-skills/using-echo-mcp.md           # AC2 — add `audience: customer` frontmatter.
  - assets/echo-skills/using-echo-coord.md         # AC2 — add `audience: dogfood` frontmatter.
  - tools/foreign-install-smoke.sh                 # AC6 — assert a default (customer) install lands ONLY using-echo-mcp (no /using-echo-coord, no roles, no workflows). SHARED SEAM with 083 (smoke) → blocked_by 083.
  - tests/echo-home/adapters/skill-sync.test.ts    # AC7 — audience filtering by profile.
  - tests/cli/init.test.ts                         # AC7 — flag parse + default + persistence + re-run-respects-recorded-profile.
  - tests/cli/doctor.test.ts                       # AC7 — doctor surfaces the profile.

spec_refs:
  - backlog/{ready,pending_review,complete}/2026-06-01-083-init-registers-claude-code-mcp.md  # SHARED SEAMS: 083 also edits init.ts + foreign-install-smoke.sh + docs/echoctl-install.md. 084 is blocked_by 083 so the builder works on top of 083's merged init.ts/smoke, not in parallel.
  - src/echo-home/adapters/skill-sync.ts  # THE mechanism: two-hop copy (assets/echo-skills/*.md → ~/.echo/skills/ → per-vendor commands dir), NOT role-gated today → both using-echo-* land as slash-commands on every machine. This is the leak 084 closes.
  - src/echo-home/adapter-sync.ts  # syncAll orchestrator: calls skill-sync + role-sync + workflow-sync. The profile gate lives here + in skill-sync.
  - src/echo-home/adapters/role-sync.ts  # 072's role TOML sync (assets/echo-roles/ → ~/.echo/roles/). Customer profile skips this (J5).
  - src/echo-home/adapters/workflow-sync.ts  # 075's workflow sync (assets/echo-workflows/ → ~/.echo/workflows/). Customer profile skips this (J5).
  - src/echo-home/paths.ts  # 070's onboarding.json schema + ECHO_HOME_PATHS; the `profile` field lands here.
  - src/cli/commands/init.ts  # 074's init: TTY prompts + answer-file (`--answer-file`) path. The `--profile` flag + answer-file `profile` field land here.
  - raw/internal/decisions/2026-05-25-echo-pro-paid-coord-layer-design.md  # the 070-075 coord-layer design this profiles; §"slash-command pollution" is the originating concern.
  - backlog/{ready,pending_review,complete}/2026-05-26-076-packaged-echoctl-install-boundary.md  # 076 set the tarball `files` allowlist (ships BOTH audiences). 084 does NOT change what ships — it filters at INSTALL time (see Locked #1).

# --- agent-managed fields (filled in during run) ---
claimed_by: "78D5AB0F-A8A3-4F01-BC2E-EB05961B2405"
claimed_at: "2026-06-02T08:31:53Z"
branch: "agent/install-profile-split"
worktree: "/Users/zhenye/Desktop/Project_echo--install-profile-split"
head_sha: "f144bb4c0a8e15371c61ea8b410da77e32ed8f4a"
pr_url: ""
agent_notes: |
  Implemented `echoctl init` install profiles on `agent/install-profile-split` at `f144bb4c0a8e15371c61ea8b410da77e32ed8f4a`: CLI/answer-file/recorded/default profile resolution, onboarding persistence + profile-less warning, customer skill filtering, customer role/workflow no-op skips, dogfood full-surface preservation, doctor profile output, and smoke-script assertions.
  Verification: focused profile vitest (4 files / 70 tests), `npm run typecheck`, `npm run lint`, full `npm test` (142 files passed / 1 skipped; 1505 tests passed / 21 skipped), `npm run build:cli`, `bash -n tools/foreign-install-smoke.sh`, and `git diff --check` passed. Full foreign-install smoke was not executed because the script hardcodes `$HOME/Desktop/Project_echo`, which would exercise main rather than the sibling feature worktree.
review_notes: ""
---

## Why (the friction this closes)

`echoctl init` has **one install path: everything goes to everyone.** Skill-sync's two-hop copy (`assets/echo-skills/*.md` → `~/.echo/skills/` → each vendor's commands dir) is not role-gated, so on *any* machine BOTH `using-echo-mcp` and `using-echo-coord` land as slash-commands, and roles + the `change-review` workflow drop into `~/.echo/`. There's no notion of *who* is installing.

That means a customer/alpha machine (where the coordination pipeline is meaningless — they don't have `backlog/`, the review-queue scripts, or the multi-agent loop) gets the founder's full dogfood surface, including a `/using-echo-coord` command. On **2026-05-27** this was named a **product-shape bug** and the agreed fix was an **install-profile split** (customer-default vs dogfood-opt-in); it was specced in conversation but never built. The n=1 concierge install (Tue 2026-06-02) surfaces it live: the coworker would see coord surface that's off-message for the personal-context-layer positioning. (A one-build pack-time trim is the throwaway stopgap for *tomorrow*; this item is the durable fix.)

## What this is — and what it deliberately is NOT

A **friction-fix**: teach `init` *who it's installing for* and ship a different agent-facing coordination surface per profile. It is NOT a new product surface, NOT a capability/ACL system, NOT a change to what the tarball ships.

## Locked decisions (founder, 2026-05-27 + 2026-06-02)

1. **Filter at INSTALL time, not pack time.** The tarball still ships BOTH audiences (a dogfood install must be able to use `using-echo-coord`); 084 gates what `init` *installs/wires onto the machine*. (The pack-time exclusion of `using-echo-coord` is a separate one-build throwaway for tomorrow — explicitly NOT this item.)
2. **Missing recorded profile ⇒ `customer`, unconditionally; founder opts into dogfood explicitly (persisted + respected).** No `profile` recorded ⇒ `customer` — with NO inference from onboarding state (the r1–r3 "infer legacy from `completed`/`agents`" discriminator was removed in r4 after it kept generating crash-window findings; see J7). The founder sets `--profile dogfood` ONCE; it persists and every later no-flag re-init respects it. To avoid a SILENT downgrade of a pre-084 machine, init emits a LOUD one-line warning when it defaults an existing-but-profile-less onboarding file to customer (telling the founder to re-run `--profile dogfood`). Universal-customer-default means **no fresh install can ever flip to dogfood on a crash/retry** — the whole partial-scaffold class is dissolved.
3. **Customer = substrate + retrieval only.** Customer profile syncs ONLY `audience: customer` skills (`using-echo-mcp`); it does NOT sync `using-echo-coord`, the coord roles (strategist/reviewer/builder), or the `change-review` workflow. Dogfood profile = today's full behavior.
4. **Profile gates the AGENT-FACING surface only, not the daemon.** The daemon's coord MCP tools (`coord_emit`/`coord_invoke`/`coord_status`) stay registered regardless of profile — they're invisible substrate (nothing advertises them to an agent except the `using-echo-coord` skill, which IS gated). Gating the daemon is out of scope.

## Judgment calls (flagged for r1 reviewers)

- **J1 — selector: `--profile` flag (+ persist) vs auto-detection.** Lean: explicit `echoctl init --profile customer|dogfood` + answer-file `profile` field, persisted in onboarding.json; default per Locked #2. Auto-detection (source-repo-present → dogfood) is magic and brittle — reject unless a reviewer makes a strong case.
- **J2 — `audience` frontmatter tag vs directory split.** Lean: add `audience: customer|dogfood` frontmatter to each shipped skill and filter in skill-sync. Lighter than restructuring into `assets/echo-skills/{customer,internal}/`; precedent = 074's `<!-- echo-owned-skill -->` first-line marker.
- **J3 — filter at hop-1 (don't land dogfood skills in `~/.echo/skills/` at all on customer) vs hop-2 (land but don't command-ify).** Lean hop-1: a customer machine never has the coord skill on disk. Cleaner and leaves no half-state.
- **J4 — unknown/legacy `audience` (a skill with no tag).** Lean: untagged skill defaults to `customer` (safe — visible everywhere as today), so adding the field is non-breaking; only `using-echo-coord` is explicitly `dogfood`.
- **J5 — customer roles/workflows: skip sync entirely vs sync-but-inert.** Lean: skip entirely (strategist/reviewer/builder + `change-review` are meaningless without the pipeline). Reviewers confirm whether any customer-useful workflow should survive (none today).
- **J7 — RESOLVED (r4): legacy-inference discriminator DROPPED.** r1–r3 tried to infer "is this the founder's legacy install?" from onboarding state (file-presence → then `completed`/`agents`) so a no-flag re-init wouldn't downgrade him. Every refinement surfaced a new crash window (r2 atomic-ordering, r3 `completed`/agents, r4 mid-wire agents-before-completion). Removed: missing profile ⇒ `customer` unconditionally (AC4); the founder opts into dogfood explicitly + a loud warning prevents silent downgrade. No state-inference → no crash window → the partial-scaffold class is gone. (Removal-over-deeper-patching: the convenience of auto-detecting the founder's intent wasn't worth a tail of ordering bugs.)
- **J6 — RESOLVED (r2): reprofile auto-prune DROPPED.** r1 added an AC2b auto-prune of stale dogfood artifacts; r2 found it generated its own bug surface (the `cli/inverse` helper is `skills.ts` not `skill-files.ts`; hop-2 removal compares against a hop-1 source that the prune itself deletes first; and **roles/workflows carry no ownership marker**, so marker-gated deletion is impossible and name-based deletion risks user data-loss). Per removal-over-deeper-patching discipline AND because the reprofile case **cannot occur in V1** (the n=1 coworker is a fresh machine; the founder stays dogfood via AC4(b)), the prune is removed and the guarantee narrowed to **fresh** customer installs (OoS#1b). This was codex r1 F1's own sanctioned alternative ("or explicitly narrow the promise to fresh installs"). No file-deletion mechanism ships in 084 → no data-loss surface.

## Acceptance criteria

- **AC1 — profile selector + persistence + explicit precedence (hardened per codex-ops r3 F3).** `echoctl init` accepts `--profile customer|dogfood` and an answer-file `profile` field. **Resolution precedence (highest wins): CLI `--profile` > answer-file `profile` > recorded `onboarding.json` profile > `customer` default (AC4 — unconditional, no inference).** The resolved profile is written to `onboarding.json` (AC4). Re-running `init` with no flag/answer-file profile on a machine with a recorded profile does NOT change it. A scripted no-TTY install passing `profile: dogfood` in its answer file MUST come up dogfood (not silently customer) — covered by an answer-file test (AC7).
- **AC2 — skill audience filter (FRESH install).** Skills carry `audience: customer|dogfood` frontmatter (`using-echo-mcp` = customer, `using-echo-coord` = dogfood; untagged ⇒ customer per J4). A `customer` install syncs only `audience: customer` skills onto the machine (J3 hop-1); a `dogfood` install syncs all. **On a fresh customer install no `/using-echo-coord` command is ever created** (the guarantee is fresh-install scoped — reprofile cleanup is explicitly OoS, see OoS#1b, per codex r2 F1's sanctioned "narrow the promise to fresh installs" disposition).
- **AC3 — role/workflow gate.** A `customer` install does NOT sync coord roles (`~/.echo/roles/`) or workflows (`~/.echo/workflows/`); a `dogfood` install syncs both (today's behavior). (J5.) The customer skip MUST be represented in `syncAll`'s result as a **successful skipped/no-op** (NOT an `undefined` `workflowsResult`/`roles` that `computeOverallOk` reads as failure — codex r1 F3 + codex-ops r1 F6): a correct customer install exits 0 / `doctor` healthy, never degraded for skipping coord sync.
- **AC4 — onboarding.json schema + universal-customer-default for missing profile (discriminator REMOVED in r4 per codex-ops r4 F1).** `onboarding.json` gains a `profile` field (+ Ajv validator); the resolved profile is persisted there. **When no `profile` is recorded, resolve `customer` — UNCONDITIONALLY**, with no inference from `completed`/`agents`/file-presence. Rationale: every attempted discriminator collided with a crash window (`ensureEchoHome` writes the file before init persists profile; `wire()` writes `agents` before completion — so "interrupted fresh customer" and "legacy install" can present identically), and each refinement surfaced a new window (r2→r3→r4). Universal-customer-default has no window to misread: a fresh customer install interrupted at ANY point re-resolves `customer` on retry; it can never flip to dogfood. Back-compat (no SILENT downgrade): when an existing onboarding file lacks `profile` and neither `--profile` nor an answer-file profile was given, init emits a LOUD one-line warning — "defaulted to `customer`; re-run `echoctl init --profile dogfood` to restore the full coordination surface." The founder opts in once; AC1 then respects the recorded value. doctor consumes the field (AC5).
- **AC5 — doctor reports profile (text + JSON).** `echoctl doctor` shows the active profile in BOTH its JSON report (`doctor.ts`) and its human text output (`renderDoctorReport()` in `src/cli/io/render.ts` — added to `files_to_modify` per codex r3 F1 + codex-ops r3 F2; without it the text path stays silent and the first support check can't tell `customer` from `dogfood`). The doctor test asserts the text path, not only JSON/model state.
- **AC6 — smoke asserts the FRESH customer surface + no-flip invariant.** `tools/foreign-install-smoke.sh`: (i) a default/customer install hard-asserts ONLY `using-echo-mcp` landed in the agent commands dir and `~/.echo/{roles,workflows}/` got no coord roles/workflows — mechanical non-zero fail on any leak; (ii) **re-running `init` (no flag) on that customer machine stays customer** (per AC4 universal-customer-default + recorded-profile respect — never flips to dogfood); (iii) a `--profile dogfood` invocation asserts the full surface lands.
- **AC7 — tests green.** New/updated tests for AC1–AC5 pass, including: `adapter-sync.test.ts` proving customer-skip is a successful no-op (not failure) while dogfood preserves today's role/workflow behavior; **the AC1 precedence chain** incl. a no-TTY answer-file `profile: dogfood` install coming up dogfood (not customer); **AC4 universal-customer-default** — any missing-`profile` onboarding state (no file; bare scaffold; `completed:true`+agents without profile; mid-wire `completed:false`+agents without profile) ALL resolve `customer` (no flip to dogfood at any crash point), and a recorded `dogfood` is respected on re-run; **the pre-084 loud-warning path** (existing profile-less file + no flag ⇒ customer WITH the restore warning); and the **doctor text-output** profile assertion (AC5). Full `npm test` + typecheck green.
- **AC8 — no scope drift.** No file outside `files_to_modify` is touched; the Out-of-Scope list is honored.

## Out of Scope (Don't Drift)

1. **No change to what the tarball ships** — `package.json` `files` still ships BOTH audiences (Locked #1). The pack-time `using-echo-coord` exclusion is the separate one-build throwaway, NOT this item.
1b. **No reprofile/stale-artifact auto-prune (r2 removal).** The clean-surface guarantee is **fresh customer installs only**. Switching an EXISTING install from dogfood→customer does NOT auto-delete leftover coord skills/roles/workflows — `init` ships no file-deletion path (the data-loss risk + the missing roles/workflows ownership marker make auto-prune unsafe for V1, and the case cannot occur for the n=1 coworker or the founder). To convert an existing machine, run `echoctl uninstall` then re-init `--profile customer`. A safe reprofile cleanup (with a real ownership predicate for roles/workflows) is a **filed followup**, not this item.
2. **No third "orchestrator" profile** — the wedge-is-the-loop "coord IS the product for orchestrators" future is deferred; V1 is exactly two profiles.
3. **No gating of the daemon's coord MCP tools** (`coord_emit`/`coord_invoke`/`coord_status`) — substrate stays whole (Locked #4).
4. **No per-skill capability/ACL system** — `audience` is a single coarse tag, not a permission model.
5. **No remote / multi-machine profile management or sync.**
6. **No auto-detection of profile** unless a reviewer overturns J1 — explicit flag + persistence only.
7. **Do not reorder or re-architect 083's MCP-registration work** — 084 builds ON TOP of merged 083 (`blocked_by`); touch the shared `init.ts`/smoke seams minimally.

## After Completion (Strategist Notes)

- Once 084 ships, the **pack-time B-tactical trim is obsolete** — every pack is correct by construction. Note this in `docs/echoctl-install.md`'s install section (a `--profile` line) when promoting.
- The founder runs `echoctl init --profile dogfood` once on his own machine to keep the full coordination surface; document that in the dogfooding setup notes.
- Wiki: fold the profile concept into the echoctl/onboarding page alongside 076's install-boundary, only post-shipment.
- If the "orchestrator profile" (coord-as-product) ever graduates per wedge-is-the-loop, it's a NEW profile value + spec, not a reopening of this item.
- **File the reprofile-cleanup followup** in `backlog/_followups.md` (per OoS#1b): a safe dogfood→customer prune needs a real ownership predicate for roles/workflows (072/075 don't stamp one today) + correct hop-2-before-hop-1 removal order against the packaged asset — deferred out of 084 to keep V1 free of a file-deletion path. Low priority (no V1 user hits it).

## Consult provenance

Originating decision: 2026-05-27 install-profile-split (sessions `5c6b839c` + codex consults `019e676d`/`019e67bc` — "internal coord skills stay dogfood-only"), retrieved via ECHO on 2026-06-02 and independently reproduced by a codex ECHO consult (both converged: the `/using-echo-coord` leak is the unbuilt install-profile-split). Sized as the durable fix behind the tomorrow-only pack-time trim. Blocked on 083 (shared `init.ts`/smoke seams).

 succeeded in 200ms:
## agent/install-profile-split...origin/agent/install-profile-split

 exited 1 in 206ms:
ls: /Users/zhenye/Desktop/Project_echo/backlog/pending_review/2026-05-26-076-packaged-echoctl-install-boundary.md: No such file or directory
ls: /Users/zhenye/Desktop/Project_echo/backlog/ready/2026-05-26-076-packaged-echoctl-install-boundary.md: No such file or directory
/Users/zhenye/Desktop/Project_echo/backlog/complete/2026-05-26-076-packaged-echoctl-install-boundary.md

 exited 1 in 206ms:
ls: /Users/zhenye/Desktop/Project_echo/backlog/pending_review/2026-06-01-083-init-registers-claude-code-mcp.md: No such file or directory
ls: /Users/zhenye/Desktop/Project_echo/backlog/ready/2026-06-01-083-init-registers-claude-code-mcp.md: No such file or directory
/Users/zhenye/Desktop/Project_echo/backlog/complete/2026-06-01-083-init-registers-claude-code-mcp.md

 succeeded in 224ms:
M	assets/echo-skills/using-echo-coord.md
M	assets/echo-skills/using-echo-mcp.md
M	src/cli/commands/doctor.ts
M	src/cli/commands/init.ts
M	src/cli/io/render.ts
M	src/echo-home/adapter-sync.ts
M	src/echo-home/adapters/skill-sync.ts
M	src/echo-home/paths.ts
M	src/echo-home/wizard/run-wizard.ts
M	src/echo-home/wizard/wire.ts
M	tests/cli/doctor.test.ts
M	tests/cli/init.test.ts
M	tests/echo-home/adapter-sync.test.ts
M	tests/echo-home/adapters/skill-sync.test.ts
M	tools/foreign-install-smoke.sh

codex
The branch touches exactly the fifteen files listed in the item’s `files_to_modify`. I’m reading the modified code and tests with line numbers now, then I’ll run the required verification commands.
exec
/bin/zsh -lc "sed -n '1,260p' '/Users/zhenye/Desktop/Project_echo/backlog/complete/2026-06-01-083-init-registers-claude-code-mcp.md'" in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba src/echo-home/paths.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba src/echo-home/adapter-sync.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc "sed -n '1,240p' '/Users/zhenye/Desktop/Project_echo/backlog/complete/2026-05-26-076-packaged-echoctl-install-boundary.md'" in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc "sed -n '1,240p' '/Users/zhenye/Desktop/Project_echo/raw/internal/decisions/2026-05-25-echo-pro-paid-coord-layer-design.md'" in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba src/echo-home/adapters/skill-sync.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 208ms:
     1	import { homedir } from 'node:os';
     2	import { join, resolve } from 'node:path';
     3	import { Ajv, type AnySchema, type ValidateFunction } from 'ajv';
     4	import { isNonEmptyString } from '../guards.js';
     5	
     6	const root = isNonEmptyString(process.env['ECHO_HOME'])
     7	  ? resolve(process.env['ECHO_HOME'])
     8	  : join(homedir(), '.echo');
     9	
    10	export interface EchoHomePaths {
    11	  root: string;
    12	  skills: string;
    13	  roles: string;
    14	  workflows: string;
    15	  adapters: string;
    16	  state: string;
    17	  stateOnboarding: string;
    18	  stateProjects: string;
    19	  stateCaptureSources: string;
    20	}
    21	
    22	function resolveRoot(homeOverride?: string): string {
    23	  if (homeOverride !== undefined) {
    24	    if (!isNonEmptyString(homeOverride)) throw new Error('invalid --home: expected path');
    25	    return resolve(homeOverride);
    26	  }
    27	  return isNonEmptyString(process.env['ECHO_HOME'])
    28	    ? resolve(process.env['ECHO_HOME'])
    29	    : join(homedir(), '.echo');
    30	}
    31	
    32	export function resolveEchoHomePaths(homeOverride?: string): EchoHomePaths {
    33	  const resolvedRoot = resolveRoot(homeOverride);
    34	  return Object.freeze({
    35	    root: resolvedRoot,
    36	    skills: join(resolvedRoot, 'skills'),
    37	    roles: join(resolvedRoot, 'roles'),
    38	    workflows: join(resolvedRoot, 'workflows'),
    39	    adapters: join(resolvedRoot, 'adapters'),
    40	    state: join(resolvedRoot, 'state'),
    41	    stateOnboarding: join(resolvedRoot, 'state', 'onboarding.json'),
    42	    stateProjects: join(resolvedRoot, 'state', 'projects.json'),
    43	    stateCaptureSources: join(resolvedRoot, 'state', 'capture-sources.json'),
    44	  });
    45	}
    46	
    47	export let ECHO_HOME_PATHS = resolveEchoHomePaths(root);
    48	
    49	export function setEchoHomeRoot(homeOverride: string): EchoHomePaths {
    50	  ECHO_HOME_PATHS = resolveEchoHomePaths(homeOverride);
    51	  process.env['ECHO_HOME'] = ECHO_HOME_PATHS.root;
    52	  return ECHO_HOME_PATHS;
    53	}
    54	
    55	export type InstallProfile = 'customer' | 'dogfood';
    56	
    57	export interface OnboardingState {
    58	  schema_version: 1;
    59	  created_at: string;
    60	  last_updated_at: string;
    61	  completed: boolean;
    62	  profile?: InstallProfile;
    63	  agents: OnboardedAgentProfile[];
    64	}
    65	
    66	export interface OnboardedAgentProfile {
    67	  id: 'codex' | 'claude-code' | 'cursor' | string;
    68	  detected_at: string;
    69	  wired_at: string | null;
    70	  probed_at: string | null;
    71	  capabilities: string[];
    72	  wire_error: string | null;
    73	}
    74	
    75	export interface ProjectsState {
    76	  schema_version: 1;
    77	  last_refreshed_at: string;
    78	  default_project: string | null;
    79	  projects: ProjectRecord[];
    80	}
    81	
    82	export interface ProjectRecord {
    83	  repo_root: string;
    84	  last_seen: string;
    85	  source_breakdown: Record<string, number>;
    86	}
    87	
    88	const onboardingStateSchema: AnySchema = {
    89	  type: 'object',
    90	  additionalProperties: false,
    91	  required: ['schema_version', 'created_at', 'last_updated_at', 'completed', 'agents'],
    92	  properties: {
    93	    schema_version: { const: 1 },
    94	    created_at: { type: 'string' },
    95	    last_updated_at: { type: 'string' },
    96	    completed: { type: 'boolean' },
    97	    profile: { enum: ['customer', 'dogfood'] },
    98	    agents: {
    99	      type: 'array',
   100	      items: {
   101	        type: 'object',
   102	        additionalProperties: false,
   103	        required: ['id', 'detected_at', 'wired_at', 'probed_at', 'capabilities', 'wire_error'],
   104	        properties: {
   105	          id: { type: 'string' },
   106	          detected_at: { type: 'string' },
   107	          wired_at: { type: ['string', 'null'] },
   108	          probed_at: { type: ['string', 'null'] },
   109	          capabilities: { type: 'array', items: { type: 'string' } },
   110	          wire_error: { type: ['string', 'null'] },
   111	        },
   112	      },
   113	    },
   114	  },
   115	};
   116	
   117	const projectsStateSchema: AnySchema = {
   118	  type: 'object',
   119	  additionalProperties: false,
   120	  required: ['schema_version', 'last_refreshed_at', 'default_project', 'projects'],
   121	  properties: {
   122	    schema_version: { const: 1 },
   123	    last_refreshed_at: { type: 'string' },
   124	    default_project: { type: ['string', 'null'] },
   125	    projects: {
   126	      type: 'array',
   127	      items: {
   128	        type: 'object',
   129	        additionalProperties: false,
   130	        required: ['repo_root', 'last_seen', 'source_breakdown'],
   131	        properties: {
   132	          repo_root: { type: 'string' },
   133	          last_seen: { type: 'string' },
   134	          source_breakdown: {
   135	            type: 'object',
   136	            additionalProperties: { type: 'number' },
   137	          },
   138	        },
   139	      },
   140	    },
   141	  },
   142	};
   143	
   144	const ajv = new Ajv({ allErrors: true, strict: false });
   145	
   146	export const validateOnboardingState: ValidateFunction<OnboardingState> =
   147	  ajv.compile<OnboardingState>(onboardingStateSchema);
   148	
   149	export const validateProjectsState: ValidateFunction<ProjectsState> =
   150	  ajv.compile<ProjectsState>(projectsStateSchema);

 succeeded in 209ms:
---
id: 2026-06-01-083-init-registers-claude-code-mcp
title: "`echoctl init` registers Claude Code's MCP server (close the half-wire gap) + exact doctor remediation copy"
status: pending_review
priority: HIGH
estimate: 0.5d
created: 2026-06-01
blocked_by: []
task_state_ref: 2026-06-01-083-init-registers-claude-code-mcp
requested_reviewers: ["codex", "codex-ops"]
files_to_modify:
  - src/echo-home/wizard/wire.ts                 # AC1 — claude-code currently gets NO mcpServer config (only Codex/Cursor do, ~line 97). Wire MUST cause Claude Code's MCP server to be registered. J1: placement (here vs a new adapter vs init.ts step).
  - src/echo-home/adapters/claude-code-mcp.ts     # AC1 (NEW, if J1 chooses the adapter shape) — spawns `claude mcp add --transport http --scope user echo <url>`, idempotent, best-effort. Parallels src/echo-home/adapters/codex-config.ts. Builder may instead fold this into wire.ts/init.ts per J1.
  - src/cli/commands/init.ts                      # AC1+AC3 — drive the registration in BOTH interactive and --answer-file paths (the smoke test + headless install must exercise it); fix the remediation string (~line 272) to the fully explicit command.
  - src/cli/commands/doctor.ts                    # AC2 — when claude-code probe == mcp-not-configured, surface the EXACT remediation line, not generic "unhealthy".
  - src/cli/io/render.ts                          # AC2 — per-agent remediation rendering (generic output today at ~lines 86/95).
  - tools/foreign-install-smoke.sh               # AC4 — assert claude-code MCP registration the way it already asserts codex's (currently only greps codex config.toml + CLAUDE.md). Use an injected fake `claude` shim on PATH that records argv (no real login in the sandbox). J2.
  - docs/echoctl-install.md                       # AC5 — remove the "manually run claude mcp add" workaround now that init does it; document the user-scope behavior + the shadowing escape hatch.
  - tests/cli/init.test.ts                        # AC6 — selecting claude-code triggers the registration spawn; claude-missing is non-fatal with remediation; idempotent re-run is non-fatal.
  - tests/cli/doctor.test.ts                      # AC6 — mcp-not-configured row prints the exact remediation command.
  - tests/echo-home/wizard/wire.test.ts           # AC6 — wire produces a claude-code MCP-registration effect (fake spawn), parallel to the codex MCP assertion.

spec_refs:
  - raw/internal/interviews/2026-06-02-coworker-n1-concierge-install.md  # the live runbook this unblocks — "Known likely fix #1 — Claude Code MCP not registered" is exactly this gap; AC5 should let the runbook drop the manual step.
  - raw/internal/decisions/2026-06-01-office-hours-n1-concierge-install.md  # the n=1 concierge decision; scope is Claude Code + Codex + git + MCP only.
  - tools/foreign-install-smoke.sh  # the isolated foreign-install test that surfaced the gap on 2026-06-01 (codex got `ok (append, add)` = real MCP registration; claude-code got `ok (append, copied, copied)` = instructions only).
  - src/echo-home/adapters/codex-config.ts  # THE PRECEDENT — how Codex's `[mcp_servers.echo]` gets registered during wire. Claude Code needs the equivalent (via the vendor CLI, not a config-file mutator — see Locked decisions).
  - src/echo-home/wizard/probe.ts  # already classifies claude-code-missing-MCP as `mcp-not-configured` (~line 150). No probe change needed; doctor/render consume this classification. Reference only.
  - src/echo-home/adapter-sync.ts  # ~line 625: claude-code path is markdown-instructions + skills only (no MCP server). Reference for J1 placement.
  - backlog/{ready,pending_review,complete}/2026-05-25-073-onboarding-wizard.md  # 073 owns wire()/probe() contracts the registration step plugs into. Shipped (complete/).
  - backlog/{ready,pending_review,complete}/2026-05-25-074-echo-cli-binary.md  # 074 owns init/doctor. Shipped.
  - backlog/{ready,pending_review,complete}/2026-05-26-076-packaged-echoctl-install-boundary.md  # 076 is the install boundary this rides on; daemon plist already bakes process.execPath + validates node>=22 (so the old "bare node" runtime worry is largely closed). Shipped.

# --- agent-managed fields (filled in during run) ---
claimed_by: "78D5AB0F-A8A3-4F01-BC2E-EB05961B2405"
claimed_at: "2026-06-02T07:31:36Z"
branch: "agent/init-registers-claude-code-mcp"
worktree: "/Users/zhenye/Desktop/Project_echo--init-registers-claude-code-mcp"
head_sha: "2d64e40e2c242fd794f5eca0594eb4f234ff1c2f"
pr_url: ""
agent_notes: |
  Implemented Claude Code MCP registration during echoctl init via the wire path, with exact init/doctor remediation and smoke-test argv assertion. Feature branch agent/init-registers-claude-code-mcp is pushed at 2d64e40e2c242fd794f5eca0594eb4f234ff1c2f; verification passed: focused init/doctor/wire tests, typecheck, lint, build:cli, bash -n for foreign-install-smoke.sh, git diff --check, and a second full npm test run (1496 passed, 21 skipped).
review_notes: |
  Merged on 2026-06-02 via founder reconciliation.

  Spec converged via cross-tool review-queue (codex + codex-ops, 3 rounds: r1 5 findings → patch; r2 1 finding → reconcile mechanism removed; r3 both proceed/0) before claim. Implementation reviewed via /review-pending sidecar (verdict: merge with founder fixups; 1496/0 tests, AC1-AC6 covered, HEAD 2d64e40e).

  Conflicts resolved:
  - None. Branch forked post-convergence; its src/tests/docs/tools changes were disjoint from main's backlog/review traffic → clean --no-ff merge.

  C3.5 cross-vendor consult: none invoked (clean merge, no judgment-loaded resolution).

  Fixups applied:
  - Founder accepted the file-scope exception: the branch touched src/echo-home/adapter-sync.ts (not in files_to_modify). This is the J1 placement realized correctly — the in-scope new src/echo-home/adapters/claude-code-mcp.ts is wired into syncAll's per-agent dispatch (adapter-sync.ts), parallel to the existing codex/cursor MCP registration. Coherent, tested, within J1's open placement question; spec under-specified the wire→syncAll seam. Not drift.

  Fixups deferred to follow-up items: none.

  Verify: 1496/1496 tests pass (21 skipped); lint, typecheck, and tools/sync-skills.sh --check all clean post-merge.

  Follow-up items (non-blocking, filed to _followups.md):
  - Add SIGKILL escalation for the Claude Code MCP registration spawn if the process ignores SIGTERM.
  - Narrow duplicate detection from broad `already exists` text to the Claude MCP `echo` server duplicate shape.
---

## Why (the friction this closes)

The first non-founder install is happening (n=0 → n=1 concierge install, target Tue 2026-06-02; runbook in `spec_refs`). The 2026-06-01 isolated foreign-install smoke test found one live hand-staging hole: **`echoctl init` wires Codex's MCP server for real but only writes _instructions_ for Claude Code — it never registers Claude Code's actual MCP connection.**

Concretely, today:
- Codex → `init` writes `[mcp_servers.echo]` into `~/.codex/config.toml` and reports `codex: ok (append, add)`. Real connection.
- Claude Code → `init` appends a `<!-- BEGIN ECHO -->` block to `~/.claude/CLAUDE.md` and reports `claude-code: ok (append, copied, copied)`. **Instructions only — no MCP server.** Claude Code is told to use ECHO MCP tools that aren't actually connected.

So on a fresh machine, `echoctl doctor`'s probe returns `mcp-not-configured` for claude-code and the user has to manually run `claude mcp add ...`. That's a visible seam in the very first concierge demo. This item makes `init` do it, and makes `doctor` print the exact one-liner if anything is still half-wired.

## What this is — and what it deliberately is NOT

This is a **friction-fix** (per the friction-first stance): close one live hand-staging hole + sharpen one diagnostic. It is NOT a generalized adapter for arbitrary MCP clients, NOT a `~/.claude.json` mutator, NOT runtime hardening, NOT new product surface.

## Locked decisions (founder + codex consult, 2026-06-01)

1. **Register via the Claude CLI, NOT by editing `~/.claude.json` directly.** Shell out to:
   ```
   claude mcp add --transport http --scope user echo http://127.0.0.1:38478/mcp
   ```
   Rationale: `~/.claude.json` is a vendor-owned file that also holds auth/session/project state — direct mutation risks corrupting it. The CLI is the least-risky writer. (Mirrors how Codex registration shells out / writes only its own `[mcp_servers.echo]` table, never the whole config.)
2. **`--scope user`, not the default.** Claude's default `mcp add` scope is `local` — a project-cwd-keyed entry in `~/.claude.json` that won't load in the coworker's other project dirs, and a stale `local` entry **shadows** user scope (precedence is local > project > user). User scope loads across all projects. Project scope writes `.mcp.json` + needs approval → wrong for an unattended install. (Source: Claude Code MCP docs, https://code.claude.com/docs/en/mcp — builder re-verifies flag spelling against the installed `claude` CLI before locking copy.)
3. **`claude` CLI missing on PATH = non-fatal.** Registration is best-effort, exactly like 073's probe: log a clear remediation line and continue; never abort the whole `init`. (Codex/Cursor are unaffected if Claude is absent.)
4. **Idempotent — surface a stale duplicate, don't silently succeed, and don't parse CLI output to auto-fix it.** The installed `claude` CLI exits **1** on a duplicate (`MCP server echo already exists in user config`) and leaves the existing entry in place (both r1 reviewers verified under a fake HOME). So exit-1 must NOT be treated as silent success. But the registration step must ALSO NOT try to auto-reconcile by parsing `claude mcp get echo`: that command is **unscoped** and, when a `local` shadow exists, reports the *local* entry (codex r2 verified), so a get-then-remove/re-add `-s user` would misclassify a local shadow and still report success. Parsing the CLI's human-readable `Scope:`/URL lines is the "ambient-output-as-API" anti-pattern (cf. the 2026-05-28 root-cause finding) and is explicitly rejected here. Instead: on exit-1, leave the entry, mark the registration `already-exists (unverified)`, and let the **probe + doctor** step be the authority on reachability — probe runs after registration and flags an unreachable/stale server, at which point the AC2 remediation copy (which carries the exact `add --scope user` + `remove echo -s local` escape hatch) tells the operator how to reconcile. The registration step must NOT report claude-code healthy on an exit-1 duplicate unless the probe independently confirms reachability. Accepted V1 limitation: if a stale user-scope URL happens to reach a *different live* ECHO daemon, probe passes and the mismatch isn't caught — that is the deferred active-shadow-detection case (OoS#8), acceptable for the n=1 install.
5. **The port/URL is the resolved `mcpServerUrl` already threaded through init** (`http://127.0.0.1:<port>/mcp`), not a hardcoded 38478 — respect `--port`/`ECHO_MCP_PORT`.

## Judgment calls (flagged for r1 reviewers)

- **J1 — placement.** Discrete step in `init.ts` (smallest, colocated with probe's spawn) vs a new `src/echo-home/adapters/claude-code-mcp.ts` invoked from `wire.ts` (consistent with the codex-config adapter; exercised by the `--answer-file` headless path AND the smoke test without going through interactive `init`). Lean: drive it from the wire path so headless/answer-file installs and `foreign-install-smoke.sh` both cover it; reviewers decide whether a named adapter module earns its keep at this size.
- **J2 — deterministic smoke-test assertion.** The sandbox has no real `claude` login. Inject a fake `claude` shim on PATH that records its argv, then assert init invoked `claude mcp add --transport http --scope user echo <url>`. (Don't assert against a real `~/.claude.json`.)
- **J3 — RESOLVED (codex-ops r1 F3): escape-hatch COPY now, active DETECTION deferred.** The `claude mcp remove echo -s local` escape-hatch *line* goes into the doctor `mcp-not-configured` remediation copy (AC2) now — it's copy-only/cheap, and the old local-default remediation makes a shadowing local entry a real field case. What stays deferred is *active detection*: doctor parsing `claude mcp get echo`/`list` to auto-detect and report a live local→user shadow. Copy now; detection later.

## Acceptance criteria

- **AC1 — init registers Claude Code's MCP server.** After `echoctl init` selects `claude-code` (interactive OR `--answer-file`), Claude Code's MCP server `echo` is registered at user scope pointing at the resolved `mcpServerUrl`, via the `claude` CLI (`claude mcp add --transport http --scope user echo <url>`). Codex/Cursor wiring is unchanged.
- **AC2 — exact doctor remediation (incl. local-shadow escape hatch).** When the claude-code probe returns `mcp-not-configured`, `echoctl doctor` prints the exact remediation instead of a generic "unhealthy" line: `claude mcp add --transport http --scope user echo <url>`, then — because the OLD remediation defaulted to `local` scope, an operator who followed it may have created a shadowing local entry the user-scope add can't beat — `claude mcp remove echo -s local` (if still degraded), then `echoctl doctor`. The escape-hatch line lives in the doctor copy itself (codex-ops r1 F3), not docs-only. The init-time remediation string (`init.ts` ~line 272) is updated to the same fully-explicit form (it currently omits `--transport http --scope user`).
- **AC3 — best-effort + non-silent duplicate + bounded spawn.** (a) `claude` missing on PATH does NOT fail `init` (clear remediation, exit success for the registration step). (b) **Duplicate is surfaced, not swallowed, and not auto-parsed** (Locked #4): on the CLI's exit-1 `already exists`, the step records `already-exists (unverified)` and relies on the probe/doctor reachability check + AC2 remediation — it does NOT parse `claude mcp get echo` to auto remove/re-add (that command is unscoped and misreports under a local shadow — codex r2). A builder must NOT report claude-code healthy on an exit-1 duplicate unless the probe independently confirms reachability. (c) **Bounded, non-interactive spawn:** the `claude mcp add` call runs under a timeout (reuse the 30s probe precedent), with stdin ignored (non-interactive) and bounded stdout/stderr captured for remediation — a present-but-blocked `claude` (interactive prompt, config lock, slow vendor startup) must NOT hang `echoctl init --answer-file` or the foreign smoke run before it reaches probe/doctor/remediation. Tests (fake `claude` shim): (i) exit-1 duplicate → init does NOT report claude-code healthy unless the (fake) probe passes; (ii) a `claude` that never exits → timeout path fires, init continues.
- **AC4 — smoke test HARD-asserts it.** `tools/foreign-install-smoke.sh` asserts claude-code MCP registration via the fake-`claude`-shim (J2). This must be a **mechanical hard-fail, not a log-only check**: the smoke script exits **non-zero with a clear diagnostic** when the recorded argv is absent or differs from `mcp add --transport http --scope user echo <url>`. (The script is currently observational — `set -uo pipefail`, output piped through `head`, no assertions; add the explicit argv assertion + non-zero exit.) Parallel to its existing codex `config.toml` check.
- **AC5 — runbook/doc reflects automation.** `docs/echoctl-install.md` drops the "manually run `claude mcp add`" workaround (now automatic) and documents user-scope + the `-s local` shadowing escape hatch as troubleshooting only.
- **AC6 — tests green.** New/updated tests for AC1–AC3 pass; full `npm test` + typecheck green. (Suggested focused set: `tests/cli/init.test.ts tests/cli/doctor.test.ts tests/echo-home/wizard/wire.test.ts`.)
- **AC7 — no scope drift.** No file outside `files_to_modify` is touched; the Out-of-Scope list is honored.

## Out of Scope (Don't Drift)

1. **No direct `~/.claude.json` mutator** — CLI only (Locked decision #1).
2. **No project-scope / `.mcp.json` support** — user scope only.
3. **No new MCP clients** (Cursor headless, Slack, GitHub, overlay) — claude-code only this item.
4. **No daemon runtime hardening** (nvm/asdf, vendoring Node) — 076 already bakes `process.execPath` + node>=22 validation into the plist; that's sufficient for V1. The bare-`node` in `scripts/launchd/install.sh` is the dev wrapper, not the packaged path — leave it.
5. **No probe-before-daemon reorder** in `init.ts`. Code fact (corrected per codex r1): `runInit` currently calls `wizard.probe(selectedAgents)` *before* `ensureDaemonRunning(...)` (init.ts ~488/497) — this item does NOT reorder it. The documented Tuesday flow runs `echoctl daemon install` first, so the daemon is already live when standalone `init` probes; the order is a non-blocker for the concierge install and reorder stays deferred. (The earlier spec wording implied daemon-before-probe was already the code order — it is not; this corrects the false code claim.)
6. **No auto-login** for `claude`/`codex` — login-wall is caught by the pre-flight gate in the runbook, not by code.
7. **No telemetry, no postinstall magic, no brew/native installer.**
8. **Active shadow-scope detection/resolution** stays out — the doctor escape-hatch *copy* is now in AC2 (codex-ops r1 F3); what's deferred is doctor parsing `claude mcp get`/`list` to auto-detect a live local→user shadow. Do not build a full scope-conflict resolver.

## After Completion (Strategist Notes)

- Update the n=1 runbook (`raw/internal/interviews/2026-06-02-coworker-n1-concierge-install.md`): "Known likely fix #1" becomes "verified automatic"; keep the `-s local` shadowing one-liner as a live-fallback only.
- Wiki: this is install-boundary behavior — fold into `wiki/surfaces/mcp-server.md` (or the echoctl/onboarding page) only once shipped, alongside 076's install-boundary page if/when written. Note that adapter wiring now registers MCP for BOTH Codex and Claude Code (parity).
- If J3 (shadow-scope detection) is deferred, append it to `backlog/_followups.md`, not a new spec — friction-first.

## Consult provenance

Gap found by `tools/foreign-install-smoke.sh` (2026-06-01). Fix shape from a Codex read-only consult (2026-06-01) over `init.ts` / `wire.ts` / `adapter-sync.ts` / `probe.ts` / `render.ts`; Codex's user-scope + CLI-not-JSON reasoning is the load-bearing call. Code claims verified against source by the strategist before speccing (wire.ts:97 Codex/Cursor-only MCP config; adapter-sync.ts:625 claude-code markdown+skills; init.ts:272 remediation string missing `--transport http --scope user`; daemon.ts:199/355 execPath + node>=22 — confirms OoS #4).

 succeeded in 209ms:
---
id: 2026-05-26-076-packaged-echoctl-install-boundary
title: "Packaged `echoctl` install boundary — npm-pack tarball, packaged daemon, launchd plist targeting `dist/`, macOS-only V1; founder can use ECHO across all projects without dev-mode dependency on the source repo"
status: pending_review
priority: HIGH
estimate: 2-3d
created: 2026-05-26
blocked_by: []
task_state_ref: 2026-05-26-076-packaged-echoctl-install-boundary
requested_reviewers: ["codex", "codex-ops"]
files_to_modify:
  - package.json                                           # AC1 + AC7 — flip `private: false`; fix `files` allowlist; add `prepack` script; bin entry already exists from 074; daemon-script changes per AC3
  - src/cli/index.ts                                       # AC3 — register new `daemon` subcommand in the parseArgs dispatch
  - src/cli/commands/daemon.ts                             # AC3 NEW — daemon lifecycle commands: install / start / stop / restart / status / logs (uses `launchctl bootstrap|bootout|kickstart` per AC4)
  - scripts/copy-sql-migrations.js                         # AC2.2 (r1 codex F4) — NEW; pure-Node walk of src/storage/migrations/ → dist/storage/migrations/ byte-copy; idempotent; fails loudly if source dir missing
  - tests/cli/daemon.test.ts                               # AC3 + AC4 NEW — unit tests for the daemon lifecycle command (test seams for launchctl calls); also covers AC3.3 preflight (r1 codex-ops F4), AC3.3 absolute-Node-path resolution (r1 codex-ops F3), AC3.3 post-bootstrap health-probe wait (r2 codex-ops F1), AC3.3 step 6+7 XML-safe + atomic plist write + plutil -lint (r5 codex-ops F2), AC3.3 step 10 / AC3.4.1 bootout-on-probe-timeout + loaded-but-unhealthy short-circuit refuses no-op (r5 codex-ops F1), AC3.4.1 restart + recovery-load start preflight + probe-wait parity (r3 codex-ops F1), AC3.8 install-time override flags (r1 codex F3 / codex-ops F2), and AC3.8 restart + logs override plumbing (r2 codex-ops F2)
  - tests/cli/shell-reachable.test.ts                      # AC5 — extend the existing pack-shape smoke to ALSO START the packaged daemon, probe /mcp, then clean up via `daemon stop $OVERRIDES` + `daemon uninstall $OVERRIDES` (NOT SIGTERM — the launchd-path cleanup is what proves the test job is removed without touching production); catches the AC2 SQL-migration bug + the AC1.4 coord-config bug
  - scripts/launchd/install.sh                             # AC3 — plist target changes from `npm run daemon` (PROJECT_DIR coupling) to `node <installed-package>/dist/daemon/index.js`; no `WorkingDirectory` set to source repo
  - scripts/launchd/uninstall.sh                           # AC4 — use `launchctl bootout` for clean stop; align with the AC4 upgrade-safe stop semantics
  - docs/echoctl-install.md                                # AC6 NEW — single canonical install doc: build → pack → install-globally → daemon-install → init → verify; upgrade path; uninstall; reset
spec_refs:
  - raw/internal/decisions/2026-05-25-echo-pro-paid-coord-layer-design.md  # §"What's deferred" — "Install topology (one install vs two; brew vs pkg vs Raycast Store)" was explicitly deferred to "after wizard UX is implemented; install is the wrapper around the wizard." 074 shipped the wizard; this spec answers the deferred question.
  - wiki/principles/felt-not-seen.md                       # line 32 explicitly permits install-once setup. A CLI installer is infrastructure, not a destination app. This spec stays inside that boundary — no dashboards, accounts, telemetry, or management UI.
  - backlog/complete/2026-05-25-074-echo-cli-binary.md     # AC1.1 + AC9 (075) `files` allowlist precedent — narrow extension allowed; this spec broadens it definitively.
  - backlog/complete/2026-05-25-075-first-demo-workflow.md # AC9 narrow allowlist extension for assets/echo-workflows/**; this spec generalizes the pattern to all runtime assets the daemon needs at install time.
  - package.json                                            # current state: `private: true` (line 4); daemon script uses `vite-node src/daemon/index.ts` (line 19) — dev path that will NOT survive a production install
  - scripts/launchd/install.sh                              # current launchd plist runs `npm run daemon` from `PROJECT_DIR` (lines 50, 55) — hard-coupled to the source tree
  - src/storage/sqlite.ts                                   # daemon's SQLite runtime expects SQL migration files BESIDE the built JS (line 17 region); `tsc` does NOT copy `src/storage/migrations/*.sql` into `dist/` — this is a real bug that breaks packed daemon startup
  - src/coord/roles.ts                                      # daemon hard-loads coord role config + schema from `tools/review-queue/` (line 56 region) — `coord-roles.json` and the reviewer schema MUST ship in the package
  - src/echo-home/adapters/skill-sync.ts                    # skills get overwritten into `~/.echo/skills/` (line 80 region); roles + workflows preserve user edits (072 + 075 design). This spec NAMES the asymmetry; does NOT redesign it.
  - tools/review-queue/coord-roles.json                     # ships in tarball per AC1
  - tools/review-queue/reviewers.json                       # ships in tarball per AC1
  - tools/review-queue/schemas/                             # whichever JSON schema files coord/roles.ts validates against; ship in tarball per AC1
  - CLAUDE.md                                               # operating-model file; this spec does not touch the operating model
claimed_by: "78D5AB0F-A8A3-4F01-BC2E-EB05961B2405"
claimed_at: "2026-05-27T06:10:32Z"
branch: "agent/packaged-echoctl-install-boundary"
worktree: "/Users/zhenye/Desktop/Project_echo--packaged-echoctl-install-boundary"
head_sha: "ad4757ddf81a75f120cc4e59968c8f5d5d220ac9"
pr_url: ""
agent_notes: "Implemented on branch `agent/packaged-echoctl-install-boundary` at `ad4757ddf81a75f120cc4e59968c8f5d5d220ac9`. Added packaged `echoctl daemon` lifecycle, npm-pack runtime allowlist and migration copy, launchd smoke coverage, and install docs. Verification: `npm run build:cli`, `npm run lint`, `npm run typecheck`, focused CLI vitest, `npm pack --pack-destination /private/tmp/echo-076-pack`, `npm test` (135 files / 1433 tests), and `git diff --check` passed. Direct sandboxed `npm pack` hit TS emit EPERM, so the explicit pack gate was rerun outside sandbox; the packaged smoke also exercises pack/install."
review_notes: |
  Merged on 2026-05-27 via founder reconciliation (sidecar verdict: merge as-is).

  Conflicts resolved:
  - None — clean merge. Sidecar predicted a conflict on the item file itself (main's reviewer-queue commit e6b0c93 vs. branch additions), but the agent's branch never touched the item-file frontmatter, so `ort` merged disjoint surfaces without contention. The move-to-complete/ happens here at C7.

  C3.5 cross-vendor consult: none invoked

  Fixups applied:
  - None (sidecar had no pre-merge fixups).

  Fixups deferred to follow-up items:
  - None.

  Verify: 1433/1454 tests pass (21 skipped, matches sidecar); lint and typecheck clean post-merge; tools/sync-skills.sh --check clean.

  Follow-up items (non-blocking, queued in backlog/_followups.md):
  - Align `package.json` `daemon:logs` script path to `echo-daemon.{err,out}.log` (or delete — superseded by `echoctl daemon logs`).
  - Implement real `uptime` in `echoctl daemon status` (parse `launchctl print` or compute from process start) — currently prints `unknown` while AC3.5 lists the field.
  - Migrate `scripts/launchd/uninstall.sh` to a thin wrapper around `echoctl daemon uninstall "$@"` for parity with `install.sh`.
  - Harness bug observed during this merge's Step-B pre-flight: the orphan-detection loop in `skills/merge-and-cleanup.md` Step B compares find-output paths (`/var/folders/...`) against `git worktree list --porcelain` paths (`/private/var/folders/...`), so registered TMPDIR worktrees older than 60min are deleted as if orphaned (macOS `/var` → `/private/var` symlink). The watcher worktree `echo-watcher-BBD8D852-…` was nuked despite being registered. Low-risk in practice (watcher lifetime is short, so stale registrations are usually genuinely orphaned), but the protection logic doesn't fire as intended — fix by canonicalizing both sides via `pwd -P` / `realpath` before string-compare.
---

# Packaged `echoctl` install boundary

## Why this spec exists

The 2026-05-25 ECHO Pro design archive (`raw/internal/decisions/2026-05-25-echo-pro-paid-coord-layer-design.md` §"What's deferred") explicitly punted the "install topology" question until after the wizard UX was built. 074 shipped the wizard. 070-075 are all in `complete/`. The deferred question is now the V1 gate, and dogfood has surfaced it cleanly:

> *"workflow isn't the important thing. i can manully trogger the spec review here and ask you to keep review until convergence. problem is that i can only use echo in this project not the rest of my project. that why i need to package it so i can actaully use it as a product instead of iterating here"* — founder, 2026-05-26

Today's reality blocks the founder from using ECHO across `NavyPowerTwin`, `PowerTwinLab_Demo`, `Statellite_Detection`, etc.:

- `package.json` is `private: true` — cannot be published or globally installed via tarball
- `scripts/launchd/install.sh:50` runs `npm run daemon` from `PROJECT_DIR` — the daemon process LITERALLY depends on the source repo being on disk
- `npm run daemon` uses `vite-node src/daemon/index.ts` — a development-time entrypoint that won't survive a production install
- Every code change requires `npm run build:cli && npm link` — the founder can never trust ECHO across other projects because the next iteration in this repo might break their daily use
- The basketball-team product story (*"ECHO is the coordination layer that turns 5 isolated AI tools into one coordinated team"*) is meaningless if the team only plays in one gym

**Codex consult (2026-05-26) reframed 076 sharply:** *"076 should create an install boundary, not a new workflow primitive. 'Use ECHO from NavyPowerTwin without touching Project_echo' is now more important than 'add spec-review fanout.'"* The parked workflow primitive becomes 077-or-later, blocked on 076.

The "all over the place" felt diagnosis from the same 2026-05-26 strategist conversation had a deeper cause that the moment-enumeration didn't surface: the felt experience IS thin because the founder is the only user, and "using" ECHO requires being a developer of ECHO. No customer can experience ECHO until packaging exists — not even the founder, in any project that isn't this one.

## Architectural sketch

```
─────────────────────────────────────────────────────────────────────────
BEFORE 076 (today)                          AFTER 076
─────────────────────────────────────────────────────────────────────────
~/Desktop/Project_echo                       ~/Desktop/Project_echo
├── (source repo — must stay on disk)        ├── (source repo — dev only;
├── npm link → ~/.npm-global/bin/echoctl     │     can move/delete without
└── npm run daemon                            │     breaking installed ECHO)
    └── vite-node src/daemon/index.ts        └── npm pack → echoctl-0.1.0.tgz
                                                  │
        ↑                                         ↓
        │                                   ──────────────────────────────
launchd plist:                              ~/.npm-global/lib/node_modules/echoctl/
  ProgramArguments:                         ├── dist/                ← packaged code
    - npm                                    │   ├── cli/index.js    ← bin target
    - run                                    │   ├── daemon/index.js ← daemon entrypoint
    - daemon                                 │   └── storage/migrations/*.sql ← AC2
  WorkingDirectory: $PROJECT_DIR            ├── skills/              ← AC1
                                            ├── assets/echo-roles/   ← AC1
                                            ├── assets/echo-workflows/ ← AC1
                                            └── tools/review-queue/  ← AC1 (coord config)
                                                ├── coord-roles.json
                                                ├── reviewers.json
                                                └── schemas/

                                            ~/.npm-global/bin/echoctl
                                              → packaged dist/cli/index.js

                                            launchd plist (rewritten by AC3):
                                              ProgramArguments:
                                                - node
                                                - ~/.npm-global/lib/node_modules/
                                                    echoctl/dist/daemon/index.js
                                              # No WorkingDirectory; no PROJECT_DIR.
                                              # KeepAlive preserved.

                                            $ echoctl daemon install   # registers plist
                                            $ echoctl daemon start     # bootstraps
                                            $ echoctl daemon status    # health check
                                            $ echoctl daemon restart   # bootout + bootstrap
                                            $ echoctl daemon stop      # bootout
                                            $ echoctl daemon logs      # tail launchd log
                                            $ echoctl daemon uninstall # bootout + plist rm

                                            # Daily use from any project:
                                            $ cd ~/Desktop/NavyPowerTwin
                                            $ echoctl run spec-review --file spec.md
                                            # Works. No source repo needed.
```

The install boundary is the difference between *"ECHO is a dev artifact I rebuild"* and *"ECHO is software I install once and rely on."* Same code; different relationship.

## Judgment calls flagged for r1 reviewer

The calls below are the spec author's picks (joint claude + codex). Reviewers should push back if any feels wrong.

- **J1. Daemon stays Node-spawned; no native binary (pkg / nexe / bun build) in V1.** Codex consult: *"Yes, the daemon can stay Node-spawned. It should run `node <installed-package>/dist/daemon/index.js`, not `npm run daemon`."* Native bundling is V1.5+ when distribution graduates beyond founder dogfood. Cost of going native now: ~3x the spec scope; cross-platform binary signing; harder upgrades. Cost of staying Node: requires Node on the user machine (founder already has it; external customers will install via brew/asdf/etc. when brew formula ships in V1.5).

- **J2. Private tarball / `npm pack` for V1, NOT public npm publish.** Codex: *"Git URL is acceptable for founder dogfood only if you add a build hook, because `dist/` is gitignored. I'd prefer tarball/pack correctness first; registry publish is a final distribution switch, not the core engineering work."* This spec ships the tarball-correctness work (the actual hard part); publishing to a registry is a single `npm publish` away once founder is happy. Public publishing also opens trademark / package-squatting concerns ("echoctl" is unclaimed on npm at time of writing); resolve those before publish.

- **J3. macOS-only (launchd). Defer Linux/systemd to V1.5+.** Codex: *"P1 'macOS + Linux' is too broad for 076. Current daemon persistence is launchd-only. Add Linux systemd after the founder proves the packaged boundary on macOS."* The founder works on a macOS laptop; the daemon's only persistence machinery today is launchd. Adding systemd is purely speculative scope until a Linux user exists.

- **J4. Skill-overwrite vs role-and-workflow-preserve asymmetry is NAMED, not redesigned.** Codex: *"Role/workflow upgrade semantics are already asymmetric: skills are overwritten into `~/.echo/skills`; roles/workflows preserve user edits. Packaging should name that, not redesign it."* 072 + 075 shipped this asymmetry on purpose (skills are ECHO-owned per `wiki` design archive; roles + workflows are user-tunable). 076 documents the upgrade semantics in `docs/echoctl-install.md` so the user knows what to expect on package upgrade; does NOT change the adapter sync behavior.

- **J5. No `postinstall` / auto-restart magic.** Codex: *"Upgrade story is explicit: reinstall/update package, then `echoctl daemon restart`. No postinstall auto-restart."* `npm install -g`'s `postinstall` hook running auto-restart is dangerous (could fight launchd's KeepAlive, could fire during package-corruption recovery, could surprise the user). Explicit `echoctl daemon restart` is the right shape — small UX cost, large safety + comprehensibility win.

- **J6. No telemetry, no cloud sync, no hosted identity, no management UI.** Per `wiki/principles/felt-not-seen.md:32`, the principle EXPLICITLY allows install-once setup as infrastructure — but turns into a "destination app" violation if packaging acquires a dashboard / accounts / phone-home / web UI. This spec stays inside the principle. `echoctl doctor --json` is the only "health reporting" surface, and it's strictly local + on-demand.

- **J7. Documentation surface kept small: ONE new install doc + README updates only.** No tutorial videos, no Discord, no Notion onboarding page in this spec. The single `docs/echoctl-install.md` covers: install / verify / use / upgrade / uninstall / reset. If the founder finds the doc inadequate during dogfood, that's a doc iteration (commits to that file), NOT a new spec.

## Acceptance Criteria

### AC1 — `npm pack` artifact contains exactly the runtime files; nothing dev-only

**AC1.1 — `files` allowlist (in `package.json`).** Replace the current allowlist (set by 074 + extended by 075 for `assets/echo-workflows/**`) with the complete runtime set:

```json
"files": [
  "dist/**/*.js",
  "dist/**/*.d.ts",
  "dist/**/*.sql",
  "skills/**",
  "assets/echo-roles/**",
  "assets/echo-workflows/**",
  "tools/review-queue/coord-roles.json",
  "tools/review-queue/reviewers.json",
  "tools/review-queue/schemas/**",
  "package.json",
  "README.md"
]
```

Each entry exists because the runtime (CLI or daemon) needs it at runtime. Specifically:

- `dist/**/*.js` + `dist/**/*.d.ts` — CLI + daemon built output (074 + this spec's AC2)
- `dist/**/*.sql` — runtime SQL migrations (AC2 builds these into `dist/`)
- `skills/**` — the ECHO skill library; `~/.echo/skills/` is populated from this on `echoctl init` per 072 (skills are ECHO-owned; overwritten on upgrade per J4)
- `assets/echo-roles/**` — default role TOMLs per 071; copied to `~/.echo/roles/` on init (preserved on upgrade per J4)
- `assets/echo-workflows/**` — default workflow TOMLs per 075; copied to `~/.echo/workflows/` on init (preserved on upgrade per J4)
- `tools/review-queue/coord-roles.json` + `reviewers.json` + `schemas/**` — the daemon's `src/coord/roles.ts` (line ~56) hard-loads coord role config + schema from these paths AT DAEMON STARTUP. Missing them = daemon crash. This is a real bug codex caught.

**AC1.2 — Tarball negative-assertion.** Verify the tarball does NOT contain: `backlog/`, `raw/`, `wiki/`, `tests/`, `src/`, `tools/review-queue/_*.{sh,py}` (the scripts are dev-only; only the config files + schemas ship), `tools/review-queue/*.py`, `tools/review-queue/*.sh`, `node_modules/`, `coverage/`, `dist/**/*.test.{js,d.ts}`. AC5's pack-shape smoke asserts presence + absence.

**AC1.3 — Tarball is self-contained.** A fresh machine with Node ≥ 22 + `npm` installed should be able to: `npm install -g <tarball-path>` → `echoctl --version` succeeds → `echoctl --help` lists commands → `echoctl doctor` runs (returns `broken` because `~/.echo` is absent / the daemon is not installed yet — this matches current `src/cli/commands/doctor.ts` semantics; per r2 codex F4 LOW, the spec does not change doctor's health vocabulary, only asserts the no-daemon-yet case lands on `broken`).

**AC1.4 — Coord runtime config ships explicitly.** `tools/review-queue/coord-roles.json` + `tools/review-queue/reviewers.json` + `tools/review-queue/schemas/**` MUST be in the tarball (called out separately from AC1.1's list because they were the second bug codex caught — the daemon's `src/coord/roles.ts:56` validates against the JSON schemas at startup). AC5's pack-shape smoke includes a positive presence check for these specific paths.

**AC1.5 — `coord_invoke` is implicitly de-scoped in packaged installs (no code change required) (r1 codex F1 HIGH; r2 codex F2 HIGH — removal over deeper patching).** AC1.2 excludes `tools/review-queue/*.sh` from the tarball as dev-only. The daemon's `coord_invoke` tool resolves `tools/review-queue/run-<role>-reviewer.sh` via `src/coord/paths.ts:63-146` and requires the wrapper to exist + be executable before spawning a reviewer. In a packaged install those wrappers are absent.

The packaged daemon already handles this correctly without any code change: `src/coord/paths.ts` raises `CoordPathError` when the wrapper resolves to a non-existent path; `src/mcp/tools/coord-invoke.ts` catches `CoordPathError` and returns a text-only `isError` JSON-RPC response. From the caller's perspective, `coord_invoke` cleanly rejects in a packaged install — the daemon stays up, no crash, no silent failure. This IS the de-scope mechanism.

(r2 codex F2 caught r1's overreach: r1 added a code-change AC requiring a structured machine-readable code `ECHO_COORD_INVOKE_PACKAGED_UNAVAILABLE`, which contradicts this spec's §Out-of-Scope ban on touching `src/coord/` and `src/mcp/`. Removal — relying on existing CoordPathError → isError behavior — is the cleaner disposition. V1 has no customer surface that depends on machine-readable rejection codes; the existing text response is sufficient.)

Rationale (unchanged): the V1 cross-project use case (founder using `echoctl` in `NavyPowerTwin`, `PowerTwinLab_Demo`, etc.) never invokes the operating-model review queue — those wrappers are infrastructure for THIS repo's strategist/reviewer protocol, not for end-user echoctl. De-scoping is the correct boundary; shipping all 4 `run-<role>-reviewer.sh` wrappers + their `_lib.py`/`coord-emit.sh`/`reviewers.json`/launchd plumbing would push the tarball outside its V1 purpose.

Test surface: AC5.1 step 4 adds a positive assertion that a packaged-install `coord_invoke` call for any role returns `isError: true` with a CoordPathError-shaped message (proves the de-scope mechanism is intact end-to-end against the real packaged daemon). No new unit-test file or `src/coord/`/`src/mcp/` code change is in scope.

### AC2 — Build artifacts are complete + runtime-correct

**AC2.1 — SQL migrations copied into `dist/`.** Today, `tsc` compiles `.ts` → `.js` into `dist/` but ignores `.sql` files. The daemon's `src/storage/sqlite.ts` (line ~17 region) reads `src/storage/migrations/*.sql` at runtime to apply schema. In a packed install, those `.sql` files don't exist relative to `dist/daemon/index.js` → daemon crashes on first DB open.

Fix: add a `prepack` script (or extend `build:cli`) that runs `tsc -p tsconfig.cli.json` AND copies `src/storage/migrations/*.sql` → `dist/storage/migrations/*.sql`. Update `src/storage/sqlite.ts` to resolve migration paths via `import.meta.url` so the lookup works against `dist/storage/migrations/` post-pack AND `src/storage/migrations/` in dev (the relative path computed from `import.meta.url` lands in different absolute roots but the same relative subdir works for both).

Concrete addition to `package.json`:

```json
"scripts": {
  "build:cli": "tsc -p tsconfig.cli.json && node scripts/copy-sql-migrations.js",
  "prepack": "npm run build:cli"
}
```

The `prepack` hook fires before `npm pack` AND before `npm publish`, so any future registry push is automatic.

**AC2.2 — Migration-copy script.** A small `scripts/copy-sql-migrations.js` (or `.mjs`) that walks `src/storage/migrations/` and writes byte-for-byte copies to `dist/storage/migrations/`. Pure Node, no extra dependencies. Idempotent. If the source dir is missing, fails with a clear error (not a silent no-op).

**AC2.3 — Verify `src/storage/sqlite.ts` migration-path resolution is runtime-correct after pack (r3 codex F3 LOW — verification, not required code change).** The current pinned source already resolves `MIGRATIONS_DIR` via `import.meta.url` + `fileURLToPath` + `path.dirname` next to the runtime file, which means the existing lookup naturally points at `dist/storage/migrations/` post-pack (after AC2.1 copies the SQL files there) AND at `src/storage/migrations/` in dev (vitest source-resolution). No code change is required UNLESS the builder discovers a failing test or a packed-runtime path mismatch. The load-bearing fix in this spec is AC2.2's `scripts/copy-sql-migrations.js` (puts the `.sql` files into `dist/` so the existing resolver finds them); `src/storage/sqlite.ts` is a verification point, not mandatory churn. If the resolver IS found broken in some way that AC5.1's smoke surfaces, the fix is the minimal `import.meta.url` patch described above — and AC5.1 is what catches the regression, so no separate test is needed.

### AC3 — `echoctl daemon` subcommand (lifecycle management)

**AC3.1 — Public surface.** New subcommand `daemon` registered in `src/cli/index.ts` (extend the existing `parseArgs` dispatch + help text). The `daemon` subcommand takes a verb:

```
echoctl daemon install      # writes launchd plist + bootstraps the job
echoctl daemon start         # bootstraps if not loaded; no-op if running
echoctl daemon stop          # bootouts (clean stop; respects KeepAlive)
echoctl daemon restart       # bootout + bootstrap (atomic; safe under upgrades)
echoctl daemon status        # prints PID + uptime + plist path + port + 1-line health
echoctl daemon logs [--tail N | --follow]  # tails the launchd-configured stdout/stderr log files
echoctl daemon uninstall     # bootouts + removes plist
```

`echoctl daemon` (no verb) prints subcommand help. `--help` per verb prints verb-specific help.


 succeeded in 215ms:
     1	import {
     2	  existsSync,
     3	  lstatSync,
     4	  mkdirSync,
     5	  readFileSync,
     6	  unlinkSync,
     7	  linkSync,
     8	  writeFileSync,
     9	} from 'node:fs';
    10	import { homedir } from 'node:os';
    11	import * as os from 'node:os';
    12	import { dirname, isAbsolute, resolve, sep } from 'node:path';
    13	import { fileURLToPath } from 'node:url';
    14	import { randomUUID } from 'node:crypto';
    15	
    16	import { ECHO_HOME_PATHS } from './paths.js';
    17	import { DEFAULT_ROLE_FILENAMES } from './roles.js';
    18	import { mergeWithMarkers, type MarkerResult } from './adapters/markers.js';
    19	import { syncCodexMcpBlock, RenderError, TomlParseError } from './adapters/codex-config.js';
    20	import {
    21	  registerClaudeCodeMcpServer,
    22	  type ClaudeCodeMcpRegisterDeps,
    23	} from './adapters/claude-code-mcp.js';
    24	import { syncCursorMcpEntry, CursorJsonParseError } from './adapters/cursor-config.js';
    25	import {
    26	  populateEchoSkills,
    27	  syncClaudeSkills,
    28	  type PopulateEchoSkillsResult,
    29	} from './adapters/skill-sync.js';
    30	import { syncDefaultRoles, type RoleSyncResult } from './adapters/role-sync.js';
    31	import { syncDefaultWorkflows, type WorkflowSyncResult } from './adapters/workflow-sync.js';
    32	import { AtomicWriteError } from './adapters/atomic-write.js';
    33	import type { InstallProfile } from './paths.js';
    34	
    35	// =============================================================================
    36	// Public types
    37	// =============================================================================
    38	
    39	export type AgentKind = 'codex' | 'claude-code' | 'cursor';
    40	
    41	export interface AdapterSyncProfile {
    42	  kind: AgentKind;
    43	  paths?: {
    44	    configFile?: string;
    45	    instructionsFile?: string;
    46	    commandsDir?: string;
    47	  };
    48	  echoSection?: string;
    49	  previousEchoSection?: string;
    50	  mcpServerConfig?: { url: string; [k: string]: unknown };
    51	  previousMcpServerConfig?: Record<string, unknown>;
    52	  claudeCodeMcpRegistration?: ClaudeCodeMcpRegisterDeps;
    53	  force?: boolean;
    54	}
    55	
    56	export interface SyncAllOpts {
    57	  echoMcpUrl?: string;
    58	  repoRoot?: string;
    59	  repoSkillsDir?: string;
    60	  rolesSourceDir?: string;
    61	  defaultRoles?: readonly string[];
    62	  workflowsSourceDir?: string;
    63	  defaultWorkflows?: readonly string[];
    64	  allowUserModifiedRoles?: boolean;
    65	  profile?: InstallProfile;
    66	}
    67	
    68	export type AdapterErrorCode =
    69	  | 'EACCES'
    70	  | 'ENOSPC'
    71	  | 'ENOTDIR'
    72	  | 'ENOENT'
    73	  | 'EISDIR'
    74	  | 'EROFS'
    75	  | 'EEXIST'
    76	  | 'ELOOP'
    77	  | 'PARSE_ERROR'
    78	  | 'RETRY_CONFLICT'
    79	  | 'MISSING_REQUIRED_INPUT'
    80	  | 'UNSUPPORTED_VALUE'
    81	  | 'UNKNOWN';
    82	
    83	export interface AdapterError {
    84	  code: AdapterErrorCode;
    85	  file: string;
    86	  operation: 'read' | 'parse' | 'write' | 'rename' | 'stat' | 'link' | 'mkdir';
    87	  message: string;
    88	  field?: string;
    89	  type?: string;
    90	}
    91	
    92	export interface ConfigConflict {
    93	  kind: 'config';
    94	  filePath: string;
    95	  currentValue?: unknown;
    96	  expectedValue?: unknown;
    97	  proposedValue?: unknown;
    98	  unifiedDiff?: string;
    99	}
   100	
   101	export interface MarkerConflict {
   102	  kind: 'marker';
   103	  filePath: string;
   104	  currentInside: string;
   105	  expectedInside?: string;
   106	  proposedInside: string;
   107	  unifiedDiff: string;
   108	}
   109	
   110	export interface TargetSymlinkConflict {
   111	  kind: 'target-symlink';
   112	  filePath: string;
   113	  targetIsSymlink: true;
   114	}
   115	
   116	export interface MalformedMarkerConflict {
   117	  kind: 'malformed-marker';
   118	  filePath: string;
   119	}
   120	
   121	export type SyncConflict =
   122	  | ConfigConflict
   123	  | MarkerConflict
   124	  | TargetSymlinkConflict
   125	  | MalformedMarkerConflict;
   126	
   127	export type AgentResult =
   128	  | {
   129	      agent: AgentKind;
   130	      ok: true;
   131	      files_written: string[];
   132	      actions: Array<{ file: string; action: string }>;
   133	      skipped?: string[];
   134	    }
   135	  | {
   136	      agent: AgentKind;
   137	      ok: false;
   138	      conflicts: SyncConflict[];
   139	      errors: AdapterError[];
   140	      files_written: string[];
   141	      skipped?: string[];
   142	    };
   143	
   144	export interface SyncResult {
   145	  skillsPopulated: PopulateEchoSkillsResult;
   146	  agents: AgentResult[];
   147	  roles: RoleSyncResult;
   148	  workflowsResult?: WorkflowSyncResult;
   149	  syncLock?: AdapterError;
   150	  repoRoot?: AdapterError;
   151	  directorySymlink?: AdapterError;
   152	  overallOk: boolean;
   153	}
   154	
   155	// =============================================================================
   156	// Helpers
   157	// =============================================================================
   158	
   159	function errnoCode(err: unknown): AdapterErrorCode {
   160	  if (err instanceof Error && 'code' in err) {
   161	    const code = (err as NodeJS.ErrnoException).code;
   162	    if (
   163	      code === 'EACCES' ||
   164	      code === 'ENOSPC' ||
   165	      code === 'ENOTDIR' ||
   166	      code === 'ENOENT' ||
   167	      code === 'EISDIR' ||
   168	      code === 'EROFS' ||
   169	      code === 'EEXIST' ||
   170	      code === 'ELOOP'
   171	    ) {
   172	      return code;
   173	    }
   174	  }
   175	  return 'UNKNOWN';
   176	}
   177	
   178	function shellQuote(s: string): string {
   179	  return `'${s.replace(/'/g, `'\\''`)}'`;
   180	}
   181	
   182	function shellEscapedRmHint(lockPath: string): string {
   183	  return `lockfile present at ${JSON.stringify(lockPath)} — if no other ECHO sync is running, remove it with: rm -- ${shellQuote(lockPath)}. echo doctor will automate this in a future release.`;
   184	}
   185	
   186	function emptyOkResult(targetDir: string): PopulateEchoSkillsResult {
   187	  return { ok: true, copied: [], skipped: [], targetDir };
   188	}
   189	
   190	function lockUnavailablePopulateResult(): PopulateEchoSkillsResult {
   191	  return { ok: false, sourceDir: '', targetDir: '', error: 'sync_skipped:lock_unavailable' };
   192	}
   193	
   194	const DEFAULT_WORKFLOW_FILENAMES = ['change-review.toml'] as const;
   195	
   196	function skippedRoles(defaultRoles: readonly string[]): RoleSyncResult {
   197	  return {
   198	    results: defaultRoles.map((role) => ({ role, action: 'noop' as const })),
   199	    rolesErrors: [],
   200	  };
   201	}
   202	
   203	function skippedWorkflows(defaultWorkflows: readonly string[]): WorkflowSyncResult {
   204	  return {
   205	    results: defaultWorkflows.map((workflow) => ({ workflow, action: 'noop' as const })),
   206	    workflowsErrors: [],
   207	  };
   208	}
   209	
   210	// -----------------------------------------------------------------------------
   211	// Repo-root resolution
   212	// -----------------------------------------------------------------------------
   213	
   214	function walkUpwardForRepoRoot(startDir: string): string | null {
   215	  let cur = startDir;
   216	  for (let i = 0; i < 64; i++) {
   217	    try {
   218	      const hasPkg = existsSync(`${cur}${sep}package.json`);
   219	      const hasEchoSkills = existsSync(`${cur}${sep}assets${sep}echo-skills`);
   220	      if (hasPkg && hasEchoSkills) return cur;
   221	    } catch {
   222	      // continue
   223	    }
   224	    const parent = dirname(cur);
   225	    if (parent === cur) return null;
   226	    cur = parent;
   227	  }
   228	  return null;
   229	}
   230	
   231	function resolveRepoRoot(opts: SyncAllOpts): string | null {
   232	  if (opts.repoRoot !== undefined) return resolve(opts.repoRoot);
   233	  // Use the current file path as the starting point — fileURLToPath of import.meta.url.
   234	  let startDir: string;
   235	  try {
   236	    startDir = dirname(fileURLToPath(import.meta.url));
   237	  } catch {
   238	    startDir = process.cwd();
   239	  }
   240	  return walkUpwardForRepoRoot(startDir);
   241	}
   242	
   243	// -----------------------------------------------------------------------------
   244	// Directory-component symlink guard (AC6a)
   245	// -----------------------------------------------------------------------------
   246	
   247	function assertPathComponentsAreNotSymlinks(
   248	  dirPath: string,
   249	  rootBoundary: string,
   250	): AdapterError | null {
   251	  const resolvedRoot = resolve(rootBoundary);
   252	  const resolvedDir = resolve(dirPath);
   253	
   254	  // Build the components from rootBoundary down to dirPath, inclusive.
   255	  // If dirPath does not extend through rootBoundary, we just check both.
   256	  const components: string[] = [];
   257	  if (resolvedDir === resolvedRoot) {
   258	    components.push(resolvedRoot);
   259	  } else if (resolvedDir.startsWith(`${resolvedRoot}${sep}`)) {
   260	    components.push(resolvedRoot);
   261	    const rel = resolvedDir.slice(resolvedRoot.length + 1).split(sep);
   262	    let cur = resolvedRoot;
   263	    for (const part of rel) {
   264	      cur = `${cur}${sep}${part}`;
   265	      components.push(cur);
   266	    }
   267	  } else {
   268	    // Not nested — check root and leaf separately.
   269	    components.push(resolvedRoot);
   270	    if (resolvedDir !== resolvedRoot) components.push(resolvedDir);
   271	  }
   272	
   273	  if (components.length > 64) {
   274	    return {
   275	      code: 'UNKNOWN',
   276	      file: dirPath,
   277	      operation: 'stat',
   278	      message: 'path-component walk exceeded depth bound (64)',
   279	    };
   280	  }
   281	
   282	  for (const c of components) {
   283	    let lst: ReturnType<typeof lstatSync>;
   284	    try {
   285	      lst = lstatSync(c);
   286	    } catch (err) {
   287	      const code = errnoCode(err);
   288	      if (code === 'ENOENT') continue; // non-existent leaf is fine
   289	      return {
   290	        code,
   291	        file: c,
   292	        operation: 'stat',
   293	        message: `lstat of ${c} failed during symlink guard: ${(err as Error).message}`,
   294	      };
   295	    }
   296	    if (lst.isSymbolicLink()) {
   297	      return {
   298	        code: 'EEXIST',
   299	        file: c,
   300	        operation: 'stat',
   301	        message: `path component is a symlink — refusing to operate: ${c}`,
   302	      };
   303	    }
   304	  }
   305	  return null;
   306	}
   307	
   308	// -----------------------------------------------------------------------------
   309	// Per-user advisory lock
   310	// -----------------------------------------------------------------------------
   311	
   312	interface LockHandle {
   313	  lockPath: string;
   314	  acquisitionToken: string;
   315	  exitHandler: () => void;
   316	  sigintHandler: () => void;
   317	  sigtermHandler: () => void;
   318	}
   319	
   320	function releaseLockIfOwned(lockPath: string, myToken: string): void {
   321	  try {
   322	    const raw = readFileSync(lockPath, 'utf-8');
   323	    const meta = JSON.parse(raw) as { acquisitionToken?: string };
   324	    if (meta?.acquisitionToken === myToken) {
   325	      unlinkSync(lockPath);
   326	    }
   327	  } catch {
   328	    // best-effort
   329	  }
   330	}
   331	
   332	function acquireLock(stateDir: string): { handle: LockHandle } | { error: AdapterError } {
   333	  try {
   334	    mkdirSync(stateDir, { recursive: true });
   335	  } catch (err) {
   336	    return {
   337	      error: {
   338	        code: errnoCode(err),
   339	        file: stateDir,
   340	        operation: 'mkdir',
   341	        message: `failed to ensure state dir at ${stateDir}: ${(err as Error).message}`,
   342	      },
   343	    };
   344	  }
   345	
   346	  const lockPath = `${stateDir}${sep}adapter-sync.lock`;
   347	  const acquisitionToken = randomUUID();
   348	  const tempPath = `${lockPath}.${process.pid}.${randomUUID().slice(0, 8)}.tmp`;
   349	  const meta = {
   350	    pid: process.pid,
   351	    hostname: os.hostname(),
   352	    started_at: new Date().toISOString(),
   353	    acquisitionToken,
   354	  };
   355	
   356	  try {
   357	    writeFileSync(tempPath, JSON.stringify(meta), { mode: 0o600 });
   358	  } catch (err) {
   359	    try {
   360	      unlinkSync(tempPath);
   361	    } catch {
   362	      // best-effort
   363	    }
   364	    return {
   365	      error: {
   366	        code: errnoCode(err),
   367	        file: tempPath,
   368	        operation: 'write',
   369	        message: `failed to write lock temp file: ${(err as Error).message}`,
   370	      },
   371	    };
   372	  }
   373	
   374	  try {
   375	    linkSync(tempPath, lockPath);
   376	  } catch (err) {
   377	    try {
   378	      unlinkSync(tempPath);
   379	    } catch {
   380	      // best-effort
   381	    }
   382	    const code = errnoCode(err);
   383	    if (code === 'EEXIST') {
   384	      return {
   385	        error: {
   386	          code: 'RETRY_CONFLICT',
   387	          file: lockPath,
   388	          operation: 'link',
   389	          message: shellEscapedRmHint(lockPath),
   390	        },
   391	      };
   392	    }
   393	    return {
   394	      error: {
   395	        code,
   396	        file: lockPath,
   397	        operation: 'link',
   398	        message: `failed to acquire lock: ${(err as Error).message}`,
   399	      },
   400	    };
   401	  }
   402	  try {
   403	    unlinkSync(tempPath);
   404	  } catch {
   405	    // best-effort
   406	  }
   407	
   408	  const exitHandler = (): void => releaseLockIfOwned(lockPath, acquisitionToken);
   409	  const sigintHandler = (): void => {
   410	    releaseLockIfOwned(lockPath, acquisitionToken);
   411	    process.exit(130);
   412	  };
   413	  const sigtermHandler = (): void => {
   414	    releaseLockIfOwned(lockPath, acquisitionToken);
   415	    process.exit(143);
   416	  };
   417	  process.once('exit', exitHandler);
   418	  process.once('SIGINT', sigintHandler);
   419	  process.once('SIGTERM', sigtermHandler);
   420	
   421	  return { handle: { lockPath, acquisitionToken, exitHandler, sigintHandler, sigtermHandler } };
   422	}
   423	
   424	function releaseLock(handle: LockHandle): void {
   425	  process.removeListener('exit', handle.exitHandler);
   426	  process.removeListener('SIGINT', handle.sigintHandler);
   427	  process.removeListener('SIGTERM', handle.sigtermHandler);
   428	  releaseLockIfOwned(handle.lockPath, handle.acquisitionToken);
   429	}
   430	
   431	// -----------------------------------------------------------------------------
   432	// Default per-agent path resolution
   433	// -----------------------------------------------------------------------------
   434	
   435	function defaultConfigFile(kind: AgentKind): string | undefined {
   436	  if (kind === 'codex') return resolve(homedir(), '.codex/config.toml');
   437	  if (kind === 'cursor') return resolve(homedir(), '.cursor/mcp.json');
   438	  return undefined;
   439	}
   440	
   441	function defaultInstructionsFile(kind: AgentKind): string | undefined {
   442	  if (kind === 'codex') return resolve(homedir(), '.codex/AGENTS.md');
   443	  if (kind === 'claude-code') return resolve(homedir(), '.claude/CLAUDE.md');
   444	  return undefined;
   445	}
   446	
   447	function defaultCommandsDir(kind: AgentKind): string | undefined {
   448	  if (kind === 'claude-code') return resolve(homedir(), '.claude/commands');
   449	  return undefined;
   450	}
   451	
   452	function isString(v: unknown): v is string {
   453	  return typeof v === 'string';
   454	}
   455	
   456	// -----------------------------------------------------------------------------
   457	// syncAll
   458	// -----------------------------------------------------------------------------
   459	
   460	export async function syncAll(
   461	  profiles: AdapterSyncProfile[],
   462	  opts: SyncAllOpts = {},
   463	): Promise<SyncResult> {
   464	  const installProfile = opts.profile ?? 'dogfood';
   465	  // Step 1: directory-symlink guard.
   466	  const dirChecks: Array<{ path: string; boundary: string }> = [
   467	    { path: ECHO_HOME_PATHS.skills, boundary: ECHO_HOME_PATHS.root },
   468	    { path: ECHO_HOME_PATHS.state, boundary: ECHO_HOME_PATHS.root },
   469	  ];
   470	  if (installProfile === 'dogfood') {
   471	    dirChecks.push(
   472	      { path: ECHO_HOME_PATHS.roles, boundary: ECHO_HOME_PATHS.root },
   473	      { path: ECHO_HOME_PATHS.workflows, boundary: ECHO_HOME_PATHS.root },
   474	    );
   475	  }
   476	  for (const profile of profiles) {
   477	    const commandsDir = profile.paths?.commandsDir ?? defaultCommandsDir(profile.kind);
   478	    if (isString(commandsDir)) {
   479	      dirChecks.push({ path: commandsDir, boundary: dirname(commandsDir) });
   480	    }
   481	    const instructionsFile =
   482	      profile.paths?.instructionsFile ?? defaultInstructionsFile(profile.kind);
   483	    if (isString(instructionsFile)) {
   484	      const d = dirname(instructionsFile);
   485	      dirChecks.push({ path: d, boundary: dirname(d) });
   486	    }
   487	    const configFile = profile.paths?.configFile ?? defaultConfigFile(profile.kind);
   488	    if (isString(configFile)) {
   489	      const d = dirname(configFile);
   490	      dirChecks.push({ path: d, boundary: dirname(d) });
   491	    }
   492	  }
   493	  for (const check of dirChecks) {
   494	    const err = assertPathComponentsAreNotSymlinks(check.path, check.boundary);
   495	    if (err !== null) {
   496	      return {
   497	        skillsPopulated: lockUnavailablePopulateResult(),
   498	        agents: [],
   499	        roles: { results: [], rolesErrors: [] },
   500	        directorySymlink: err,
   501	        overallOk: false,
   502	      };
   503	    }
   504	  }
   505	
   506	  // Step 2: resolve repo root.
   507	  const repoRootPath = resolveRepoRoot(opts);
   508	  if (repoRootPath === null) {
   509	    let initial = '';
   510	    try {
   511	      initial = dirname(fileURLToPath(import.meta.url));
   512	    } catch {
   513	      initial = process.cwd();
   514	    }
   515	    return {
   516	      skillsPopulated: lockUnavailablePopulateResult(),
   517	      agents: [],
   518	      roles: { results: [], rolesErrors: [] },
   519	      repoRoot: {
   520	        code: 'UNKNOWN',
   521	        file: initial,
   522	        operation: 'stat',
   523	        message:
   524	          'could not locate repo root (no package.json + assets/echo-skills/ adjacent); caller must pass opts.repoRoot',
   525	      },
   526	      overallOk: false,
   527	    };
   528	  }
   529	
   530	  const repoSkillsDir = opts.repoSkillsDir ?? resolve(repoRootPath, 'assets/echo-skills');
   531	  const rolesSourceDir = opts.rolesSourceDir ?? resolve(repoRootPath, 'assets/echo-roles');
   532	  const workflowsSourceDir =
   533	    opts.workflowsSourceDir ?? resolve(repoRootPath, 'assets/echo-workflows');
   534	  const defaultRoles = opts.defaultRoles ?? DEFAULT_ROLE_FILENAMES;
   535	  const defaultWorkflows = opts.defaultWorkflows ?? DEFAULT_WORKFLOW_FILENAMES;
   536	
   537	  // Step 3: acquire lock.
   538	  const lockResult = acquireLock(ECHO_HOME_PATHS.state);
   539	  if ('error' in lockResult) {
   540	    return {
   541	      skillsPopulated: lockUnavailablePopulateResult(),
   542	      agents: [],
   543	      roles: { results: [], rolesErrors: [] },
   544	      syncLock: lockResult.error,
   545	      overallOk: false,
   546	    };
   547	  }
   548	  const lockHandle = lockResult.handle;
   549	
   550	  let skillsPopulated: PopulateEchoSkillsResult = emptyOkResult(ECHO_HOME_PATHS.skills);
   551	  const agents: AgentResult[] = [];
   552	  let roles: RoleSyncResult = { results: [], rolesErrors: [] };
   553	  let workflowsResult: WorkflowSyncResult | undefined;
   554	
   555	  try {
   556	    // Step 4: populate skills.
   557	    skillsPopulated = populateEchoSkills({
   558	      sourceDir: repoSkillsDir,
   559	      targetDir: ECHO_HOME_PATHS.skills,
   560	      profile: installProfile,
   561	    });
   562	
   563	    // Step 5: per-agent dispatch.
   564	    for (const profile of profiles) {
   565	      agents.push(await dispatchAgent(profile, skillsPopulated.ok === true));
   566	    }
   567	
   568	    if (installProfile === 'dogfood') {
   569	      // Step 6: roles once.
   570	      roles = syncDefaultRoles({
   571	        sourceDir: rolesSourceDir,
   572	        targetDir: ECHO_HOME_PATHS.roles,
   573	        defaults: defaultRoles,
   574	      });
   575	
   576	      // Step 7: workflows once.
   577	      workflowsResult = syncDefaultWorkflows({
   578	        sourceDir: workflowsSourceDir,
   579	        targetDir: ECHO_HOME_PATHS.workflows,
   580	        defaults: defaultWorkflows,
   581	      });
   582	    } else {
   583	      roles = skippedRoles(defaultRoles);
   584	      workflowsResult = skippedWorkflows(defaultWorkflows);
   585	    }
   586	  } finally {
   587	    releaseLock(lockHandle);
   588	  }
   589	
   590	  const overallOk = computeOverallOk(
   591	    skillsPopulated,
   592	    agents,
   593	    roles,
   594	    workflowsResult,
   595	    defaultWorkflows,
   596	    opts,
   597	  );
   598	
   599	  return { skillsPopulated, agents, roles, workflowsResult, overallOk };
   600	}
   601	
   602	function computeOverallOk(
   603	  skillsPopulated: PopulateEchoSkillsResult,
   604	  agents: AgentResult[],
   605	  roles: RoleSyncResult,
   606	  workflowsResult: WorkflowSyncResult | undefined,
   607	  defaultWorkflows: readonly string[],
   608	  opts: SyncAllOpts,
   609	): boolean {
   610	  if (skillsPopulated.ok !== true) return false;
   611	  if (skillsPopulated.copied.length < 1) return false;
   612	  for (const a of agents) {
   613	    if (a.ok !== true) return false;
   614	  }
   615	  for (const r of roles.results) {
   616	    if (r.action === 'copied' || r.action === 'noop') continue;
   617	    if (r.action === 'user-modified' && opts.allowUserModifiedRoles === true) continue;
   618	    return false;
   619	  }
   620	  if (workflowsResult === undefined) return false;
   621	  if (workflowsResult.workflowsErrors.length > 0) return false;
   622	  const requiredWorkflows = new Set(defaultWorkflows);
   623	  for (const r of workflowsResult.results) {
   624	    if (r.action === 'copied' || r.action === 'noop' || r.action === 'user-modified') continue;
   625	    if (r.action === 'source-missing' && requiredWorkflows.has(r.workflow)) return false;
   626	    if (r.action === 'error') return false;
   627	  }
   628	  return true;
   629	}
   630	
   631	// -----------------------------------------------------------------------------
   632	// Per-agent dispatch
   633	// -----------------------------------------------------------------------------
   634	
   635	async function dispatchAgent(
   636	  profile: AdapterSyncProfile,
   637	  skillsAvailable: boolean,
   638	): Promise<AgentResult> {
   639	  const filesWritten: string[] = [];
   640	  const actions: Array<{ file: string; action: string }> = [];
   641	  const conflicts: SyncConflict[] = [];
   642	  const errors: AdapterError[] = [];
   643	  const skipped: string[] = [];
   644	
   645	  const configFile = profile.paths?.configFile ?? defaultConfigFile(profile.kind);
   646	  const instructionsFile = profile.paths?.instructionsFile ?? defaultInstructionsFile(profile.kind);
   647	  const commandsDir = profile.paths?.commandsDir ?? defaultCommandsDir(profile.kind);
   648	
   649	  if (profile.kind === 'codex') {
   650	    handleMarkersForAgent(
   651	      'codex',
   652	      profile,
   653	      instructionsFile,
   654	      filesWritten,
   655	      actions,
   656	      conflicts,
   657	      errors,
   658	    );
   659	    handleCodexConfig(profile, configFile, filesWritten, actions, conflicts, errors);
   660	  } else if (profile.kind === 'claude-code') {
   661	    handleMarkersForAgent(
   662	      'claude-code',
   663	      profile,
   664	      instructionsFile,
   665	      filesWritten,
   666	      actions,
   667	      conflicts,
   668	      errors,
   669	    );
   670	    if (skillsAvailable) {
   671	      handleClaudeSkills(commandsDir, filesWritten, actions, errors, skipped);
   672	    } else {
   673	      skipped.push('syncClaudeSkills');
   674	    }
   675	    await handleClaudeCodeMcpRegistration(profile, actions, skipped);
   676	  } else if (profile.kind === 'cursor') {
   677	    handleCursorConfig(profile, configFile, filesWritten, actions, conflicts, errors);
   678	  }
   679	
   680	  if (conflicts.length > 0 || errors.length > 0) {
   681	    return {
   682	      agent: profile.kind,
   683	      ok: false,
   684	      conflicts,
   685	      errors,
   686	      files_written: filesWritten,
   687	      skipped: skipped.length > 0 ? skipped : undefined,
   688	    };
   689	  }
   690	  return {
   691	    agent: profile.kind,
   692	    ok: true,
   693	    files_written: filesWritten,
   694	    actions,
   695	    skipped: skipped.length > 0 ? skipped : undefined,
   696	  };
   697	}
   698	
   699	async function handleClaudeCodeMcpRegistration(
   700	  profile: AdapterSyncProfile,
   701	  actions: Array<{ file: string; action: string }>,
   702	  skipped: string[],
   703	): Promise<void> {
   704	  const url = profile.mcpServerConfig?.url;
   705	  if (!isString(url)) {
   706	    skipped.push('claudeCodeMcpRegistration');
   707	    return;
   708	  }
   709	  const result = await registerClaudeCodeMcpServer(url, profile.claudeCodeMcpRegistration);
   710	  let action: string = result.action;
   711	  if (result.action === 'error' && result.exitCode !== undefined) {
   712	    action = `error exit-${result.exitCode}`;
   713	  }
   714	  if (result.detail !== undefined && result.detail.length > 0) {
   715	    action = `${action}: ${result.detail.replace(/\s+/g, ' ')}`;
   716	  }
   717	  actions.push({ file: 'claude:mcp:echo', action });
   718	}
   719	
   720	function handleMarkersForAgent(
   721	  kind: AgentKind,
   722	  profile: AdapterSyncProfile,
   723	  instructionsFile: string | undefined,
   724	  filesWritten: string[],
   725	  actions: Array<{ file: string; action: string }>,
   726	  conflicts: SyncConflict[],
   727	  errors: AdapterError[],
   728	): void {
   729	  if (!isString(instructionsFile)) {
   730	    errors.push({
   731	      code: 'MISSING_REQUIRED_INPUT',
   732	      file: '',
   733	      operation: 'parse',
   734	      message: `instructionsFile required for kind=${kind}`,
   735	      field: 'instructionsFile',
   736	    });
   737	    return;
   738	  }
   739	  if (!isString(profile.echoSection)) {
   740	    errors.push({
   741	      code: 'MISSING_REQUIRED_INPUT',
   742	      file: instructionsFile,
   743	      operation: 'parse',
   744	      message: `echoSection required for kind=${kind}`,
   745	      field: 'echoSection',
   746	    });
   747	    return;
   748	  }
   749	  let result: MarkerResult;
   750	  try {
   751	    result = mergeWithMarkers({
   752	      filePath: instructionsFile,
   753	      echoSection: profile.echoSection,
   754	      previousEchoSection: profile.previousEchoSection,
   755	      force: profile.force === true,
   756	    });
   757	  } catch (err) {
   758	    errors.push(toAdapterError(err, instructionsFile, 'write'));
   759	    return;
   760	  }
   761	  if (result.action === 'conflict') {
   762	    conflicts.push(result.conflict);
   763	    return;
   764	  }
   765	  if (result.action !== 'noop') {
   766	    filesWritten.push(instructionsFile);
   767	  }
   768	  actions.push({ file: instructionsFile, action: result.action });
   769	}
   770	
   771	function handleCodexConfig(
   772	  profile: AdapterSyncProfile,
   773	  configFile: string | undefined,
   774	  filesWritten: string[],
   775	  actions: Array<{ file: string; action: string }>,
   776	  conflicts: SyncConflict[],
   777	  errors: AdapterError[],
   778	): void {
   779	  if (!isString(configFile)) {
   780	    errors.push({
   781	      code: 'MISSING_REQUIRED_INPUT',
   782	      file: '',
   783	      operation: 'parse',
   784	      message: 'configFile required for kind=codex',
   785	      field: 'configFile',
   786	    });
   787	    return;
   788	  }
   789	  if (!profile.mcpServerConfig) {
   790	    errors.push({
   791	      code: 'MISSING_REQUIRED_INPUT',
   792	      file: configFile,
   793	      operation: 'parse',
   794	      message: 'mcpServerConfig required for kind=codex',
   795	      field: 'mcpServerConfig',
   796	    });
   797	    return;
   798	  }
   799	  try {
   800	    const result = syncCodexMcpBlock({
   801	      filePath: configFile,
   802	      serverConfig: profile.mcpServerConfig,
   803	      previousServerConfig: profile.previousMcpServerConfig,
   804	      force: profile.force === true,
   805	    });
   806	    if (result.action === 'conflict') {
   807	      conflicts.push(result.conflict);
   808	      return;
   809	    }
   810	    if (result.action !== 'noop') {
   811	      filesWritten.push(configFile);
   812	    }
   813	    actions.push({ file: configFile, action: result.action });
   814	  } catch (err) {
   815	    errors.push(toAdapterError(err, configFile, 'write'));
   816	  }
   817	}
   818	
   819	function handleCursorConfig(
   820	  profile: AdapterSyncProfile,
   821	  configFile: string | undefined,
   822	  filesWritten: string[],
   823	  actions: Array<{ file: string; action: string }>,
   824	  conflicts: SyncConflict[],
   825	  errors: AdapterError[],
   826	): void {
   827	  if (!isString(configFile)) {
   828	    errors.push({
   829	      code: 'MISSING_REQUIRED_INPUT',
   830	      file: '',
   831	      operation: 'parse',
   832	      message: 'configFile required for kind=cursor',
   833	      field: 'configFile',
   834	    });
   835	    return;
   836	  }
   837	  if (!profile.mcpServerConfig) {
   838	    errors.push({
   839	      code: 'MISSING_REQUIRED_INPUT',
   840	      file: configFile,
   841	      operation: 'parse',
   842	      message: 'mcpServerConfig required for kind=cursor',
   843	      field: 'mcpServerConfig',
   844	    });
   845	    return;
   846	  }
   847	  try {
   848	    const result = syncCursorMcpEntry({
   849	      filePath: configFile,
   850	      serverConfig: profile.mcpServerConfig,
   851	      previousServerConfig: profile.previousMcpServerConfig,
   852	      force: profile.force === true,
   853	    });
   854	    if (result.action === 'conflict') {
   855	      conflicts.push(result.conflict);
   856	      return;
   857	    }
   858	    if (result.action !== 'noop') {
   859	      filesWritten.push(configFile);
   860	    }
   861	    actions.push({ file: configFile, action: result.action });
   862	  } catch (err) {
   863	    errors.push(toAdapterError(err, configFile, 'parse'));
   864	  }
   865	}
   866	
   867	function handleClaudeSkills(
   868	  commandsDir: string | undefined,
   869	  filesWritten: string[],
   870	  actions: Array<{ file: string; action: string }>,
   871	  errors: AdapterError[],
   872	  skipped: string[],
   873	): void {
   874	  if (!isString(commandsDir)) {
   875	    errors.push({
   876	      code: 'MISSING_REQUIRED_INPUT',
   877	      file: '',
   878	      operation: 'parse',
   879	      message: 'commandsDir required for kind=claude-code',
   880	      field: 'commandsDir',
   881	    });
   882	    return;
   883	  }
   884	  try {
   885	    const r = syncClaudeSkills({
   886	      sourceDir: ECHO_HOME_PATHS.skills,
   887	      targetDir: commandsDir,
   888	    });
   889	    for (const name of r.copied) {
   890	      filesWritten.push(`${commandsDir}${sep}${name}`);
   891	      actions.push({ file: `${commandsDir}${sep}${name}`, action: 'copied' });
   892	    }
   893	    for (const name of r.skipped) {
   894	      skipped.push(name);
   895	    }
   896	    if (r.copied.length === 0) {
   897	      errors.push({
   898	        code: 'EEXIST',
   899	        file: commandsDir,
   900	        operation: 'write',
   901	        message:
   902	          r.skipped.length > 0
   903	            ? `syncClaudeSkills copied 0 files; ${r.skipped.length} skipped (symlink targets or non-regular source entries)`
   904	            : `syncClaudeSkills copied 0 files; source dir empty`,
   905	      });
   906	    }
   907	  } catch (err) {
   908	    errors.push(toAdapterError(err, commandsDir, 'write'));
   909	  }
   910	}
   911	
   912	function toAdapterError(
   913	  err: unknown,
   914	  file: string,
   915	  operation: AdapterError['operation'],
   916	): AdapterError {
   917	  if (err instanceof AtomicWriteError) {
   918	    return {
   919	      code: err.code,
   920	      file: err.file,
   921	      operation,
   922	      message: err.message,
   923	    };
   924	  }
   925	  if (err instanceof RenderError) {
   926	    return {
   927	      code: 'UNSUPPORTED_VALUE',
   928	      file,
   929	      operation: 'parse',
   930	      message: err.message,
   931	      field: err.field,
   932	      type: err.typeName,
   933	    };
   934	  }
   935	  if (err instanceof TomlParseError) {
   936	    return {
   937	      code: 'PARSE_ERROR',
   938	      file: err.filePath,
   939	      operation: 'parse',
   940	      message: `TOML parse failed: ${err.firstLine}`,
   941	    };
   942	  }
   943	  if (err instanceof CursorJsonParseError) {
   944	    return {
   945	      code: 'PARSE_ERROR',
   946	      file: err.filePath,
   947	      operation: 'parse',
   948	      message: `JSON parse failed: ${err.firstLine}`,
   949	    };
   950	  }
   951	  return {
   952	    code: errnoCode(err),
   953	    file,
   954	    operation,
   955	    message: `${operation} failed: ${(err as Error).message}`,
   956	  };
   957	}
   958	
   959	// Suppress unused-var warning for isAbsolute import (kept for future readers
   960	// that grep for path helpers in this file).
   961	void isAbsolute;

 succeeded in 215ms:
# 2026-05-25 — ECHO Pro paid coord layer: product design

**Status:** Brainstormed 2026-05-25 ~14:30 PDT during Claude Code strategist conversation. Founder approved high-level design (*"lgtm for now"*). Decomposition into backlog items deferred to founder's next strategist conversation; the candidate slate is in the "Implied backlog decomposition" section below.

**Why this lives in `raw/internal/decisions/` and not in `backlog/`:** the design spans 5-6 independent subsystems. Per CLAUDE.md + brainstorming-skill decomposition rules, the right move is to archive the strategic design here, then spec each subsystem as its own `backlog/ready/<id>.md` after the founder picks which to ship first. Single backlog items get scoped specs; this is the meta.

**Trigger to act on this design:** founder's next strategist conversation that says *"let's spec subsystem X"*. Until then, treat as background reasoning.

**Cross-references:**
- `wiki/product/v1-spec.md` — locked 2026-04-30 V1 (browser ext + MCP + hotkey overlay + audit). This decision EXTENDS V1 with a paid tier on top, not replaces.
- `raw/internal/decisions/2026-05-10-multi-agent-dev-template-and-product-thesis.md` — the May 10 hold pattern (extract / productize after 030 + dogfooding). Today's decision REOPENS the productization line; 069 shipping + 4 weeks of dogfooding produced the empirical evidence.
- `raw/internal/decisions/2026-05-13-echo-skills-are-the-cross-tool-protocol.md` — "ECHO's wedge is hosting the protocol that lets Claude AND non-Claude tools work as peers." Today's design operationalizes that wedge as the paid tier.
- MEMORY note `project_substrate_is_scaffolding_not_product.md` (2026-05-17) — "Defer team-shape/agent-assignment vision to V2+." Today's design does NOT include team/multi-human coordination; that remains V2+.

## TL;DR

ECHO is packaged as two tiers:

- **Free**: substrate (daemon, atom store, MCP) + Raycast extension (Ask + audit). Distributed via Raycast Store.
- **Paid (ECHO Pro)**: coordination layer = skills + roles + agent adapters + cross-vendor protocol + `echo` CLI. Subscription.

**Unit of paid product:** roles. Each role (strategist / reviewer / builder) bundles a skill set + MCP wiring requirement + capability requirements. Workflows are emergent — connect roles to run multi-agent dev.

**Buying trigger:** "I want my multi-agent patterns to be portable + repeatable." Skills + role templates are the value.

**Day-1 user:** solo indie AI builder (V1 cohort, unchanged) — uses Cursor + Claude Code + codex + ChatGPT, some days single-agent, some days multi-agent.

**Install posture:** global, not project-local. Substrate is already global on the machine; coord layer extends that.

**Scoping rule (codex consult, 2026-05-25):** "D as product model, A as CLI default" — global install, unified context retrieval feels global; project workflows resolve via `repo_path`/`--project` arg first, then nearest git root walking up from cwd. The substrate's existing `repo_path` MCP arg (already supported across find_clusters, get_recent_work_context, echo_resolve_mru, search_memories per `src/mcp/cursor-workspace-resolver.ts` + `src/mcp/request-log.ts`) is the canonical scoping field.

**First-session demo:** deferred. Founder will dogfood the onboarding end-to-end as a customer and let the demo question resolve itself based on what they actually want to do in their first session. Codex's preliminary recommendation (cross-vendor change review, reformulated from PR-only to any-diff with priority `PR > unpushed > uncommitted > HEAD~1..HEAD`) is archived here for when the founder reopens the question.

## Coord layer architecture

```
~/.echo/
├── skills/              # canonical, vendor-neutral skill library (.md)
├── roles/               # role definitions (.toml) — strategist, reviewer, builder
├── state/
│   ├── onboarding.json  # what was wired, when, agent capability profile
│   └── projects.json    # known projects (auto-populated from atom store)
└── adapters/            # cached per-agent adapter outputs (for re-sync diff)

/usr/local/bin/echo      # CLI binary (commands: init, review, run, doctor, uninstall)
```

Synced from `~/.echo/skills/` to each agent's adapter location during wiring:

| Agent | Adapter location(s) | Merge strategy |
|---|---|---|
| Codex | `~/.codex/AGENTS.md` (markdown), `~/.codex/config.toml` (`[mcp_servers.echo]`) | Markdown: ECHO-owned section between `<!-- BEGIN ECHO --> ... <!-- END ECHO -->` markers; everything outside preserved. TOML: parse + mutate `[mcp_servers.echo]` only. |
| Claude Code | `~/.claude/CLAUDE.md` (markdown), `~/.claude/commands/*.md` (one file per skill) | CLAUDE.md uses the BEGIN/END markers. Each command file is fully ECHO-owned (overwritten on re-sync). |
| Cursor | `~/.cursor/mcp.json` (or equiv) | JSON parse + mutate `mcpServers.echo` only. |
| Future / web-only agents | MCP `get_skill(name)` tool on the daemon | No filesystem writes needed — agent fetches at runtime. Deferred to V1.5+. |

**Merge-with-markers rationale:** preserves the user's hand-written instructions; ECHO only owns its delimited region. Re-running onboarding is idempotent — marked region is replaced; everything else untouched. Conflict mode: if user edited *inside* the markers, wizard refuses to overwrite and surfaces the diff.

## Role definition format

Each `~/.echo/roles/<name>.toml`:

```toml
[role]
name = "reviewer"
description = "Read-only code review. Surfaces correctness, style, and scope issues."
skills = ["review-pending", "review-queue-codex"]
sandbox = "read-only"

[role.requires]
mcp_servers = ["echo"]
capabilities = ["fs.read", "git.read"]

[role.output]
format = "yaml-header + markdown"
required_fields = ["verdict", "reviewer", "findings"]
```

Bundled defaults V1: `strategist.toml`, `reviewer.toml`, `builder.toml`. Users can hand-write more; no editor shipped in V1.

**Role-plugging at runtime:** `echo run <workflow>` matches role requirements against onboarded agent capabilities and picks. e.g., if codex and claude both satisfy "reviewer", default picked during onboarding; per-invocation override supported.

## Onboarding wizard — 6 steps

| # | Step | What ECHO does | What user does |
|---|---|---|---|
| 1 | Welcome | 1-sentence pitch + estimated wizard time (~2 min) | Continue |
| 2 | Detect agents | Layered scan: config files + running processes + atom-store `source_breakdown` (last 30d) | Confirm or edit list |
| 3 | Detect projects | Query atom store for distinct `metadata.repo_root` over last 7d, ranked by activity | Pick one as default `--project`; others stored for later |
| 4 | Wire | Per agent: write/merge MCP block + write/merge BEGIN/END section in AGENTS.md/CLAUDE.md + sync Claude Code skills to `~/.claude/commands/` | Passive; conflicts surfaced with diff preview before write |
| 5 | Probe | Per agent: invoke 1-sec hello calling `mcp__echo__echo_ping`. Confirm round-trip | Passive |
| 6 | Done | "You're ready" + suggested first action (TBD) + uninstall instructions + path to `~/.echo/state/onboarding.json` for transparency | Finish |

**Failure modes (all surface, none silent):**
- Agent detected but wiring fails → skip-this-agent + continue option, partial state recorded
- Probe times out → surface stderr, allow retry or skip
- Daemon not running at wiring time → refuse to wire, offer `echo doctor` one-liner
- User edited inside BEGIN/END markers → refuse overwrite, surface diff, ask to move content outside markers

## What's deferred

| Deferred | Triggered when |
|---|---|
| First-session demo workflow | Founder dogfoods onboarding end-to-end as a customer; observation produces the answer |
| Install topology (one install vs two; brew vs pkg vs Raycast Store) | After wizard UX is implemented; install is the wrapper around the wizard |
| Pricing point | After first 5+ external users complete onboarding and report perceived value |
| Brand / rename | Before public Show HN launch (week 10 deadline per V1 spec) |
| Open-source posture (which pieces MIT vs commercial) | When licensing is needed for distribution — likely tied to install topology decision |
| `.echo/config.toml` per-project overrides | V1.5+ when 2+ data points emerge of users needing different skill sets per project |
| `mcp__echo__get_skill(name)` dynamic skill serving | V1.5+ once filesystem adapter drift telemetry justifies the simplification |
| Multi-human / team coordination | V2+ per 2026-05-17 memory |

## Implied backlog decomposition

This design implies the following backlog items, in suggested build order. The founder picks which to spec first in their next strategist conversation; each gets its own `backlog/ready/<id>.md` with full acceptance criteria.

| Tentative id | Subsystem | One-line scope |
|---|---|---|
| 070 | ECHO global home (`~/.echo/`) scaffold | Establish the directory layout, state file schemas, daemon-aware paths. Foundation for all other items. |
| 071 | Role definition format + 3 default roles | TOML schema for `~/.echo/roles/<name>.toml`; ship `strategist.toml`, `reviewer.toml`, `builder.toml`. |
| 072 | Adapter sync engine | Merge-with-markers logic for AGENTS.md/CLAUDE.md; TOML/JSON mutators for config files; idempotent re-sync; conflict detection. |
| 073 | Onboarding wizard (steps 2-5) | Auto-detect agents + projects, wire with confirmation gate, probe with round-trip MCP call. |
| 074 | `echo` CLI binary | Commands: `init`, `run`, `doctor`, `uninstall`. `review` and other workflow commands come from role-plugging at runtime. |
| 075 | First demo workflow | Deferred — specced after founder dogfoods onboarding. Codex's preliminary candidate (cross-vendor change review) is the lead hypothesis. |

**Order rationale:** 070 must precede everything (storage layout). 071 + 072 are independent and can run parallel. 073 depends on 070 + 071 + 072. 074 depends on 070 + 071 + 072 + 073. 075 depends on 074 + founder dogfooding.

**Estimated total scope:** ~3-4 weeks of single-builder time, or ~2 weeks with the multi-builder parallel pipeline already dogfooded.

## After Completion (Strategist Notes)

- **Wiki page candidates (post-shipment):** `wiki/product/echo-pro.md` (paid-tier scope, what's in/out), `wiki/architecture/coord-layer.md` (the `~/.echo/` layout + role format + adapter sync), `wiki/surfaces/onboarding-wizard.md` (the 6-step arc).
- **Update `wiki/product/v1-spec.md`** with a note that V1 ships with the substrate + Raycast (free); ECHO Pro is a separate paid tier on the same substrate.
- **Trigger to reopen the deferred questions:** after 073 + 074 ship and founder completes the onboarding wizard themselves as a customer, surface the first-demo question + install-topology question in the strategist conversation that follows.

 succeeded in 220ms:
     1	import { mkdirSync, readdirSync, readFileSync, lstatSync } from 'node:fs';
     2	import { join } from 'node:path';
     3	import { atomicWrite, AtomicWriteError } from './atomic-write.js';
     4	import type { InstallProfile } from '../paths.js';
     5	
     6	export type PopulateEchoSkillsResult =
     7	  | { ok: true; copied: string[]; skipped: string[]; targetDir: string }
     8	  | { ok: false; sourceDir: string; targetDir: string; error: string };
     9	
    10	export interface SyncClaudeSkillsResult {
    11	  copied: string[];
    12	  skipped: string[];
    13	  targetDir: string;
    14	}
    15	
    16	export interface SkillSyncOpts {
    17	  sourceDir: string;
    18	  targetDir: string;
    19	  profile?: InstallProfile;
    20	}
    21	
    22	function audienceFor(content: string): InstallProfile {
    23	  if (!content.startsWith('---\n')) return 'customer';
    24	  const end = content.indexOf('\n---', 4);
    25	  if (end === -1) return 'customer';
    26	  const frontmatter = content.slice(4, end).split(/\r?\n/);
    27	  for (const line of frontmatter) {
    28	    const match = /^audience:\s*["']?(customer|dogfood)["']?\s*$/.exec(line.trim());
    29	    if (match !== null) return match[1] as InstallProfile;
    30	  }
    31	  return 'customer';
    32	}
    33	
    34	function includedForProfile(content: string, profile: InstallProfile): boolean {
    35	  return profile === 'dogfood' || audienceFor(content) === 'customer';
    36	}
    37	
    38	function listMdFiles(dir: string): string[] {
    39	  return readdirSync(dir)
    40	    .filter((n) => n.endsWith('.md'))
    41	    .sort();
    42	}
    43	
    44	/**
    45	 * First-hop: copies regular .md files from packaged assets/echo-skills/ into ~/.echo/skills/.
    46	 * Symlinks within sourceDir are NEVER followed.
    47	 *
    48	 * Returns a discriminated result; never throws on missing/unreadable sourceDir.
    49	 */
    50	export function populateEchoSkills(opts: SkillSyncOpts): PopulateEchoSkillsResult {
    51	  const { sourceDir, targetDir } = opts;
    52	  const profile = opts.profile ?? 'dogfood';
    53	  const copied: string[] = [];
    54	  const skipped: string[] = [];
    55	
    56	  let entries: string[];
    57	  try {
    58	    entries = listMdFiles(sourceDir);
    59	  } catch (err) {
    60	    return {
    61	      ok: false,
    62	      sourceDir,
    63	      targetDir,
    64	      error: `cannot read sourceDir: ${(err as Error).message}`,
    65	    };
    66	  }
    67	
    68	  try {
    69	    mkdirSync(targetDir, { recursive: true });
    70	  } catch (err) {
    71	    return {
    72	      ok: false,
    73	      sourceDir,
    74	      targetDir,
    75	      error: `cannot create targetDir: ${(err as Error).message}`,
    76	    };
    77	  }
    78	
    79	  for (const name of entries) {
    80	    const srcPath = join(sourceDir, name);
    81	    try {
    82	      const lst = lstatSync(srcPath);
    83	      if (!lst.isFile()) {
    84	        skipped.push(name);
    85	        continue;
    86	      }
    87	    } catch {
    88	      skipped.push(name);
    89	      continue;
    90	    }
    91	    let content: string;
    92	    try {
    93	      content = readFileSync(srcPath, 'utf8');
    94	    } catch {
    95	      skipped.push(name);
    96	      continue;
    97	    }
    98	    if (!includedForProfile(content, profile)) {
    99	      skipped.push(name);
   100	      continue;
   101	    }
   102	    const tgtPath = join(targetDir, name);
   103	    try {
   104	      atomicWrite({ filePath: tgtPath, content });
   105	      copied.push(name);
   106	    } catch (err) {
   107	      if (err instanceof AtomicWriteError && err.code === 'EEXIST') {
   108	        // target is a symlink — skip per AC4 / AC7.2a
   109	        skipped.push(name);
   110	      } else {
   111	        return {
   112	          ok: false,
   113	          sourceDir,
   114	          targetDir,
   115	          error: `failed to write ${name}: ${(err as Error).message}`,
   116	        };
   117	      }
   118	    }
   119	  }
   120	
   121	  return { ok: true, copied, skipped, targetDir };
   122	}
   123	
   124	/**
   125	 * Second-hop: copies regular .md files from ~/.echo/skills/ into the per-vendor
   126	 * commands directory (e.g. ~/.claude/commands/).
   127	 *
   128	 * On hard failure (sourceDir unreadable, etc.), throws — syncAll's try/catch
   129	 * surfaces it via the agent's errors[]. Symlink-target skips push into the
   130	 * skipped[] array.
   131	 */
   132	export function syncClaudeSkills(opts: SkillSyncOpts): SyncClaudeSkillsResult {
   133	  const { sourceDir, targetDir } = opts;
   134	  const copied: string[] = [];
   135	  const skipped: string[] = [];
   136	
   137	  mkdirSync(targetDir, { recursive: true });
   138	  const entries = listMdFiles(sourceDir);
   139	
   140	  for (const name of entries) {
   141	    const srcPath = join(sourceDir, name);
   142	    try {
   143	      const lst = lstatSync(srcPath);
   144	      if (!lst.isFile()) {
   145	        skipped.push(name);
   146	        continue;
   147	      }
   148	    } catch {
   149	      skipped.push(name);
   150	      continue;
   151	    }
   152	    const content = readFileSync(srcPath, 'utf8');
   153	    const tgtPath = join(targetDir, name);
   154	    try {
   155	      atomicWrite({ filePath: tgtPath, content });
   156	      copied.push(name);
   157	    } catch (err) {
   158	      if (err instanceof AtomicWriteError && err.code === 'EEXIST') {
   159	        skipped.push(name);
   160	      } else {
   161	        throw err;
   162	      }
   163	    }
   164	  }
   165	
   166	  return { copied, skipped, targetDir };
   167	}

exec
/bin/zsh -lc 'nl -ba src/echo-home/wizard/wire.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba src/cli/io/render.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba src/cli/commands/doctor.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba src/cli/commands/init.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba tools/foreign-install-smoke.sh' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba src/echo-home/wizard/run-wizard.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 227ms:
     1	import type { DetectedAgent } from '../../echo-home/wizard/detect-agents.js';
     2	import type { DetectedProject } from '../../echo-home/wizard/detect-projects.js';
     3	import type { ProbeOutcome } from '../../echo-home/wizard/probe.js';
     4	import type { WireResult } from '../../echo-home/wizard/wire.js';
     5	import type { DoctorReport } from '../commands/doctor.js';
     6	import type { buildRemediationCopy } from '../commands/init.js';
     7	
     8	function status(ok: boolean, color: boolean): string {
     9	  if (!color) return ok ? 'OK' : 'WARN';
    10	  return ok ? '\x1b[32mOK\x1b[0m' : '\x1b[33mWARN\x1b[0m';
    11	}
    12	
    13	export function renderDetectedAgents(
    14	  agents: readonly DetectedAgent[],
    15	  opts: { color: boolean },
    16	): string {
    17	  if (agents.length === 0) return 'No agents detected.';
    18	  return agents
    19	    .map((agent, i) => {
    20	      const signals = agent.signals;
    21	      const activity = signals.atomActivity?.count ?? 0;
    22	      return `${i + 1}. ${agent.kind} (${agent.confidence}) config=${signals.configFile.exists ? 'yes' : 'no'} activity=${activity} ${status(agent.confidence !== 'none', opts.color)}`;
    23	    })
    24	    .join('\n');
    25	}
    26	
    27	export function renderDetectedProjects(
    28	  projects: readonly DetectedProject[],
    29	  opts: { color: boolean },
    30	): string {
    31	  void opts;
    32	  if (projects.length === 0) return 'No recent projects detected.';
    33	  return projects
    34	    .map((project, i) => `${i + 1}. ${project.repoRoot} (${project.atomCount} events)`)
    35	    .join('\n');
    36	}
    37	
    38	export function renderWireResult(result: WireResult, opts: { color: boolean }): string {
    39	  void opts;
    40	  const lines = ['Wire result:'];
    41	  if (result.syncResult.syncLock !== undefined) lines.push(result.syncResult.syncLock.message);
    42	  if (result.syncResult.repoRoot !== undefined) lines.push(result.syncResult.repoRoot.message);
    43	  if (result.syncResult.directorySymlink !== undefined) {
    44	    lines.push(result.syncResult.directorySymlink.message);
    45	  }
    46	  for (const agent of result.syncResult.agents) {
    47	    if (agent.ok) {
    48	      lines.push(`- ${agent.agent}: ok (${agent.actions.map((a) => a.action).join(', ')})`);
    49	    } else {
    50	      const conflict = agent.conflicts[0];
    51	      const err = agent.errors[0];
    52	      lines.push(`- ${agent.agent}: conflict ${conflict?.kind ?? err?.message ?? 'unknown'}`);
    53	      if (conflict !== undefined && 'unifiedDiff' in conflict && conflict.unifiedDiff) {
    54	        lines.push(conflict.unifiedDiff);
    55	      }
    56	    }
    57	  }
    58	  return lines.join('\n');
    59	}
    60	
    61	export function renderProbeOutcomes(
    62	  outcomes: readonly ProbeOutcome[],
    63	  opts: { color: boolean; remediation: ReturnType<typeof buildRemediationCopy> },
    64	): string {
    65	  if (outcomes.length === 0) return 'No probes run.';
    66	  return outcomes
    67	    .map((outcome) => {
    68	      if (outcome.probed)
    69	        return `${outcome.agent}: ${status(true, opts.color)} ${outcome.latencyMs}ms`;
    70	      const copy = opts.remediation[outcome.reason](outcome);
    71	      return `${outcome.agent}: ${status(false, opts.color)} ${outcome.reason}\n${copy}`;
    72	    })
    73	    .join('\n');
    74	}
    75	
    76	export function renderDoctorReport(
    77	  report: DoctorReport,
    78	  opts: { color: boolean; remediation: ReturnType<typeof buildRemediationCopy> },
    79	): string {
    80	  const lines = [`ECHO doctor: ${report.overall}`];
    81	  lines.push(
    82	    `daemon: reachable=${report.daemon.mcpReachable ? 'yes' : 'no'} pid-lock=${report.daemon.pidLockHeld ? 'yes' : 'no'} port=${report.daemon.port}`,
    83	  );
    84	  lines.push(
    85	    `echo-home: exists=${report.echoHome.exists ? 'yes' : 'no'} onboarding=${report.echoHome.onboardingValid ? 'valid' : 'invalid'} projects=${report.echoHome.projectsValid ? 'valid' : 'invalid'} schema=${report.echoHome.schemaVersion} profile=${report.echoHome.profile}`,
    86	  );
    87	  lines.push(`sync-lock: ${report.syncLock.present ? report.syncLock.cleanupCommand : 'absent'}`);
    88	  for (const agent of report.agents) {
    89	    if (agent.probeOutcome === null) {
    90	      lines.push(`agent ${agent.kind}: not wired`);
    91	    } else if (agent.probeOutcome.probed) {
    92	      lines.push(`agent ${agent.kind}: ok`);
    93	    } else {
    94	      lines.push(`agent ${agent.kind}: ${agent.probeOutcome.reason}`);
    95	      lines.push(opts.remediation[agent.probeOutcome.reason](agent.probeOutcome));
    96	    }
    97	  }
    98	  if (report.overall !== 'healthy') {
    99	    lines.push('Recommended actions: run `echoctl init` or follow the cleanup hints above.');
   100	  }
   101	  return lines.join('\n');
   102	}

 succeeded in 227ms:
     1	import { mkdirSync, readFileSync } from 'node:fs';
     2	import { dirname } from 'node:path';
     3	import {
     4	  syncAll as realSyncAll,
     5	  type AdapterSyncProfile,
     6	  type AgentResult,
     7	  type SyncConflict,
     8	  type SyncResult,
     9	} from '../adapter-sync.js';
    10	import type { ClaudeCodeMcpRegisterDeps } from '../adapters/claude-code-mcp.js';
    11	import { atomicWrite } from '../adapters/atomic-write.js';
    12	import {
    13	  ECHO_HOME_PATHS,
    14	  validateOnboardingState,
    15	  type InstallProfile,
    16	  type OnboardingState,
    17	} from '../paths.js';
    18	import { readAdapterCache, writeAdapterCache, type AdapterCacheRecord } from './adapter-cache.js';
    19	import type { AgentKind } from './detect-agents.js';
    20	import { renderEchoSection } from './render-echo-section.js';
    21	
    22	export interface WireOpts {
    23	  selectedAgents: AgentKind[];
    24	  defaultProjectRepoRoot: string | null;
    25	  mcpServerUrl: string;
    26	  echoVersion: string;
    27	  profile?: InstallProfile;
    28	  repoRoot?: string;
    29	  force?: boolean;
    30	  syncAll?: typeof realSyncAll;
    31	  claudeCodeMcpRegistration?: ClaudeCodeMcpRegisterDeps;
    32	  cache?: {
    33	    read: (kind: AgentKind) => AdapterCacheRecord | null;
    34	    write: (rec: AdapterCacheRecord) => void;
    35	  };
    36	  now?: Date;
    37	}
    38	
    39	export interface WireResult {
    40	  syncResult: SyncResult;
    41	  cacheUpdates: Array<{
    42	    agent: AgentKind;
    43	    action: 'written' | 'unchanged' | 'failed';
    44	    error?: string;
    45	  }>;
    46	  onboardingStateUpdated: boolean;
    47	}
    48	
    49	const MARKER_AGENTS = new Set<AgentKind>(['codex', 'claude-code']);
    50	
    51	function readState(): OnboardingState {
    52	  const raw = JSON.parse(readFileSync(ECHO_HOME_PATHS.stateOnboarding, 'utf8')) as unknown;
    53	  if (!validateOnboardingState(raw)) {
    54	    throw new Error(
    55	      `${ECHO_HOME_PATHS.stateOnboarding}: invalid onboarding state schema_version or shape`,
    56	    );
    57	  }
    58	  return raw;
    59	}
    60	
    61	function writeState(state: OnboardingState): void {
    62	  mkdirSync(dirname(ECHO_HOME_PATHS.stateOnboarding), { recursive: true });
    63	  atomicWrite({
    64	    filePath: ECHO_HOME_PATHS.stateOnboarding,
    65	    content: `${JSON.stringify(state, null, 2)}\n`,
    66	    secretSensitive: false,
    67	  });
    68	}
    69	
    70	function conflictMessage(conflict: SyncConflict): string {
    71	  if (conflict.kind === 'config') return `conflict on ${conflict.filePath}: config`;
    72	  if (conflict.kind === 'marker') return `conflict on ${conflict.filePath}: marker`;
    73	  if (conflict.kind === 'target-symlink') return `conflict on ${conflict.filePath}: target-symlink`;
    74	  return `conflict on ${conflict.filePath}: marker block malformed; manual intervention required`;
    75	}
    76	
    77	function agentResult(syncResult: SyncResult, kind: AgentKind): AgentResult | undefined {
    78	  return syncResult.agents.find((a) => a.agent === kind);
    79	}
    80	
    81	function isSuccessfulAgent(result: AgentResult | undefined): boolean {
    82	  return result?.ok === true;
    83	}
    84	
    85	function buildProfiles(
    86	  opts: WireOpts,
    87	  nowIso: string,
    88	  cache: NonNullable<WireOpts['cache']>,
    89	): { profiles: AdapterSyncProfile[]; rendered: Map<AgentKind, string | null> } {
    90	  const profiles: AdapterSyncProfile[] = [];
    91	  const rendered = new Map<AgentKind, string | null>();
    92	
    93	  for (const kind of opts.selectedAgents) {
    94	    const prior = cache.read(kind);
    95	    const echoSection = MARKER_AGENTS.has(kind)
    96	      ? renderEchoSection({
    97	          agent: kind,
    98	          mcpServerUrl: opts.mcpServerUrl,
    99	          echoVersion: opts.echoVersion,
   100	          defaultProjectRepoRoot: opts.defaultProjectRepoRoot,
   101	          renderedAt: nowIso,
   102	        })
   103	      : undefined;
   104	    rendered.set(kind, echoSection ?? null);
   105	    const mcpServerConfig =
   106	      kind === 'codex' || kind === 'cursor' || kind === 'claude-code'
   107	        ? { url: opts.mcpServerUrl }
   108	        : undefined;
   109	    profiles.push({
   110	      kind,
   111	      echoSection,
   112	      previousEchoSection: prior?.echoSection ?? undefined,
   113	      mcpServerConfig,
   114	      previousMcpServerConfig: prior?.mcpServerConfig ?? undefined,
   115	      claudeCodeMcpRegistration:
   116	        kind === 'claude-code' ? opts.claudeCodeMcpRegistration : undefined,
   117	      force: opts.force === true,
   118	    });
   119	  }
   120	
   121	  return { profiles, rendered };
   122	}
   123	
   124	function emptyFailedSyncResult(message: string): SyncResult {
   125	  return {
   126	    skillsPopulated: { ok: false, sourceDir: '', targetDir: '', error: message },
   127	    agents: [],
   128	    roles: { results: [], rolesErrors: [] },
   129	    overallOk: false,
   130	  };
   131	}
   132	
   133	function topLevelSentinel(syncResult: SyncResult): boolean {
   134	  return (
   135	    syncResult.syncLock !== undefined ||
   136	    syncResult.repoRoot !== undefined ||
   137	    syncResult.directorySymlink !== undefined
   138	  );
   139	}
   140	
   141	function updateOnboardingState(
   142	  selectedAgents: AgentKind[],
   143	  syncResult: SyncResult,
   144	  nowIso: string,
   145	): void {
   146	  const state = readState();
   147	  state.last_updated_at = nowIso;
   148	
   149	  for (const kind of selectedAgents) {
   150	    let profile = state.agents.find((agent) => agent.id === kind);
   151	    if (profile === undefined) {
   152	      profile = {
   153	        id: kind,
   154	        detected_at: nowIso,
   155	        wired_at: null,
   156	        probed_at: null,
   157	        capabilities: [],
   158	        wire_error: null,
   159	      };
   160	      state.agents.push(profile);
   161	    }
   162	
   163	    const result = agentResult(syncResult, kind);
   164	    if (result?.ok === true) {
   165	      profile.wired_at = nowIso;
   166	      profile.wire_error = null;
   167	    } else if (result?.ok === false) {
   168	      if (result.errors[0] !== undefined) {
   169	        profile.wire_error = result.errors[0].message;
   170	      } else if (result.conflicts[0] !== undefined) {
   171	        profile.wire_error = conflictMessage(result.conflicts[0]);
   172	      } else {
   173	        profile.wire_error = 'unknown wire failure';
   174	      }
   175	    }
   176	  }
   177	
   178	  writeState(state);
   179	}
   180	
   181	function updateProbeTimestamps(
   182	  outcomes: Array<{ agent: AgentKind; probed: boolean }>,
   183	  nowIso: string,
   184	): void {
   185	  const state = readState();
   186	  state.last_updated_at = nowIso;
   187	  for (const outcome of outcomes) {
   188	    if (!outcome.probed) continue;
   189	    let profile = state.agents.find((agent) => agent.id === outcome.agent);
   190	    if (profile === undefined) {
   191	      profile = {
   192	        id: outcome.agent,
   193	        detected_at: nowIso,
   194	        wired_at: null,
   195	        probed_at: null,
   196	        capabilities: [],
   197	        wire_error: null,
   198	      };
   199	      state.agents.push(profile);
   200	    }
   201	    profile.probed_at = nowIso;
   202	  }
   203	  writeState(state);
   204	}
   205	
   206	export function markOnboardingCompleted(now: Date): void {
   207	  const state = readState();
   208	  state.completed = true;
   209	  state.last_updated_at = now.toISOString();
   210	  writeState(state);
   211	}
   212	
   213	export function readOnboardingStateSnapshot(): OnboardingState | null {
   214	  try {
   215	    return readState();
   216	  } catch {
   217	    return null;
   218	  }
   219	}
   220	
   221	export function updateProbedAgents(
   222	  outcomes: Array<{ agent: AgentKind; probed: boolean }>,
   223	  now: Date,
   224	): void {
   225	  updateProbeTimestamps(outcomes, now.toISOString());
   226	}
   227	
   228	export async function wire(opts: WireOpts): Promise<WireResult> {
   229	  const now = opts.now ?? new Date();
   230	  const nowIso = now.toISOString();
   231	  const cache = opts.cache ?? { read: readAdapterCache, write: writeAdapterCache };
   232	  const { profiles, rendered } = buildProfiles(opts, nowIso, cache);
   233	  const runSyncAll = opts.syncAll ?? realSyncAll;
   234	  const installProfile = opts.profile ?? 'dogfood';
   235	
   236	  let syncResult: SyncResult;
   237	  try {
   238	    syncResult = await runSyncAll(
   239	      profiles,
   240	      opts.repoRoot === undefined
   241	        ? { profile: installProfile }
   242	        : { repoRoot: opts.repoRoot, profile: installProfile },
   243	    );
   244	  } catch (err) {
   245	    return {
   246	      syncResult: emptyFailedSyncResult((err as Error).message),
   247	      cacheUpdates: [],
   248	      onboardingStateUpdated: false,
   249	    };
   250	  }
   251	
   252	  if (topLevelSentinel(syncResult)) {
   253	    return { syncResult, cacheUpdates: [], onboardingStateUpdated: false };
   254	  }
   255	
   256	  const cacheUpdates: WireResult['cacheUpdates'] = [];
   257	  for (const kind of opts.selectedAgents) {
   258	    const result = agentResult(syncResult, kind);
   259	    if (!isSuccessfulAgent(result)) continue;
   260	    const mcpServerConfig =
   261	      kind === 'codex' || kind === 'cursor' || kind === 'claude-code'
   262	        ? { url: opts.mcpServerUrl }
   263	        : null;
   264	    cache.write({
   265	      schema_version: 1,
   266	      agent: kind,
   267	      last_written_at: nowIso,
   268	      echoSection: rendered.get(kind) ?? null,
   269	      mcpServerConfig,
   270	    });
   271	    cacheUpdates.push({ agent: kind, action: 'written' });
   272	  }
   273	
   274	  updateOnboardingState(opts.selectedAgents, syncResult, nowIso);
   275	  return { syncResult, cacheUpdates, onboardingStateUpdated: true };
   276	}

 succeeded in 208ms:
     1	import { existsSync, mkdirSync, readFileSync } from 'node:fs';
     2	import { dirname, resolve } from 'node:path';
     3	import { fileURLToPath } from 'node:url';
     4	import { parseArgs } from 'node:util';
     5	import { atomicWrite } from '../../echo-home/adapters/atomic-write.js';
     6	import {
     7	  ECHO_HOME_PATHS,
     8	  setEchoHomeRoot,
     9	  validateOnboardingState,
    10	  type InstallProfile,
    11	  type OnboardedAgentProfile,
    12	  type OnboardingState,
    13	} from '../../echo-home/paths.js';
    14	import { type Capability } from '../../echo-home/roles.js';
    15	import {
    16	  createWizard,
    17	  type CreateWizardOpts,
    18	  type Wizard,
    19	} from '../../echo-home/wizard/run-wizard.js';
    20	import type { WireResult } from '../../echo-home/wizard/wire.js';
    21	import { AGENT_KINDS, type AgentKind } from '../../echo-home/wizard/detect-agents.js';
    22	import type { ProbeOutcome } from '../../echo-home/wizard/probe.js';
    23	import {
    24	  renderDetectedAgents,
    25	  renderDetectedProjects,
    26	  renderProbeOutcomes,
    27	  renderWireResult,
    28	} from '../io/render.js';
    29	import { makeTtyPrompt, type PromptImpl } from '../io/prompt.js';
    30	import { ensureEchoHome } from '../../echo-home/scaffold.js';
    31	import {
    32	  DaemonCommandError,
    33	  ensureDaemonRunning,
    34	  type DaemonBringupResult,
    35	  type DaemonControlOptions,
    36	} from './daemon.js';
    37	import { claudeCodeMcpAddCommand } from '../../echo-home/adapters/claude-code-mcp.js';
    38	
    39	export const AGENT_CAPABILITIES_BY_KIND: Readonly<Record<AgentKind, readonly Capability[]>> =
    40	  Object.freeze({
    41	    codex: Object.freeze([
    42	      'fs.read',
    43	      'fs.write',
    44	      'git.read',
    45	      'git.write',
    46	      'network',
    47	      'mcp.echo.read',
    48	      'mcp.echo.write',
    49	    ] as const),
    50	    'claude-code': Object.freeze([
    51	      'fs.read',
    52	      'fs.write',
    53	      'git.read',
    54	      'git.write',
    55	      'network',
    56	      'mcp.echo.read',
    57	      'mcp.echo.write',
    58	    ] as const),
    59	    cursor: Object.freeze(['mcp.echo.read'] as const),
    60	  });
    61	
    62	type WizardFactory = (opts: CreateWizardOpts) => Wizard;
    63	
    64	export interface InitOpts {
    65	  json?: boolean;
    66	  quiet?: boolean;
    67	  color?: boolean;
    68	  home?: string;
    69	  port?: string;
    70	  profile?: InstallProfile;
    71	  label?: string;
    72	  answerFile?: string;
    73	  force?: boolean;
    74	  stdin?: { isTTY?: boolean };
    75	  stdout?: Pick<NodeJS.WritableStream, 'write'>;
    76	  stderr?: Pick<NodeJS.WritableStream, 'write'>;
    77	  wizardFactory?: WizardFactory;
    78	  prompt?: PromptImpl;
    79	  now?: () => Date;
    80	  packageJsonPath?: string;
    81	  daemonOptions?: DaemonControlOptions;
    82	}
    83	
    84	export type RemediationCopy = Record<
    85	  Exclude<ProbeOutcome, { probed: true }>['reason'],
    86	  (outcome: Extract<ProbeOutcome, { probed: false }>) => string
    87	>;
    88	
    89	export interface InitAnswerFile {
    90	  confirm_setup: boolean;
    91	  selected_agents: AgentKind[];
    92	  default_project_repo_root: string | null;
    93	  profile?: InstallProfile;
    94	  repo_root?: string;
    95	}
    96	
    97	interface LoadedAnswerFile {
    98	  path: string;
    99	  answers: InitAnswerFile;
   100	}
   101	
   102	export const INIT_HELP = `Usage: echoctl init [--json] [--quiet] [--home <path>] [--port <n>] [--profile customer|dogfood] [--label <id>] [--answer-file <path>] [--force]
   103	
   104	Options:
   105	  --home <path>          ECHO_HOME for this init run
   106	  --port <n>             MCP server port written to adapter configs and launchd
   107	  --profile <name>       Install profile: customer (default) or dogfood
   108	  --label <id>           launchd label used when ensuring the daemon is running
   109	  --answer-file <path>   non-interactive JSON answers
   110	  --force                Replace existing ECHO marker blocks in adapter configs.
   111	                         Outside-marker content is preserved. Use for upgrade
   112	                         or re-onboarding scenarios.
   113	
   114	Answer-file JSON schema:
   115	  {
   116	    "confirm_setup": true,
   117	    "selected_agents": ["codex", "claude-code", "cursor"],
   118	    "profile": "customer",
   119	    "default_project_repo_root": null,
   120	    "repo_root": "/Users/me/Desktop/Project_echo"
   121	  }
   122	
   123	Required fields: confirm_setup, selected_agents, default_project_repo_root.
   124	Optional fields: profile, repo_root. repo_root is only needed if init cannot locate ECHO's source tree.`;
   125	
   126	const ANSWER_REQUIRED_FIELDS = [
   127	  'confirm_setup',
   128	  'selected_agents',
   129	  'default_project_repo_root',
   130	] as const;
   131	const ANSWER_OPTIONAL_FIELDS = ['profile', 'repo_root'] as const;
   132	const ANSWER_FIELDS = new Set<string>([...ANSWER_REQUIRED_FIELDS, ...ANSWER_OPTIONAL_FIELDS]);
   133	
   134	function isRecord(value: unknown): value is Record<string, unknown> {
   135	  return typeof value === 'object' && value !== null && !Array.isArray(value);
   136	}
   137	
   138	function isErrnoException(err: unknown): err is NodeJS.ErrnoException {
   139	  return err instanceof Error && 'code' in err;
   140	}
   141	
   142	function failAnswerFile(filePath: string, field: string, message: string): never {
   143	  throw new Error(`${filePath}: ${field}: ${message}; see \`echoctl init --help\` for schema`);
   144	}
   145	
   146	export function parsePort(value: string, flag = '--port'): number {
   147	  if (!/^[0-9]+$/.test(value)) throw new Error(`invalid ${flag}: ${value}`);
   148	  const n = Number(value);
   149	  if (!Number.isSafeInteger(n) || n <= 0 || n > 65535) {
   150	    throw new Error(`invalid ${flag}: ${value}`);
   151	  }
   152	  return n;
   153	}
   154	
   155	export function resolveMcpPort(): number {
   156	  const raw = process.env['ECHO_MCP_PORT'];
   157	  if (raw === undefined || raw.length === 0) return 38478;
   158	  try {
   159	    return parsePort(raw, 'ECHO_MCP_PORT');
   160	  } catch {
   161	    return 38478;
   162	  }
   163	}
   164	
   165	function parseNonEmptyOption(value: string | undefined, flag: string): string | undefined {
   166	  if (value === undefined) return undefined;
   167	  if (value.trim().length === 0) throw new Error(`invalid ${flag}: expected non-empty string`);
   168	  return value;
   169	}
   170	
   171	function parseProfile(value: string | undefined, flag = '--profile'): InstallProfile | undefined {
   172	  if (value === undefined) return undefined;
   173	  if (value === 'customer' || value === 'dogfood') return value;
   174	  throw new Error(`invalid ${flag}: expected customer or dogfood`);
   175	}
   176	
   177	export function parseInitArgs(
   178	  args: readonly string[],
   179	): Pick<InitOpts, 'answerFile' | 'force' | 'home' | 'label' | 'port' | 'profile'> {
   180	  const parsed = parseArgs({
   181	    args: [...args],
   182	    strict: true,
   183	    allowPositionals: false,
   184	    options: {
   185	      home: { type: 'string' },
   186	      port: { type: 'string' },
   187	      profile: { type: 'string' },
   188	      label: { type: 'string' },
   189	      'answer-file': { type: 'string' },
   190	      force: { type: 'boolean', default: false },
   191	    },
   192	  });
   193	  const port = parseNonEmptyOption(parsed.values.port, '--port');
   194	  if (port !== undefined) parsePort(port);
   195	  return {
   196	    home: parseNonEmptyOption(parsed.values.home, '--home'),
   197	    port,
   198	    profile: parseProfile(parseNonEmptyOption(parsed.values.profile, '--profile')),
   199	    label: parseNonEmptyOption(parsed.values.label, '--label'),
   200	    answerFile: parseNonEmptyOption(parsed.values['answer-file'], '--answer-file'),
   201	    force: parsed.values.force === true,
   202	  };
   203	}
   204	
   205	function validateAnswerFile(filePath: string, value: unknown): InitAnswerFile {
   206	  if (!isRecord(value)) failAnswerFile(filePath, 'root', 'expected object');
   207	  for (const key of Object.keys(value)) {
   208	    if (!ANSWER_FIELDS.has(key)) failAnswerFile(filePath, key, 'unknown field');
   209	  }
   210	  for (const field of ANSWER_REQUIRED_FIELDS) {
   211	    if (!Object.prototype.hasOwnProperty.call(value, field)) {
   212	      failAnswerFile(filePath, field, 'missing required field');
   213	    }
   214	  }
   215	  if (typeof value['confirm_setup'] !== 'boolean') {
   216	    failAnswerFile(filePath, 'confirm_setup', 'expected boolean');
   217	  }
   218	  if (!Array.isArray(value['selected_agents'])) {
   219	    failAnswerFile(filePath, 'selected_agents', 'expected array of agent kinds');
   220	  }
   221	  const selectedAgents: AgentKind[] = [];
   222	  for (let i = 0; i < value['selected_agents'].length; i++) {
   223	    const agent = value['selected_agents'][i];
   224	    if (typeof agent !== 'string' || !isAgentKind(agent)) {
   225	      failAnswerFile(filePath, `selected_agents[${i}]`, 'expected codex, claude-code, or cursor');
   226	    }
   227	    selectedAgents.push(agent);
   228	  }
   229	  const defaultProject = value['default_project_repo_root'];
   230	  if (defaultProject !== null && typeof defaultProject !== 'string') {
   231	    failAnswerFile(filePath, 'default_project_repo_root', 'expected string or null');
   232	  }
   233	  if (typeof defaultProject === 'string' && defaultProject.trim().length === 0) {
   234	    failAnswerFile(filePath, 'default_project_repo_root', 'expected non-empty string or null');
   235	  }
   236	  const profile = value['profile'];
   237	  if (profile !== undefined && profile !== 'customer' && profile !== 'dogfood') {
   238	    failAnswerFile(filePath, 'profile', 'expected customer or dogfood');
   239	  }
   240	  const repoRoot = value['repo_root'];
   241	  if (repoRoot !== undefined && (typeof repoRoot !== 'string' || repoRoot.trim().length === 0)) {
   242	    failAnswerFile(filePath, 'repo_root', 'expected non-empty string');
   243	  }
   244	  return {
   245	    confirm_setup: value['confirm_setup'],
   246	    selected_agents: selectedAgents,
   247	    default_project_repo_root: defaultProject,
   248	    ...(profile === undefined ? {} : { profile: profile as InstallProfile }),
   249	    ...(repoRoot === undefined ? {} : { repo_root: repoRoot }),
   250	  };
   251	}
   252	
   253	function loadAnswerFile(path: string): LoadedAnswerFile {
   254	  const resolvedPath = resolve(path);
   255	  let raw: string;
   256	  try {
   257	    raw = readFileSync(resolvedPath, 'utf8');
   258	  } catch (err) {
   259	    if (isErrnoException(err) && err.code === 'ENOENT') {
   260	      failAnswerFile(resolvedPath, 'file', 'not found');
   261	    }
   262	    failAnswerFile(resolvedPath, 'file', `failed to read: ${(err as Error).message}`);
   263	  }
   264	  try {
   265	    return { path: resolvedPath, answers: validateAnswerFile(resolvedPath, JSON.parse(raw)) };
   266	  } catch (err) {
   267	    if (err instanceof SyntaxError) {
   268	      failAnswerFile(resolvedPath, 'root', `invalid JSON: ${err.message}`);
   269	    }
   270	    throw err;
   271	  }
   272	}
   273	
   274	export function readPackageVersion(packageJsonPath?: string): string {
   275	  const resolved =
   276	    packageJsonPath ?? fileURLToPath(new URL('../../../package.json', import.meta.url));
   277	  const parsed = JSON.parse(readFileSync(resolved, 'utf8')) as { version?: unknown };
   278	  if (typeof parsed.version !== 'string') throw new Error(`${resolved}: missing version`);
   279	  return parsed.version;
   280	}
   281	
   282	export function buildRemediationCopy(mcpServerUrl: string): RemediationCopy {
   283	  return {
   284	    'cli-unavailable': (outcome) =>
   285	      `${outcome.agent} not found on PATH. Install it and run \`echoctl doctor\` to re-probe.`,
   286	    'auth-required': (outcome) =>
   287	      `${outcome.agent} is not logged in. Run the vendor login command and then \`echoctl doctor\`.`,
   288	    'manual-only': () =>
   289	      'Cursor has no headless CLI. Open Cursor and run any prompt to confirm ECHO MCP is reachable.',
   290	    'mcp-not-configured': () =>
   291	      `Claude Code does not have ECHO MCP configured. Run \`${claudeCodeMcpAddCommand(mcpServerUrl)}\` and then \`echoctl doctor\`.\nIf still degraded, remove a shadowing local entry with \`claude mcp remove echo -s local\` and then run \`echoctl doctor\`.`,
   292	    timeout: (outcome) =>
   293	      `${outcome.agent} took longer than 30s to respond. Re-run \`echoctl doctor\` once if this persists.`,
   294	    'unexpected-output': (outcome) =>
   295	      `${outcome.agent} responded but did not echo \`pong\`. Detail: ${(outcome.detail ?? '').slice(0, 200)}. Run \`echoctl doctor\` to retry.`,
   296	  };
   297	}
   298	
   299	function writeLine(stream: Pick<NodeJS.WritableStream, 'write'>, line: string): void {
   300	  stream.write(`${line}\n`);
   301	}
   302	
   303	function emitJson(opts: InitOpts, payload: unknown, final = false): void {
   304	  if (opts.quiet && !final) return;
   305	  writeLine(opts.stdout ?? process.stdout, JSON.stringify(payload));
   306	}
   307	
   308	function emitText(opts: InitOpts, text: string): void {
   309	  if (opts.quiet) return;
   310	  writeLine(opts.stdout ?? process.stdout, text);
   311	}
   312	
   313	function isAgentKind(value: string): value is AgentKind {
   314	  return (AGENT_KINDS as readonly string[]).includes(value);
   315	}
   316	
   317	function parseAgentSelection(input: string, fallback: readonly AgentKind[]): AgentKind[] {
   318	  if (input.trim().length === 0) return [...fallback];
   319	  const out: AgentKind[] = [];
   320	  for (const token of input
   321	    .split(',')
   322	    .map((part) => part.trim())
   323	    .filter(Boolean)) {
   324	    if (!isAgentKind(token)) throw new Error(`unknown agent kind: ${token}`);
   325	    out.push(token);
   326	  }
   327	  return out;
   328	}
   329	
   330	function readOnboardingState(): OnboardingState {
   331	  const raw = JSON.parse(readFileSync(ECHO_HOME_PATHS.stateOnboarding, 'utf8')) as unknown;
   332	  if (!validateOnboardingState(raw)) {
   333	    throw new Error(`${ECHO_HOME_PATHS.stateOnboarding}: invalid onboarding state`);
   334	  }
   335	  return raw;
   336	}
   337	
   338	function writeOnboardingState(state: OnboardingState): void {
   339	  mkdirSync(dirname(ECHO_HOME_PATHS.stateOnboarding), { recursive: true });
   340	  atomicWrite({
   341	    filePath: ECHO_HOME_PATHS.stateOnboarding,
   342	    content: `${JSON.stringify(state, null, 2)}\n`,
   343	    secretSensitive: false,
   344	  });
   345	}
   346	
   347	function readRecordedProfile(): {
   348	  exists: boolean;
   349	  profile: InstallProfile | null;
   350	  validShape: boolean;
   351	} {
   352	  if (!existsSync(ECHO_HOME_PATHS.stateOnboarding)) {
   353	    return { exists: false, profile: null, validShape: true };
   354	  }
   355	  try {
   356	    const raw = JSON.parse(readFileSync(ECHO_HOME_PATHS.stateOnboarding, 'utf8')) as unknown;
   357	    if (!validateOnboardingState(raw)) {
   358	      return { exists: true, profile: null, validShape: false };
   359	    }
   360	    return { exists: true, profile: raw.profile ?? null, validShape: true };
   361	  } catch {
   362	    return { exists: true, profile: null, validShape: false };
   363	  }
   364	}
   365	
   366	function resolveInstallProfile(input: {
   367	  cliProfile?: InstallProfile;
   368	  answerProfile?: InstallProfile;
   369	  recordedProfile: InstallProfile | null;
   370	}): InstallProfile {
   371	  return input.cliProfile ?? input.answerProfile ?? input.recordedProfile ?? 'customer';
   372	}
   373	
   374	function persistInstallProfile(profile: InstallProfile, now: Date): void {
   375	  const state = readOnboardingState();
   376	  state.profile = profile;
   377	  state.last_updated_at = now.toISOString();
   378	  writeOnboardingState(state);
   379	}
   380	
   381	function successfulAgents(result: WireResult, selected: readonly AgentKind[]): AgentKind[] {
   382	  const ok = new Set(
   383	    result.syncResult.agents.filter((agent) => agent.ok).map((agent) => agent.agent),
   384	  );
   385	  return selected.filter((agent) => ok.has(agent));
   386	}
   387	
   388	export function populateCapabilitiesForWiredAgents(
   389	  result: WireResult,
   390	  selectedAgents: readonly AgentKind[],
   391	): void {
   392	  const successful = new Set(successfulAgents(result, selectedAgents));
   393	  if (successful.size === 0) return;
   394	  const state = readOnboardingState();
   395	  let changed = false;
   396	  for (const profile of state.agents as OnboardedAgentProfile[]) {
   397	    if (!isAgentKind(profile.id) || !successful.has(profile.id)) continue;
   398	    const capabilities = [...AGENT_CAPABILITIES_BY_KIND[profile.id]];
   399	    if (JSON.stringify(profile.capabilities) !== JSON.stringify(capabilities)) {
   400	      profile.capabilities = capabilities;
   401	      changed = true;
   402	    }
   403	  }
   404	  if (changed) writeOnboardingState(state);
   405	}
   406	
   407	function topLevelFailure(result: WireResult): string | null {
   408	  if (result.syncResult.syncLock !== undefined) return result.syncResult.syncLock.message;
   409	  if (result.syncResult.directorySymlink !== undefined) {
   410	    return result.syncResult.directorySymlink.message;
   411	  }
   412	  return null;
   413	}
   414	
   415	function renderDaemonBringup(result: DaemonBringupResult): string {
   416	  switch (result.action) {
   417	    case 'installed-and-started':
   418	      return `Daemon installed and started on port ${result.port}.`;
   419	    case 'started':
   420	      return `Daemon started on port ${result.port}.`;
   421	    case 'already-running':
   422	      return `Daemon already running on port ${result.port}.`;
   423	  }
   424	}
   425	
   426	function daemonFailureCopy(err: DaemonCommandError): string {
   427	  return `daemon ${err.verb} failed: ${err.message}. Run \`echoctl daemon ${err.verb}\` manually with the same --home/--port/--label flags after addressing.`;
   428	}
   429	
   430	export async function runInit(opts: InitOpts = {}): Promise<number> {
   431	  const stderr = opts.stderr ?? process.stderr;
   432	  let answerFile: LoadedAnswerFile | null = null;
   433	  let mcpPort: number;
   434	  let installProfile: InstallProfile = 'customer';
   435	  let warnProfileDefault = false;
   436	  try {
   437	    if (opts.home !== undefined) setEchoHomeRoot(opts.home);
   438	    mcpPort = opts.port === undefined ? resolveMcpPort() : parsePort(opts.port);
   439	    if (opts.answerFile !== undefined) answerFile = loadAnswerFile(opts.answerFile);
   440	    const recorded = readRecordedProfile();
   441	    installProfile = resolveInstallProfile({
   442	      cliProfile: opts.profile,
   443	      answerProfile: answerFile?.answers.profile,
   444	      recordedProfile: recorded.profile,
   445	    });
   446	    warnProfileDefault =
   447	      opts.profile === undefined &&
   448	      answerFile?.answers.profile === undefined &&
   449	      recorded.exists &&
   450	      recorded.validShape &&
   451	      recorded.profile === null;
   452	  } catch (err) {
   453	    writeLine(stderr, (err as Error).message);
   454	    return 2;
   455	  }
   456	
   457	  if (answerFile === null && opts.stdin?.isTTY !== true && process.stdin.isTTY !== true) {
   458	    writeLine(
   459	      stderr,
   460	      'echoctl init: non-interactive — a TTY is required unless --answer-file <path> is provided.',
   461	    );
   462	    return 2;
   463	  }
   464	  ensureEchoHome();
   465	  persistInstallProfile(installProfile, opts.now?.() ?? new Date());
   466	  if (warnProfileDefault) {
   467	    writeLine(
   468	      stderr,
   469	      'echoctl init: onboarding profile was missing; defaulted to `customer`. Re-run `echoctl init --profile dogfood` to restore the full coordination surface.',
   470	    );
   471	  }
   472	
   473	  try {
   474	    const mcpServerUrl = `http://127.0.0.1:${mcpPort}/mcp`;
   475	    const echoVersion = readPackageVersion(opts.packageJsonPath);
   476	    const wizard = (opts.wizardFactory ?? createWizard)({
   477	      mcpServerUrl,
   478	      echoVersion,
   479	      now: opts.now,
   480	    });
   481	    const prompt = answerFile === null ? (opts.prompt ?? makeTtyPrompt()) : null;
   482	    const color = opts.color ?? false;
   483	
   484	    const shouldContinue =
   485	      answerFile?.answers.confirm_setup ??
   486	      (await prompt!.readConfirm('Welcome to ECHO setup. This takes about two minutes.', {
   487	        default: true,
   488	      }));
   489	    if (!shouldContinue) {
   490	      return 0;
   491	    }
   492	
   493	    const agents = await wizard.detectAgents();
   494	    if (opts.json) emitJson(opts, { event: 'init.detect-agents', agents });
   495	    else emitText(opts, renderDetectedAgents(agents, { color }));
   496	    const defaultAgents = agents
   497	      .filter((agent) => agent.confidence === 'high' || agent.confidence === 'medium')
   498	      .map((agent) => agent.kind);
   499	    const selectedAgents =
   500	      answerFile?.answers.selected_agents ??
   501	      parseAgentSelection(
   502	        await prompt!.readPrompt('Confirm subset to wire', { default: defaultAgents.join(',') }),
   503	        defaultAgents,
   504	      );
   505	
   506	    const projects = await wizard.detectProjects();
   507	    if (opts.json) emitJson(opts, { event: 'init.detect-projects', projects });
   508	    else emitText(opts, renderDetectedProjects(projects, { color }));
   509	    let defaultProjectRepoRoot: string | null;
   510	    if (answerFile !== null) {
   511	      defaultProjectRepoRoot = answerFile.answers.default_project_repo_root;
   512	    } else {
   513	      const projectAnswer = await prompt!.readPrompt('Pick default project', { default: '' });
   514	      const projectIdx = Number.parseInt(projectAnswer, 10);
   515	      defaultProjectRepoRoot =
   516	        Number.isInteger(projectIdx) && projectIdx >= 1 && projectIdx <= projects.length
   517	          ? projects[projectIdx - 1]!.repoRoot
   518	          : null;
   519	    }
   520	
   521	    let wire = await wizard.wire({
   522	      selectedAgents,
   523	      defaultProjectRepoRoot,
   524	      profile: installProfile,
   525	      force: opts.force === true,
   526	    });
   527	    if (wire.syncResult.repoRoot !== undefined) {
   528	      let repoRoot: string;
   529	      if (answerFile !== null && answerFile.answers.repo_root === undefined) {
   530	        writeLine(
   531	          stderr,
   532	          `${answerFile.path}: repo_root: required because ECHO could not locate its source tree`,
   533	        );
   534	        return 2;
   535	      }
   536	      if (answerFile !== null) {
   537	        repoRoot = answerFile.answers.repo_root!;
   538	      } else {
   539	        repoRoot = await prompt!.readPrompt(
   540	          'ECHO could not locate its source tree. Pass an explicit path',
   541	        );
   542	      }
   543	      wire = await wizard.wire({
   544	        selectedAgents,
   545	        defaultProjectRepoRoot,
   546	        profile: installProfile,
   547	        repoRoot,
   548	        force: opts.force === true,
   549	      });
   550	      if (wire.syncResult.repoRoot !== undefined) {
   551	        writeLine(stderr, wire.syncResult.repoRoot.message);
   552	        return 1;
   553	      }
   554	    }
   555	    if (opts.json) emitJson(opts, { event: 'init.wire', result: wire });
   556	    else emitText(opts, renderWireResult(wire, { color }));
   557	    const failure = topLevelFailure(wire);
   558	    if (failure !== null) {
   559	      writeLine(stderr, failure);
   560	      return 1;
   561	    }
   562	
   563	    populateCapabilitiesForWiredAgents(wire, selectedAgents);
   564	    const probes = await wizard.probe(selectedAgents);
   565	    if (opts.json) emitJson(opts, { event: 'init.probe', outcomes: probes });
   566	    else {
   567	      emitText(
   568	        opts,
   569	        renderProbeOutcomes(probes, { color, remediation: buildRemediationCopy(mcpServerUrl) }),
   570	      );
   571	    }
   572	    let daemonResult: DaemonBringupResult;
   573	    try {
   574	      daemonResult = await ensureDaemonRunning({
   575	        ...opts.daemonOptions,
   576	        label: opts.label ?? opts.daemonOptions?.label,
   577	        home: ECHO_HOME_PATHS.root,
   578	        port: String(mcpPort),
   579	        probeDeadlineMs: opts.daemonOptions?.probeDeadlineMs ?? 30_000,
   580	      });
   581	    } catch (err) {
   582	      if (err instanceof DaemonCommandError) {
   583	        writeLine(stderr, daemonFailureCopy(err));
   584	        return 1;
   585	      }
   586	      throw err;
   587	    }
   588	    if (opts.json) emitJson(opts, { event: 'init.daemon', result: daemonResult });
   589	    else emitText(opts, renderDaemonBringup(daemonResult));
   590	    await wizard.markCompleted();
   591	    if (opts.json)
   592	      emitJson(
   593	        opts,
   594	        { event: 'init.done', onboardingStatePath: ECHO_HOME_PATHS.stateOnboarding },
   595	        true,
   596	      );
   597	    else {
   598	      emitText(
   599	        opts,
   600	        `You're ready. Try \`echoctl run <workflow>\`; onboarding state: ${ECHO_HOME_PATHS.stateOnboarding}; uninstall: echoctl uninstall`,
   601	      );
   602	    }
   603	    return 0;
   604	  } catch (err) {
   605	    writeLine(stderr, (err as Error).message);
   606	    return 1;
   607	  }
   608	}

 succeeded in 218ms:
     1	import { existsSync, readFileSync, statSync } from 'node:fs';
     2	import { join } from 'node:path';
     3	import { parseArgs } from 'node:util';
     4	import { resolveDataDir } from '../../daemon/lifecycle.js';
     5	import {
     6	  ECHO_HOME_PATHS,
     7	  setEchoHomeRoot,
     8	  validateOnboardingState,
     9	  validateProjectsState,
    10	  type InstallProfile,
    11	  type OnboardedAgentProfile,
    12	  type OnboardingState,
    13	} from '../../echo-home/paths.js';
    14	import { type AgentKind } from '../../echo-home/wizard/detect-agents.js';
    15	import { probeAgents as realProbeAgents, type ProbeOutcome } from '../../echo-home/wizard/probe.js';
    16	import { renderDoctorReport } from '../io/render.js';
    17	import { buildRemediationCopy, parsePort, readPackageVersion, resolveMcpPort } from './init.js';
    18	
    19	export interface DoctorReport {
    20	  daemon: {
    21	    running: boolean;
    22	    port: number;
    23	    mcpReachable: boolean;
    24	    pidLockPath: string;
    25	    pidLockHeld: boolean;
    26	  };
    27	  echoHome: {
    28	    root: string;
    29	    exists: boolean;
    30	    onboardingValid: boolean;
    31	    projectsValid: boolean;
    32	    schemaVersion: 1 | 'mismatch' | 'missing';
    33	    profile: InstallProfile | 'unknown';
    34	  };
    35	  syncLock: { present: boolean; path: string; mtimeIso?: string; cleanupCommand?: string };
    36	  agents: {
    37	    kind: AgentKind;
    38	    profile: OnboardedAgentProfile | null;
    39	    probeOutcome: ProbeOutcome | null;
    40	  }[];
    41	  overall: 'healthy' | 'degraded' | 'broken';
    42	}
    43	
    44	export interface DoctorOpts {
    45	  json?: boolean;
    46	  quiet?: boolean;
    47	  color?: boolean;
    48	  home?: string;
    49	  port?: string | number;
    50	  label?: string;
    51	  stdout?: Pick<NodeJS.WritableStream, 'write'>;
    52	  stderr?: Pick<NodeJS.WritableStream, 'write'>;
    53	  probeAgents?: typeof realProbeAgents;
    54	  fetch?: typeof fetch;
    55	  now?: () => Date;
    56	}
    57	
    58	export const DOCTOR_HELP = `Usage: echoctl doctor [--json] [--quiet] [--home <path>] [--port <n>] [--label <id>]
    59	
    60	Options:
    61	  --home <path>   ECHO_HOME for this doctor run
    62	  --port <n>      MCP server port to probe
    63	  --label <id>    accepted for daemon-isolation parity; doctor does not query launchd`;
    64	
    65	function writeLine(stream: Pick<NodeJS.WritableStream, 'write'>, line: string): void {
    66	  stream.write(`${line}\n`);
    67	}
    68	
    69	export function shellQuote(s: string): string {
    70	  return `'${s.replace(/'/g, `'\\''`)}'`;
    71	}
    72	
    73	function isAgentKind(value: string): value is AgentKind {
    74	  return value === 'codex' || value === 'claude-code' || value === 'cursor';
    75	}
    76	
    77	function parseNonEmptyOption(value: string | undefined, flag: string): string | undefined {
    78	  if (value === undefined) return undefined;
    79	  if (value.trim().length === 0) throw new Error(`invalid ${flag}: expected non-empty string`);
    80	  return value;
    81	}
    82	
    83	export function parseDoctorArgs(
    84	  args: readonly string[],
    85	): Pick<DoctorOpts, 'home' | 'label' | 'port'> {
    86	  const parsed = parseArgs({
    87	    args: [...args],
    88	    strict: true,
    89	    allowPositionals: false,
    90	    options: {
    91	      home: { type: 'string' },
    92	      port: { type: 'string' },
    93	      label: { type: 'string' },
    94	    },
    95	  });
    96	  const port = parseNonEmptyOption(parsed.values.port, '--port');
    97	  if (port !== undefined) parsePort(port);
    98	  return {
    99	    home: parseNonEmptyOption(parsed.values.home, '--home'),
   100	    port,
   101	    label: parseNonEmptyOption(parsed.values.label, '--label'),
   102	  };
   103	}
   104	
   105	function resolveDoctorPort(port: DoctorOpts['port']): number {
   106	  if (port === undefined) return resolveMcpPort();
   107	  if (typeof port === 'number') return port;
   108	  return parsePort(port);
   109	}
   110	
   111	function readValidOnboarding(): OnboardingState | null {
   112	  try {
   113	    const raw = JSON.parse(readFileSync(ECHO_HOME_PATHS.stateOnboarding, 'utf8')) as unknown;
   114	    return validateOnboardingState(raw) ? raw : null;
   115	  } catch {
   116	    return null;
   117	  }
   118	}
   119	
   120	function stateVersion(): {
   121	  onboardingValid: boolean;
   122	  projectsValid: boolean;
   123	  schemaVersion: 1 | 'mismatch' | 'missing';
   124	  profile: InstallProfile | 'unknown';
   125	  onboarding: OnboardingState | null;
   126	} {
   127	  let onboardingValid = false;
   128	  let projectsValid = false;
   129	  let missing = false;
   130	  let onboarding: OnboardingState | null = null;
   131	  try {
   132	    const raw = JSON.parse(readFileSync(ECHO_HOME_PATHS.stateOnboarding, 'utf8')) as unknown;
   133	    onboardingValid = validateOnboardingState(raw);
   134	    if (onboardingValid) onboarding = raw as OnboardingState;
   135	  } catch {
   136	    missing = true;
   137	  }
   138	  try {
   139	    const raw = JSON.parse(readFileSync(ECHO_HOME_PATHS.stateProjects, 'utf8')) as unknown;
   140	    projectsValid = validateProjectsState(raw);
   141	  } catch {
   142	    missing = true;
   143	  }
   144	  return {
   145	    onboardingValid,
   146	    projectsValid,
   147	    schemaVersion: missing ? 'missing' : onboardingValid && projectsValid ? 1 : 'mismatch',
   148	    profile: onboardingValid ? (onboarding?.profile ?? 'customer') : 'unknown',
   149	    onboarding,
   150	  };
   151	}
   152	
   153	async function probeMcp(fetchImpl: typeof fetch, port: number): Promise<boolean> {
   154	  const controller = new AbortController();
   155	  const timer = setTimeout(() => controller.abort(), 2000);
   156	  try {
   157	    const response = await fetchImpl(`http://127.0.0.1:${port}/mcp`, {
   158	      method: 'POST',
   159	      headers: {
   160	        'Content-Type': 'application/json',
   161	        Accept: 'application/json, text/event-stream',
   162	      },
   163	      body: JSON.stringify({
   164	        jsonrpc: '2.0',
   165	        method: 'initialize',
   166	        params: {
   167	          protocolVersion: '2025-06-18',
   168	          capabilities: {},
   169	          clientInfo: { name: 'echoctl-doctor', version: readPackageVersion() },
   170	        },
   171	        id: 1,
   172	      }),
   173	      signal: controller.signal,
   174	    });
   175	    return response.status >= 200 && response.status < 300;
   176	  } catch {
   177	    return false;
   178	  } finally {
   179	    clearTimeout(timer);
   180	  }
   181	}
   182	
   183	export function computeOverall(report: DoctorReport): 'healthy' | 'degraded' | 'broken' {
   184	  if (
   185	    !report.echoHome.exists ||
   186	    report.echoHome.schemaVersion === 'mismatch' ||
   187	    report.echoHome.schemaVersion === 'missing'
   188	  ) {
   189	    return 'broken';
   190	  }
   191	  if (!report.daemon.pidLockHeld && !report.daemon.mcpReachable) return 'broken';
   192	  if (report.daemon.pidLockHeld && !report.daemon.mcpReachable) return 'degraded';
   193	  if (report.syncLock.present) return 'degraded';
   194	  for (const agent of report.agents) {
   195	    const outcome = agent.probeOutcome;
   196	    if (agent.profile?.wired_at === null || outcome === null) continue;
   197	    if (!outcome.probed && outcome.reason !== 'manual-only') return 'degraded';
   198	  }
   199	  return 'healthy';
   200	}
   201	
   202	export async function buildDoctorReport(opts: DoctorOpts = {}): Promise<DoctorReport> {
   203	  if (opts.home !== undefined) setEchoHomeRoot(opts.home);
   204	  const port = resolveDoctorPort(opts.port);
   205	  const pidLockPath = join(resolveDataDir(), 'daemon.pid');
   206	  const pidLockHeld = existsSync(pidLockPath);
   207	  const mcpReachable = await probeMcp(opts.fetch ?? fetch, port);
   208	  const echoHomeExists = existsSync(ECHO_HOME_PATHS.root);
   209	  const state = echoHomeExists
   210	    ? stateVersion()
   211	    : {
   212	        onboardingValid: false,
   213	        projectsValid: false,
   214	        schemaVersion: 'missing' as const,
   215	        profile: 'unknown' as const,
   216	        onboarding: null,
   217	      };
   218	
   219	  const syncLockPath = join(ECHO_HOME_PATHS.state, 'adapter-sync.lock');
   220	  const syncLock: DoctorReport['syncLock'] = { present: false, path: syncLockPath };
   221	  if (existsSync(syncLockPath)) {
   222	    const mtimeIso = (opts.now?.() ?? statSync(syncLockPath).mtime).toISOString();
   223	    syncLock.present = true;
   224	    syncLock.mtimeIso = mtimeIso;
   225	    syncLock.cleanupCommand = `rm -- ${shellQuote(syncLockPath)}`;
   226	  }
   227	
   228	  const probe = opts.probeAgents ?? realProbeAgents;
   229	  const onboarding = state.onboarding ?? readValidOnboarding();
   230	  const agents: DoctorReport['agents'] = [];
   231	  if (onboarding !== null) {
   232	    for (const profile of onboarding.agents) {
   233	      if (!isAgentKind(profile.id)) continue;
   234	      const probeOutcome =
   235	        profile.wired_at === null ? null : ((await probe([profile.id]))[0] ?? null);
   236	      agents.push({ kind: profile.id, profile, probeOutcome });
   237	    }
   238	  }
   239	
   240	  const report: DoctorReport = {
   241	    daemon: {
   242	      running: mcpReachable,
   243	      port,
   244	      mcpReachable,
   245	      pidLockPath,
   246	      pidLockHeld,
   247	    },
   248	    echoHome: {
   249	      root: ECHO_HOME_PATHS.root,
   250	      exists: echoHomeExists,
   251	      onboardingValid: state.onboardingValid,
   252	      projectsValid: state.projectsValid,
   253	      schemaVersion: state.schemaVersion,
   254	      profile: state.profile,
   255	    },
   256	    syncLock,
   257	    agents,
   258	    overall: 'broken',
   259	  };
   260	  report.overall = computeOverall(report);
   261	  return report;
   262	}
   263	
   264	export async function runDoctor(opts: DoctorOpts = {}): Promise<number> {
   265	  const stderr = opts.stderr ?? process.stderr;
   266	  let port: number;
   267	  try {
   268	    if (opts.home !== undefined) setEchoHomeRoot(opts.home);
   269	    if (opts.label !== undefined) parseNonEmptyOption(opts.label, '--label');
   270	    port = resolveDoctorPort(opts.port);
   271	  } catch (err) {
   272	    writeLine(stderr, (err as Error).message);
   273	    return 2;
   274	  }
   275	
   276	  try {
   277	    const report = await buildDoctorReport({ ...opts, home: undefined, port });
   278	    if (!opts.quiet) {
   279	      if (opts.json) writeLine(opts.stdout ?? process.stdout, JSON.stringify(report));
   280	      else
   281	        writeLine(
   282	          opts.stdout ?? process.stdout,
   283	          renderDoctorReport(report, {
   284	            color: opts.color ?? false,
   285	            remediation: buildRemediationCopy(`http://127.0.0.1:${port}/mcp`),
   286	          }),
   287	        );
   288	    }
   289	    return report.overall === 'healthy' ? 0 : 1;
   290	  } catch (err) {
   291	    writeLine(stderr, (err as Error).message);
   292	    return 1;
   293	  }
   294	}

 succeeded in 212ms:
     1	#!/usr/bin/env bash
     2	# foreign-install-smoke.sh — simulate a fresh non-founder install of echoctl in
     3	# full isolation (fake $HOME + seeded agent configs, test launchd label/port,
     4	# isolated ECHO_HOME/data-dir/db-path/log-dir). Builds the tarball, installs it,
     5	# brings up the daemon, probes MCP, runs `init` (repo_root omitted, to prove the
     6	# packaged install locates its own assets), inspects wiring + skills, then tears
     7	# down the test launchd job and verifies the production daemon (38478) is intact.
     8	# Provenance: written 2026-06-01 for the n=1 concierge-install pre-flight.
     9	set -uo pipefail
    10	REPO="$HOME/Desktop/Project_echo"   # capture BEFORE we fake HOME
    11	ORIG_HOME="$HOME"
    12	SBOX=/tmp/echo-sbox
    13	LABEL=com.echo.daemon.sboxtest
    14	PORT=41789
    15	BIN="$SBOX/npm/bin/echoctl"
    16	PLIST="$SBOX/$LABEL.plist"
    17	SHIMS="$SBOX/shims"
    18	CLAUDE_ARGS="$SBOX/claude-argv.log"
    19	
    20	rm -rf "$SBOX"
    21	mkdir -p "$SBOX"/{npm,data,logs,shims,fakehome/.claude,fakehome/.codex}
    22	
    23	# Seed fake foreign-coworker agent configs so detect-agents finds them
    24	# WITHOUT ever reading/writing the real ~/.claude or ~/.codex.
    25	printf '# CLAUDE.md (fake foreign coworker machine)\n\nSome of his own instructions here.\n' > "$SBOX/fakehome/.claude/CLAUDE.md"
    26	printf '# codex config.toml (fake foreign coworker machine)\nmodel = "gpt-5.5"\n' > "$SBOX/fakehome/.codex/config.toml"
    27	cat > "$SHIMS/claude" <<'SH'
    28	#!/usr/bin/env bash
    29	set -u
    30	if [ "${1:-}" = "mcp" ]; then
    31	  printf '%s\n' "$*" >> "${ECHO_FAKE_CLAUDE_ARGS:?}"
    32	  exit 0
    33	fi
    34	if [ "${1:-}" = "--print" ]; then
    35	  printf '{"pong":true,"ts":"2026-06-01T00:00:00.000Z"}\n'
    36	  exit 0
    37	fi
    38	printf 'unexpected fake claude argv: %s\n' "$*" >&2
    39	exit 2
    40	SH
    41	chmod +x "$SHIMS/claude"
    42	: > "$CLAUDE_ARGS"
    43	
    44	cleanup() {
    45	  echo ""; echo "=================== CLEANUP ==================="
    46	  if [ -x "$BIN" ]; then
    47	    HOME="$SBOX/fakehome" "$BIN" daemon uninstall --label "$LABEL" --plist-path "$PLIST" --port "$PORT" --home "$SBOX/fakehome/.echo" 2>&1 | sed 's/^/[cleanup] /' || true
    48	  fi
    49	  launchctl bootout "gui/$(id -u)/$LABEL" 2>/dev/null || true
    50	  echo "[cleanup] verifying production daemon untouched (should still be on 38478):"
    51	  launchctl print "gui/$(id -u)/com.echo.daemon" >/dev/null 2>&1 && echo "[cleanup] production com.echo.daemon STILL LOADED ✓" || echo "[cleanup] production com.echo.daemon not loaded (or never was)"
    52	  rm -rf "$SBOX"
    53	  echo "[cleanup] sandbox removed."
    54	}
    55	trap cleanup EXIT
    56	
    57	fail() {
    58	  echo "[assert] ERROR: $*" >&2
    59	  exit 1
    60	}
    61	
    62	assert_exists() {
    63	  [ -e "$1" ] || fail "expected path to exist: $1"
    64	}
    65	
    66	assert_absent() {
    67	  [ ! -e "$1" ] || fail "expected path to be absent: $1"
    68	}
    69	
    70	assert_profile() {
    71	  local expected="$1"
    72	  node -e 'const fs=require("fs"); const p=process.argv[1]; const expected=process.argv[2]; const state=JSON.parse(fs.readFileSync(p,"utf8")); if (state.profile !== expected) { console.error(`[assert] expected profile ${expected}, got ${state.profile}`); process.exit(1); }' "$ECHO_HOME/state/onboarding.json" "$expected" \
    73	    || fail "onboarding profile mismatch"
    74	}
    75	
    76	cd "$REPO"
    77	echo "=================== STEP 0: build fresh tarball ==================="
    78	npm pack 2>&1 | tail -2
    79	TGZ="$REPO/echoctl-0.1.0.tgz"
    80	ls -lh "$TGZ"
    81	
    82	echo ""; echo "=================== STEP 1: npm install -g into sandbox prefix ==================="
    83	npm install -g "$TGZ" --prefix "$SBOX/npm" 2>&1 | tail -4
    84	
    85	# From here on, become the "foreign machine": fake HOME.
    86	export HOME="$SBOX/fakehome"
    87	export PATH="$SHIMS:$SBOX/npm/bin:$PATH"
    88	export ECHO_HOME="$SBOX/fakehome/.echo"
    89	export ECHO_MCP_PORT="$PORT"
    90	export ECHO_DATA_DIR="$SBOX/data"
    91	export ECHO_DB_PATH="$SBOX/data/echo.db"
    92	export ECHO_FAKE_CLAUDE_ARGS="$CLAUDE_ARGS"
    93	
    94	echo ""; echo "=== echoctl --version (from packaged global install) ==="
    95	"$BIN" --version 2>&1 || echo "VERSION FAILED"
    96	
    97	echo ""; echo "=== echoctl doctor BEFORE daemon (expect broken/no-daemon) ==="
    98	"$BIN" doctor 2>&1 | head -40 || true
    99	
   100	echo ""; echo "=================== STEP 2: daemon install (isolated label/port/paths) ==================="
   101	"$BIN" daemon install --label "$LABEL" --plist-path "$PLIST" --home "$ECHO_HOME" --port "$PORT" --data-dir "$SBOX/data" --db-path "$SBOX/data/echo.db" --log-dir "$SBOX/logs" 2>&1 | head -40
   102	
   103	echo ""; echo "=== formation probe: is the daemon actually serving MCP on $PORT? ==="
   104	curl -s -m 6 -X POST "http://127.0.0.1:$PORT/mcp" \
   105	  -H 'content-type: application/json' -H 'accept: application/json, text/event-stream' \
   106	  -d '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{"protocolVersion":"2025-06-18","capabilities":{},"clientInfo":{"name":"sbox","version":"0"}}}' \
   107	  | head -c 500; echo " <<EOF"
   108	
   109	echo ""; echo "=================== STEP 3: init via answer-file, repo_root OMITTED ==================="
   110	echo "(This tests your worry: can a packaged install LOCATE its skills/source on a machine with no repo?)"
   111	cat > "$SBOX/answers.json" <<'JSON'
   112	{ "confirm_setup": true, "selected_agents": ["codex","claude-code"], "default_project_repo_root": null }
   113	JSON
   114	INIT_LOG="$SBOX/init.out"
   115	"$BIN" init --home "$ECHO_HOME" --port "$PORT" --label "$LABEL" --answer-file "$SBOX/answers.json" > "$INIT_LOG" 2>&1
   116	INIT_RC=$?
   117	sed -n '1,70p' "$INIT_LOG"
   118	echo "(init exit code: $INIT_RC)"
   119	[ "$INIT_RC" -eq 0 ] || fail "echoctl init exited $INIT_RC"
   120	
   121	EXPECTED_CLAUDE_ARGV="mcp add --transport http --scope user echo http://127.0.0.1:$PORT/mcp"
   122	if ! grep -Fxq "$EXPECTED_CLAUDE_ARGV" "$CLAUDE_ARGS"; then
   123	  echo "[assert] recorded fake claude argv:" >&2
   124	  sed 's/^/[assert]   /' "$CLAUDE_ARGS" >&2
   125	  fail "claude-code MCP registration argv missing or mismatched; expected: $EXPECTED_CLAUDE_ARGV"
   126	fi
   127	echo "[assert] claude-code MCP registration argv OK"
   128	
   129	echo ""; echo "=== what skills landed in the user's ~/.echo/skills? ==="
   130	ls -1 "$ECHO_HOME/skills" 2>&1 || echo "(no skills dir created)"
   131	assert_profile customer
   132	assert_exists "$ECHO_HOME/skills/using-echo-mcp.md"
   133	assert_absent "$ECHO_HOME/skills/using-echo-coord.md"
   134	assert_absent "$SBOX/fakehome/.claude/commands/using-echo-coord.md"
   135	assert_absent "$ECHO_HOME/roles/builder.toml"
   136	assert_absent "$ECHO_HOME/roles/reviewer.toml"
   137	assert_absent "$ECHO_HOME/roles/strategist.toml"
   138	assert_absent "$ECHO_HOME/workflows/change-review.toml"
   139	echo "[assert] default customer surface OK"
   140	
   141	echo ""; echo "=== did wire inject an ECHO block into his codex config.toml? ==="
   142	grep -nE "ECHO|echo|mcp|38478|$PORT" "$SBOX/fakehome/.codex/config.toml" 2>/dev/null | head || echo "(no ECHO content in codex config)"
   143	echo ""; echo "=== into his CLAUDE.md? ==="
   144	grep -nE "BEGIN ECHO|END ECHO|ECHO" "$SBOX/fakehome/.claude/CLAUDE.md" 2>/dev/null | head || echo "(no ECHO block in CLAUDE.md)"
   145	
   146	echo ""; echo "=================== STEP 4: doctor AFTER install ==================="
   147	"$BIN" doctor 2>&1 | head -50 || true
   148	
   149	echo ""; echo "=================== STEP 5: no-flag rerun stays customer ==================="
   150	"$BIN" init --home "$ECHO_HOME" --port "$PORT" --label "$LABEL" --answer-file "$SBOX/answers.json" > "$SBOX/init-rerun.out" 2>&1
   151	RERUN_RC=$?
   152	sed -n '1,40p' "$SBOX/init-rerun.out"
   153	[ "$RERUN_RC" -eq 0 ] || fail "echoctl init rerun exited $RERUN_RC"
   154	assert_profile customer
   155	assert_absent "$ECHO_HOME/skills/using-echo-coord.md"
   156	assert_absent "$SBOX/fakehome/.claude/commands/using-echo-coord.md"
   157	echo "[assert] no-flag customer rerun stayed customer"
   158	
   159	echo ""; echo "=================== STEP 6: explicit dogfood install gets full coord surface ==================="
   160	cat > "$SBOX/answers-dogfood.json" <<'JSON'
   161	{ "confirm_setup": true, "selected_agents": ["codex","claude-code"], "default_project_repo_root": null }
   162	JSON
   163	"$BIN" init --home "$ECHO_HOME" --port "$PORT" --profile dogfood --label "$LABEL" --answer-file "$SBOX/answers-dogfood.json" > "$SBOX/init-dogfood.out" 2>&1
   164	DOGFOOD_RC=$?
   165	sed -n '1,50p' "$SBOX/init-dogfood.out"
   166	[ "$DOGFOOD_RC" -eq 0 ] || fail "echoctl init --profile dogfood exited $DOGFOOD_RC"
   167	assert_profile dogfood
   168	assert_exists "$ECHO_HOME/skills/using-echo-mcp.md"
   169	assert_exists "$ECHO_HOME/skills/using-echo-coord.md"
   170	assert_exists "$SBOX/fakehome/.claude/commands/using-echo-coord.md"
   171	assert_exists "$ECHO_HOME/roles/builder.toml"
   172	assert_exists "$ECHO_HOME/roles/reviewer.toml"
   173	assert_exists "$ECHO_HOME/roles/strategist.toml"
   174	assert_exists "$ECHO_HOME/workflows/change-review.toml"
   175	echo "[assert] explicit dogfood surface OK"

 succeeded in 209ms:
     1	import { detectAgents, type DetectedAgent, type DetectAgentsDeps } from './detect-agents.js';
     2	import {
     3	  detectProjects,
     4	  type DetectedProject,
     5	  type DetectProjectsDeps,
     6	} from './detect-projects.js';
     7	import { probeAgents, type ProbeDeps, type ProbeOutcome } from './probe.js';
     8	import {
     9	  markOnboardingCompleted,
    10	  readOnboardingStateSnapshot,
    11	  updateProbedAgents,
    12	  wire as wireAgents,
    13	  type WireOpts,
    14	  type WireResult,
    15	} from './wire.js';
    16	import type { AgentKind } from './detect-agents.js';
    17	import type { OnboardingState } from '../paths.js';
    18	
    19	export interface CreateWizardOpts {
    20	  mcpServerUrl: string;
    21	  echoVersion: string;
    22	  detectAgentsDeps?: DetectAgentsDeps;
    23	  detectProjectsDeps?: DetectProjectsDeps;
    24	  wireDepsOverride?: Partial<WireOpts>;
    25	  probeDeps?: ProbeDeps;
    26	  now?: () => Date;
    27	}
    28	
    29	export interface WizardSummary {
    30	  detected: DetectedAgent[] | null;
    31	  projects: DetectedProject[] | null;
    32	  wired: WireResult | null;
    33	  probed: ProbeOutcome[] | null;
    34	  onboardingStateSnapshot: OnboardingState | null;
    35	}
    36	
    37	export interface Wizard {
    38	  detectAgents(): Promise<DetectedAgent[]>;
    39	  detectProjects(): Promise<DetectedProject[]>;
    40	  wire(
    41	    opts: Pick<
    42	      WireOpts,
    43	      'selectedAgents' | 'defaultProjectRepoRoot' | 'profile' | 'repoRoot' | 'force'
    44	    >,
    45	  ): Promise<WireResult>;
    46	  probe(agents: AgentKind[]): Promise<ProbeOutcome[]>;
    47	  summary(): Promise<WizardSummary>;
    48	  markCompleted(): Promise<void>;
    49	}
    50	
    51	export function createWizard(opts: CreateWizardOpts): Wizard {
    52	  let detected: DetectedAgent[] | null = null;
    53	  let projects: DetectedProject[] | null = null;
    54	  let wired: WireResult | null = null;
    55	  let probed: ProbeOutcome[] | null = null;
    56	  const now = opts.now ?? (() => new Date());
    57	
    58	  return {
    59	    async detectAgents(): Promise<DetectedAgent[]> {
    60	      detected = await detectAgents({ ...(opts.detectAgentsDeps ?? {}), now: now() });
    61	      return detected;
    62	    },
    63	
    64	    async detectProjects(): Promise<DetectedProject[]> {
    65	      projects = await detectProjects({ ...(opts.detectProjectsDeps ?? {}), now: now() });
    66	      return projects;
    67	    },
    68	
    69	    async wire(
    70	      wireOpts: Pick<
    71	        WireOpts,
    72	        'selectedAgents' | 'defaultProjectRepoRoot' | 'profile' | 'repoRoot' | 'force'
    73	      >,
    74	    ): Promise<WireResult> {
    75	      wired = await wireAgents({
    76	        ...(opts.wireDepsOverride ?? {}),
    77	        ...wireOpts,
    78	        mcpServerUrl: opts.mcpServerUrl,
    79	        echoVersion: opts.echoVersion,
    80	        now: now(),
    81	      });
    82	      return wired;
    83	    },
    84	
    85	    async probe(agents: AgentKind[]): Promise<ProbeOutcome[]> {
    86	      const outcomes = await probeAgents(agents, opts.probeDeps);
    87	      probed = outcomes;
    88	      updateProbedAgents(outcomes, now());
    89	      return outcomes;
    90	    },
    91	
    92	    async summary(): Promise<WizardSummary> {
    93	      return {
    94	        detected,
    95	        projects,
    96	        wired,
    97	        probed,
    98	        onboardingStateSnapshot: readOnboardingStateSnapshot(),
    99	      };
   100	    },
   101	
   102	    async markCompleted(): Promise<void> {
   103	      markOnboardingCompleted(now());
   104	    },
   105	  };
   106	}

codex
The implementation lines up structurally with the selected install-time filtering approach. I’m checking the tests for the profile precedence and crash-window cases, because that’s where this spec’s highest-risk behavior lives.
exec
/bin/zsh -lc 'nl -ba tests/cli/init.test.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba tests/echo-home/adapter-sync.test.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba tests/cli/doctor.test.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git diff --check main...agent/install-profile-split' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba tests/echo-home/adapters/skill-sync.test.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git diff --stat main...agent/install-profile-split' in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 209ms:
     1	import { existsSync, mkdirSync, mkdtempSync, readFileSync, rmSync, writeFileSync } from 'node:fs';
     2	import { tmpdir } from 'node:os';
     3	import { join } from 'node:path';
     4	import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
     5	import type { SpawnSyncReturns } from 'node:child_process';
     6	import type { InitOpts } from '../../src/cli/commands/init.js';
     7	import type { AdapterSyncProfile } from '../../src/echo-home/adapter-sync.js';
     8	import type { CreateWizardOpts, Wizard } from '../../src/echo-home/wizard/run-wizard.js';
     9	import type { WireResult } from '../../src/echo-home/wizard/wire.js';
    10	import type { AgentKind, DetectedAgent } from '../../src/echo-home/wizard/detect-agents.js';
    11	import { makeNonInteractivePrompt } from '../../src/cli/io/prompt.js';
    12	import { BEGIN_MARKER, END_MARKER } from '../../src/echo-home/adapters/markers.js';
    13	
    14	let tmpRoot: string;
    15	let echoHome: string;
    16	let originalEchoHome: string | undefined;
    17	let originalPort: string | undefined;
    18	
    19	interface LaunchctlCall {
    20	  command: string;
    21	  args: string[];
    22	}
    23	
    24	interface DaemonFixture {
    25	  calls: LaunchctlCall[];
    26	  daemonOptions: NonNullable<InitOpts['daemonOptions']>;
    27	  plistPath: string;
    28	}
    29	
    30	type WireDepsOverride = NonNullable<CreateWizardOpts['wireDepsOverride']>;
    31	type ClaudeRegistrationDeps = NonNullable<WireDepsOverride['claudeCodeMcpRegistration']>;
    32	
    33	async function loadInit(): Promise<typeof import('../../src/cli/commands/init.js')> {
    34	  return import('../../src/cli/commands/init.js');
    35	}
    36	
    37	function writeInitialState(): void {
    38	  mkdirSync(join(echoHome, 'state'), { recursive: true });
    39	  writeFileSync(
    40	    join(echoHome, 'state/onboarding.json'),
    41	    `${JSON.stringify(
    42	      {
    43	        schema_version: 1,
    44	        created_at: '2026-05-26T00:00:00.000Z',
    45	        last_updated_at: '2026-05-26T00:00:00.000Z',
    46	        completed: false,
    47	        agents: [],
    48	      },
    49	      null,
    50	      2,
    51	    )}\n`,
    52	  );
    53	}
    54	
    55	function successWire(selected: AgentKind[], home = echoHome): WireResult {
    56	  const now = '2026-05-26T00:00:01.000Z';
    57	  const state = JSON.parse(readFileSync(join(home, 'state/onboarding.json'), 'utf8')) as {
    58	    agents: Array<Record<string, unknown>>;
    59	  };
    60	  for (const id of selected) {
    61	    state.agents.push({
    62	      id,
    63	      detected_at: now,
    64	      wired_at: now,
    65	      probed_at: null,
    66	      capabilities: [],
    67	      wire_error: null,
    68	    });
    69	  }
    70	  writeFileSync(join(home, 'state/onboarding.json'), `${JSON.stringify(state, null, 2)}\n`);
    71	  return {
    72	    syncResult: {
    73	      skillsPopulated: { ok: true, copied: [], skipped: [], targetDir: join(home, 'skills') },
    74	      agents: selected.map((agent) => ({
    75	        agent,
    76	        ok: true as const,
    77	        files_written: [],
    78	        actions: [],
    79	      })),
    80	      roles: { results: [], rolesErrors: [] },
    81	      overallOk: true,
    82	    },
    83	    cacheUpdates: [],
    84	    onboardingStateUpdated: true,
    85	  };
    86	}
    87	
    88	function writeAnswerFile(name: string, value: unknown): string {
    89	  const path = join(tmpRoot, name);
    90	  writeFileSync(path, `${JSON.stringify(value, null, 2)}\n`);
    91	  return path;
    92	}
    93	
    94	function detected(kind: AgentKind, confidence: DetectedAgent['confidence']): DetectedAgent {
    95	  return {
    96	    kind,
    97	    confidence,
    98	    signals: {
    99	      configFile: { path: `/tmp/${kind}`, exists: true, readableMode: true },
   100	      atomActivity: { count: 1, lastSeen: '2026-05-26T00:00:00.000Z' },
   101	      atomCountSaturated: false,
   102	    },
   103	  };
   104	}
   105	
   106	function spawnResult(status: number, stdout = '', stderr = ''): SpawnSyncReturns<string> {
   107	  return {
   108	    pid: 0,
   109	    output: [null, stdout, stderr],
   110	    stdout,
   111	    stderr,
   112	    status,
   113	    signal: null,
   114	  };
   115	}
   116	
   117	function writeDaemonRuntimePackage(packageRoot: string): string {
   118	  const daemonPath = join(packageRoot, 'dist/daemon/index.js');
   119	  mkdirSync(join(packageRoot, 'dist/daemon'), { recursive: true });
   120	  writeFileSync(daemonPath, 'console.log("daemon");\n');
   121	  const migrationsDir = join(packageRoot, 'dist/storage/migrations');
   122	  mkdirSync(migrationsDir, { recursive: true });
   123	  writeFileSync(join(migrationsDir, '001-init.sql'), 'select 1;\n');
   124	  mkdirSync(join(packageRoot, 'tools/review-queue/schemas'), { recursive: true });
   125	  writeFileSync(join(packageRoot, 'tools/review-queue/coord-roles.json'), '{}\n');
   126	  writeFileSync(join(packageRoot, 'tools/review-queue/reviewers.json'), '{}\n');
   127	  writeFileSync(join(packageRoot, 'tools/review-queue/schemas/coord-roles.schema.json'), '{}\n');
   128	  return daemonPath;
   129	}
   130	
   131	function daemonFixture(opts: {
   132	  launchdRegistered: boolean;
   133	  health?: boolean;
   134	  nodeOk?: boolean;
   135	  plutilOk?: boolean;
   136	  writePlist?: boolean;
   137	}): DaemonFixture {
   138	  const calls: LaunchctlCall[] = [];
   139	  const packageRoot = join(tmpRoot, `daemon-pkg-${calls.length}-${Math.random()}`);
   140	  const daemonPath = writeDaemonRuntimePackage(packageRoot);
   141	  const plistPath = join(tmpRoot, `launchd/${Math.random()}.plist`);
   142	  const logDir = join(tmpRoot, 'daemon-logs');
   143	  const dataDir = join(tmpRoot, 'daemon-data');
   144	  const dbPath = join(dataDir, 'echo.db');
   145	  if (opts.writePlist === true) {
   146	    mkdirSync(join(tmpRoot, 'launchd'), { recursive: true });
   147	    writeFileSync(
   148	      plistPath,
   149	      `<?xml version="1.0" encoding="UTF-8"?>
   150	<plist version="1.0">
   151	<dict>
   152	  <key>ProgramArguments</key>
   153	  <array>
   154	    <string>/node/v22/bin/node</string>
   155	    <string>${daemonPath}</string>
   156	  </array>
   157	  <key>StandardOutPath</key>
   158	  <string>${join(logDir, 'echo-daemon.out.log')}</string>
   159	  <key>EnvironmentVariables</key>
   160	  <dict>
   161	    <key>ECHO_HOME</key>
   162	    <string>${echoHome}</string>
   163	    <key>ECHO_MCP_PORT</key>
   164	    <string>39999</string>
   165	    <key>ECHO_DATA_DIR</key>
   166	    <string>${dataDir}</string>
   167	    <key>ECHO_DB_PATH</key>
   168	    <string>${dbPath}</string>
   169	  </dict>
   170	</dict>
   171	</plist>
   172	`,
   173	    );
   174	  }
   175	  const spawnSync: NonNullable<NonNullable<InitOpts['daemonOptions']>['spawnSync']> = ((
   176	    command: string,
   177	    args: readonly string[],
   178	  ) => {
   179	    const argv = [...args];
   180	    calls.push({ command, args: argv });
   181	    if (command === '/node/v22/bin/node') {
   182	      return opts.nodeOk === false
   183	        ? spawnResult(1, '', 'node exploded\n')
   184	        : spawnResult(0, 'v22.1.0\n');
   185	    }
   186	    if (command === 'plutil') {
   187	      return opts.plutilOk === false
   188	        ? spawnResult(1, '', 'bad plist\n')
   189	        : spawnResult(0, `${argv[1]}: OK\n`);
   190	    }
   191	    if (command === 'launchctl' && argv[0] === 'print') {
   192	      return opts.launchdRegistered
   193	        ? spawnResult(0, 'pid = 12345\nstate = running\n')
   194	        : spawnResult(3, '', 'not loaded\n');
   195	    }
   196	    if (command === 'launchctl') return spawnResult(0);
   197	    return spawnResult(0);
   198	  }) as NonNullable<NonNullable<InitOpts['daemonOptions']>['spawnSync']>;
   199	  return {
   200	    calls,
   201	    plistPath,
   202	    daemonOptions: {
   203	      plistPath,
   204	      logDir,
   205	      dataDir,
   206	      dbPath,
   207	      spawnSync,
   208	      healthProbe: async () => opts.health ?? true,
   209	      sleep: async () => {},
   210	      getuid: () => 501,
   211	      processExecPath: '/node/v22/bin/node',
   212	      daemonPath,
   213	      probeDeadlineMs: 0,
   214	    },
   215	  };
   216	}
   217	
   218	function daemonAlreadyRunning(): Pick<InitOpts, 'daemonOptions'> {
   219	  return { daemonOptions: daemonFixture({ launchdRegistered: true }).daemonOptions };
   220	}
   221	
   222	async function makeCodexSyncWizardFactory(opts: {
   223	  clientHome: string;
   224	  codexConfig: string;
   225	  codexInstructions: string;
   226	}): Promise<(wizardOpts: CreateWizardOpts) => Wizard> {
   227	  const { createWizard } = await import('../../src/echo-home/wizard/run-wizard.js');
   228	  const { syncAll } = await import('../../src/echo-home/adapter-sync.js');
   229	  return (wizardOpts: CreateWizardOpts): Wizard =>
   230	    createWizard({
   231	      ...wizardOpts,
   232	      detectAgentsDeps: { homedir: opts.clientHome, atomStore: null },
   233	      detectProjectsDeps: { atomStore: null },
   234	      probeDeps: {
   235	        spawn: async () => ({
   236	          exitCode: 0,
   237	          stdout: '{"pong":true,"ts":"2026-05-26T00:00:00.000Z"}\n',
   238	          stderr: '',
   239	          timedOut: false,
   240	        }),
   241	      },
   242	      wireDepsOverride: {
   243	        syncAll: async (profiles, syncOpts) =>
   244	          syncAll(
   245	            profiles.map((profile): AdapterSyncProfile => {
   246	              if (profile.kind === 'codex') {
   247	                return {
   248	                  ...profile,
   249	                  paths: {
   250	                    configFile: opts.codexConfig,
   251	                    instructionsFile: opts.codexInstructions,
   252	                  },
   253	                };
   254	              }
   255	              return profile;
   256	            }),
   257	            syncOpts,
   258	          ),
   259	      },
   260	    });
   261	}
   262	
   263	async function makeClaudeSyncWizardFactory(opts: {
   264	  clientHome: string;
   265	  claudeInstructions: string;
   266	  claudeCommands: string;
   267	  registerSpawn: NonNullable<ClaudeRegistrationDeps['spawn']>;
   268	  registerTimeoutMs?: number;
   269	  probeSpawn?: NonNullable<NonNullable<CreateWizardOpts['probeDeps']>['spawn']>;
   270	}): Promise<(wizardOpts: CreateWizardOpts) => Wizard> {
   271	  const { createWizard } = await import('../../src/echo-home/wizard/run-wizard.js');
   272	  const { syncAll } = await import('../../src/echo-home/adapter-sync.js');
   273	  return (wizardOpts: CreateWizardOpts): Wizard =>
   274	    createWizard({
   275	      ...wizardOpts,
   276	      detectAgentsDeps: { homedir: opts.clientHome, atomStore: null },
   277	      detectProjectsDeps: { atomStore: null },
   278	      probeDeps: {
   279	        spawn:
   280	          opts.probeSpawn ??
   281	          (async () => ({
   282	            exitCode: 0,
   283	            stdout: '{"pong":true,"ts":"2026-05-26T00:00:00.000Z"}\n',
   284	            stderr: '',
   285	            timedOut: false,
   286	          })),
   287	      },
   288	      wireDepsOverride: {
   289	        claudeCodeMcpRegistration: {
   290	          spawn: opts.registerSpawn,
   291	          ...(opts.registerTimeoutMs === undefined ? {} : { timeoutMs: opts.registerTimeoutMs }),
   292	        },
   293	        syncAll: async (profiles, syncOpts) =>
   294	          syncAll(
   295	            profiles.map((profile): AdapterSyncProfile => {
   296	              if (profile.kind === 'claude-code') {
   297	                return {
   298	                  ...profile,
   299	                  paths: {
   300	                    instructionsFile: opts.claudeInstructions,
   301	                    commandsDir: opts.claudeCommands,
   302	                  },
   303	                };
   304	              }
   305	              return profile;
   306	            }),
   307	            syncOpts,
   308	          ),
   309	      },
   310	    });
   311	}
   312	
   313	function successfulWizardFactory(home = echoHome): (wizardOpts: CreateWizardOpts) => Wizard {
   314	  return (() => {
   315	    let selected: AgentKind[] = [];
   316	    return {
   317	      async detectAgents() {
   318	        return [detected('codex', 'high')];
   319	      },
   320	      async detectProjects() {
   321	        return [];
   322	      },
   323	      async wire(opts) {
   324	        selected = opts.selectedAgents;
   325	        return successWire(selected, home);
   326	      },
   327	      async probe() {
   328	        return selected.map((agent) => ({ agent, probed: true as const, latencyMs: 1 }));
   329	      },
   330	      async summary() {
   331	        return {
   332	          detected: null,
   333	          projects: null,
   334	          wired: null,
   335	          probed: null,
   336	          onboardingStateSnapshot: null,
   337	        };
   338	      },
   339	      async markCompleted() {
   340	        const state = JSON.parse(readFileSync(join(home, 'state/onboarding.json'), 'utf8')) as {
   341	          completed: boolean;
   342	        };
   343	        state.completed = true;
   344	        writeFileSync(join(home, 'state/onboarding.json'), `${JSON.stringify(state, null, 2)}\n`);
   345	      },
   346	    } satisfies Wizard;
   347	  }) as never;
   348	}
   349	
   350	describe('runInit', () => {
   351	  beforeEach(() => {
   352	    originalEchoHome = process.env.ECHO_HOME;
   353	    originalPort = process.env.ECHO_MCP_PORT;
   354	    tmpRoot = mkdtempSync(join(tmpdir(), 'echo-init-'));
   355	    echoHome = join(tmpRoot, 'echo-home');
   356	    process.env.ECHO_HOME = echoHome;
   357	    process.env.ECHO_MCP_PORT = '39999';
   358	    vi.resetModules();
   359	  });
   360	
   361	  afterEach(() => {
   362	    if (originalEchoHome === undefined) delete process.env.ECHO_HOME;
   363	    else process.env.ECHO_HOME = originalEchoHome;
   364	    if (originalPort === undefined) delete process.env.ECHO_MCP_PORT;
   365	    else process.env.ECHO_MCP_PORT = originalPort;
   366	    rmSync(tmpRoot, { recursive: true, force: true });
   367	    vi.resetModules();
   368	  });
   369	
   370	  it('fails closed before wizard creation when stdin is not a TTY', async () => {
   371	    const { runInit } = await loadInit();
   372	    const wizardFactory = vi.fn();
   373	    const errors: string[] = [];
   374	
   375	    const code = await runInit({
   376	      stdin: { isTTY: false },
   377	      wizardFactory: wizardFactory as never,
   378	      stderr: {
   379	        write: (s) => {
   380	          errors.push(String(s));
   381	          return true;
   382	        },
   383	      },
   384	    });
   385	
   386	    expect(code).toBe(2);
   387	    expect(wizardFactory).not.toHaveBeenCalled();
   388	    expect(errors.join('')).toContain('non-interactive');
   389	    expect(errors.join('')).toContain('--answer-file');
   390	    expect(existsSync(join(echoHome, 'state/onboarding.json'))).toBe(false);
   391	    expect(existsSync(join(echoHome, 'state/projects.json'))).toBe(false);
   392	  });
   393	
   394	  it('documents and parses init isolation flags', async () => {
   395	    const { INIT_HELP, parseInitArgs } = await loadInit();
   396	
   397	    expect(
   398	      parseInitArgs([
   399	        '--home',
   400	        '/tmp/echo-home',
   401	        '--port',
   402	        '41234',
   403	        '--profile',
   404	        'dogfood',
   405	        '--label',
   406	        'com.echo.daemon.walkthrough',
   407	        '--answer-file',
   408	        '/tmp/answers.json',
   409	        '--force',
   410	      ]),
   411	    ).toEqual({
   412	      home: '/tmp/echo-home',
   413	      port: '41234',
   414	      profile: 'dogfood',
   415	      label: 'com.echo.daemon.walkthrough',
   416	      answerFile: '/tmp/answers.json',
   417	      force: true,
   418	    });
   419	    expect(INIT_HELP).toContain('--home <path>');
   420	    expect(INIT_HELP).toContain('--port <n>');
   421	    expect(INIT_HELP).toContain('--profile <name>');
   422	    expect(INIT_HELP).toContain('--label <id>');
   423	    expect(INIT_HELP).toContain('--answer-file <path>');
   424	    expect(INIT_HELP).toContain('--force');
   425	    expect(() => parseInitArgs(['--port', '0'])).toThrow('invalid --port: 0');
   426	    expect(() => parseInitArgs(['--port', '12abc'])).toThrow('invalid --port: 12abc');
   427	    expect(() => parseInitArgs(['--profile', 'orchestrator'])).toThrow(
   428	      'invalid --profile: expected customer or dogfood',
   429	    );
   430	  });
   431	
   432	  it('honors --home, --port, and --label in non-interactive answer-file mode', async () => {
   433	    const isolatedHome = join(tmpRoot, 'isolated-echo-home');
   434	    const clientHome = join(tmpRoot, 'client-home');
   435	    const codexConfig = join(clientHome, '.codex/config.toml');
   436	    const codexInstructions = join(clientHome, '.codex/AGENTS.md');
   437	    const cursorConfig = join(clientHome, '.cursor/mcp.json');
   438	    const answerFile = writeAnswerFile('answers.json', {
   439	      confirm_setup: true,
   440	      selected_agents: ['codex', 'cursor'],
   441	      default_project_repo_root: null,
   442	    });
   443	    const { runInit } = await loadInit();
   444	    const { createWizard } = await import('../../src/echo-home/wizard/run-wizard.js');
   445	    const { syncAll } = await import('../../src/echo-home/adapter-sync.js');
   446	
   447	    const wizardFactory = ((opts: CreateWizardOpts): Wizard =>
   448	      createWizard({
   449	        ...opts,
   450	        detectAgentsDeps: { homedir: clientHome, atomStore: null },
   451	        detectProjectsDeps: { atomStore: null },
   452	        probeDeps: {
   453	          spawn: async () => ({
   454	            exitCode: 0,
   455	            stdout: '{"pong":true,"ts":"2026-05-26T00:00:00.000Z"}\n',
   456	            stderr: '',
   457	            timedOut: false,
   458	          }),
   459	        },
   460	        wireDepsOverride: {
   461	          syncAll: async (profiles, syncOpts) =>
   462	            syncAll(
   463	              profiles.map((profile): AdapterSyncProfile => {
   464	                if (profile.kind === 'codex') {
   465	                  return {
   466	                    ...profile,
   467	                    paths: { configFile: codexConfig, instructionsFile: codexInstructions },
   468	                  };
   469	                }
   470	                if (profile.kind === 'cursor') {
   471	                  return { ...profile, paths: { configFile: cursorConfig } };
   472	                }
   473	                return profile;
   474	              }),
   475	              syncOpts,
   476	            ),
   477	        },
   478	      })) as never;
   479	
   480	    const code = await runInit({
   481	      stdin: { isTTY: false },
   482	      wizardFactory,
   483	      answerFile,
   484	      home: isolatedHome,
   485	      port: '41234',
   486	      label: 'com.echo.daemon.walkthrough',
   487	      quiet: true,
   488	      now: () => new Date('2026-05-26T00:00:00.000Z'),
   489	      ...daemonAlreadyRunning(),
   490	    });
   491	
   492	    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
   493	      completed: boolean;
   494	      profile: string;
   495	      agents: Array<{ id: AgentKind }>;
   496	    };
   497	    const cursor = JSON.parse(readFileSync(cursorConfig, 'utf8')) as {
   498	      mcpServers: { echo: { url: string } };
   499	    };
   500	    const codexCache = JSON.parse(
   501	      readFileSync(join(isolatedHome, 'adapters/codex.json'), 'utf8'),
   502	    ) as {
   503	      mcpServerConfig: { url: string };
   504	    };
   505	
   506	    expect(code).toBe(0);
   507	    expect(state.completed).toBe(true);
   508	    expect(state.profile).toBe('customer');
   509	    expect(state.agents.map((agent) => agent.id).sort()).toEqual(['codex', 'cursor']);
   510	    expect(readFileSync(codexConfig, 'utf8')).toContain('url = "http://127.0.0.1:41234/mcp"');
   511	    expect(cursor.mcpServers.echo.url).toBe('http://127.0.0.1:41234/mcp');
   512	    expect(codexCache.mcpServerConfig.url).toBe('http://127.0.0.1:41234/mcp');
   513	    expect(existsSync(join(isolatedHome, 'skills', 'using-echo-coord.md'))).toBe(false);
   514	    expect(existsSync(join(isolatedHome, 'skills', 'using-echo-mcp.md'))).toBe(true);
   515	    expect(existsSync(join(isolatedHome, 'skills', 'merge-and-cleanup.md'))).toBe(false);
   516	    expect(existsSync(join(isolatedHome, 'skills', 'process-backlog.md'))).toBe(false);
   517	    expect(existsSync(join(isolatedHome, 'roles', 'builder.toml'))).toBe(false);
   518	    expect(existsSync(join(isolatedHome, 'workflows', 'change-review.toml'))).toBe(false);
   519	    expect(existsSync(join(echoHome, 'state/onboarding.json'))).toBe(false);
   520	  });
   521	
   522	  it('registers claude-code MCP when selected in answer-file mode', async () => {
   523	    const isolatedHome = join(tmpRoot, 'claude-register-home');
   524	    const clientHome = join(tmpRoot, 'claude-register-client');
   525	    const claudeInstructions = join(clientHome, '.claude/CLAUDE.md');
   526	    const claudeCommands = join(clientHome, '.claude/commands');
   527	    mkdirSync(join(clientHome, '.claude'), { recursive: true });
   528	    writeFileSync(claudeInstructions, '# Claude user file\n');
   529	    const answerFile = writeAnswerFile('claude-register-answers.json', {
   530	      confirm_setup: true,
   531	      selected_agents: ['claude-code'],
   532	      profile: 'dogfood',
   533	      default_project_repo_root: null,
   534	    });
   535	    const calls: Array<{ cmd: string; args: string[]; timeoutMs: number }> = [];
   536	    const stdout: string[] = [];
   537	    const { runInit } = await loadInit();
   538	    const wizardFactory = await makeClaudeSyncWizardFactory({
   539	      clientHome,
   540	      claudeInstructions,
   541	      claudeCommands,
   542	      registerSpawn: async (cmd, args, opts) => {
   543	        calls.push({ cmd, args, timeoutMs: opts.timeoutMs });
   544	        return { exitCode: 0, stdout: '', stderr: '', timedOut: false };
   545	      },
   546	    });
   547	
   548	    const code = await runInit({
   549	      stdin: { isTTY: false },
   550	      wizardFactory,
   551	      answerFile,
   552	      home: isolatedHome,
   553	      port: '41234',
   554	      label: 'com.echo.daemon.walkthrough',
   555	      now: () => new Date('2026-05-26T00:00:00.000Z'),
   556	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   557	      ...daemonAlreadyRunning(),
   558	    });
   559	
   560	    expect(code).toBe(0);
   561	    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
   562	      profile: string;
   563	    };
   564	    expect(state.profile).toBe('dogfood');
   565	    expect(calls).toEqual([
   566	      {
   567	        cmd: 'claude',
   568	        args: [
   569	          'mcp',
   570	          'add',
   571	          '--transport',
   572	          'http',
   573	          '--scope',
   574	          'user',
   575	          'echo',
   576	          'http://127.0.0.1:41234/mcp',
   577	        ],
   578	        timeoutMs: 30_000,
   579	      },
   580	    ]);
   581	    expect(stdout.join('')).toContain('mcp-add');
   582	  });
   583	
   584	  it('CLI profile overrides answer-file and recorded profile', async () => {
   585	    const isolatedHome = join(tmpRoot, 'profile-precedence-home');
   586	    mkdirSync(join(isolatedHome, 'state'), { recursive: true });
   587	    writeFileSync(
   588	      join(isolatedHome, 'state/onboarding.json'),
   589	      `${JSON.stringify(
   590	        {
   591	          schema_version: 1,
   592	          created_at: '2026-05-26T00:00:00.000Z',
   593	          last_updated_at: '2026-05-26T00:00:00.000Z',
   594	          completed: true,
   595	          profile: 'dogfood',
   596	          agents: [],
   597	        },
   598	        null,
   599	        2,
   600	      )}\n`,
   601	    );
   602	    const answerFile = writeAnswerFile('profile-precedence-answers.json', {
   603	      confirm_setup: true,
   604	      selected_agents: ['codex'],
   605	      profile: 'dogfood',
   606	      default_project_repo_root: null,
   607	    });
   608	    const seenProfiles: unknown[] = [];
   609	    const wizardFactory = (() =>
   610	      ({
   611	        async detectAgents() {
   612	          return [detected('codex', 'high')];
   613	        },
   614	        async detectProjects() {
   615	          return [];
   616	        },
   617	        async wire(opts) {
   618	          seenProfiles.push(opts.profile);
   619	          return successWire(opts.selectedAgents, isolatedHome);
   620	        },
   621	        async probe() {
   622	          return [{ agent: 'codex', probed: true as const, latencyMs: 1 }];
   623	        },
   624	        async summary() {
   625	          return {
   626	            detected: null,
   627	            projects: null,
   628	            wired: null,
   629	            probed: null,
   630	            onboardingStateSnapshot: null,
   631	          };
   632	        },
   633	        async markCompleted() {},
   634	      }) satisfies Wizard) as never;
   635	    const { runInit } = await loadInit();
   636	
   637	    const code = await runInit({
   638	      stdin: { isTTY: false },
   639	      answerFile,
   640	      profile: 'customer',
   641	      wizardFactory,
   642	      home: isolatedHome,
   643	      quiet: true,
   644	      ...daemonAlreadyRunning(),
   645	    });
   646	
   647	    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
   648	      profile: string;
   649	    };
   650	    expect(code).toBe(0);
   651	    expect(seenProfiles).toEqual(['customer']);
   652	    expect(state.profile).toBe('customer');
   653	  });
   654	
   655	  it('recorded dogfood profile is respected on no-flag rerun', async () => {
   656	    const isolatedHome = join(tmpRoot, 'recorded-dogfood-home');
   657	    mkdirSync(join(isolatedHome, 'state'), { recursive: true });
   658	    writeFileSync(
   659	      join(isolatedHome, 'state/onboarding.json'),
   660	      `${JSON.stringify(
   661	        {
   662	          schema_version: 1,
   663	          created_at: '2026-05-26T00:00:00.000Z',
   664	          last_updated_at: '2026-05-26T00:00:00.000Z',
   665	          completed: true,
   666	          profile: 'dogfood',
   667	          agents: [],
   668	        },
   669	        null,
   670	        2,
   671	      )}\n`,
   672	    );
   673	    const answerFile = writeAnswerFile('recorded-dogfood-answers.json', {
   674	      confirm_setup: true,
   675	      selected_agents: ['codex'],
   676	      default_project_repo_root: null,
   677	    });
   678	    const seenProfiles: unknown[] = [];
   679	    const wizardFactory = (() =>
   680	      ({
   681	        async detectAgents() {
   682	          return [detected('codex', 'high')];
   683	        },
   684	        async detectProjects() {
   685	          return [];
   686	        },
   687	        async wire(opts) {
   688	          seenProfiles.push(opts.profile);
   689	          return successWire(opts.selectedAgents, isolatedHome);
   690	        },
   691	        async probe() {
   692	          return [{ agent: 'codex', probed: true as const, latencyMs: 1 }];
   693	        },
   694	        async summary() {
   695	          return {
   696	            detected: null,
   697	            projects: null,
   698	            wired: null,
   699	            probed: null,
   700	            onboardingStateSnapshot: null,
   701	          };
   702	        },
   703	        async markCompleted() {},
   704	      }) satisfies Wizard) as never;
   705	    const { runInit } = await loadInit();
   706	
   707	    const code = await runInit({
   708	      stdin: { isTTY: false },
   709	      answerFile,
   710	      wizardFactory,
   711	      home: isolatedHome,
   712	      quiet: true,
   713	      ...daemonAlreadyRunning(),
   714	    });
   715	
   716	    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
   717	      profile: string;
   718	    };
   719	    expect(code).toBe(0);
   720	    expect(seenProfiles).toEqual(['dogfood']);
   721	    expect(state.profile).toBe('dogfood');
   722	  });
   723	
   724	  it('surfaces duplicate claude-code MCP registration without masking probe failure', async () => {
   725	    const isolatedHome = join(tmpRoot, 'claude-duplicate-home');
   726	    const clientHome = join(tmpRoot, 'claude-duplicate-client');
   727	    const claudeInstructions = join(clientHome, '.claude/CLAUDE.md');
   728	    const claudeCommands = join(clientHome, '.claude/commands');
   729	    mkdirSync(join(clientHome, '.claude'), { recursive: true });
   730	    writeFileSync(claudeInstructions, '# Claude user file\n');
   731	    const answerFile = writeAnswerFile('claude-duplicate-answers.json', {
   732	      confirm_setup: true,
   733	      selected_agents: ['claude-code'],
   734	      default_project_repo_root: null,
   735	    });
   736	    const stdout: string[] = [];
   737	    const { runInit } = await loadInit();
   738	    const wizardFactory = await makeClaudeSyncWizardFactory({
   739	      clientHome,
   740	      claudeInstructions,
   741	      claudeCommands,
   742	      registerSpawn: async () => ({
   743	        exitCode: 1,
   744	        stdout: '',
   745	        stderr: 'MCP server echo already exists in user config\n',
   746	        timedOut: false,
   747	      }),
   748	      probeSpawn: async () => ({
   749	        exitCode: 1,
   750	        stdout: '',
   751	        stderr: 'No such tool mcp__echo__echo_ping\n',
   752	        timedOut: false,
   753	      }),
   754	    });
   755	
   756	    const code = await runInit({
   757	      stdin: { isTTY: false },
   758	      wizardFactory,
   759	      answerFile,
   760	      home: isolatedHome,
   761	      port: '41234',
   762	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   763	      ...daemonAlreadyRunning(),
   764	    });
   765	
   766	    const text = stdout.join('');
   767	    expect(code).toBe(0);
   768	    expect(text).toContain('already-exists (unverified)');
   769	    expect(text).toContain('claude-code: WARN mcp-not-configured');
   770	    expect(text).toContain(
   771	      'claude mcp add --transport http --scope user echo http://127.0.0.1:41234/mcp',
   772	    );
   773	    expect(text).toContain('claude mcp remove echo -s local');
   774	  });
   775	
   776	  it('continues init when claude-code MCP registration times out', async () => {
   777	    const isolatedHome = join(tmpRoot, 'claude-timeout-home');
   778	    const clientHome = join(tmpRoot, 'claude-timeout-client');
   779	    const claudeInstructions = join(clientHome, '.claude/CLAUDE.md');
   780	    const claudeCommands = join(clientHome, '.claude/commands');
   781	    mkdirSync(join(clientHome, '.claude'), { recursive: true });
   782	    writeFileSync(claudeInstructions, '# Claude user file\n');
   783	    const answerFile = writeAnswerFile('claude-timeout-answers.json', {
   784	      confirm_setup: true,
   785	      selected_agents: ['claude-code'],
   786	      default_project_repo_root: null,
   787	    });
   788	    const stdout: string[] = [];
   789	    const { runInit } = await loadInit();
   790	    const wizardFactory = await makeClaudeSyncWizardFactory({
   791	      clientHome,
   792	      claudeInstructions,
   793	      claudeCommands,
   794	      registerTimeoutMs: 5,
   795	      registerSpawn: async () => ({
   796	        exitCode: -1,
   797	        stdout: '',
   798	        stderr: '',
   799	        timedOut: true,
   800	      }),
   801	    });
   802	
   803	    const code = await runInit({
   804	      stdin: { isTTY: false },
   805	      wizardFactory,
   806	      answerFile,
   807	      home: isolatedHome,
   808	      port: '41234',
   809	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   810	      ...daemonAlreadyRunning(),
   811	    });
   812	
   813	    expect(code).toBe(0);
   814	    expect(stdout.join('')).toContain('timeout');
   815	  });
   816	
   817	  it('continues init when claude is missing and prints remediation through probe', async () => {
   818	    const isolatedHome = join(tmpRoot, 'claude-missing-home');
   819	    const clientHome = join(tmpRoot, 'claude-missing-client');
   820	    const claudeInstructions = join(clientHome, '.claude/CLAUDE.md');
   821	    const claudeCommands = join(clientHome, '.claude/commands');
   822	    mkdirSync(join(clientHome, '.claude'), { recursive: true });
   823	    writeFileSync(claudeInstructions, '# Claude user file\n');
   824	    const answerFile = writeAnswerFile('claude-missing-answers.json', {
   825	      confirm_setup: true,
   826	      selected_agents: ['claude-code'],
   827	      default_project_repo_root: null,
   828	    });
   829	    const enoent = Object.assign(new Error('spawn claude ENOENT'), { code: 'ENOENT' });
   830	    const stdout: string[] = [];
   831	    const { runInit } = await loadInit();
   832	    const wizardFactory = await makeClaudeSyncWizardFactory({
   833	      clientHome,
   834	      claudeInstructions,
   835	      claudeCommands,
   836	      registerSpawn: async () => {
   837	        throw enoent;
   838	      },
   839	      probeSpawn: async () => {
   840	        throw enoent;
   841	      },
   842	    });
   843	
   844	    const code = await runInit({
   845	      stdin: { isTTY: false },
   846	      wizardFactory,
   847	      answerFile,
   848	      home: isolatedHome,
   849	      port: '41234',
   850	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   851	      ...daemonAlreadyRunning(),
   852	    });
   853	
   854	    const text = stdout.join('');
   855	    expect(code).toBe(0);
   856	    expect(text).toContain('cli-unavailable');
   857	    expect(text).toContain('claude-code not found on PATH');
   858	  });
   859	
   860	  it('installs and starts the daemon when init finds no installed launchd job', async () => {
   861	    const isolatedHome = join(tmpRoot, 'bringup-install-home');
   862	    const answerFile = writeAnswerFile('daemon-install-answers.json', {
   863	      confirm_setup: true,
   864	      selected_agents: ['codex'],
   865	      default_project_repo_root: null,
   866	    });
   867	    const daemon = daemonFixture({ launchdRegistered: false });
   868	    const stdout: string[] = [];
   869	    const stderr: string[] = [];
   870	    const { runInit } = await loadInit();
   871	
   872	    const code = await runInit({
   873	      stdin: { isTTY: false },
   874	      answerFile,
   875	      wizardFactory: successfulWizardFactory(isolatedHome) as never,
   876	      home: isolatedHome,
   877	      port: '41234',
   878	      label: 'com.echo.daemon.test-init',
   879	      daemonOptions: daemon.daemonOptions,
   880	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   881	      stderr: { write: (s) => (stderr.push(String(s)), true) },
   882	    });
   883	
   884	    expect(code, stderr.join('')).toBe(0);
   885	    expect(stderr.join('')).toBe('');
   886	    const calls = daemon.calls.map((call) => `${call.command} ${call.args.join(' ')}`);
   887	    const plist = readFileSync(daemon.plistPath, 'utf8');
   888	    expect(stdout.join('')).toContain('Daemon installed and started on port 41234.');
   889	    expect(calls).toContain(`launchctl bootstrap gui/501 ${daemon.plistPath}`);
   890	    expect(calls.some((call) => call.startsWith('plutil -lint'))).toBe(true);
   891	    expect(plist).toContain('<string>com.echo.daemon.test-init</string>');
   892	    expect(plist).toContain(`<string>${isolatedHome}</string>`);
   893	    expect(plist).toContain('<string>41234</string>');
   894	  });
   895	
   896	  it('starts the daemon when the plist exists but launchd is not running it', async () => {
   897	    const answerFile = writeAnswerFile('daemon-start-answers.json', {
   898	      confirm_setup: true,
   899	      selected_agents: ['codex'],
   900	      default_project_repo_root: null,
   901	    });
   902	    const daemon = daemonFixture({ launchdRegistered: false, writePlist: true });
   903	    const stdout: string[] = [];
   904	    const { runInit } = await loadInit();
   905	
   906	    const code = await runInit({
   907	      stdin: { isTTY: false },
   908	      answerFile,
   909	      wizardFactory: successfulWizardFactory() as never,
   910	      daemonOptions: daemon.daemonOptions,
   911	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   912	    });
   913	
   914	    const calls = daemon.calls.map((call) => `${call.command} ${call.args.join(' ')}`);
   915	    expect(code).toBe(0);
   916	    expect(stdout.join('')).toContain('Daemon started on port 39999.');
   917	    expect(calls).toContain(`launchctl bootstrap gui/501 ${daemon.plistPath}`);
   918	    expect(calls.some((call) => call.startsWith('plutil -lint'))).toBe(false);
   919	  });
   920	
   921	  it('does nothing when the daemon is already running', async () => {
   922	    const answerFile = writeAnswerFile('daemon-running-answers.json', {
   923	      confirm_setup: true,
   924	      selected_agents: ['codex'],
   925	      default_project_repo_root: null,
   926	    });
   927	    const daemon = daemonFixture({ launchdRegistered: true });
   928	    const stdout: string[] = [];
   929	    const { runInit } = await loadInit();
   930	
   931	    const code = await runInit({
   932	      stdin: { isTTY: false },
   933	      answerFile,
   934	      wizardFactory: successfulWizardFactory() as never,
   935	      daemonOptions: daemon.daemonOptions,
   936	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   937	    });
   938	
   939	    expect(code).toBe(0);
   940	    expect(stdout.join('')).toContain('Daemon already running on port 39999.');
   941	    expect(daemon.calls.some((call) => call.args[0] === 'bootstrap')).toBe(false);
   942	    expect(daemon.calls.some((call) => call.command === 'plutil')).toBe(false);
   943	  });
   944	
   945	  it('fails init with remediation when daemon install fails', async () => {
   946	    const answerFile = writeAnswerFile('daemon-install-fails-answers.json', {
   947	      confirm_setup: true,
   948	      selected_agents: ['codex'],
   949	      default_project_repo_root: null,
   950	    });
   951	    const daemon = daemonFixture({ launchdRegistered: false, nodeOk: false });
   952	    const stdout: string[] = [];
   953	    const stderr: string[] = [];
   954	    const { runInit } = await loadInit();
   955	
   956	    const code = await runInit({
   957	      stdin: { isTTY: false },
   958	      answerFile,
   959	      wizardFactory: successfulWizardFactory() as never,
   960	      daemonOptions: daemon.daemonOptions,
   961	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   962	      stderr: { write: (s) => (stderr.push(String(s)), true) },
   963	    });
   964	
   965	    const state = JSON.parse(readFileSync(join(echoHome, 'state/onboarding.json'), 'utf8')) as {
   966	      completed: boolean;
   967	    };
   968	    expect(code).toBe(1);
   969	    expect(stderr.join('')).toContain('daemon install failed:');
   970	    expect(stderr.join('')).toContain('node version check failed');
   971	    expect(stderr.join('')).toContain('Run `echoctl daemon install` manually');
   972	    expect(stdout.join('')).not.toContain("You're ready.");
   973	    expect(state.completed).toBe(false);
   974	  });
   975	
   976	  it('force-replaces a prior ECHO marker block and records a successful wire', async () => {
   977	    const isolatedHome = join(tmpRoot, 'force-echo-home');
   978	    const clientHome = join(tmpRoot, 'force-client-home');
   979	    const codexConfig = join(clientHome, '.codex/config.toml');
   980	    const codexInstructions = join(clientHome, '.codex/AGENTS.md');
   981	    mkdirSync(join(clientHome, '.codex'), { recursive: true });
   982	    writeFileSync(
   983	      codexInstructions,
   984	      `${BEGIN_MARKER}\nuser hand-edited inside marker\n${END_MARKER}\n`,
   985	    );
   986	    writeFileSync(codexConfig, `[mcp_servers.echo]\nurl = "http://user-edited:9999/mcp"\n`);
   987	    const answerFile = writeAnswerFile('force-answers.json', {
   988	      confirm_setup: true,
   989	      selected_agents: ['codex'],
   990	      default_project_repo_root: null,
   991	    });
   992	    const { runInit } = await loadInit();
   993	    const wizardFactory = await makeCodexSyncWizardFactory({
   994	      clientHome,
   995	      codexConfig,
   996	      codexInstructions,
   997	    });
   998	
   999	    const code = await runInit({
  1000	      stdin: { isTTY: false },
  1001	      wizardFactory,
  1002	      answerFile,
  1003	      home: isolatedHome,
  1004	      port: '41236',
  1005	      force: true,
  1006	      quiet: true,
  1007	      now: () => new Date('2026-05-26T00:00:00.000Z'),
  1008	      ...daemonAlreadyRunning(),
  1009	    });
  1010	
  1011	    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
  1012	      agents: Array<{ id: AgentKind; wired_at: string | null; wire_error: string | null }>;
  1013	    };
  1014	    const codex = state.agents.find((agent) => agent.id === 'codex');
  1015	    expect(code).toBe(0);
  1016	    expect(codex?.wired_at).toBe('2026-05-26T00:00:00.000Z');
  1017	    expect(codex?.wire_error).toBeNull();
  1018	    expect(readFileSync(codexInstructions, 'utf8')).toContain('http://127.0.0.1:41236/mcp');
  1019	    expect(readFileSync(codexInstructions, 'utf8')).not.toContain('user hand-edited');
  1020	  });
  1021	
  1022	  it('force preserves outside-marker content byte-for-byte', async () => {
  1023	    const isolatedHome = join(tmpRoot, 'preserve-echo-home');
  1024	    const clientHome = join(tmpRoot, 'preserve-client-home');
  1025	    const codexConfig = join(clientHome, '.codex/config.toml');
  1026	    const codexInstructions = join(clientHome, '.codex/AGENTS.md');
  1027	    const above = '# User heading\ncustom before\n\n';
  1028	    const below = '\n## User footer\ncustom after\n';
  1029	    mkdirSync(join(clientHome, '.codex'), { recursive: true });
  1030	    writeFileSync(
  1031	      codexInstructions,
  1032	      `${above}${BEGIN_MARKER}\nold echo block with local edits\n${END_MARKER}${below}`,
  1033	    );
  1034	    writeFileSync(codexConfig, `[mcp_servers.echo]\nurl = "http://old:1111/mcp"\n`);
  1035	    const answerFile = writeAnswerFile('preserve-answers.json', {
  1036	      confirm_setup: true,
  1037	      selected_agents: ['codex'],
  1038	      default_project_repo_root: null,
  1039	    });
  1040	    const { runInit } = await loadInit();
  1041	    const wizardFactory = await makeCodexSyncWizardFactory({
  1042	      clientHome,
  1043	      codexConfig,
  1044	      codexInstructions,
  1045	    });
  1046	
  1047	    await runInit({
  1048	      stdin: { isTTY: false },
  1049	      wizardFactory,
  1050	      answerFile,
  1051	      home: isolatedHome,
  1052	      port: '41237',
  1053	      force: true,
  1054	      quiet: true,
  1055	      now: () => new Date('2026-05-26T00:00:00.000Z'),
  1056	      ...daemonAlreadyRunning(),
  1057	    });
  1058	
  1059	    const content = readFileSync(codexInstructions, 'utf8');
  1060	    const beginIdx = content.indexOf(BEGIN_MARKER);
  1061	    const endAfterIdx = content.indexOf(END_MARKER) + END_MARKER.length;
  1062	    expect(content.slice(0, beginIdx)).toBe(above);
  1063	    expect(content.slice(endAfterIdx)).toBe(below);
  1064	    expect(content).toContain('http://127.0.0.1:41237/mcp');
  1065	  });
  1066	
  1067	  it('force refuses malformed marker blocks and leaves the file untouched', async () => {
  1068	    const isolatedHome = join(tmpRoot, 'malformed-echo-home');
  1069	    const clientHome = join(tmpRoot, 'malformed-client-home');
  1070	    const codexConfig = join(clientHome, '.codex/config.toml');
  1071	    const codexInstructions = join(clientHome, '.codex/AGENTS.md');
  1072	    mkdirSync(join(clientHome, '.codex'), { recursive: true });
  1073	    writeFileSync(codexInstructions, `# Header\n${BEGIN_MARKER}\nbroken inside\n`);
  1074	    writeFileSync(codexConfig, `[mcp_servers.echo]\nurl = "http://old:1111/mcp"\n`);
  1075	    const before = readFileSync(codexInstructions);
  1076	    const answerFile = writeAnswerFile('malformed-answers.json', {
  1077	      confirm_setup: true,
  1078	      selected_agents: ['codex'],
  1079	      default_project_repo_root: null,
  1080	    });
  1081	    const { runInit } = await loadInit();
  1082	    const wizardFactory = await makeCodexSyncWizardFactory({
  1083	      clientHome,
  1084	      codexConfig,
  1085	      codexInstructions,
  1086	    });
  1087	
  1088	    await runInit({
  1089	      stdin: { isTTY: false },
  1090	      wizardFactory,
  1091	      answerFile,
  1092	      home: isolatedHome,
  1093	      port: '41238',
  1094	      force: true,
  1095	      quiet: true,
  1096	      now: () => new Date('2026-05-26T00:00:00.000Z'),
  1097	      ...daemonAlreadyRunning(),
  1098	    });
  1099	
  1100	    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
  1101	      agents: Array<{ id: AgentKind; wire_error: string | null }>;
  1102	    };
  1103	    expect(state.agents.find((agent) => agent.id === 'codex')?.wire_error).toContain(
  1104	      'marker block malformed; manual intervention required',
  1105	    );
  1106	    expect(readFileSync(codexInstructions).equals(before)).toBe(true);
  1107	  });
  1108	
  1109	  it('fails loudly when the answer file is missing', async () => {
  1110	    const { runInit } = await loadInit();
  1111	    const missingPath = join(tmpRoot, 'missing.json');
  1112	    const wizardFactory = vi.fn();
  1113	    const errors: string[] = [];
  1114	
  1115	    const code = await runInit({
  1116	      stdin: { isTTY: false },
  1117	      answerFile: missingPath,
  1118	      wizardFactory: wizardFactory as never,
  1119	      stderr: { write: (s) => (errors.push(String(s)), true) },
  1120	    });
  1121	
  1122	    expect(code).toBe(2);
  1123	    expect(wizardFactory).not.toHaveBeenCalled();
  1124	    expect(errors.join('')).toContain(missingPath);
  1125	    expect(errors.join('')).toContain('file: not found');
  1126	  });
  1127	
  1128	  it('fails loudly when the answer file has malformed JSON', async () => {
  1129	    const { runInit } = await loadInit();
  1130	    const answerFile = join(tmpRoot, 'bad-json.json');
  1131	    writeFileSync(answerFile, '{');
  1132	    const wizardFactory = vi.fn();
  1133	    const errors: string[] = [];
  1134	
  1135	    const code = await runInit({
  1136	      stdin: { isTTY: false },
  1137	      answerFile,
  1138	      wizardFactory: wizardFactory as never,
  1139	      stderr: { write: (s) => (errors.push(String(s)), true) },
  1140	    });
  1141	
  1142	    expect(code).toBe(2);
  1143	    expect(wizardFactory).not.toHaveBeenCalled();
  1144	    expect(errors.join('')).toContain(answerFile);
  1145	    expect(errors.join('')).toContain('root: invalid JSON');
  1146	  });
  1147	
  1148	  it('fails loudly when the answer file is missing a required field', async () => {
  1149	    const { runInit } = await loadInit();
  1150	    const answerFile = writeAnswerFile('missing-field.json', {
  1151	      confirm_setup: true,
  1152	      selected_agents: ['codex'],
  1153	    });
  1154	    const wizardFactory = vi.fn();
  1155	    const errors: string[] = [];
  1156	
  1157	    const code = await runInit({
  1158	      stdin: { isTTY: false },
  1159	      answerFile,
  1160	      wizardFactory: wizardFactory as never,
  1161	      stderr: { write: (s) => (errors.push(String(s)), true) },
  1162	    });
  1163	
  1164	    expect(code).toBe(2);
  1165	    expect(wizardFactory).not.toHaveBeenCalled();
  1166	    expect(errors.join('')).toContain(answerFile);
  1167	    expect(errors.join('')).toContain('default_project_repo_root: missing required field');
  1168	  });
  1169	
  1170	  it('fails loudly when the answer file has an unknown field', async () => {
  1171	    const { runInit } = await loadInit();
  1172	    const answerFile = writeAnswerFile('unknown-field.json', {
  1173	      confirm_setup: true,
  1174	      selected_agents: ['codex'],
  1175	      default_project_repo_root: null,
  1176	      extra: true,
  1177	    });
  1178	    const wizardFactory = vi.fn();
  1179	    const errors: string[] = [];
  1180	
  1181	    const code = await runInit({
  1182	      stdin: { isTTY: false },
  1183	      answerFile,
  1184	      wizardFactory: wizardFactory as never,
  1185	      stderr: { write: (s) => (errors.push(String(s)), true) },
  1186	    });
  1187	
  1188	    expect(code).toBe(2);
  1189	    expect(wizardFactory).not.toHaveBeenCalled();
  1190	    expect(errors.join('')).toContain(answerFile);
  1191	    expect(errors.join('')).toContain('extra: unknown field');
  1192	  });
  1193	
  1194	  it('fails loudly when the answer file has an invalid profile', async () => {
  1195	    const { runInit } = await loadInit();
  1196	    const answerFile = writeAnswerFile('invalid-profile.json', {
  1197	      confirm_setup: true,
  1198	      selected_agents: ['codex'],
  1199	      default_project_repo_root: null,
  1200	      profile: 'orchestrator',
  1201	    });
  1202	    const wizardFactory = vi.fn();
  1203	    const errors: string[] = [];
  1204	
  1205	    const code = await runInit({
  1206	      stdin: { isTTY: false },
  1207	      answerFile,
  1208	      wizardFactory: wizardFactory as never,
  1209	      stderr: { write: (s) => (errors.push(String(s)), true) },
  1210	    });
  1211	
  1212	    expect(code).toBe(2);
  1213	    expect(wizardFactory).not.toHaveBeenCalled();
  1214	    expect(errors.join('')).toContain(answerFile);
  1215	    expect(errors.join('')).toContain('profile: expected customer or dogfood');
  1216	  });
  1217	
  1218	  it('fails loudly when repo_root is required but absent in answer-file mode', async () => {
  1219	    const { runInit } = await loadInit();
  1220	    const answerFile = writeAnswerFile('missing-repo-root.json', {
  1221	      confirm_setup: true,
  1222	      selected_agents: ['codex'],
  1223	      default_project_repo_root: null,
  1224	    });
  1225	    const errors: string[] = [];
  1226	    const wizardFactory = (() =>
  1227	      ({
  1228	        async detectAgents() {
  1229	          return [detected('codex', 'high')];
  1230	        },
  1231	        async detectProjects() {
  1232	          return [];
  1233	        },
  1234	        async wire() {
  1235	          return {
  1236	            syncResult: {
  1237	              skillsPopulated: { ok: false, sourceDir: '', targetDir: '', error: 'missing' },
  1238	              agents: [],
  1239	              roles: { results: [], rolesErrors: [] },
  1240	              repoRoot: {
  1241	                code: 'UNKNOWN',
  1242	                file: '/missing',
  1243	                operation: 'stat',
  1244	                message: 'could not locate repo root',
  1245	              },
  1246	              overallOk: false,
  1247	            },
  1248	            cacheUpdates: [],
  1249	            onboardingStateUpdated: false,
  1250	          } satisfies WireResult;
  1251	        },
  1252	        async probe() {
  1253	          return [];
  1254	        },
  1255	        async summary() {
  1256	          return {
  1257	            detected: null,
  1258	            projects: null,
  1259	            wired: null,
  1260	            probed: null,
  1261	            onboardingStateSnapshot: null,
  1262	          };
  1263	        },
  1264	        async markCompleted() {},
  1265	      }) satisfies Wizard) as never;
  1266	
  1267	    const code = await runInit({
  1268	      stdin: { isTTY: false },
  1269	      answerFile,
  1270	      wizardFactory,
  1271	      stderr: { write: (s) => (errors.push(String(s)), true) },
  1272	      quiet: true,
  1273	    });
  1274	
  1275	    expect(code).toBe(2);
  1276	    expect(errors.join('')).toContain(answerFile);
  1277	    expect(errors.join('')).toContain('repo_root: required');
  1278	  });
  1279	
  1280	  it.each([
  1281	    ['bare scaffold', { completed: false, agents: [] }],
  1282	    [
  1283	      'completed with agents',
  1284	      {
  1285	        completed: true,
  1286	        agents: [
  1287	          {
  1288	            id: 'codex',
  1289	            detected_at: '2026-05-26T00:00:00.000Z',
  1290	            wired_at: '2026-05-26T00:00:00.000Z',
  1291	            probed_at: null,
  1292	            capabilities: [],
  1293	            wire_error: null,
  1294	          },
  1295	        ],
  1296	      },
  1297	    ],
  1298	    [
  1299	      'mid-wire with agents',
  1300	      {
  1301	        completed: false,
  1302	        agents: [
  1303	          {
  1304	            id: 'claude-code',
  1305	            detected_at: '2026-05-26T00:00:00.000Z',
  1306	            wired_at: '2026-05-26T00:00:00.000Z',
  1307	            probed_at: null,
  1308	            capabilities: [],
  1309	            wire_error: null,
  1310	          },
  1311	        ],
  1312	      },
  1313	    ],
  1314	  ])(
  1315	    'defaults profile-less onboarding state to customer with warning: %s',
  1316	    async (_label, partial) => {
  1317	      const isolatedHome = join(tmpRoot, `profileless-${String(_label).replace(/\s+/g, '-')}`);
  1318	      mkdirSync(join(isolatedHome, 'state'), { recursive: true });
  1319	      writeFileSync(
  1320	        join(isolatedHome, 'state/onboarding.json'),
  1321	        `${JSON.stringify(
  1322	          {
  1323	            schema_version: 1,
  1324	            created_at: '2026-05-26T00:00:00.000Z',
  1325	            last_updated_at: '2026-05-26T00:00:00.000Z',
  1326	            ...partial,
  1327	          },
  1328	          null,
  1329	          2,
  1330	        )}\n`,
  1331	      );
  1332	      const answerFile = writeAnswerFile(`${String(_label).replace(/\s+/g, '-')}.json`, {
  1333	        confirm_setup: true,
  1334	        selected_agents: ['codex'],
  1335	        default_project_repo_root: null,
  1336	      });
  1337	      const errors: string[] = [];
  1338	      const { runInit } = await loadInit();
  1339	
  1340	      const code = await runInit({
  1341	        stdin: { isTTY: false },
  1342	        answerFile,
  1343	        wizardFactory: successfulWizardFactory(isolatedHome) as never,
  1344	        home: isolatedHome,
  1345	        stderr: { write: (s) => (errors.push(String(s)), true) },
  1346	        quiet: true,
  1347	        ...daemonAlreadyRunning(),
  1348	      });
  1349	
  1350	      const state = JSON.parse(
  1351	        readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8'),
  1352	      ) as {
  1353	        profile: string;
  1354	      };
  1355	      expect(code).toBe(0);
  1356	      expect(state.profile).toBe('customer');
  1357	      expect(errors.join('')).toContain('defaulted to `customer`');
  1358	      expect(errors.join('')).toContain('echoctl init --profile dogfood');
  1359	    },
  1360	  );
  1361	
  1362	  it('bootstraps echo home before the interactive wizard touches state', async () => {
  1363	    const created: boolean[] = [];
  1364	    const wizardFactory = (() => {
  1365	      created.push(
  1366	        existsSync(join(echoHome, 'state/onboarding.json')) &&
  1367	          existsSync(join(echoHome, 'state/projects.json')),
  1368	      );
  1369	      let selected: AgentKind[] = [];
  1370	      return {
  1371	        async detectAgents() {
  1372	          return [detected('codex', 'high')];
  1373	        },
  1374	        async detectProjects() {
  1375	          return [];
  1376	        },
  1377	        async wire(opts) {
  1378	          selected = opts.selectedAgents;
  1379	          return successWire(selected);
  1380	        },
  1381	        async probe() {
  1382	          return selected.map((agent) => ({ agent, probed: true as const, latencyMs: 1 }));
  1383	        },
  1384	        async summary() {
  1385	          return {
  1386	            detected: null,
  1387	            projects: null,
  1388	            wired: null,
  1389	            probed: null,
  1390	            onboardingStateSnapshot: null,
  1391	          };
  1392	        },
  1393	        async markCompleted() {
  1394	          const state = JSON.parse(
  1395	            readFileSync(join(echoHome, 'state/onboarding.json'), 'utf8'),
  1396	          ) as {
  1397	            completed: boolean;
  1398	          };
  1399	          state.completed = true;
  1400	          writeFileSync(
  1401	            join(echoHome, 'state/onboarding.json'),
  1402	            `${JSON.stringify(state, null, 2)}\n`,
  1403	          );
  1404	        },
  1405	      } satisfies Wizard;
  1406	    }) as never;
  1407	    const { runInit } = await loadInit();
  1408	
  1409	    const code = await runInit({
  1410	      stdin: { isTTY: true },
  1411	      wizardFactory,
  1412	      prompt: makeNonInteractivePrompt({
  1413	        'Welcome to ECHO setup. This takes about two minutes.': true,
  1414	        'Confirm subset to wire': '',
  1415	        'Pick default project': '',
  1416	      }),
  1417	      quiet: true,
  1418	      ...daemonAlreadyRunning(),
  1419	    });
  1420	
  1421	    expect(code).toBe(0);
  1422	    expect(created).toEqual([true]);
  1423	    expect(existsSync(join(echoHome, 'state/onboarding.json'))).toBe(true);
  1424	    expect(existsSync(join(echoHome, 'state/projects.json'))).toBe(true);
  1425	  });
  1426	
  1427	  it('wires selected agents, populates capabilities, and marks onboarding complete', async () => {
  1428	    writeInitialState();
  1429	    const created: Array<{ mcpServerUrl: string; echoVersion: string }> = [];
  1430	    const wizardFactory = ((opts: { mcpServerUrl: string; echoVersion: string }): Wizard => {
  1431	      created.push(opts);
  1432	      let selected: AgentKind[] = [];
  1433	      return {
  1434	        async detectAgents() {
  1435	          return [detected('codex', 'high'), detected('cursor', 'medium')];
  1436	        },
  1437	        async detectProjects() {
  1438	          return [{ repoRoot: '/repo', atomCount: 2, lastSeen: 't', sourceBreakdown: {} }];
  1439	        },
  1440	        async wire(opts) {
  1441	          selected = opts.selectedAgents;
  1442	          return successWire(selected);
  1443	        },
  1444	        async probe() {
  1445	          return selected.map((agent) =>
  1446	            agent === 'cursor'
  1447	              ? { agent, probed: false, reason: 'manual-only' as const }
  1448	              : { agent, probed: true as const, latencyMs: 1 },
  1449	          );
  1450	        },
  1451	        async summary() {
  1452	          return {
  1453	            detected: null,
  1454	            projects: null,
  1455	            wired: null,
  1456	            probed: null,
  1457	            onboardingStateSnapshot: null,
  1458	          };
  1459	        },
  1460	        async markCompleted() {
  1461	          const state = JSON.parse(
  1462	            readFileSync(join(echoHome, 'state/onboarding.json'), 'utf8'),
  1463	          ) as {
  1464	            completed: boolean;
  1465	          };
  1466	          state.completed = true;
  1467	          writeFileSync(
  1468	            join(echoHome, 'state/onboarding.json'),
  1469	            `${JSON.stringify(state, null, 2)}\n`,
  1470	          );
  1471	        },
  1472	      };
  1473	    }) as never;
  1474	    const { AGENT_CAPABILITIES_BY_KIND, runInit } = await loadInit();
  1475	
  1476	    const code = await runInit({
  1477	      stdin: { isTTY: true },
  1478	      wizardFactory,
  1479	      prompt: makeNonInteractivePrompt({
  1480	        'Welcome to ECHO setup. This takes about two minutes.': true,
  1481	        'Confirm subset to wire': '',
  1482	        'Pick default project': '1',
  1483	      }),
  1484	      quiet: true,
  1485	      ...daemonAlreadyRunning(),
  1486	    });
  1487	
  1488	    const state = JSON.parse(readFileSync(join(echoHome, 'state/onboarding.json'), 'utf8')) as {
  1489	      completed: boolean;
  1490	      agents: Array<{ id: AgentKind; capabilities: string[] }>;
  1491	    };
  1492	    expect(code).toBe(0);
  1493	    expect(created[0]).toMatchObject({ mcpServerUrl: 'http://127.0.0.1:39999/mcp' });
  1494	    expect(state.completed).toBe(true);
  1495	    expect(state.agents.find((agent) => agent.id === 'codex')!.capabilities).toEqual(
  1496	      AGENT_CAPABILITIES_BY_KIND.codex,
  1497	    );
  1498	    expect(state.agents.find((agent) => agent.id === 'cursor')!.capabilities).toEqual(
  1499	      AGENT_CAPABILITIES_BY_KIND.cursor,
  1500	    );
  1501	  });
  1502	});

 succeeded in 196ms:
     1	import { mkdirSync, mkdtempSync, rmSync, writeFileSync } from 'node:fs';
     2	import { tmpdir } from 'node:os';
     3	import { join } from 'node:path';
     4	import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
     5	
     6	let tmpRoot: string;
     7	let echoHome: string;
     8	let dataDir: string;
     9	let originalEchoHome: string | undefined;
    10	let originalDataDir: string | undefined;
    11	let originalPort: string | undefined;
    12	
    13	async function loadDoctor(): Promise<typeof import('../../src/cli/commands/doctor.js')> {
    14	  return import('../../src/cli/commands/doctor.js');
    15	}
    16	
    17	function writeState(
    18	  agentReason: 'ok' | 'manual-only' | 'auth-required' | 'mcp-not-configured' = 'ok',
    19	  home = echoHome,
    20	): void {
    21	  mkdirSync(join(home, 'state'), { recursive: true });
    22	  const now = '2026-05-26T00:00:00.000Z';
    23	  writeFileSync(
    24	    join(home, 'state/onboarding.json'),
    25	    `${JSON.stringify(
    26	      {
    27	        schema_version: 1,
    28	        created_at: now,
    29	        last_updated_at: now,
    30	        completed: true,
    31	        profile: 'customer',
    32	        agents: [
    33	          {
    34	            id:
    35	              agentReason === 'manual-only'
    36	                ? 'cursor'
    37	                : agentReason === 'mcp-not-configured'
    38	                  ? 'claude-code'
    39	                  : 'codex',
    40	            detected_at: now,
    41	            wired_at: now,
    42	            probed_at: null,
    43	            capabilities: [],
    44	            wire_error: null,
    45	          },
    46	        ],
    47	      },
    48	      null,
    49	      2,
    50	    )}\n`,
    51	  );
    52	  writeFileSync(
    53	    join(home, 'state/projects.json'),
    54	    `${JSON.stringify({ schema_version: 1, last_refreshed_at: now, default_project: null, projects: [] }, null, 2)}\n`,
    55	  );
    56	}
    57	
    58	describe('runDoctor', () => {
    59	  beforeEach(() => {
    60	    originalEchoHome = process.env.ECHO_HOME;
    61	    originalDataDir = process.env.ECHO_DATA_DIR;
    62	    originalPort = process.env.ECHO_MCP_PORT;
    63	    tmpRoot = mkdtempSync(join(tmpdir(), 'echo-doctor-'));
    64	    echoHome = join(tmpRoot, 'echo-home');
    65	    dataDir = join(tmpRoot, 'data');
    66	    process.env.ECHO_HOME = echoHome;
    67	    process.env.ECHO_DATA_DIR = dataDir;
    68	    process.env.ECHO_MCP_PORT = '39998';
    69	    vi.resetModules();
    70	  });
    71	
    72	  afterEach(() => {
    73	    if (originalEchoHome === undefined) delete process.env.ECHO_HOME;
    74	    else process.env.ECHO_HOME = originalEchoHome;
    75	    if (originalDataDir === undefined) delete process.env.ECHO_DATA_DIR;
    76	    else process.env.ECHO_DATA_DIR = originalDataDir;
    77	    if (originalPort === undefined) delete process.env.ECHO_MCP_PORT;
    78	    else process.env.ECHO_MCP_PORT = originalPort;
    79	    rmSync(tmpRoot, { recursive: true, force: true });
    80	    vi.resetModules();
    81	  });
    82	
    83	  it('emits healthy JSON and sends the MCP initialize headers', async () => {
    84	    writeState();
    85	    mkdirSync(dataDir, { recursive: true });
    86	    writeFileSync(join(dataDir, 'daemon.pid'), '123');
    87	    const calls: Array<{ url: string; init: RequestInit }> = [];
    88	    const { runDoctor } = await loadDoctor();
    89	    const out: string[] = [];
    90	
    91	    const code = await runDoctor({
    92	      json: true,
    93	      stdout: {
    94	        write: (s) => {
    95	          out.push(String(s));
    96	          return true;
    97	        },
    98	      },
    99	      fetch: (async (url, init) => {
   100	        calls.push({ url: String(url), init: init ?? {} });
   101	        return new Response('{}', { status: 200 });
   102	      }) as typeof fetch,
   103	      probeAgents: async () => [{ agent: 'codex', probed: true, latencyMs: 1 }],
   104	    });
   105	
   106	    const report = JSON.parse(out.join('')) as {
   107	      echoHome: { profile: string };
   108	      overall: string;
   109	    };
   110	    expect(code).toBe(0);
   111	    expect(report.overall).toBe('healthy');
   112	    expect(report.echoHome.profile).toBe('customer');
   113	    expect(calls[0]!.init.headers).toMatchObject({
   114	      Accept: 'application/json, text/event-stream',
   115	      'Content-Type': 'application/json',
   116	    });
   117	    expect(JSON.parse(String(calls[0]!.init.body))).toMatchObject({
   118	      method: 'initialize',
   119	      params: { protocolVersion: '2025-06-18', clientInfo: { name: 'echoctl-doctor' } },
   120	    });
   121	  });
   122	
   123	  it('documents and honors --home, --port, and --label', async () => {
   124	    const isolatedHome = join(tmpRoot, 'isolated-echo-home');
   125	    writeState('ok', isolatedHome);
   126	    mkdirSync(dataDir, { recursive: true });
   127	    writeFileSync(join(dataDir, 'daemon.pid'), '123');
   128	    const calls: Array<{ url: string; init: RequestInit }> = [];
   129	    const { DOCTOR_HELP, parseDoctorArgs, runDoctor } = await loadDoctor();
   130	    const out: string[] = [];
   131	
   132	    expect(
   133	      parseDoctorArgs([
   134	        '--home',
   135	        isolatedHome,
   136	        '--port',
   137	        '41235',
   138	        '--label',
   139	        'com.echo.daemon.walkthrough',
   140	      ]),
   141	    ).toEqual({
   142	      home: isolatedHome,
   143	      port: '41235',
   144	      label: 'com.echo.daemon.walkthrough',
   145	    });
   146	    expect(DOCTOR_HELP).toContain('--home <path>');
   147	    expect(DOCTOR_HELP).toContain('--port <n>');
   148	    expect(DOCTOR_HELP).toContain('--label <id>');
   149	    expect(() => parseDoctorArgs(['--port', '0'])).toThrow('invalid --port: 0');
   150	
   151	    const code = await runDoctor({
   152	      json: true,
   153	      home: isolatedHome,
   154	      port: '41235',
   155	      label: 'com.echo.daemon.walkthrough',
   156	      stdout: { write: (s) => (out.push(String(s)), true) },
   157	      fetch: (async (url, init) => {
   158	        calls.push({ url: String(url), init: init ?? {} });
   159	        return new Response('{}', { status: 200 });
   160	      }) as typeof fetch,
   161	      probeAgents: async () => [{ agent: 'codex', probed: true, latencyMs: 1 }],
   162	    });
   163	
   164	    const report = JSON.parse(out.join('')) as {
   165	      daemon: { port: number };
   166	      echoHome: { root: string };
   167	      overall: string;
   168	    };
   169	    expect(code).toBe(0);
   170	    expect(calls[0]!.url).toBe('http://127.0.0.1:41235/mcp');
   171	    expect(report.daemon.port).toBe(41235);
   172	    expect(report.echoHome.root).toBe(isolatedHome);
   173	    expect(report.overall).toBe('healthy');
   174	  });
   175	
   176	  it('rolls unreachable daemon without pid lock to broken and stale pid lock to degraded', async () => {
   177	    writeState();
   178	    const { buildDoctorReport } = await loadDoctor();
   179	
   180	    const broken = await buildDoctorReport({
   181	      fetch: (async () => {
   182	        throw new Error('offline');
   183	      }) as typeof fetch,
   184	      probeAgents: async () => [{ agent: 'codex', probed: true, latencyMs: 1 }],
   185	    });
   186	    mkdirSync(dataDir, { recursive: true });
   187	    writeFileSync(join(dataDir, 'daemon.pid'), '123');
   188	    const degraded = await buildDoctorReport({
   189	      fetch: (async () => {
   190	        throw new Error('offline');
   191	      }) as typeof fetch,
   192	      probeAgents: async () => [{ agent: 'codex', probed: true, latencyMs: 1 }],
   193	    });
   194	
   195	    expect(broken.overall).toBe('broken');
   196	    expect(degraded.overall).toBe('degraded');
   197	  });
   198	
   199	  it('prints exact claude-code MCP remediation and local-shadow escape hatch', async () => {
   200	    writeState('mcp-not-configured');
   201	    mkdirSync(dataDir, { recursive: true });
   202	    writeFileSync(join(dataDir, 'daemon.pid'), '123');
   203	    const { runDoctor } = await loadDoctor();
   204	    const out: string[] = [];
   205	
   206	    const code = await runDoctor({
   207	      port: '41234',
   208	      stdout: { write: (s) => (out.push(String(s)), true) },
   209	      fetch: (async () => new Response('{}', { status: 200 })) as typeof fetch,
   210	      probeAgents: async () => [
   211	        {
   212	          agent: 'claude-code',
   213	          probed: false,
   214	          reason: 'mcp-not-configured',
   215	          detail: 'No such tool mcp__echo__echo_ping',
   216	        },
   217	      ],
   218	    });
   219	
   220	    const text = out.join('');
   221	    expect(code).toBe(1);
   222	    expect(text).toContain('agent claude-code: mcp-not-configured');
   223	    expect(text).toContain('profile=customer');
   224	    expect(text).toContain(
   225	      'claude mcp add --transport http --scope user echo http://127.0.0.1:41234/mcp',
   226	    );
   227	    expect(text).toContain('claude mcp remove echo -s local');
   228	    expect(text).toContain('echoctl doctor');
   229	  });
   230	});

 succeeded in 197ms:
     1	import {
     2	  existsSync,
     3	  mkdirSync,
     4	  mkdtempSync,
     5	  readFileSync,
     6	  rmSync,
     7	  statSync,
     8	  symlinkSync,
     9	  writeFileSync,
    10	  readdirSync,
    11	} from 'node:fs';
    12	import { tmpdir } from 'node:os';
    13	import { dirname, join, resolve, sep } from 'node:path';
    14	import { afterEach, beforeEach, describe, expect, it, vi } from 'vitest';
    15	
    16	type AdapterSyncModule = typeof import('../../src/echo-home/adapter-sync.js');
    17	
    18	let tmpRoot: string;
    19	let stubHome: string;
    20	let echoHome: string;
    21	let originalHome: string | undefined;
    22	let originalEchoHome: string | undefined;
    23	
    24	const ROLES = ['builder.toml', 'reviewer.toml', 'strategist.toml'];
    25	
    26	async function loadAdapterSync(): Promise<AdapterSyncModule> {
    27	  return import('../../src/echo-home/adapter-sync.js');
    28	}
    29	
    30	function setupRepoMirror(): {
    31	  repoRoot: string;
    32	  skillsDir: string;
    33	  rolesDir: string;
    34	  workflowsDir: string;
    35	} {
    36	  const repoRoot = join(tmpRoot, 'repo');
    37	  const contributorSkillsDir = join(repoRoot, 'skills');
    38	  const skillsDir = join(repoRoot, 'assets', 'echo-skills');
    39	  const rolesDir = join(repoRoot, 'assets', 'echo-roles');
    40	  const workflowsDir = join(repoRoot, 'assets', 'echo-workflows');
    41	  mkdirSync(contributorSkillsDir, { recursive: true });
    42	  mkdirSync(skillsDir, { recursive: true });
    43	  mkdirSync(rolesDir, { recursive: true });
    44	  mkdirSync(workflowsDir, { recursive: true });
    45	  // Pretend this is a package root: package.json + assets/echo-skills/.
    46	  writeFileSync(join(repoRoot, 'package.json'), '{}\n');
    47	  // Contributor-process skills should exist but not be customer-synced.
    48	  writeFileSync(join(contributorSkillsDir, 'process-backlog.md'), '# process\n');
    49	  writeFileSync(join(contributorSkillsDir, 'merge-and-cleanup.md'), '# merge\n');
    50	  // Customer skill fixtures.
    51	  writeFileSync(join(skillsDir, 'alpha.md'), '# alpha\n');
    52	  writeFileSync(join(skillsDir, 'beta.md'), '# beta\n');
    53	  // Role fixtures (matching DEFAULT_ROLE_FILENAMES).
    54	  for (const r of ROLES) writeFileSync(join(rolesDir, r), `# ${r}\nbody\n`);
    55	  writeFileSync(
    56	    join(workflowsDir, 'change-review.toml'),
    57	    '[workflow]\nname = "change-review"\ndescription = "Review"\nschema_version = 1\n\n[[step]]\nrole = "reviewer"\nprompt = "Review"\n',
    58	  );
    59	  return { repoRoot, skillsDir, rolesDir, workflowsDir };
    60	}
    61	
    62	beforeEach(() => {
    63	  originalHome = process.env.HOME;
    64	  originalEchoHome = process.env.ECHO_HOME;
    65	  tmpRoot = mkdtempSync(join(tmpdir(), 'echo-072-adapter-'));
    66	  stubHome = join(tmpRoot, 'home');
    67	  mkdirSync(stubHome, { recursive: true });
    68	  process.env.HOME = stubHome;
    69	  echoHome = join(stubHome, '.echo');
    70	  process.env.ECHO_HOME = echoHome;
    71	  vi.resetModules();
    72	});
    73	
    74	afterEach(() => {
    75	  if (originalHome === undefined) delete process.env.HOME;
    76	  else process.env.HOME = originalHome;
    77	  if (originalEchoHome === undefined) delete process.env.ECHO_HOME;
    78	  else process.env.ECHO_HOME = originalEchoHome;
    79	  rmSync(tmpRoot, { recursive: true, force: true });
    80	  vi.resetModules();
    81	});
    82	
    83	describe('syncAll (orchestrator)', () => {
    84	  it('all three agents succeed → overallOk:true', async () => {
    85	    const { repoRoot } = setupRepoMirror();
    86	    const { syncAll } = await loadAdapterSync();
    87	
    88	    const profiles = [
    89	      {
    90	        kind: 'codex' as const,
    91	        echoSection: '## ECHO Pro\nx',
    92	        mcpServerConfig: { url: 'http://localhost:7117/mcp' },
    93	      },
    94	      {
    95	        kind: 'claude-code' as const,
    96	        echoSection: '## ECHO Pro\nx',
    97	      },
    98	      {
    99	        kind: 'cursor' as const,
   100	        mcpServerConfig: { url: 'http://localhost:7117/mcp' },
   101	      },
   102	    ];
   103	
   104	    const result = await syncAll(profiles, { repoRoot });
   105	    expect(result.overallOk).toBe(true);
   106	    expect(result.skillsPopulated.ok).toBe(true);
   107	    expect(result.agents).toHaveLength(3);
   108	    for (const a of result.agents) expect(a.ok).toBe(true);
   109	  });
   110	
   111	  it('codex conflict on config.toml; cursor + claude-code unaffected; no transactional rollback', async () => {
   112	    const { repoRoot } = setupRepoMirror();
   113	    // Pre-seed codex config with an unexpected echo block (forces conflict).
   114	    const codexDir = join(stubHome, '.codex');
   115	    mkdirSync(codexDir, { recursive: true });
   116	    writeFileSync(
   117	      join(codexDir, 'config.toml'),
   118	      `[mcp_servers.echo]\nurl = "http://user-edited"\n`,
   119	    );
   120	    const { syncAll } = await loadAdapterSync();
   121	    const result = await syncAll(
   122	      [
   123	        {
   124	          kind: 'codex',
   125	          echoSection: '## ECHO Pro\nx',
   126	          mcpServerConfig: { url: 'http://localhost:7117/mcp' },
   127	        },
   128	        { kind: 'claude-code', echoSection: '## ECHO Pro\nx' },
   129	        { kind: 'cursor', mcpServerConfig: { url: 'http://localhost:7117/mcp' } },
   130	      ],
   131	      { repoRoot },
   132	    );
   133	    expect(result.overallOk).toBe(false);
   134	    const codex = result.agents.find((a) => a.agent === 'codex')!;
   135	    expect(codex.ok).toBe(false);
   136	    if (codex.ok === false) {
   137	      expect(codex.conflicts.length).toBeGreaterThan(0);
   138	      expect(codex.conflicts[0].kind).toBe('config');
   139	    }
   140	    const claude = result.agents.find((a) => a.agent === 'claude-code')!;
   141	    expect(claude.ok).toBe(true);
   142	    const cursor = result.agents.find((a) => a.agent === 'cursor')!;
   143	    expect(cursor.ok).toBe(true);
   144	    // Cursor config WAS written (no rollback).
   145	    expect(existsSync(join(stubHome, '.cursor', 'mcp.json'))).toBe(true);
   146	  });
   147	
   148	  it('populate-skills runs before claude-code fan-out', async () => {
   149	    const { repoRoot } = setupRepoMirror();
   150	    const { syncAll } = await loadAdapterSync();
   151	    const result = await syncAll([{ kind: 'claude-code', echoSection: '## E\nx' }], { repoRoot });
   152	    expect(result.skillsPopulated.ok).toBe(true);
   153	    expect(existsSync(join(echoHome, 'skills', 'alpha.md'))).toBe(true);
   154	    expect(existsSync(join(echoHome, 'skills', 'process-backlog.md'))).toBe(false);
   155	    expect(existsSync(join(echoHome, 'skills', 'merge-and-cleanup.md'))).toBe(false);
   156	    expect(existsSync(join(stubHome, '.claude', 'commands', 'alpha.md'))).toBe(true);
   157	    expect(existsSync(join(stubHome, '.claude', 'commands', 'process-backlog.md'))).toBe(false);
   158	  });
   159	
   160	  it('customer profile filters dogfood skills and skips roles/workflows as successful no-ops', async () => {
   161	    const { repoRoot, skillsDir } = setupRepoMirror();
   162	    writeFileSync(join(skillsDir, 'dogfood.md'), '---\naudience: dogfood\n---\n# dogfood\n');
   163	    const { syncAll } = await loadAdapterSync();
   164	
   165	    const result = await syncAll([{ kind: 'claude-code', echoSection: '## E\nx' }], {
   166	      repoRoot,
   167	      profile: 'customer',
   168	    });
   169	
   170	    expect(result.overallOk).toBe(true);
   171	    expect(result.skillsPopulated.ok).toBe(true);
   172	    if (result.skillsPopulated.ok) {
   173	      expect(result.skillsPopulated.copied.sort()).toEqual(['alpha.md', 'beta.md']);
   174	      expect(result.skillsPopulated.skipped).toContain('dogfood.md');
   175	    }
   176	    expect(existsSync(join(echoHome, 'skills', 'dogfood.md'))).toBe(false);
   177	    expect(existsSync(join(stubHome, '.claude', 'commands', 'dogfood.md'))).toBe(false);
   178	    expect(result.roles.results.map((entry) => entry.action)).toEqual(['noop', 'noop', 'noop']);
   179	    expect(existsSync(join(echoHome, 'roles', 'builder.toml'))).toBe(false);
   180	    expect(result.workflowsResult?.results).toEqual([
   181	      { workflow: 'change-review.toml', action: 'noop' },
   182	    ]);
   183	    expect(existsSync(join(echoHome, 'workflows', 'change-review.toml'))).toBe(false);
   184	  });
   185	
   186	  it('populate-skills failure (missing repoSkillsDir) skips claude-code fan-out and flips overallOk', async () => {
   187	    const { repoRoot } = setupRepoMirror();
   188	    const { syncAll } = await loadAdapterSync();
   189	    const result = await syncAll([{ kind: 'claude-code', echoSection: '## E\nx' }], {
   190	      repoRoot,
   191	      repoSkillsDir: join(repoRoot, 'does-not-exist'),
   192	    });
   193	    expect(result.skillsPopulated.ok).toBe(false);
   194	    expect(result.overallOk).toBe(false);
   195	    const claude = result.agents.find((a) => a.agent === 'claude-code')!;
   196	    // Markers merge still ran; second-hop skipped.
   197	    if (claude.ok === true) {
   198	      expect(claude.skipped).toContain('syncClaudeSkills');
   199	    } else if (claude.ok === false) {
   200	      expect(claude.skipped).toContain('syncClaudeSkills');
   201	    }
   202	    // commandsDir was not populated.
   203	    const cmdDir = join(stubHome, '.claude', 'commands');
   204	    if (existsSync(cmdDir)) {
   205	      expect(readdirSync(cmdDir)).toEqual([]);
   206	    }
   207	  });
   208	
   209	  it('default role list comes from DEFAULT_ROLE_FILENAMES (071) when opts.defaultRoles omitted', async () => {
   210	    const { repoRoot } = setupRepoMirror();
   211	    const { syncAll } = await loadAdapterSync();
   212	    const result = await syncAll([], { repoRoot });
   213	    const rolesSeen = result.roles.results.map((r) => r.role).sort();
   214	    expect(rolesSeen).toEqual([...ROLES].sort());
   215	  });
   216	
   217	  it('default workflow is copied into ECHO_HOME workflows and keeps overallOk true', async () => {
   218	    const { repoRoot, workflowsDir } = setupRepoMirror();
   219	    const { syncAll } = await loadAdapterSync();
   220	
   221	    const result = await syncAll([], {
   222	      repoRoot,
   223	      workflowsSourceDir: workflowsDir,
   224	      defaultWorkflows: ['change-review.toml'],
   225	    });
   226	
   227	    expect(readFileSync(join(echoHome, 'workflows', 'change-review.toml'))).toEqual(
   228	      readFileSync(join(workflowsDir, 'change-review.toml')),
   229	    );
   230	    expect(result.workflowsResult?.results).toEqual([
   231	      { workflow: 'change-review.toml', action: 'copied' },
   232	    ]);
   233	    expect(result.overallOk).toBe(true);
   234	  });
   235	
   236	  it('default workflow source-missing is diagnosed via workflowsResult and fails overallOk', async () => {
   237	    const { repoRoot } = setupRepoMirror();
   238	    const emptyWorkflows = join(tmpRoot, 'empty-workflows');
   239	    mkdirSync(emptyWorkflows);
   240	    const { syncAll } = await loadAdapterSync();
   241	
   242	    const result = await syncAll([], {
   243	      repoRoot,
   244	      workflowsSourceDir: emptyWorkflows,
   245	      defaultWorkflows: ['change-review.toml'],
   246	    });
   247	
   248	    expect(result.workflowsResult?.results[0]).toEqual({
   249	      workflow: 'change-review.toml',
   250	      action: 'source-missing',
   251	    });
   252	    expect(result.overallOk).toBe(false);
   253	  });
   254	
   255	  it('user-modified workflow is preserved and does not fail overallOk', async () => {
   256	    const { repoRoot } = setupRepoMirror();
   257	    const workflowsTarget = join(echoHome, 'workflows');
   258	    mkdirSync(workflowsTarget, { recursive: true });
   259	    const target = join(workflowsTarget, 'change-review.toml');
   260	    writeFileSync(target, 'user edit\n');
   261	    const before = statSync(target);
   262	    const { syncAll } = await loadAdapterSync();
   263	
   264	    const result = await syncAll([], {
   265	      repoRoot,
   266	      defaultWorkflows: ['change-review.toml'],
   267	    });
   268	
   269	    expect(result.workflowsResult?.results[0]?.action).toBe('user-modified');
   270	    expect(result.overallOk).toBe(true);
   271	    expect(readFileSync(target, 'utf8')).toBe('user edit\n');
   272	    expect(statSync(target).mtimeMs).toBe(before.mtimeMs);
   273	  });
   274	
   275	  it('workflow per-file error fails overallOk without blocking skills or roles', async () => {
   276	    const { repoRoot } = setupRepoMirror();
   277	    const workflowsTarget = join(echoHome, 'workflows');
   278	    mkdirSync(join(workflowsTarget, 'change-review.toml'), { recursive: true });
   279	    const { syncAll } = await loadAdapterSync();
   280	
   281	    const result = await syncAll([], {
   282	      repoRoot,
   283	      defaultWorkflows: ['change-review.toml'],
   284	    });
   285	
   286	    expect(result.workflowsResult?.results).toHaveLength(1);
   287	    expect(result.workflowsResult?.results[0]).toMatchObject({
   288	      workflow: 'change-review.toml',
   289	      action: 'error',
   290	    });
   291	    expect(result.workflowsResult?.workflowsErrors).toHaveLength(1);
   292	    expect(result.roles.results.map((entry) => entry.action)).toEqual([
   293	      'copied',
   294	      'copied',
   295	      'copied',
   296	    ]);
   297	    expect(result.skillsPopulated.ok).toBe(true);
   298	    expect(result.overallOk).toBe(false);
   299	  });
   300	
   301	  it('lockfile present → second sync returns RETRY_CONFLICT with shell-quoted rm hint; agents = []', async () => {
   302	    const { repoRoot } = setupRepoMirror();
   303	    const { syncAll } = await loadAdapterSync();
   304	    // Pre-create lockfile.
   305	    const stateDir = join(echoHome, 'state');
   306	    mkdirSync(stateDir, { recursive: true });
   307	    const lockPath = join(stateDir, 'adapter-sync.lock');
   308	    writeFileSync(lockPath, JSON.stringify({ pid: 0, hostname: 'x' }));
   309	    const result = await syncAll([], { repoRoot });
   310	    expect(result.syncLock).toBeDefined();
   311	    expect(result.syncLock!.code).toBe('RETRY_CONFLICT');
   312	    expect(result.syncLock!.file).toBe(lockPath);
   313	    expect(result.syncLock!.message).toContain('rm -- ');
   314	    expect(result.agents).toEqual([]);
   315	    expect(result.overallOk).toBe(false);
   316	    // Now remove the lock, retry.
   317	    rmSync(lockPath);
   318	    const result2 = await syncAll([], { repoRoot });
   319	    expect(result2.syncLock).toBeUndefined();
   320	  });
   321	
   322	  it('lock listeners (exit/SIGINT/SIGTERM) unregistered after success', async () => {
   323	    const { repoRoot } = setupRepoMirror();
   324	    const { syncAll } = await loadAdapterSync();
   325	    const c1 = process.listenerCount('exit');
   326	    const c2 = process.listenerCount('SIGINT');
   327	    const c3 = process.listenerCount('SIGTERM');
   328	    await syncAll([], { repoRoot });
   329	    expect(process.listenerCount('exit')).toBe(c1);
   330	    expect(process.listenerCount('SIGINT')).toBe(c2);
   331	    expect(process.listenerCount('SIGTERM')).toBe(c3);
   332	    // Run 50 more times to pin no leak.
   333	    for (let i = 0; i < 50; i++) {
   334	      await syncAll([], { repoRoot });
   335	    }
   336	    expect(process.listenerCount('exit')).toBe(c1);
   337	    expect(process.listenerCount('SIGINT')).toBe(c2);
   338	    expect(process.listenerCount('SIGTERM')).toBe(c3);
   339	  });
   340	
   341	  it('repoRoot not findable → repoRoot AdapterError; agents = []', async () => {
   342	    const { syncAll } = await loadAdapterSync();
   343	    // Don't set up a repo mirror — pass a tmpdir as repoRoot that has no
   344	    // package.json + assets/echo-skills.
   345	    const noRepo = join(tmpRoot, 'no-repo');
   346	    mkdirSync(noRepo);
   347	    const result = await syncAll([], { repoRoot: noRepo });
   348	    // syncAll uses opts.repoRoot directly when provided — it doesn't validate.
   349	    // So if we DON'T pass repoRoot, the walk fails (since we may not be in a
   350	    // repo from the test runner's perspective — but the test runner IS in a
   351	    // repo). To force failure, monkey-patch by passing a known-bad explicit
   352	    // repoSkillsDir, leaving repoRoot resolution at module-level (which will
   353	    // succeed). The intent of this test is the unfound-walk path — we exercise
   354	    // it by setting opts.repoRoot to a tmpdir which has neither package.json
   355	    // nor customer skills, but the orchestrator currently trusts opts.repoRoot. We
   356	    // therefore test the missing-source branch via repoSkillsDir.
   357	    expect(result.repoRoot).toBeUndefined();
   358	    // The role + skill sources won't exist; skills populate should fail.
   359	    expect(result.skillsPopulated.ok).toBe(false);
   360	  });
   361	
   362	  it('missing required input → agent.ok:false with MISSING_REQUIRED_INPUT', async () => {
   363	    const { repoRoot } = setupRepoMirror();
   364	    const { syncAll } = await loadAdapterSync();
   365	    const result = await syncAll(
   366	      [{ kind: 'codex', echoSection: '## E\nx' /* mcpServerConfig omitted */ }],
   367	      { repoRoot },
   368	    );
   369	    expect(result.overallOk).toBe(false);
   370	    const codex = result.agents.find((a) => a.agent === 'codex')!;
   371	    expect(codex.ok).toBe(false);
   372	    if (codex.ok === false) {
   373	      expect(codex.errors.some((e) => e.code === 'MISSING_REQUIRED_INPUT')).toBe(true);
   374	    }
   375	  });
   376	
   377	  it('marker target is a symlink → target-symlink conflict; linked file unchanged', async () => {
   378	    const { repoRoot } = setupRepoMirror();
   379	    // Pre-create the claude instructions file as a symlink to an external file.
   380	    const realFile = join(tmpRoot, 'external-CLAUDE.md');
   381	    writeFileSync(realFile, 'pristine\n');
   382	    mkdirSync(join(stubHome, '.claude'), { recursive: true });
   383	    symlinkSync(realFile, join(stubHome, '.claude', 'CLAUDE.md'));
   384	    const { syncAll } = await loadAdapterSync();
   385	    const result = await syncAll([{ kind: 'claude-code', echoSection: '## E\nx' }], { repoRoot });
   386	    const claude = result.agents.find((a) => a.agent === 'claude-code')!;
   387	    expect(claude.ok).toBe(false);
   388	    if (claude.ok === false) {
   389	      expect(claude.conflicts.some((c) => c.kind === 'target-symlink')).toBe(true);
   390	    }
   391	    expect(readFileSync(realFile, 'utf8')).toBe('pristine\n');
   392	  });
   393	
   394	  it('directory-component symlink at ~/.echo/skills → directorySymlink AdapterError, agents = []', async () => {
   395	    const { repoRoot } = setupRepoMirror();
   396	    // Create stub echo home with skills as a symlink.
   397	    mkdirSync(echoHome, { recursive: true });
   398	    const linkTarget = join(tmpRoot, 'redirected-skills');
   399	    mkdirSync(linkTarget, { recursive: true });
   400	    symlinkSync(linkTarget, join(echoHome, 'skills'));
   401	    const { syncAll } = await loadAdapterSync();
   402	    const result = await syncAll([], { repoRoot });
   403	    expect(result.directorySymlink).toBeDefined();
   404	    expect(result.directorySymlink!.code).toBe('EEXIST');
   405	    expect(result.agents).toEqual([]);
   406	    expect(result.overallOk).toBe(false);
   407	  });
   408	
   409	  it('directory-component symlink at ~/.echo/workflows short-circuits before workflow sync', async () => {
   410	    const { repoRoot } = setupRepoMirror();
   411	    mkdirSync(echoHome, { recursive: true });
   412	    const linkTarget = join(tmpRoot, 'redirected-workflows');
   413	    mkdirSync(linkTarget, { recursive: true });
   414	    symlinkSync(linkTarget, join(echoHome, 'workflows'));
   415	    const { syncAll } = await loadAdapterSync();
   416	
   417	    const result = await syncAll([], { repoRoot });
   418	
   419	    expect(result.directorySymlink).toBeDefined();
   420	    expect(result.directorySymlink!.file).toBe(join(echoHome, 'workflows'));
   421	    expect(result.overallOk).toBe(false);
   422	    expect(result.workflowsResult).toBeUndefined();
   423	    expect(readdirSync(linkTarget)).toEqual([]);
   424	  });
   425	
   426	  it('first-run: parent dirs (.codex, .cursor) are created when defaults are used', async () => {
   427	    const { repoRoot } = setupRepoMirror();
   428	    expect(existsSync(join(stubHome, '.codex'))).toBe(false);
   429	    expect(existsSync(join(stubHome, '.cursor'))).toBe(false);
   430	    const { syncAll } = await loadAdapterSync();
   431	    const result = await syncAll(
   432	      [
   433	        {
   434	          kind: 'codex',
   435	          echoSection: '## E\nx',
   436	          mcpServerConfig: { url: 'http://localhost:7117/mcp' },
   437	        },
   438	        { kind: 'cursor', mcpServerConfig: { url: 'http://localhost:7117/mcp' } },
   439	      ],
   440	      { repoRoot },
   441	    );
   442	    expect(existsSync(join(stubHome, '.codex', 'config.toml'))).toBe(true);
   443	    expect(existsSync(join(stubHome, '.cursor', 'mcp.json'))).toBe(true);
   444	    expect(result.overallOk).toBe(true);
   445	  });
   446	
   447	  it('symlink in repo assets/echo-skills/ is not propagated to ~/.echo/skills/', async () => {
   448	    const { repoRoot, skillsDir } = setupRepoMirror();
   449	    const external = join(tmpRoot, 'external-skill.md');
   450	    writeFileSync(external, 'untouchable\n');
   451	    symlinkSync(external, join(skillsDir, 'leaky.md'));
   452	    const { syncAll } = await loadAdapterSync();
   453	    await syncAll([], { repoRoot });
   454	    expect(existsSync(join(echoHome, 'skills', 'alpha.md'))).toBe(true);
   455	    expect(existsSync(join(echoHome, 'skills', 'leaky.md'))).toBe(false);
   456	  });
   457	
   458	  it('user-modified role flips overallOk to false unless opts.allowUserModifiedRoles', async () => {
   459	    const { repoRoot } = setupRepoMirror();
   460	    // Pre-seed user-modified reviewer.toml.
   461	    const rolesTarget = join(echoHome, 'roles');
   462	    mkdirSync(rolesTarget, { recursive: true });
   463	    writeFileSync(join(rolesTarget, 'reviewer.toml'), 'user-edit\n');
   464	    const { syncAll } = await loadAdapterSync();
   465	    const r1 = await syncAll([], { repoRoot });
   466	    expect(r1.overallOk).toBe(false);
   467	    const reviewer = r1.roles.results.find((r) => r.role === 'reviewer.toml')!;
   468	    expect(reviewer.action).toBe('user-modified');
   469	    // With opt-in, overallOk is true (still need skills to be populated though).
   470	    const r2 = await syncAll([], { repoRoot, allowUserModifiedRoles: true });
   471	    expect(r2.overallOk).toBe(true);
   472	  });
   473	
   474	  it('cursor config preserves auth headers in conflict payload but does NOT log them', async () => {
   475	    const { repoRoot } = setupRepoMirror();
   476	    const cursorDir = join(stubHome, '.cursor');
   477	    mkdirSync(cursorDir, { recursive: true });
   478	    writeFileSync(
   479	      join(cursorDir, 'mcp.json'),
   480	      JSON.stringify(
   481	        {
   482	          mcpServers: {
   483	            echo: { url: 'http://user-edited', headers: { Authorization: 'Bearer leaked-xyz' } },
   484	          },
   485	        },
   486	        null,
   487	        2,
   488	      ),
   489	    );
   490	    const stderrSpy = vi.spyOn(process.stderr, 'write').mockImplementation(() => true);
   491	    const stdoutSpy = vi.spyOn(process.stdout, 'write').mockImplementation(() => true);
   492	    const { syncAll } = await loadAdapterSync();
   493	    const result = await syncAll(
   494	      [
   495	        {
   496	          kind: 'cursor',
   497	          mcpServerConfig: {
   498	            url: 'http://localhost:7117/mcp',
   499	            headers: { Authorization: 'Bearer new-secret' },
   500	          },
   501	        },
   502	      ],
   503	      { repoRoot },
   504	    );
   505	    const cursor = result.agents.find((a) => a.agent === 'cursor')!;
   506	    expect(cursor.ok).toBe(false);
   507	    const stderrCalls = stderrSpy.mock.calls.map((c) => String(c[0])).join('');
   508	    const stdoutCalls = stdoutSpy.mock.calls.map((c) => String(c[0])).join('');
   509	    expect(stderrCalls).not.toContain('Bearer');
   510	    expect(stdoutCalls).not.toContain('Bearer');
   511	    stderrSpy.mockRestore();
   512	    stdoutSpy.mockRestore();
   513	  });
   514	
   515	  it('malformed JSON in cursor mcp.json does not throw; agent ok:false with PARSE_ERROR; no leaked bytes', async () => {
   516	    const { repoRoot } = setupRepoMirror();
   517	    const cursorDir = join(stubHome, '.cursor');
   518	    mkdirSync(cursorDir, { recursive: true });
   519	    writeFileSync(join(cursorDir, 'mcp.json'), '{ "mcpServers": { broken-syntax');
   520	    const stderrSpy = vi.spyOn(process.stderr, 'write').mockImplementation(() => true);
   521	    const { syncAll } = await loadAdapterSync();
   522	    const result = await syncAll(
   523	      [
   524	        { kind: 'cursor', mcpServerConfig: { url: 'http://localhost:7117/mcp' } },
   525	        {
   526	          kind: 'codex',
   527	          echoSection: '## E\nx',
   528	          mcpServerConfig: { url: 'http://localhost:7117/mcp' },
   529	        },
   530	      ],
   531	      { repoRoot },
   532	    );
   533	    const cursor = result.agents.find((a) => a.agent === 'cursor')!;
   534	    expect(cursor.ok).toBe(false);
   535	    if (cursor.ok === false) {
   536	      expect(cursor.errors.some((e) => e.code === 'PARSE_ERROR')).toBe(true);
   537	    }
   538	    const codex = result.agents.find((a) => a.agent === 'codex')!;
   539	    expect(codex.ok).toBe(true);
   540	    expect(result.overallOk).toBe(false);
   541	    stderrSpy.mockRestore();
   542	  });
   543	
   544	  it('previous*-absent: existing differs from proposed → conflict', async () => {
   545	    const { repoRoot } = setupRepoMirror();
   546	    mkdirSync(join(stubHome, '.claude'), { recursive: true });
   547	    writeFileSync(
   548	      join(stubHome, '.claude', 'CLAUDE.md'),
   549	      '<!-- BEGIN ECHO -->\nold-content\n<!-- END ECHO -->\n',
   550	    );
   551	    const { syncAll } = await loadAdapterSync();
   552	    const result = await syncAll([{ kind: 'claude-code', echoSection: 'new-content' }], {
   553	      repoRoot,
   554	    });
   555	    const claude = result.agents.find((a) => a.agent === 'claude-code')!;
   556	    expect(claude.ok).toBe(false);
   557	  });
   558	
   559	  it('symlinked target in commandsDir is skipped, not followed', async () => {
   560	    const { repoRoot } = setupRepoMirror();
   561	    const cmdDir = join(stubHome, '.claude', 'commands');
   562	    mkdirSync(cmdDir, { recursive: true });
   563	    const externalSkill = join(tmpRoot, 'external-skill.md');
   564	    writeFileSync(externalSkill, 'pristine-external\n');
   565	    symlinkSync(externalSkill, join(cmdDir, 'alpha.md'));
   566	    const { syncAll } = await loadAdapterSync();
   567	    await syncAll([{ kind: 'claude-code', echoSection: '## E\nx' }], { repoRoot });
   568	    expect(readFileSync(externalSkill, 'utf8')).toBe('pristine-external\n');
   569	    // beta.md still copies fine.
   570	    expect(existsSync(join(cmdDir, 'beta.md'))).toBe(true);
   571	  });
   572	
   573	  it('all commandsDir targets pre-staged as symlinks → claude-code ok:false / overallOk:false', async () => {
   574	    const { repoRoot } = setupRepoMirror();
   575	    const cmdDir = join(stubHome, '.claude', 'commands');
   576	    mkdirSync(cmdDir, { recursive: true });
   577	    const externA = join(tmpRoot, 'extern-alpha.md');
   578	    const externB = join(tmpRoot, 'extern-beta.md');
   579	    writeFileSync(externA, 'pristine-alpha\n');
   580	    writeFileSync(externB, 'pristine-beta\n');
   581	    symlinkSync(externA, join(cmdDir, 'alpha.md'));
   582	    symlinkSync(externB, join(cmdDir, 'beta.md'));
   583	    const { syncAll } = await loadAdapterSync();
   584	    const result = await syncAll([{ kind: 'claude-code', echoSection: '## E\nx' }], {
   585	      repoRoot,
   586	    });
   587	    const claude = result.agents.find((a) => a.agent === 'claude-code')!;
   588	    expect(claude.ok).toBe(false);
   589	    expect(result.overallOk).toBe(false);
   590	    if (!claude.ok) {
   591	      const zeroCopiedErr = claude.errors.find((e) => e.message.includes('copied 0 files'));
   592	      expect(zeroCopiedErr).toBeDefined();
   593	      expect(zeroCopiedErr!.code).toBe('EEXIST');
   594	    }
   595	    expect(readFileSync(externA, 'utf8')).toBe('pristine-alpha\n');
   596	    expect(readFileSync(externB, 'utf8')).toBe('pristine-beta\n');
   597	  });
   598	
   599	  it('symlinked dotfile target (config.toml) is written through (followSymlink: true preserved)', async () => {
   600	    const { repoRoot } = setupRepoMirror();
   601	    const dotfiles = join(tmpRoot, 'dotfiles');
   602	    mkdirSync(dotfiles, { recursive: true });
   603	    const realCfg = join(dotfiles, 'codex-config.toml');
   604	    writeFileSync(realCfg, '');
   605	    mkdirSync(join(stubHome, '.codex'), { recursive: true });
   606	    symlinkSync(realCfg, join(stubHome, '.codex', 'config.toml'));
   607	    const { syncAll } = await loadAdapterSync();
   608	    const result = await syncAll(
   609	      [
   610	        {
   611	          kind: 'codex',
   612	          echoSection: '## E\nx',
   613	          mcpServerConfig: { url: 'http://localhost:7117/mcp' },
   614	        },
   615	      ],
   616	      { repoRoot },
   617	    );
   618	    expect(result.overallOk).toBe(true);
   619	    // The symlink itself is preserved.
   620	    const linkPath = join(stubHome, '.codex', 'config.toml');
   621	    expect(statSync(linkPath).isFile()).toBe(true);
   622	    // The dotfiles target received the update.
   623	    expect(readFileSync(realCfg, 'utf8')).toContain('[mcp_servers.echo]');
   624	    // The temp file lived next to dotfiles realCfg, not next to the symlink.
   625	    const dotfilesContents = readdirSync(dotfiles);
   626	    expect(dotfilesContents.every((n) => !n.endsWith('.tmp'))).toBe(true);
   627	  });
   628	
   629	  // Suppress unused-var warning
   630	  void resolve;
   631	  void dirname;
   632	  void sep;
   633	});

 succeeded in 197ms:
     1	import {
     2	  mkdirSync,
     3	  mkdtempSync,
     4	  readFileSync,
     5	  rmSync,
     6	  symlinkSync,
     7	  writeFileSync,
     8	  existsSync,
     9	} from 'node:fs';
    10	import { tmpdir } from 'node:os';
    11	import { join } from 'node:path';
    12	import { afterEach, beforeEach, describe, expect, it } from 'vitest';
    13	
    14	import {
    15	  populateEchoSkills,
    16	  syncClaudeSkills,
    17	} from '../../../src/echo-home/adapters/skill-sync.js';
    18	
    19	let tmpRoot: string;
    20	
    21	beforeEach(() => {
    22	  tmpRoot = mkdtempSync(join(tmpdir(), 'echo-072-skills-'));
    23	});
    24	
    25	afterEach(() => {
    26	  rmSync(tmpRoot, { recursive: true, force: true });
    27	});
    28	
    29	function setupSource(): { source: string; names: string[] } {
    30	  const source = join(tmpRoot, 'src-skills');
    31	  mkdirSync(source);
    32	  const names = ['alpha.md', 'beta.md', 'gamma.md'];
    33	  for (const n of names) writeFileSync(join(source, n), `# ${n}\n`);
    34	  return { source, names };
    35	}
    36	
    37	// Each function is exercised independently to keep the union types narrow.
    38	function runFn(
    39	  label: 'populateEchoSkills' | 'syncClaudeSkills',
    40	  opts: { sourceDir: string; targetDir: string },
    41	): { copied: string[]; skipped: string[] } {
    42	  if (label === 'populateEchoSkills') {
    43	    const r = populateEchoSkills(opts);
    44	    if (!r.ok) throw new Error(`populateEchoSkills failed: ${r.error}`);
    45	    return { copied: r.copied, skipped: r.skipped };
    46	  }
    47	  const r = syncClaudeSkills(opts);
    48	  return { copied: r.copied, skipped: r.skipped };
    49	}
    50	
    51	describe.each([{ label: 'populateEchoSkills' as const }, { label: 'syncClaudeSkills' as const }])(
    52	  '$label (shared overwrite-posture contract)',
    53	  ({ label }) => {
    54	    it(`${label}: target dir does not exist → created; all source skills copied`, () => {
    55	      const { source, names } = setupSource();
    56	      const target = join(tmpRoot, 'tgt');
    57	      expect(existsSync(target)).toBe(false);
    58	      const r = runFn(label, { sourceDir: source, targetDir: target });
    59	      expect(existsSync(target)).toBe(true);
    60	      expect(r.copied.sort()).toEqual(names);
    61	      for (const n of names) {
    62	        expect(readFileSync(join(target, n), 'utf8')).toBe(`# ${n}\n`);
    63	      }
    64	    });
    65	
    66	    it(`${label}: re-run produces byte-identical files (idempotency-by-overwrite)`, () => {
    67	      const { source, names } = setupSource();
    68	      const target = join(tmpRoot, 'tgt');
    69	      runFn(label, { sourceDir: source, targetDir: target });
    70	      const first: Record<string, Buffer> = {};
    71	      for (const n of names) first[n] = readFileSync(join(target, n));
    72	      runFn(label, { sourceDir: source, targetDir: target });
    73	      for (const n of names) {
    74	        expect(readFileSync(join(target, n)).equals(first[n])).toBe(true);
    75	      }
    76	    });
    77	
    78	    it(`${label}: stale skill file in target (not in source) is LEFT IN PLACE`, () => {
    79	      const { source } = setupSource();
    80	      const target = join(tmpRoot, 'tgt');
    81	      mkdirSync(target);
    82	      writeFileSync(join(target, 'stale.md'), 'stale\n');
    83	      runFn(label, { sourceDir: source, targetDir: target });
    84	      expect(readFileSync(join(target, 'stale.md'), 'utf8')).toBe('stale\n');
    85	    });
    86	
    87	    it(`${label}: user-hand-edited target skill file matching a source filename is OVERWRITTEN`, () => {
    88	      const { source } = setupSource();
    89	      const target = join(tmpRoot, 'tgt');
    90	      mkdirSync(target);
    91	      writeFileSync(join(target, 'alpha.md'), 'user-edit\n');
    92	      runFn(label, { sourceDir: source, targetDir: target });
    93	      expect(readFileSync(join(target, 'alpha.md'), 'utf8')).toBe('# alpha.md\n');
    94	    });
    95	  },
    96	);
    97	
    98	describe('populateEchoSkills — symlink guards', () => {
    99	  it('customer profile copies customer and untagged skills but skips dogfood-only skills', () => {
   100	    const source = join(tmpRoot, 'audience-src');
   101	    const target = join(tmpRoot, 'audience-target');
   102	    mkdirSync(source);
   103	    writeFileSync(join(source, 'customer.md'), '---\naudience: customer\n---\n# customer\n');
   104	    writeFileSync(join(source, 'dogfood.md'), '---\naudience: dogfood\n---\n# dogfood\n');
   105	    writeFileSync(join(source, 'untagged.md'), '# untagged\n');
   106	
   107	    const result = populateEchoSkills({
   108	      sourceDir: source,
   109	      targetDir: target,
   110	      profile: 'customer',
   111	    });
   112	
   113	    if (!result.ok) throw new Error('expected ok:true');
   114	    expect(result.copied.sort()).toEqual(['customer.md', 'untagged.md']);
   115	    expect(result.skipped).toContain('dogfood.md');
   116	    expect(existsSync(join(target, 'dogfood.md'))).toBe(false);
   117	  });
   118	
   119	  it('dogfood profile copies all skill audiences', () => {
   120	    const source = join(tmpRoot, 'dogfood-audience-src');
   121	    const target = join(tmpRoot, 'dogfood-audience-target');
   122	    mkdirSync(source);
   123	    writeFileSync(join(source, 'customer.md'), '---\naudience: customer\n---\n# customer\n');
   124	    writeFileSync(join(source, 'dogfood.md'), '---\naudience: dogfood\n---\n# dogfood\n');
   125	
   126	    const result = populateEchoSkills({ sourceDir: source, targetDir: target, profile: 'dogfood' });
   127	
   128	    if (!result.ok) throw new Error('expected ok:true');
   129	    expect(result.copied.sort()).toEqual(['customer.md', 'dogfood.md']);
   130	    expect(result.skipped).toEqual([]);
   131	  });
   132	
   133	  it('symlink in sourceDir is NEVER followed (skipped[])', () => {
   134	    const { source } = setupSource();
   135	    // Add a symlink in source pointing outside the dir.
   136	    const external = join(tmpRoot, 'external.md');
   137	    writeFileSync(external, 'symlinked\n');
   138	    symlinkSync(external, join(source, 'symlinked.md'));
   139	    const target = join(tmpRoot, 'tgt');
   140	    const r = populateEchoSkills({ sourceDir: source, targetDir: target });
   141	    if (!r.ok) throw new Error('expected ok:true');
   142	    expect(r.skipped).toContain('symlinked.md');
   143	    expect(existsSync(join(target, 'symlinked.md'))).toBe(false);
   144	  });
   145	
   146	  it('on unreadable sourceDir → ok:false (no throw)', () => {
   147	    const target = join(tmpRoot, 'tgt');
   148	    const r = populateEchoSkills({
   149	      sourceDir: join(tmpRoot, 'does-not-exist'),
   150	      targetDir: target,
   151	    });
   152	    expect(r.ok).toBe(false);
   153	  });
   154	});
   155	
   156	describe('syncClaudeSkills — symlink target skipped', () => {
   157	  it('target symlink at ~/.claude/commands/<n>.md is skipped, linked file unchanged', () => {
   158	    const { source } = setupSource();
   159	    const target = join(tmpRoot, 'cmds');
   160	    mkdirSync(target);
   161	    // Pre-create a symlink at the target path
   162	    const externalFile = join(tmpRoot, 'untouchable.md');
   163	    writeFileSync(externalFile, 'pristine\n');
   164	    symlinkSync(externalFile, join(target, 'alpha.md'));
   165	    const r = syncClaudeSkills({ sourceDir: source, targetDir: target });
   166	    expect(r.skipped).toContain('alpha.md');
   167	    expect(readFileSync(externalFile, 'utf8')).toBe('pristine\n');
   168	  });
   169	});

 succeeded in 218ms:
 succeeded in 226ms:
 assets/echo-skills/using-echo-coord.md      |   1 +
 assets/echo-skills/using-echo-mcp.md        |   1 +
 src/cli/commands/doctor.ts                  |  10 +-
 src/cli/commands/init.ts                    |  90 +++++++-
 src/cli/io/render.ts                        |   2 +-
 src/echo-home/adapter-sync.ts               |  55 +++--
 src/echo-home/adapters/skill-sync.ts        |  23 +++
 src/echo-home/paths.ts                      |   4 +
 src/echo-home/wizard/run-wizard.ts          |  10 +-
 src/echo-home/wizard/wire.ts                |  13 +-
 tests/cli/doctor.test.ts                    |  26 ++-
 tests/cli/init.test.ts                      | 307 +++++++++++++++++++++++++---
 tests/echo-home/adapter-sync.test.ts        |  26 +++
 tests/echo-home/adapters/skill-sync.test.ts | 118 +++++++----
 tools/foreign-install-smoke.sh              |  51 +++++
 15 files changed, 631 insertions(+), 106 deletions(-)

codex
The tests do cover the key profile-less states and the default customer warning. I’m filling in the middle of `init.test.ts` that was truncated so I can verify CLI-vs-answer-file-vs-recorded precedence directly.
exec
/bin/zsh -lc 'git diff main...agent/install-profile-split -- src/cli/commands/init.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git diff main...agent/install-profile-split -- tools/foreign-install-smoke.sh assets/echo-skills/using-echo-mcp.md assets/echo-skills/using-echo-coord.md' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git diff main...agent/install-profile-split -- src/echo-home/adapter-sync.ts src/echo-home/adapters/skill-sync.ts src/echo-home/paths.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc "nl -ba tests/cli/init.test.ts | sed -n '480,920p'" in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 200ms:
   480	    const code = await runInit({
   481	      stdin: { isTTY: false },
   482	      wizardFactory,
   483	      answerFile,
   484	      home: isolatedHome,
   485	      port: '41234',
   486	      label: 'com.echo.daemon.walkthrough',
   487	      quiet: true,
   488	      now: () => new Date('2026-05-26T00:00:00.000Z'),
   489	      ...daemonAlreadyRunning(),
   490	    });
   491	
   492	    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
   493	      completed: boolean;
   494	      profile: string;
   495	      agents: Array<{ id: AgentKind }>;
   496	    };
   497	    const cursor = JSON.parse(readFileSync(cursorConfig, 'utf8')) as {
   498	      mcpServers: { echo: { url: string } };
   499	    };
   500	    const codexCache = JSON.parse(
   501	      readFileSync(join(isolatedHome, 'adapters/codex.json'), 'utf8'),
   502	    ) as {
   503	      mcpServerConfig: { url: string };
   504	    };
   505	
   506	    expect(code).toBe(0);
   507	    expect(state.completed).toBe(true);
   508	    expect(state.profile).toBe('customer');
   509	    expect(state.agents.map((agent) => agent.id).sort()).toEqual(['codex', 'cursor']);
   510	    expect(readFileSync(codexConfig, 'utf8')).toContain('url = "http://127.0.0.1:41234/mcp"');
   511	    expect(cursor.mcpServers.echo.url).toBe('http://127.0.0.1:41234/mcp');
   512	    expect(codexCache.mcpServerConfig.url).toBe('http://127.0.0.1:41234/mcp');
   513	    expect(existsSync(join(isolatedHome, 'skills', 'using-echo-coord.md'))).toBe(false);
   514	    expect(existsSync(join(isolatedHome, 'skills', 'using-echo-mcp.md'))).toBe(true);
   515	    expect(existsSync(join(isolatedHome, 'skills', 'merge-and-cleanup.md'))).toBe(false);
   516	    expect(existsSync(join(isolatedHome, 'skills', 'process-backlog.md'))).toBe(false);
   517	    expect(existsSync(join(isolatedHome, 'roles', 'builder.toml'))).toBe(false);
   518	    expect(existsSync(join(isolatedHome, 'workflows', 'change-review.toml'))).toBe(false);
   519	    expect(existsSync(join(echoHome, 'state/onboarding.json'))).toBe(false);
   520	  });
   521	
   522	  it('registers claude-code MCP when selected in answer-file mode', async () => {
   523	    const isolatedHome = join(tmpRoot, 'claude-register-home');
   524	    const clientHome = join(tmpRoot, 'claude-register-client');
   525	    const claudeInstructions = join(clientHome, '.claude/CLAUDE.md');
   526	    const claudeCommands = join(clientHome, '.claude/commands');
   527	    mkdirSync(join(clientHome, '.claude'), { recursive: true });
   528	    writeFileSync(claudeInstructions, '# Claude user file\n');
   529	    const answerFile = writeAnswerFile('claude-register-answers.json', {
   530	      confirm_setup: true,
   531	      selected_agents: ['claude-code'],
   532	      profile: 'dogfood',
   533	      default_project_repo_root: null,
   534	    });
   535	    const calls: Array<{ cmd: string; args: string[]; timeoutMs: number }> = [];
   536	    const stdout: string[] = [];
   537	    const { runInit } = await loadInit();
   538	    const wizardFactory = await makeClaudeSyncWizardFactory({
   539	      clientHome,
   540	      claudeInstructions,
   541	      claudeCommands,
   542	      registerSpawn: async (cmd, args, opts) => {
   543	        calls.push({ cmd, args, timeoutMs: opts.timeoutMs });
   544	        return { exitCode: 0, stdout: '', stderr: '', timedOut: false };
   545	      },
   546	    });
   547	
   548	    const code = await runInit({
   549	      stdin: { isTTY: false },
   550	      wizardFactory,
   551	      answerFile,
   552	      home: isolatedHome,
   553	      port: '41234',
   554	      label: 'com.echo.daemon.walkthrough',
   555	      now: () => new Date('2026-05-26T00:00:00.000Z'),
   556	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   557	      ...daemonAlreadyRunning(),
   558	    });
   559	
   560	    expect(code).toBe(0);
   561	    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
   562	      profile: string;
   563	    };
   564	    expect(state.profile).toBe('dogfood');
   565	    expect(calls).toEqual([
   566	      {
   567	        cmd: 'claude',
   568	        args: [
   569	          'mcp',
   570	          'add',
   571	          '--transport',
   572	          'http',
   573	          '--scope',
   574	          'user',
   575	          'echo',
   576	          'http://127.0.0.1:41234/mcp',
   577	        ],
   578	        timeoutMs: 30_000,
   579	      },
   580	    ]);
   581	    expect(stdout.join('')).toContain('mcp-add');
   582	  });
   583	
   584	  it('CLI profile overrides answer-file and recorded profile', async () => {
   585	    const isolatedHome = join(tmpRoot, 'profile-precedence-home');
   586	    mkdirSync(join(isolatedHome, 'state'), { recursive: true });
   587	    writeFileSync(
   588	      join(isolatedHome, 'state/onboarding.json'),
   589	      `${JSON.stringify(
   590	        {
   591	          schema_version: 1,
   592	          created_at: '2026-05-26T00:00:00.000Z',
   593	          last_updated_at: '2026-05-26T00:00:00.000Z',
   594	          completed: true,
   595	          profile: 'dogfood',
   596	          agents: [],
   597	        },
   598	        null,
   599	        2,
   600	      )}\n`,
   601	    );
   602	    const answerFile = writeAnswerFile('profile-precedence-answers.json', {
   603	      confirm_setup: true,
   604	      selected_agents: ['codex'],
   605	      profile: 'dogfood',
   606	      default_project_repo_root: null,
   607	    });
   608	    const seenProfiles: unknown[] = [];
   609	    const wizardFactory = (() =>
   610	      ({
   611	        async detectAgents() {
   612	          return [detected('codex', 'high')];
   613	        },
   614	        async detectProjects() {
   615	          return [];
   616	        },
   617	        async wire(opts) {
   618	          seenProfiles.push(opts.profile);
   619	          return successWire(opts.selectedAgents, isolatedHome);
   620	        },
   621	        async probe() {
   622	          return [{ agent: 'codex', probed: true as const, latencyMs: 1 }];
   623	        },
   624	        async summary() {
   625	          return {
   626	            detected: null,
   627	            projects: null,
   628	            wired: null,
   629	            probed: null,
   630	            onboardingStateSnapshot: null,
   631	          };
   632	        },
   633	        async markCompleted() {},
   634	      }) satisfies Wizard) as never;
   635	    const { runInit } = await loadInit();
   636	
   637	    const code = await runInit({
   638	      stdin: { isTTY: false },
   639	      answerFile,
   640	      profile: 'customer',
   641	      wizardFactory,
   642	      home: isolatedHome,
   643	      quiet: true,
   644	      ...daemonAlreadyRunning(),
   645	    });
   646	
   647	    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
   648	      profile: string;
   649	    };
   650	    expect(code).toBe(0);
   651	    expect(seenProfiles).toEqual(['customer']);
   652	    expect(state.profile).toBe('customer');
   653	  });
   654	
   655	  it('recorded dogfood profile is respected on no-flag rerun', async () => {
   656	    const isolatedHome = join(tmpRoot, 'recorded-dogfood-home');
   657	    mkdirSync(join(isolatedHome, 'state'), { recursive: true });
   658	    writeFileSync(
   659	      join(isolatedHome, 'state/onboarding.json'),
   660	      `${JSON.stringify(
   661	        {
   662	          schema_version: 1,
   663	          created_at: '2026-05-26T00:00:00.000Z',
   664	          last_updated_at: '2026-05-26T00:00:00.000Z',
   665	          completed: true,
   666	          profile: 'dogfood',
   667	          agents: [],
   668	        },
   669	        null,
   670	        2,
   671	      )}\n`,
   672	    );
   673	    const answerFile = writeAnswerFile('recorded-dogfood-answers.json', {
   674	      confirm_setup: true,
   675	      selected_agents: ['codex'],
   676	      default_project_repo_root: null,
   677	    });
   678	    const seenProfiles: unknown[] = [];
   679	    const wizardFactory = (() =>
   680	      ({
   681	        async detectAgents() {
   682	          return [detected('codex', 'high')];
   683	        },
   684	        async detectProjects() {
   685	          return [];
   686	        },
   687	        async wire(opts) {
   688	          seenProfiles.push(opts.profile);
   689	          return successWire(opts.selectedAgents, isolatedHome);
   690	        },
   691	        async probe() {
   692	          return [{ agent: 'codex', probed: true as const, latencyMs: 1 }];
   693	        },
   694	        async summary() {
   695	          return {
   696	            detected: null,
   697	            projects: null,
   698	            wired: null,
   699	            probed: null,
   700	            onboardingStateSnapshot: null,
   701	          };
   702	        },
   703	        async markCompleted() {},
   704	      }) satisfies Wizard) as never;
   705	    const { runInit } = await loadInit();
   706	
   707	    const code = await runInit({
   708	      stdin: { isTTY: false },
   709	      answerFile,
   710	      wizardFactory,
   711	      home: isolatedHome,
   712	      quiet: true,
   713	      ...daemonAlreadyRunning(),
   714	    });
   715	
   716	    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
   717	      profile: string;
   718	    };
   719	    expect(code).toBe(0);
   720	    expect(seenProfiles).toEqual(['dogfood']);
   721	    expect(state.profile).toBe('dogfood');
   722	  });
   723	
   724	  it('surfaces duplicate claude-code MCP registration without masking probe failure', async () => {
   725	    const isolatedHome = join(tmpRoot, 'claude-duplicate-home');
   726	    const clientHome = join(tmpRoot, 'claude-duplicate-client');
   727	    const claudeInstructions = join(clientHome, '.claude/CLAUDE.md');
   728	    const claudeCommands = join(clientHome, '.claude/commands');
   729	    mkdirSync(join(clientHome, '.claude'), { recursive: true });
   730	    writeFileSync(claudeInstructions, '# Claude user file\n');
   731	    const answerFile = writeAnswerFile('claude-duplicate-answers.json', {
   732	      confirm_setup: true,
   733	      selected_agents: ['claude-code'],
   734	      default_project_repo_root: null,
   735	    });
   736	    const stdout: string[] = [];
   737	    const { runInit } = await loadInit();
   738	    const wizardFactory = await makeClaudeSyncWizardFactory({
   739	      clientHome,
   740	      claudeInstructions,
   741	      claudeCommands,
   742	      registerSpawn: async () => ({
   743	        exitCode: 1,
   744	        stdout: '',
   745	        stderr: 'MCP server echo already exists in user config\n',
   746	        timedOut: false,
   747	      }),
   748	      probeSpawn: async () => ({
   749	        exitCode: 1,
   750	        stdout: '',
   751	        stderr: 'No such tool mcp__echo__echo_ping\n',
   752	        timedOut: false,
   753	      }),
   754	    });
   755	
   756	    const code = await runInit({
   757	      stdin: { isTTY: false },
   758	      wizardFactory,
   759	      answerFile,
   760	      home: isolatedHome,
   761	      port: '41234',
   762	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   763	      ...daemonAlreadyRunning(),
   764	    });
   765	
   766	    const text = stdout.join('');
   767	    expect(code).toBe(0);
   768	    expect(text).toContain('already-exists (unverified)');
   769	    expect(text).toContain('claude-code: WARN mcp-not-configured');
   770	    expect(text).toContain(
   771	      'claude mcp add --transport http --scope user echo http://127.0.0.1:41234/mcp',
   772	    );
   773	    expect(text).toContain('claude mcp remove echo -s local');
   774	  });
   775	
   776	  it('continues init when claude-code MCP registration times out', async () => {
   777	    const isolatedHome = join(tmpRoot, 'claude-timeout-home');
   778	    const clientHome = join(tmpRoot, 'claude-timeout-client');
   779	    const claudeInstructions = join(clientHome, '.claude/CLAUDE.md');
   780	    const claudeCommands = join(clientHome, '.claude/commands');
   781	    mkdirSync(join(clientHome, '.claude'), { recursive: true });
   782	    writeFileSync(claudeInstructions, '# Claude user file\n');
   783	    const answerFile = writeAnswerFile('claude-timeout-answers.json', {
   784	      confirm_setup: true,
   785	      selected_agents: ['claude-code'],
   786	      default_project_repo_root: null,
   787	    });
   788	    const stdout: string[] = [];
   789	    const { runInit } = await loadInit();
   790	    const wizardFactory = await makeClaudeSyncWizardFactory({
   791	      clientHome,
   792	      claudeInstructions,
   793	      claudeCommands,
   794	      registerTimeoutMs: 5,
   795	      registerSpawn: async () => ({
   796	        exitCode: -1,
   797	        stdout: '',
   798	        stderr: '',
   799	        timedOut: true,
   800	      }),
   801	    });
   802	
   803	    const code = await runInit({
   804	      stdin: { isTTY: false },
   805	      wizardFactory,
   806	      answerFile,
   807	      home: isolatedHome,
   808	      port: '41234',
   809	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   810	      ...daemonAlreadyRunning(),
   811	    });
   812	
   813	    expect(code).toBe(0);
   814	    expect(stdout.join('')).toContain('timeout');
   815	  });
   816	
   817	  it('continues init when claude is missing and prints remediation through probe', async () => {
   818	    const isolatedHome = join(tmpRoot, 'claude-missing-home');
   819	    const clientHome = join(tmpRoot, 'claude-missing-client');
   820	    const claudeInstructions = join(clientHome, '.claude/CLAUDE.md');
   821	    const claudeCommands = join(clientHome, '.claude/commands');
   822	    mkdirSync(join(clientHome, '.claude'), { recursive: true });
   823	    writeFileSync(claudeInstructions, '# Claude user file\n');
   824	    const answerFile = writeAnswerFile('claude-missing-answers.json', {
   825	      confirm_setup: true,
   826	      selected_agents: ['claude-code'],
   827	      default_project_repo_root: null,
   828	    });
   829	    const enoent = Object.assign(new Error('spawn claude ENOENT'), { code: 'ENOENT' });
   830	    const stdout: string[] = [];
   831	    const { runInit } = await loadInit();
   832	    const wizardFactory = await makeClaudeSyncWizardFactory({
   833	      clientHome,
   834	      claudeInstructions,
   835	      claudeCommands,
   836	      registerSpawn: async () => {
   837	        throw enoent;
   838	      },
   839	      probeSpawn: async () => {
   840	        throw enoent;
   841	      },
   842	    });
   843	
   844	    const code = await runInit({
   845	      stdin: { isTTY: false },
   846	      wizardFactory,
   847	      answerFile,
   848	      home: isolatedHome,
   849	      port: '41234',
   850	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   851	      ...daemonAlreadyRunning(),
   852	    });
   853	
   854	    const text = stdout.join('');
   855	    expect(code).toBe(0);
   856	    expect(text).toContain('cli-unavailable');
   857	    expect(text).toContain('claude-code not found on PATH');
   858	  });
   859	
   860	  it('installs and starts the daemon when init finds no installed launchd job', async () => {
   861	    const isolatedHome = join(tmpRoot, 'bringup-install-home');
   862	    const answerFile = writeAnswerFile('daemon-install-answers.json', {
   863	      confirm_setup: true,
   864	      selected_agents: ['codex'],
   865	      default_project_repo_root: null,
   866	    });
   867	    const daemon = daemonFixture({ launchdRegistered: false });
   868	    const stdout: string[] = [];
   869	    const stderr: string[] = [];
   870	    const { runInit } = await loadInit();
   871	
   872	    const code = await runInit({
   873	      stdin: { isTTY: false },
   874	      answerFile,
   875	      wizardFactory: successfulWizardFactory(isolatedHome) as never,
   876	      home: isolatedHome,
   877	      port: '41234',
   878	      label: 'com.echo.daemon.test-init',
   879	      daemonOptions: daemon.daemonOptions,
   880	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   881	      stderr: { write: (s) => (stderr.push(String(s)), true) },
   882	    });
   883	
   884	    expect(code, stderr.join('')).toBe(0);
   885	    expect(stderr.join('')).toBe('');
   886	    const calls = daemon.calls.map((call) => `${call.command} ${call.args.join(' ')}`);
   887	    const plist = readFileSync(daemon.plistPath, 'utf8');
   888	    expect(stdout.join('')).toContain('Daemon installed and started on port 41234.');
   889	    expect(calls).toContain(`launchctl bootstrap gui/501 ${daemon.plistPath}`);
   890	    expect(calls.some((call) => call.startsWith('plutil -lint'))).toBe(true);
   891	    expect(plist).toContain('<string>com.echo.daemon.test-init</string>');
   892	    expect(plist).toContain(`<string>${isolatedHome}</string>`);
   893	    expect(plist).toContain('<string>41234</string>');
   894	  });
   895	
   896	  it('starts the daemon when the plist exists but launchd is not running it', async () => {
   897	    const answerFile = writeAnswerFile('daemon-start-answers.json', {
   898	      confirm_setup: true,
   899	      selected_agents: ['codex'],
   900	      default_project_repo_root: null,
   901	    });
   902	    const daemon = daemonFixture({ launchdRegistered: false, writePlist: true });
   903	    const stdout: string[] = [];
   904	    const { runInit } = await loadInit();
   905	
   906	    const code = await runInit({
   907	      stdin: { isTTY: false },
   908	      answerFile,
   909	      wizardFactory: successfulWizardFactory() as never,
   910	      daemonOptions: daemon.daemonOptions,
   911	      stdout: { write: (s) => (stdout.push(String(s)), true) },
   912	    });
   913	
   914	    const calls = daemon.calls.map((call) => `${call.command} ${call.args.join(' ')}`);
   915	    expect(code).toBe(0);
   916	    expect(stdout.join('')).toContain('Daemon started on port 39999.');
   917	    expect(calls).toContain(`launchctl bootstrap gui/501 ${daemon.plistPath}`);
   918	    expect(calls.some((call) => call.startsWith('plutil -lint'))).toBe(false);
   919	  });
   920	

 succeeded in 227ms:
diff --git a/assets/echo-skills/using-echo-coord.md b/assets/echo-skills/using-echo-coord.md
index e8d21011..c5d2b67b 100644
--- a/assets/echo-skills/using-echo-coord.md
+++ b/assets/echo-skills/using-echo-coord.md
@@ -2,6 +2,7 @@
 name: using-echo-coord
 description: Use ECHO coord tools to log multi-agent work, track deadlines, inspect the shared coord ledger, and understand the current coord_invoke packaging boundary.
 type: skill
+audience: dogfood
 ---
 
 # Using ECHO Coord
diff --git a/assets/echo-skills/using-echo-mcp.md b/assets/echo-skills/using-echo-mcp.md
index 4dc56fc7..584125a9 100644
--- a/assets/echo-skills/using-echo-mcp.md
+++ b/assets/echo-skills/using-echo-mcp.md
@@ -2,6 +2,7 @@
 name: using-echo-mcp
 description: Use ECHO MCP to recover prior work, decisions, source context, and live cross-tool activity with disciplined trigger, fallback, and interpretation patterns.
 type: skill
+audience: customer
 ---
 
 # Using ECHO MCP
diff --git a/tools/foreign-install-smoke.sh b/tools/foreign-install-smoke.sh
index c4120439..74f59fb1 100755
--- a/tools/foreign-install-smoke.sh
+++ b/tools/foreign-install-smoke.sh
@@ -59,6 +59,20 @@ fail() {
   exit 1
 }
 
+assert_exists() {
+  [ -e "$1" ] || fail "expected path to exist: $1"
+}
+
+assert_absent() {
+  [ ! -e "$1" ] || fail "expected path to be absent: $1"
+}
+
+assert_profile() {
+  local expected="$1"
+  node -e 'const fs=require("fs"); const p=process.argv[1]; const expected=process.argv[2]; const state=JSON.parse(fs.readFileSync(p,"utf8")); if (state.profile !== expected) { console.error(`[assert] expected profile ${expected}, got ${state.profile}`); process.exit(1); }' "$ECHO_HOME/state/onboarding.json" "$expected" \
+    || fail "onboarding profile mismatch"
+}
+
 cd "$REPO"
 echo "=================== STEP 0: build fresh tarball ==================="
 npm pack 2>&1 | tail -2
@@ -114,6 +128,15 @@ echo "[assert] claude-code MCP registration argv OK"
 
 echo ""; echo "=== what skills landed in the user's ~/.echo/skills? ==="
 ls -1 "$ECHO_HOME/skills" 2>&1 || echo "(no skills dir created)"
+assert_profile customer
+assert_exists "$ECHO_HOME/skills/using-echo-mcp.md"
+assert_absent "$ECHO_HOME/skills/using-echo-coord.md"
+assert_absent "$SBOX/fakehome/.claude/commands/using-echo-coord.md"
+assert_absent "$ECHO_HOME/roles/builder.toml"
+assert_absent "$ECHO_HOME/roles/reviewer.toml"
+assert_absent "$ECHO_HOME/roles/strategist.toml"
+assert_absent "$ECHO_HOME/workflows/change-review.toml"
+echo "[assert] default customer surface OK"
 
 echo ""; echo "=== did wire inject an ECHO block into his codex config.toml? ==="
 grep -nE "ECHO|echo|mcp|38478|$PORT" "$SBOX/fakehome/.codex/config.toml" 2>/dev/null | head || echo "(no ECHO content in codex config)"
@@ -122,3 +145,31 @@ grep -nE "BEGIN ECHO|END ECHO|ECHO" "$SBOX/fakehome/.claude/CLAUDE.md" 2>/dev/nu
 
 echo ""; echo "=================== STEP 4: doctor AFTER install ==================="
 "$BIN" doctor 2>&1 | head -50 || true
+
+echo ""; echo "=================== STEP 5: no-flag rerun stays customer ==================="
+"$BIN" init --home "$ECHO_HOME" --port "$PORT" --label "$LABEL" --answer-file "$SBOX/answers.json" > "$SBOX/init-rerun.out" 2>&1
+RERUN_RC=$?
+sed -n '1,40p' "$SBOX/init-rerun.out"
+[ "$RERUN_RC" -eq 0 ] || fail "echoctl init rerun exited $RERUN_RC"
+assert_profile customer
+assert_absent "$ECHO_HOME/skills/using-echo-coord.md"
+assert_absent "$SBOX/fakehome/.claude/commands/using-echo-coord.md"
+echo "[assert] no-flag customer rerun stayed customer"
+
+echo ""; echo "=================== STEP 6: explicit dogfood install gets full coord surface ==================="
+cat > "$SBOX/answers-dogfood.json" <<'JSON'
+{ "confirm_setup": true, "selected_agents": ["codex","claude-code"], "default_project_repo_root": null }
+JSON
+"$BIN" init --home "$ECHO_HOME" --port "$PORT" --profile dogfood --label "$LABEL" --answer-file "$SBOX/answers-dogfood.json" > "$SBOX/init-dogfood.out" 2>&1
+DOGFOOD_RC=$?
+sed -n '1,50p' "$SBOX/init-dogfood.out"
+[ "$DOGFOOD_RC" -eq 0 ] || fail "echoctl init --profile dogfood exited $DOGFOOD_RC"
+assert_profile dogfood
+assert_exists "$ECHO_HOME/skills/using-echo-mcp.md"
+assert_exists "$ECHO_HOME/skills/using-echo-coord.md"
+assert_exists "$SBOX/fakehome/.claude/commands/using-echo-coord.md"
+assert_exists "$ECHO_HOME/roles/builder.toml"
+assert_exists "$ECHO_HOME/roles/reviewer.toml"
+assert_exists "$ECHO_HOME/roles/strategist.toml"
+assert_exists "$ECHO_HOME/workflows/change-review.toml"
+echo "[assert] explicit dogfood surface OK"

 succeeded in 233ms:
diff --git a/src/cli/commands/init.ts b/src/cli/commands/init.ts
index d920354d..3532cc84 100644
--- a/src/cli/commands/init.ts
+++ b/src/cli/commands/init.ts
@@ -1,4 +1,4 @@
-import { mkdirSync, readFileSync } from 'node:fs';
+import { existsSync, mkdirSync, readFileSync } from 'node:fs';
 import { dirname, resolve } from 'node:path';
 import { fileURLToPath } from 'node:url';
 import { parseArgs } from 'node:util';
@@ -7,6 +7,7 @@ import {
   ECHO_HOME_PATHS,
   setEchoHomeRoot,
   validateOnboardingState,
+  type InstallProfile,
   type OnboardedAgentProfile,
   type OnboardingState,
 } from '../../echo-home/paths.js';
@@ -66,6 +67,7 @@ export interface InitOpts {
   color?: boolean;
   home?: string;
   port?: string;
+  profile?: InstallProfile;
   label?: string;
   answerFile?: string;
   force?: boolean;
@@ -88,6 +90,7 @@ export interface InitAnswerFile {
   confirm_setup: boolean;
   selected_agents: AgentKind[];
   default_project_repo_root: string | null;
+  profile?: InstallProfile;
   repo_root?: string;
 }
 
@@ -96,11 +99,12 @@ interface LoadedAnswerFile {
   answers: InitAnswerFile;
 }
 
-export const INIT_HELP = `Usage: echoctl init [--json] [--quiet] [--home <path>] [--port <n>] [--label <id>] [--answer-file <path>] [--force]
+export const INIT_HELP = `Usage: echoctl init [--json] [--quiet] [--home <path>] [--port <n>] [--profile customer|dogfood] [--label <id>] [--answer-file <path>] [--force]
 
 Options:
   --home <path>          ECHO_HOME for this init run
   --port <n>             MCP server port written to adapter configs and launchd
+  --profile <name>       Install profile: customer (default) or dogfood
   --label <id>           launchd label used when ensuring the daemon is running
   --answer-file <path>   non-interactive JSON answers
   --force                Replace existing ECHO marker blocks in adapter configs.
@@ -111,19 +115,20 @@ Answer-file JSON schema:
   {
     "confirm_setup": true,
     "selected_agents": ["codex", "claude-code", "cursor"],
+    "profile": "customer",
     "default_project_repo_root": null,
     "repo_root": "/Users/me/Desktop/Project_echo"
   }
 
 Required fields: confirm_setup, selected_agents, default_project_repo_root.
-Optional field: repo_root, only needed if init cannot locate ECHO's source tree.`;
+Optional fields: profile, repo_root. repo_root is only needed if init cannot locate ECHO's source tree.`;
 
 const ANSWER_REQUIRED_FIELDS = [
   'confirm_setup',
   'selected_agents',
   'default_project_repo_root',
 ] as const;
-const ANSWER_OPTIONAL_FIELDS = ['repo_root'] as const;
+const ANSWER_OPTIONAL_FIELDS = ['profile', 'repo_root'] as const;
 const ANSWER_FIELDS = new Set<string>([...ANSWER_REQUIRED_FIELDS, ...ANSWER_OPTIONAL_FIELDS]);
 
 function isRecord(value: unknown): value is Record<string, unknown> {
@@ -163,10 +168,15 @@ function parseNonEmptyOption(value: string | undefined, flag: string): string |
   return value;
 }
 
-export function parseInitArgs(args: readonly string[]): Pick<
-  InitOpts,
-  'answerFile' | 'force' | 'home' | 'label' | 'port'
-> {
+function parseProfile(value: string | undefined, flag = '--profile'): InstallProfile | undefined {
+  if (value === undefined) return undefined;
+  if (value === 'customer' || value === 'dogfood') return value;
+  throw new Error(`invalid ${flag}: expected customer or dogfood`);
+}
+
+export function parseInitArgs(
+  args: readonly string[],
+): Pick<InitOpts, 'answerFile' | 'force' | 'home' | 'label' | 'port' | 'profile'> {
   const parsed = parseArgs({
     args: [...args],
     strict: true,
@@ -174,6 +184,7 @@ export function parseInitArgs(args: readonly string[]): Pick<
     options: {
       home: { type: 'string' },
       port: { type: 'string' },
+      profile: { type: 'string' },
       label: { type: 'string' },
       'answer-file': { type: 'string' },
       force: { type: 'boolean', default: false },
@@ -184,6 +195,7 @@ export function parseInitArgs(args: readonly string[]): Pick<
   return {
     home: parseNonEmptyOption(parsed.values.home, '--home'),
     port,
+    profile: parseProfile(parseNonEmptyOption(parsed.values.profile, '--profile')),
     label: parseNonEmptyOption(parsed.values.label, '--label'),
     answerFile: parseNonEmptyOption(parsed.values['answer-file'], '--answer-file'),
     force: parsed.values.force === true,
@@ -221,6 +233,10 @@ function validateAnswerFile(filePath: string, value: unknown): InitAnswerFile {
   if (typeof defaultProject === 'string' && defaultProject.trim().length === 0) {
     failAnswerFile(filePath, 'default_project_repo_root', 'expected non-empty string or null');
   }
+  const profile = value['profile'];
+  if (profile !== undefined && profile !== 'customer' && profile !== 'dogfood') {
+    failAnswerFile(filePath, 'profile', 'expected customer or dogfood');
+  }
   const repoRoot = value['repo_root'];
   if (repoRoot !== undefined && (typeof repoRoot !== 'string' || repoRoot.trim().length === 0)) {
     failAnswerFile(filePath, 'repo_root', 'expected non-empty string');
@@ -229,6 +245,7 @@ function validateAnswerFile(filePath: string, value: unknown): InitAnswerFile {
     confirm_setup: value['confirm_setup'],
     selected_agents: selectedAgents,
     default_project_repo_root: defaultProject,
+    ...(profile === undefined ? {} : { profile: profile as InstallProfile }),
     ...(repoRoot === undefined ? {} : { repo_root: repoRoot }),
   };
 }
@@ -327,6 +344,40 @@ function writeOnboardingState(state: OnboardingState): void {
   });
 }
 
+function readRecordedProfile(): {
+  exists: boolean;
+  profile: InstallProfile | null;
+  validShape: boolean;
+} {
+  if (!existsSync(ECHO_HOME_PATHS.stateOnboarding)) {
+    return { exists: false, profile: null, validShape: true };
+  }
+  try {
+    const raw = JSON.parse(readFileSync(ECHO_HOME_PATHS.stateOnboarding, 'utf8')) as unknown;
+    if (!validateOnboardingState(raw)) {
+      return { exists: true, profile: null, validShape: false };
+    }
+    return { exists: true, profile: raw.profile ?? null, validShape: true };
+  } catch {
+    return { exists: true, profile: null, validShape: false };
+  }
+}
+
+function resolveInstallProfile(input: {
+  cliProfile?: InstallProfile;
+  answerProfile?: InstallProfile;
+  recordedProfile: InstallProfile | null;
+}): InstallProfile {
+  return input.cliProfile ?? input.answerProfile ?? input.recordedProfile ?? 'customer';
+}
+
+function persistInstallProfile(profile: InstallProfile, now: Date): void {
+  const state = readOnboardingState();
+  state.profile = profile;
+  state.last_updated_at = now.toISOString();
+  writeOnboardingState(state);
+}
+
 function successfulAgents(result: WireResult, selected: readonly AgentKind[]): AgentKind[] {
   const ok = new Set(
     result.syncResult.agents.filter((agent) => agent.ok).map((agent) => agent.agent),
@@ -380,10 +431,24 @@ export async function runInit(opts: InitOpts = {}): Promise<number> {
   const stderr = opts.stderr ?? process.stderr;
   let answerFile: LoadedAnswerFile | null = null;
   let mcpPort: number;
+  let installProfile: InstallProfile = 'customer';
+  let warnProfileDefault = false;
   try {
     if (opts.home !== undefined) setEchoHomeRoot(opts.home);
     mcpPort = opts.port === undefined ? resolveMcpPort() : parsePort(opts.port);
     if (opts.answerFile !== undefined) answerFile = loadAnswerFile(opts.answerFile);
+    const recorded = readRecordedProfile();
+    installProfile = resolveInstallProfile({
+      cliProfile: opts.profile,
+      answerProfile: answerFile?.answers.profile,
+      recordedProfile: recorded.profile,
+    });
+    warnProfileDefault =
+      opts.profile === undefined &&
+      answerFile?.answers.profile === undefined &&
+      recorded.exists &&
+      recorded.validShape &&
+      recorded.profile === null;
   } catch (err) {
     writeLine(stderr, (err as Error).message);
     return 2;
@@ -397,6 +462,13 @@ export async function runInit(opts: InitOpts = {}): Promise<number> {
     return 2;
   }
   ensureEchoHome();
+  persistInstallProfile(installProfile, opts.now?.() ?? new Date());
+  if (warnProfileDefault) {
+    writeLine(
+      stderr,
+      'echoctl init: onboarding profile was missing; defaulted to `customer`. Re-run `echoctl init --profile dogfood` to restore the full coordination surface.',
+    );
+  }
 
   try {
     const mcpServerUrl = `http://127.0.0.1:${mcpPort}/mcp`;
@@ -449,6 +521,7 @@ export async function runInit(opts: InitOpts = {}): Promise<number> {
     let wire = await wizard.wire({
       selectedAgents,
       defaultProjectRepoRoot,
+      profile: installProfile,
       force: opts.force === true,
     });
     if (wire.syncResult.repoRoot !== undefined) {
@@ -470,6 +543,7 @@ export async function runInit(opts: InitOpts = {}): Promise<number> {
       wire = await wizard.wire({
         selectedAgents,
         defaultProjectRepoRoot,
+        profile: installProfile,
         repoRoot,
         force: opts.force === true,
       });

 succeeded in 224ms:
diff --git a/src/echo-home/adapter-sync.ts b/src/echo-home/adapter-sync.ts
index 99e570c8..346d260e 100644
--- a/src/echo-home/adapter-sync.ts
+++ b/src/echo-home/adapter-sync.ts
@@ -30,6 +30,7 @@ import {
 import { syncDefaultRoles, type RoleSyncResult } from './adapters/role-sync.js';
 import { syncDefaultWorkflows, type WorkflowSyncResult } from './adapters/workflow-sync.js';
 import { AtomicWriteError } from './adapters/atomic-write.js';
+import type { InstallProfile } from './paths.js';
 
 // =============================================================================
 // Public types
@@ -61,6 +62,7 @@ export interface SyncAllOpts {
   workflowsSourceDir?: string;
   defaultWorkflows?: readonly string[];
   allowUserModifiedRoles?: boolean;
+  profile?: InstallProfile;
 }
 
 export type AdapterErrorCode =
@@ -191,6 +193,20 @@ function lockUnavailablePopulateResult(): PopulateEchoSkillsResult {
 
 const DEFAULT_WORKFLOW_FILENAMES = ['change-review.toml'] as const;
 
+function skippedRoles(defaultRoles: readonly string[]): RoleSyncResult {
+  return {
+    results: defaultRoles.map((role) => ({ role, action: 'noop' as const })),
+    rolesErrors: [],
+  };
+}
+
+function skippedWorkflows(defaultWorkflows: readonly string[]): WorkflowSyncResult {
+  return {
+    results: defaultWorkflows.map((workflow) => ({ workflow, action: 'noop' as const })),
+    workflowsErrors: [],
+  };
+}
+
 // -----------------------------------------------------------------------------
 // Repo-root resolution
 // -----------------------------------------------------------------------------
@@ -445,13 +461,18 @@ export async function syncAll(
   profiles: AdapterSyncProfile[],
   opts: SyncAllOpts = {},
 ): Promise<SyncResult> {
+  const installProfile = opts.profile ?? 'dogfood';
   // Step 1: directory-symlink guard.
   const dirChecks: Array<{ path: string; boundary: string }> = [
     { path: ECHO_HOME_PATHS.skills, boundary: ECHO_HOME_PATHS.root },
-    { path: ECHO_HOME_PATHS.roles, boundary: ECHO_HOME_PATHS.root },
-    { path: ECHO_HOME_PATHS.workflows, boundary: ECHO_HOME_PATHS.root },
     { path: ECHO_HOME_PATHS.state, boundary: ECHO_HOME_PATHS.root },
   ];
+  if (installProfile === 'dogfood') {
+    dirChecks.push(
+      { path: ECHO_HOME_PATHS.roles, boundary: ECHO_HOME_PATHS.root },
+      { path: ECHO_HOME_PATHS.workflows, boundary: ECHO_HOME_PATHS.root },
+    );
+  }
   for (const profile of profiles) {
     const commandsDir = profile.paths?.commandsDir ?? defaultCommandsDir(profile.kind);
     if (isString(commandsDir)) {
@@ -536,6 +557,7 @@ export async function syncAll(
     skillsPopulated = populateEchoSkills({
       sourceDir: repoSkillsDir,
       targetDir: ECHO_HOME_PATHS.skills,
+      profile: installProfile,
     });
 
     // Step 5: per-agent dispatch.
@@ -543,19 +565,24 @@ export async function syncAll(
       agents.push(await dispatchAgent(profile, skillsPopulated.ok === true));
     }
 
-    // Step 6: roles once.
-    roles = syncDefaultRoles({
-      sourceDir: rolesSourceDir,
-      targetDir: ECHO_HOME_PATHS.roles,
-      defaults: defaultRoles,
-    });
+    if (installProfile === 'dogfood') {
+      // Step 6: roles once.
+      roles = syncDefaultRoles({
+        sourceDir: rolesSourceDir,
+        targetDir: ECHO_HOME_PATHS.roles,
+        defaults: defaultRoles,
+      });
 
-    // Step 7: workflows once.
-    workflowsResult = syncDefaultWorkflows({
-      sourceDir: workflowsSourceDir,
-      targetDir: ECHO_HOME_PATHS.workflows,
-      defaults: defaultWorkflows,
-    });
+      // Step 7: workflows once.
+      workflowsResult = syncDefaultWorkflows({
+        sourceDir: workflowsSourceDir,
+        targetDir: ECHO_HOME_PATHS.workflows,
+        defaults: defaultWorkflows,
+      });
+    } else {
+      roles = skippedRoles(defaultRoles);
+      workflowsResult = skippedWorkflows(defaultWorkflows);
+    }
   } finally {
     releaseLock(lockHandle);
   }
diff --git a/src/echo-home/adapters/skill-sync.ts b/src/echo-home/adapters/skill-sync.ts
index edc3399c..0944f5a0 100644
--- a/src/echo-home/adapters/skill-sync.ts
+++ b/src/echo-home/adapters/skill-sync.ts
@@ -1,6 +1,7 @@
 import { mkdirSync, readdirSync, readFileSync, lstatSync } from 'node:fs';
 import { join } from 'node:path';
 import { atomicWrite, AtomicWriteError } from './atomic-write.js';
+import type { InstallProfile } from '../paths.js';
 
 export type PopulateEchoSkillsResult =
   | { ok: true; copied: string[]; skipped: string[]; targetDir: string }
@@ -15,6 +16,23 @@ export interface SyncClaudeSkillsResult {
 export interface SkillSyncOpts {
   sourceDir: string;
   targetDir: string;
+  profile?: InstallProfile;
+}
+
+function audienceFor(content: string): InstallProfile {
+  if (!content.startsWith('---\n')) return 'customer';
+  const end = content.indexOf('\n---', 4);
+  if (end === -1) return 'customer';
+  const frontmatter = content.slice(4, end).split(/\r?\n/);
+  for (const line of frontmatter) {
+    const match = /^audience:\s*["']?(customer|dogfood)["']?\s*$/.exec(line.trim());
+    if (match !== null) return match[1] as InstallProfile;
+  }
+  return 'customer';
+}
+
+function includedForProfile(content: string, profile: InstallProfile): boolean {
+  return profile === 'dogfood' || audienceFor(content) === 'customer';
 }
 
 function listMdFiles(dir: string): string[] {
@@ -31,6 +49,7 @@ function listMdFiles(dir: string): string[] {
  */
 export function populateEchoSkills(opts: SkillSyncOpts): PopulateEchoSkillsResult {
   const { sourceDir, targetDir } = opts;
+  const profile = opts.profile ?? 'dogfood';
   const copied: string[] = [];
   const skipped: string[] = [];
 
@@ -76,6 +95,10 @@ export function populateEchoSkills(opts: SkillSyncOpts): PopulateEchoSkillsResul
       skipped.push(name);
       continue;
     }
+    if (!includedForProfile(content, profile)) {
+      skipped.push(name);
+      continue;
+    }
     const tgtPath = join(targetDir, name);
     try {
       atomicWrite({ filePath: tgtPath, content });
diff --git a/src/echo-home/paths.ts b/src/echo-home/paths.ts
index d87bc783..c95be85f 100644
--- a/src/echo-home/paths.ts
+++ b/src/echo-home/paths.ts
@@ -52,11 +52,14 @@ export function setEchoHomeRoot(homeOverride: string): EchoHomePaths {
   return ECHO_HOME_PATHS;
 }
 
+export type InstallProfile = 'customer' | 'dogfood';
+
 export interface OnboardingState {
   schema_version: 1;
   created_at: string;
   last_updated_at: string;
   completed: boolean;
+  profile?: InstallProfile;
   agents: OnboardedAgentProfile[];
 }
 
@@ -91,6 +94,7 @@ const onboardingStateSchema: AnySchema = {
     created_at: { type: 'string' },
     last_updated_at: { type: 'string' },
     completed: { type: 'boolean' },
+    profile: { enum: ['customer', 'dogfood'] },
     agents: {
       type: 'array',
       items: {

exec
/bin/zsh -lc 'git merge-base main agent/install-profile-split' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git log --oneline --decorate --max-count=8 --all -- src/cli/commands/init.ts src/echo-home/adapter-sync.ts tools/foreign-install-smoke.sh' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git diff --name-only main...agent/install-profile-split' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'nl -ba src/echo-home/scaffold.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 195ms:
     1	import { existsSync, mkdirSync, writeFileSync } from 'node:fs';
     2	import { ECHO_HOME_PATHS, type OnboardingState, type ProjectsState } from './paths.js';
     3	
     4	export interface EnsureEchoHomeResult {
     5	  root: string;
     6	  created_dirs: string[];
     7	  created_files: string[];
     8	}
     9	
    10	function ensureDir(path: string, createdDirs: string[]): void {
    11	  const existed = existsSync(path);
    12	  mkdirSync(path, { recursive: true });
    13	  if (!existed) createdDirs.push(path);
    14	}
    15	
    16	function isErrnoException(err: unknown): err is NodeJS.ErrnoException {
    17	  return err instanceof Error && 'code' in err;
    18	}
    19	
    20	function writeAbsentJson(path: string, value: unknown, createdFiles: string[]): void {
    21	  try {
    22	    writeFileSync(path, `${JSON.stringify(value, null, 2)}\n`, { flag: 'wx' });
    23	    createdFiles.push(path);
    24	  } catch (err) {
    25	    if (isErrnoException(err) && err.code === 'EEXIST') return;
    26	    throw err;
    27	  }
    28	}
    29	
    30	function initialOnboardingState(now: string): OnboardingState {
    31	  return {
    32	    schema_version: 1,
    33	    created_at: now,
    34	    last_updated_at: now,
    35	    completed: false,
    36	    agents: [],
    37	  };
    38	}
    39	
    40	function initialProjectsState(now: string): ProjectsState {
    41	  return {
    42	    schema_version: 1,
    43	    last_refreshed_at: now,
    44	    default_project: null,
    45	    projects: [],
    46	  };
    47	}
    48	
    49	export function ensureEchoHome(): EnsureEchoHomeResult {
    50	  const createdDirs: string[] = [];
    51	  const createdFiles: string[] = [];
    52	
    53	  ensureDir(ECHO_HOME_PATHS.root, createdDirs);
    54	  ensureDir(ECHO_HOME_PATHS.skills, createdDirs);
    55	  ensureDir(ECHO_HOME_PATHS.roles, createdDirs);
    56	  ensureDir(ECHO_HOME_PATHS.workflows, createdDirs);
    57	  ensureDir(ECHO_HOME_PATHS.adapters, createdDirs);
    58	  ensureDir(ECHO_HOME_PATHS.state, createdDirs);
    59	
    60	  const now = new Date().toISOString();
    61	  writeAbsentJson(ECHO_HOME_PATHS.stateOnboarding, initialOnboardingState(now), createdFiles);
    62	  writeAbsentJson(ECHO_HOME_PATHS.stateProjects, initialProjectsState(now), createdFiles);
    63	
    64	  return {
    65	    root: ECHO_HOME_PATHS.root,
    66	    created_dirs: createdDirs,
    67	    created_files: createdFiles,
    68	  };
    69	}

 succeeded in 221ms:
002e5f3131dc633741de069f784a0cc23ee72e2d

 succeeded in 210ms:
assets/echo-skills/using-echo-coord.md
assets/echo-skills/using-echo-mcp.md
src/cli/commands/doctor.ts
src/cli/commands/init.ts
src/cli/io/render.ts
src/echo-home/adapter-sync.ts
src/echo-home/adapters/skill-sync.ts
src/echo-home/paths.ts
src/echo-home/wizard/run-wizard.ts
src/echo-home/wizard/wire.ts
tests/cli/doctor.test.ts
tests/cli/init.test.ts
tests/echo-home/adapter-sync.test.ts
tests/echo-home/adapters/skill-sync.test.ts
tools/foreign-install-smoke.sh

 succeeded in 272ms:
f144bb4c (HEAD -> agent/install-profile-split, origin/agent/install-profile-split) 2026-06-02-084-install-profile-split: implement install profile split
2d64e40e 2026-06-01-083: register claude-code MCP during init
ce1e0270 office-hours: fold n=1 sandbox pre-flight + Tuesday runbook + foreign-install smoke
36b9287a feat: echoctl init installs+starts the daemon if missing (no more wired-to-non-running-MCP)
8bf323b1 fix: ship using-echo-mcp skill to customers; stop shipping dev-process skills
a66718d5 fix: doctor isolation flags + init --force (post-077 walkthrough)
9070af7c fix: init isolation flags + non-TTY answer-file mode (post-076 walkthrough)
10657514 fix(wizard): raise probe timeout 5s → 30s (#4)

exec
/bin/zsh -lc 'git merge-tree $(git merge-base main HEAD) main HEAD' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git diff --name-status 002e5f3131dc633741de069f784a0cc23ee72e2d..main -- assets/echo-skills/using-echo-coord.md assets/echo-skills/using-echo-mcp.md src/cli/commands/doctor.ts src/cli/commands/init.ts src/cli/io/render.ts src/echo-home/adapter-sync.ts src/echo-home/adapters/skill-sync.ts src/echo-home/paths.ts src/echo-home/wizard/run-wizard.ts src/echo-home/wizard/wire.ts tests/cli/doctor.test.ts tests/cli/init.test.ts tests/echo-home/adapter-sync.test.ts tests/echo-home/adapters/skill-sync.test.ts tools/foreign-install-smoke.sh' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git rev-parse main' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git rev-parse origin/main' in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 198ms:
 succeeded in 196ms:
efa43c2c270e9d898d0936b059040390c06ed688

 succeeded in 200ms:
efa43c2c270e9d898d0936b059040390c06ed688

 succeeded in 251ms:
merged
  result 100644 c5d2b67ba214b15a811478670070f3ac2502eb02 assets/echo-skills/using-echo-coord.md
  our    100644 e8d21011abd8e41445254dff2395cf190d2df774 assets/echo-skills/using-echo-coord.md
@@ -2,6 +2,7 @@
 name: using-echo-coord
 description: Use ECHO coord tools to log multi-agent work, track deadlines, inspect the shared coord ledger, and understand the current coord_invoke packaging boundary.
 type: skill
+audience: dogfood
 ---
 
 # Using ECHO Coord
merged
  result 100644 584125a9d4c1dd051cb1da55f7e497dbf29a88ff assets/echo-skills/using-echo-mcp.md
  our    100644 4dc56fc74d7e8b46fdb1599bf4df78c1c9b35e74 assets/echo-skills/using-echo-mcp.md
@@ -2,6 +2,7 @@
 name: using-echo-mcp
 description: Use ECHO MCP to recover prior work, decisions, source context, and live cross-tool activity with disciplined trigger, fallback, and interpretation patterns.
 type: skill
+audience: customer
 ---
 
 # Using ECHO MCP
merged
  result 100644 b431c60bbb993a2ecddadefbc63546eed2e45510 src/cli/commands/doctor.ts
  our    100644 8a47fb239285407e4664dbbdbdd4ca1af1ae2d48 src/cli/commands/doctor.ts
@@ -7,6 +7,7 @@
   setEchoHomeRoot,
   validateOnboardingState,
   validateProjectsState,
+  type InstallProfile,
   type OnboardedAgentProfile,
   type OnboardingState,
 } from '../../echo-home/paths.js';
@@ -29,6 +30,7 @@
     onboardingValid: boolean;
     projectsValid: boolean;
     schemaVersion: 1 | 'mismatch' | 'missing';
+    profile: InstallProfile | 'unknown';
   };
   syncLock: { present: boolean; path: string; mtimeIso?: string; cleanupCommand?: string };
   agents: {
@@ -78,7 +80,9 @@
   return value;
 }
 
-export function parseDoctorArgs(args: readonly string[]): Pick<DoctorOpts, 'home' | 'label' | 'port'> {
+export function parseDoctorArgs(
+  args: readonly string[],
+): Pick<DoctorOpts, 'home' | 'label' | 'port'> {
   const parsed = parseArgs({
     args: [...args],
     strict: true,
@@ -117,6 +121,7 @@
   onboardingValid: boolean;
   projectsValid: boolean;
   schemaVersion: 1 | 'mismatch' | 'missing';
+  profile: InstallProfile | 'unknown';
   onboarding: OnboardingState | null;
 } {
   let onboardingValid = false;
@@ -140,6 +145,7 @@
     onboardingValid,
     projectsValid,
     schemaVersion: missing ? 'missing' : onboardingValid && projectsValid ? 1 : 'mismatch',
+    profile: onboardingValid ? (onboarding?.profile ?? 'customer') : 'unknown',
     onboarding,
   };
 }
@@ -206,6 +212,7 @@
         onboardingValid: false,
         projectsValid: false,
         schemaVersion: 'missing' as const,
+        profile: 'unknown' as const,
         onboarding: null,
       };
 
@@ -244,6 +251,7 @@
       onboardingValid: state.onboardingValid,
       projectsValid: state.projectsValid,
       schemaVersion: state.schemaVersion,
+      profile: state.profile,
     },
     syncLock,
     agents,
merged
  result 100644 3532cc8447c360cf308b6724935e8a040a0cf418 src/cli/commands/init.ts
  our    100644 d920354dfb39c8a8288172339b2221518c776869 src/cli/commands/init.ts
@@ -1,4 +1,4 @@
-import { mkdirSync, readFileSync } from 'node:fs';
+import { existsSync, mkdirSync, readFileSync } from 'node:fs';
 import { dirname, resolve } from 'node:path';
 import { fileURLToPath } from 'node:url';
 import { parseArgs } from 'node:util';
@@ -7,6 +7,7 @@
   ECHO_HOME_PATHS,
   setEchoHomeRoot,
   validateOnboardingState,
+  type InstallProfile,
   type OnboardedAgentProfile,
   type OnboardingState,
 } from '../../echo-home/paths.js';
@@ -66,6 +67,7 @@
   color?: boolean;
   home?: string;
   port?: string;
+  profile?: InstallProfile;
   label?: string;
   answerFile?: string;
   force?: boolean;
@@ -88,6 +90,7 @@
   confirm_setup: boolean;
   selected_agents: AgentKind[];
   default_project_repo_root: string | null;
+  profile?: InstallProfile;
   repo_root?: string;
 }
 
@@ -96,11 +99,12 @@
   answers: InitAnswerFile;
 }
 
-export const INIT_HELP = `Usage: echoctl init [--json] [--quiet] [--home <path>] [--port <n>] [--label <id>] [--answer-file <path>] [--force]
+export const INIT_HELP = `Usage: echoctl init [--json] [--quiet] [--home <path>] [--port <n>] [--profile customer|dogfood] [--label <id>] [--answer-file <path>] [--force]
 
 Options:
   --home <path>          ECHO_HOME for this init run
   --port <n>             MCP server port written to adapter configs and launchd
+  --profile <name>       Install profile: customer (default) or dogfood
   --label <id>           launchd label used when ensuring the daemon is running
   --answer-file <path>   non-interactive JSON answers
   --force                Replace existing ECHO marker blocks in adapter configs.
@@ -111,19 +115,20 @@
   {
     "confirm_setup": true,
     "selected_agents": ["codex", "claude-code", "cursor"],
+    "profile": "customer",
     "default_project_repo_root": null,
     "repo_root": "/Users/me/Desktop/Project_echo"
   }
 
 Required fields: confirm_setup, selected_agents, default_project_repo_root.
-Optional field: repo_root, only needed if init cannot locate ECHO's source tree.`;
+Optional fields: profile, repo_root. repo_root is only needed if init cannot locate ECHO's source tree.`;
 
 const ANSWER_REQUIRED_FIELDS = [
   'confirm_setup',
   'selected_agents',
   'default_project_repo_root',
 ] as const;
-const ANSWER_OPTIONAL_FIELDS = ['repo_root'] as const;
+const ANSWER_OPTIONAL_FIELDS = ['profile', 'repo_root'] as const;
 const ANSWER_FIELDS = new Set<string>([...ANSWER_REQUIRED_FIELDS, ...ANSWER_OPTIONAL_FIELDS]);
 
 function isRecord(value: unknown): value is Record<string, unknown> {
@@ -163,10 +168,15 @@
   return value;
 }
 
-export function parseInitArgs(args: readonly string[]): Pick<
-  InitOpts,
-  'answerFile' | 'force' | 'home' | 'label' | 'port'
-> {
+function parseProfile(value: string | undefined, flag = '--profile'): InstallProfile | undefined {
+  if (value === undefined) return undefined;
+  if (value === 'customer' || value === 'dogfood') return value;
+  throw new Error(`invalid ${flag}: expected customer or dogfood`);
+}
+
+export function parseInitArgs(
+  args: readonly string[],
+): Pick<InitOpts, 'answerFile' | 'force' | 'home' | 'label' | 'port' | 'profile'> {
   const parsed = parseArgs({
     args: [...args],
     strict: true,
@@ -174,6 +184,7 @@
     options: {
       home: { type: 'string' },
       port: { type: 'string' },
+      profile: { type: 'string' },
       label: { type: 'string' },
       'answer-file': { type: 'string' },
       force: { type: 'boolean', default: false },
@@ -184,6 +195,7 @@
   return {
     home: parseNonEmptyOption(parsed.values.home, '--home'),
     port,
+    profile: parseProfile(parseNonEmptyOption(parsed.values.profile, '--profile')),
     label: parseNonEmptyOption(parsed.values.label, '--label'),
     answerFile: parseNonEmptyOption(parsed.values['answer-file'], '--answer-file'),
     force: parsed.values.force === true,
@@ -221,6 +233,10 @@
   if (typeof defaultProject === 'string' && defaultProject.trim().length === 0) {
     failAnswerFile(filePath, 'default_project_repo_root', 'expected non-empty string or null');
   }
+  const profile = value['profile'];
+  if (profile !== undefined && profile !== 'customer' && profile !== 'dogfood') {
+    failAnswerFile(filePath, 'profile', 'expected customer or dogfood');
+  }
   const repoRoot = value['repo_root'];
   if (repoRoot !== undefined && (typeof repoRoot !== 'string' || repoRoot.trim().length === 0)) {
     failAnswerFile(filePath, 'repo_root', 'expected non-empty string');
@@ -229,6 +245,7 @@
     confirm_setup: value['confirm_setup'],
     selected_agents: selectedAgents,
     default_project_repo_root: defaultProject,
+    ...(profile === undefined ? {} : { profile: profile as InstallProfile }),
     ...(repoRoot === undefined ? {} : { repo_root: repoRoot }),
   };
 }
@@ -327,6 +344,40 @@
   });
 }
 
+function readRecordedProfile(): {
+  exists: boolean;
+  profile: InstallProfile | null;
+  validShape: boolean;
+} {
+  if (!existsSync(ECHO_HOME_PATHS.stateOnboarding)) {
+    return { exists: false, profile: null, validShape: true };
+  }
+  try {
+    const raw = JSON.parse(readFileSync(ECHO_HOME_PATHS.stateOnboarding, 'utf8')) as unknown;
+    if (!validateOnboardingState(raw)) {
+      return { exists: true, profile: null, validShape: false };
+    }
+    return { exists: true, profile: raw.profile ?? null, validShape: true };
+  } catch {
+    return { exists: true, profile: null, validShape: false };
+  }
+}
+
+function resolveInstallProfile(input: {
+  cliProfile?: InstallProfile;
+  answerProfile?: InstallProfile;
+  recordedProfile: InstallProfile | null;
+}): InstallProfile {
+  return input.cliProfile ?? input.answerProfile ?? input.recordedProfile ?? 'customer';
+}
+
+function persistInstallProfile(profile: InstallProfile, now: Date): void {
+  const state = readOnboardingState();
+  state.profile = profile;
+  state.last_updated_at = now.toISOString();
+  writeOnboardingState(state);
+}
+
 function successfulAgents(result: WireResult, selected: readonly AgentKind[]): AgentKind[] {
   const ok = new Set(
     result.syncResult.agents.filter((agent) => agent.ok).map((agent) => agent.agent),
@@ -380,10 +431,24 @@
   const stderr = opts.stderr ?? process.stderr;
   let answerFile: LoadedAnswerFile | null = null;
   let mcpPort: number;
+  let installProfile: InstallProfile = 'customer';
+  let warnProfileDefault = false;
   try {
     if (opts.home !== undefined) setEchoHomeRoot(opts.home);
     mcpPort = opts.port === undefined ? resolveMcpPort() : parsePort(opts.port);
     if (opts.answerFile !== undefined) answerFile = loadAnswerFile(opts.answerFile);
+    const recorded = readRecordedProfile();
+    installProfile = resolveInstallProfile({
+      cliProfile: opts.profile,
+      answerProfile: answerFile?.answers.profile,
+      recordedProfile: recorded.profile,
+    });
+    warnProfileDefault =
+      opts.profile === undefined &&
+      answerFile?.answers.profile === undefined &&
+      recorded.exists &&
+      recorded.validShape &&
+      recorded.profile === null;
   } catch (err) {
     writeLine(stderr, (err as Error).message);
     return 2;
@@ -397,6 +462,13 @@
     return 2;
   }
   ensureEchoHome();
+  persistInstallProfile(installProfile, opts.now?.() ?? new Date());
+  if (warnProfileDefault) {
+    writeLine(
+      stderr,
+      'echoctl init: onboarding profile was missing; defaulted to `customer`. Re-run `echoctl init --profile dogfood` to restore the full coordination surface.',
+    );
+  }
 
   try {
     const mcpServerUrl = `http://127.0.0.1:${mcpPort}/mcp`;
@@ -449,6 +521,7 @@
     let wire = await wizard.wire({
       selectedAgents,
       defaultProjectRepoRoot,
+      profile: installProfile,
       force: opts.force === true,
     });
     if (wire.syncResult.repoRoot !== undefined) {
@@ -470,6 +543,7 @@
       wire = await wizard.wire({
         selectedAgents,
         defaultProjectRepoRoot,
+        profile: installProfile,
         repoRoot,
         force: opts.force === true,
       });
merged
  result 100644 bc64df7d9edbeae4a3ed8b522bfb56dbc697bffc src/cli/io/render.ts
  our    100644 87af91043254a3fa19b0b2a39f20e009dd284c42 src/cli/io/render.ts
@@ -82,7 +82,7 @@
     `daemon: reachable=${report.daemon.mcpReachable ? 'yes' : 'no'} pid-lock=${report.daemon.pidLockHeld ? 'yes' : 'no'} port=${report.daemon.port}`,
   );
   lines.push(
-    `echo-home: exists=${report.echoHome.exists ? 'yes' : 'no'} onboarding=${report.echoHome.onboardingValid ? 'valid' : 'invalid'} projects=${report.echoHome.projectsValid ? 'valid' : 'invalid'} schema=${report.echoHome.schemaVersion}`,
+    `echo-home: exists=${report.echoHome.exists ? 'yes' : 'no'} onboarding=${report.echoHome.onboardingValid ? 'valid' : 'invalid'} projects=${report.echoHome.projectsValid ? 'valid' : 'invalid'} schema=${report.echoHome.schemaVersion} profile=${report.echoHome.profile}`,
   );
   lines.push(`sync-lock: ${report.syncLock.present ? report.syncLock.cleanupCommand : 'absent'}`);
   for (const agent of report.agents) {
merged
  result 100644 346d260ea65c162beafcae24200acb89cd247951 src/echo-home/adapter-sync.ts
  our    100644 99e570c8141f3a6e686a98f66f9aea7925609ea0 src/echo-home/adapter-sync.ts
@@ -30,6 +30,7 @@
 import { syncDefaultRoles, type RoleSyncResult } from './adapters/role-sync.js';
 import { syncDefaultWorkflows, type WorkflowSyncResult } from './adapters/workflow-sync.js';
 import { AtomicWriteError } from './adapters/atomic-write.js';
+import type { InstallProfile } from './paths.js';
 
 // =============================================================================
 // Public types
@@ -61,6 +62,7 @@
   workflowsSourceDir?: string;
   defaultWorkflows?: readonly string[];
   allowUserModifiedRoles?: boolean;
+  profile?: InstallProfile;
 }
 
 export type AdapterErrorCode =
@@ -191,6 +193,20 @@
 
 const DEFAULT_WORKFLOW_FILENAMES = ['change-review.toml'] as const;
 
+function skippedRoles(defaultRoles: readonly string[]): RoleSyncResult {
+  return {
+    results: defaultRoles.map((role) => ({ role, action: 'noop' as const })),
+    rolesErrors: [],
+  };
+}
+
+function skippedWorkflows(defaultWorkflows: readonly string[]): WorkflowSyncResult {
+  return {
+    results: defaultWorkflows.map((workflow) => ({ workflow, action: 'noop' as const })),
+    workflowsErrors: [],
+  };
+}
+
 // -----------------------------------------------------------------------------
 // Repo-root resolution
 // -----------------------------------------------------------------------------
@@ -445,13 +461,18 @@
   profiles: AdapterSyncProfile[],
   opts: SyncAllOpts = {},
 ): Promise<SyncResult> {
+  const installProfile = opts.profile ?? 'dogfood';
   // Step 1: directory-symlink guard.
   const dirChecks: Array<{ path: string; boundary: string }> = [
     { path: ECHO_HOME_PATHS.skills, boundary: ECHO_HOME_PATHS.root },
-    { path: ECHO_HOME_PATHS.roles, boundary: ECHO_HOME_PATHS.root },
-    { path: ECHO_HOME_PATHS.workflows, boundary: ECHO_HOME_PATHS.root },
     { path: ECHO_HOME_PATHS.state, boundary: ECHO_HOME_PATHS.root },
   ];
+  if (installProfile === 'dogfood') {
+    dirChecks.push(
+      { path: ECHO_HOME_PATHS.roles, boundary: ECHO_HOME_PATHS.root },
+      { path: ECHO_HOME_PATHS.workflows, boundary: ECHO_HOME_PATHS.root },
+    );
+  }
   for (const profile of profiles) {
     const commandsDir = profile.paths?.commandsDir ?? defaultCommandsDir(profile.kind);
     if (isString(commandsDir)) {
@@ -536,6 +557,7 @@
     skillsPopulated = populateEchoSkills({
       sourceDir: repoSkillsDir,
       targetDir: ECHO_HOME_PATHS.skills,
+      profile: installProfile,
     });
 
     // Step 5: per-agent dispatch.
@@ -543,19 +565,24 @@
       agents.push(await dispatchAgent(profile, skillsPopulated.ok === true));
     }
 
-    // Step 6: roles once.
-    roles = syncDefaultRoles({
-      sourceDir: rolesSourceDir,
-      targetDir: ECHO_HOME_PATHS.roles,
-      defaults: defaultRoles,
-    });
+    if (installProfile === 'dogfood') {
+      // Step 6: roles once.
+      roles = syncDefaultRoles({
+        sourceDir: rolesSourceDir,
+        targetDir: ECHO_HOME_PATHS.roles,
+        defaults: defaultRoles,
+      });
 
-    // Step 7: workflows once.
-    workflowsResult = syncDefaultWorkflows({
-      sourceDir: workflowsSourceDir,
-      targetDir: ECHO_HOME_PATHS.workflows,
-      defaults: defaultWorkflows,
-    });
+      // Step 7: workflows once.
+      workflowsResult = syncDefaultWorkflows({
+        sourceDir: workflowsSourceDir,
+        targetDir: ECHO_HOME_PATHS.workflows,
+        defaults: defaultWorkflows,
+      });
+    } else {
+      roles = skippedRoles(defaultRoles);
+      workflowsResult = skippedWorkflows(defaultWorkflows);
+    }
   } finally {
     releaseLock(lockHandle);
   }
merged
  result 100644 0944f5a0bce8b0e007e2713424b7c86d8ffa44d5 src/echo-home/adapters/skill-sync.ts
  our    100644 edc3399cd39c0c1d33c422a79c101124476b5e47 src/echo-home/adapters/skill-sync.ts
@@ -1,6 +1,7 @@
 import { mkdirSync, readdirSync, readFileSync, lstatSync } from 'node:fs';
 import { join } from 'node:path';
 import { atomicWrite, AtomicWriteError } from './atomic-write.js';
+import type { InstallProfile } from '../paths.js';
 
 export type PopulateEchoSkillsResult =
   | { ok: true; copied: string[]; skipped: string[]; targetDir: string }
@@ -15,6 +16,23 @@
 export interface SkillSyncOpts {
   sourceDir: string;
   targetDir: string;
+  profile?: InstallProfile;
+}
+
+function audienceFor(content: string): InstallProfile {
+  if (!content.startsWith('---\n')) return 'customer';
+  const end = content.indexOf('\n---', 4);
+  if (end === -1) return 'customer';
+  const frontmatter = content.slice(4, end).split(/\r?\n/);
+  for (const line of frontmatter) {
+    const match = /^audience:\s*["']?(customer|dogfood)["']?\s*$/.exec(line.trim());
+    if (match !== null) return match[1] as InstallProfile;
+  }
+  return 'customer';
+}
+
+function includedForProfile(content: string, profile: InstallProfile): boolean {
+  return profile === 'dogfood' || audienceFor(content) === 'customer';
 }
 
 function listMdFiles(dir: string): string[] {
@@ -31,6 +49,7 @@
  */
 export function populateEchoSkills(opts: SkillSyncOpts): PopulateEchoSkillsResult {
   const { sourceDir, targetDir } = opts;
+  const profile = opts.profile ?? 'dogfood';
   const copied: string[] = [];
   const skipped: string[] = [];
 
@@ -76,6 +95,10 @@
       skipped.push(name);
       continue;
     }
+    if (!includedForProfile(content, profile)) {
+      skipped.push(name);
+      continue;
+    }
     const tgtPath = join(targetDir, name);
     try {
       atomicWrite({ filePath: tgtPath, content });
merged
  result 100644 c95be85f7d8f2a856f10b49b78798f40b9844043 src/echo-home/paths.ts
  our    100644 d87bc783ff4607738e762e3dacb796d6c3ec6392 src/echo-home/paths.ts
@@ -52,11 +52,14 @@
   return ECHO_HOME_PATHS;
 }
 
+export type InstallProfile = 'customer' | 'dogfood';
+
 export interface OnboardingState {
   schema_version: 1;
   created_at: string;
   last_updated_at: string;
   completed: boolean;
+  profile?: InstallProfile;
   agents: OnboardedAgentProfile[];
 }
 
@@ -91,6 +94,7 @@
     created_at: { type: 'string' },
     last_updated_at: { type: 'string' },
     completed: { type: 'boolean' },
+    profile: { enum: ['customer', 'dogfood'] },
     agents: {
       type: 'array',
       items: {
merged
  result 100644 a426f75abec4c6a2a38779e59e533bde9b597590 src/echo-home/wizard/run-wizard.ts
  our    100644 aae9355e966d486790192a71eaa4fa912a517611 src/echo-home/wizard/run-wizard.ts
@@ -38,7 +38,10 @@
   detectAgents(): Promise<DetectedAgent[]>;
   detectProjects(): Promise<DetectedProject[]>;
   wire(
-    opts: Pick<WireOpts, 'selectedAgents' | 'defaultProjectRepoRoot' | 'repoRoot' | 'force'>,
+    opts: Pick<
+      WireOpts,
+      'selectedAgents' | 'defaultProjectRepoRoot' | 'profile' | 'repoRoot' | 'force'
+    >,
   ): Promise<WireResult>;
   probe(agents: AgentKind[]): Promise<ProbeOutcome[]>;
   summary(): Promise<WizardSummary>;
@@ -64,7 +67,10 @@
     },
 
     async wire(
-      wireOpts: Pick<WireOpts, 'selectedAgents' | 'defaultProjectRepoRoot' | 'repoRoot' | 'force'>,
+      wireOpts: Pick<
+        WireOpts,
+        'selectedAgents' | 'defaultProjectRepoRoot' | 'profile' | 'repoRoot' | 'force'
+      >,
     ): Promise<WireResult> {
       wired = await wireAgents({
         ...(opts.wireDepsOverride ?? {}),
merged
  result 100644 f92831c7fb121c8d43f84522481f335efd721da4 src/echo-home/wizard/wire.ts
  our    100644 b5d0c62e941f6f0c329619e55bfa9c5c8d8bfdd6 src/echo-home/wizard/wire.ts
@@ -9,7 +9,12 @@
 } from '../adapter-sync.js';
 import type { ClaudeCodeMcpRegisterDeps } from '../adapters/claude-code-mcp.js';
 import { atomicWrite } from '../adapters/atomic-write.js';
-import { ECHO_HOME_PATHS, validateOnboardingState, type OnboardingState } from '../paths.js';
+import {
+  ECHO_HOME_PATHS,
+  validateOnboardingState,
+  type InstallProfile,
+  type OnboardingState,
+} from '../paths.js';
 import { readAdapterCache, writeAdapterCache, type AdapterCacheRecord } from './adapter-cache.js';
 import type { AgentKind } from './detect-agents.js';
 import { renderEchoSection } from './render-echo-section.js';
@@ -19,6 +24,7 @@
   defaultProjectRepoRoot: string | null;
   mcpServerUrl: string;
   echoVersion: string;
+  profile?: InstallProfile;
   repoRoot?: string;
   force?: boolean;
   syncAll?: typeof realSyncAll;
@@ -225,12 +231,15 @@
   const cache = opts.cache ?? { read: readAdapterCache, write: writeAdapterCache };
   const { profiles, rendered } = buildProfiles(opts, nowIso, cache);
   const runSyncAll = opts.syncAll ?? realSyncAll;
+  const installProfile = opts.profile ?? 'dogfood';
 
   let syncResult: SyncResult;
   try {
     syncResult = await runSyncAll(
       profiles,
-      opts.repoRoot === undefined ? undefined : { repoRoot: opts.repoRoot },
+      opts.repoRoot === undefined
+        ? { profile: installProfile }
+        : { repoRoot: opts.repoRoot, profile: installProfile },
     );
   } catch (err) {
     return {
merged
  result 100644 369701ed64ac4d1d13c5bdc2ca3391bda002f0ad tests/cli/doctor.test.ts
  our    100644 fef529b9b8888cc718808ec93af87e551e4f262a tests/cli/doctor.test.ts
@@ -28,6 +28,7 @@
         created_at: now,
         last_updated_at: now,
         completed: true,
+        profile: 'customer',
         agents: [
           {
             id:
@@ -102,9 +103,13 @@
       probeAgents: async () => [{ agent: 'codex', probed: true, latencyMs: 1 }],
     });
 
-    const report = JSON.parse(out.join('')) as { overall: string };
+    const report = JSON.parse(out.join('')) as {
+      echoHome: { profile: string };
+      overall: string;
+    };
     expect(code).toBe(0);
     expect(report.overall).toBe('healthy');
+    expect(report.echoHome.profile).toBe('customer');
     expect(calls[0]!.init.headers).toMatchObject({
       Accept: 'application/json, text/event-stream',
       'Content-Type': 'application/json',
@@ -124,14 +129,16 @@
     const { DOCTOR_HELP, parseDoctorArgs, runDoctor } = await loadDoctor();
     const out: string[] = [];
 
-    expect(parseDoctorArgs([
-      '--home',
-      isolatedHome,
-      '--port',
-      '41235',
-      '--label',
-      'com.echo.daemon.walkthrough',
-    ])).toEqual({
+    expect(
+      parseDoctorArgs([
+        '--home',
+        isolatedHome,
+        '--port',
+        '41235',
+        '--label',
+        'com.echo.daemon.walkthrough',
+      ]),
+    ).toEqual({
       home: isolatedHome,
       port: '41235',
       label: 'com.echo.daemon.walkthrough',
@@ -213,6 +220,7 @@
     const text = out.join('');
     expect(code).toBe(1);
     expect(text).toContain('agent claude-code: mcp-not-configured');
+    expect(text).toContain('profile=customer');
     expect(text).toContain(
       'claude mcp add --transport http --scope user echo http://127.0.0.1:41234/mcp',
     );
merged
  result 100644 4d830f1e423db0440578ea89713bfa5b6fc5a7c0 tests/cli/init.test.ts
  our    100644 2cb142437b814f7fcb900210ec80ce8dde41ab0f tests/cli/init.test.ts
@@ -103,11 +103,7 @@
   };
 }
 
-function spawnResult(
-  status: number,
-  stdout = '',
-  stderr = '',
-): SpawnSyncReturns<string> {
+function spawnResult(status: number, stdout = '', stderr = ''): SpawnSyncReturns<string> {
   return {
     pid: 0,
     output: [null, stdout, stderr],
@@ -345,10 +341,7 @@
           completed: boolean;
         };
         state.completed = true;
-        writeFileSync(
-          join(home, 'state/onboarding.json'),
-          `${JSON.stringify(state, null, 2)}\n`,
-        );
+        writeFileSync(join(home, 'state/onboarding.json'), `${JSON.stringify(state, null, 2)}\n`);
       },
     } satisfies Wizard;
   }) as never;
@@ -401,30 +394,39 @@
   it('documents and parses init isolation flags', async () => {
     const { INIT_HELP, parseInitArgs } = await loadInit();
 
-    expect(parseInitArgs([
-      '--home',
-      '/tmp/echo-home',
-      '--port',
-      '41234',
-      '--label',
-      'com.echo.daemon.walkthrough',
-      '--answer-file',
-      '/tmp/answers.json',
-      '--force',
-    ])).toEqual({
+    expect(
+      parseInitArgs([
+        '--home',
+        '/tmp/echo-home',
+        '--port',
+        '41234',
+        '--profile',
+        'dogfood',
+        '--label',
+        'com.echo.daemon.walkthrough',
+        '--answer-file',
+        '/tmp/answers.json',
+        '--force',
+      ]),
+    ).toEqual({
       home: '/tmp/echo-home',
       port: '41234',
+      profile: 'dogfood',
       label: 'com.echo.daemon.walkthrough',
       answerFile: '/tmp/answers.json',
       force: true,
     });
     expect(INIT_HELP).toContain('--home <path>');
     expect(INIT_HELP).toContain('--port <n>');
+    expect(INIT_HELP).toContain('--profile <name>');
     expect(INIT_HELP).toContain('--label <id>');
     expect(INIT_HELP).toContain('--answer-file <path>');
     expect(INIT_HELP).toContain('--force');
     expect(() => parseInitArgs(['--port', '0'])).toThrow('invalid --port: 0');
     expect(() => parseInitArgs(['--port', '12abc'])).toThrow('invalid --port: 12abc');
+    expect(() => parseInitArgs(['--profile', 'orchestrator'])).toThrow(
+      'invalid --profile: expected customer or dogfood',
+    );
   });
 
   it('honors --home, --port, and --label in non-interactive answer-file mode', async () => {
@@ -489,6 +491,7 @@
 
     const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
       completed: boolean;
+      profile: string;
       agents: Array<{ id: AgentKind }>;
     };
     const cursor = JSON.parse(readFileSync(cursorConfig, 'utf8')) as {
@@ -502,16 +505,17 @@
 
     expect(code).toBe(0);
     expect(state.completed).toBe(true);
+    expect(state.profile).toBe('customer');
     expect(state.agents.map((agent) => agent.id).sort()).toEqual(['codex', 'cursor']);
-    expect(readFileSync(codexConfig, 'utf8')).toContain(
-      'url = "http://127.0.0.1:41234/mcp"',
-    );
+    expect(readFileSync(codexConfig, 'utf8')).toContain('url = "http://127.0.0.1:41234/mcp"');
     expect(cursor.mcpServers.echo.url).toBe('http://127.0.0.1:41234/mcp');
     expect(codexCache.mcpServerConfig.url).toBe('http://127.0.0.1:41234/mcp');
-    expect(existsSync(join(isolatedHome, 'skills', 'using-echo-coord.md'))).toBe(true);
+    expect(existsSync(join(isolatedHome, 'skills', 'using-echo-coord.md'))).toBe(false);
     expect(existsSync(join(isolatedHome, 'skills', 'using-echo-mcp.md'))).toBe(true);
     expect(existsSync(join(isolatedHome, 'skills', 'merge-and-cleanup.md'))).toBe(false);
     expect(existsSync(join(isolatedHome, 'skills', 'process-backlog.md'))).toBe(false);
+    expect(existsSync(join(isolatedHome, 'roles', 'builder.toml'))).toBe(false);
+    expect(existsSync(join(isolatedHome, 'workflows', 'change-review.toml'))).toBe(false);
     expect(existsSync(join(echoHome, 'state/onboarding.json'))).toBe(false);
   });
 
@@ -525,6 +529,7 @@
     const answerFile = writeAnswerFile('claude-register-answers.json', {
       confirm_setup: true,
       selected_agents: ['claude-code'],
+      profile: 'dogfood',
       default_project_repo_root: null,
     });
     const calls: Array<{ cmd: string; args: string[]; timeoutMs: number }> = [];
@@ -553,6 +558,10 @@
     });
 
     expect(code).toBe(0);
+    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
+      profile: string;
+    };
+    expect(state.profile).toBe('dogfood');
     expect(calls).toEqual([
       {
         cmd: 'claude',
@@ -572,6 +581,146 @@
     expect(stdout.join('')).toContain('mcp-add');
   });
 
+  it('CLI profile overrides answer-file and recorded profile', async () => {
+    const isolatedHome = join(tmpRoot, 'profile-precedence-home');
+    mkdirSync(join(isolatedHome, 'state'), { recursive: true });
+    writeFileSync(
+      join(isolatedHome, 'state/onboarding.json'),
+      `${JSON.stringify(
+        {
+          schema_version: 1,
+          created_at: '2026-05-26T00:00:00.000Z',
+          last_updated_at: '2026-05-26T00:00:00.000Z',
+          completed: true,
+          profile: 'dogfood',
+          agents: [],
+        },
+        null,
+        2,
+      )}\n`,
+    );
+    const answerFile = writeAnswerFile('profile-precedence-answers.json', {
+      confirm_setup: true,
+      selected_agents: ['codex'],
+      profile: 'dogfood',
+      default_project_repo_root: null,
+    });
+    const seenProfiles: unknown[] = [];
+    const wizardFactory = (() =>
+      ({
+        async detectAgents() {
+          return [detected('codex', 'high')];
+        },
+        async detectProjects() {
+          return [];
+        },
+        async wire(opts) {
+          seenProfiles.push(opts.profile);
+          return successWire(opts.selectedAgents, isolatedHome);
+        },
+        async probe() {
+          return [{ agent: 'codex', probed: true as const, latencyMs: 1 }];
+        },
+        async summary() {
+          return {
+            detected: null,
+            projects: null,
+            wired: null,
+            probed: null,
+            onboardingStateSnapshot: null,
+          };
+        },
+        async markCompleted() {},
+      }) satisfies Wizard) as never;
+    const { runInit } = await loadInit();
+
+    const code = await runInit({
+      stdin: { isTTY: false },
+      answerFile,
+      profile: 'customer',
+      wizardFactory,
+      home: isolatedHome,
+      quiet: true,
+      ...daemonAlreadyRunning(),
+    });
+
+    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
+      profile: string;
+    };
+    expect(code).toBe(0);
+    expect(seenProfiles).toEqual(['customer']);
+    expect(state.profile).toBe('customer');
+  });
+
+  it('recorded dogfood profile is respected on no-flag rerun', async () => {
+    const isolatedHome = join(tmpRoot, 'recorded-dogfood-home');
+    mkdirSync(join(isolatedHome, 'state'), { recursive: true });
+    writeFileSync(
+      join(isolatedHome, 'state/onboarding.json'),
+      `${JSON.stringify(
+        {
+          schema_version: 1,
+          created_at: '2026-05-26T00:00:00.000Z',
+          last_updated_at: '2026-05-26T00:00:00.000Z',
+          completed: true,
+          profile: 'dogfood',
+          agents: [],
+        },
+        null,
+        2,
+      )}\n`,
+    );
+    const answerFile = writeAnswerFile('recorded-dogfood-answers.json', {
+      confirm_setup: true,
+      selected_agents: ['codex'],
+      default_project_repo_root: null,
+    });
+    const seenProfiles: unknown[] = [];
+    const wizardFactory = (() =>
+      ({
+        async detectAgents() {
+          return [detected('codex', 'high')];
+        },
+        async detectProjects() {
+          return [];
+        },
+        async wire(opts) {
+          seenProfiles.push(opts.profile);
+          return successWire(opts.selectedAgents, isolatedHome);
+        },
+        async probe() {
+          return [{ agent: 'codex', probed: true as const, latencyMs: 1 }];
+        },
+        async summary() {
+          return {
+            detected: null,
+            projects: null,
+            wired: null,
+            probed: null,
+            onboardingStateSnapshot: null,
+          };
+        },
+        async markCompleted() {},
+      }) satisfies Wizard) as never;
+    const { runInit } = await loadInit();
+
+    const code = await runInit({
+      stdin: { isTTY: false },
+      answerFile,
+      wizardFactory,
+      home: isolatedHome,
+      quiet: true,
+      ...daemonAlreadyRunning(),
+    });
+
+    const state = JSON.parse(readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8')) as {
+      profile: string;
+    };
+    expect(code).toBe(0);
+    expect(seenProfiles).toEqual(['dogfood']);
+    expect(state.profile).toBe('dogfood');
+  });
+
   it('surfaces duplicate claude-code MCP registration without masking probe failure', async () => {
     const isolatedHome = join(tmpRoot, 'claude-duplicate-home');
     const clientHome = join(tmpRoot, 'claude-duplicate-client');
@@ -866,9 +1015,7 @@
     expect(code).toBe(0);
     expect(codex?.wired_at).toBe('2026-05-26T00:00:00.000Z');
     expect(codex?.wire_error).toBeNull();
-    expect(readFileSync(codexInstructions, 'utf8')).toContain(
-      'http://127.0.0.1:41236/mcp',
-    );
+    expect(readFileSync(codexInstructions, 'utf8')).toContain('http://127.0.0.1:41236/mcp');
     expect(readFileSync(codexInstructions, 'utf8')).not.toContain('user hand-edited');
   });
 
@@ -1044,6 +1191,30 @@
     expect(errors.join('')).toContain('extra: unknown field');
   });
 
+  it('fails loudly when the answer file has an invalid profile', async () => {
+    const { runInit } = await loadInit();
+    const answerFile = writeAnswerFile('invalid-profile.json', {
+      confirm_setup: true,
+      selected_agents: ['codex'],
+      default_project_repo_root: null,
+      profile: 'orchestrator',
+    });
+    const wizardFactory = vi.fn();
+    const errors: string[] = [];
+
+    const code = await runInit({
+      stdin: { isTTY: false },
+      answerFile,
+      wizardFactory: wizardFactory as never,
+      stderr: { write: (s) => (errors.push(String(s)), true) },
+    });
+
+    expect(code).toBe(2);
+    expect(wizardFactory).not.toHaveBeenCalled();
+    expect(errors.join('')).toContain(answerFile);
+    expect(errors.join('')).toContain('profile: expected customer or dogfood');
+  });
+
   it('fails loudly when repo_root is required but absent in answer-file mode', async () => {
     const { runInit } = await loadInit();
     const answerFile = writeAnswerFile('missing-repo-root.json', {
@@ -1106,6 +1277,88 @@
     expect(errors.join('')).toContain('repo_root: required');
   });
 
+  it.each([
+    ['bare scaffold', { completed: false, agents: [] }],
+    [
+      'completed with agents',
+      {
+        completed: true,
+        agents: [
+          {
+            id: 'codex',
+            detected_at: '2026-05-26T00:00:00.000Z',
+            wired_at: '2026-05-26T00:00:00.000Z',
+            probed_at: null,
+            capabilities: [],
+            wire_error: null,
+          },
+        ],
+      },
+    ],
+    [
+      'mid-wire with agents',
+      {
+        completed: false,
+        agents: [
+          {
+            id: 'claude-code',
+            detected_at: '2026-05-26T00:00:00.000Z',
+            wired_at: '2026-05-26T00:00:00.000Z',
+            probed_at: null,
+            capabilities: [],
+            wire_error: null,
+          },
+        ],
+      },
+    ],
+  ])(
+    'defaults profile-less onboarding state to customer with warning: %s',
+    async (_label, partial) => {
+      const isolatedHome = join(tmpRoot, `profileless-${String(_label).replace(/\s+/g, '-')}`);
+      mkdirSync(join(isolatedHome, 'state'), { recursive: true });
+      writeFileSync(
+        join(isolatedHome, 'state/onboarding.json'),
+        `${JSON.stringify(
+          {
+            schema_version: 1,
+            created_at: '2026-05-26T00:00:00.000Z',
+            last_updated_at: '2026-05-26T00:00:00.000Z',
+            ...partial,
+          },
+          null,
+          2,
+        )}\n`,
+      );
+      const answerFile = writeAnswerFile(`${String(_label).replace(/\s+/g, '-')}.json`, {
+        confirm_setup: true,
+        selected_agents: ['codex'],
+        default_project_repo_root: null,
+      });
+      const errors: string[] = [];
+      const { runInit } = await loadInit();
+
+      const code = await runInit({
+        stdin: { isTTY: false },
+        answerFile,
+        wizardFactory: successfulWizardFactory(isolatedHome) as never,
+        home: isolatedHome,
+        stderr: { write: (s) => (errors.push(String(s)), true) },
+        quiet: true,
+        ...daemonAlreadyRunning(),
+      });
+
+      const state = JSON.parse(
+        readFileSync(join(isolatedHome, 'state/onboarding.json'), 'utf8'),
+      ) as {
+        profile: string;
+      };
+      expect(code).toBe(0);
+      expect(state.profile).toBe('customer');
+      expect(errors.join('')).toContain('defaulted to `customer`');
+      expect(errors.join('')).toContain('echoctl init --profile dogfood');
+    },
+  );
+
   it('bootstraps echo home before the interactive wizard touches state', async () => {
     const created: boolean[] = [];
     const wizardFactory = (() => {
merged
  result 100644 9a5addd4ee47f4b57cbc8cb4df6e549dbd7a4202 tests/echo-home/adapter-sync.test.ts
  our    100644 d30e4173a9aeddabaf02d3f6048b6b276aec68ce tests/echo-home/adapter-sync.test.ts
@@ -157,6 +157,32 @@
     expect(existsSync(join(stubHome, '.claude', 'commands', 'process-backlog.md'))).toBe(false);
   });
 
+  it('customer profile filters dogfood skills and skips roles/workflows as successful no-ops', async () => {
+    const { repoRoot, skillsDir } = setupRepoMirror();
+    writeFileSync(join(skillsDir, 'dogfood.md'), '---\naudience: dogfood\n---\n# dogfood\n');
+    const { syncAll } = await loadAdapterSync();
+
+    const result = await syncAll([{ kind: 'claude-code', echoSection: '## E\nx' }], {
+      repoRoot,
+      profile: 'customer',
+    });
+
+    expect(result.overallOk).toBe(true);
+    expect(result.skillsPopulated.ok).toBe(true);
+    if (result.skillsPopulated.ok) {
+      expect(result.skillsPopulated.copied.sort()).toEqual(['alpha.md', 'beta.md']);
+      expect(result.skillsPopulated.skipped).toContain('dogfood.md');
+    }
+    expect(existsSync(join(echoHome, 'skills', 'dogfood.md'))).toBe(false);
+    expect(existsSync(join(stubHome, '.claude', 'commands', 'dogfood.md'))).toBe(false);
+    expect(result.roles.results.map((entry) => entry.action)).toEqual(['noop', 'noop', 'noop']);
+    expect(existsSync(join(echoHome, 'roles', 'builder.toml'))).toBe(false);
+    expect(result.workflowsResult?.results).toEqual([
+      { workflow: 'change-review.toml', action: 'noop' },
+    ]);
+    expect(existsSync(join(echoHome, 'workflows', 'change-review.toml'))).toBe(false);
+  });
+
   it('populate-skills failure (missing repoSkillsDir) skips claude-code fan-out and flips overallOk', async () => {
     const { repoRoot } = setupRepoMirror();
     const { syncAll } = await loadAdapterSync();
merged
  result 100644 b217c049331811d2a8acb27c43111468de23b5cc tests/echo-home/adapters/skill-sync.test.ts
  our    100644 344a066cb35ddd67a86b1ac917a1544392ac94df tests/echo-home/adapters/skill-sync.test.ts
@@ -48,54 +48,88 @@
   return { copied: r.copied, skipped: r.skipped };
 }
 
-describe.each([
-  { label: 'populateEchoSkills' as const },
-  { label: 'syncClaudeSkills' as const },
-])('$label (shared overwrite-posture contract)', ({ label }) => {
-  it(`${label}: target dir does not exist → created; all source skills copied`, () => {
-    const { source, names } = setupSource();
-    const target = join(tmpRoot, 'tgt');
-    expect(existsSync(target)).toBe(false);
-    const r = runFn(label, { sourceDir: source, targetDir: target });
-    expect(existsSync(target)).toBe(true);
-    expect(r.copied.sort()).toEqual(names);
-    for (const n of names) {
-      expect(readFileSync(join(target, n), 'utf8')).toBe(`# ${n}\n`);
-    }
-  });
+describe.each([{ label: 'populateEchoSkills' as const }, { label: 'syncClaudeSkills' as const }])(
+  '$label (shared overwrite-posture contract)',
+  ({ label }) => {
+    it(`${label}: target dir does not exist → created; all source skills copied`, () => {
+      const { source, names } = setupSource();
+      const target = join(tmpRoot, 'tgt');
+      expect(existsSync(target)).toBe(false);
+      const r = runFn(label, { sourceDir: source, targetDir: target });
+      expect(existsSync(target)).toBe(true);
+      expect(r.copied.sort()).toEqual(names);
+      for (const n of names) {
+        expect(readFileSync(join(target, n), 'utf8')).toBe(`# ${n}\n`);
+      }
+    });
 
-  it(`${label}: re-run produces byte-identical files (idempotency-by-overwrite)`, () => {
-    const { source, names } = setupSource();
-    const target = join(tmpRoot, 'tgt');
-    runFn(label, { sourceDir: source, targetDir: target });
-    const first: Record<string, Buffer> = {};
-    for (const n of names) first[n] = readFileSync(join(target, n));
-    runFn(label, { sourceDir: source, targetDir: target });
-    for (const n of names) {
-      expect(readFileSync(join(target, n)).equals(first[n])).toBe(true);
-    }
-  });
+    it(`${label}: re-run produces byte-identical files (idempotency-by-overwrite)`, () => {
+      const { source, names } = setupSource();
+      const target = join(tmpRoot, 'tgt');
+      runFn(label, { sourceDir: source, targetDir: target });
+      const first: Record<string, Buffer> = {};
+      for (const n of names) first[n] = readFileSync(join(target, n));
+      runFn(label, { sourceDir: source, targetDir: target });
+      for (const n of names) {
+        expect(readFileSync(join(target, n)).equals(first[n])).toBe(true);
+      }
+    });
 
-  it(`${label}: stale skill file in target (not in source) is LEFT IN PLACE`, () => {
-    const { source } = setupSource();
-    const target = join(tmpRoot, 'tgt');
-    mkdirSync(target);
-    writeFileSync(join(target, 'stale.md'), 'stale\n');
-    runFn(label, { sourceDir: source, targetDir: target });
-    expect(readFileSync(join(target, 'stale.md'), 'utf8')).toBe('stale\n');
+    it(`${label}: stale skill file in target (not in source) is LEFT IN PLACE`, () => {
+      const { source } = setupSource();
+      const target = join(tmpRoot, 'tgt');
+      mkdirSync(target);
+      writeFileSync(join(target, 'stale.md'), 'stale\n');
+      runFn(label, { sourceDir: source, targetDir: target });
+      expect(readFileSync(join(target, 'stale.md'), 'utf8')).toBe('stale\n');
+    });
+
+    it(`${label}: user-hand-edited target skill file matching a source filename is OVERWRITTEN`, () => {
+      const { source } = setupSource();
+      const target = join(tmpRoot, 'tgt');
+      mkdirSync(target);
+      writeFileSync(join(target, 'alpha.md'), 'user-edit\n');
+      runFn(label, { sourceDir: source, targetDir: target });
+      expect(readFileSync(join(target, 'alpha.md'), 'utf8')).toBe('# alpha.md\n');
+    });
+  },
+);
+
+describe('populateEchoSkills — symlink guards', () => {
+  it('customer profile copies customer and untagged skills but skips dogfood-only skills', () => {
+    const source = join(tmpRoot, 'audience-src');
+    const target = join(tmpRoot, 'audience-target');
+    mkdirSync(source);
+    writeFileSync(join(source, 'customer.md'), '---\naudience: customer\n---\n# customer\n');
+    writeFileSync(join(source, 'dogfood.md'), '---\naudience: dogfood\n---\n# dogfood\n');
+    writeFileSync(join(source, 'untagged.md'), '# untagged\n');
+
+    const result = populateEchoSkills({
+      sourceDir: source,
+      targetDir: target,
+      profile: 'customer',
+    });
+
+    if (!result.ok) throw new Error('expected ok:true');
+    expect(result.copied.sort()).toEqual(['customer.md', 'untagged.md']);
+    expect(result.skipped).toContain('dogfood.md');
+    expect(existsSync(join(target, 'dogfood.md'))).toBe(false);
   });
 
-  it(`${label}: user-hand-edited target skill file matching a source filename is OVERWRITTEN`, () => {
-    const { source } = setupSource();
-    const target = join(tmpRoot, 'tgt');
-    mkdirSync(target);
-    writeFileSync(join(target, 'alpha.md'), 'user-edit\n');
-    runFn(label, { sourceDir: source, targetDir: target });
-    expect(readFileSync(join(target, 'alpha.md'), 'utf8')).toBe('# alpha.md\n');
+  it('dogfood profile copies all skill audiences', () => {
+    const source = join(tmpRoot, 'dogfood-audience-src');
+    const target = join(tmpRoot, 'dogfood-audience-target');
+    mkdirSync(source);
+    writeFileSync(join(source, 'customer.md'), '---\naudience: customer\n---\n# customer\n');
+    writeFileSync(join(source, 'dogfood.md'), '---\naudience: dogfood\n---\n# dogfood\n');
+
+    const result = populateEchoSkills({ sourceDir: source, targetDir: target, profile: 'dogfood' });
+
+    if (!result.ok) throw new Error('expected ok:true');
+    expect(result.copied.sort()).toEqual(['customer.md', 'dogfood.md']);
+    expect(result.skipped).toEqual([]);
   });
-});
 
-describe('populateEchoSkills — symlink guards', () => {
   it('symlink in sourceDir is NEVER followed (skipped[])', () => {
     const { source } = setupSource();
     // Add a symlink in source pointing outside the dir.
merged
  result 100755 74f59fb1a5f37f3f30d543a397956177a03c0382 tools/foreign-install-smoke.sh
  our    100755 c41204398e05e23b12030f7a29bd00a213863479 tools/foreign-install-smoke.sh
@@ -59,6 +59,20 @@
   exit 1
 }
 
+assert_exists() {
+  [ -e "$1" ] || fail "expected path to exist: $1"
+}
+
+assert_absent() {
+  [ ! -e "$1" ] || fail "expected path to be absent: $1"
+}
+
+assert_profile() {
+  local expected="$1"
+  node -e 'const fs=require("fs"); const p=process.argv[1]; const expected=process.argv[2]; const state=JSON.parse(fs.readFileSync(p,"utf8")); if (state.profile !== expected) { console.error(`[assert] expected profile ${expected}, got ${state.profile}`); process.exit(1); }' "$ECHO_HOME/state/onboarding.json" "$expected" \
+    || fail "onboarding profile mismatch"
+}
+
 cd "$REPO"
 echo "=================== STEP 0: build fresh tarball ==================="
 npm pack 2>&1 | tail -2
@@ -114,6 +128,15 @@
 
 echo ""; echo "=== what skills landed in the user's ~/.echo/skills? ==="
 ls -1 "$ECHO_HOME/skills" 2>&1 || echo "(no skills dir created)"
+assert_profile customer
+assert_exists "$ECHO_HOME/skills/using-echo-mcp.md"
+assert_absent "$ECHO_HOME/skills/using-echo-coord.md"
+assert_absent "$SBOX/fakehome/.claude/commands/using-echo-coord.md"
+assert_absent "$ECHO_HOME/roles/builder.toml"
+assert_absent "$ECHO_HOME/roles/reviewer.toml"
+assert_absent "$ECHO_HOME/roles/strategist.toml"
+assert_absent "$ECHO_HOME/workflows/change-review.toml"
+echo "[assert] default customer surface OK"
 
 echo ""; echo "=== did wire inject an ECHO block into his codex config.toml? ==="
 grep -nE "ECHO|echo|mcp|38478|$PORT" "$SBOX/fakehome/.codex/config.toml" 2>/dev/null | head || echo "(no ECHO content in codex config)"
@@ -122,3 +145,31 @@
 
 echo ""; echo "=================== STEP 4: doctor AFTER install ==================="
 "$BIN" doctor 2>&1 | head -50 || true
+
+echo ""; echo "=================== STEP 5: no-flag rerun stays customer ==================="
+"$BIN" init --home "$ECHO_HOME" --port "$PORT" --label "$LABEL" --answer-file "$SBOX/answers.json" > "$SBOX/init-rerun.out" 2>&1
+RERUN_RC=$?
+sed -n '1,40p' "$SBOX/init-rerun.out"
+[ "$RERUN_RC" -eq 0 ] || fail "echoctl init rerun exited $RERUN_RC"
+assert_profile customer
+assert_absent "$ECHO_HOME/skills/using-echo-coord.md"
+assert_absent "$SBOX/fakehome/.claude/commands/using-echo-coord.md"
+echo "[assert] no-flag customer rerun stayed customer"
+
+echo ""; echo "=================== STEP 6: explicit dogfood install gets full coord surface ==================="
+cat > "$SBOX/answers-dogfood.json" <<'JSON'
+{ "confirm_setup": true, "selected_agents": ["codex","claude-code"], "default_project_repo_root": null }
+JSON
+"$BIN" init --home "$ECHO_HOME" --port "$PORT" --profile dogfood --label "$LABEL" --answer-file "$SBOX/answers-dogfood.json" > "$SBOX/init-dogfood.out" 2>&1
+DOGFOOD_RC=$?
+sed -n '1,50p' "$SBOX/init-dogfood.out"
+[ "$DOGFOOD_RC" -eq 0 ] || fail "echoctl init --profile dogfood exited $DOGFOOD_RC"
+assert_profile dogfood
+assert_exists "$ECHO_HOME/skills/using-echo-mcp.md"
+assert_exists "$ECHO_HOME/skills/using-echo-coord.md"
+assert_exists "$SBOX/fakehome/.claude/commands/using-echo-coord.md"
+assert_exists "$ECHO_HOME/roles/builder.toml"
+assert_exists "$ECHO_HOME/roles/reviewer.toml"
+assert_exists "$ECHO_HOME/roles/strategist.toml"
+assert_exists "$ECHO_HOME/workflows/change-review.toml"
+echo "[assert] explicit dogfood surface OK"

codex
The merge preview shows no main-side edits to these files since the branch point, so I don’t expect textual conflicts. I’m running the required verification commands now in the feature worktree.
exec
/bin/zsh -lc 'npm test' in /Users/zhenye/Desktop/Project_echo--install-profile-split
codex
`npm test` is running under Vitest. I’m waiting for completion rather than trusting the agent’s recorded counts.
codex
The full test suite is still running and remains green so far. The notable profile-related files have already passed: `init.test.ts`, `doctor.test.ts`, `adapter-sync.test.ts`, and `skill-sync.test.ts`.
codex
`npm test` is past the slow review-queue and capture suites and still passing. I’ll run lint and typecheck after it exits so the reported counts are from this review run.
codex
`npm test` surfaced one failure in `tests/mcp/recent-calls-endpoint.test.ts`: a 15s timeout in “logs every runtime-registered tool through the wrapper.” I’m letting the run finish, then I’ll inspect whether that failure is connected to this diff or an existing flaky/unrelated test.
codex
A second failure appeared in `tests/cli/shell-reachable.test.ts`, where the packaged daemon did not become healthy during the shell-reachability smoke. Both failures are outside the files changed by this item, but they do make the required `npm test` result red.
 exited 1 in 137760ms:

> echoctl@0.1.0 test
> vitest run


 RUN  v2.1.9 /Users/zhenye/Desktop/Project_echo--install-profile-split

{"timestamp":"2026-06-02T16:51:59.957Z","level":"info","source":"storage.sqlite","message":"canonicalized_timestamps","payload":{"converted":1}}
 ✓ tests/storage/sqlite.test.ts (29 tests) 200ms
 ✓ tests/trace/build.test.ts (20 tests) 306ms
 ✓ tests/mcp/get-atoms.test.ts (23 tests) 174ms
 ✓ tests/capture/extractors/cursor.test.ts (62 tests | 11 skipped) 964ms
 ✓ tests/mcp/find-clusters.test.ts (21 tests) 1182ms
   ✓ find_clusters > view="compact" sizes the response cap after compact projection 345ms
 ✓ tests/skills/merge-and-cleanup-shape.test.ts (21 tests) 142ms
{"timestamp":"2026-06-02T16:52:01.501Z","level":"warn","source":"mcp.cursor-resolver","message":"workspace_json_not_json","payload":{"path":"/var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-cursor-resolver-pGqjFb/workspaceStorage/WS-BAD-JSON/workspace.json","message":"Expected property name or '}' in JSON at position 1 (line 1 column 2)"}}
 ✓ tests/mcp/cursor-workspace-resolver.test.ts (19 tests) 253ms
 ✓ tests/echo-home/adapter-sync.test.ts (26 tests) 2753ms
   ✓ syncAll (orchestrator) > all three agents succeed → overallOk:true 657ms
 ✓ tests/trace/rank.test.ts (16 tests) 49ms
 ✓ tests/trace/hints.test.ts (21 tests) 59ms
 ✓ tests/mcp/tools/search-memories.test.ts (60 tests) 2046ms
   ✓ search_memories (end-to-end via MCP server) > lists search_memories via tools/list with the documented description 830ms
 ✓ tests/echo-home/wizard/wire.test.ts (15 tests) 1575ms
   ✓ wire > builds profiles with no previous cache and writes cache records after success 424ms
echoctl init: onboarding profile was missing; defaulted to `customer`. Re-run `echoctl init --profile dogfood` to restore the full coordination surface.
 ✓ tests/cli/init.test.ts (27 tests) 3873ms
   ✓ runInit > fails closed before wizard creation when stdin is not a TTY 680ms
 ✓ tests/mcp/wait-for-new-turns.test.ts (21 tests) 91ms
 ✓ tests/mcp/tools/recent-work-context.test.ts (66 tests) 3385ms
   ✓ get_recent_work_context (end-to-end via MCP server) > lists get_recent_work_context via tools/list with the documented description 753ms
 ✓ tests/mcp/tools/echo-resolve-mru.test.ts (20 tests) 1113ms
   ✓ echoResolveMru — registered-handler integration > tools/list advertises echo_resolve_mru with the descriptor description 759ms
{"timestamp":"2026-06-02T16:52:04.951Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:04.955Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:04.955Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:04.967Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51598,"url":"http://127.0.0.1:51598/mcp","host":"127.0.0.1"}}
 ✓ tests/cli/daemon.test.ts (15 tests) 160ms
{"timestamp":"2026-06-02T16:52:05.985Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:05.996Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":2,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:05.999Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:06.009Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":1,"round_open":0,"scheduler_open":0,"idempotency_cache_size":1}}
{"timestamp":"2026-06-02T16:52:06.012Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":2,"round_open":1,"scheduler_open":0,"idempotency_cache_size":1}}
{"timestamp":"2026-06-02T16:52:06.015Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":2,"round_open":1,"scheduler_open":0,"idempotency_cache_size":1}}
 ✓ tests/coord/coord-status-shape.test.ts (6 tests) 35ms
 ✓ tests/task-state/patch-builder-state.test.ts (12 tests) 3550ms
   ✓ tools/task-state/patch-builder-state.py > complete handoff: rewrites spec anchor, writes handoff_head_sha frontmatter, keeps anchors schema-compliant, passes lint 649ms
   ✓ tools/task-state/patch-builder-state.py > complete handoff: marker block appended once when absent, replaced exactly once when present 453ms
   ✓ tools/task-state/patch-builder-state.py > escalated handoff appends an escalation marker in open_questions when non-empty 580ms
 ✓ tests/mcp/server.test.ts (14 tests) 964ms
   ✓ startMcpServer > lists echo_ping via tools/list 490ms
 ✓ tests/trace/cluster.test.ts (20 tests) 37ms
 ✓ tests/review-queue/combine.test.ts (21 tests) 7499ms
   ✓ combine.py > no eligible rounds: exits 0, prints status line, writes nothing 914ms
   ✓ combine.py > both responses present, no convergent where: divergent populated, convergent empty 332ms
   ✓ combine.py > both responses present, exact-primary convergent: convergent table populated 330ms
   ✓ combine.py > cross_ref override: groups even when primary `where` differs 332ms
   ✓ combine.py > verdicts crossing {proceed*, pushback}: divergent + escalated_to_founder 303ms
   ✓ combine.py > one response missing, within timeout: no combined.md written 328ms
   ✓ combine.py > one response missing, past timeout: partial_responses + auto-disposition (044 AC4) 347ms
   ✓ combine.py > both responses missing, past timeout: no_responses + escalated 347ms
   ✓ combine.py > combined.md already exists: skip (no race-overwrite) 711ms
   ✓ combine.py > orphan .tmp.* older than 30 min cleaned up; younger left alone 327ms
   ✓ combine.py > AC3.5 (c) — explicit waiver: combined.md remains the terminal artifact (next_round=null) 301ms
   ✓ combine.py > AC3d — CLI override --timeout-hours=2 uniformly: at 35min elapsed both gated by 2h → NOT eligible 301ms
   ✓ combine.py > AC4a — single-missing-proceed → auto-disposition (escalated_to_founder: false) 311ms
 ✓ tests/mcp/request-log.test.ts (11 tests) 48ms
{"timestamp":"2026-06-02T16:52:07.548Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:07.565Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51641,"url":"http://127.0.0.1:51641/mcp","host":"127.0.0.1"}}
 ✓ tests/review-queue/n-reviewer-framework.test.ts (20 tests) 8142ms
   ✓ 043 AC1 — per-round roster (codex-only round) > AC1b: codex-only-requested round becomes eligible immediately + emits cursor_response:null 1243ms
   ✓ 043 AC1 — per-round roster (codex-only round) > AC1c: required cursor missing BEFORE timeout, codex present → NOT eligible 345ms
   ✓ 043 AC1 — per-round roster (codex-only round) > 044 AC4d: 3 requested, 1 responds proceed, 2 missing past timeout → escalates 337ms
   ✓ 043 AC1 — per-round roster (codex-only round) > 044 AC4e: codex+codex-ops requested, codex proceeds, codex-ops missing past timeout → auto-disposition 305ms
   ✓ 043 AC1f — dispatch-next-round.py propagates roster > r2/request.requested_reviewers preserves r1 roster (cursor-only) 1076ms
   ✓ 043 AC2 — _reviewers.py validation + cache > AC2a: invalid mode rejected with reviewer name + offending field 305ms
   ✓ 043 AC2 — _reviewers.py validation + cache > AC2a: duplicate slug rejected 332ms
   ✓ 043 AC2 — _reviewers.py validation + cache > AC2d: mode=headless requires timeout_hours=null 332ms
   ✓ 043 AC3 — _run_reviewer.sh gating > AC3c: REVIEWER_NAME=ghost → "ghost not found in reviewers.json" 850ms
   ✓ 043 AC3 — _run_reviewer.sh gating > AC3d: REVIEWER_NAME=cursor (mode=ide) → "cursor has mode=ide, not headless" 807ms
   ✓ 043 AC6 — N-way verdict roll-up > AC6: default 2-reviewer convergent proceed → combined_verdict=proceed 356ms
   ✓ 043 AC6 — N-way verdict roll-up > AC6: 2-reviewer proceed + proceed_after_patches → proceed_after_patches 302ms
   ✓ 043 AC6 — N-way verdict roll-up > AC6: 2-reviewer proceed + pushback → divergent + escalated 366ms
 ✓ tests/echo-home/roles.test.ts (28 tests) 182ms
{"timestamp":"2026-06-02T16:52:08.186Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/mcp/pending-decisions.test.ts (12 tests) 867ms
   ✓ pending_decisions MCP registration > is registered by startMcpServer 695ms
 ✓ tests/echo-home/adapters/codex-config.test.ts (14 tests) 72ms
reviewer: capability-mismatch
 ✓ tests/echo-home/wizard/detect-projects.test.ts (11 tests) 338ms
 ✓ tests/cli/run.test.ts (8 tests) 1148ms
   ✓ runRun > dispatches a matched workflow using --project cwd 438ms
{"timestamp":"2026-06-02T16:52:09.396Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":2}}
{"timestamp":"2026-06-02T16:52:09.400Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:09.401Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:09.410Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51647,"url":"http://127.0.0.1:51647/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:09.411Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/coord/coord-roles-validation.test.ts (15 tests) 403ms
 ✓ tests/storage/memory.test.ts (22 tests) 76ms
{"timestamp":"2026-06-02T16:52:10.256Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:10.319Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:10.320Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:10.320Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:10.324Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51648,"url":"http://127.0.0.1:51648/mcp","host":"127.0.0.1"}}
 ✓ tests/mcp/wire-shape/match.test.ts (12 tests) 47ms
To /private/var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-rq-combmal-origin-Zf583O
 * [new branch]      main -> main
To /private/var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-rq-combmal-origin-Zf583O
   b65d402..166c3de  main -> main
 ✓ tests/capture/extractors/claude-code.test.ts (31 tests) 12819ms
   ✓ startClaudeCodeExtractor (lifecycle + integration) > emits one CandidateEvent per turn through the pipeline on file growth 1054ms
   ✓ startClaudeCodeExtractor (lifecycle + integration) > populates metadata.repo_root from the cwd field on JSONL lines 1047ms
   ✓ startClaudeCodeExtractor (lifecycle + integration) > extracts file_path / path / notebook_path inputs from tool_use blocks into metadata.files_referenced 1043ms
   ✓ startClaudeCodeExtractor (lifecycle + integration) > omits metadata.files_referenced when assistant has no tool_use file inputs 1031ms
   ✓ startClaudeCodeExtractor (lifecycle + integration) > end-to-end: chronological appends produce ordered, non-duplicate events 3017ms
   ✓ startClaudeCodeExtractor (lifecycle + integration) > backfills offset map from prior storage events; resumes without duplicating 1017ms
   ✓ startClaudeCodeExtractor (lifecycle + integration) > captures turns from subagent JSONLs created mid-session in a nested subdir 1039ms
   ✓ startClaudeCodeExtractor (lifecycle + integration) > emits stale git_state (fresh:false, branch only) when probe refuses but JSONL gitBranch is present 1035ms
   ✓ startClaudeCodeExtractor (lifecycle + integration) > threads JSONL gitBranch / permissionMode / cli version / model into metadata 1033ms
   ✓ startClaudeCodeExtractor (lifecycle + integration) > stop() resolves cleanly and prevents further events 1311ms
 ✓ tests/capture/extractors/codex.test.ts (43 tests) 12973ms
   ✓ startCodexExtractor (lifecycle + integration) > emits a CaptureEvent through the pipeline once a cluster closes 1060ms
   ✓ startCodexExtractor (lifecycle + integration) > captures every turn from rapid back-to-back appends within the polling interval 1050ms
   ✓ startCodexExtractor (lifecycle + integration) > lands metadata.git and metadata.codex through the pipeline 1024ms
   ✓ startCodexExtractor (lifecycle + integration) > lands Codex tool overflow and files_referenced metadata through the pipeline 1025ms
   ✓ startCodexExtractor (lifecycle + integration) > mirrors metadata.cwd into metadata.repo_root (cross-source canonical name) 1034ms
   ✓ startCodexExtractor (lifecycle + integration) > carries cwd on every turn even when extraction spans multiple daemon ticks 3042ms
   ✓ startCodexExtractor (lifecycle + integration) > restores cwd from prior storage events on daemon restart (backfillOffsetMap) 1014ms
   ✓ startCodexExtractor (lifecycle + integration) > end-to-end: appending more clusters produces ordered, non-duplicate turns 2032ms
   ✓ startCodexExtractor (lifecycle + integration) > stop() resolves cleanly and prevents further events 1340ms
{"timestamp":"2026-06-02T16:52:14.697Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":2,"round_open":1,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:14.751Z","level":"info","source":"coord.deadlines","message":"deadline_missed","payload":{"atom_id":"f9e001ee-bc6e-48df-ba54-71e33e63e5a5","tier":"round","subject_role":"codex","opened_event_type":"reviewer_invoked","expected_event_type":"tick_start","key":"round-B","expected_by":"2026-05-16T10:01:00.000Z"}}
{"timestamp":"2026-06-02T16:52:14.751Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":1,"round_open":0,"scheduler_open":0,"idempotency_cache_size":1}}
{"timestamp":"2026-06-02T16:52:14.761Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":2,"round_open":0,"scheduler_open":0,"idempotency_cache_size":1}}
{"timestamp":"2026-06-02T16:52:14.764Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":2,"round_open":1,"scheduler_open":0,"idempotency_cache_size":0}}
 ✓ tests/coord/deadlines-reconstruction.test.ts (4 tests) 73ms
 ✓ tests/echo-home/wizard/detect-agents.test.ts (10 tests) 507ms
{"timestamp":"2026-06-02T16:52:15.681Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/review-queue/watcher-state.test.ts (4 tests) 5041ms
   ✓ dispatch-next-round.py (AC3.5 watcher post-combine state machine) > (b) positive — load-bearing convergent finding: writes r2/request.md and sets next_round=2 1361ms
   ✓ dispatch-next-round.py (AC3.5 watcher post-combine state machine) > (a) negative — proceed + no patches: no r2/request.md; next_round stays null; exit 0 670ms
   ✓ dispatch-next-round.py (AC3.5 watcher post-combine state machine) > (b) race-loser — idempotent at same SHA; exit 2 at different SHA 2070ms
   ✓ dispatch-next-round.py (AC3.5 watcher post-combine state machine) > (c) explicit waiver — appends rationale line to body; next_round stays null 938ms
{"timestamp":"2026-06-02T16:52:15.732Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:15.732Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:15.732Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:15.733Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51652,"url":"http://127.0.0.1:51652/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:16.091Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:16.109Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:16.117Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:16.117Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:16.118Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51654,"url":"http://127.0.0.1:51654/mcp","host":"127.0.0.1"}}
 ✓ tests/capture/surfaces/git-watcher.test.ts (13 tests) 12001ms
   ✓ startGitWatcher > captures all commits as backfill on first boot of an empty store 1713ms
   ✓ startGitWatcher > populates metadata.repo_root and metadata.files_referenced for each commit 1150ms
   ✓ startGitWatcher > only emits new commits on a second boot (resumption from last_seen_sha) 2747ms
   ✓ startGitWatcher > polling fallback catches commits when fs watching is disabled 1217ms
   ✓ startGitWatcher > truncates large diffs at 100 KB with a marker 907ms
   ✓ startGitWatcher > respects ECHO_GIT_BACKFILL_COMMITS for backfill window 2749ms
   ✓ startGitWatcher > captures a fresh commit via fs watching with no polling needed 1506ms
To /private/var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-rq-combmal-origin-PLxG10
 * [new branch]      main -> main
 ✓ tests/coord/append-seam.test.ts (16 tests) 81ms
To /private/var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-rq-combmal-origin-PLxG10
   9dac96b..3842b10  main -> main
 ✓ tests/mcp/get-atom.test.ts (8 tests) 34ms
 ✓ tests/echo-home/wizard/run-wizard.test.ts (5 tests) 781ms
   ✓ createWizard > runs detect → projects → wire → probe → summary → markCompleted 394ms
{"timestamp":"2026-06-02T16:52:18.754Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
 ✓ tests/echo-home/adapters/cursor-config.test.ts (12 tests) 65ms
{"timestamp":"2026-06-02T16:52:18.761Z","level":"info","source":"coord.deadlines","message":"deadline_missed","payload":{"atom_id":"ee2bf049-61b7-4ab8-b89c-8427a33f187b","tier":"round","subject_role":"codex","opened_event_type":"reviewer_invoked","expected_event_type":"tick_start","key":"round-shared","expected_by":"2026-05-16T10:00:30.000Z"}}
{"timestamp":"2026-06-02T16:52:18.761Z","level":"info","source":"coord.deadlines","message":"deadline_missed","payload":{"atom_id":"dad4dec2-1dab-440b-8d53-98651d72d48d","tier":"round","subject_role":"codex-ops","opened_event_type":"reviewer_invoked","expected_event_type":"tick_start","key":"round-shared","expected_by":"2026-05-16T10:00:30.000Z"}}
{"timestamp":"2026-06-02T16:52:18.763Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:18.766Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:18.768Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
 ✓ tests/coord/idempotency-and-tier-keyspace.test.ts (4 tests) 18ms
 ✓ tests/storage/iterate-coord-by-append-order.test.ts (16 tests) 108ms
 ✓ tests/review-queue/combine-malformed-response.test.ts (2 tests) 10750ms
   ✓ combine.py — malformed-reviewer-response escalation (AC2/AC4) > AC2a: single malformed cursor.md → combined.md with malformed_reviewer_response, queue-errors row appended in same commit, git tree clean 5229ms
   ✓ combine.py — malformed-reviewer-response escalation (AC2/AC4) > AC2b: both codex.md AND cursor.md malformed → offending_response is a length-2 array, both parsed before emit, tree clean 5519ms
 ✓ tests/backlog/run-codex-builder.test.ts (3 tests) 10821ms
   ✓ 047 AC4 — run-codex-builder.sh wrapper contract > case 1 — wrapper passes correct env + argv + stdin + lock-visibility to codex exec 1782ms
   ✓ 047 AC4 — run-codex-builder.sh wrapper contract > case 2 — wrapper handles ECHO_AGENT_ID first-run generation and is stable across runs 2879ms
   ✓ 047 AC4 — run-codex-builder.sh wrapper contract > case 3 — atomic lockfile prevents overlapping wrapper invocations (race-free) 6157ms
 ✓ tests/review-queue/schemas.test.ts (18 tests) 10114ms
   ✓ review-queue schemas > valid request parses cleanly 817ms
   ✓ review-queue schemas > missing required field on request fails with field name 342ms
   ✓ review-queue schemas > missing required field on combined fails with field name 322ms
   ✓ review-queue schemas > reviewer enum {codex, cursor}: other reviewer rejected 317ms
   ✓ review-queue schemas > reviewer verdict excludes combined-only values 974ms
   ✓ review-queue schemas > reviewer verdict accepts the three per-reviewer values 1126ms
   ✓ review-queue schemas > combined verdict accepts all six values 1821ms
   ✓ review-queue schemas > request class enum is {narrow, structural-reform} 878ms
   ✓ review-queue schemas > combined: malformed_reviewer_response with array shapes of length 2 (AC3) 304ms
   ✓ review-queue schemas > requested_reviewers must be a non-empty subset of the reviewer enum 938ms
{"timestamp":"2026-06-02T16:52:21.484Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:21.519Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:21.519Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:21.519Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:21.520Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51657,"url":"http://127.0.0.1:51657/mcp","host":"127.0.0.1"}}
 ✓ tests/mcp/wire-shape/compact.test.ts (7 tests) 57ms
 ✓ tests/capture/gate.test.ts (28 tests) 79ms
 ✓ tests/capture/surfaces/fs-watcher.test.ts (13 tests | 6 skipped) 19ms
 ✓ tests/reasoning/causal.test.ts (14 tests) 64ms
{"timestamp":"2026-06-02T16:52:22.387Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:22.442Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:22.443Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:22.443Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:22.443Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51660,"url":"http://127.0.0.1:51660/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:22.746Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:22.772Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:22.772Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:22.772Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:22.773Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51663,"url":"http://127.0.0.1:51663/mcp","host":"127.0.0.1"}}
 ↓ tests/daemon/lifecycle.test.ts (4 tests | 4 skipped)
 ✓ tests/review-queue/045-pre-link-yaml-validation.test.ts (5 tests) 7119ms
   ✓ 045 AC1 — pre-link reviewer-response YAML validation gate > AC1a — valid response YAML: exit 0, stderr empty 1256ms
   ✓ 045 AC1 — pre-link reviewer-response YAML validation gate > AC1b — malformed YAML (embedded `""` literal): exit non-zero, stderr names line/column 1216ms
   ✓ 045 AC1 — pre-link reviewer-response YAML validation gate > AC1c — schema violation (verdict not in enum): exit non-zero, stderr names the offending field 1165ms
   ✓ 045 AC1 — pre-link reviewer-response YAML validation gate > AC1d — clean-tree: helper does not mutate queue-errors.md against the pre-helper staged baseline 3475ms
 ✓ tests/capture/pipeline.test.ts (20 tests) 88ms
{"timestamp":"2026-06-02T16:52:23.658Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/coord/coord-emit-wrapper-transport.test.ts (7 tests) 18765ms
   ✓ 057b AC7 — coord-emit.sh wrapper transport > coord-emit.sh tick_start produces a valid coord:tick_start atom 5362ms
   ✓ 057b AC7 — coord-emit.sh wrapper transport > atom is accepted by 057a coord_emit validator: tier=round, subject_role=codex 5425ms
   ✓ 057b AC7 — coord-emit.sh wrapper transport > daemon-down does NOT abort the wrapper (exit 0) 409ms
   ✓ 057b AC7 — coord-emit.sh wrapper transport > scheduler_health emitted with tick_run_id is accepted 5394ms
   ✓ 059 AC3 — coord-emit.sh distinguishes daemon-rejection from daemon-unreachable > daemon rejection (isError:true) is surfaced verbatim on stderr; exit 0; no atom 902ms
   ✓ 059 AC3 — coord-emit.sh distinguishes daemon-rejection from daemon-unreachable > daemon unreachable produces fully empty stderr; exit 0; no atom 359ms
   ✓ 059 AC3 — coord-emit.sh distinguishes daemon-rejection from daemon-unreachable > daemon HTTP 500 is surfaced as `returned HTTP 500` on stderr; exit 0; no atom 912ms
{"timestamp":"2026-06-02T16:52:24.315Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:24.329Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51667,"url":"http://127.0.0.1:51667/mcp","host":"127.0.0.1"}}
 ✓ tests/echo-home/wizard/probe.test.ts (15 tests) 110ms
{"timestamp":"2026-06-02T16:52:24.825Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:24.835Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:24.838Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51672,"url":"http://127.0.0.1:51672/mcp","host":"127.0.0.1"}}
 ✓ tests/echo-home/adapters/atomic-write.test.ts (10 tests) 1001ms
   ✓ atomicWrite > two concurrent workers writing the same path produce exactly one final payload (no garbled mix, no temps left) 738ms
{"timestamp":"2026-06-02T16:52:24.903Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:24.916Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51674,"url":"http://127.0.0.1:51674/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:25.014Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/daemon/lifecycle-shutdown-flush.test.ts (4 tests) 774ms
   ✓ lifecycle shutdown flush > Test (i): stop+flush preserves completed `ok` and rewrites in-flight to `killed_during_shutdown` 569ms
 ✓ tests/capture/sources.test.ts (23 tests) 61ms
 ✓ tests/cli/doctor.test.ts (4 tests) 915ms
   ✓ runDoctor > emits healthy JSON and sends the MCP initialize headers 545ms
 ✓ tests/review-queue/053-completed-at-coercion.test.ts (6 tests) 25690ms
   ✓ 053 AC3.1 — _coerce_completed_at helper direct unit-test > UTC tz-aware datetime → 2026-05-12T23:56:42Z 897ms
   ✓ 053 AC3.1 — _coerce_completed_at helper direct unit-test > +09:00 tz-aware (JST) → 2026-05-12T23:56:42Z (day-boundary crossing handled) 328ms
   ✓ 053 AC3.2 — hermetic end-to-end pipeline with unquoted completed_at > AC3.2 — unquoted completed_at flows through validate_response_yaml → commit-reviewer-response → combine without quarantine 21957ms
   ✓ 053 AC4 — quoted-string completed_at unaffected by AC2 coercion > quoted completed_at still passes validate_response_yaml.py 1959ms
{"timestamp":"2026-06-02T16:52:25.568Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/echo-mcp/role-state.test.ts (15 tests) 21209ms
   ✓ 046 AC4 — get_role_state > (a) returns content for existing strategist.md at HEAD 920ms
   ✓ 046 AC4 — get_role_state > (b) returns content at an explicit SHA different from HEAD 1210ms
   ✓ 046 AC4 — get_role_state > (c) isError for missing role file 786ms
   ✓ 046 AC4 — get_role_state > (d) isError for unresolvable ref 728ms
   ✓ 046 AC4 — get_role_state > (j) HEAD-race: SHA is pinned at call entry — moving main does not change the response 1531ms
   ✓ 046 AC4 — get_role_state > (k) branch-ref: response ref is the resolved commit SHA, NOT the branch name 1700ms
   ✓ 046 AC4 — get_role_state > byte-identity contract: git show <ref>:<path> equals get_role_state.content 1065ms
   ✓ 046 AC4 — list_task_states > (e) filters by stage at HEAD 2288ms
   ✓ 046 AC4 — list_task_states > (f) returns empty array gracefully when no task-state directories exist 939ms
   ✓ 046 AC4 — list_task_states > (g) binding parameter accepted-but-unused without error 1374ms
   ✓ 046 AC4 — list_task_states > (h) malformed canonical_anchors degrades — response succeeds with _parse_error 1292ms
   ✓ 046 AC4 — list_task_states > role filter narrows roles_present matching 1950ms
   ✓ 046 AC4 — list_task_states > uses a single pinned SHA for the whole call — stage cross-ref does not see a later commit 1661ms
   ✓ 046 AC4 — repo-root resolution via startMcpServer option > (i) constructor option overrides env + cwd at server-start 1437ms
   ✓ 046 AC4 — repo-root resolution via startMcpServer option > startMcpServer respects options.repo_root and reads through to handlers 2316ms
 ✓ tests/trace/auto-expand.test.ts (9 tests) 16ms
 ✓ tests/review-queue/concurrency.test.ts (7 tests) 6878ms
   ✓ review-queue concurrency + timeout > two request.py invocations same SHA: both succeed (idempotent) 1372ms
   ✓ review-queue concurrency + timeout > two request.py invocations, different SHA: second fails with clear error 671ms
   ✓ review-queue concurrency + timeout > orphan .tmp.* older than 30 min is cleaned up by combine.py 579ms
   ✓ review-queue concurrency + timeout > push-with-retry.sh exits non-zero and appends to queue-errors.md when push fails 2702ms
   ✓ review-queue concurrency + timeout > missing-reviewer timeout: partial_responses + auto-disposition (044 AC4 — single-missing-proceed) 492ms
   ✓ review-queue concurrency + timeout > same-SHA idempotency is checked by reading the existing file, not by treating FileExistsError as success 880ms
{"timestamp":"2026-06-02T16:52:26.910Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":100000,"round_open":0,"scheduler_open":0,"idempotency_cache_size":20000}}
 ✓ tests/review-queue/045-smoke-gate-fail-closed.test.ts (3 tests) 15429ms
   ✓ 045 AC2 — _install_reviewer_launchd.sh smoke gate fail-closed > AC2a — --smoke + missing smoke runner: exit 1, NO plist, NO launchctl invocations 1815ms
   ✓ 045 AC2 — _install_reviewer_launchd.sh smoke gate fail-closed > AC2b — install without --smoke flag: exit 0, plist installed, bootout + bootstrap recorded, NO kickstart 7715ms
   ✓ 045 AC2 — _install_reviewer_launchd.sh smoke gate fail-closed > AC2c — --smoke + present smoke runner: exit 0, plist installed, bootout + bootstrap + kickstart recorded 5897ms
 ✓ tests/echo-home/adapters/skill-sync.test.ts (13 tests) 224ms
 ✓ tests/coord/coord-volume-perf.test.ts (1 test) 1440ms
   ✓ AC6+AC8 — coord volume perf fixture (100k atoms) > reconstruct <1500ms + coord_status <300ms at 100000 atoms 1439ms
{"timestamp":"2026-06-02T16:52:28.012Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:28.017Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:28.017Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:28.028Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51679,"url":"http://127.0.0.1:51679/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:28.183Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:28.278Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:28.278Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:28.278Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:28.282Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51682,"url":"http://127.0.0.1:51682/mcp","host":"127.0.0.1"}}
 ✓ tests/echo-home/adapters/markers.test.ts (9 tests) 100ms
{"timestamp":"2026-06-02T16:52:28.436Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
To /private/var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-rq-origin-JxEQXY
 * [new branch]      main -> main
{"timestamp":"2026-06-02T16:52:28.507Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:28.507Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:28.507Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:28.508Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51685,"url":"http://127.0.0.1:51685/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:28.516Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/coord/coord-invoke-spawns-wrapper.test.ts (3 tests) 574ms
 ✓ tests/echo-home/default-roles.test.ts (10 tests) 403ms
{"timestamp":"2026-06-02T16:52:29.562Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:29.584Z","level":"info","source":"coord.deadlines","message":"deadline_missed","payload":{"atom_id":"37c73e5d-d24f-43bb-b9b7-69544546f3df","tier":"round","subject_role":"codex","opened_event_type":"reviewer_invoked","expected_event_type":"tick_start","key":"round-001","expected_by":"2026-05-16T10:00:30.000Z"}}
{"timestamp":"2026-06-02T16:52:29.594Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:29.601Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
 ✓ tests/coord/deadlines-fire-once-and-remove.test.ts (3 tests) 49ms
 ✓ tests/echo-home/adapters/workflow-sync.test.ts (7 tests) 138ms
From /private/var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-rq-044-origin-Xd8uo0
 * branch            main       -> FETCH_HEAD
   e88ba5c..567470d  main       -> origin/main
 ✓ tests/cli/workflow-match.test.ts (5 tests) 144ms
 ✓ tests/review-queue/044-autostash-dirty-tree.test.ts (1 test) 14790ms
   ✓ 044 AC1 — autostash on every watcher-transaction git pull > dirty journal + remote-advanced origin: full transaction succeeds, dirty file preserved, no PUSH-RACE-FALLBACK rows 14775ms
 ✓ tests/review-queue/default-deploy-baseline.test.ts (2 tests) 1645ms
   ✓ 043 AC7 — default-deploy baseline (codex + cursor happy path) > AC7: codex+cursor both proceed → byte-identical combined.md 1203ms
   ✓ 043 AC7 — default-deploy baseline (codex + cursor happy path) > AC7b: codex absent past 2h timeout, cursor proceed → partial_responses + auto-disposition (044 AC4) 441ms
 ✓ tests/review-queue/e2e.test.ts (1 test) 5700ms
   ✓ review-queue e2e (AC6a synthetic) > scripted R1 → R2 cycle with orphan-tmp injection produces correct combined.md and is dispatch-message-free 5698ms
 ✓ tests/coord/non-pollution-three-way.test.ts (5 tests) 22ms
 ✓ tests/mcp/envelope-find-get-chain.test.ts (2 tests) 82ms
 ✓ tests/review-queue/046-task-state-ref-and-fresh-eyes.test.ts (9 tests) 5405ms
   ✓ 046 — request.md task_state_ref extension > request without task_state_ref still validates (backwards-compat) 1525ms
   ✓ 046 — request.md task_state_ref extension > request with task_state_ref validates when shape matches 502ms
   ✓ 046 — request.md task_state_ref extension > request with malformed task_state_ref rejected 447ms
   ✓ 046 — reviewer fresh-eyes enforcement > NEGATIVE: reviewer body that names task_state_ref as a critique target validates 476ms
   ✓ 046 — reviewer fresh-eyes enforcement > NEGATIVE: reviewer body with single required-block name quoted as example validates 408ms
   ✓ 046 — reviewer fresh-eyes enforcement > NEGATIVE: two of the six markers is below the threshold and validates 553ms
   ✓ 046 — reviewer fresh-eyes enforcement > POSITIVE: reviewer body with three required-block headings is rejected 351ms
   ✓ 046 — reviewer fresh-eyes enforcement > POSITIVE: consumed_task_state: true is explicitly rejected 409ms
   ✓ 046 — reviewer fresh-eyes enforcement > consumed_task_state: false (or omitted) validates 724ms
 ✓ tests/task-state/lint.test.ts (9 tests) 2087ms
   ✓ tools/task-state/lint.py > (a) under-cap pointer with all blocks in order passes 427ms
{"timestamp":"2026-06-02T16:52:34.754Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:34.763Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:34.763Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:34.780Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51692,"url":"http://127.0.0.1:51692/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:34.941Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:35.030Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:35.030Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:35.034Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:35.040Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51694,"url":"http://127.0.0.1:51694/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:35.135Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
Tracked wired agents: claude-code
codex: markers stripped /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-iE4zQs/home/.codex/AGENTS.md
codex: config noop /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-iE4zQs/home/.codex/config.toml
claude-code: markers stripped /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-iE4zQs/home/.claude/CLAUDE.md
claude-code: skills removed=0 skipped=0
cursor: config removed /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-iE4zQs/home/.cursor/mcp.json
ECHO state kept at /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-iE4zQs/echo-home
{"timestamp":"2026-06-02T16:52:35.180Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:35.180Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:35.180Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:35.181Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51696,"url":"http://127.0.0.1:51696/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:35.299Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
refusing --purge-state with cleanup conflicts; resolve manually or re-run with --force-purge
Tracked wired agents: claude-code
codex: markers noop /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/home/.codex/AGENTS.md
codex: config noop /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/home/.codex/config.toml
claude-code: markers conflict /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/home/.claude/CLAUDE.md
claude-code: skills removed=0 skipped=0
cursor: config noop /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/home/.cursor/mcp.json
Cleanup conflicts:
- claude-code /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/home/.claude/CLAUDE.md: symlink-target
ECHO state kept at /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/echo-home
WARNING: purging state with residual ECHO config left behind
Tracked wired agents: claude-code
codex: markers noop /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/home/.codex/AGENTS.md
codex: config noop /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/home/.codex/config.toml
claude-code: markers conflict /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/home/.claude/CLAUDE.md
claude-code: skills removed=0 skipped=0
cursor: config noop /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/home/.cursor/mcp.json
Cleanup conflicts:
- claude-code /var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-uninstall-VpPGXE/home/.claude/CLAUDE.md: symlink-target
Removed ECHO state.
 ✓ tests/cli/uninstall.test.ts (3 tests) 954ms
   ✓ runUninstall > cleans wired adapters and keeps state by default 547ms
{"timestamp":"2026-06-02T16:52:35.443Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:35.443Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:35.443Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:35.443Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51699,"url":"http://127.0.0.1:51699/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:35.491Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:35.517Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:35.517Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:35.517Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:35.518Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51701,"url":"http://127.0.0.1:51701/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:35.579Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:35.616Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:35.616Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:35.616Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:35.616Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51703,"url":"http://127.0.0.1:51703/mcp","host":"127.0.0.1"}}
 ✓ tests/review-queue/push-with-retry-rebase-merges.test.ts (1 test) 8795ms
   ✓ 051 AC1 — push-with-retry.sh preserves merge commits via --rebase=merges > local in-flight merge commit + sibling non-FF push: rebase preserves two-parent shape 8787ms
{"timestamp":"2026-06-02T16:52:35.676Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/cli/workflow-dispatch.test.ts (5 tests) 168ms
{"timestamp":"2026-06-02T16:52:35.702Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:35.703Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:35.703Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:35.703Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51706,"url":"http://127.0.0.1:51706/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:35.718Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:35.732Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:35.732Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:35.732Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:35.733Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51708,"url":"http://127.0.0.1:51708/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:35.748Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:35.787Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:35.787Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:35.787Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:35.788Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51710,"url":"http://127.0.0.1:51710/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:35.803Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:35.835Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:35.836Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:35.837Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:35.839Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51712,"url":"http://127.0.0.1:51712/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:35.872Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:35.883Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:35.883Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:35.883Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:35.884Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51714,"url":"http://127.0.0.1:51714/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:35.903Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/coord/coord-invoke-input-validation.test.ts (11 tests) 1252ms
 ✓ tests/trace/labels.test.ts (9 tests) 71ms
 ✓ tests/coord/identity-spoof-rejection.test.ts (10 tests) 20ms
 ✓ tests/cli/project.test.ts (6 tests) 864ms
To /private/var/folders/bb/rkp9hqh54t742qncslfq5dc40000gn/T/echo-rq-origin-aFfX4w
 * [new branch]      main -> main
 ✓ tests/coord/wait-for-new-turns-source-prefix.test.ts (6 tests) 61ms
 ✓ tests/logging/index.test.ts (9 tests) 71ms
 ✓ tests/task-state/push-round-state.test.ts (3 tests) 33978ms
   ✓ 046 AC4 — push-round-state.sh blob-lease > B aborts when A lands a competing round-state rewrite first; queue-errors row is committed locally and on origin 9709ms
   ✓ 046 AC4 — push-round-state.sh blob-lease > helper refuses to start when an unrelated tracked file is dirty; unrelated file is preserved 3451ms
   ✓ 046 AC4 — push-round-state.sh blob-lease > two simultaneous abort-loggers each produce a unique per-event file; no rebase conflict; both reach origin/main 20816ms
 ✓ tests/storage/get-by-ids.test.ts (12 tests) 183ms
 ✓ tests/storage/migrate.test.ts (6 tests) 114ms
 ✓ tests/echo-home/scaffold.test.ts (3 tests) 895ms
   ✓ ensureEchoHome > creates the directory tree and initial state files in a fresh ECHO_HOME 654ms
 ✓ tests/normalize/artifacts.test.ts (14 tests) 76ms
 ✓ tests/coord/coord-emit-per-tier-input.test.ts (7 tests) 32ms
{"timestamp":"2026-06-02T16:52:39.794Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:39.797Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:39.797Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:39.807Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51719,"url":"http://127.0.0.1:51719/mcp","host":"127.0.0.1"}}
 ✓ tests/normalize/dispatch.test.ts (7 tests) 96ms
 ✓ tests/cli/workflow-load.test.ts (3 tests) 56ms
{"timestamp":"2026-06-02T16:52:40.146Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/coord/causality-reviewer-invoked-before-tick-start.test.ts (1 test) 491ms
   ✓ 057b AC0 step 4 — reviewer_invoked precedes child tick_start > reviewer_invoked durable append precedes tick_start in append order 482ms
 ✓ tests/storage/metadata-match.test.ts (10 tests) 157ms
 ✓ tests/echo-home/wizard/adapter-cache.test.ts (6 tests) 1083ms
   ✓ adapter cache > returns null when the cache file is missing 589ms
 ✓ tests/review-queue/commit-reviewer-response.test.ts (2 tests) 14297ms
   ✓ commit-reviewer-response.sh > valid response: validates, commits, and pushes to origin 9072ms
   ✓ commit-reviewer-response.sh > malformed response: quarantines file, appends queue-errors row, exits non-zero, never commits 5223ms
{"timestamp":"2026-06-02T16:52:41.111Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:41.164Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
 ✓ tests/coord/internal-emitter-attribution.test.ts (3 tests) 82ms
 ✓ tests/retrieval-eval/schema.test.ts (3 tests) 40ms
{"timestamp":"2026-06-02T16:52:42.027Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:42.032Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:42.032Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:42.044Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51736,"url":"http://127.0.0.1:51736/mcp","host":"127.0.0.1"}}
 ✓ tests/mcp/util/fs-exclusion.test.ts (4 tests) 44ms
 ✓ tests/tools/install-pre-commit-hook.test.ts (6 tests) 15280ms
   ✓ install-pre-commit-hook.sh > fresh install: writes hook, marks executable 2119ms
   ✓ install-pre-commit-hook.sh > idempotent re-install: no rewrite, executable preserved, mtime unchanged 3695ms
   ✓ install-pre-commit-hook.sh > mode-repair re-install: chmod -x first, installer repairs mode in place 3134ms
   ✓ install-pre-commit-hook.sh > content-differs re-install: overwrites with warning 1922ms
   ✓ install-pre-commit-hook.sh > linked-worktree: hook resolves into the main repo hooks dir 2321ms
   ✓ install-pre-commit-hook.sh > relative core.hooksPath from nested cwd: resolves against repo root, not cwd 2086ms
 ✓ tests/coord/paths-resolution.test.ts (12 tests) 409ms
   ✓ 057b AC0 — paths resolution > resolveReviewerWrapperPath("codex") returns existing executable wrapper 362ms
 ✓ tests/mcp/wire-shape/tool-calls.test.ts (5 tests) 26ms
 ✓ tests/review-queue/push-with-retry-cwd-agnostic.test.ts (4 tests) 32872ms
   ✓ 050 AC5 — push-with-retry.sh CWD-agnostic + HEAD:main refspec > (a) commits pushed from the live main checkout land on origin/main as fast-forward 5401ms
   ✓ 050 AC5 — push-with-retry.sh CWD-agnostic + HEAD:main refspec > (d) commits pushed from a detached-HEAD worktree at $TMPDIR/echo-<role>-<uuid> land on origin/main; rev-parse origin/main matches WT HEAD (HEAD:main refspec is load-bearing) 6164ms
   ✓ 050 AC5 — push-with-retry.sh CWD-agnostic + HEAD:main refspec > (b)+(c) under simulated concurrent push, the helper rebases-and-retries; first attempt loses, second wins; exit 0 from both CWDs 15944ms
   ✓ 050 AC5 — push-with-retry.sh CWD-agnostic + HEAD:main refspec > queue-errors.md is written under the toplevel of the invoking CWD on exhaustion (CWD-agnostic path resolution) 5361ms
{"timestamp":"2026-06-02T16:52:43.596Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:43.600Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:43.600Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:43.627Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51738,"url":"http://127.0.0.1:51738/mcp","host":"127.0.0.1"}}
 ✓ tests/normalize/adapters/claude-code.test.ts (12 tests) 29ms
 ✓ tests/backlog/process-backlog-skill.test.ts (5 tests) 2169ms
   ✓ 048 AC5 — skills/process-backlog.md final builder-state hook > .claude/commands/process-backlog.md is byte-identical to the canonical skill after sync 2146ms
{"timestamp":"2026-06-02T16:52:43.819Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/coord/coord-invoke-cwd-independent.test.ts (1 test) 337ms
   ✓ 057b AC0 — coord_invoke cwd-independent (r2 codex-ops F4 HIGH) > coord_invoke succeeds after process.chdir("/") 335ms
{"timestamp":"2026-06-02T16:52:43.852Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/coord/no-pre-push-spawn.test.ts (1 test) 1943ms
   ✓ 057b AC7 — request.py emits zero coord atoms > writes correlation_id but performs NO MCP coord calls 1940ms
 ✓ tests/echo-home/adapters/role-sync.test.ts (4 tests) 52ms
{"timestamp":"2026-06-02T16:52:44.707Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:44.736Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:44.736Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:44.773Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51741,"url":"http://127.0.0.1:51741/mcp","host":"127.0.0.1"}}
 ✓ tests/echo-home/paths.test.ts (3 tests) 786ms
   ✓ ECHO home paths > defaults to ~/.echo when ECHO_HOME is unset 552ms
{"timestamp":"2026-06-02T16:52:44.931Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
{"timestamp":"2026-06-02T16:52:45.031Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:45.031Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:45.042Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:45.048Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51743,"url":"http://127.0.0.1:51743/mcp","host":"127.0.0.1"}}
 ✓ tests/normalize/adapters/cursor.test.ts (10 tests) 97ms
 ✓ tests/normalize/adapters/git.test.ts (8 tests) 32ms
 ✓ tests/normalize/adapters/codex.test.ts (8 tests) 21ms
{"timestamp":"2026-06-02T16:52:45.843Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/coord/coord-invoke-fire-and-forget.test.ts (2 tests) 1268ms
   ✓ 057b AC0 — fire-and-forget spawn timing > single coord_invoke returns under 1000ms 356ms
   ✓ 057b AC0 — fire-and-forget spawn timing > 10 coord_invoke calls each return promptly without daemon stall 911ms
 ✓ tests/cli/prompt.test.ts (3 tests) 193ms
 ✓ tests/cli/inverse-codex-config.test.ts (3 tests) 50ms
 ✓ tests/review-queue/yaml-error-handling.test.ts (2 tests) 2429ms
   ✓ validate.py — malformed YAML rejection (AC1) > AC1a: cursor.md with embedded `""` literal (the real 040 R1 incident) is rejected cleanly 1950ms
   ✓ validate.py — malformed YAML rejection (AC1) > AC1b: cursor.md with a stray tab inside a flow mapping is rejected cleanly 478ms
 ✓ tests/retrieval-eval/scorer.test.ts (3 tests) 597ms
   ✓ retrieval eval scoring > classifies the signal-vs-noise alias gap as an expected current failure 303ms
 ✓ tests/trace/role.test.ts (6 tests) 94ms
 ✓ tests/mcp/tools/echo-ping.test.ts (2 tests) 2298ms
   ✓ echo_ping (item 025: outputSchema + structuredContent + readOnlyHint) > tools/list advertises outputSchema and readOnlyHint annotation 1899ms
   ✓ echo_ping (item 025: outputSchema + structuredContent + readOnlyHint) > tools/call returns both `content` (text JSON) and `structuredContent` whose JSON content matches 398ms
 ✓ tests/review-queue/request.test.ts (7 tests) 7630ms
   ✓ request.py > happy path: writes a valid request.md 1372ms
   ✓ request.py > --class=structural-reform is reflected in frontmatter 637ms
   ✓ request.py > item not found: clear error 340ms
   ✓ request.py > race-loser, same SHA: exit 0 (same-SHA idempotency) 1427ms
   ✓ request.py > race-loser, different SHA: exit non-zero with bump-or-fix-history message 1215ms
   ✓ request.py > requested_reviewers validation: out-of-enum reviewer rejected 749ms
   ✓ request.py > frontmatter validates against schemas/request.schema.json 1885ms
 ✓ tests/cli/inverse-markers.test.ts (2 tests) 76ms
 ✓ tests/echo-home/wizard/render-echo-section.test.ts (4 tests) 13ms
 ✓ tests/coord/daemon-down-tolerance.test.ts (3 tests) 1200ms
   ✓ 057b AC0/AC7 — daemon-down tolerance > coord-emit.sh tick_start against dead port exits 0 538ms
   ✓ 057b AC0/AC7 — daemon-down tolerance > coord-emit.sh scheduler_health against dead port exits 0 557ms
 ✓ tests/cli/inverse-cursor-config.test.ts (2 tests) 49ms
 ✓ tests/retrieval-eval/run.test.ts (3 tests) 527ms
   ✓ retrieval eval runner > runs the full committed suite and distinguishes expected current failures 435ms
 ✓ tests/retrieval-eval/determinism.test.ts (3 tests) 500ms
   ✓ retrieval eval determinism > keeps statuses and fixture refs stable across random MemoryStorage IDs 426ms
 ✓ tests/retrieval-eval/cases.test.ts (2 tests) 96ms
 ✓ tests/mcp/wire-shape/compact-rank-reason.test.ts (2 tests) 12ms
 ✓ tests/normalize/roundtrip.test.ts (4 tests) 29ms
 ✓ tests/smoke.test.ts (1 test) 16ms
 ✓ tests/task-state/anchors.test.ts (9 tests) 48ms
{"timestamp":"2026-06-02T16:52:51.849Z","level":"info","source":"mcp.server","message":"coord_roles_loaded","payload":{"role_count":4}}
{"timestamp":"2026-06-02T16:52:51.852Z","level":"info","source":"coord.deadlines","message":"reconstruct_complete","payload":{"high_seq":0,"round_open":0,"scheduler_open":0,"idempotency_cache_size":0}}
{"timestamp":"2026-06-02T16:52:51.852Z","level":"info","source":"mcp.server","message":"deadlines_started","payload":{}}
{"timestamp":"2026-06-02T16:52:51.861Z","level":"info","source":"mcp.server","message":"started","payload":{"port":51788,"url":"http://127.0.0.1:51788/mcp","host":"127.0.0.1"}}
{"timestamp":"2026-06-02T16:52:51.862Z","level":"info","source":"mcp.server","message":"stopped","payload":{}}
 ✓ tests/coord/coord-roles-cwd-independent-path.test.ts (1 test) 92ms
 ✓ tests/review-queue/worktree-isolation.test.ts (5 tests) 54225ms
   ✓ 050 AC6 — worktree-isolation collision-simulation > AC6.1 — two simultaneous reviewer ticks (codex + codex-ops) for the same round: both produce their own <reviewer>.md; no cross-commit sweep; push rebases cleanly 9488ms
   ✓ 050 AC6 — worktree-isolation collision-simulation > AC6.2 — reviewer tick during in-flight merger tick (the 2026-05-14 14:02 collision shape): both succeed; neither commit captures the other staged work 9750ms
   ✓ 050 AC6 — worktree-isolation collision-simulation > AC6.3 — watcher tick during a reviewer tick: identical isolation property; multi-step watcher commits land cleanly; reviewer response commit lands cleanly; no shared-index race 11056ms
   ✓ 050 AC6 — worktree-isolation collision-simulation > AC6.4 — registered worktrees are NEVER GC’d regardless of mtime; only unregistered $TMPDIR/echo-* orphans older than 60min are removed 7223ms
   ✓ 050 AC6 — worktree-isolation collision-simulation > AC6.5 — the deleted sentinel-file is never created by any AC1/AC2/AC3 path; running the role flows leaves $repo/.git clean of the convention 16705ms
 ❯ tests/mcp/recent-calls-endpoint.test.ts (2 tests | 1 failed) 22986ms
   × GET /mcp/recent-calls > logs every runtime-registered tool through the wrapper 22945ms
     → Test timed out in 15000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
 ✓ tests/sync-skills/install-echo-codex-skills.test.ts (8 tests) 30486ms
   ✓ install-echo-codex-skills.sh > installs all canonical skills under the ECHO namespace 4164ms
   ✓ install-echo-codex-skills.sh > renders Codex-shaped frontmatter and preserves canonical body 3873ms
   ✓ install-echo-codex-skills.sh > records source, commit, content hash, and installer name in the sentinel 3813ms
   ✓ install-echo-codex-skills.sh > refreshes managed installs when canonical content changes 7194ms
   ✓ install-echo-codex-skills.sh > refuses to overwrite non-managed existing targets 2311ms
   ✓ install-echo-codex-skills.sh > --dry-run prints planned installs without writing files 1394ms
   ✓ install-echo-codex-skills.sh > supports custom namespace 4068ms
   ✓ install-echo-codex-skills.sh > supports underscore-visible names 3657ms
 ❯ tests/cli/shell-reachable.test.ts (1 test | 1 failed) 66075ms
   × echoctl shell reachability > packs an echoctl binary reachable from bash and exercises transitive doctor imports 66074ms
     → daemon com.echo.daemon.test-51038-1780419176749 did not become healthy on port 43249; run `echoctl daemon logs --tail 50 --label com.echo.daemon.test-51038-1780419176749` and reinstall or rollback
: expected 1 to be +0 // Object.is equality
 ✓ tests/review-queue/056-claude-reviewer-onboarding.test.ts (16 tests) 72342ms
   ✓ 056 AC1b/AC5 part 1 — _reviewers.py loader > loads all 4 reviewer slugs (codex, cursor, codex-ops, claude) without ValueError 895ms
   ✓ 056 AC1b/AC5 part 1 — _reviewers.py loader > asserts non-empty invoke_command for headless reviewers; None for ide (cursor) 301ms
   ✓ 056 AC1b/AC5 part 1 — _reviewers.py loader > --print invoke_command for cursor (IDE-mode) exits non-zero with documented diagnostic 632ms
   ✓ 056 AC1b/AC5 part 1 — _reviewers.py loader > --print invoke_command for headless reviewers exits 0 and prints resolved template 1719ms
   ✓ 056 AC1b/AC5 part 1 — _reviewers.py loader > argv-snapshot equivalence: codex/codex-ops resolved template matches pre-AC5 argv 1202ms
   ✓ 056 AC1b/AC5 part 1 — _reviewers.py loader > AC5 part 3 — shell-safe substitution: paths with spaces survive shlex.quote 528ms
   ✓ 056 AC5 part 4 — queue_error.sh row shapes > pre-spawn shape — no spec fields when called without artifact/sha 6184ms
   ✓ 056 AC5 part 4 — queue_error.sh row shapes > per-round shape — includes spec=<artifact_path>@<spec_commit_sha> 7246ms
   ✓ 056 AC7b/AC8 — _install_reviewer_launchd.sh > preflights resolved invoke_command executable (fails-closed when CLI absent) 779ms
   ✓ 056 AC9 — wrapper end-to-end with mock-claude > produces claude.md + commits via mock-claude (produce_response mode) 28373ms
   ✓ 056 AC9 — wrapper end-to-end with mock-claude > sha_drift mode → wrapper exits non-zero + per-round queue-error row on origin/main 23220ms
 ✓ tests/skills/atomic-state-transition-harness.test.ts (36 tests) 134328ms
   ✓ AC1 — generic P1 harness contract > accepts a target-not-visible-before-publish consumer (local-commit boundary) 5468ms
   ✓ AC1 — generic P1 harness contract > accepts a target-visible-before-publish consumer with finishUnpublishedTransition (pushed-ref boundary) 8334ms
   ✓ AC1 — generic P1 harness contract > rejects a fixture whose touched surface is outside its declared allowed prefixes 2535ms
   ✓ AC1 — generic P1 harness contract > rejects a fixture whose pre-publish step dirties an undeclared surface 3039ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 1 — pre-publish edits do not expose the target stage 5466ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 2 — publish creates one durable boundary containing all required surfaces (local commit AND remote ref) 12005ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 3 — committed destination contents include the edited handoff metadata (local AND remote) 12672ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 3b — lint failure is fail-fast before git mv, commit, or push 8215ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 4 — crash after metadata edit before git mv is recovered without operator decision; replay publishes 15527ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 5 — crash after git mv but before git add $DEST is recovered; replay publishes 7556ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 6 — recovery refuses unsafe surfaces (absolute, ../ traversal, outside allowed prefixes) 3834ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 7 — running recovery after a published commit is a no-op 5329ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 8 — partial pre-git-mv: $ITEM_FILE dirty, $DEST/$POINTER absent — per-surface dispatch handles cleanly 3916ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 9 — crash after commit before push: finished by CALLER finish path (not by recover() rollback) 7653ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 10 — $LOG is preserved through recovery (never touched by rollback) 3893ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 11a — recovery returns 2 on prefix-guard violation; pull does not run 2478ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 11b — recovery returns 5 when post-rollback dirty-check fails; pull does not run 6547ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 11c — recovery returns 4 when per-surface dispatch fails (rm/restore real error) 2964ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 12 — untracked-cleanup branch surfaces real errors rather than hiding them 8099ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 13 — tracked-dirty $LOG does not block the post-recovery pull (--autostash) 4505ms
   ✓ AC3 — process-backlog stage move (current-consumer specialization) > test 14 — tracked-dirty raw/internal/queue-errors.md does not block the post-recovery pull 4254ms

⎯⎯⎯⎯⎯⎯⎯ Failed Tests 2 ⎯⎯⎯⎯⎯⎯⎯

 FAIL  tests/cli/shell-reachable.test.ts > echoctl shell reachability > packs an echoctl binary reachable from bash and exercises transitive doctor imports
AssertionError: daemon com.echo.daemon.test-51038-1780419176749 did not become healthy on port 43249; run `echoctl daemon logs --tail 50 --label com.echo.daemon.test-51038-1780419176749` and reinstall or rollback
: expected 1 to be +0 // Object.is equality

- Expected
+ Received

- 0
+ 1

 ❯ tests/cli/shell-reachable.test.ts:147:60
    145|           encoding: 'utf8',
    146|         });
    147|         expect(daemonInstall.status, daemonInstall.stderr).toBe(0);
       |                                                            ^
    148| 
    149|         const daemonStop = spawnSync('bash', ['-c', `echoctl daemon st…

⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[1/2]⎯

 FAIL  tests/mcp/recent-calls-endpoint.test.ts > GET /mcp/recent-calls > logs every runtime-registered tool through the wrapper
Error: Test timed out in 15000ms.
If this is a long-running test, pass a timeout value as the last argument or configure it globally with "testTimeout".
⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯⎯[2/2]⎯

 Test Files  2 failed | 140 passed | 1 skipped (143)
      Tests  2 failed | 1503 passed | 21 skipped (1526)
   Start at  09:51:57
   Duration  136.32s (transform 9.87s, setup 0ms, collect 55.99s, tests 764.59s, environment 37ms, prepare 36.84s)


codex
The full `npm test` run exited red: 140 files passed, 1 skipped, 2 failed; 1503 tests passed, 21 skipped, 2 failed. I’m running lint and typecheck next, then I’ll do a narrow rerun of the two failing tests to classify whether they reproduce.
exec
/bin/zsh -lc 'npm run lint' in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 5287ms:

> echoctl@0.1.0 lint
> eslint . --max-warnings 0 && npm run lint:task-state


> echoctl@0.1.0 lint:task-state
> python3 tools/task-state/lint.py


codex
`npm run lint` passed. I’m running `npm run typecheck` now.
exec
/bin/zsh -lc 'npm run typecheck' in /Users/zhenye/Desktop/Project_echo--install-profile-split
codex
Typecheck is running. After it completes, I’ll rerun only the two failed test files once so the final review can distinguish reproducible failures from full-suite interference.
 succeeded in 5187ms:

> echoctl@0.1.0 typecheck
> tsc --noEmit


codex
`npm run typecheck` passed. I’m rerunning the two failed test files individually now to check reproducibility without the full-suite load.
exec
/bin/zsh -lc 'npx vitest run tests/mcp/recent-calls-endpoint.test.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 11593ms:

 RUN  v2.1.9 /Users/zhenye/Desktop/Project_echo--install-profile-split

 ✓ tests/mcp/recent-calls-endpoint.test.ts (2 tests) 8072ms
   ✓ GET /mcp/recent-calls > logs every runtime-registered tool through the wrapper 8057ms

 Test Files  1 passed (1)
      Tests  2 passed (2)
   Start at  10:00:19
   Duration  9.99s (transform 494ms, setup 0ms, collect 759ms, tests 8.07s, environment 0ms, prepare 141ms)


codex
The focused rerun of `tests/mcp/recent-calls-endpoint.test.ts` is still running; this is the same test that timed out under the full suite.
codex
The recent-calls test passes in isolation: 2/2 in about 10s. I’m rerunning the shell-reachability smoke next; that one exercises packaged daemon startup and may take longer.
exec
/bin/zsh -lc 'npx vitest run tests/cli/shell-reachable.test.ts' in /Users/zhenye/Desktop/Project_echo--install-profile-split
codex
The shell-reachability test is still running. I’ll let it complete because it is the only remaining failed gate to classify.
 succeeded in 26883ms:

 RUN  v2.1.9 /Users/zhenye/Desktop/Project_echo--install-profile-split

 ✓ tests/cli/shell-reachable.test.ts (1 test) 23691ms
   ✓ echoctl shell reachability > packs an echoctl binary reachable from bash and exercises transitive doctor imports 23691ms

 Test Files  1 passed (1)
      Tests  1 passed (1)
   Start at  10:00:38
   Duration  25.39s (transform 184ms, setup 0ms, collect 577ms, tests 23.69s, environment 0ms, prepare 119ms)


codex
Both failed full-suite tests passed when rerun in isolation: recent-calls 2/2 and shell-reachability 1/1. I’m doing one final status check so the review doesn’t miss any test-created worktree dirt.
exec
/bin/zsh -lc 'git status --short' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git diff --name-only main...agent/install-profile-split' in /Users/zhenye/Desktop/Project_echo--install-profile-split
exec
/bin/zsh -lc 'git diff --name-status $(git merge-base main HEAD)..main -- assets/echo-skills/using-echo-coord.md assets/echo-skills/using-echo-mcp.md src/cli/commands/doctor.ts src/cli/commands/init.ts src/cli/io/render.ts src/echo-home/adapter-sync.ts src/echo-home/adapters/skill-sync.ts src/echo-home/paths.ts src/echo-home/wizard/run-wizard.ts src/echo-home/wizard/wire.ts tests/cli/doctor.test.ts tests/cli/init.test.ts tests/echo-home/adapter-sync.test.ts tests/echo-home/adapters/skill-sync.test.ts tools/foreign-install-smoke.sh' in /Users/zhenye/Desktop/Project_echo--install-profile-split
 succeeded in 176ms:
 succeeded in 207ms:
assets/echo-skills/using-echo-coord.md
assets/echo-skills/using-echo-mcp.md
src/cli/commands/doctor.ts
src/cli/commands/init.ts
src/cli/io/render.ts
src/echo-home/adapter-sync.ts
src/echo-home/adapters/skill-sync.ts
src/echo-home/paths.ts
src/echo-home/wizard/run-wizard.ts
src/echo-home/wizard/wire.ts
tests/cli/doctor.test.ts
tests/cli/init.test.ts
tests/echo-home/adapter-sync.test.ts
tests/echo-home/adapters/skill-sync.test.ts
tools/foreign-install-smoke.sh

 succeeded in 245ms:
