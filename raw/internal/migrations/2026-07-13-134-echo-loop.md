# Migration record — 2026-07-13-134 echo-loop source extraction (fix cycle; awaiting re-review)

**Status: fix cycle complete for the codex-ops REJECT; UNACCEPTED pending
re-review.** The prior handoff was rejected on four HIGH + one MEDIUM sealed-
contract gaps; all are now addressed and the full suite is green. The target is
not installed, has no remote, and remains a local `DEV` candidate; active
Project_echo stays authoritative.

- `authority: false`
- `installed: false`

## Honest AC accounting (no COMPLETE claimed over residuals)

97 tests pass across 21 files: coord 28, watcher 27, workflows 4, task-state 16,
review-queue 9, migration 13. `tsc -p tsconfig.json --noEmit` clean. Dual-route
`verify:extraction` runs the full 14-row roster with a byte-identical inner
projection, verdict pass.

- **AC1 DONE.** Isolated repo; fsck clean; single-root; no remote/alternates/
  promisor/replace.
- **AC2 DONE.** Sealed policy verified + byte-identically copied; 148-row
  source-seed; 76 byte-identical ports (`check:provenance`); edge-record fixed
  point (78 runtime edges resolved exactly once); `check:dependencies`
  (no lock drift, no extraneous), `check:skills` (no drift), `lint`; independent
  source-seed fixture oracle. **Design note (non-blocking, not a residual):** the
  lock is npm-generated (drift/extraneous verified), not hand-minimized.
- **AC3 DONE.** `./coord` better-sqlite3 publication protocol + atomic init;
  28 tests; native build via the sole named
  `npm rebuild better-sqlite3 --offline --foreground-scripts` row (AC7).
- **AC4 DONE.** Templates + echo-loop's own empty queue; no Project_echo history.
- **AC5 DONE.** Watcher force-with-lease CAS + all recovery fixtures:
  crash-before/after-push, `git gc --prune=now` anchor survival, both watcher
  orders, mismatched-digest-after-transition; own-process-group isolation
  implemented (detached spawn) with a deadline-bound reap fixture; 27 tests.
- **AC6 DONE.** Local fixture loop on disposable repos; 4 tests.
- **AC7 DONE.** Dual-route verifier over the FULL sealed roster (task-state,
  review-queue, coord, workflows, full-tests, source-independence added), byte-
  identical inner projection + differing route records; the npm route binds its
  outer launcher argv + npm banner + user-agent (F5); offline install matrix
  (private clone, sandbox-exec deny-network denies DNS + direct-IP, offline
  `npm ci`, sole named better-sqlite3 rebuild, binary loads).
- **AC8 builder leg DONE (this record).** The independent codex-ops child-commit
  ceremony is the reviewer's leg, not the builder's.

## Fix-cycle dispositions (against codex-ops REJECT ca82e523)

- **F1** — `tests/task-state/` (anchors fixture-conformance via `parse-anchors`,
  schema via ported `lint.py`, line cap, ref pinning) + `tests/review-queue/`
  (request SHA-pinning + race-loser via `request.py`, reviewer binding via
  `_reviewer_gate.py`, response validation + fresh-eyes via `validate.py`).
  `test:task-state` and `test:review-queue` now exit 0 with real tests.
- **F2** — `verification-workload.v1.json` completed to the full sealed roster;
  recursion broken by `vitest.workload.config.ts` (excludes the verifier + the
  offline source-independence row).
- **F3** — `tests/migration/source-seed-fixture.test.ts` with an INDEPENDENT
  oracle (expected bytes from raw `git cat-file`, not the resolver).
- **F4** — the five AC5 recovery fixtures above + own-pgid isolation.
- **F5** — npm route envelope now binds the outer launcher argv + npm banner.
- Portability fix: node here is x86_64 (Rosetta); node-spawned `python3` could not
  load jsonschema's arm64 wheel, so the python-invoking tests run under
  `arch -arm64`.

## Binding (final, fix cycle)

- **Source pin:** `Project_echo@2971310441b69735cbe759293abd8c4d044bf347`
- **Sealed source policy:** blob `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`,
  sha256 `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a`
- **Target HEAD:** `2aeb1ede21b16d47f9d11da69f0a3cb10425ddb6`
- **Target tree:** `a56fe5e042fbcfa609918fe7b61c420805421880` (14 commits)
- **source-seed.v1.json:** blob `5278839736a53a82bfce884ea16eac0b254437ed`,
  sha256 `c6121b5092de071efebe1cd8a0c9f6cccbe3364f6edb3edd4548cf97618dd41a`
- **edge-record.v1.json fixed point:**
  `9044a61cde6ccf63031e5f88a7004bd7cdeb41c88bc28d51f3950c17393dda5f`
- **edge-record.v1.schema.json sha256:**
  `30d25c7fe55c27e2b79e06bbf38dbb410ab7e3ebbf3484c8ef8a408ef4429277`
- **watcher-project.v1.json sha256:**
  `2efa5f264a76b3326a7e783bc7ec2cde6dab70c257f8a86a7f1f354b005bd302`
- **verification-workload.v1.json sha256:**
  `5444243de498f28da61eceda9d818c76df8410cef82366fea1e779d630af5ce2`
- **package.json sha256:** `c1ec2d0e0e02a16ca24b4fbc45e249442b4cc136073f276df12748651c6616b7`
- **package-lock.json sha256:** `74c56cce3e6703137a1b0ba4b97bd5c6fa087a192fb28c7af795ac89e0a195ea`
- **Dual-route inner-result sha256 (direct == npm):**
  `8c81ece2fe3bd29ab97c50285bf51316316d8814078c1eb838f457db8c8c2377`

## Exclusions honored

No Project_echo history/wiki/raw/backlog archives copied; no product/context
capabilities. Rewrite-lane `.ts` beyond `src/coord`, `src/watcher`,
`src/task-state` and authored/generated manifests were not fabricated as
byte-identical ports. Passing proves only a local split; active Project_echo
remains authoritative.
