# Migration record — 2026-07-13-134 echo-loop source extraction (A–E campaign, INCOMPLETE checkpoint)

**Status: INCOMPLETE checkpoint of the A–E campaign; UNACCEPTED pending
re-review.** The second codex-ops review (child `c2a33138`) confirmed the prior
F1/F3/F4/F5 fixes but raised findings A–E. This checkpoint lands Findings D
(the real resolver), A (verify-not-overwrite), and B (envelope-invariant hash);
Findings C (watcher runtime invariants) and E (final AC8 record) remain. The
target is not installed, has no remote, and remains a local `DEV` candidate.

- `authority: false`
- `installed: false`

## Findings A–E status

- **Finding D — DONE.** `tools/lib/source-plan.mjs` (+ CLI `tools/build-source-plan.mjs`):
  loads the sealed policy by OID, scans the whole committed HEAD tree
  (TS/shell/python), full 14-class edge model with binding contexts (tsconfig
  `paths`, package.json `#imports`/`exports`, workspaces), stated resolution
  precedence, fail-closed computed/unknown rejection. Repository edges resolve to
  `path@blob-oid` (verified equal to git's real blob OID). Committed closure =
  **516 edges across 9 classes** (was 3 classes / 78 rows). Sealed fixture roster
  in `tests/migration/source-plan.test.ts`: each edge class + tsconfig/workspace
  alias, shell/python, metacharacter, queue-order determinism, transitive, cycle,
  computed-reject, unknown-reject.
- **Finding A — DONE.** `build-source-plan.mjs --check` verifies the committed
  edge record against the HEAD closure and NEVER writes (fail-closed on drift);
  the workload `source-plan` row uses `--check`; the test validates the COMMITTED
  artifact and asserts `--check` leaves the tree clean. Record regenerated to the
  true fixed point.
- **Finding B — DONE.** `tools/run-verification.mjs` spawns with a real HOME but
  tokenizes absolute roots (`<HOME>`/`<TMPDIR>`) in the inner projection —
  envelope-invariant hash (verified: two different HOMEs → identical inner).
- **Finding C — NOT DONE (next run).** Seven watcher gaps: (1) group-directed
  TERM-then-KILL via `kill(-pgid)` + PID/PGID + termination evidence; (2) a
  recovery fixture that spawns a real descendant and asserts full-group
  termination (not elapsed time); (3) expired-lease takeover requires prior-owner
  group-termination evidence before `attemptPush`; (4) `candidate.ts` must use
  `gitEnv()`'s config-free env (no ambient `process.env` in private-index git);
  (5) the APPLYING takeover path revalidates the approval token AND
  `row.repoIdentity`/`row.fullRef` vs the sealed project; (6) enforce
  `next_attempt_at`, escalate repeated identical failures, and ESCALATE (not
  APPROVED) on ambiguous post-push state; (7) delete `prepared/` + `-base` refs on
  terminal state.
- **Finding E — NOT DONE (next run).** A new immutable migration record with the
  full AC8 bindings — exact commands + exit results, target path/branch,
  target-policy-copy OID/SHA binding, truthful clean-status, the corrected fixed
  point, and the envelope-invariant dual-route hash.

## Current binding (checkpoint)

- **Source pin:** `Project_echo@2971310441b69735cbe759293abd8c4d044bf347`
- **Sealed source policy:** blob `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`,
  sha256 `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a`
- **Target HEAD:** `3fe5383b49e5256b45d5d1ee9993f1ea92dfdcbd`
- **Target tree:** `95754d748ab9ac34623974b8704532859190f28d` (22 commits)
- **edge-record.v1.json fixed point:**
  `6a8f6a61a7d4d58c10d852e3fb77d04a75ad60e7846224fc5bda5ede1a894a38`
- Full `vitest run`: **112 tests / 21 files pass**; `tsc --noEmit` clean;
  `git fsck --full` exit 0; tree clean; no remote.

## Exclusions honored

No Project_echo history/wiki/raw/backlog archives; no product/context. Passing
the landed findings proves only a local split; active Project_echo remains
authoritative.
