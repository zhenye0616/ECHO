# Migration record — 2026-07-13-134 echo-loop source extraction (sixth-review portability remediation)

**Status: sixth-review portability defect remediated; ready for a new
independent review; UNACCEPTED, not installed.** The sixth independent
`codex-ops` judgment (immutable rejection child
`e9ae6519070144a7dd04f98b5016ebe20b66ef38`) closed the fifth-review watcher
transport and tracked-clean repairs, then rejected one remaining defect:
Draft-07/source-plan validation selected mutable founder user-site Python state
and failed under the required scratch-HOME system toolchain. This record binds
the successor target that closes only that portability finding. The target
remains a local `DEV` candidate with no remote, installation, or authority;
Project_echo remains authoritative.

- `authority: false`
- `installed: false`

## Sixth-review defect — final disposition

- **Absolute schema identity — CLOSED.** The edge-record schema now declares
  the absolute reserved URI
  `https://echo-loop.invalid/provenance/edge-record.v1.schema.json`; the JS
  boundary rejects a missing or relative `$id`. Local fragment references now
  resolve identically under the accepted system `jsonschema` 4.16.0 instead of
  duplicating a relative base.
- **Validator isolation — CLOSED.** Draft-07 validation launches exact
  `/usr/bin/arch -arm64 /usr/local/bin/python3 -I -B -X utf8` with a newly
  allocated scratch HOME/TMPDIR and an explicit minimal environment. It does
  not inherit `PYTHONPATH`, `PYTHONHOME`, user-site selection, proxy, shell, or
  other Python state. The existing mode-0600 outside-repository payload,
  ignored stdin, 30-second timeout, and unconditional cleanup are preserved.
- **Version-stable meta-validation — CLOSED.** The accepted system toolchain is
  Python 3.10.7 plus system `jsonschema` 4.16.0 from
  `/Library/Frameworks/Python.framework/Versions/3.10/lib/python3.10/site-packages/jsonschema/__init__.py`.
  The validator explicitly validates schemas with
  `Draft7Validator(Draft7Validator.META_SCHEMA,
  format_checker=FormatChecker())`; malformed regex patterns deterministically
  produce `invalid_schema`, while rejecting valid schemas still produce
  `validation_error`.
- **Regression proof — CLOSED.** New tests validate the exact committed
  artifact under scratch HOME, then under a poisoned user-site and
  `PYTHONPATH` containing a fake `jsonschema` package. Both pass through the
  isolated system toolchain. The dual-route test now executes direct→npm and
  npm→direct and compares all four inner result files byte-for-byte.

The successor target consists of two linear commits after the sixth-reviewed
target: `a2d9fb5` (absolute identity, isolated validator, meta-validation, and
regressions) and `e945e33` (regenerated source closure).

## Bindings (AC8)

### Source and target

| Binding | Value |
|---|---|
| Source pin | `Project_echo@2971310441b69735cbe759293abd8c4d044bf347` |
| Sealed source policy | Project_echo blob `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`; SHA-256 `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` |
| Target policy copy | `provenance/source-policy.v1.json` — git blob `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`; SHA-256 `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` (byte-identical to the sealed source object) |
| Target repository | `/Users/zhenye/Desktop/echo-loop` |
| Target branch | `migration/2026-07-13-134` |
| Target HEAD | `e945e33c498b5bb5689e30eb70952b688a6347df` |
| Target tree | `bbc6832871655c138aab31420037e9dec480a611` |
| Target history | 43 linear commits; one root and one branch; 487/487 objects reachable (43 commit / 185 tree / 259 blob); **no remote**, alternates, promisor, replace, graft, shallow state, tags, symlinks, or gitlinks; `git fsck --full --strict --no-reflogs --unreachable` exit 0 |
| Target filesystem | 177 tracked files = 177 non-`.git` filesystem files; no ignored or untracked residue; working tree clean after verification cleanup |
| Authority / install / maturity | `authority:false`; `installed:false`; local `DEV` candidate |

### Project_echo publication (AC8 feature-branch push target)

| Binding | Value |
|---|---|
| Publication endpoint (literal origin URL) | `https://github.com/zhenye0616/ECHO.git` |
| Full feature ref | `refs/heads/agent/134-echo-loop` |
| Expected-old before remediation publication | `e9ae6519070144a7dd04f98b5016ebe20b66ef38` (immutable sixth-judgment child) |

The AC8 feature-branch push and strict `ls-remote` probe address the literal
origin URL above, never a bare remote name. This is the Project_echo repository
which carries the migration and independent-review records; it is not a remote
of `/Users/zhenye/Desktop/echo-loop`, which remains remote-less.

### Manifest / artifact hashes

| Artifact | Git blob OID | SHA-256 |
|---|---|---|
| `provenance/source-policy.v1.json` | `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a` | `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` |
| `provenance/edge-record.v1.json` | `d4dc3e22971ee8aaf778b5b9654b3fd2829f0b67` | `f7db0c5ce6c2ee3a3bb71abec8342caa3185eb781feb3a3776647a09781cb487` |
| `provenance/edge-record.v1.schema.json` | `f8fe0ab1c58442dfa2dfb6375a1d3ca7475d52f7` | `209c9cea6e04a61ad037d07731b0e0700b75ed7e500bcce1cfb43baf034b702e` |
| `provenance/watcher-project.v1.json` | `af08720815120b4c40bc07ad1bf78977b1573f20` | `2efa5f264a76b3326a7e783bc7ec2cde6dab70c257f8a86a7f1f354b005bd302` |
| `provenance/verification-workload.v1.json` | `c367b354660af5ecd91789968bb668a2a03de963` | `ead0d267f78e6ecf8f940cb7c6d11bb5a4cf72de26be8876c189638bdcc84d0b` |
| `provenance/source-seed.v1.json` | `5278839736a53a82bfce884ea16eac0b254437ed` | `c6121b5092de071efebe1cd8a0c9f6cccbe3364f6edb3edd4548cf97618dd41a` |
| `package.json` | `afaedd015a49f98bf7bb3d154281665879f2aa10` | `c1ec2d0e0e02a16ca24b4fbc45e249442b4cc136073f276df12748651c6616b7` |
| `package-lock.json` | `b67ca3f905e5c87023198d47edaaf24f725edea2` | `74c56cce3e6703137a1b0ba4b97bd5c6fa087a192fb28c7af795ac89e0a195ea` |

- **edge-record fixed point:**
  `b9139e30a55e6b9dcf23bd3ecb3dfaa0fecfc65f58341ee452ebe5c570ae5054`
  (646 rows / 9 classes; 110 source blobs / 3 manifest blobs; class counts:
  138 node builtins, 31 npm packages, 137 Python modules, 3 Python scripts,
  7 repository file reads, 102 repository imports, 189 shell commands,
  6 shell scripts, and 33 system-toolchain helpers).
- **dual-route inner-result SHA-256:**
  `84aa6bad2081be452470a0db2fab11d463f479f290f3c46737ad60fa3987944f`
  — byte-identical across canonical direct→npm and reverse npm→direct order.
- **route-result SHA-256:** direct
  `0d1ee2b93ce479b61bb1ba379e1e2c29b2a93d919a45bf395d02ab9a4420ac2a`;
  npm `c0095e584c4a845b23c7b59ed26f3156d2e091816553c5915e623a4da9689e52`;
  reverse npm
  `b4b4d9a30a18dc36ca93e079a69a519b31362dcb4307c765872a7f9c29226982`;
  reverse direct
  `cc6ee0250a2ed834cbfe831ff5debfdc72beca80fa45e7506e402dc41b3eca63`.
- **Dependencies:** 5 declared, 132 locked packages, no root drift.
- **Skills:** 13 ported skills byte-identical to source.
- **Ports:** 76 byte-identical ports of 148 inventory rows.

## Commands + exits

All commands ran in `/Users/zhenye/Desktop/echo-loop` at exact HEAD/tree
`e945e33…` / `bbc6832…`. The validator itself always selected isolated system
Python 3.10.7 and `jsonschema` 4.16.0 even when the invoking Node process had a
scratch or poisoned Python environment.

| Command | Exit | Result |
|---|---|---|
| `/usr/local/bin/node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit` | 0 | typecheck clean |
| `/usr/local/bin/node node_modules/vitest/vitest.mjs run` | 0 | **24 files / 171 tests pass** in 571.59s |
| four-route test inside the full suite | 0 | direct→npm and npm→direct; four sealed workloads pass in 469.18s; all inner files byte-identical |
| focused source-plan suite | 0 | **48/48 pass**, including scratch HOME, poisoned user-site/PYTHONPATH, and malformed-regex meta-validation |
| focused watcher suite | 0 | **7 files / 53 tests pass**, preserving every prior launch/takeover/recovery fix |
| focused review-queue + verification-cleanliness suites | 0 | **4 files / 12 tests pass** |
| `/usr/local/bin/node tools/lint.mjs` | 0 | 46 source files, no stray debug |
| `/usr/local/bin/node tools/check-provenance.mjs` | 0 | sealed policy verified; 76 byte-identical ports of 148 inventory rows |
| `/usr/local/bin/node tools/check-dependencies.mjs` | 0 | 5 declared, 132 locked, no root drift |
| `/usr/local/bin/node tools/check-skills.mjs` | 0 | 13 ported skills byte-identical |
| `/usr/local/bin/node tools/build-source-plan.mjs --check` (twice before and twice after cleanup) | 0 | stable 646-row closure; fixed point `b9139e30…`; no writes |
| source-independence workload | 0 | fresh private clone at exact HEAD; deny-network offline install, native SQLite rebuild/load, origin removal, strict fsck clean |
| `/usr/local/bin/git fsck --full --strict --no-reflogs --unreachable` | 0 | 487/487 reachable objects; zero garbage |
| `/usr/local/bin/git status --porcelain=v1 --untracked-files=all --ignored` | — | empty after removing only known verifier directories and `node_modules/` |
| tracked-files vs non-`.git` filesystem comparison | 0 | exact equality at 177 files |
| `/usr/local/bin/git remote -v` | — | empty (no remote) |

### Dual-route verification workload (AC7)

The identical ordered 14-row workload passed in both route orders:

- canonical direct→npm: direct and npm both `verdict=pass rows=14`;
- reverse npm→direct: npm and direct both `verdict=pass rows=14`;
- all four `inner.json` files were byte-identical at SHA-256 `84aa6bad…`.

Roster, all status 0 and in this exact order: `provenance`, `dependencies`,
`skills`, `source-plan`, `task-state`, `review-queue`, `coord`, `workflows`,
`lint`, `typecheck`, `full-tests`, `source-independence`, `fsck`,
`tracked-clean`. The final row remains exactly
`["git","status","--porcelain=v1","--untracked-files=no"]` with expected
empty stdout.

## Exclusions / differences honored

The target delta from sixth-reviewed `c8ed1b0…` is exactly five paths (199
insertions / 74 deletions): the edge record and its schema, the Draft-07
wrapper, the source-plan portability tests, and the route-order test. No new
dependency, product/context/history code, watcher change, authority mechanism,
remote, installation, release, maturity advance, sibling change, wiki change,
or review judgment was introduced. No Project_echo review sidecar was edited.
Passing proves only a local source split and requests a fresh independent
review. This remediation made zero ECHO MCP calls, so no MCP journal entry was
added.
