# Migration record — 2026-07-13-134 echo-loop source extraction (A–E campaign COMPLETE)

**Status: A–E campaign complete; normal builder handoff to `pending_review`;
UNACCEPTED, not installed.** The second independent `codex-ops` review (child
`c2a33138`) confirmed the prior F1/F3/F5 fixes and F2's roster/equivalence
repair, and raised findings A–E. This record binds the completed state in which
all five are resolved on disposable fixtures only. The target is a local `DEV`
candidate: it has no remote, is not installed, and carries no authority. Passing
proves only a local split; active Project_echo remains the authoritative loop.

- `authority: false`
- `installed: false`

## Findings A–E — final dispositions

- **Finding D — DONE.** Real 14-edge-class source-plan resolver
  `tools/lib/source-plan.mjs` (+ CLI `tools/build-source-plan.mjs`): loads the
  sealed policy by OID, scans the whole committed HEAD tree (TS / shell / python),
  applies the full sealed edge model with binding contexts (tsconfig `paths`,
  `package.json` `#imports`/`exports`, workspaces), stated resolution precedence,
  and fail-closed computed/unknown-edge rejection. Repository edges resolve to
  `path@blob-oid` (verified equal to git's real blob OID, so the closure detects
  content drift). Committed closure = **538 rows across the 9 edge classes present
  in this tree** (`shell_literal_command` 148, `python_literal_module` 137,
  `node_builtin` 119, `repository_static_import` 86, `npm_package` 29,
  `shell_literal_script` 7, `system_toolchain_helper` 6,
  `repository_literal_file_read` 3, `python_literal_script` 3). The resolver
  supports all 14 sealed classes; classes with no instance in this target's tree
  correctly do not appear (fail-closed, not silently emitted). Sealed fixture
  roster in `tests/migration/source-plan.test.ts` exercises every edge class plus
  tsconfig/workspace alias, shell/python, metacharacter, queue-order determinism,
  transitive helper, cycle, computed-reject, and unknown-reject.
- **Finding A — DONE.** `build-source-plan.mjs --check` verifies the committed
  `provenance/edge-record.v1.json` against the recomputed HEAD closure and NEVER
  writes (fail-closed on any drift; non-zero exit). The workload `source-plan` row
  runs `--check`, so a stale committed record fails verification instead of being
  silently overwritten. `tests/migration/source-plan.test.ts` validates the
  committed artifact and asserts `--check` leaves the tree clean. The record is at
  its true fixed point (below).
- **Finding B — DONE.** `tools/run-verification.mjs` synthesizes a canonical
  workload env and tokenizes absolute roots (`<HOME>`, `<TMPDIR>`) in the
  route-invariant inner projection, so the inner hash is envelope-invariant.
  **Independently re-confirmed** in this run: the direct route under a
  deliberately different scratch `HOME`/`TMPDIR` produced the identical inner hash
  as the in-test-env run (`eb614a43…`, below). The founder-HOME coupling the
  reviewer flagged is gone; the bound hash reproduces under the mandated scratch
  envelope.
- **Finding C — DONE.** The seven AC5 watcher containment/takeover/recovery gaps
  are implemented in source and covered by **real** fixtures (real descendants,
  real process groups, disposable `file://` remotes) in
  `tests/watcher/containment.test.ts`:
  1. Group-directed **TERM-then-KILL** via `kill(-pgid, …)` with recorded
     PID/PGID and `TerminationEvidence` (`src/watcher/reaper.ts`); a fixture spawns
     `sh -c 'sleep 30 & … wait'`, lets the deadline SIGKILL the leader, and asserts
     the backgrounded **descendant** PID is no longer alive — proving whole-group
     reap, not just leader kill.
  2. A recovery fixture creates an **actual orphaned process group** (a detached
     launcher backgrounds `sleep` then exits) and asserts `reapGroup` genuinely
     terminates the whole group (`isGroupAlive` → false) — not an elapsed-time
     proxy.
  3. Expired-lease takeover requires **prior-owner group-termination evidence**
     before `attemptPush`: `src/watcher/apply.ts` reaps the recorded `owner_pgid`
     and proceeds only once the group is gone.
  4. `src/watcher/candidate.ts` private-index `git add`/`write-tree`/`commit-tree`
     use `gitEnv()`'s config-free environment (no ambient `process.env` spread); a
     fixture sets a hostile `GIT_DIR` and asserts prep is unaffected.
  5. APPLYING takeover **revalidates** the approval token AND `row.repoIdentity` /
     `row.fullRef` against the sealed project's `repository_identity` / `full_ref`;
     a fixture substitutes the project identity/endpoint and asserts `ESCALATED`
     with a `repo-identity-mismatch|endpoint-substitution` reason.
  6. `next_attempt_at` is **enforced** (no attempt before it; fixture asserts the
     remote is untouched), repeated identical failures **escalate** rather than
     hot-loop, and an ambiguous post-push state **ESCALATES** (not APPROVED).
  7. Prepared (`refs/echo-watcher/prepared/<id>`) and `-base` refs are **deleted**
     on terminal state; a fixture asserts both refs exist pre-apply and are gone
     after APPLIED.
- **Finding E — DONE.** This record is the new immutable feature-head migration
  record with the complete AC8 bindings below: exact verification commands + exit
  results, target repository path/branch, the target-policy-copy OID/SHA binding,
  a truthful clean-status binding, the current edge-record fixed point, and the
  envelope-invariant dual-route inner hash. It restores the `## Commands + exits`
  section the reviewer found missing.

## Bindings (AC8)

### Source and target

| Binding | Value |
|---|---|
| Source pin | `Project_echo@2971310441b69735cbe759293abd8c4d044bf347` |
| Sealed source policy | Project_echo blob `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`; SHA-256 `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` |
| Target policy copy | `provenance/source-policy.v1.json` — git blob `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`; SHA-256 `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` (byte-identical to the sealed source object) |
| Target repository | `/Users/zhenye/Desktop/echo-loop` |
| Target branch | `migration/2026-07-13-134` |
| Target HEAD | `171fdfc724f74f7cd6d4b8502e03264a517816d9` |
| Target tree | `8f636631db2160370f051caaba1754ea30cb6d69` |
| Target history | 26 linear commits; one branch; **no remote** (`git remote -v` empty); `git fsck --full --strict` exit 0; working tree **clean** (`git status --porcelain` empty) |
| Authority / install / maturity | `authority:false`; `installed:false`; local `DEV` candidate |

### Manifest / artifact hashes

| Artifact | Git blob OID | SHA-256 |
|---|---|---|
| `provenance/source-policy.v1.json` | `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a` | `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` |
| `provenance/edge-record.v1.json` | `03667a39c3c95c897ee89e1ecf6cf0038856db8f` | `5067d2f3a33ab230b2a513929ac94d64833ae1685fb924d3e8aae46320c723fc` |
| `provenance/edge-record.v1.schema.json` | `2ad3c15fb478246fb753373d3638dc086e310f89` | `30d25c7fe55c27e2b79e06bbf38dbb410ab7e3ebbf3484c8ef8a408ef4429277` |
| `provenance/watcher-project.v1.json` | `af08720815120b4c40bc07ad1bf78977b1573f20` | `2efa5f264a76b3326a7e783bc7ec2cde6dab70c257f8a86a7f1f354b005bd302` |
| `provenance/verification-workload.v1.json` | `f5b632170bcd0c8b639d119124a3c962aa80ff52` | `be2d600767d88c9f4e057dc2a539ba9f5032fa40dab25495ffd4462902972a33` |
| `provenance/source-seed.v1.json` | `5278839736a53a82bfce884ea16eac0b254437ed` | `c6121b5092de071efebe1cd8a0c9f6cccbe3364f6edb3edd4548cf97618dd41a` |
| `package.json` | `afaedd015a49f98bf7bb3d154281665879f2aa10` | `c1ec2d0e0e02a16ca24b4fbc45e249442b4cc136073f276df12748651c6616b7` |
| `package-lock.json` | `b67ca3f905e5c87023198d47edaaf24f725edea2` | `74c56cce3e6703137a1b0ba4b97bd5c6fa087a192fb28c7af795ac89e0a195ea` |

- **edge-record fixed point:** `d1d0bc612fa1e914011f714d842bd69d28fe2d323ebc143a547657d7ee345c33`
  (538 rows / 9 classes; matches the HEAD closure under `build-source-plan.mjs --check`).
- **dual-route inner-result SHA-256:** `eb614a430ad5355addba076df9a2216e5b903b205c097fba67576fdf868b1483`
  — byte-identical across the `direct` and `npm` routes, and reproduced under a
  distinct scratch `HOME`/`TMPDIR` envelope (envelope-invariant; Finding B).
- **Dependencies:** 5 declared, 132 locked packages, no root drift.
- **Skills:** 13 ported skills byte-identical to source.
- **Ports:** 76 byte-identical ports intact of 148 inventory rows.

## Commands + exits

All commands run in the target repo `/Users/zhenye/Desktop/echo-loop` at HEAD
`171fdfc7…`. Node here is x86_64 under Rosetta, so node-spawned `python3` cannot
load jsonschema's arm64 wheel; python-invoking tests run under `arch -arm64`
(`tests/helpers/py.ts`).

| Command | Exit | Result |
|---|---|---|
| `tsc -p tsconfig.json --noEmit` | 0 | typecheck clean |
| `vitest run` (full suite) | 0 | **22 files / 121 tests pass** |
| `node tools/lint.mjs` | 0 | 43 source files, no stray debug |
| `node tools/check-provenance.mjs` | 0 | sealed policy verified; 76 byte-identical ports of 148 inventory rows |
| `node tools/check-dependencies.mjs` | 0 | 5 declared, 132 locked, no root drift |
| `node tools/check-skills.mjs` | 0 | 13 ported skills byte-identical |
| `node tools/build-source-plan.mjs --check` | 0 | committed edge record matches HEAD closure (538 rows, fixed_point `d1d0bc61…`); never writes |
| `git fsck --full --strict` | 0 | object graph intact |
| `git status --porcelain` | — | empty (clean tree) |
| `git remote -v` | — | empty (no remote) |

### Dual-route verification workload (AC7)

Both routes run the identical ordered 14-row roster and each row exits status 0;
the inner projection is byte-identical across routes and envelope-invariant.

- **direct:** `node tools/run-verification.mjs --manifest provenance/verification-workload.v1.json --route direct --out <dir>` → `verdict=pass rows=14`
- **npm:** `node <npm-cli> run verify:extraction -- --manifest provenance/verification-workload.v1.json --route npm --out <dir>` → `verdict=pass rows=14`

Roster (all status 0): `provenance`, `dependencies`, `skills`, `source-plan`
(`--check`), `task-state`, `review-queue`, `coord`, `workflows`, `lint`,
`typecheck`, `full-tests` (`--config vitest.workload.config.ts`, breaking the
verifier-in-workload recursion), `source-independence`, `fsck`, `diff-tree`. The
route record (`route.json`) differs by design: the npm route binds its actual
outer launcher argv + npm banner + user-agent; the direct route's npm-specific
fields are null (F5).

## Exclusions / differences honored

No Project_echo history, wiki, `raw/`, or backlog archives; no product or user
context. Only the three authorized operations were used: pinned read-only
Project_echo source reads, creation of the local no-remote target, and the AC8
feature-branch push. No sibling repositories, wiki, or holdout-131 were touched.
All watcher and workflow tests use only disposable fixture repositories, scratch
`HOME`/`XDG`/`TMP`, and disabled global/system config/hooks/signing. Passing this
migration proves only a local split; active Project_echo remains authoritative,
and this record authorizes no installation, cutover, remote creation, release,
maturity advancement, or transfer of authority.
