# Migration record — 2026-07-13-134 echo-loop source extraction (INCOMPLETE)

**Status: INCOMPLETE / UNACCEPTED.** Attended one-shot local extraction carried
across multiple founder-endorsed attended sessions (founder chose "continue as
one long attended build, in place"). Per
`raw/internal/decisions/2026-07-13-one-shot-local-extraction-lifecycle.md` the
target remains unaccepted until every acceptance criterion passes; not installed;
no remote. Each session commits verified progress and hands off honestly.

- `authority: false`
- `installed: false`
- Active Project_echo loop remains source, backup, active authority, and history.

## Binding (current, Run 3)

- **Source pin:** `Project_echo@2971310441b69735cbe759293abd8c4d044bf347`
- **Source policy (sealed, Project_echo):** blob OID
  `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`, sha256
  `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a`
- **Target repo:** `/Users/zhenye/Desktop/echo-loop`, branch `migration/2026-07-13-134`
- **Target HEAD:** `2f367da11823b82cf0ecd8221a28112d24179aaf`
- **Target tree:** `cda5b122ca8499740d37ecbad18b7113c793dd86`
- **Target `provenance/source-policy.v1.json`:** blob OID
  `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a` (byte-identical to source), sha256
  `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a`
- **Target `provenance/source-seed.v1.json`:** blob OID
  `5278839736a53a82bfce884ea16eac0b254437ed`, sha256
  `c6121b5092de071efebe1cd8a0c9f6cccbe3364f6edb3edd4548cf97618dd41a`
- **Target `provenance/watcher-project.v1.json`:** blob OID
  `af08720815120b4c40bc07ad1bf78977b1573f20`, sha256
  `2efa5f264a76b3326a7e783bc7ec2cde6dab70c257f8a86a7f1f354b005bd302`
- **Target `provenance/verification-workload.v1.json`:** sha256
  `5d1ccb8df6057ffe8402904095ee8cf937acc32f3178a42585760c93d6ca4ac6`
- **Target `package-lock.json`:** sha256
  `74c56cce3e6703137a1b0ba4b97bd5c6fa087a192fb28c7af795ac89e0a195ea`
  (real npm-install lock; NOT yet the minimal derived closure — AC2 remaining)
- **Raw pinned-SHA inventory:** 148 rows; 76 byte-identical ports.

## Clean-status assertions (verified)

- `git fsck --full` on target: exit 0. Single-root history, one branch, no remote,
  no alternates/promisor/replace.
- Config-free `/usr/local/bin/git` 2.37.3 envelope for all source reads/target ops.
- Source reads: `git ls-tree -rz --full-tree` + `git cat-file blob <oid>`.
- `npm run check:provenance`: GREEN. `tsc -p tsconfig.json --noEmit`: clean.
  Full `vitest run`: **54 tests passed** (coord 28, watcher 21, workflows 4,
  migration 1).
- Dual-route `verify:extraction` (direct + npm) inner projections byte-identical
  (verdict pass).

## Completion status by acceptance criterion

- **AC1 — DONE.**
- **AC2 — PARTIAL.** Core done (policy verify + byte-identical copy, 148-row
  source-seed, 76 ports, `check:provenance` green). NOT done: edge-record schema
  + source-plan closed-edge fixed point; dependency-set minimal derived lock;
  full battery rows; excluded-capability edge disposition.
- **AC3 — DONE (core), refinement pending.** `./coord` publication protocol +
  atomic init, 28 tests green, builds to the `./coord` export. Pending: native
  build via the exact named `npm rebuild better-sqlite3 --offline
  --foreground-scripts` row (currently prebuilt binary).
- **AC4 — DONE.**
- **AC5 — DONE (core), refinements pending.** Probe parser, endpoint substitution
  guard, SQLite CAS state machine + expired-lease takeover, ephemeral-worktree
  candidate + anchor ref, force-with-lease apply proven on disposable remotes
  (happy/concurrent-advance/substitution/idempotent/takeover), founder worktree
  untouched; 21 tests. Pending: own-pgid isolation + TERM/KILL escalation;
  crash-before/after-push, gc-survival, both-orders, mismatched-digest fixtures.
- **AC6 — DONE.** `local-fixture-loop.test.ts`: single-winner claim, seal
  freshness, proposed-unclaimable, worktree isolation, explicit merge checkpoint;
  4 tests, disposable repos only.
- **AC7 — PARTIAL (equivalence core done).** Dual-route verifier: byte-identical
  inner projection + differing route records, verdict pass; 1 test. NOT done:
  offline install matrix under `sandbox-exec` deny-network + loopback control +
  probes; better-sqlite3 rebuild as sole named lifecycle row; private-clone
  source-independence; full battery.
- **AC8 — PARTIAL.** This record maintained each checkpoint (binder above).
  Builder stops at `pending_review` as a checkpoint escalation. The independent
  `codex-ops` reviewer child-commit ceremony is the reviewer's leg, not the
  builder's (same as lane 133) — not performed here.

## Exclusions honored

No Project_echo history/wiki/raw/backlog archives copied. No product/context
capabilities included. Rewrite-lane `.ts` beyond `src/coord` and
authored/generated manifests were not fabricated as byte-identical ports.
