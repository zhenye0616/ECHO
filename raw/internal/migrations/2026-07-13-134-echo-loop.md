# Migration record — 2026-07-13-134 echo-loop source extraction (INCOMPLETE)

**Status: INCOMPLETE / UNACCEPTED.** This attended one-shot local extraction is
being carried across multiple founder-endorsed attended sessions (the founder
chose "continue as one long attended build, in place" over archive/re-scope).
Per `raw/internal/decisions/2026-07-13-one-shot-local-extraction-lifecycle.md`
the target remains unaccepted until every acceptance criterion passes; it is not
installed and has no remote. Each session commits verified progress and hands
off honestly.

- `authority: false`
- `installed: false`
- Active Project_echo loop remains source, backup, active authority, and
  historical record. Target is local, single-branch, and has **no remote**.

## Binding (current)

- **Source pin:** `Project_echo@2971310441b69735cbe759293abd8c4d044bf347`
- **Source policy (sealed, Project_echo):** blob OID
  `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`, sha256
  `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a`
- **Target repo:** `/Users/zhenye/Desktop/echo-loop`, branch
  `migration/2026-07-13-134`
- **Target HEAD:** `2dcb0f9fbca1be3aa66fd784e293f4abd8c1afbd`
- **Target tree:** `141925b0ef124d2de66ac4ab44d19032d8b88143`
- **Target `provenance/source-policy.v1.json`:** blob OID
  `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a` (byte-identical to source — equal
  content-addressed OID), sha256
  `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a`
- **Target `provenance/source-seed.v1.json`:** blob OID
  `5278839736a53a82bfce884ea16eac0b254437ed`, sha256
  `c6121b5092de071efebe1cd8a0c9f6cccbe3364f6edb3edd4548cf97618dd41a`
- **Target `package.json`:** sha256
  `732357aa96eb2babe9cb642e0bb10ce6bfeda4c4374ecbd43554bb4b778a7051`
- **Target `package-lock.json`:** sha256
  `74c56cce3e6703137a1b0ba4b97bd5c6fa087a192fb28c7af795ac89e0a195ea`
  (real npm-install lock; NOT yet the minimal derived closure — see AC2 remaining)
- **Raw pinned-SHA inventory:** 148 rows; 76 byte-identical ports.

## Clean-status assertions (verified)

- `git fsck --full` on target: exit 0.
- Single-root history, one branch `migration/2026-07-13-134`.
- No remote; no alternates; no promisor pack; no replace refs.
- Config-free `/usr/local/bin/git` 2.37.3 envelope for all source reads and
  target ops; fixed identity; hooks/signing/templates disabled.
- Source reads used `git ls-tree -rz --full-tree` + `git cat-file blob <oid>`.
- `npm run check:provenance`: GREEN (sealed policy + 76 ports byte-identical).
- `tests/coord` (`vitest run`): 28 passed.
- `tsc -p tsconfig.json`: builds `dist/coord/index.js` matching the `./coord`
  export; `tsc --noEmit`: clean.

## Completion status by acceptance criterion

- **AC1 — DONE.** Isolated target repository created per contract.
- **AC2 — PARTIAL.** Deterministic core + integrity check done (policy verify +
  byte-identical copy, 148-row source-seed, 76 ports, `check:provenance` green).
  NOT done: `edge-record.v1.schema.json` + source-plan closed-edge fixed point;
  dependency-set partition + minimal derived lock; full `verify:extraction`;
  edge-closure disposition of ported files that reference excluded capabilities.
- **AC3 — DONE (core) / refinement pending.** `./coord` better-sqlite3
  publication protocol + atomic init implemented; 28 tests green; builds to the
  `./coord` export. Refinement pending: native build via the exact named row
  `npm rebuild better-sqlite3 --offline --foreground-scripts`
  (`npm_config_nodedir=/usr/local/Cellar/node@22/22.22.1_1`) — currently uses
  the prebuilt binary from prebuild-install.
- **AC4 — DONE.** `templates/project/` minimal protocol templates + echo-loop's
  own empty backlog queue; no Project_echo history/wiki/raw/product content.
- **AC5 — NOT DONE.** Watcher force-with-lease CAS state machine + fixtures.
- **AC6 / AC7 — NOT DONE.** Fixture loop + dual-route verifier + source
  independence.
- **AC8 — PARTIAL.** This record maintained; builder stops at `pending_review`
  as a checkpoint escalation (multi-session build), not a completion. Reviewer
  child-commit step not performed (nothing accepted to review yet).

## Exclusions honored

No Project_echo history/wiki/raw/backlog archives copied. No product or context
capabilities included. Rewrite-lane `.ts` files beyond `src/coord` and the
authored/generated manifests were not fabricated as byte-identical ports.
