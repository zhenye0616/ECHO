# Migration record — 2026-07-13-134 echo-loop source extraction (fourth-review remediation)

**Status: fourth-review residuals remediated; normal builder handoff for a new
independent review; UNACCEPTED, not installed.** The fourth independent
`codex-ops` judgment (immutable child
`fe26a78f89d130364af5b2207e7b07b41eecb78b`) reproduced the round-two hashes
and rejected four same-class residuals: an edge record invalid under its own
schema, fail-open executable extraction, non-atomic takeover evidence, and
stale-owner APPLYING mutation. This record binds the successor target in which
those four residuals are closed and their exact adversarial fixtures pass. The
target remains a local `DEV` candidate with no remote, installation, or
authority. Passing proves only a local split; active Project_echo remains the
authoritative loop.

- `authority: false`
- `installed: false`

## Fourth-review residuals — final dispositions

- **D1 — edge-record schema false-green — CLOSED.** The Draft-07 schema now
  declares and constrains every emitted root and row field: `policy_sha256`,
  `source_blobs`, `manifest_blobs`, `from_blob`, the closed edge enum, and the
  class/resolution-kind relationships. `build-source-plan --check` loads the
  exact committed schema and validates both the generated value and exact
  committed value before fixed-point or byte comparison. Generation validates
  before write. Regressions validate the exact HEAD pair and prove that a
  rejecting schema and a structurally invalid schema both fail closed.
- **D2 — executable extraction omissions / global context — CLOSED.** The JS
  extractor is now a lexical, string/comment-aware scanner rather than a global
  regex literal map. It scans executable calls inside test bodies while ignoring
  fixture text inside string tokens; recognizes member/computed expressions such
  as `process.env.SCRIPT`; treats absolute repository-root paths such as
  `/src/...` as repository-capable; tracks function/local scope and reassignment;
  and binds one source-specific tsconfig chain instead of globally last-winning.
  Equal-depth competing tsconfig contexts reject as
  `ambiguous_tsconfig_context`. Exact fixtures cover all six fourth-review
  adversarial forms. Repository-root anchors are derived from a module URL;
  disposable fixture-repository roots remain runtime handles. The sealed HEAD
  closure is 604 rows across 9 classes, 104 source blobs, and 3 manifest blobs,
  with no self-referential edge-record target.
- **C1 — takeover evidence crash window — CLOSED.** Takeover accepts the exact
  prior owner token, prior PGID, prior PGID file, and validated termination
  evidence as CAS inputs. The same guarded SQL update installs the successor and
  durably writes `prior_owner_pgid` plus `takeover_evidence`; there is no
  follow-up evidence write to lose. A crash-immediately-after-CAS fixture
  reopens the database and observes the successor plus the exact prior identity
  and proof already committed.
- **C2 — stale-owner terminal/failure mutation — CLOSED.** Every mutation of an
  APPLYING row is now predicated on `state='APPLYING' AND owner_token=?`,
  including escalation, retry/failure accounting, PGID/evidence writes,
  recovery, and APPLIED. Non-applying escalation is restricted to
  PREPARED/APPROVED. Failure accounting and APPLYING→APPROVED recovery are one
  owner-guarded transaction. A stale-owner-after-takeover fixture proves the
  prior owner cannot escalate, increment attempts, overwrite termination
  evidence, or mark the successor applied.

The successor target consists of five linear commits after the rejected target:
`27290be` (schema/scanner/watcher fix), `cf2e936` (repository-vs-fixture roots),
`02e0107` (validated closure regeneration), `7d349fe` (remove closure
self-reference), and `38989db` (final stable closure).

## Bindings (AC8)

### Source and target

| Binding | Value |
|---|---|
| Source pin | `Project_echo@2971310441b69735cbe759293abd8c4d044bf347` |
| Sealed source policy | Project_echo blob `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`; SHA-256 `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` |
| Target policy copy | `provenance/source-policy.v1.json` — git blob `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`; SHA-256 `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` (byte-identical to the sealed source object) |
| Target repository | `/Users/zhenye/Desktop/echo-loop` |
| Target branch | `migration/2026-07-13-134` |
| Target HEAD | `38989db78e221a7e15b2adbe859fa76244bf16e4` |
| Target tree | `76a7bc47a9aaf0196d2a43497d01460b0df86847` |
| Target history | 34 linear commits; one root and one branch; 400/400 objects reachable (34 commit / 147 tree / 219 blob); **no remote**, alternates, promisor, replace, shallow state, tags, symlinks, or gitlinks; `git fsck --full --strict --unreachable` exit 0 |
| Target filesystem | 173 tracked files = 173 non-`.git` filesystem files; no ignored/untracked residue; working tree clean. The two `.verify-smoke*` directories are tracked historical artifacts and exactly match HEAD, not generated residue. |
| Authority / install / maturity | `authority:false`; `installed:false`; local `DEV` candidate |

### Project_echo publication (AC8 feature-branch push target)

| Binding | Value |
|---|---|
| Publication endpoint (literal origin URL) | `https://github.com/zhenye0616/ECHO.git` |
| Full feature ref | `refs/heads/agent/134-echo-loop` |
| Expected-old before remediation publication | `fe26a78f89d130364af5b2207e7b07b41eecb78b` (immutable fourth-judgment child) |

The AC8 feature-branch push and its strict `ls-remote` probe address the literal
origin URL above (never a bare remote name). This is the Project_echo repository
that carries the builder migration record + reviewer records; it is NOT a remote
of the target `/Users/zhenye/Desktop/echo-loop`, which remains remote-less.

### Manifest / artifact hashes

| Artifact | Git blob OID | SHA-256 |
|---|---|---|
| `provenance/source-policy.v1.json` | `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a` | `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` |
| `provenance/edge-record.v1.json` | `02b50876b9f07c5fa4a004b68fe6816622c97fa4` | `2dbb5f13edabdac076ba4b961281f36aef3bae93d251c53fa001c439197b87c0` |
| `provenance/edge-record.v1.schema.json` | `9ed57b7a338f12a6258a4ea4fdddab7f1da78813` | `70af1779c9a864073e3690b8eb93662292d4b5d5e4c4d47480fcd9210d775c09` |
| `provenance/watcher-project.v1.json` | `af08720815120b4c40bc07ad1bf78977b1573f20` | `2efa5f264a76b3326a7e783bc7ec2cde6dab70c257f8a86a7f1f354b005bd302` |
| `provenance/verification-workload.v1.json` | `f5b632170bcd0c8b639d119124a3c962aa80ff52` | `be2d600767d88c9f4e057dc2a539ba9f5032fa40dab25495ffd4462902972a33` |
| `provenance/source-seed.v1.json` | `5278839736a53a82bfce884ea16eac0b254437ed` | `c6121b5092de071efebe1cd8a0c9f6cccbe3364f6edb3edd4548cf97618dd41a` |
| `package.json` | `afaedd015a49f98bf7bb3d154281665879f2aa10` | `c1ec2d0e0e02a16ca24b4fbc45e249442b4cc136073f276df12748651c6616b7` |
| `package-lock.json` | `b67ca3f905e5c87023198d47edaaf24f725edea2` | `74c56cce3e6703137a1b0ba4b97bd5c6fa087a192fb28c7af795ac89e0a195ea` |

- **edge-record fixed point:** `b1745bfd39cd51d89b52d0e669b073643103ec597ee9c5f731d1bcb599b9cb5a`
  (604 rows / 9 classes; SHA-256 over `{policy_sha256, source_blobs (104),
  manifest_blobs (3), rows}`; matches the HEAD closure under
  `build-source-plan.mjs --check`).
- **dual-route inner-result SHA-256:** `e21c1d0fb2f4afcbe88b27d4fd7988cb7f180c9b72a18936ca4ea98c7d81d0ef`
  — byte-identical across the `direct` and `npm` routes, and reproduced under a
  distinct scratch `HOME`/`TMPDIR` envelope (envelope-invariant; Finding B).
- **route-result SHA-256:** direct
  `e545fadfde78db82fe846fe2e4ed1ec1efc9396187740a27f85c5ac5171bc32e`;
  npm `c0095e584c4a845b23c7b59ed26f3156d2e091816553c5915e623a4da9689e52`
  (different by design).
- **Dependencies:** 5 declared, 132 locked packages, no root drift.
- **Skills:** 13 ported skills byte-identical to source.
- **Ports:** 76 byte-identical ports of 148 inventory rows.

## Commands + exits

All commands run in the target repo `/Users/zhenye/Desktop/echo-loop` at HEAD
`38989db7…`. Node here is x86_64 under Rosetta, so node-spawned `python3` cannot
load jsonschema's arm64 wheel; python-invoking tests run under `arch -arm64`
(`tests/helpers/py.ts`).

| Command | Exit | Result |
|---|---|---|
| `/usr/local/bin/node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit` | 0 | typecheck clean |
| `/usr/local/bin/node node_modules/vitest/vitest.mjs run` (full suite) | 0 | **22 files / 153 tests pass** in 225.37s |
| `/usr/local/bin/node node_modules/vitest/vitest.mjs run tests/migration/source-plan.test.ts --pool=forks --maxWorkers=1 --minWorkers=1` | 0 | **46/46 pass**, including exact-schema and six adversarial extraction/context regressions |
| `/usr/local/bin/node node_modules/vitest/vitest.mjs run tests/watcher/state.test.ts tests/watcher/recovery.test.ts tests/watcher/containment.test.ts tests/watcher/apply.test.ts` | 0 | **28/28 pass**, including atomic-CAS crash and stale-owner fixtures |
| `/usr/local/bin/node tools/lint.mjs` | 0 | 43 source files, no stray debug |
| `/usr/local/bin/node tools/check-provenance.mjs` | 0 | sealed policy verified; 76 byte-identical ports of 148 inventory rows |
| `/usr/local/bin/node tools/check-dependencies.mjs` | 0 | 5 declared, 132 locked, no root drift |
| `/usr/local/bin/node tools/check-skills.mjs` | 0 | 13 ported skills byte-identical |
| `/usr/local/bin/node tools/build-source-plan.mjs --check` (twice consecutively, then again post-cleanup) | 0 | exact committed Draft-07 schema validates generated + committed values before comparison; stable 604 rows, fixed point `b1745bfd…`; never writes |
| `/usr/local/bin/node tools/check-source-independence.mjs` (post-cleanup) | 0 | fresh private clone at exact HEAD; no alternates/promisor/replace/shallow/remote; fsck clean |
| `/usr/local/bin/git fsck --full --strict --unreachable` | 0 | 400/400 reachable objects; zero garbage |
| `/usr/local/bin/git status --porcelain=v1 --untracked-files=all --ignored` | — | empty after removing only known ignored `.DS_Store`, `.verify-*`, `dist/`, and `node_modules/` residue |
| `/usr/local/bin/git remote -v` | — | empty (no remote) |

### Dual-route verification workload (AC7) — concrete route argv

Both routes run the identical ordered 14-row roster; each row exits status 0; the
inner projection is byte-identical across routes and envelope-invariant. Concrete
launcher argv (no placeholders; the out directories are the fixed `.verify-*`
scratch paths):

- **direct:** `/usr/local/bin/node tools/run-verification.mjs --manifest provenance/verification-workload.v1.json --route direct --out .verify-direct` → `verdict=pass rows=14`
- **npm:** `/usr/local/bin/node /usr/local/lib/node_modules/npm/bin/npm-cli.js run verify:extraction -- --manifest provenance/verification-workload.v1.json --route npm --out .verify-npm` → `verdict=pass rows=14`; recorded resolved outer launcher argv is `/usr/local/Cellar/node@22/22.22.1_1/bin/node /usr/local/Cellar/node@22/22.22.1_1/lib/node_modules/npm/bin/npm-cli.js run verify:extraction`

Roster (all status 0): `provenance`, `dependencies`, `skills`, `source-plan`
(`--check`), `task-state`, `review-queue`, `coord`, `workflows`, `lint`,
`typecheck`, `full-tests` (`--config vitest.workload.config.ts`, breaking the
verifier-in-workload recursion), `source-independence`
(`tools/check-source-independence.mjs`, deterministic network-free
private-clone-escape check), `fsck`, `diff-tree`. The route record (`route.json`)
differs by design: the npm route binds its actual outer launcher argv + npm
banner + user-agent; the direct route's npm-specific fields are null (F5).

## Exclusions / differences honored

No Project_echo history, wiki, `raw/`, or backlog archives; no product or user
context. Only the three authorized operations were used: pinned read-only
Project_echo source reads, creation of the local no-remote target, and the AC8
feature-branch push to `https://github.com/zhenye0616/ECHO.git`
`refs/heads/agent/134-echo-loop`. No sibling repositories, wiki, or holdout-131
were touched. All watcher and workflow tests use only disposable fixture
repositories, scratch `HOME`/`XDG`/`TMP`, and disabled global/system config /
hooks / signing. Passing this migration proves only a local split; active
Project_echo remains authoritative, and this record authorizes no installation,
cutover, remote creation, release, maturity advancement, or transfer of
authority. This remediation made no ECHO MCP call and therefore created no MCP
dogfooding-journal entry.
