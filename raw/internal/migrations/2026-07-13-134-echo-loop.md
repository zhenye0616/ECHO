# Migration record — 2026-07-13-134 echo-loop source extraction (A–E campaign, round 2)

**Status: A–E residuals closed; normal builder handoff to `pending_review` for
re-review; UNACCEPTED, not installed.** The third independent `codex-ops` review
(child `770b101f`) confirmed A and B repaired and all 18 bound hashes
reproduced, and raised same-class residuals in C, D, and E. This record binds
the completed state in which those three residuals are closed on disposable
fixtures only. The target is a local `DEV` candidate: no remote, not installed,
no authority. Passing proves only a local split; active Project_echo remains the
authoritative loop.

- `authority: false`
- `installed: false`

## Third-review residuals — final dispositions

- **Finding D residual — CLOSED.** The resolver is now genuinely policy-driven
  and produces the sealed `(source-blob, path, binding-context-byte)` closure:
  - `tools/build-source-plan.mjs` no longer discards the policy (`void policy`
    removed). The parsed sealed policy object is passed into the resolver and
    DRIVES the accepted `edge_classes` set and the fail-closed reject flags
    (`unknown_edges`, `computed_repository_capable_edges`); its SHA-256 is bound
    into the fixed point.
  - Every edge row is `(from, from_blob, specifier, edge_class, resolution,
    context_hash)`. `source_blobs` binds the git blob OID of **all 103 executable
    code files** (including the verifier `tools/build-source-plan.mjs` and
    `tools/lib/source-plan.mjs`), and `manifest_blobs` binds the manifest-context
    files. The fixed point is SHA-256 over `{policy_sha256, source_blobs,
    manifest_blobs, rows}`, so a change to the verifier code, any source blob, or
    the policy MOVES the fixed point — verified this run: a 1-byte change to
    `build-source-plan.mjs` and a change to the bound policy sha each move it.
  - `context_hash` hashes the CANONICAL BYTES of the manifest/context that
    resolved the edge (tsconfig `paths` entry, package `imports`/`exports`,
    workspace exports, npm lock row), not a constant label — verified: two lock
    rows that resolve to the same target produce different `context_hash`.
  - Fail-closed: unresolved repository-capable file/schema/worker/script/exec
    edges and every computed repository-capable edge throw `EdgeReject`
    (`unknown_edge` / `computed_repository_capable_edge`); no silent null-skip.
    Bare toolchain command names and absolute system paths (`/usr/local/bin/git`)
    are not repository-capable, so they classify as toolchain, never reject.
    Extractor precision was tightened (script-extension boundary guard + heredoc
    body skipping) so the real tree yields a clean closure with zero rejects.
  - Workspace exports / package exports / tsconfig `extends` / `baseUrl` /
    `references` now have real resolution paths; the workspace-only-alias fixture
    (`@acme/foo` from `packages/*`) resolves to the workspace blob.
  - Fixture roster completed: dynamic literal import, CommonJS require,
    schema/template, npm JavaScript CLI, python script, worker entry,
    workspace-only alias, baseUrl, fail-closed rejects (missing file / computed
    read / computed worker / unresolved script / unresolved exec), policy-driven
    reject flags, edge-class-not-in-policy, and tamper-evidence (source blob /
    policy / context byte).
- **Finding C residual — CLOSED.** The crash/takeover window is closed with
  durable pre-exec PGID capture + termination-evidence-gated takeover:
  - Every probe/push transport child writes its own process-group id to a durable
    `pgid_file` as its FIRST action, BEFORE contacting the remote (an `sh` shim:
    `echo $$ > file; exec git …`, group leader under the detached spawn). A crash
    mid-probe/mid-push therefore leaves the exact group id recoverable on disk —
    closing the window where `owner_pgid` was persisted only AFTER the synchronous
    child returned.
  - Takeover reaps the ACTUAL prior group (`owner_pgid`, or the `pgid_file` when
    the prior owner crashed before persisting it) with a group-directed
    TERM-then-KILL and requires termination evidence (`terminated` and
    `!isGroupAlive`) BEFORE `attemptPush`; the evidence is stored durably.
  - `setOwnerPgid` and the takeover CAS are owner-token-predicated, so a
    resurfacing prior owner cannot overwrite the new owner's pgid or seize the
    lease. State schema adds `pgid_file` + `termination_evidence` (with
    forward-migration of pre-existing databases).
  - Fixtures couple a LIVE prior-owner process group (orphaned to init — no
    zombie, modelling a crashed prior owner) to an expired APPLYING lease and
    assert the group is reaped before apply; a second fixture proves crash
    recovery via the durable `pgid_file` alone (`owner_pgid` null).
- **Finding E residual — CLOSED.** This record binds the literal Project_echo
  publication endpoint and full feature ref, and states the verification route
  commands as concrete argv (no `<dir>` / `<npm-cli>` placeholders). See below.

## Bindings (AC8)

### Source and target

| Binding | Value |
|---|---|
| Source pin | `Project_echo@2971310441b69735cbe759293abd8c4d044bf347` |
| Sealed source policy | Project_echo blob `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`; SHA-256 `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` |
| Target policy copy | `provenance/source-policy.v1.json` — git blob `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a`; SHA-256 `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` (byte-identical to the sealed source object) |
| Target repository | `/Users/zhenye/Desktop/echo-loop` |
| Target branch | `migration/2026-07-13-134` |
| Target HEAD | `d69c003ae4146140d3d0ee3fe945778781ae5a43` |
| Target tree | `ca77fbda46887a4b7e6170029cd5615e2feefad8` |
| Target history | 29 linear commits; one branch; **no remote** (`git remote -v` empty); `git fsck --full --strict` exit 0; working tree **clean** (`git status --porcelain` empty) |
| Authority / install / maturity | `authority:false`; `installed:false`; local `DEV` candidate |

### Project_echo publication (AC8 feature-branch push target)

| Binding | Value |
|---|---|
| Publication endpoint (literal origin URL) | `https://github.com/zhenye0616/ECHO.git` |
| Full feature ref | `refs/heads/agent/134-echo-loop` |

The AC8 feature-branch push and its strict `ls-remote` probe address the literal
origin URL above (never a bare remote name). This is the Project_echo repository
that carries the builder migration record + reviewer records; it is NOT a remote
of the target `/Users/zhenye/Desktop/echo-loop`, which remains remote-less.

### Manifest / artifact hashes

| Artifact | Git blob OID | SHA-256 |
|---|---|---|
| `provenance/source-policy.v1.json` | `dd9d78abbbecf01d5de6ab7edbc8bace07e4f50a` | `44bef194b379b83185aab3f7055ce547c7a51a7b6df18764022a6f565cbde52a` |
| `provenance/edge-record.v1.json` | `3a7bf876d352f7c3c4762afe97fdcb72c70c1661` | `5e49ae70c5f67739c7fd176a808a95629013985c684ac617aa3ec5e2cec9b495` |
| `provenance/edge-record.v1.schema.json` | `2ad3c15fb478246fb753373d3638dc086e310f89` | `30d25c7fe55c27e2b79e06bbf38dbb410ab7e3ebbf3484c8ef8a408ef4429277` |
| `provenance/watcher-project.v1.json` | `af08720815120b4c40bc07ad1bf78977b1573f20` | `2efa5f264a76b3326a7e783bc7ec2cde6dab70c257f8a86a7f1f354b005bd302` |
| `provenance/verification-workload.v1.json` | `f5b632170bcd0c8b639d119124a3c962aa80ff52` | `be2d600767d88c9f4e057dc2a539ba9f5032fa40dab25495ffd4462902972a33` |
| `provenance/source-seed.v1.json` | `5278839736a53a82bfce884ea16eac0b254437ed` | `c6121b5092de071efebe1cd8a0c9f6cccbe3364f6edb3edd4548cf97618dd41a` |
| `package.json` | `afaedd015a49f98bf7bb3d154281665879f2aa10` | `c1ec2d0e0e02a16ca24b4fbc45e249442b4cc136073f276df12748651c6616b7` |
| `package-lock.json` | `b67ca3f905e5c87023198d47edaaf24f725edea2` | `74c56cce3e6703137a1b0ba4b97bd5c6fa087a192fb28c7af795ac89e0a195ea` |

- **edge-record fixed point:** `203f53420fb50ce82d0064ef238b568371a7095d8f7d2fff52256f7875a8dac8`
  (584 rows / 9 classes; SHA-256 over `{policy_sha256, source_blobs (103),
  manifest_blobs (3), rows}`; matches the HEAD closure under
  `build-source-plan.mjs --check`).
- **dual-route inner-result SHA-256:** `b47d8c8d2bc0c99fbdd1550b4930a7199468360cb04f0f03c9332373840e3f9d`
  — byte-identical across the `direct` and `npm` routes, and reproduced under a
  distinct scratch `HOME`/`TMPDIR` envelope (envelope-invariant; Finding B).
- **Dependencies:** 5 declared, 132 locked packages, no root drift.
- **Skills:** 13 ported skills byte-identical to source.
- **Ports:** 76 byte-identical ports of 148 inventory rows.

## Commands + exits

All commands run in the target repo `/Users/zhenye/Desktop/echo-loop` at HEAD
`d69c003a…`. Node here is x86_64 under Rosetta, so node-spawned `python3` cannot
load jsonschema's arm64 wheel; python-invoking tests run under `arch -arm64`
(`tests/helpers/py.ts`).

| Command | Exit | Result |
|---|---|---|
| `/usr/local/bin/node node_modules/typescript/bin/tsc -p tsconfig.json --noEmit` | 0 | typecheck clean |
| `/usr/local/bin/node node_modules/vitest/vitest.mjs run` (full suite) | 0 | **22 files / 143 tests pass** |
| `/usr/local/bin/node tools/lint.mjs` | 0 | 43 source files, no stray debug |
| `/usr/local/bin/node tools/check-provenance.mjs` | 0 | sealed policy verified; 76 byte-identical ports of 148 inventory rows |
| `/usr/local/bin/node tools/check-dependencies.mjs` | 0 | 5 declared, 132 locked, no root drift |
| `/usr/local/bin/node tools/check-skills.mjs` | 0 | 13 ported skills byte-identical |
| `/usr/local/bin/node tools/build-source-plan.mjs --check` | 0 | committed edge record matches HEAD closure (584 rows, fixed_point `203f5342…`); never writes |
| `/usr/local/bin/git fsck --full --strict` | 0 | object graph intact |
| `/usr/local/bin/git status --porcelain` | — | empty (clean tree) |
| `/usr/local/bin/git remote -v` | — | empty (no remote) |

### Dual-route verification workload (AC7) — concrete route argv

Both routes run the identical ordered 14-row roster; each row exits status 0; the
inner projection is byte-identical across routes and envelope-invariant. Concrete
launcher argv (no placeholders; the out directories are the fixed `.verify-*`
scratch paths):

- **direct:** `/usr/local/bin/node tools/run-verification.mjs --manifest provenance/verification-workload.v1.json --route direct --out .verify-direct` → `verdict=pass rows=14`
- **npm:** `/usr/local/bin/node /usr/local/lib/node_modules/npm/bin/npm-cli.js run verify:extraction -- --manifest provenance/verification-workload.v1.json --route npm --out .verify-npm` → `verdict=pass rows=14`

Roster (all status 0): `provenance`, `dependencies`, `skills`, `source-plan`
(`--check`), `task-state`, `review-queue`, `coord`, `workflows`, `lint`,
`typecheck`, `full-tests` (`--config vitest.workload.config.ts`, breaking the
verifier-in-workload recursion), `source-independence`
(`tools/check-source-independence.mjs`, deterministic network-free
private-clone-escape check), `fsck`, `diff-tree`. The route record (`route.json`)
differs by design: the npm route binds its actual outer launcher argv + npm
banner + user-agent; the direct route's npm-specific fields are null (F5).

## Exclusions / differences honored

No Project_echo history, wiki, `raw/`, or backlog archives; no product or user
context. Only the three authorized operations were used: pinned read-only
Project_echo source reads, creation of the local no-remote target, and the AC8
feature-branch push to `https://github.com/zhenye0616/ECHO.git`
`refs/heads/agent/134-echo-loop`. No sibling repositories, wiki, or holdout-131
were touched. All watcher and workflow tests use only disposable fixture
repositories, scratch `HOME`/`XDG`/`TMP`, and disabled global/system config /
hooks / signing. Passing this migration proves only a local split; active
Project_echo remains authoritative, and this record authorizes no installation,
cutover, remote creation, release, maturity advancement, or transfer of
authority.
