# Migration record — echo-context (item 2026-07-13-135)

**Immutable builder record.** Local standalone `echo-context` source extraction +
synthetic parity proof. This binds the accepted target HEAD/tree, every
provenance/tool hash, all verification commands + results, and the deviations
adjudicated by the founder. Passing proves only a local split — Project_echo
remains the authoritative daemon/MCP and live state (`authority: false`,
`installed: false`).

## Bindings

- **Source commit (pinned):** `2971310441b69735cbe759293abd8c4d044bf347`
- **Target repo:** `/Users/zhenye/Desktop/echo-context` (local-only, no remote)
- **Target HEAD:** `aabf144e156bc6582f4a094b9c668c83aaac935b`
- **Target tree:** `899d769ae72e16e940d3879c828ef1a35c3010fc`
- **Target branch (sole):** `migration/2026-07-13-135`
- **ready_content_sha:** `aa9fa9d89c30b2ba2823d6b3eecdc32e389120bb9f3bc46538b9335a301c8392`
- **Project_echo origin (reviewer push endpoint):** `https://github.com/zhenye0616/ECHO.git`
- **Builder feature-branch head (immutable, pre-existing):** `8d5ae35e502fb6105f17ae9088b51f750f721cc2`
  (this item's code lives in the standalone target repo; the builder branch carries no code by design).

## Source closure (AC6)

- Inventory: **217 paths (110 source, 107 test/fixture)**, SHA-256
  `8b0280660ea5eb64851a5ce0d1a9d56b707d6e29ce00d113ec6656b055d72d37`,
  reproduced by `tools/emit-source-inventory.mjs` over the 20 sealed roots.
- Partition: **144 ported / 8 rewritten / 65 excluded** (import-closed, 0 unresolved).
- Accepted tracked-file set: **190 = 38 target-only + 152 source-derived** (exact-HEAD).

## Target file hashes (SHA-256, first 16 hex)

| path | sha256[:16] |
|---|---|
| package.json | 99efa7b4bd754cf5 |
| package-lock.json | 4c5639f08feb5d64 |
| context-tools.v1.json | db6eab7d3046f692 |
| schemas/service-api.v1.json | ff4e2f2aec002e39 |
| provenance/target-only-policy.v1.json | 4f075fcaee338aaf |
| provenance/runtime-inventory.v1.json | 687e74265ae07ffc |
| provenance/source-evidence.v1.json | bc3094d81b47f22f |
| provenance/parity-matrix.v1.json | 4499bcc575ca1687 |
| provenance/source-extraction.v1.json | c9d5801c4eb826e8 |
| provenance/lifecycle-expected.v1.json | be8f50ec9be56220 |
| provenance/lifecycle-observed.v1.json | ed3b7eb844239a98 |
| provenance/native-toolchain.v1.json | 4341f8703999c60c |
| provenance/context-tool-parity.v1.json | 1aa7ae620208120d |
| tools/emit-source-inventory.mjs | 8aea6b7acd6aacdb |
| tools/check-runtime-inventory.mjs | 1a37c1eb849fc4f8 |
| tools/check-parity.mjs | 91c60e191cf90b2e |
| tools/audit-pinned-extraction.mjs | dcd035cbfcf632df |
| tools/verify-context-tools.mjs | 3c21ca81b908cb63 |
| tools/verify-service-parity.mjs | ee6e6973c59eb8ed |

## Verification commands + results (all green at HEAD `aabf144e`)

- **Build:** `tsc --noEmit` → 0 errors. `vitest run` → 66 files / 966 tests pass, 17 skipped, 0 fail.
- **Lint:** eslint 9.39.5 (source-mirrored flat config) over src+tests `*.ts` → exit 0, 0 findings; all 6 tools `node --check` clean.
- **AC2 (`tools/check-runtime-inventory.mjs`):** `runtime-inventory OK: 6 entrypoints, 3 script CLIs` — every edge in the closed grammar; `dependency-set` test 3/3 (grammar-closed, lock hygiene: no path/git/workspace deps).
- **AC3 (context-tool parity):** roster is exactly the 8 context tools; 10 ordered cases on synthetic seed with a fixed clock (empty volatile-pointer allowlist); aggregate SHA-256
  `632a7b2f2515a68d92e819fcedfa5d26f3960bb631995046ccf3d23de245da90`; `verify-context-tools.mjs` projects 8 / classifies ignored.
- **AC6 (`tools/audit-pinned-extraction.mjs` + `tools/check-parity.mjs`):**
  `audit-pinned-extraction OK: 217 paths (110 src, 107 test)`;
  `check-parity OK: 217 source-evidence rows; ported=144 rewritten=8 excluded=65`;
  every provenance doc validates against its schema (ajv).
- **AC7 (clean-install proof under `sandbox-exec` deny-network, private clone of the accepted OID):**
  `git clone --no-local --no-hardlinks --no-checkout` + hook-disabled detached checkout, origin removed (0 remotes);
  online cache-fill `npm ci --ignore-scripts` exit 0 into a distinct cache root;
  inside `(version 1) (allow default) (deny network*)`: DNS FAIL-ENOTFOUND, direct-IP TCP FAIL-EPERM, https FAIL-ENOTFOUND, loopback control outside=ACCEPT / inside=LISTEN-DENIED-EPERM;
  `npm ci --offline --ignore-scripts` exit 0 (291 packages, zero scripts);
  `npm rebuild better-sqlite3 --offline --foreground-scripts --build-from-source` (npm_config_nodedir=/usr/local/Cellar/node@22/22.22.1_1) exit 0 — ONLY better-sqlite3 executed (esbuild + fsevents carry install scripts but were skipped; tsx carries none);
  native artifact `node_modules/better-sqlite3/build/Release/better_sqlite3.node` SHA-256
  `289ac2671fc501b275af7ce170ea2ef84e07be7e2a4a403aaa055cef02018557` (loads under deny-network).
  Toolchain: node v22.22.1, npm 10.9.4, Apple clang 21.0.0, python3 3.10.7.
- **AC8 (context service, `tests/integration/context-service.test.ts`):** 7/7 pass.
  `node --import tsx tools/verify-service-parity.mjs --home <scratch> --host 127.0.0.1 --port 0 --ready-fd 3`
  spawned as a detached process-group leader; exactly one canonical JSON-LF FD3
  readiness record `{host:"127.0.0.1",port,pid}`; GET /v1/ping + POST
  /v1/{capture,search,clusters,atoms,wait} against the real TS retrieval stack;
  capture→search→atoms round-trip verified; unknown request fields rejected (400);
  loopback-only listener; SIGTERM graceful teardown ahead of group KILL.
- **Object closure:** sole branch `migration/2026-07-13-135`; no tags/remotes/reflogs;
  `git fsck --full --no-reflogs --unreachable` empty; object set == reachable from the branch;
  tracked==190==38+152 (exact-HEAD).

## Deviations (each founder-adjudicated; recorded here per the item's adjudication sections)

- **Founder adjudication #2 (Q2, AC4):** AC4's literal `src/state/paths.ts` is neither
  in the 20 roots nor the 38-path set; implemented as a `rewritten` disposition of the
  extracted `src/echo-home/state-paths.ts` (`ECHO_HOME`→`ECHO_CONTEXT_HOME`, default
  `~/.echo`→`~/.echo-context`). The echo-home onboarding/config-sync layer (adapter-sync,
  roles, scaffold, external-config adapters) is `excluded` — not context substrate per AC2,
  reaches `assets/` outside the roots. Both recorded in `parity-matrix.v1.json`.
- **Founder adjudication #3 (Q3, AC8 harness):** option (b)-refined — `tsx` pinned as a
  devDependency (no lifecycle execution beyond the adjudicated better-sqlite3 rebuild);
  `verify-service-parity.mjs` stays a plain `.mjs` child launched
  `node --import tsx tools/verify-service-parity.mjs`, dynamically importing the TS service
  entry, preserving the real child-process/FD3/process-group/loopback ceremony. The
  `--import tsx` prefix is the invocation deviation from the spec's literal argv.
- **AC3 wait-timeout mapping:** AC3's literal "10ms" maps to the extracted tool's
  integer-seconds schema as `timeout:0` (fast empty return; bounded-fast-timeout intent held).
- **Check-time scratch configs (never committed; Q1 pattern):** tsconfig SHA-256
  `7164ed9356aa3bd1d9108283eee164053bc6f251418d0aa1dc4d4b02726bf78f` (NodeNext/ES2022 strict,
  invoked `--project <scratch>`); eslint flat config SHA-256
  `df912afc56372010d08414de6421d28fee931b908cdbbf0fd742ecf20e605bba` (mirrors the source repo's
  eslint.config.js). The AC6 38-path target-only set stays intact; the reviewer reruns
  typecheck/lint with byte-identical scratch config from its own clone.

## Authority

- `authority: false` — Project_echo remains the active daemon/MCP, backup, and historical record.
- `installed: false` — no target remote, publish/install, live-state migration, or cutover.
- Target history is unchanged and has no remote. The independent `codex-ops` reviewer leg
  (a sole-parent child commit adding `2026-07-13-135-echo-context-review.md` to the feature
  branch) is out of scope for this builder record and comes next, same Option B ceremony as 133/134.
