# Migration record — echo-brain local source extraction (item 133)

**Item:** 2026-07-13-133-local-echo-brain-source-extraction
**Builder:** fable-builder-133 (Claude Code / claude actor)
**Current remediation builder:** codex-builder-133 (Codex / codex actor)
**Attended one-time local build.** No migration controller / evidence publisher /
recovery daemon was built (forbidden by `2026-07-13-one-shot-local-extraction-lifecycle.md`).

> **Current candidate:** the latest builder-remediation section at the end of this record binds the
> current target candidate `41c28171c64710b3ad23392a2606d75cfe8e7b2c` / tree
> `5691ab527de8eb622ed1d333ed867a2191afdf8a`. Earlier target identities and tuples are retained
> as superseded history, not as current acceptance evidence.

## Identity binding

| Field | Value |
|---|---|
| Source repository | Project_echo |
| Pinned source SHA | `2971310441b69735cbe759293abd8c4d044bf347` |
| Target path | `/Users/zhenye/Desktop/echo-brain` |
| Target branch | `migration/2026-07-13-133` |
| Current candidate target HEAD | `41c28171c64710b3ad23392a2606d75cfe8e7b2c` |
| Current candidate target tree | `5691ab527de8eb622ed1d333ed867a2191afdf8a` |
| authority | **false** |
| maturity | **DEV** |
| Target remotes | none |

## Content hashes (SHA-256)

| Path | SHA-256 |
|---|---|
| package.json (relocated from product/package.template.json) | `e7dd03dfce75c3ae4053541bb813b17f228dc6e15cec32aa268d0adbf3320736` |
| npm-shrinkwrap.json (relocated; sole lock; no package-lock.json) | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |
| README.md | `ae9a62af323194c09124fa2023fd66420488f8f8788646938df65b30b4de8e4f` |
| provenance/extraction-policy.v1.json | `771a59ed73be2221ef10d1814beeb6d8d5cc06b937186407332f3cde67072945` |
| provenance/source-plan.v1.json | `3874b30f28955ff07850b9c841a35e76a1241cb82ba38191691335f9142840ab` |
| provenance/source-extraction.v1.json | `221dc36f859cc9c1aeb5fde0ee591024bb4c9bc5cabd9fd7f5d305db5f722009` |
| provenance/test-parity.v1.json | `97be8b6a5a8e2a3bb1519a6198c219e09d7474ecbcd017227100cc2b57d491cc` |
| provenance/dependency-toolchain.v1.json | `5990cfa19f036b7defebd6764052cb341b98ce46608d2c977aee2fef499d6ef2` |

## Extraction facts

- 57 tracked target blobs: 36 source-derived (32 byte-identical closure blobs + 4 meta
  relocations/copies) + 21 target-only authored files.
- Source-derived rows: 23 production `src/` modules (copied), 8 `tests/product` parity
  leaves (copied), `tools/product/toolchain-preflight.mjs` (copied, reached by a literal
  static import in `packaged-product.test.ts`), `product/source-boundary.v1.json` (copied),
  `product/package.template.json`→`package.json`, `product/npm-shrinkwrap.json`→
  `npm-shrinkwrap.json`, `schemas/product/runtime-config.v1.schema.json`→
  `schemas/runtime-config.v1.schema.json` (relocated).
- Every source-derived blob verified byte-identical: `git hash-object` of each target file
  equals the pinned source blob OID; the independent operator audit confirmed byte equality
  against the pinned source object database.
- Reviewed rewrite and exclusion allowlists are empty. Production TypeScript,
  `product/source-boundary.v1.json`, and the eight `tests/product` files are byte and mode
  identical.

## Commands and results

All source reads used the AC1 sanitized Git envelope (`env -i HOME=<scratch> LC_ALL=C TZ=UTC
PATH=/usr/local/bin:/usr/bin:/bin GIT_CONFIG_NOSYSTEM=1 GIT_CONFIG_GLOBAL=/dev/null
GIT_ATTR_NOSYSTEM=1 GIT_NO_REPLACE_OBJECTS=1 /usr/local/bin/git`), pinned git 2.37.3,
Node 22.22.1, npm 10.9.4.

| Command | Result |
|---|---|
| `node tools/check-provenance.mjs` | exit 0, `ok:true`, 56-row partition, 21 target-only |
| `node tools/check-boundary.mjs` | exit 0, `ok:true`, closure resolves locally, external = {ajv, better-sqlite3} |
| `node tools/check-dependencies.mjs` | exit 0, `ok:true`, 43 locked packages, used_external = {ajv, better-sqlite3} |
| `node tools/audit-pinned-extraction.mjs --source-git-dir … --target-git-dir … --out …` | verdict **PASS**, source_tree 2847, target_tree 57, 56 rows, 0 byte divergences |
| `git fsck --full --no-reflogs --unreachable` | empty (no dangling/unreachable) |
| object-set == reachable-set (`cat-file --batch-all-objects` vs `rev-list --objects`) | MATCH (78 objects) |
| `vitest run tests/migration tests/product/end-to-end-synthetic.test.ts` | **14 passed / 14** (5 files) |
| `node tools/verify-artifact.mjs --run-id B0/B1` | identical tarball SHA-256 across independent builds |

## Artifact tuple (AC5)

| Field | Value |
|---|---|
| tarball SHA-256 | `d8abbae572bac1a00c93522263d9e8f94112fe582aa7ef2ddf992267e39c970f` |
| ordered members | 27 (`{path,mode,size,sha256}` manifest emitted to `artifact-identity.json`) |
| head_tree | `e13b0cc57365acf9600f06882750982e02412c5a` |
| lock SHA-256 | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |

The `packaged-product` migration test proved two independent builds (B0, B1) share one
tarball SHA-256, member manifest, tree, and lock hash, and that the tarball installs offline
into a clean prefix. The tarball builder is deterministic by construction (pure ustar writer
with fixed mtime/uid/gid, byte-sorted members, gzip mtime zeroed) rather than relying on
`npm pack` mtime behavior.

## Interpretation decisions and one genuine tension (for reviewer/founder adjudication)

1. **Eight product tests are byte-parity leaves, not executed.** They reference item-132
   qualification tooling and CI files via runtime `join(REPO_ROOT, '<literal>')` construction
   passed to `spawnSanitizedChild`/`readFileSync`. Those are computed-base runtime paths, not
   module edges, so the fixed-point resolver records them as parity-only references and does
   not follow them. Executed suite = `tests/migration/**` + `tests/product/end-to-end-synthetic.test.ts`.
2. **AC3↔AC5 schema-path tension (surfaced, not hidden).** The byte-identical
   `src/product/config.ts:55` loads its schema at runtime from
   `../../schemas/product/runtime-config.v1.schema.json`, and the byte-identical `package.json`
   `files` field references that same `schemas/product/...` path — but AC5 names the committed
   schema `schemas/runtime-config.v1.schema.json`. Because production TS and package.json are
   byte-immutable, the only build-coherent reconciliation is: commit the schema at the AC5 path
   (`schemas/runtime-config.v1.schema.json`) and have `verify-artifact.mjs` stage it into the
   tarball at `schemas/product/runtime-config.v1.schema.json` so the unchangeable runtime loader
   resolves it. This is what was built. **A reviewer should confirm this reconciliation or
   direct the alternative (schema copied same-path at `schemas/product/...`).**
3. **Dev/test toolchain is provisioned out-of-band**, not a runtime dependency: the
   byte-identical `npm-shrinkwrap.json` is runtime-only (ajv, better-sqlite3; 44 packages, zero
   devDeps). TypeScript/`@types` and system helpers are named in
   `provenance/dependency-toolchain.v1.json` and injected via `ECHO_TSC` / a provisioned
   toolchain (`@types/node`, `@types/better-sqlite3` copied into `node_modules/@types` for the
   `verify-artifact.mjs` compile, which is byte-immutable at the accepted OID). The formal
   offline `sandbox-exec` four-clone matrix was run for the builder legs — see the section below.

## AC5/AC7 four-clone offline matrix — builder legs B0/B1/B2

Ran on 2026-07-14 (founder-directed continuation after adjudication). R1 and the AC8 reviewer
leg are intentionally NOT run here — they belong to the independent `codex-ops` reviewer.

Discipline enforced for every leg:
- **Isolated cache fill, no lifecycle:** a shared content-addressed npm cache was filled online
  (`npm ci --ignore-scripts` for the runtime lock + one `npm install <tgz>` for the tarball
  packuments), then copied to a **distinct cache root per run** (`cache2-B0`, `cache2-B1`,
  `cache2-B2`). No package lifecycle code ran during the fill.
- **All lifecycle phases offline under** `/usr/bin/sandbox-exec -p '(version 1) (allow default)
  (deny network*)'`: `npm ci` (runtime deps + native `better-sqlite3` built from source with
  bundled Node 22.22.1 headers via `npm_config_nodedir`, no prebuild/header fetch), build+pack
  (`verify-artifact.mjs`), fresh-project tarball install into a clean prefix, and CLI smoke.
- **Network-denial proven before AND after every lifecycle-bearing phase** (`pre`,
  `post-npm-ci`, `post-build`, `post-install`, `post-smoke`): a DNS probe and a direct-IP
  (`1.1.1.1:443`) socket probe both fail inside the profile.
- **Loopback control (both halves) per leg:** a `127.0.0.1` listener is accepted by a connector
  run OUTSIDE the profile and denied by the same connector run INSIDE it.
- **Provenance of clones:** B0 ran in the accepted target; B1/B2 ran in clones created with the
  sanitized AC1 envelope (`git clone --no-local --no-hardlinks --no-checkout`, empty
  `GIT_TEMPLATE_DIR`, `core.hooksPath` → empty dir), detached checkout of the accepted OID
  `54259ef6…`, origin removed, verified clean / no-remote / no-alternates.
- **Poisoned proxy** (`http(s)_proxy=http://127.0.0.1:9`, empty `NO_PROXY`) and `--offline` on
  every npm invocation.

All three legs succeeded through every phase and produced **one identical identity tuple**:

| Field | Value (shared by B0, B1, B2) |
|---|---|
| tarball SHA-256 | `d8abbae572bac1a00c93522263d9e8f94112fe582aa7ef2ddf992267e39c970f` |
| ordered member manifest | 27 members; manifest SHA-256 `59ed001c9974…` |
| head_tree | `e13b0cc57365acf9600f06882750982e02412c5a` |
| lock SHA-256 | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |

CLI smoke (offline, synthetic config, no credentials/network) on each installed tarball:
`validate-config` → `ok:true`, `filesystem.kind:"local"`; `selftest` → `ok:true`,
`brain_adapter.status:"pending"`, `wedge_executed:false`, `maturity:"DEV"`.

The accepted target repository was left pristine (HEAD `54259ef6…` unchanged, working tree
clean; toolchain `node_modules` moved out after B0). No file in the target changed; this matrix
added only evidence to this migration record on the Project_echo feature branch.

## Difference from Project_echo

The 21 target-only files (README, five provenance JSONs, five provenance schemas, five
`tools/*.mjs` verifiers, four `tests/migration/*.test.ts`, and `tests/product/end-to-end-synthetic.test.ts`)
have no source counterpart. All other target blobs are byte/mode identical to the pinned source,
**except** the founder-adjudicated `package.json` npm-pin transform (see fix cycle below).
Target history is a single root commit; no target remote exists; Project_echo is unchanged.

---

## Fix cycle (2026-07-14, post codex-ops REJECT) — supersedes the tuple above

The independent codex-ops review (record `2026-07-13-133-echo-brain-review.md`, feature child
`90fb2b64`) returned REJECT on two acceptance failures (F1 lint gate not runnable; F2
dependency checker did not enforce the helper/CLI partition it claimed) plus F3 (npm pin), F4
(.DS_Store), F6/F7 (traceability). Founder adjudication #3 approved the fix cycle. This section
records the corrected work. **The accepted target OID, tree, and artifact tuple below supersede
the earlier `54259ef6…` / `d8abbae…` values.**

### New accepted target identity

| Field | Value |
|---|---|
| Accepted target HEAD | `493b558f30d0e7b24dd2ebef883c10285f835f48` |
| Accepted target tree | `98d8549b55cdfd4f10d9452c840c006fa2c7a693` |
| Branch | `migration/2026-07-13-133` (single root commit; archive-and-recreate, not the repo) |
| Tracked blobs | 57 (36 source-derived + 21 target-only) |
| authority / maturity | `false` / `DEV` |

### Corrections

- **F4** — the untracked `.DS_Store` was removed; the accepted working tree is literally clean.
- **F3** — `package.json` now pins npm `10.9.4` in `engines` alongside Node `22.22.1`. This makes
  `package.json` a **relocated-with-transform** row (source `product/package.template.json`); the
  reviewed `provenance/extraction-policy.v1.json` records the sole transform in
  `dispositions.transform_allowlist` with authority "founder adjudication #3, F3" and the exact
  rule "target equals source parsed-JSON with exactly one added key engines.npm=\"10.9.4\"". The
  operator audit (`tools/audit-pinned-extraction.mjs`) verifies this transform semantically
  (parse source + target, confirm only that key was added) instead of byte-equality; every other
  source-derived row remains byte/mode identical. `exclusion_allowlist` stays empty.
- **F2** — `tools/check-dependencies.mjs` now consumes the helper/CLI partition: it scans
  spawn/exec command invocations (direct string literals, `const NAME='…'` bindings, and
  tuple-dispatch `['cmd',[args]]` lists), and requires every command to be a declared
  `system_helper` (by absolute path or name) or `javascript_cli`; a computed spawn is allowed only
  in a declared `computed_command_owner`. `provenance/dependency-toolchain.v1.json` gains the
  previously-missing `clang++`, `xcode-select`, `xcrun` helper rows (invoked by
  `tools/product/toolchain-preflight.mjs`), the `@types/node@22.10.5` + `@types/better-sqlite3@7.6.11`
  `build_inputs`, and registry integrity digests for `typescript`, `vitest`, `eslint`,
  `typescript-eslint`, `@types/*` (replacing `provisioned-out-of-band`). `tests/migration/dependency-set.test.ts`
  adds omission and evasion fixtures (undeclared helper invocation, dropped manifest row, and a
  computed spawn in a non-owner file each make the checker exit nonzero).
- **F1 (lint, per adjudication #3)** — the AC7 lint gate runs ESLint provisioned out-of-band via
  an explicit `--config` against a scratch flat config that is **not committed** to the target.
  Its bytes (below) and SHA-256 `eb0562e63321f18f5ded9edfbb5fcb0c2058054455bccfb7458030c104b4a84c`
  are recorded here; `provenance/dependency-toolchain.v1.json.lint` names the CLI (`eslint@9.18.0`),
  the config name, its digest, the invocation, and `committed_to_target:false`. The target
  `README.md` was corrected to describe this accurately (no phantom committed linter).

### Scratch ESLint config bytes (recorded, not committed; sha256 `eb0562e6…`)

```
// echo-brain scratch lint config (provisioned out-of-band; never committed to the target).
import tseslint from 'typescript-eslint';

export default tseslint.config({
  files: ['src/**/*.ts', 'tools/**/*.mjs', 'tests/migration/**/*.ts', 'tests/product/end-to-end-synthetic.test.ts'],
  extends: [...tseslint.configs.recommended],
  languageOptions: { ecmaVersion: 2023, sourceType: 'module' },
  rules: {
    '@typescript-eslint/no-explicit-any': 'off',
    '@typescript-eslint/no-unused-vars': 'off',
    'no-empty': 'off',
    '@typescript-eslint/no-empty-function': 'off',
  },
});
```

### Re-verification (this fix cycle)

- `check-provenance`, `check-boundary`, `check-dependencies` exit 0 against the new HEAD;
  `check-dependencies.used_commands` now includes `clang++`/`xcode-select`/`xcrun`.
- Independent `audit-pinned-extraction` (transform-aware) verdict **PASS**, 0 errors, 57 target
  blobs, 56 partition rows, 21 target-only, package.json transform verified.
- `vitest run` **18/18** across 5 files (incl. the 3 new F2 fixtures and the clang++ helper check).
- `git fsck --full --no-reflogs --unreachable` empty; object-set == reachable-set (78 objects).

### AC5/AC7 four-clone offline matrix re-run (builder legs B0/B1/B2, now including a lint leg)

Same discipline as the prior matrix (isolated no-lifecycle cache fill, distinct cache root per
run, all phases under `sandbox-exec -p '(version 1) (allow default) (deny network*)'`, DNS +
direct-IP probes failing before/after every lifecycle phase, both-half loopback control, native
`better-sqlite3` built offline from source with bundled Node 22.22.1 headers, B0 in the accepted
target and B1/B2 in sanitized AC1 clones of `493b558f…`). **A lint phase was added**: ESLint via
the scratch `--config`, offline under the sandbox, with a post-lint denial probe. All three legs
succeeded through npm ci / build+pack / **lint** / tarball install / CLI smoke and produced one
identical identity tuple:

| Field | Value (shared by B0, B1, B2) |
|---|---|
| tarball SHA-256 | `72a32d2dcf34625856512b82f914596a77f5c62b1220b4ef96c9757e20a1922b` |
| ordered members | 27 |
| **canonical member-manifest digest (F7)** | `1f9dbd66932a6120e9cfad90b1c820faf5052ca9cb8e481772744289460467ae` |
| head_tree | `98d8549b55cdfd4f10d9452c840c006fa2c7a693` |
| lock SHA-256 | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |

**F7 canonical serialization (spelled out):** the manifest digest is `SHA-256` over the UTF-8
bytes of the compact JSON (`separators (",",":")`, no whitespace) of the member array in
byte-sorted `path` order, each object emitted with keys in the exact order `path`, `mode`,
`size`, `sha256`. CLI smoke per leg: `validate-config` → `ok:true`, `filesystem.kind:"local"`;
`selftest` → `wedge_executed:false`, `maturity:"DEV"`.

**F6 argv/environment traceability:** each leg wrote `argv-echo.log` echoing the exact
`sandbox-exec` profile, full argv, and environment (including `HOME`, isolated
`npm_config_cache`, `npm_config_offline=true`, `npm_config_nodedir`, `npm_config_build_from_source`,
`ECHO_TSC`, and the lint `--config` path) for every sandboxed lifecycle command.

**F6 proxy wording (aligned with observed practice):** every offline npm invocation runs with a
**poisoned** proxy (`http_proxy=https_proxy=http://127.0.0.1:9`) and empty `NO_PROXY`/`no_proxy`,
not with proxy variables absent. Network denial is enforced by the sandbox profile; the poisoned
proxy is defense-in-depth, and this record states the poisoned-proxy fact rather than an
"absent-proxy" claim.

The accepted target `493b558f…` is byte-for-byte unchanged by the matrix (working tree clean;
toolchain `node_modules` moved out after B0). R1 rerun and codex-ops re-judgment follow this
re-handoff under the standing Option B split.

---

## Fix cycle 2 (2026-07-14, post second codex-ops REJECT) — supersedes ALL tuples/hashes above

The second codex-ops re-judgment (record `2026-07-13-133-echo-brain-review.md`, feature child
`4e43c996`) confirmed F3/F4/F5 fixed but rejected on N2 (a real checker gap) plus N3/N4/N5/N6.
N1 is the executor's R1 lint-binding defect (reviewer side), not the target. **This section's
accepted OID, tuple, and hash table supersede every earlier value in this record.**

### New accepted target identity

| Field | Value |
|---|---|
| Accepted target HEAD | `ffc11b45ca42fba19e25582f00e92dbaa63c9a3c` |
| Accepted target tree | `27250ad06e90d6ed4534dc6e50f880874818e659` |
| Branch | `migration/2026-07-13-133` (single root; archive-and-recreate) |
| Tracked blobs | 57 (36 source-derived + 21 target-only) |
| authority / maturity | `false` / `DEV`; no remote |

### N4 — complete fresh content-hash table at `ffc11b45…` (SHA-256)

| Path | SHA-256 |
|---|---|
| `package.json` | `e7dd03dfce75c3ae4053541bb813b17f228dc6e15cec32aa268d0adbf3320736` |
| `npm-shrinkwrap.json` (sole lock) | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |
| `README.md` | `ae9a62af323194c09124fa2023fd66420488f8f8788646938df65b30b4de8e4f` |
| `provenance/extraction-policy.v1.json` | `771a59ed73be2221ef10d1814beeb6d8d5cc06b937186407332f3cde67072945` |
| `provenance/source-plan.v1.json` | `3874b30f28955ff07850b9c841a35e76a1241cb82ba38191691335f9142840ab` |
| `provenance/source-extraction.v1.json` | `d8e66f2b8d7ef6898bd2402fcbc00d0cb151fd48e24ee64bcd8e5015ba07c6e1` |
| `provenance/test-parity.v1.json` | `97be8b6a5a8e2a3bb1519a6198c219e09d7474ecbcd017227100cc2b57d491cc` |
| `provenance/dependency-toolchain.v1.json` | `5990cfa19f036b7defebd6764052cb341b98ce46608d2c977aee2fef499d6ef2` |

### N2 — `check-dependencies.mjs` now enforces the exhaustive command partition

- **`spawnSanitizedChild` is recognized** as a child_process spawner (it is the repo's sanctioned
  owner, `src/product/spawn-sanitized-child.ts`). The `/sbin/mount` edge at `src/product/config.ts:165`
  is now enforced — `check-dependencies.used_commands` includes `/sbin/mount`, and dropping the
  `/sbin/mount` row would be detected. A fixture drives an undeclared command through
  `spawnSanitizedChild` and confirms the checker fails.
- **Computed-command acceptance is fail-closed per variable.** The previous file-level
  `hasTuple` whitelist is removed: a computed spawn is accepted only when the file is a declared
  `computed_command_owner` OR the spawn variable is directly destructured from a tuple
  (`[X, …]`). A fixture proves a computed spawn (`process.env`-derived) in a file that ALSO
  contains an unrelated tuple still fails.
- **`tools/product/toolchain-preflight.mjs` is a declared `computed_command_owner`** with recorded
  rationale: its spawn command is fed through an injected `run()` wrapper and `which(name)`, so
  the command is statically unprovable, but the full set of commands it can run is enumerated in
  its `commandChecks` tuple and every tuple entry (`python3`/`make`/`clang`/`clang++`/`xcode-select`/
  `xcrun`/`node`/`npm`) is independently classified against the manifest — removing any helper row
  still fails (the F2 "dropping a helper row" fixture proves this).
- The executed suite is now **21 tests / 21** (5 files), incl. the three new N2 fixtures.

### N3 — AC5 sanitized-environment evidence, strengthened

Every sandboxed lifecycle invocation now runs under strict **`env -i`** with an explicit
allowlist (`PATH=/usr/local/bin:/usr/bin:/bin`, `HOME`, `LC_ALL=C`, `TZ=UTC`, the isolated
`npm_config_*`/`nodedir`/`build_from_source`, poisoned proxy for npm; `ECHO_TSC` for build;
`--config` for lint). Inherited `NODE_OPTIONS`/`NODE_PATH`/`DYLD_*`/`LD_PRELOAD`/npm/Git variables
are therefore absent by construction. The runner itself is launched under `env -i` and writes a
durable `runner-env.txt` outer transcript; the runner asserts (and this cycle confirmed, 0 hits)
that no dangerous inherited variable survives. Each leg's `argv-echo.log` now records **all six**
sandboxed commands — npm ci, build+pack, lint, tarball install, **validate-config**, and
**selftest** — not four.

### N5 — README corrected

`README.md` now states that `product/package.template.json` was relocated to `package.json` with
exactly the one founder-adjudicated npm-pin transform (the sole `transform_allowlist` entry,
operator-audit-verified), instead of "without content change".

### N6 — manifest ordering comparator named correctly

`tools/verify-artifact.mjs` orders members with **JavaScript `String.prototype.localeCompare`**
(via `Array.prototype.sort((a,b) => a.name.localeCompare(b.name))`), not raw-byte ordering. The
comparator is unchanged (switching would churn the tuple for a wording fix); this record names it
accurately. The F7 canonical member-manifest digest is `SHA-256` over the UTF-8 bytes of the
compact JSON (`separators (",",":")`) of the member array **in `String.localeCompare` order**,
each object with keys `path`, `mode`, `size`, `sha256`.

### Re-verification and matrix re-run (builder legs B0/B1/B2 + lint leg)

- `check-provenance`/`check-boundary`/`check-dependencies` exit 0; transform-aware operator audit
  **PASS** (0 errors, 57 blobs, 21 target-only); `vitest` **21/21**; `git fsck --full --no-reflogs
  --unreachable` empty; object-set == reachable-set (78 objects).
- Full four-clone offline matrix re-run under `sandbox-exec -p '(version 1) (allow default) (deny
  network*)'` with the N3 `env -i` discipline, sanitized runner, per-command argv echoes,
  distinct cache roots, B0 in the accepted target and B1/B2 in sanitized AC1 clones of `ffc11b45…`.
  All three legs share one identity tuple:

| Field | Value (shared by B0, B1, B2) |
|---|---|
| tarball SHA-256 | `b7708d8f195662a9180347ea0a52e6440af3b572fa2a6248c61e146d65f26e8b` |
| ordered members | 27 |
| canonical member-manifest digest | `f868ad68125b2d0943f98793419784ba7399357eaf3ecd13f770a55d8f25cc24` |
| head_tree | `27250ad06e90d6ed4534dc6e50f880874818e659` |
| lock SHA-256 | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |

CLI smoke per leg (offline, synthetic config): `validate-config` → `ok:true`,
`filesystem.kind:"local"`; `selftest` → `wedge_executed:false`, `maturity:"DEV"`.

The accepted target `ffc11b45…` is byte-for-byte unchanged by the matrix (working tree clean;
toolchain `node_modules` moved out). R1 rerun + codex-ops re-judgment follow under the Option B
split. Per the standing rule, a third rejection on any NEW finding class halts to the founder.

---

## Micro-cycle (2026-07-14, post third codex-ops REJECT) — supersedes all tuples/hashes above

The third codex-ops judgment (record `2026-07-13-133-echo-brain-review.md`, feature child
`2b4ebbe8`) confirmed F1/F3–F7 and N1/N3–N6 all fixed and the four-clone tuple matched again; it
rejected on a single same-class residual (founder-authorized a surgical micro-cycle for exactly
it). **This section's accepted OID, tuple, and hash table supersede every earlier value.**

### New accepted target identity

| Field | Value |
|---|---|
| Accepted target HEAD | `4a6dcb33d2e73f718e36832941b4fa2bc34c7285` |
| Accepted target tree | `e2466238d90691cc4488b9909ec31064a1b3df33` |
| Branch | `migration/2026-07-13-133` (single root; archive-and-recreate) |
| Tracked blobs | 57 (36 source-derived + 21 target-only); authority `false`; maturity `DEV`; no remote |

### The one residual (N2 `DESTRUCTURE_RE`) — fixed

The prior `DESTRUCTURE_RE = /\[\s*([A-Za-z_$][\w$]*)\s*,/g` matched **any** bracketed array
beginning with an identifier, so an ordinary array *construction* such as
`const unrelated = [c, ['--version']]` falsely recorded `c` as tuple-destructured and let
`spawnSync(c, [])` pass. The regex is now
`/\b(?:const|let|var)\s+\[\s*([A-Za-z_$][\w$]*)\s*,/g` — it requires a `const`/`let`/`var`
binding keyword immediately before the destructuring pattern, so a value-position array literal
is not counted while genuine `const [x, y] = …` / `for (const [x, y] of …)` bindings still are.
A new fixture (`fixture-array-construction.mjs`) drives exactly the reviewer's evasion: a computed
executable whose variable appears only in an ordinary array literal now fails closed. Executed
suite is **22 tests / 22** (5 files). Only `tools/check-dependencies.mjs` and
`tests/migration/dependency-set.test.ts` changed (plus the mechanically-regenerated
`provenance/source-extraction.v1.json`); nothing else was touched.

### N4 — complete fresh content-hash table at `4a6dcb33…` (SHA-256)

| Path | SHA-256 |
|---|---|
| `package.json` | `e7dd03dfce75c3ae4053541bb813b17f228dc6e15cec32aa268d0adbf3320736` |
| `npm-shrinkwrap.json` (sole lock) | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |
| `README.md` | `ae9a62af323194c09124fa2023fd66420488f8f8788646938df65b30b4de8e4f` |
| `provenance/extraction-policy.v1.json` | `771a59ed73be2221ef10d1814beeb6d8d5cc06b937186407332f3cde67072945` |
| `provenance/source-plan.v1.json` | `3874b30f28955ff07850b9c841a35e76a1241cb82ba38191691335f9142840ab` |
| `provenance/source-extraction.v1.json` | `1db0a00eb6a2cf1a06c10b0fe66c8b8157ab94e420333dd49bc62ebd210c8ff9` |
| `provenance/test-parity.v1.json` | `97be8b6a5a8e2a3bb1519a6198c219e09d7474ecbcd017227100cc2b57d491cc` |
| `provenance/dependency-toolchain.v1.json` | `5990cfa19f036b7defebd6764052cb341b98ce46608d2c977aee2fef499d6ef2` |

### Re-verification and matrix re-run (builder legs B0/B1/B2 + lint leg)

- `check-provenance`/`check-boundary`/`check-dependencies` exit 0; transform-aware operator audit
  **PASS** (0 errors, 57 blobs, 21 target-only); `vitest` **22/22**; `git fsck --full
  --no-reflogs --unreachable` empty; object-set == reachable-set (78 objects).
- Full four-clone offline matrix re-run under `sandbox-exec -p '(version 1) (allow default) (deny
  network*)'` with the strict `env -i` discipline, sanitized runner transcript, six per-command
  argv echoes per leg, distinct cache roots, B0 in the accepted target and B1/B2 in sanitized AC1
  clones of `4a6dcb33…`. All three legs share one identity tuple:

| Field | Value (shared by B0, B1, B2) |
|---|---|
| tarball SHA-256 | `b7708d8f195662a9180347ea0a52e6440af3b572fa2a6248c61e146d65f26e8b` (unchanged — no tarball member changed) |
| ordered members | 27 |
| canonical member-manifest digest | `f868ad68125b2d0943f98793419784ba7399357eaf3ecd13f770a55d8f25cc24` (unchanged) |
| head_tree | `e2466238d90691cc4488b9909ec31064a1b3df33` (new — reflects the checker + test changes) |
| lock SHA-256 | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |

The tarball/manifest are unchanged because `check-dependencies.mjs` and the migration test are not
tarball members; only the Git tree moved. CLI smoke per leg: `validate-config` → `ok:true`,
`filesystem.kind:"local"`; `selftest` → `wedge_executed:false`, `maturity:"DEV"`.

The accepted target `4a6dcb33…` is byte-for-byte unchanged by the matrix (working tree clean).
Two executor-disclosed notes from the third judgment are recordable-only and NOT changed here:
the `typescript-eslint@8.20.0` vs TypeScript `5.9.2` peer-range needing `--legacy-peer-deps`
(reconcile before any qualification/portability claim), and the pre-lifecycle sanitizer-regex
abort (a harness preflight correction, no lifecycle retried). R1 pass #4 + judgment #4 follow.

---

## Builder remediation (2026-07-14, post independent HIGH sidecar) — supersedes all target-bound proofs above

The latest independent sidecar rejected `4a6dcb33…` because the dependency checker still trusted
any destructured command variable, regardless of the right-hand-side provenance. The exact
review fixture was reproduced against the old target in a disposable clone:

```js
const attackerControlled = [process.env.SNEAKY, []];
const [command, args] = attackerControlled;
spawnSync(command, args);
```

After committing that fixture on top of the old candidate as disposable commit `5f80742`,
`node tools/check-dependencies.mjs` exited **0** with `"ok":true`. That is the pre-fix fail-open
proof. Against the candidate below, the same exact fixture exits **1** with:

```text
computed spawn command 'command' in tools/fixture-arbitrary-rhs.mjs (not a declared computed_command_owner)
```

### Current candidate identity

| Field | Value |
|---|---|
| Target HEAD | `957ad4680f6c67d15fb3dfa0941b52c2ab9c3110` |
| Target tree | `0a34ef4aa27ca460b0697773c78a2281ff534f31` |
| Branch | `migration/2026-07-13-133` |
| Commit topology | one parentless root; one local branch; no remotes |
| Tracked files | 57; clean working tree |
| authority / maturity | `false` / `DEV` |

The old-to-new candidate delta is exactly three modified paths, with no additions or deletions:

- `tools/check-dependencies.mjs`
- `tests/migration/dependency-set.test.ts`
- `provenance/source-extraction.v1.json` (mechanically regenerated hashes)

The checker no longer treats binding syntax as executable provenance. A computed spawn command is
accepted only in one of the three explicit, reviewed owners:
`src/product/spawn-sanitized-child.ts`, `tools/verify-artifact.mjs`, or
`tools/product/toolchain-preflight.mjs`. Literal command tuples remain independently enumerated
and classified. The regression suite includes the reviewer's exact arbitrary-RHS fixture and a
same-class adversarial case proving that even a literal-tuple destructure in a non-owner file is
not trusted merely because it uses destructuring syntax.

### Complete current content hashes (SHA-256)

| Path | SHA-256 |
|---|---|
| `package.json` | `e7dd03dfce75c3ae4053541bb813b17f228dc6e15cec32aa268d0adbf3320736` |
| `npm-shrinkwrap.json` | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |
| `README.md` | `ae9a62af323194c09124fa2023fd66420488f8f8788646938df65b30b4de8e4f` |
| `provenance/extraction-policy.v1.json` | `771a59ed73be2221ef10d1814beeb6d8d5cc06b937186407332f3cde67072945` |
| `provenance/source-plan.v1.json` | `3874b30f28955ff07850b9c841a35e76a1241cb82ba38191691335f9142840ab` |
| `provenance/source-extraction.v1.json` | `89b2d0d3d26a4bef4e814588b75f463c59da57225377634e215baf986989ce39` |
| `provenance/test-parity.v1.json` | `97be8b6a5a8e2a3bb1519a6198c219e09d7474ecbcd017227100cc2b57d491cc` |
| `provenance/dependency-toolchain.v1.json` | `5990cfa19f036b7defebd6764052cb341b98ce46608d2c977aee2fef499d6ef2` |

Changed-file Git blob OIDs are `64f426b57e17b4f45ae754556f51cc2b1c5aa5c4`
(`check-dependencies.mjs`), `4090f1c06e22ef6a77cc31e0a8041bd5822e01f2`
(`dependency-set.test.ts`), and `43d0b639b56a945984bb11580c5e4b8c718984fa`
(`source-extraction.v1.json`).

### Static, security, and test proof at the exact candidate

| Command / proof | Result |
|---|---|
| `node tools/check-provenance.mjs` | PASS; `ok:true`; partition 56; target-only 21 |
| `node tools/check-boundary.mjs` | PASS; `ok:true`; closure 23; external `{ajv, better-sqlite3}` |
| `node tools/check-dependencies.mjs` | PASS; `ok:true`; 43 locked packages; all used commands classified |
| focused `vitest run tests/migration/dependency-set.test.ts` | PASS; 14/14 |
| full `vitest run tests/migration tests/product/end-to-end-synthetic.test.ts` | PASS; 25/25 across 5 files |
| TypeScript typecheck | PASS; no diagnostics |
| exact ESLint invocation with scratch config SHA-256 `eb0562…` | PASS; zero findings |
| `node tools/audit-pinned-extraction.mjs` against pinned Project_echo `.git` | PASS; source tree 2847; target 57; partition 56; target-only 21; errors `[]` |
| `gitleaks git --no-banner --redact` 8.30.1 | PASS; one commit / about 392 KB; no leaks |
| `git fsck --full --no-reflogs --unreachable` | PASS; empty |
| all-object set vs reachable-object set | MATCH; 78/78 |
| no-follow disk-vs-HEAD enumeration | MATCH; 57 regular files / 57 tracked; no extra or missing paths |
| symlink / gitlink / residue audit | 0 filesystem symlinks; 0 tracked symlinks; 0 gitlinks; no `node_modules`, `.DS_Store`, or `package-lock.json` |
| lock metadata audit | 43/43 registry rows have metadata; license set limited to MIT/ISC/BSD/Apache/WTFPL expressions recorded by the lock |

### Exact four-leg lifecycle matrix at `957ad468…`

B0 ran in the accepted target; B1, B2, and R1 ran in separate fresh sanitized clones detached at
the exact candidate, with origin removed and no alternates/promisor/replace state. Every runner
was launched under strict `env -i`; every lifecycle-bearing command ran under the deny-network
`sandbox-exec` profile with distinct offline caches, pinned Node headers, poisoned proxies, and
pre/post DNS plus direct-IP denial probes. Each leg also passed the two-half loopback control.

All four legs passed, each recording exactly six sandboxed argv echoes and zero dangerous
inherited runner-environment variables. Every leg passed offline `npm ci`, deterministic
build+pack, lint, clean-prefix tarball install, `validate-config`, and `selftest`. The shared
identity tuple is:

| Field | B0 = B1 = B2 = R1 |
|---|---|
| target HEAD | `957ad4680f6c67d15fb3dfa0941b52c2ab9c3110` |
| head tree | `0a34ef4aa27ca460b0697773c78a2281ff534f31` |
| tarball SHA-256 | `b7708d8f195662a9180347ea0a52e6440af3b572fa2a6248c61e146d65f26e8b` |
| lock SHA-256 | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |
| ordered members | 27 |
| canonical member-manifest digest | `f868ad68125b2d0943f98793419784ba7399357eaf3ecd13f770a55d8f25cc24` |

Every smoke returned `ok:true`; `validate-config` reported local/APFS filesystem and DEV
maturity, and `selftest` reported `brain_adapter:"pending"`, `wedge_executed:false`, and DEV
maturity. Each clone's final audit reproduced the exact HEAD/tree, 78/78 reachable objects,
clean status, zero remotes, no alternates/promisor/replace state, no symlinks/residue, and a
passing `git fsck`.

The formal four lifecycle legs passed on their first execution. Two earlier attempts to invoke
the *separate full Vitest suite* used incomplete harness arguments: the first omitted
`npm_config_nodedir`/`npm_config_build_from_source`, so a nested offline install correctly failed
when it could not fetch Node headers; the second supplied those variables but retained Vitest's
default five-second timeout, so the nested native install timed out. The complete strict-env
invocation with 180-second test and hook timeouts passed 25/25, with no intervening target-code
change. A scratch no-follow enumeration helper also initially applied `Buffer.compare` to
strings; correcting that scratch-only comparator produced the 57/57 proof above and did not
alter the target.

R1 in this section is a **builder-operated mechanical reproduction**, not independent reviewer
judgment. The existing independent rejection and sidecar remain unchanged. Fresh independent
review of this exact OID/tree and its proofs is still required before item 133 can be accepted.

---

## Builder remediation 2 (2026-07-14, post scope-shadow REJECT) — supersedes all target-bound proofs above

The fresh independent review committed at feature child `1caf83fc63c83f044a2f0d2608a85bcf4759bbcc`
rejected `957ad4680f6c67d15fb3dfa0941b52c2ab9c3110`: a file-global literal-binding map allowed an
outer `const command = 'git'` to authorize an attacker-controlled same-name command in an inner
scope. This bounded remediation removes that name-based authorization entirely. Outside the
three unchanged whole-file `computed_command_owners`, every nonliteral executable expression
now fails closed; binding syntax is not treated as provenance.

### Current candidate identity

| Field | Value |
|---|---|
| Target HEAD | `41c28171c64710b3ad23392a2606d75cfe8e7b2c` |
| Target tree | `5691ab527de8eb622ed1d333ed867a2191afdf8a` |
| Branch | `migration/2026-07-13-133` |
| Commit topology | one parentless root; one local branch; no remotes |
| Tracked files / objects | 57 files; 78/78 all objects reachable |
| Working tree | clean; no `node_modules` |
| authority / maturity | `false` / `DEV` |

The exact old-to-new delta from `957ad4680f6c67d15fb3dfa0941b52c2ab9c3110` is three
modified paths, with no additions or deletions:

- `tools/check-dependencies.mjs`
- `tests/migration/dependency-set.test.ts`
- `provenance/source-extraction.v1.json` (mechanically regenerated hashes)

The checker still classifies direct literal commands and literal tuples independently. Its
reviewed computed-command owner list remains exactly:

- `src/product/spawn-sanitized-child.ts`
- `tools/verify-artifact.mjs`
- `tools/product/toolchain-preflight.mjs`

Spawner discovery was tightened at the same boundary so the fail-closed rule also applies to
aliased named imports, namespace/default `node:child_process` imports, named-destructure and
namespace `require` bindings, and dot or literal-bracket member calls. Canonical bare spawner
names remain conservatively recognized. No new owner or executable was added.

Four committed regressions cover the fresh review fixture and the requested residual probes:

1. the exact outer-literal / inner attacker-controlled same-name shadowing case;
2. a `let command` initialized to an allowed literal and later reassigned to attacker input;
3. an aliased named `child_process` import with an undeclared executable; and
4. a `child_process` namespace-member call with an undeclared executable.

The prior exact arbitrary-RHS and literal-destructure regressions remain. A test-helper-only
`const GIT = '/usr/local/bin/git'` binding was replaced with direct command literals so the
fixture harness follows the same conservative policy; this does not widen production behavior.

### Complete current content hashes (SHA-256)

| Path | SHA-256 |
|---|---|
| `package.json` | `e7dd03dfce75c3ae4053541bb813b17f228dc6e15cec32aa268d0adbf3320736` |
| `npm-shrinkwrap.json` | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |
| `README.md` | `ae9a62af323194c09124fa2023fd66420488f8f8788646938df65b30b4de8e4f` |
| `provenance/extraction-policy.v1.json` | `771a59ed73be2221ef10d1814beeb6d8d5cc06b937186407332f3cde67072945` |
| `provenance/source-plan.v1.json` | `3874b30f28955ff07850b9c841a35e76a1241cb82ba38191691335f9142840ab` |
| `provenance/source-extraction.v1.json` | `221dc36f859cc9c1aeb5fde0ee591024bb4c9bc5cabd9fd7f5d305db5f722009` |
| `provenance/test-parity.v1.json` | `97be8b6a5a8e2a3bb1519a6198c219e09d7474ecbcd017227100cc2b57d491cc` |
| `provenance/dependency-toolchain.v1.json` | `5990cfa19f036b7defebd6764052cb341b98ce46608d2c977aee2fef499d6ef2` |

Changed-file Git blob OIDs are `59eac78635ef64609f40c3ea6304b7dd634cd3c3`
(`check-dependencies.mjs`), `586c28352b22c0fe3bfdad34afc02949512c224c`
(`dependency-set.test.ts`), and `6ce732c2ac78e5383eb1284fbfe9279f7fc9aac5`
(`source-extraction.v1.json`).

### Static, security, and test proof at the exact candidate

| Command / proof | Result |
|---|---|
| `node tools/check-provenance.mjs` | PASS; `ok:true`; partition 56; target-only 21 |
| `node tools/check-boundary.mjs` | PASS; `ok:true`; closure 23; external `{ajv, better-sqlite3}` |
| `node tools/check-dependencies.mjs` | PASS; `ok:true`; 43 locked packages; expected command set; errors `[]` |
| focused `vitest run tests/migration/dependency-set.test.ts` | PASS; 18/18 |
| full `vitest run tests/migration tests/product/end-to-end-synthetic.test.ts` | PASS; 29/29 across 5 files |
| TypeScript typecheck | PASS; no diagnostics in every lifecycle leg |
| exact ESLint invocation with scratch config SHA-256 `eb0562e63321f18f5ded9edfbb5fcb0c2058054455bccfb7458030c104b4a84c` | PASS; zero findings in every leg |
| `node tools/audit-pinned-extraction.mjs` against pinned Project_echo `.git` | PASS; source tree 2,847; target 57; partition 56; target-only 21; errors `[]`; output SHA-256 `ec19e08a89650d278a788ade116b9a3a4087b37b5b73fd32570d07f87ed70a96` |
| `gitleaks git --no-banner --redact` 8.30.1 | PASS; one commit / about 397 KB; no leaks |
| `git fsck` and all-object/reachable comparison | PASS; empty fsck; 78/78 |

The first attempted full-suite harness was incompletely provisioned: it omitted target runtime
and type dependencies, so 23 tests passed while two packaged-product tests failed and one suite
could not load Ajv. That result was discarded as a harness setup miss, no target byte changed,
and the definitive strict offline invocation with the validated B0 dependency set passed all
29 tests. Separately, an unsanitized diagnostic `bash -x` attempt was discarded before the
canonical lifecycle matrix after lint could not resolve `typescript-eslint` from a copied
scratch-config location. Its outputs/cache were reset, no target byte changed, and the strict
`env -i` B0 rerun below passed on its canonical execution.

### Exact four-leg lifecycle matrix at `41c28171…`

B0 ran in the accepted target. B1, B2, and builder-operated R1 ran in separate sanitized clones
created with `clone --no-local --no-hardlinks --no-checkout`, detached at the exact candidate,
with origin removed and no alternate/promisor/replace state. Each canonical runner was launched
through strict `env -i` with only `PATH`, `HOME`, `LC_ALL=C`, and `TZ=UTC`; the per-leg dangerous
runner-variable capture was zero bytes.

Every lifecycle-bearing command ran under the `sandbox-exec` deny-network profile with a
distinct offline cache, poisoned proxies, pinned Node 22.22.1 headers, and build-from-source
native dependencies. Each leg recorded six sandboxed argv lines, 24 successful denial probes
(DNS and direct IP before and after each of six phases), and both loopback-control halves
(allowed outside, denied inside). Offline `npm ci`, build+pack, lint, clean-prefix tarball
install, `validate-config`, and `selftest` all passed.

| Field | B0 = B1 = B2 = builder R1 |
|---|---|
| target HEAD | `41c28171c64710b3ad23392a2606d75cfe8e7b2c` |
| head tree | `5691ab527de8eb622ed1d333ed867a2191afdf8a` |
| tarball SHA-256 | `b7708d8f195662a9180347ea0a52e6440af3b572fa2a6248c61e146d65f26e8b` |
| lock SHA-256 | `9ffc39fa013a67517d95399c80759a4fd359ce1ab1ccc5ee0e957504796ab296` |
| ordered members | 27 |
| canonical member-manifest digest | `f868ad68125b2d0943f98793419784ba7399357eaf3ecd13f770a55d8f25cc24` |
| object / file audit | 78/78 reachable objects; 57 tracked files |

Every `validate-config` smoke returned `ok:true`, local/APFS, `maturity:"DEV"`, and
`wedge_executed:false`. Every `selftest` returned `ok:true`, pending brain adapter,
`wedge_executed:false`, and `maturity:"DEV"`. The target was restored to a clean, remote-free
state with no `node_modules`; its HEAD, tree, refs, and 78-object closure were unchanged by the
matrix.

R1 here is a **builder-operated mechanical reproduction**, not fresh independent judgment. The
immutable REJECT record remains unchanged. A different reviewer must inspect this exact OID/tree
and run the required fresh review before acceptance. This remediation transfers no authority,
does not authorize installation or release, and does not advance the candidate beyond DEV.
