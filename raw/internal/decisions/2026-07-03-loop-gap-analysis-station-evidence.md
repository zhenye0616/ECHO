
================================================================================
## STATION 1 — CAPTURE  [verdict: partial]


### Eng-domain capture (CC/Codex/Cursor transcripts, git, fs)  (exists)
  - src/capture/extractors/claude-code.ts:520 — startClaudeCodeExtractor watches ~/.claude/projects/ JSONL, emits turn candidates via processCandidate
  - src/capture/extractors/codex.ts:766 — startCodexExtractor watches ~/.codex/sessions/ rollout JSONL
  - src/capture/extractors/cursor.ts:1235 — startCursorExtractor reads Cursor state.vscdb bubbles/composers with chokidar + 15s repoll
  - src/capture/surfaces/git-watcher.ts:332 — startGitWatcher polls/fs-watches repos, emits per-commit atoms (diff, stats, changed files)
  - src/capture/surfaces/fs-watcher.ts:91 — startFsWatcher emits add/change/unlink candidates for allowlisted path prefixes
  - src/daemon/index.ts:72 — daemon boots all six surfaces concurrently against the shared store
  - src/capture/pipeline.ts:30 — processCandidate is the single chokepoint (gate + timestamp canonicalize + append) all six surfaces call

### Meeting-domain capture (Granola notes/transcripts)  (exists)
  - src/capture/surfaces/granola-poller.ts:695 — startGranolaPoller runs a checkpointed interval loop against the Granola API
  - src/capture/surfaces/granola-poller.ts:579 — pollGranolaOnce paginates since high-water mark, converts each new note, checkpoints per note
  - src/capture/surfaces/granola-poller.ts:490 — granolaNoteToCaptureEvents emits a [summary, transcript] atom pair with dedupe_key and attendee/calendar/web_url metadata, through processCandidate
  - src/capture/sources.ts:18 — 'granola' is on the api allowlist so the gate accepts source 'api:granola' (constant at src/capture/surfaces/granola-poller.ts:11)
  - src/daemon/index.ts:72 — poller booted by the daemon alongside the extractors

### Slack-domain capture (intake channel, questions, responder threads → atoms)  (partial)
  - src/surfaces/ceo-slack-responder/decision-store.ts:90 — the ONLY Slack-originated storage.append in src/surfaces + src/brain: a human-confirmed team decision lands as a 'derived:team-decisions' atom (source constant at decision-store.ts:5)
  - src/surfaces/ceo-slack-responder/responder.ts:1404 — handleSocketMessage already receives every allowed-channel Slack envelope in-process (seeds, questions, button actions), but none are fed to processCandidate
  - src/surfaces/ceo-slack-responder/responder.ts:66 + :1051 — question/answer traffic is logged only to a markdown event log (raw/internal/ceo-loop-events.md via appendUsageRecord), not to the atom store
  - src/surfaces/ceo-slack-responder/intake-draft-store.ts:127 — intake conversations (fields, seen events, created issue id/url) persist only in a JSON draft file (~/.echo/state/linear-intake-drafts.json), outside the atom store
  - src/surfaces/ceo-slack-responder/responder.ts:319 — the responder is a separate process opening its own SqliteStorage handle solely for the team-decision store
  GAP: Slack conversations never become atoms: intake seeds, counterpart questions, follow-up threads, and responder answers are surface traffic that evaporates into a JSON draft file and a markdown log. This is load-bearing for station 6 — a drift-triggering statement made in the intake channel is invisible to retrieval. The natural attach point is the responder's Socket Mode event stream (handleSocketMessage, responder.ts:1404) or a dedicated src/capture/surfaces/slack-*.ts surface feeding processCandidate, plus a 'slack' entry in CAPTURED_SOURCES.apis and a normalize adapter. The one path that does land (confirmed decision → decision-store.ts:90) captures the decision record, not the conversation.

### Linear-domain capture (issues, status changes → atoms)  (missing)
  GAP: Linear is write-only and outbound-only: linear-client.ts exposes exactly one operation, the EchoCreateIssue GraphQL mutation (createIssue, src/surfaces/ceo-slack-responder/linear-client.ts:55, mutation text at :69). Verified no issue reads, status polling, or webhook ingestion anywhere in src/ (grep for 'linear' outside src/surfaces hits only intake-field shaping in src/enrich/granola-intake-candidates.ts and src/brain/). The created issue's id/url is recorded only inside the intake draft JSON file (IntakeDraft in intake-draft-store.ts), not as an atom — so Linear data exists in ECHO only at issue-create time and then goes dark. Status backflow (item 112, the demo's station 5) has no capture substrate to read from. Natural attach: a checkpointed poller modeled on granola-poller.ts (src/capture/surfaces/), source 'api:linear', gated through processCandidate.

### Capture gate/allowlist readiness for new sources (Slack, Linear)  (partial)
  - src/capture/gate.ts:74 — gate() is source-kind generic: parseSource (gate.ts:57) accepts any 'api:<name>' and dispatches to isAllowedApi, so 'api:slack'/'api:linear' are structurally supported with zero gate changes
  - src/capture/sources.ts:18 — but CAPTURED_SOURCES.apis is hardcoded to ['granola']; apps and domains are empty objects (sources.ts:10-11), so new API sources are rejected ('unknown_api') until a code edit
  - src/capture/sources.ts:140 — the only runtime-extensible allowlist is git_repos via ~/.echo/state/capture-sources.json (readCaptureSourcesConfig; applied at sources.ts:194); no config path exists for apis/apps/domains
  - src/capture/gate.ts:30 — SourceKind omits 'derived': derived atoms bypass the pipeline chokepoint entirely and self-police via isAllowedDerived + direct storage.append (src/enrich/granola-signals.ts:648, src/surfaces/ceo-slack-responder/decision-store.ts:90)
  GAP: Two readiness caveats. (1) Adding Slack/Linear needs a one-line edit to the hardcoded apis array in src/capture/sources.ts:18 — trivial but a code change, not config; the gate mechanism itself needs nothing. (2) The 'derived:' source class is a sanctioned bypass of the processCandidate chokepoint (gate.ts:57-72 does not recognize the prefix), so any new derived writer must remember to self-check isAllowedDerived — the single-chokepoint invariant is already policy-enforced rather than structurally enforced for this class. Adjacent read-side note: src/normalize/adapters/ covers only claude-code/codex/cursor/git/granola, so new Slack/Linear atoms would also need adapters to be visible to trace/clustering (station 2 territory, flagged here because capture without normalization is half-captured).

SUMMARY: Station 1's eng and meeting edges are solid: five capture surfaces plus the Granola poller all funnel through the single processCandidate/gate chokepoint into the append-only store, booted together by the daemon. The load-bearing gap is that the two org-alignment domains the demo turns on — Slack and Linear — are surfaces, not capture sources. Slack conversation content (intake seeds, counterpart questions, responder threads) never lands as atoms; it persists only in a JSON intake-draft file and a markdown usage log, with the single exception of human-confirmed team decisions (decision-store.ts:90). Linear is outbound write-only: one issueCreate mutation, no issue/status reads anywhere in src/, so the status-backflow scene (item 112) has no substrate to read from. The gate mechanism is ready for both ('api:<name>' parses generically) but the apis allowlist is hardcoded to ['granola'], and the 'derived:' source class bypasses the gate chokepoint entirely, relying on writer self-policing. Net effect for station 6: a decision-contradicting statement made in Slack or reflected in Linear state is currently invisible to retrieval unless it was said in a Granola meeting or an eng tool.
SURPRISES: The architecture map is accurate (every pinned line I checked verified at commit-adjacent positions), but it under-states one thing: its data-flow diagram shows 'RESP -- decisions / drafts --> STORE', which reads as if the responder's drafts land in storage — in code only confirmed decisions do (one storage.append at decision-store.ts:90); decision drafts and all intake drafts live in JSON files outside the atom store. Also worth noting: the gate's documented 'single sandbox chokepoint' invariant has a structural exception — derived: sources (granola-signals, team-decisions) never pass through gate()/processCandidate at all, since parseSource does not recognize the 'derived' prefix; they append directly with a self-checked isAllowedDerived call.

================================================================================
## Station 2 — Structure signals (raw atoms → structured, decision-relevant signals)  [verdict: partial]


### Granola signal extraction (types, schema, mechanism)  (exists)
  - src/enrich/granola-signals.ts:32 — GranolaSignalType union: 'decision' | 'rationale' | 'action'; decision_status 'proposed'|'decided'|'unresolved' at :33
  - src/enrich/granola-signals.ts:44 — GranolaExtractedSignal schema: signal_type, text, canonical_subject, source_span (summary vs transcript quote span), confidence, owner?, rationale_for?, decision_status?
  - src/enrich/granola-signals.ts:396-451 — prepareSignals builds atom metadata: dedupe_key `granola:signal:<note_id>:<extractor_version>:<type>:<sha256-16>` (:412), parent_dedupe_key back to raw summary/transcript atom (:413), low_confidence flag (:427), rationale→decision linking via decisionLinks map (:442-449)
  - src/enrich/granola-signals.ts:743-778 — defaultExtractorFromBrain runs buildExtractionPrompt (:758) through runBrain (codex/claude CLI subprocess) and strict-parses JSON via parseExtractorAnswer (:769)
  - src/enrich/granola-signals.ts:372-394 — validateSignal enforces type union, non-empty text/canonical_subject, confidence in [0,1], transcript spans require quote
  - src/enrich/granola-signals.ts:18-20 — signals persisted as append-only atoms under derived:granola-signals, manifests under derived:granola-signals-index; worker gated by isAllowedDerived at :581
  - src/mcp/tools/search-memories.ts:35 — signal metadata filters (signal_type, canonical_subject, note_id, dedupe_key, parent_dedupe_key, extraction_run_id) exposed for retrieval; current-manifest-run scoping documented at :25

### Intake-candidate detection (meeting signals → intake seeds)  (exists)
  - src/enrich/granola-intake-candidates.ts:31 — INTAKE_SIGNAL_TYPES restricts eligibility to 'action' and 'decision'; 'rationale' signals never become intake candidates
  - src/enrich/granola-intake-candidates.ts:303-338 — extractSignal validates signal atoms (signal_type, note_id, dedupe_key required; transcript quote preferred over content)
  - src/enrich/granola-intake-candidates.ts:388-397 — per-note filters: note must exist in raw info, must have an external attendee (hasExternalAttendee vs internalDomains, fn at :260), and a resolvable Slack owner (:340-349, ownerMap→defaultOwner); unresolved owners logged and skipped
  - src/enrich/granola-intake-candidates.ts:400-423 — LLM classifier maps signals to Linear IntakeFields (prompt at :503, parse at :516); candidates filtered to known signal refs and capped at perNoteCap (default 3, :23)
  - src/enrich/granola-intake-candidates.ts:436-467 — each candidate claimed through the durable seed store keyed by signal dedupe_key, posted to Slack with a decodable seed marker (src/brain/intake-seed.ts:87 marker codec carrying candidateKey)
  - src/enrich/granola-intake-candidates.ts:189-229 — loadGranolaIntakeConfig fail-closed: enabled-but-missing Slack token/channel throws GranolaIntakeConfigError; 7-day lookback default at :22
  - src/daemon/index.ts:96-98 — bridge wired in the daemon with runSignalsFirst coupling so seeds build from fresh signal atoms

### Signal extraction from non-meeting sources (Claude/Codex/Cursor/git eng sessions)  (partial)
  - src/enrich/dispatch.ts:17-28 — startEnrichmentDispatch boots exactly one worker: the Granola signal worker; no eng-session enrichment worker exists
  - skills/echo-emit-decision.md:7 — the only eng-session structuring path is manual: the AI client itself decides a moment is decision-grade and calls propose_decision (contract at :17)
  - src/surfaces/ceo-slack-responder/propose-decision-tool.ts:43-72 — propose_decision MCP tool: subject/decision/rationale/source_app ('claude-code'|'codex' only, :53) → draft store → Slack confirm card; nothing scans stored atoms
  - src/reasoning/causal.ts:120,150,194 — deriveToolTouchEdges / deriveStateTransitionEdges / deriveTaskClusters derive structural edges over eng-session CaptureEvents, but produce no signal_type/canonical_subject vocabulary and have ZERO production consumers — only tests/reasoning/causal.test.ts:7 imports the module
  GAP: Automated structuring of eng-session atoms into decision-grade signals is missing. The Granola worker pattern (settle → brain extract → validated signal atoms + manifest) has no counterpart for jsonl:claude-code/jsonl:codex/cursor/git sources; the manual echo-emit-decision skill covers only moments the client happens to flag in-session, misses Cursor and git entirely (source_app enum excludes them), and produces team-decision drafts, not signal atoms. Natural attach point: a second worker registered in src/enrich/dispatch.ts:17 (startEnrichmentDispatch), reusing the brain-extractor + checkpoint/manifest pattern from granola-signals.ts and optionally the unwired structural-edge derivations in src/reasoning/causal.ts as candidate-moment selectors.

### Subject normalization (the drift join key)  (partial)
  - src/enrich/granola-signals.ts:368-370 — normalizeSubject (lowercase, trim, collapse whitespace) applied at :411, written as metadata.canonical_subject at :419 on every Granola signal atom
  - src/surfaces/ceo-slack-responder/decision-store.ts:54-56 — normalizeDecisionSubject (identical algorithm, duplicated code) written as metadata.normalized_subject at :77 and baked into team-decision dedupe_key `team-decision:<normalized>` at :58-59
  - src/mcp/tools/search-memories.ts:189 — retrieval free-text query matches against metadata.canonical_subject only; normalized_subject is not in the substring-match path or the documented signal-filter key list (:35)
  - src/surfaces/ceo-slack-responder/intake-draft-store.ts:44-45 — the identity that survives into intake drafts/Linear issues is candidate_key (the signal dedupe_key) + meeting_provenance, not the subject
  GAP: There is no shared normalization module: two byte-identical functions live in src/enrich/granola-signals.ts:368 and src/surfaces/ceo-slack-responder/decision-store.ts:54 and write to two different metadata keys (canonical_subject vs normalized_subject). Both subjects are free-form LLM/agent output with no shared vocabulary or entity resolution, so a cross-source drift join would depend on two independent LLM invocations emitting the same string modulo case/whitespace. Eng-session atoms (claude-code/codex/cursor/git captures) carry no subject field at all. Natural attach point: extract the normalizer into a shared util (src/util/) consumed by both writers, and unify on one metadata key so the station-6 drift join has a single key across derived:granola-signals and derived:team-decisions.

### Durability/idempotency of extraction runs  (exists)
  - src/enrich/granola-signals.ts:621 — extraction_run_id = randomUUID() per note per run; run manifest atom (:657-673) records note_id, extractor_version, run id, completed_at, supersedes pointer, and signal_atom_ids
  - src/enrich/granola-signals.ts:482-508 — resolveCurrentGranolaSignalRuns excludes superseded runs and picks latest by completed_at then run id, so retrieval sees exactly one current run per note
  - src/enrich/granola-signals.ts:331-335 — inputFingerprint hashes note_id + updated_at + summary/transcript dedupe keys; shouldExtractNote (:344-362) re-extracts only on fingerprint or extractor_version change and skips notes whose last attempt at the same fingerprint/version failed terminally
  - src/enrich/granola-signals.ts:236-242 — checkpoint written atomically (atomicWrite) after each note; checkpoint schema strictly validated on load (:184) with error-not-silent-reset on corruption
  - src/enrich/granola-signals.ts:613-615 — settle window (default 10 min, :24) plus per-tick cap (default 5, :27) bound each run
  - src/enrich/granola-intake-seed-store.ts:86-160 — downstream seed store: single-flight claim, pending→posting→posted/failed state machine, retry_count with terminal failure at maxRetries, atomic temp-file+rename writes (:199) and an in-process promise mutex (:162) — at-least-once Slack delivery
  GAP: Two minor at-least-once edges, noted not designed-around: (1) a crash between signal-atom appends and the manifest append (granola-signals.ts:647-673) orphans signal atoms with no manifest — invisible to current-run retrieval but present in the store; (2) one note's extraction failure aborts the whole tick (return at :637) instead of continuing to remaining candidates. Also the intake bridge queries all lookback signal atoms (granola-intake-candidates.ts:364) without current-manifest filtering — superseded-run signals can re-enter classification, deduped only because dedupe_key is content-hash-stable across runs.

SUMMARY: Station 2 is solidly built for exactly one source: Granola. The meeting path is production-grade — a versioned brain-backed extractor producing typed decision/rationale/action signal atoms with canonical_subject, transcript-span provenance, content-hash dedupe keys, and a checkpoint+manifest+supersedes durability chain, feeding a well-filtered intake-candidate bridge (action/decision only, external attendee required, owner-resolvable, per-note capped, at-least-once seed store). The station's real gap is that structuring is Granola-only: eng-session atoms (Claude Code, Codex, Cursor, git) are captured but never structured into signals — the only path is the manual echo-emit-decision skill calling propose_decision, which depends on the client self-flagging in-session, excludes Cursor/git by enum, and bypasses the signal-atom vocabulary entirely; src/reasoning/causal.ts derives structural edges over those atoms but is wired to nothing. Second gap: the drift join key is fractured — canonical_subject (Granola signals) and normalized_subject (team decisions) are computed by two duplicated normalizer functions writing different metadata keys, with no shared vocabulary, so a station-6 subject join across sources currently has no single key to join on. Durability/idempotency is genuinely strong and needs no work before the demo freeze.
SURPRISES: The architecture map's src-reasoning.md claims src/reasoning/causal.ts is "Consumed by MCP tools, render-trace, and future audit UI" — this is false at the current code state: a repo-wide grep finds its only importer is tests/reasoning/causal.test.ts:7; no MCP tool, trace viewer, or any src/ or tools/ file imports it. It is dead-but-tested code, which matters because it is the most natural existing seed for eng-session signal structuring. Minor second surprise: the intake bridge (granola-intake-candidates.ts:364) does not apply the current-manifest-run filter that search_memories advertises — it classifies all signal atoms in the lookback window including superseded runs, relying on dedupe_key stability rather than manifest supersession for idempotency.

================================================================================
## 3 — Decision packet  [verdict: partial]


### a) Intake seed content (Slack seed fields + context)  (exists)
  - src/brain/intake-seed.ts:176 — renderSeedMessage posts labeled best-effort IntakeFields (all 8 incl. why), meeting title/date/Granola URL/quote, owner @mention, and the machine marker last
  - src/brain/intake-seed.ts:76 — renderSeedMarker embeds candidateKey + owner + fields + provenance as versioned (v1) base64url payload; strict parse rejects unsupported versions (intake-seed.ts:96-122)
  - src/enrich/granola-intake-candidates.ts:31 — INTAKE_SIGNAL_TYPES = {'action','decision'}: only these signal types are eligible for intake
  - src/enrich/granola-intake-candidates.ts:503 — buildClassificationPrompt maps signal text/quote to the 8 intake-field keys best-effort ('Leave a field out if not stated')
  - src/enrich/granola-intake-candidates.ts:391 — external-attendee filter + owner resolution gate which notes seed at all; per-note cap 3 (line 23, applied at 421-423)
  - src/enrich/granola-intake-seed-store.ts:19 — durable at-least-once pending→posting→posted/failed state machine per candidate key

### b) Rationale/WHY capture in the packet  (partial)
  - src/brain/brain.ts:41 — IntakeFields includes a `why` field; it is one of 8 REQUIRED fields (INTAKE_FIELDS at brain.ts:101-110; missingIntakeFields at 196-216 blocks confirm until present)
  - src/brain/brain.ts:634 — follow-up question 'Why does this matter now?' forces a human-supplied WHY when the meeting didn't state one
  - src/enrich/granola-signals.ts:32 — upstream extractor DOES capture `rationale` as a first-class signal type, with rationale_for linkage to the decision's dedupe_key (granola-signals.ts:442-449)
  - src/enrich/granola-intake-candidates.ts:31 — but the intake bridge drops rationale signals: INTAKE_SIGNAL_TYPES excludes 'rationale', so extracted meeting-WHY never reaches the classifier input (lines 402-415) or the seed
  - src/surfaces/ceo-slack-responder/propose-decision-tool.ts:52 — code-session decision rationale is optional (z.string().min(1).optional()); skills/echo-emit-decision.md caps it at 'optional one-sentence why/tradeoff'
  - src/surfaces/ceo-slack-responder/decision-store.ts:86 — confirmed team-decision atom persists rationale only when supplied; a rationale-less decision is fully valid
  GAP: The WHY slot exists in every packet shape but the highest-value rationale channel is severed: rationale signals extracted from meetings (granola-signals.ts) are filtered out at the intake bridge chokepoint (granola-intake-candidates.ts:31 / the classifier input assembly at :402-415), so the packet's `why` is either LLM-synthesized from a single action/decision signal's text or typed fresh by the founder. On the propose_decision path, rationale is optional and one-sentence by convention; nothing prompts for it when absent. The natural attach points: include rationale signals (with their rationale_for links) in GranolaIntakeClassificationInput, and/or make rationale load-bearing in the decision draft/confirm flow.

### c) Confirm interaction (what founder sees / can edit or annotate)  (partial)
  - src/surfaces/ceo-slack-responder/responder.ts:561 — driveIntakeDraftReply: shared reply driver posts up-to-2 follow-up questions per turn (brain.ts:234-236) until fields+project resolve, then posts the confirm card
  - src/surfaces/ceo-slack-responder/responder.ts:521-551 — founder thread replies are parsed by runIntakeBrain and merged into the draft's fields (mergedFields at :527), so fields remain editable-by-reply until confirm
  - src/surfaces/ceo-slack-responder/responder.ts:1309-1341 — intake confirm card shows only Project/Request/Why/Done-when (4 of 8 fields), no meeting provenance, with Confirm/Dismiss buttons carrying only the draft key
  - src/surfaces/ceo-slack-responder/responder.ts:879-887 — requester-only guard: only the seed owner can confirm/dismiss
  - src/surfaces/ceo-slack-responder/responder.ts:785-790 — decision-card Edit button is a dead-end: replies 'submit a revised /echo decision; nothing was shared'
  - src/surfaces/ceo-slack-responder/draft-store.ts:92 — editDraft exists on the decision draft store but has zero callers outside the store (verified by grep across src/)
  GAP: Two annotation gaps. (1) Decision path: the founder cannot edit or annotate a proposed decision at confirm time — Edit is an instruction message, editDraft is dead code, and confirm is a bare button click that adds no founder-supplied rationale/context to the atom. (2) Intake path: the confirm card omits 4 of 8 fields and all meeting provenance, so the founder confirms a packet they only partially see; annotation is possible only via pre-confirm thread replies parsed by the deterministic intake brain. Natural attach points: wire editDraft to the echo_decision_edit action (or a Slack modal), and render full fields + provenance on the confirm card.

### d) propose_decision MCP tool (schema + gate)  (exists)
  - src/surfaces/ceo-slack-responder/propose-decision-tool.ts:49-54 — input schema: subject, decision, rationale?, source_app ∈ {claude-code, codex}
  - src/surfaces/ceo-slack-responder/propose-decision-tool.ts:95-102 — creates a pending draft then posts the Slack confirm card; nothing durable until human confirm
  - src/surfaces/ceo-slack-responder/propose-decision-tool.ts:130-138 — author resolved server-side (ECHO_TEAM_DECISION_AUTHOR → ECHO_MACHINE_ID → ECHO_AGENT_ID → USER → 'local-machine')
  - src/mcp/server.ts:93-94,272 — registerProposeDecision dynamically imported and registered on the MCP daemon (optional, tolerates missing module)
  - src/surfaces/ceo-slack-responder/decision-store.ts:74-95 — confirmed atom appended to derived:team-decisions with subject/normalized_subject/decision/rationale?/author/confirmed_by/confirmed_at/source_app/dedupe_key/draft_id; idempotent by draft_id (:69-70)
  - src/surfaces/ceo-slack-responder/draft-store.ts:143 — confirmDraft is idempotent under per-draft lock; re-entry returns the existing confirmed draft

### e) Provenance linkage back to source atoms  (partial)
  - src/enrich/granola-signals.ts:412,425 — each signal atom carries a stable dedupe_key plus parent_dedupe_key back to the raw summary/transcript atom and note_id/extraction_run_id
  - src/enrich/granola-intake-candidates.ts:399,421-426,436-440 — candidate.ref must be a real signal dedupe_key and becomes the seed candidateKey, so seed↔signal-atom linkage is machine-verifiable
  - src/brain/intake-seed.ts:9-15 — MeetingProvenance {noteId, meetingTitle, meetingDate?, webUrl?, quote} rides the marker payload
  - src/surfaces/ceo-slack-responder/intake-draft-store.ts:44-45 — IntakeDraft durably persists candidate_key + meeting_provenance; dismissals are logged with candidate_key as noise signal (responder.ts:894-902)
  - src/surfaces/ceo-slack-responder/issue-render.ts:112-126 — the created Linear issue renders only human-level provenance (Meeting title/Granola URL/quote); note_id and candidate_key are NOT written into the issue body
  - src/surfaces/ceo-slack-responder/decision-store.ts:74-88 — team-decision atoms carry draft_id + source_app but zero links to source atoms, sessions, repos, or commits
  - src/surfaces/ceo-slack-responder/decision-store.ts:90 — the only storage.append in the whole surface; the intake packet, confirm event, and created issue {id,url} live only in ~/.echo/state JSON files (intake-draft-store.ts:49), never in the atom store
  GAP: Provenance is strong up to the draft and then thins out at both terminals. (1) The Linear issue body drops the machine keys (note_id, candidate_key/signal dedupe_key) that would let station-5/6 backflow join an issue back to its source atoms — issue-render.ts:42-110 is the attach point. (2) The decision packet from code sessions has no source-atom provenance at all: propose_decision's input schema (propose-decision-tool.ts:49-54) has no field for session/atom refs, and the confirmed atom records only source_app+author. (3) The confirmed intake outcome (created issue id/url + confirmed_by) never lands in the append-only atom store, so ECHO's own substrate cannot see the packet it assembled — the natural chokepoint is a storage.append alongside runCreateOnce success in responder.ts:912-958.

SUMMARY: Station 3 is structurally shipped: a meeting signal becomes a durable, versioned, confirmable Slack seed carrying 8 typed intake fields, meeting provenance, and a machine key that traces back to the exact signal atom, and code-session decisions have a parallel propose→confirm gate over MCP. The three real gaps are all about depth, not existence. First, rationale: the extractor upstream already captures rationale signals with rationale_for links, but the intake bridge filters them out (INTAKE_SIGNAL_TYPES excludes 'rationale'), so the packet's required `why` field is synthesized from a single action/decision signal or typed by the founder, and on the decision path rationale is optional and unprompted. Second, the confirm interaction is thinner than the packet: the intake card shows only 4 of 8 fields and no provenance, and the decision card's Edit button is a dead end (editDraft exists but has no callers), so confirm is take-it-or-leave-it rather than annotate-and-confirm. Third, provenance linkage frays at the terminals: the created Linear issue omits note_id/candidate_key (human-readable Granola link only), the confirmed decision atom has no source-atom refs, and the intake packet plus its created-issue outcome live only in ~/.echo/state JSON files, never entering the atom store — which matters directly for stations 5-6 since drift detection will want to join issues back to their originating atoms.
SURPRISES: The strategy record's claim "ECHO captures what/how, never why" is now only half true in code: granola-signals.ts extracts `rationale` as a first-class signal type with rationale_for linkage to decisions (granola-signals.ts:32, 442-449), and `why` is a hard-required intake field the bot will interrogate the founder for (brain.ts:101-110, 634). The remaining why-gap is narrower and more specific than the record states: extracted meeting rationale is dropped at exactly one line (granola-intake-candidates.ts:31) before packet assembly, and code-session rationale is optional/one-sentence by skill convention. Also minor: the architecture map is accurate throughout; every line number I re-verified matched commit state.

================================================================================
## Station 6 — Drift Alert (understanding-drift, cofounder vs cofounder, Slack alert to decision owner)  [verdict: missing]


### Join key: normalized_subject comparability  (partial)
  - src/surfaces/ceo-slack-responder/decision-store.ts:54 — normalizeDecisionSubject = trim + lowercase + collapse whitespace (pure lexical)
  - src/surfaces/ceo-slack-responder/decision-store.ts:77 — normalized_subject persisted in team-decision atom metadata; :191 reconstructs it on read
  - src/enrich/granola-signals.ts:368 — normalizeSubject in the signal worker is byte-identical logic (lowercase/trim/collapse)
  - src/enrich/granola-signals.ts:411 — canonical_subject normalized at prepare time and stored at :419 as signal atom metadata
  - src/mcp/tools/search-memories.ts:35 — canonical_subject is a first-class metadata_match filter key (and :189 substring-matches it in free-text query), so subject-keyed retrieval of candidate statements already works over MCP
  - src/surfaces/ceo-slack-responder/decision-store.ts:214-224 — matchesQuery gives exact-normalized-subject and token-overlap lookup over decisions
  GAP: The two normalizers are compatible but the join is LEXICAL only — a Granola signal's canonical_subject and a decision's normalized_subject match only if the extraction LLM happens to emit the same phrase; there is no semantic/alias subject resolution, and nothing enforces that the granola-signals extractor and the propose_decision author use a shared subject vocabulary. Natural attach point: a comparison step (exact-key first, then LLM fallback for near-misses) in a new enrich worker, reusing queryLatestTeamDecisions (decision-store.ts:122) as the decision side of the join.

### Candidate-statement supply (new statements with subjects)  (partial)
  - src/enrich/granola-signals.ts:44-53 — GranolaExtractedSignal carries canonical_subject, signal_type ('decision'|'rationale'|'action'), confidence, transcript quote span; decision_status ('proposed'|'decided'|'unresolved') persisted at :429 — Granola statements are drift-scan-ready
  - src/enrich/granola-signals.ts:19 — persisted as derived:granola-signals atoms in the shared store, queryable by source
  - src/capture/sources.ts:9-21 — allowlist: fs_paths cover Claude Code/Codex/Cursor session transcripts; apis=['granola']; derived=['granola-signals','granola-signals-index','team-decisions']; NO slack source exists anywhere
  - src/capture/sources.ts:12-17 — eng sessions ARE captured raw (~/.claude/projects/, ~/.codex/sessions/, Cursor storage) but no worker extracts subject-tagged statements from them
  - src/surfaces/ceo-slack-responder/propose-decision-tool.ts:43 — the only eng-session→subjected-statement path is the agent-driven propose_decision tool (a proposal, not a passive candidate statement)
  GAP: Only Granola yields subject-bearing candidate statements today. Slack is the biggest hole: the responder consumes Socket Mode envelopes but never appends any Slack message to storage (zero storage.append calls in src/surfaces/ceo-slack-responder/responder.ts; no slack entry in the sources.ts allowlist) — the 'Slack statement contradicting a decision' leg of the v0 definition has no capture rail at all. Eng-session statements would need a signal-extraction worker analogous to granola-signals.ts over the already-captured JSONL transcripts. Attach points: allowlist entry + append in the responder envelope handler (handleSocketMessage, responder.ts:1404) for Slack; a sibling worker in src/enrich/ for eng sessions.

### Contradiction detection machinery  (missing)
  - src/brain/brain.ts:301 — runBrain: generic scoped-subprocess LLM runner (codex/claude registry at :115) with timeout, JSON-final-message parsing — the natural judgment engine
  - src/enrich/granola-signals.ts:743 — defaultExtractorFromBrain + buildExtractionPrompt (:758) + strict-JSON parseExtractorAnswer (:769): worked example #1 of a brain-backed enrichment judgment
  - src/enrich/granola-intake-candidates.ts:565 — defaultClassifierFromBrain + parseClassifierAnswer (:516): worked example #2, including per-candidate validation (:529)
  - src/enrich/dispatch.ts:17 — startEnrichmentDispatch is the declared fan-out point for new enrichment workers (currently boots only the granola-signals worker)
  GAP: No compare/contradiction code exists anywhere (grep for contradict/drift in src/ hits only config-drift in cli/commands/doctor.ts:189 and comments). The capability would attach as a third brain-backed worker: query latest decision per normalized_subject (decision-store.ts:122) x new candidate statements sharing that subject, send both texts to runBrain with a strict-JSON contradiction verdict prompt, following the granola-signals checkpoint/idempotency pattern (granola-signals.ts:82-103) to avoid re-judging.

### Alert delivery + decision-owner identity  (partial)
  - src/surfaces/ceo-slack-responder/responder.ts:1210 — postSlackMessage: plain chat.postMessage push, threaded, reusable
  - src/enrich/granola-intake-candidates.ts:586 — postGranolaIntakeSeed: standalone daemon-side Slack post (no Socket Mode dependency) — proof an enrich worker can push to Slack directly
  - src/surfaces/ceo-slack-responder/responder.ts:1234 — postDecisionDraftCard and :1292 postIntakeConfirmCard: Block Kit cards with action buttons an alert card could clone
  - src/surfaces/ceo-slack-responder/decision-store.ts:26-28 — TeamDecisionAtom carries author, confirmed_by, confirmed_at
  - src/surfaces/ceo-slack-responder/propose-decision-tool.ts:130 — resolveDecisionAuthor falls through ECHO_TEAM_DECISION_AUTHOR → ECHO_MACHINE_ID → ECHO_AGENT_ID → USER → 'local-machine' (a machine label, not a person)
  - src/surfaces/ceo-slack-responder/identity.ts:23 — confirmed_by is a cofounder id via confirmAttributionForSlackUser; CofounderIdentity (:1-5) holds the id↔slack_user_id pair needed to route an alert
  - src/enrich/granola-intake-candidates.ts:340 — resolveOwner: existing email→Slack-user-id owner-map + defaultOwner precedent for alert routing
  GAP: TeamDecisionAtom has no owner field suitable for alerting: author is a machine-id fallback and confirmed_by is a cofounder id with only a forward (slack→cofounder) resolver — no cofounder-id→slack_user_id reverse helper exists in identity.ts, though the data is present in ECHO_TEAM_DECISION... sorry, ECHO_TEAM_COFUNDER_IDENTITIES. An alert path would attach either as a reverse lookup over parseCofounderIdentities output plus postSlackMessage from the daemon-side worker (postGranolaIntakeSeed pattern), treating confirmed_by as the owner-of-record.

### Scheduling rails for a periodic drift scan  (exists)
  - src/enrich/dispatch.ts:17 — startEnrichmentDispatch: single chokepoint where a drift worker handle would be added alongside granolaSignals
  - src/enrich/granola-signals.ts:814 — startGranolaSignalWorker: interval worker template (default 300s tick at :23, per-tick note cap, checkpoint idempotency)
  - src/enrich/granola-intake-candidates.ts:665-705 — bridge loop template: setInterval(trigger) at :703, debounce (:692), single-flight in-flight guard (:674), runSignalsFirst upstream chaining (:676) — exactly the shape a drift scan needs to run after fresh signal extraction
  - src/daemon/index.ts:85 — enrichment dispatch wired into the daemon boot Promise.all; :96 — intake bridge wired with runSignalsFirst coupling; :115 — orderly stop in the shutdown hook

### False-positive containment (confirm-gate pattern to reuse)  (exists)
  - src/surfaces/ceo-slack-responder/draft-store.ts:143-181 — confirmDraft: locked, idempotent pending→confirmed transition; the propose-confirm gate a drift alert should mirror (alert = cheap card, action = human verdict)
  - src/surfaces/ceo-slack-responder/responder.ts:1234 — postDecisionDraftCard with Confirm/Edit/Dismiss buttons; :745 respondToDecisionAction handles the clicks
  - src/surfaces/ceo-slack-responder/responder.ts:879-884 — requester-mismatch guard: only the addressed person can confirm/dismiss (owner-gating precedent for a drift card)
  - src/surfaces/ceo-slack-responder/responder.ts:1123 — appendIntakeSeedDismissalRecord (wired at :896): dismissals logged to the event log explicitly as a noise-tuning signal — the exact cheap-wrong-alert feedback loop the drift alert needs
  - src/enrich/granola-intake-seed-store.ts:129 — markFailure with retry cap → terminal 'failed': at-least-once posting with bounded retries so a broken alert can't spam

SUMMARY: The drift-alert station itself does not exist in any form — no contradiction detection, no drift scan, no alert — but the rails are stronger than the "hero build" framing suggests. The join key is real and already bilateral: team decisions persist normalized_subject (decision-store.ts:54,77) and Granola signals persist canonical_subject through a byte-identical normalizer (granola-signals.ts:368,419), with decision_status and transcript quotes as bonus drift semantics; the risk is that the join is purely lexical and depends on two independent LLM prompts converging on the same subject phrase. Candidate-statement supply is Granola-only: Slack messages are never captured (no allowlist source, no append in the responder), so the v0 definition's Slack leg has no rail, and eng sessions are captured raw but never subject-tagged. Detection would be a third brain-backed enrich worker following the two existing extractor/classifier templates, scheduled via startEnrichmentDispatch exactly like the intake bridge (interval + debounce + runSignalsFirst chaining). Delivery can reuse the daemon-side Slack post pattern and Block Kit cards, but the decision atom has no alertable owner: author is a machine id and confirmed_by (a cofounder id) needs a trivial-but-missing reverse mapping to a Slack user. Containment is fully solved by precedent — the propose-confirm card, requester-only action guard, and the dismissal-as-noise-signal log give a cheap wrong-alert path to copy verbatim.
SURPRISES: Three things worth flagging against the strategy record. (1) The v0 drift definition names Slack statements as a contradiction source, but Slack is entirely absent from the capture layer — the responder consumes envelopes without persisting any message atom (zero storage.append in responder.ts; sources.ts:9-21 has no slack source), so cofounder-vs-cofounder drift via Slack requires new capture work, not just a scan; the only same-day supply is Granola. (2) Granola decision signals already carry decision_status ('proposed'|'decided'|'unresolved', granola-signals.ts:33, persisted at :429), which the architecture map underplays — it lets a drift scan distinguish "someone proposed differently" from "someone decided differently" for free. (3) The responder opens its own SqliteStorage handle at the decision-store path (responder.ts:322-323) in a separate process from the daemon; a daemon-side drift worker reading team decisions will share that db cross-process — fine under WAL, but the demo topology assumption "one process owns the store" is already false.

================================================================================
## Station 4 — Record (confirmed decision becomes durable, addressable truth)  [verdict: partial]


### Team-decision store (schema, append-only, normalized_subject)  (exists)
  - src/surfaces/ceo-slack-responder/decision-store.ts:20 — TeamDecisionAtom fields: atom_id, subject, normalized_subject, decision, rationale?, author, confirmed_by, confirmed_at, source_app ('claude-code'|'codex'), dedupe_key, draft_id
  - src/surfaces/ceo-slack-responder/decision-store.ts:90 — appendConfirmedDecision writes via storage.append() only (no update/delete path exists in the module); source is 'derived:team-decisions' (line 5)
  - src/surfaces/ceo-slack-responder/decision-store.ts:54 — normalizeDecisionSubject trims/lowercases/collapses whitespace; decisionDedupeKey (line 58) builds 'team-decision:<normalized subject>'
  - src/surfaces/ceo-slack-responder/decision-store.ts:146 — assertTeamDecisionSourceAllowed throws (fail-closed) if 'team-decisions' is off the derived allowlist; allowlisted at src/capture/sources.ts:19
  - src/surfaces/ceo-slack-responder/decision-store.ts:122 — queryLatestTeamDecisions keeps latest atom per dedupe_key, sorted by confirmed_at (compareDecisionRecency, line 258)
  - tests/surfaces/ceo-slack-responder/decision-store-latest-wins.test.ts:1 — latest-wins semantics under test

### Propose→confirm gate (exactly-once confirm, fail-closed)  (exists)
  - src/surfaces/ceo-slack-responder/propose-decision-tool.ts:43 — registerProposeDecision registers the propose_decision MCP tool; proposeDecision (line 74) fail-closes on missing Slack bot token / invalid confirm target / unresolvable author; registered optionally at src/mcp/server.ts:205,272
  - src/surfaces/ceo-slack-responder/draft-store.ts:143 — confirmDraft: per-draft mutex, already-confirmed short-circuit (line 152), ensurePending, injected append, then re-read + mark confirmed with decision_atom_id (line 174)
  - src/surfaces/ceo-slack-responder/decision-store.ts:69 — appendConfirmedDecision dedupes by draft_id before appending (returns existing atom), making crash-between-append-and-draft-write retries exactly-once
  - src/surfaces/ceo-slack-responder/draft-store.ts:224 — writeFile uses tmp-file + atomic rename
  - src/surfaces/ceo-slack-responder/responder.ts:763 — Slack confirm button path wires confirmDraft→appendConfirmedDecision
  - tests/surfaces/ceo-slack-responder/confirm-idempotency.test.ts:1 and propose-confirm.test.ts:1 — idempotency under test

### Linear issue creation (payload, provenance, client capabilities)  (exists)
  - src/surfaces/ceo-slack-responder/linear-client.ts:55 — createIssue is the ONLY client method; the sole GraphQL operation is the issueCreate mutation (lines 68-77) sending teamId, projectId, stateId, assigneeId, title, description (lines 80-87); returns {id, url}. Create-only: no read, no update, no query capability
  - src/surfaces/ceo-slack-responder/issue-render.ts:42 — body carries Request/Why/Scope/Done-when/Delivery sections plus Receipts with requester (line 98), Slack thread permalink (line 99), Granola meeting provenance (line 100 via renderMeetingProvenanceLines, lines 112-126: meeting title/date, Granola webUrl, quote), and Linear project ID (line 107)
  - src/surfaces/ceo-slack-responder/intake-draft-store.ts:233 — runCreateOnce state machine: pending→creating→created; a replayed 'creating' status is marked needs-reconcile with NO retry (lines 258-268), create failure → needs-reconcile with evidence (catch block ~line 318); created issue persisted on the draft (created_issue, line 49)
  - src/surfaces/ceo-slack-responder/responder.ts:912 — confirm button drives runCreateOnce → linearClient.createIssue; requester-mismatch rejected at line 879
  - tests/surfaces/ceo-slack-responder/intake-confirm-idempotency.test.ts:1, linear-client.test.ts:1, issue-provenance.test.ts:1

### Decision addressability via MCP (retrieve recorded decision by subject/id; decision↔Linear linkage)  (partial)
  - src/mcp/tools/get-atom.ts:66 — get_atom is generic by id and source-agnostic, so a team-decision atom IS retrievable by atom_id
  - src/mcp/tools/search-memories.ts:319 — filter.source accepts any exact string (e.g. 'derived:team-decisions'); metadata_match tool whitelist (lines 31-41) includes 'dedupe_key', so subject-keyed lookup works via dedupe_key='team-decision:<normalized subject>' filtered in-tool at line 371
  - src/mcp/tools/search-memories.ts:25 — free-text query matches only content + metadata.canonical_subject; team-decision atoms write 'subject'/'normalized_subject' (decision-store.ts:76-77), NOT canonical_subject, so subject text is invisible to free-text search; 'normalized_subject', 'subject', 'draft_id', 'decision_atom_type' are not whitelisted metadata_match keys
  - src/brain/brain.ts:403 — latest-wins decision retrieval (queryLatestDecisions) is only reachable in-process by the Slack brain; no MCP tool exposes it
  - src/mcp/tools/pending-decisions.ts:224 — pending_decisions is a DecisionCard projection of backlog/review-playbook git files (projectPlaybookDecisions, line 62), NOT a reader of team-decision atoms or the draft store
  - src/surfaces/ceo-slack-responder/draft-store.ts:23 — decision_atom_id linkage lives only in the JSON draft file (~/.echo/state/team-decision-drafts.json); src/surfaces/ceo-slack-responder/intake-draft-store.ts:49 — created Linear issue {id,url} lives only in the JSON intake-draft file
  GAP: Three gaps. (1) No MCP surface exposes latest-wins decision query semantics — a demo client must know the internal dedupe_key format and use search_memories(source='derived:team-decisions', metadata_match={dedupe_key}) and then de-duplicate manually; natural attach point is a new MCP tool wrapping queryLatestTeamDecisions (src/mcp/server.ts tool registration next to propose_decision). (2) Free-text/subject search over decisions is dead: decisions don't write canonical_subject and normalized_subject isn't a filterable key — attach point is either the metadata written in appendConfirmedDecision (decision-store.ts:74) or the TOOL_METADATA_MATCH_KEYS set (search-memories.ts:31). (3) decision_id↔Linear-issue linkage: nothing connects a team-decision atom to a Linear issue anywhere — the decision-confirm flow and the intake→Linear flow are disjoint pipelines; the created issue id/url is never appended to the atom store at all (only the local JSON draft files), so Linear linkage is not addressable, not machine-scoped-durable, and invisible to search/trace. Attach point: the runCreateOnce success path (responder.ts:939 / intake-draft-store.ts:233) writing a derived atom, and/or a linear_issue_id field in decision metadata.

### Decision updates / supersede-amend chain  (partial)
  - src/surfaces/ceo-slack-responder/decision-store.ts:126 — queryLatestTeamDecisions implements implicit query-time supersede: latest atom per dedupe_key wins (compareDecisionRecency by confirmed_at, line 258)
  - src/surfaces/ceo-slack-responder/decision-store.ts:20 — TeamDecisionAtom has NO supersedes/amends/parent-atom pointer field; metadata written at lines 74-88 carries dedupe_key but no chain link
  - src/surfaces/ceo-slack-responder/draft-store.ts:92 — editDraft only mutates a still-pending draft (ensurePending, line 101); a confirmed decision cannot be amended through the draft lifecycle
  - tests/surfaces/ceo-slack-responder/decision-store-latest-wins.test.ts:1 — the implicit mechanism is tested
  GAP: A changed decision is just a second append. If the new subject normalizes byte-identically to the old one, latest-wins hides the predecessor at query time — but there is no explicit supersede/amend chain (no superseded_by/supersedes pointer, no revision counter), so history vs. current is distinguishable only by timestamp, and any wording drift in the subject ('use sqlite' vs 'use sqlite for storage') produces two coexisting 'live' decisions with no conflict signal. This directly limits Station 6 drift detection over decisions. Natural attach point: the metadata block in appendConfirmedDecision (decision-store.ts:74) — the append-only + dedupe_key design already anticipates a chain/hash supersede (per the 2026-06-21 append-only decision record).

SUMMARY: The Record station's write side is solid: a confirmed decision durably lands as an append-only `derived:team-decisions` atom with a well-typed schema (normalized_subject, dedupe_key, draft_id, full attribution), the propose→confirm gate is fail-closed and exactly-once (draft_id dedupe makes crash-retries safe; both idempotency paths are tested), and Linear issue creation is exactly-once via a pending→creating→created/needs-reconcile state machine with rich provenance (Slack thread URL + Granola meeting citation) in the issue body. The read side is the weak half: no MCP tool exposes latest-wins decision retrieval (only the in-process Slack brain gets it), subject text is invisible to free-text search because decisions write `subject` rather than the searched `canonical_subject` key, and the only MCP path to a decision by subject requires knowing the internal `team-decision:<subject>` dedupe_key format. Decision↔Linear linkage is effectively absent — decision_atom_id and created_issue live only in two disjoint local JSON draft files, the two flows never touch, and no atom is ever appended for a created Linear issue, so the recorded truth cannot be traversed from decision to issue (or back, which Station 5 backflow will need). Supersede is implicit-only (latest-per-dedupe_key at query time) with no amend chain and full sensitivity to subject wording drift, which limits what Station 6 can say about changed decisions. Caveats worth knowing at demo scale: draft-store locks are in-process only (single-responder-process assumption), and every decision lookup full-scans all team-decision atoms.
SURPRISES: Two findings contradict likely assumptions. (1) The `pending_decisions` MCP tool has nothing to do with team decisions: it is a read-only DecisionCard projection of backlog/review-playbook git files (src/mcp/tools/pending-decisions.ts:224, projectPlaybookDecisions) — the team-decision store has no dedicated MCP retrieval tool at all. (2) The intake draft's `idempotency_token` (intake-draft-store.ts:47, derived at line 436) is never sent to Linear — the issueCreate mutation (linear-client.ts:68-87) carries no idempotency key, so exactly-once rests entirely on the local state machine's no-retry/needs-reconcile discipline, and the token is currently vestigial.

================================================================================
## Station 5 — Translate / status backflow  [verdict: partial]


### Slack responder answer path (question types + brain routing)  (partial)
  - src/surfaces/ceo-slack-responder/responder.ts:401 — respondToQuestion: ack 'Looking...' (line 411), then routes intake first (line 413), then intake-only help (line 417), then Q&A
  - src/surfaces/ceo-slack-responder/responder.ts:431-436 — if deps.teamDecisionStore is wired, ALL questions are answered by answerFromTeamDecisions (confirmed-decisions-only); the codex/claude brain is completely bypassed; the two answer modes are mutually exclusive per deployment env (ECHO_TEAM_DECISION_STORE, responder.ts:321-325)
  - src/surfaces/ceo-slack-responder/responder.ts:388-399 — answerQuestion passes the raw Slack text to runBrain with contextRepoPath + timeout; no question-type classification (no status-vs-why distinction anywhere)
  - src/brain/brain.ts:250 — isLikelyLinearIntake returns true if the text mentions ANY known Linear project name, so a status question like 'where is <ProjectName>?' is hijacked into the intake follow-up flow (responder.ts:413, 493-497) and never reaches the brain — a direct scene-3 hazard
  GAP: No status-question type exists. 'Where is X?' either (a) gets swallowed by the intake router when X is a known project name (brain.ts:250 + responder.ts:493-497), (b) gets a decision-store-only answer that knows nothing about eng/Linear state, or (c) goes to the free-form brain with no status-specific retrieval. A status-question route would attach in respondToQuestion (responder.ts:401) before/alongside the intake check, and isLikelyLinearIntake needs a question-vs-request discriminator.

### Brain: grounding, provenance, and 'couldn't find' faithfulness guard  (partial)
  - src/brain/brain.ts:115-137 — BRAIN_REGISTRY: codex runs `codex exec -C <repo> --sandbox read-only --json -`, claude runs `claude --dangerously-skip-permissions -p`; prompt piped on stdin (runBrain, brain.ts:301)
  - src/brain/brain.ts:146-163 — buildBrainPrompt: grounding ('Answer the Slack question from scoped ECHO context only', repo_path pinning line 154), citation ('Cite at least one concrete scoped fact', line 157), and couldn't-find ('If scoped context is insufficient, say that directly and name the missing grounding', line 158) are ALL prompt-level instructions with zero code enforcement
  - src/brain/brain.ts:345-352 — the only code-level guard is rejecting an empty answer; no verification that the answer actually cites scoped facts or that claimed facts exist in the store
  - src/brain/brain.ts:388-410,649-662 — answerFromTeamDecisions is the one deterministic path with a real coded couldn't-find ('I don\'t have a confirmed shared decision for that yet.', line 651) and a raw-context refusal (lines 393-401, 412-424), but it only sees confirmed team decisions, never eng/Linear state
  - tests/surfaces/ceo-slack-brain-regressions.test.ts:9-147 — all 'brain regression' tests cover intake field parsing and title bounds; zero tests exercise Q&A grounding or faithfulness (item 113 is a proposed backlog item, no spec file in backlog/proposed|ready|claimed)
  GAP: Faithfulness is a prompt hope, not a mechanism, on the brain path (the demo path). No answer post-check, no structured retrieval trace, no coded refusal when the subprocess hallucinates. Item 113 (brain faithfulness A/B) exists only as a review-round proposal. A guard would attach in runBrain's result handling (brain.ts:331-358) or as a verification pass in respondToQuestion (responder.ts:457) before posting.

### Linear READ (issue status/assignee/updates query)  (missing)
  - src/surfaces/ceo-slack-responder/linear-client.ts:68-69 — the file's ONLY GraphQL operation is `mutation EchoCreateIssue`; the LinearClient interface (line 24) has a single method, createIssue (line 55); grep across src/ confirms no other Linear GraphQL caller
  - src/surfaces/ceo-slack-responder/linear-client.ts:126-141 — loadLinearConfig reads LINEAR_API_KEY/LINEAR_TEAM_ID/state/assignee/project env vars, so credentials for a read already exist — only the query surface is absent
  GAP: HEADLINE GAP for scene 3. ECHO can create Linear issues but cannot read one back — no issue-state, assignee, comment, or status query exists anywhere in src/. A 'where is X?' answer grounded in Linear state is impossible today. Attaches naturally as query methods on LinearGraphqlClient / the LinearClient interface (linear-client.ts:24,46), consumed by the answer path. Backlog item 112 (v0.1 linear status backflow) was proposed 2026-07-01 but has no spec file in proposed/ready/claimed — nothing is built.

### Eng-capture grounding for status answers (issue/decision → eng atoms linkage)  (partial)
  - src/brain/brain.ts:154 — the prompt instructs the brain to use ECHO MCP tools with the exact repo_path, so eng atoms (git commits via git-watcher, claude-code/codex sessions) ARE reachable in principle through the subprocess's own MCP wiring
  - src/surfaces/ceo-slack-responder/responder.ts:397 — ECHO_MCP_URL is injected into the brain subprocess env, but grep shows no consumer of that variable outside responder.ts itself (src/echo-home adapters never reference it or port 38478), so the brain's ECHO access depends entirely on the CLI's ambient MCP config in the scoped repo — not code-enforced
  - src/surfaces/ceo-slack-responder/intake-draft-store.ts:49 — the created Linear issue (id/url) is persisted only in the file-backed JSON draft store (written at intake-draft-store.ts:311), never into the atom store
  - src/capture/sources.ts:19 — the derived-source allowlist is ['granola-signals','granola-signals-index','team-decisions']; there is no linear source, no Linear capture surface, and no Linear normalize adapter anywhere in src/capture or src/normalize
  - src/surfaces/ceo-slack-responder/decision-store.ts:20-33 — TeamDecisionAtom carries subject/decision/rationale/attribution only; no field links a decision to eng atoms, commits, or a Linear issue
  GAP: There is no join between a Linear issue (or decision subject) and the eng atoms that evidence its progress. The issue id/url lives outside the atom store, so even a future Linear read couldn't be correlated with commits/sessions without new linkage. Attaches at the intake create path (persist the created issue as a derived atom via the capture pipeline chokepoint, src/capture/pipeline.ts) and/or at retrieval (subject/artifact matching in the trace engine). The brain's eng-atom access is best-effort LLM tool use, not a deterministic evidence pull.

### Per-audience translation (audience/altitude in answers)  (missing)
  - src/echo-home/adapters/skill-sync.ts:42 — the only 'audience' concept in src/ is the skill install-profile (customer/dogfood), unrelated to answers
  GAP: Answers have exactly one flat style: buildBrainPrompt produces one 'Slack-ready answer' with a fixed 'business why' register (brain.ts:156-159), and formatTeamDecisionAnswer has one fixed sentence template (brain.ts:649-662). No notion of who is asking (the cofounder identity resolution in identity.ts is used only for intake/confirm attribution, not answer shaping). Would attach in buildBrainPrompt (brain.ts:146) parameterized by asker identity from respondToQuestion, which already has question.user.

### Provenance links in Slack replies (atom ids, Linear URLs, commit SHAs)  (partial)
  - src/brain/brain.ts:157 — brain path: 'Cite at least one concrete scoped fact' is the entire provenance mechanism — free-text, unverified, no atom ids/URLs/SHAs required or extracted
  - src/brain/brain.ts:659 — decision-only answers include human attribution ('Confirmed by <who> at <when>') but no atom_id, draft link, or Slack permalink
  - src/surfaces/ceo-slack-responder/responder.ts:939-957 — the intake receipt does post the created Linear issue URL, and the issue body embeds the Slack thread URL + Granola meeting provenance (issue-render.ts:42, renderMeetingProvenanceLines issue-render.ts:112-126) — but that is forward-direction (intake, stations 3/4), not backflow answers
  GAP: The backflow answer path has no structured provenance: no atom ids, no Linear URLs, no commit SHAs in Q&A replies, and nothing verifies the brain's free-text 'citation' corresponds to real atoms. Structured provenance would attach as a typed answer envelope from runBrain (brain.ts:353-358) rendered by respondToQuestion (responder.ts:457-464), fed by whatever retrieval trace the brain used.

SUMMARY: Station 5 is scaffolded but the demo-critical spine is absent. The Slack responder and brain-subprocess machinery exist and are production-hardened (socket lifecycle, ack, usage logging, injectable deps), but the headline gap is that Linear is create-only: linear-client.ts contains exactly one GraphQL operation (the EchoCreateIssue mutation), so an answer grounded in Linear issue state is structurally impossible today even though LINEAR_API_KEY/LINEAR_TEAM_ID credentials are already loaded. Second, there is no status-question type at all — and worse, isLikelyLinearIntake matches on any known project name, so the exact scene-3 utterance "where is <Project>?" gets hijacked into the intake follow-up flow before the brain ever sees it. Third, faithfulness is prompt-level only on the brain path: the "couldn't find" behavior and citation requirements live in buildBrainPrompt with zero code enforcement or tests (the only coded couldn't-find is in the decision-store-only mode, which is mutually exclusive with the brain per env config). Finally, there is no linkage substrate for evidence: the created Linear issue persists only in a JSON draft file outside the atom store, team-decision atoms carry no refs to eng atoms, and per-audience translation does not exist anywhere. Items 112 (Linear status backflow) and 113 (faithfulness A/B) correctly target the two biggest holes but have no spec files in the pipeline folders yet — nothing is built.
SURPRISES: Three things the architecture map under-states: (1) the two answer modes are mutually exclusive by env — when ECHO_TEAM_DECISION_STORE is set, createResponderRuntimeDependencies wires teamDecisionStore and respondToQuestion (responder.ts:431-436) then answers EVERY question from confirmed decisions only, never running the codex/claude brain; the map describes both paths without noting one disables the other. (2) ECHO_MCP_URL is injected into the brain subprocess env (responder.ts:397) but nothing consumes it — no echo-home adapter or vendor config references the variable, so the brain's ECHO access rides entirely on the CLI's ambient MCP configuration. (3) isLikelyLinearIntake's known-project-name match (brain.ts:250) means any status question naming a known project is routed into intake — a live routing bug for the planned demo scene, not just a missing feature.

================================================================================
## Cross-cutting rails — Slack surface + daemon + config/deploy  [verdict: partial]


### Slack responder Socket Mode lifecycle (reconnect, error handling)  (partial)
  - src/surfaces/ceo-slack-responder/responder.ts:1386 — onDisconnect on close/error sets exitCode=1 and triggers exitAfterSocketDisconnect; there is NO in-process reconnect or backoff
  - src/surfaces/ceo-slack-responder/responder.ts:1482 — exitAfterSocketDisconnect drains in-flight work (30s cap, responder.ts:68) then process.exit(1); restart is delegated to the supervisor
  - fly.toml:25 — [[restart]] policy = "always" is the production keep-alive; fly.toml:7 runs `node dist/surfaces/ceo-slack-responder/index.js` as the app process
  - Dockerfile:36 — CMD runs the responder entrypoint; repo /app baked in as ECHO_CEO_CONTEXT_REPO_PATH
  - scripts/launchd/install.sh:16 — launchd install covers ONLY the daemon (`daemon install`); no plist, npm script, or supervisor exists for a locally-run responder
  - tests/surfaces/ceo-slack-responder/socket-lifecycle.test.ts:120 — tests cover exit-after-close and drain-before-exit, matching the crash-restart design
  GAP: Reconnect = crash-and-restart, which is fine on Fly but the local full-mode responder (the only place decision-confirm is configured — fly.toml sets no ECHO_TEAM_DECISION_* vars, so the Fly worker answers decision buttons with 'Decision sharing is not configured', responder.ts:752-760) has no supervisor at all: if the founder's local responder isn't manually running, decision confirms die. Additionally, if a local responder and the Fly worker ever connect with the same Slack app token, Socket Mode distributes each envelope to one connection — a decision-confirm click can land on the intake-only Fly worker. This topology hazard is not documented or guarded anywhere. Natural attach point: a `runSlackResponder` reconnect loop or an `echoctl`-managed launchd unit for the local responder, plus a startup log of which flows are configured.

### Channel/config model — intake channel vs question separation  (exists)
  - src/surfaces/ceo-slack-responder/responder.ts:273 — single allowlist ECHO_CEO_SLACK_CHANNEL_IDS gates questions/intake threads; responder.ts:275-279 hard-fails startup if intake-only mode has an empty list
  - src/surfaces/ceo-slack-responder/responder.ts:310-315 — seed carve-out is separately configured via ECHO_INTAKE_SEED_BOT_ID + ECHO_INTAKE_SEED_CHANNEL_ID
  - src/surfaces/ceo-slack-responder/responder.ts:711-718 — extractIntakeSeed accepts ONLY self-bot-authored, marker-bearing messages in the configured seed channel; human/other-bot/off-channel messages fall through (regression-tested in tests/surfaces/ceo-slack-responder/intake-seed.test.ts:149-181)
  - src/surfaces/ceo-slack-responder/responder.ts:1424-1479 — dispatch priority: seed (write-before-ack) → decision action → intake action → question; intake-only mode short-circuits brain Q&A with a help message (responder.ts:417-429)
  - docs/onboarding/slack-linear-intake-runbook.md:76 — the bridge↔responder invariants (same bot token; bridge channel ∈ responder allowlist AND == seed channel) are documented as a runbook

### Daemon wiring vs separate-process boundary  (exists)
  - src/daemon/index.ts:72-90 — daemon boots fs/git watchers, granola poller, enrichment dispatch, 3 extractors, and the MCP server concurrently; src/daemon/index.ts:96 wires startGranolaIntakeBridge with runSignalsFirst coupling to the signals worker
  - package.json:16 — `!dist/surfaces/ceo-slack-responder/**` excludes the responder from the packaged npm dist; src/surfaces/ceo-slack-responder/index.ts:8 module-run guard makes it a standalone process entrypoint
  - src/mcp/server.ts:87-97 — propose_decision is loaded via optional dynamic import with a module_not_found graceful skip (registered at server.ts:272), so packaged daemons no longer crash (item 110 fix) but silently lack the tool
  - fly.toml:7 + Dockerfile:36 — the deployment story for the responder: Fly.io app `project-ech0`, sjc, 512MB shared VM, /data volume for the draft store and event log

### Config/secrets loading and config-error behavior  (partial)
  - src/surfaces/ceo-slack-responder/responder.ts:267-317 — loadResponderConfig validates tokens, absolute context path, and the intake-only/channel-list invariant at startup, before opening the socket
  - src/surfaces/ceo-slack-responder/linear-client.ts:126-156 — loadLinearConfig requires all six LINEAR_* vars, gated by shouldLoadLinearConfig/ECHO_LINEAR_INTAKE_ENABLED
  - src/enrich/granola-intake-candidates.ts:217-227 — loadGranolaIntakeConfig fail-closed: enabled-but-missing token/channel throws structured GranolaIntakeConfigError, handled to a disabled handle at granola-intake-candidates.ts:615-622
  - src/enrich/granola-intake-candidates.ts:165-179 + :624 — parseOwnerMap throws a PLAIN Error on a JSON typo which startGranolaIntakeBridge rethrows (only GranolaIntakeConfigError is caught), and src/daemon/index.ts:96 calls it unguarded at top level → an ECHO_GRANOLA_INTAKE_OWNER_MAP typo crashes the whole daemon at startup (open 109 follow-up, confirmed live in code)
  - .gitignore:56-58 — .env / .env.* gitignored; verified .env.fly-secrets and .env are untracked (public repo is safe); .env.fly-secrets holds 9 secret names, ECHO_CEO_SLACK_CHANNEL_IDS and both ECHO_INTAKE_SEED_* ids are absent from all committed deploy files and exist only as out-of-band `fly secrets set` state
  - src/enrich/granola-intake-candidates.ts:182-188 — the bot-token/channel equality between daemon-side bridge and Fly-side responder is explicitly a prose deploy invariant, 'not machine-checked here'
  GAP: Two real holes for demo-week traffic: (1) the owner-map JSON-typo daemon-crash vector — attach the fix at loadGranolaIntakeConfig by wrapping parseOwnerMap errors as GranolaIntakeConfigError (already named in backlog/_followups.md line 411, still open); (2) the bridge↔responder config-equality invariant spans two processes on two machines with zero verification — the natural chokepoint would be a doctor-style check or a startup log line on both sides printing the seed channel + bot id so drift is at least visible.

### Intake-path durability under real traffic (seed state machine, retry, terminal states)  (partial)
  - src/enrich/granola-intake-seed-store.ts:86-147 — pending→posting→posted/failed state machine with atomic tmp+rename writes, idempotent claim, retry-cap→terminal failed; crashed-'posting' records are retryable (tests/enrich/granola-intake-seed-store.test.ts:65) and concurrent claims converge (test line 78)
  - src/enrich/granola-intake-candidates.ts:436-467 — bridge skips posted/failed, re-posts pending/posting, marks failure with retry count; at-least-once semantics confirmed
  - src/enrich/granola-intake-candidates.ts:388-467 — CONFIRMED OPEN: the retry loop iterates classifier output, not seedStore.list(); a pending/posting seed is retried ONLY if the LLM re-emits the identical candidate ref within the 7-day lookback — non-deterministic brains or aged-out notes strand seeds forever (backlog/_followups.md:408 'fast-follow before real bridge traffic', not built)
  - src/enrich/granola-intake-candidates.ts:400-419 — CONFIRMED OPEN: no terminal-candidate skip, so every 10-min pass re-runs the brain on every external-attendee note in the lookback (_followups.md:409)
  - src/surfaces/ceo-slack-responder/responder.ts:824-838 — responder seed path is durable-write-before-ack; recordSeed is exactly-once per candidate_key (src/surfaces/ceo-slack-responder/intake-draft-store.ts:175, tested at tests/surfaces/ceo-slack-responder/intake-seed.test.ts:240,281)
  - src/surfaces/ceo-slack-responder/intake-draft-store.ts:257-267 — in-flight 'creating' replay → needs-reconcile without a second Linear create (exactly-once create), and linear_create failure → needs-reconcile with evidence (intake-draft-store.ts:318-330)
  GAP: Three durability gaps, all pre-identified in the 109 follow-ups and all still real in code: store-driven retry (seeds strand on classifier non-re-emission), terminal-skip (cost/nondeterminism per pass), and the needs-reconcile state having no reconcile path other than a human reading the event log — there is no tool or surface listing drafts stuck in needs-reconcile/failed. Attach points: runGranolaIntakeBridgeOnce should start from seedStore.list() non-terminal records; a reconcile listing belongs on the seed/draft stores' list() methods (both already exist on the seed store; the intake draft store has no list()).

### Observability — can the founder tell WHY a seed or answer didn't appear?  (partial)
  - src/surfaces/ceo-slack-responder/responder.ts:1051-1130 — four durable event-log appenders (usage, intake-failure/needs-reconcile, Slack-post failure, seed dismissal) writing to ECHO_CEO_EVENT_LOG_PATH (/data/slack-responder-events.md on Fly per fly.toml:18)
  - src/cli/commands/daemon.ts:114 — daemon-side structured logs land in ~/Library/Logs/echo via launchd StandardOut/ErrorPath (daemon.ts:493-495); bridge logs bridge_ok counts and seed_post_failed with retry_count+terminal flag (granola-intake-candidates.ts:461-466)
  - src/surfaces/ceo-slack-responder/responder.ts:711 — SILENT DROP: when seed carve-out env is unset, extractIntakeSeed returns null with no log at any level, and the bot-authored seed then dies in the question path as an ignored bot message; a misdeployed Fly worker eats every seed invisibly
  - src/surfaces/ceo-slack-responder/responder.ts:809 — accepted-seed-but-Linear-unconfigured logs only at debug, invisible at Fly's shipped ECHO_LOG_LEVEL=info (fly.toml:19; README documents which messages are debug-only) — open 109 follow-up (_followups.md:412)
  - src/surfaces/ceo-slack-responder/responder.ts:446-455 — CONFIRMED OPEN [105 follow-up, _followups.md:382]: if only the usage-log append throws, a SUCCESSFUL brain answer is replaced by a failure message posted to Slack — the answer is lost
  GAP: The per-hop logs exist but the cross-process story is weak: tracing one intake requires manually joining candidate_key across the daemon log (~/Library/Logs/echo), the seed store JSON (~/.echo/state/granola-intake-seeds.json), the Fly log stream, the /data event log, and the intake draft store JSON — five files on two machines. The two silent-drop points (unconfigured carve-out: no log; Linear-unconfigured: debug-only) are exactly the 'why didn't it appear' cases. Cheapest attach: warn-level logs at responder.ts:711 and :809, and keeping the debug-visibility caveat out of demo week by setting Fly to debug.

### Test coverage of the rails (tests/enrich, tests/surfaces, tests/daemon)  (partial)
  - tests/enrich/ — 3 suites: granola-intake-candidates.test.ts (fail-closed config :112, posted-dedupe :261, lookback :288, failure record :318), granola-intake-seed-store.test.ts (full state machine incl. crash recovery :65 and concurrent claims :78), granola-signals.test.ts
  - tests/surfaces/ — 14 files: intake-seed.test.ts (carve-out gates :149-181, write-before-ack :281, exactly-once :240), intake-confirm-idempotency, intake-gate, intake-followup, propose-confirm, confirm-idempotency, cross-team-scope, decision-store-latest-wins, issue-provenance, linear-client, socket-lifecycle (:120,:135), plus brain suites
  - tests/daemon/ — granola-intake-schedule.test.ts (disabled-by-default :43, fail-closed :55, runSignalsFirst ordering :76), lifecycle-shutdown-flush.test.ts; lifecycle.test.ts is QUARANTINED (docs/architecture-map/index.md:161)
  - docs/architecture-map/index.md:159 — all of these run inside the product CI gate (vitest.product.config.ts); none are in the orchestration exclusion list
  GAP: Loop-critical behaviors with no test: (1) daemon startup crash on OWNER_MAP typo (the crash IS current behavior, so a test would force the fix); (2) store-driven retry of stranded non-terminal seeds (feature absent); (3) any bridge→responder integration across the deploy invariant (bot-token/channel equality is prose-only); (4) duplicate 'Looking...' ack on Slack event replays (responder.ts:411 acks before dedupe — open 108 follow-up, _followups.md:407); (5) usage-log-append-failure answer-downgrade path; (6) daemon boot integration remains quarantined. Natural home: tests/daemon for (1)/(6), tests/enrich for (2), a new cross-suite fixture for (3).

SUMMARY: The rails are genuinely shipped and demo-capable: the daemon boots the full capture+enrich+bridge+MCP stack with graceful degradation for most config errors, the responder runs on Fly with restart-always as its keep-alive, the seed and intake-draft stores implement real at-least-once / exactly-once semantics with crash-recovery tests, and the intake channel model (allowlist + self-bot seed carve-out) is precise and well-tested. The readiness risks for real traffic through Jul 24 concentrate in five places, four of which the 109 follow-ups already name and none of which are fixed in code today: an ECHO_GRANOLA_INTAKE_OWNER_MAP JSON typo crashes the entire daemon at startup (unguarded plain-Error path through daemon/index.ts:96); seed retry is classifier-dependent rather than store-driven, so a transient Slack failure plus a non-re-emitting brain strands a seed permanently; a misconfigured seed carve-out drops every seed with zero log output at any level; and a usage-log write failure destroys a successful brain answer. The fifth is structural: the bridge (local daemon) and responder (Fly) are two processes on two machines whose correctness depends on a bot-token/channel equality invariant that is documented only in a runbook and verified by nothing, and the decision-confirm flow lives only on a local responder that has no supervisor at all. Observability is good per-hop but requires manually joining candidate_key across five artifacts on two machines, and Fly's shipped info log level hides the exact debug lines needed to diagnose ignored events.
SURPRISES: Two things the architecture map under-states: (1) fly.toml/Dockerfile define a full production deployment story (Fly app project-ech0, sjc, /data volume, restart=always, intake-only + Claude intake agent) that the map's src-surfaces page never mentions — the responder is not just 'a separate long-running process', it is a cloud-deployed worker whose config diverges deliberately from the local daemon; (2) the decision-confirm flow (propose_decision → Slack confirm card → team-decision store) is configured NOWHERE in the committed deploy files — the Fly worker will answer decision buttons with 'not configured', so station-4 recording depends on an unsupervised, manually-started local responder, and running both simultaneously on one Slack app token would split envelopes between them arbitrarily. Also verified safe: .env and .env.fly-secrets are gitignored and untracked despite the repo being public. The map itself was accurate everywhere I re-verified line numbers.