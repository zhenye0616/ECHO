---
backlog_item: 2026-05-28-078-decision-card-board
agent_run_started: 2026-05-29T03:50:05Z
agent_run_ended: 2026-05-29T04:17:05Z
status: ready_for_review
test_status: passing
---

# Agent Run: 078 DecisionCard Board

## What I Implemented

Built the daemon-owned `pending_decisions` MCP tool and the Raycast `decisions` board on branch `agent/decision-card-board` at `1ea38793100aabc720cc802805987074e6f3dc8c`.

The daemon side adds a playbook-agnostic `DecisionCard` wire type, an isolated ECHO-review-playbook adapter that reads active backlog items plus `combined.md` frontmatter, and a read-only MCP registration with bounded non-interactive freshness checks against `origin/main`. The Raycast side adds the third command, a single-flight polling board with stale/dirty/partial warnings, A1 signal accessories, SEE+JUMP source actions, README docs, and focused tests.

No `mcp__echo__*` calls were made during this run, so no dogfooding journal entry was required.

## Files Modified

- `src/mcp/tools/internal/decision-card-types.ts` — added playbook-agnostic DecisionCard and pending-decisions result types.
- `src/mcp/tools/internal/decision-source-playbook.ts` — added the v0 ECHO review-playbook source adapter.
- `src/mcp/tools/pending-decisions.ts` — added MCP tool handler, freshness state, rate-limited fetch cache, and tool registration export.
- `src/mcp/server.ts` — registered `pending_decisions`.
- `tests/mcp/pending-decisions.test.ts` — added adapter/tool/freshness tests.
- `tests/mcp/tools/recent-work-context.test.ts` — updated the runtime tool registration contract for the new tool.
- `tests/mcp/recent-calls-endpoint.test.ts` — added smoke args for the new runtime-registered tool.
- `tools/raycast-echo/package.json` — added the `decisions` command; command count is now `echo,recap,decisions`.
- `tools/raycast-echo/src/decisions.tsx` — added the read-only DecisionCard board.
- `tools/raycast-echo/src/lib/mcp.ts` — added `pendingDecisions(repoPath)`.
- `tools/raycast-echo/test/decisions.test.tsx` — added board/action/polling tests.
- `tools/raycast-echo/test/mcp-pending-decisions.test.ts` — added Raycast MCP client tests.
- `tools/raycast-echo/README.md` — documented the Decisions command and dogfooding template.

## Decisions Made During Implementation

### Decision 1: Keep YAML/frontmatter parsing local and narrow
- **Options considered:** bring in a parser, reuse `combine.py`, or parse only scalar frontmatter lines.
- **Chose:** local scalar frontmatter parsing in `decision-source-playbook.ts`.
- **Why:** the spec forbids changing `combine.py` and requires no body/table parsing. The adapter only needs simple fields (`next_round`, `escalated_to_founder`, reviewer response links, `combined_verdict`).
- **Worth founder review?** No.

### Decision 2: Treat `/tmp` as smoke repo for recent-calls audit coverage
- **Options considered:** create a temp git repo in the generic recent-calls smoke test or pass an absolute non-repo path.
- **Chose:** pass `repo_path: "/tmp"`.
- **Why:** the tool contract requires an absolute path; a non-repo path exercises the wrapper without network/git setup and returns stale source state instead of throwing.
- **Worth founder review?** No.

### Decision 3: No Raycast mock changes
- **Options considered:** extend `test/raycast-api-mock.ts` or use its existing `List`/`Action` surface.
- **Chose:** no mock edit.
- **Why:** the existing mock already had the needed List and Action components.
- **Worth founder review?** No.

## Acceptance Criteria Status

- [x] AC1 — `pending_decisions` registered and returns cards plus freshness state with rate-limited, timeout-bounded, non-interactive `git fetch`.
- [x] AC2 — playbook adapter uses active backlog dirs plus latest `combined.md` frontmatter only; no body/table parsing.
- [x] AC3 — A1-only `runaway_churn` signal implemented and reset by `escalated_to_founder: true`; no A2.
- [x] AC4 — Raycast `decisions` board renders cards, warnings, polling, teardown suppression, and SEE+JUMP open actions.
- [x] AC5 — card/result types are playbook-agnostic; combined.md/backlog knowledge is isolated in the adapter.
- [x] AC6 — added only the `decisions` command; extension has three commands; README section added.
- [x] AC7 — daemon and Raycast tests/typecheck/lint pass.
- [x] AC8 — dogfooding gate is documented for post-merge founder validation; builder did not fabricate journal entries.

## Tests Run

```text
$ npm run typecheck
> echoctl@0.1.0 typecheck
> tsc --noEmit
```

```text
$ npm run lint
> echoctl@0.1.0 lint
> eslint . --max-warnings 0 && npm run lint:task-state

> echoctl@0.1.0 lint:task-state
> python3 tools/task-state/lint.py
```

```text
$ npm test
Test Files  137 passed | 1 skipped (138)
Tests  1476 passed | 21 skipped (1497)
Duration  72.79s
```

```text
$ npx vitest run --config vitest.config.ts tests/mcp/pending-decisions.test.ts tests/mcp/tools/recent-work-context.test.ts tests/mcp/recent-calls-endpoint.test.ts
Test Files  3 passed (3)
Tests  80 passed (80)
Duration  5.76s
```

```text
$ cd tools/raycast-echo && npm run typecheck
> echo-context@0.4.0 typecheck
> tsc --noEmit
```

```text
$ cd tools/raycast-echo && npm test
Test Files  18 passed (18)
Tests  149 passed (149)
Duration  5.22s
```

```text
$ git diff --check
# no output
```

```text
$ git diff --cached --check
# no output
```

```text
$ cd tools/raycast-echo && npm ci --ignore-scripts --dry-run
npm warn EBADENGINE Unsupported engine {
npm warn EBADENGINE   package: '@raycast/api@1.104.17',
npm warn EBADENGINE   required: { node: '>=22.22.2' },
npm warn EBADENGINE   current: { node: 'v22.22.1', npm: '10.9.4' }
npm warn EBADENGINE }
added 71 packages in 312ms
```

## Open Questions for Founder

None.

## Anything I Almost Did But Stopped Myself

- Did not add A2/non-converging-patch logic.
- Did not add a `monitor` command or a fourth command.
- Did not add SEE+ACT or any write path under `backlog/`.
- Did not change `combine.py`, watcher logic, the coord ledger, `echo`, or `recap`.
- Did not add a caller-provided AbortSignal to `tools/raycast-echo/src/lib/mcp.ts`; the board suppresses stale results on cleanup instead.

## Drift Events

- Necessary test-contract drift: updated `tests/mcp/tools/recent-work-context.test.ts` and `tests/mcp/recent-calls-endpoint.test.ts`, which were not individually named in `files_to_modify`, because registering a new runtime MCP tool made the existing full-suite registration/audit contracts fail until they knew about `pending_decisions`. No extra product behavior was added.

## Next Suggested Backlog Items (Don't Auto-Create)

- None from the builder run. The spec already tracks Recap folding and AC8 dogfooding validation as post-merge work.
