# Agent run: 2026-05-17-061-doc-leanout

## Run 1 (2026-05-18T21:36 PDT)

### What implemented

Implemented the doc-surface leanout on branch `agent/doc-leanout` at `1d0b8840327946964d0817541878df3e0fda2954`.

- Removed all tracked generated `raw/internal/**/*.html` twins except `raw/internal/dogfooding/journal-style.html`.
- Added the `.gitignore` HTML-twin guard.
- Renamed the monolith journal to `raw/internal/dogfooding/mcp-interactions-journal-archive-through-2026-05-17.md`.
- Created `raw/internal/dogfooding/mcp-interactions-journal-2026-05.md` and carried forward the two uncommitted live-checkout Raycast dogfooding entries that would otherwise be stranded behind the rename.
- Rewrote `CLAUDE.md` journal policy for monthly shards, zero-MCP-call skip rule, and local-only HTML regeneration.
- Mirrored the current-shard journal rule into `AGENTS.md` to avoid a direct Codex-vs-Claude policy conflict.
- Created `backlog/archive/README.md`; moved item 001's original body to `backlog/archive/shipped/2026-04/`; replaced `backlog/complete/2026-04-30-001-repo-bootstrap.md` with the stub schema.
- Added the post-merge wiki-promotion stub step to `skills/merge-and-cleanup.md` and re-synced `.claude/commands/merge-and-cleanup.md`.
- Updated `docs/review-queue-setup.md` to the item-050 ephemeral-worktree reviewer wrapper model and canonical Codex invocation shape.
- Deleted `docs/STATUS.md`.

### Files modified

Feature branch: `agent/doc-leanout`

Head SHA: `1d0b8840327946964d0817541878df3e0fda2954`

Tracked diff: 24 files, 212 insertions, 33,960 deletions.

### Decisions

- Treated the spec's "13 HTML files" count as a stale count. The actual tracked set was 12 generated HTML twins plus the preserved `journal-style.html`; both `find raw/internal -name '*.html'` and `git ls-files 'raw/internal/**/*.html'` now return only `journal-style.html`.
- Did not edit `docs/BACKLOG.md`; the Ready-table row already existed before claim, and the builder skill forbids builder edits to `docs/BACKLOG.md`.
- Did not duplicate the pre-existing AC7 `_followups.md` section; it was already present on `origin/main`.
- Did not bulk-update historical references to `mcp-interactions-journal.md`. The broad grep still finds 58 markdown files, mostly historical backlog/raw/wiki/spec references and review-queue skill prose that AC3/OoS explicitly excluded. Operational-current files changed in this run (`CLAUDE.md`, `AGENTS.md`, new shard, merge skill, setup doc) have no bare old-path reference.
- Added a minimal `AGENTS.md` mirror edit even though the spec omitted it, because AGENTS declares `CLAUDE.md` as source of truth and says conflicts must be surfaced. Leaving Codex's current instructions pointing at the frozen path would create an immediate conflict with AC2.

### Acceptance status

- AC1: Complete. Generated HTML twins removed; `.gitignore` guard added; `CLAUDE.md` now documents local-only pandoc regeneration.
- AC2: Complete. Monolith renamed to archive; May shard created with preamble and carried-forward post-cutover entries; `CLAUDE.md` current-shard pointer updated.
- AC3: Complete for policy source of truth. `CLAUDE.md` has the zero-MCP-call skip rule. Review-queue skill prose was not edited per the spec's explicit out-of-scope clause.
- AC4: Complete. Archive README added; item 001 stubbed; original item 001 body archived; merge-and-cleanup canonical and adapter include the post-wiki-promotion stub step.
- AC5: Already satisfied / no-op. `.claude/worktrees/laughing-shaw-952357` and branch `claude/laughing-shaw-952357` were absent at build time.
- AC6: Complete. `docs/review-queue-setup.md` now documents item-050 worktree isolation and the canonical Codex invocation shape; `docs/STATUS.md` deleted.
- AC7: Already satisfied before build. `_followups.md` already contained the required 2026-05-17 TZ-offset retrieval bug section.

### Verification output

```text
$ tools/sync-skills.sh --check
OK: all Claude command adapters match canonical skills/

$ git diff --cached --check
<no output>

$ find raw/internal -name '*.html' | sort
raw/internal/dogfooding/journal-style.html

$ git ls-files 'raw/internal/**/*.html' | sort
raw/internal/dogfooding/journal-style.html

$ du -sh raw backlog wiki docs skills   # before
4.0M    raw
4.1M    backlog
732K    wiki
120K    docs
208K    skills

$ du -sh raw backlog wiki docs skills   # after
2.3M    raw
4.1M    backlog
732K    wiki
116K    docs
208K    skills

$ rg -l "mcp-interactions-journal\\.md" --glob '*.md' | wc -l
58
```

The last command is the documented caveat: satisfying the spec's broad grep literally would require editing historical/OoS files, including review-queue skills the spec explicitly says not to edit. I left those untouched and recorded the caveat for review.

### Open questions

- Should a followup update review-queue skill prose to use the current shard and stop regenerating journal HTML? The spec explicitly excluded that edit, but those skills still contain old explicit path/add commands.

### Drift events

- No product or feature drift. One scope note: `AGENTS.md` was edited as a minimal mirror of `CLAUDE.md` to prevent a load-bearing instruction conflict for future Codex sessions.
