# `tests/skills/` — architecture map

> Part of the [ECHO architecture map](index.md). Generated 2026-07-03 from code at commit `0f77efa1`; every symbol row cites its declaration as `path:line`.
> Covers 2 files.

### `tests/skills/atomic-state-transition-harness.test.ts` — P1 atomic state-transition harness + process-backlog current-consumer tests

**Purpose:** Defines a substrate-neutral contract (`P1ConsumerFixture`) and generic harness (`runP1Harness`) asserting the 7 "P1" atomic-state-transition invariants (crash-safety, deterministic recovery, no undeclared touched/dirty surfaces, durable-boundary shape validity, idempotent recovery/finish, key-scoped concurrency isolation), proves the contract itself with two neutral fixtures (local-commit boundary, pushed-ref boundary with a finish path), then specializes it with 14 tests against an embedded byte-for-byte copy of the process-backlog skill's Step E publish/recovery bash transcript, run against a real temp git repo + bare "origin". A final structural-pin describe block asserts the canonical `skills/process-backlog.md` still contains the literal markers the embedded transcript assumes.

**Depends on:** `skills/process-backlog.md` (read + pinned, not imported), `tools/review-queue/push-with-retry.sh` (embedded literal source), `tools/review-queue/_effect-runner.sh` (embedded literal source), `tools/task-state/patch-builder-state.py` (embedded literal source), `tools/task-state/lint.py` (embedded literal source); external: `node:child_process`, `node:fs`, `node:os`, `node:path`, `vitest`.

**Symbols:**

| Symbol | Kind | Location | Description |
|---|---|---|---|
| `DurableBoundaryObservation` | type | `tests/skills/atomic-state-transition-harness.test.ts:56` | Shape of an observed publish boundary: kind, commit/ref count, token, published flag, observer scope (local/remote/external). |
| `ObservedState` | type | `tests/skills/atomic-state-transition-harness.test.ts:64` | Snapshot a fixture returns from `observe()`: boundary state, source/target visibility+completeness, staged and dirty surfaces. |
| `PrePublishStep` | type | `tests/skills/atomic-state-transition-harness.test.ts:73` | One named step in the pre-publish sequence with its allowed dirty surfaces and whether the target may already be visible. |
| `P1ConsumerFixture` | type | `tests/skills/atomic-state-transition-harness.test.ts:80` | Full contract a consumer must implement: setup, touched surfaces, observe, pre-publish steps, publish/recover/finish/assert hooks. |
| `VALID_BOUNDARY_KINDS` | const | `tests/skills/atomic-state-transition-harness.test.ts:100` | Enumerates the 5 legal `DurableBoundaryObservation.kind` values used to validate fixture output. |
| `VALID_OBSERVER_SCOPES` | const | `tests/skills/atomic-state-transition-harness.test.ts:107` | Enumerates the 3 legal `observerScope` values (local/remote/external). |
| `dirtySurfacePath(surface)` | function | `tests/skills/atomic-state-transition-harness.test.ts:113` | Strips `git status --porcelain` status-code prefix and rename arrow from a raw dirty-surface line to recover the bare path. |
| `assertPrePublishObservation(fixture, step, obs)` | function | `tests/skills/atomic-state-transition-harness.test.ts:120` | Throws/asserts that target visibility and every dirty surface at a given pre-publish step match that step's declared allowances. |
| `convergeFromPartialState(fixture)` | function | `tests/skills/atomic-state-transition-harness.test.ts:141` | Calls `recover()`, then if unpublished and target-visible/complete calls `finishUnpublishedTransition()`; else asserts clean source state; returns the resulting boundary or undefined. |
| `runP1Harness(fixture)` | function | `tests/skills/atomic-state-transition-harness.test.ts:167` | The generic harness: validates touched-surface prefixes, replays each pre-publish step as a crash point and proves deterministic convergence from it, then does a full clean run to publish, validates the boundary shape, and re-runs recover/finish to prove idempotency. |
| `git(cwd, ...args)` | function | `tests/skills/atomic-state-transition-harness.test.ts:239` | Runs `git <args>` synchronously in `cwd` via execSync, returns trimmed stdout, throws on nonzero exit. |
| `tryGit(cwd, ...args)` | function | `tests/skills/atomic-state-transition-harness.test.ts:243` | Runs `git <args>` via spawnSync without throwing; returns `{code, stdout, stderr}`. |
| `RepoEnv` | interface | `tests/skills/atomic-state-transition-harness.test.ts:248` | Holds temp-dir paths: `base`, `bare` (origin.git), `repo` (working clone). |
| `initRepo()` | function | `tests/skills/atomic-state-transition-harness.test.ts:254` | Creates a temp dir with a bare `origin.git` and a working repo configured with a test git identity and `origin` remote. |
| `bootstrapFile(repo, path, content)` | function | `tests/skills/atomic-state-transition-harness.test.ts:271` | Writes a file, commits it, and pushes to `origin main` to seed the fixture's starting state. |
| `teardown(env)` | function | `tests/skills/atomic-state-transition-harness.test.ts:280` | Best-effort recursive removal of the temp base directory. |
| `makeFixtureA(transitionKey)` | function | `tests/skills/atomic-state-transition-harness.test.ts:296` | Builds the "neutral fixture A" P1ConsumerFixture: local git-commit boundary, target invisible until publish, one pre-publish step dirtying the source file. |
| `makeFixtureB(transitionKey)` | function | `tests/skills/atomic-state-transition-harness.test.ts:410` | Builds "neutral fixture B": pushed-ref boundary where the rename is committed locally (target visible) before push; implements `finishUnpublishedTransition` to push an already-committed rename. |
| `MemoryTransition` | type | `tests/skills/atomic-state-transition-harness.test.ts:545` | In-memory per-key transition state (visibility flags, dirty surfaces, published flag, token) used by the shared-substrate concurrency fixtures. |
| `SharedMemorySubstrate` | type | `tests/skills/atomic-state-transition-harness.test.ts:554` | Holds a `records` map keyed by transitionKey plus an `observations` log and a token counter, shared across concurrently-run fixtures. |
| `makeSharedMemorySubstrate()` | function | `tests/skills/atomic-state-transition-harness.test.ts:560` | Constructs an empty `SharedMemorySubstrate`. |
| `recordMemoryObservation(substrate, key, surfaces)` | function | `tests/skills/atomic-state-transition-harness.test.ts:568` | Appends observed surfaces to the substrate's per-key observation log. |
| `makeSharedMemoryFixture(substrate, transitionKey)` | function | `tests/skills/atomic-state-transition-harness.test.ts:578` | Builds a non-git `P1ConsumerFixture` operating purely on the shared in-memory substrate, scoped to `mem/<key>/`, used to prove key-scoped concurrency isolation (property 7) when two fixtures run via `Promise.all`. |
| `describe: "AC1 — generic P1 harness contract"` | describe | `tests/skills/atomic-state-transition-harness.test.ts:704` | Exercises `runP1Harness` against fixtures A and B (accept), a fixture with an undeclared touched-surface prefix (reject), a fixture whose pre-publish step dirties an undeclared surface (reject), and two shared-memory fixtures run concurrently to prove distinct transitionKeys never observe each other's surfaces. |
| `HANDOFF_SCRIPT` | const | `tests/skills/atomic-state-transition-harness.test.ts:810` | Embedded bash transcript byte-equivalent to `skills/process-backlog.md` Step E: implements `recover_p1_stage_move` (prefix-guarded rollback of `backlog/claimed/<id>.md`↔`backlog/pending_review/<id>.md` plus task-state pointer) and the `publish` phase (task-state patch + lint, `git mv`, commit, push via `push-with-retry.sh`, remote-boundary verification). |
| `ConsumerEnv` | interface | `tests/skills/atomic-state-transition-harness.test.ts:957` | Extends `RepoEnv` with the handoff script path and computed item/dest/pointer/log relative paths for the current-consumer fixture. |
| `ITEM_BODY` | const | `tests/skills/atomic-state-transition-harness.test.ts:968` | Frontmatter+body content of the synthetic `backlog/claimed/TEST-066-fixture.md` seed file. |
| `POINTER_BODY` | const | `tests/skills/atomic-state-transition-harness.test.ts:988` | Frontmatter+body content of the synthetic builder task-state pointer file used when `withPointer` is set. |
| `makeConsumerEnv(opts)` | function | `tests/skills/atomic-state-transition-harness.test.ts:1018` | Builds a temp repo mirroring the real process-backlog layout (tools/review-queue, tools/task-state, backlog/claimed, backlog/pending_review with `.gitkeep`, optional task-state pointer, an untracked run-log), bootstraps+pushes it, and writes the embedded `HANDOFF_SCRIPT` to disk as an executable. |
| `applyMetadataEdit(env, headSha)` | function | `tests/skills/atomic-state-transition-harness.test.ts:1083` | Rewrites `head_sha` and `agent_notes` fields in the claimed item file to simulate the agent's completion edit. |
| `runHandoff(env, phase, extraEnv)` | function | `tests/skills/atomic-state-transition-harness.test.ts:1091` | Spawns the embedded handoff script with `PHASE`, `REPO_ROOT`, `ITEM_ID`, `SLUG`, `HEAD_SHA`, `OUTCOME`, `LOG` env vars (plus overrides) and returns the spawnSync result. |
| `expectClean(r, ctx)` | function | `tests/skills/atomic-state-transition-harness.test.ts:1113` | Throws with stdout/stderr context if a handoff invocation's exit status is nonzero. |
| `runFullHandoff(env, headSha)` | function | `tests/skills/atomic-state-transition-harness.test.ts:1122` | Convenience: runs `recover`, applies the metadata edit, then runs `publish`, asserting both succeed. |
| `describe: "AC3 — process-backlog stage move (current-consumer specialization)"` | describe | `tests/skills/atomic-state-transition-harness.test.ts:1132` | 14 tests (test 1–14) against `makeConsumerEnv`/`runHandoff`: pre-publish edits stay unexposed; publish creates one durable boundary (rename + log add, local+remote); committed destination carries edited metadata locally and remotely; lint failure is fail-fast before any git mutation (test 3b); crash recovery at various points (after metadata edit, after `git mv` before `git add`, after local commit before push) always converges without operator input; recovery rejects unsafe/absolute/traversal/out-of-prefix surfaces (exit 2); recovery is a no-op post-publish; per-surface untracked-cleanup dispatch surfaces real errors (exit 4) vs proceeding past benign untracked absence; post-rollback dirty-check failure returns exit 5; tracked-dirty `$LOG` and `raw/internal/queue-errors.md` never block the post-recovery `pull --rebase --autostash`. |
| `describe: "skills/process-backlog.md — P1 transcript markers (pins skill ↔ test coupling)"` | describe | `tests/skills/atomic-state-transition-harness.test.ts:1470` | Table-driven (`it.each`) structural pin asserting the canonical skill markdown still contains 13 literal bash markers (set -euo pipefail, prefix array, helper function names, git mv/add/rm lines, push-with-retry call, pull --rebase --autostash, --spec-path, and the three numeric return codes) that the embedded `HANDOFF_SCRIPT` assumes are kept in sync. |

### `tests/skills/merge-and-cleanup-shape.test.ts` — structural pin tests for `skills/merge-and-cleanup.md` sections

**Purpose:** Pure-markdown structural assertions (no code execution) that pin specific sections of the canonical `skills/merge-and-cleanup.md` skill file to required literal content and shape, guarding against silent regressions such as the verify command sliding out of its fence, or the §C3/§C3.5 cross-vendor-consult contract losing required branches/fields. Each extraction helper is also exercised against synthetic fixtures independent of the canonical file to prove the extraction logic itself is correct.

**Depends on:** `skills/merge-and-cleanup.md` (read-only target); external: `node:fs`, `node:path`, `vitest`.

**Symbols:**

| Symbol | Kind | Location | Description |
|---|---|---|---|
| `C5_HEADING` | const | `tests/skills/merge-and-cleanup-shape.test.ts:20` | Anchored regex matching a `C5` heading line without matching `AC5`/`C50`/`C5A`. |
| `C6_HEADING` | const | `tests/skills/merge-and-cleanup-shape.test.ts:21` | Anchored regex matching a `C6` heading line, same non-alphanumeric-boundary discipline as C5_HEADING. |
| `FENCE` | const | `tests/skills/merge-and-cleanup-shape.test.ts:23` | Regex matching a markdown fenced-code-block delimiter line (optional leading whitespace, triple backtick, optional language tag). |
| `ExtractionResult` | interface | `tests/skills/merge-and-cleanup-shape.test.ts:25` | Return shape `{fenceBody, error}` for `extractFirstC5CodeFence`. |
| `extractFirstC5CodeFence(text)` | function | `tests/skills/merge-and-cleanup-shape.test.ts:30` | Finds the C5 heading, then the following C6 heading, then the first fenced code block strictly between them, and returns its body or a specific error string if any anchor/fence is missing. |
| `describe: "skills/merge-and-cleanup.md — C5 verify block shape"` | describe | `tests/skills/merge-and-cleanup-shape.test.ts:76` | Asserts the real skill's first C5 fenced block literally contains `tools/sync-skills.sh --check`, and that the extractor correctly rejects synthetic content with only AC5/C50 headings, missing C6, or no fence — each with a distinct error message. |
| `C3_HEADING` | const | `tests/skills/merge-and-cleanup-shape.test.ts:131` | Anchored regex matching a `C3` heading line. |
| `C3_5_HEADING` | const | `tests/skills/merge-and-cleanup-shape.test.ts:132` | Anchored regex matching a `C3.5` heading line. |
| `C4_HEADING` | const | `tests/skills/merge-and-cleanup-shape.test.ts:133` | Anchored regex matching a `C4` heading line. |
| `POST_REVIEW_HEADING` | const | `tests/skills/merge-and-cleanup-shape.test.ts:134` | Case-insensitive anchored regex matching a "Post-review handling" heading. |
| `CONSULT_FAILURE_HEADING` | const | `tests/skills/merge-and-cleanup-shape.test.ts:135` | Case-insensitive anchored regex matching a "Consult-failure recovery" heading. |
| `PROMPT_SUBHEADING` | const | `tests/skills/merge-and-cleanup-shape.test.ts:136` | Case-insensitive regex matching any heading containing "prompt". |
| `BlockResult` | interface | `tests/skills/merge-and-cleanup-shape.test.ts:138` | Return shape `{block, error}` for `extractBetween`. |
| `extractBetween(text, startRe, endRe, startLabel, endLabel)` | function | `tests/skills/merge-and-cleanup-shape.test.ts:140` | Generic section extractor: finds the first line matching `startRe`, then the first subsequent line matching `endRe`, and returns the text strictly between them, or a labeled "heading not found" error. |
| `joinContinuations(text)` | function | `tests/skills/merge-and-cleanup-shape.test.ts:157` | Joins backslash-continued lines into single logical lines, used before scanning for a possibly multi-line codex-exec invocation. |
| `findCodexExecLine(block)` | function | `tests/skills/merge-and-cleanup-shape.test.ts:169` | Runs `joinContinuations` over a block and returns the first logical line containing `codex exec`, or null. |
| `describe: "skills/merge-and-cleanup.md — §C3 pause contract (054 AC2a)"` | describe | `tests/skills/merge-and-cleanup-shape.test.ts:177` | Asserts the real §C3 (between C3 and C3.5 headings) surfaces the `c3.5`/`continue`/`abort` branches within 30 lines of a `<<<<<<<` conflict marker; also tests the extractor's distinct-error behavior on synthetic content missing C3.5 or lacking the c3.5 branch. |
| `describe: "skills/merge-and-cleanup.md — §C3.5 contract (054 AC2b)"` | describe | `tests/skills/merge-and-cleanup-shape.test.ts:211` | Asserts §C3.5 contains the OPTIONAL marker, a codex-exec recipe with `$MERGER_WT`, `--sandbox read-only`, and stdout/stderr redirect targets, founder-explicit/strategist-recommended trigger mentions, the three verdict labels plus `verdict:`/`reviewer:`/`consult_cwd:` fields, and `pwd -P`; a prompt-template subsection with ≥6 list items mentioning `$MERGER_WT`/`consult_cwd`/`pwd -P`; a Post-review handling subsection with exactly 3 verdict entries each with ≥30 chars of action prose; a Consult-failure recovery subsection naming all 4 failure-mode signatures (not-found/127, exit-non-zero, malformed/unparsable, consult_cwd mismatch); plus numerous synthetic-fixture tests proving each assertion's extractor/regex rejects incomplete inputs. |
| `describe: "skills/merge-and-cleanup.md — review_notes + commit-body audit-trail (054 AC2c)"` | describe | `tests/skills/merge-and-cleanup-shape.test.ts:371` | Asserts the §C6 review_notes template contains the literal "C3.5 cross-vendor consult:" line and the §C8 commit-body HEREDOC contains the literal "Cross-vendor consult:" signpost. |
