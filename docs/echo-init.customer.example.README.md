# echo-init.customer.example.json — RETIRED (2026-07-11)

> **Not the client-runtime answer file.** The `"profile": "customer"` in the sibling JSON is the legacy Machine-context audience split, which only filters coordination assets; it is **not** a Team-product runtime/composition selector (graduation-pipeline separation invariant 3). It configures the retired dev context-layer beta, not the meeting→brief product. Do not hand to a client. The real client-runtime example is owed under WS5 once Contract B (`docs/install-contracts.md`) exists.
