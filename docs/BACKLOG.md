# BACKLOG

**Kanban view of all active work.** Auto-orderable; each item is a file in `backlog/<status>/`.

> **System:** the spec for any in-flight item lives inside the item file in `backlog/ready/` — the wiki is **not** updated at decision time. Agents claim items from `ready/`, work on a feature branch in their own worktree, and move items through `claimed/` and `pending_review/`. Founder merges and moves to `complete/`. The strategist promotes shipped decisions to `wiki/` only after items land in `complete/`. See [`backlog/README.md`](./backlog/README.md) for the full workflow.

---

## 📥 Inbox (new items, not yet specced)

*Items added during conversation that need spec refinement before agents can claim them.*

| ID (reserved) | Title | Depends on | Source |
|---|---|---|---|
| 2026-05-25-074-echo-cli-binary | `echo` CLI binary — `init` / `run` / `doctor` / `uninstall` + runtime role-plugging | 070 + 071 + 072 + 073 | Decision archive §"Coord layer architecture" + §"Implied backlog decomposition". `echo init` launches 073 wizard. `echo run <workflow> [--project PATH]` matches role requirements against onboarded agent capabilities (per 071's controlled vocabulary), picks an agent per role, dispatches. `echo doctor` reports daemon status + agent wiring health + state-file consistency. `echo uninstall` rolls back 072's adapter writes via stored `~/.echo/state/onboarding.json`. CLI ergonomics: `cwd` is implicit project root if `--project` absent (codex consult 2026-05-25 recommendation). Estimate at spec time. |
| 2026-05-25-075-first-demo-workflow | First-session demo workflow (lead candidate: cross-vendor change review with diff-source priority `PR > unpushed > uncommitted > HEAD~1..HEAD`) | 074 + founder dogfooding | Decision archive §"What's deferred" — founder will dogfood the onboarding end-to-end as a customer first; the demo question is meant to resolve itself based on what the founder actually wants to do in their first session. Codex's preliminary recommendation (cross-vendor change review reformulated from PR-only to any-diff with auto-detected substrate priority) is the lead hypothesis. Specced after 074 ships AND founder completes their own onboarding run as a customer. Estimate at spec time. |

*Note: IDs 074-075 are reserved to prevent collision while these subsystems remain unspecced. Next free id is 076.*

---

## 🔨 Ready (specced, agents may claim)

*029, 030, 032, 033 all shipped 2026-05-10 — see `backlog/complete/`. V1.6 friction-revisit (2026-05-10) opened 6 successor items; this row tracks the smallest-first per Codex pushback #5 sequencing.*

| Priority | ID | Title | Estimate | Notes |
|---|---|---|---|---|
| HIGH | [2026-05-25-070-echo-global-home-scaffold](../backlog/ready/2026-05-25-070-echo-global-home-scaffold.md) | ECHO Pro global home (`~/.echo/`) scaffold — directory layout + state-file schemas + daemon-aware paths | 0.5-1d | Foundation for ECHO Pro paid coord layer per 2026-05-25 brainstorm (`raw/internal/decisions/2026-05-25-echo-pro-paid-coord-layer-design.md`). Establishes `~/.echo/{skills,roles,state,adapters}/` tree, `onboarding.json` + `projects.json` schemas (TypeScript types in `src/echo-home/paths.ts`), idempotent `ensureEchoHome()` in `scaffold.ts`, daemon bootstrap integration (`src/daemon/index.ts`, non-fatal on failure). Reuses existing `resolveDataDir` env+homedir pattern from `src/daemon/lifecycle.ts:18-22`. AgentProfile + ProjectsState schemas pre-extended with fields 073 wizard will need (`capabilities`, `wire_error`, `default_project`). Ajv validators inlined; no new deps. 4 ACs, 6 test cases. Specced 2026-05-25 by parallel team-mode subagent `070-scaffold@echo-pro-spec-drafting`. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | [2026-05-25-071-role-definition-format-and-defaults](../backlog/ready/2026-05-25-071-role-definition-format-and-defaults.md) | ECHO Pro role definition format + 3 default roles (strategist / reviewer / builder) | 0.5-1d | The product unit of ECHO Pro per the 2026-05-25 brainstorm. TOML schema for `~/.echo/roles/<name>.toml`, loader/validator at `src/echo-home/roles.ts` (avoids naming conflict with existing `src/coord/roles.ts` from 057a coord SLA loader), 3 canonical role TOMLs at `assets/echo-roles/` (072 copies them to `~/.echo/roles/` at install). Design choices flagged for review: `name` derived from filename; 7-entry controlled capabilities vocabulary (`fs.read/write`, `git.read/write`, `network`, `mcp.echo.read/write`); `mcp_servers` carries names not URLs; `sandbox` enum `read-only` / `workspace-write` (no `danger-full-access` in V1); strict unknown-key rejection; smol-toml@^1.3.1 parser. Strategist bundles `review-pending` + `merge-and-cleanup` (per Reviewer Independence Rule). Loader does skill-existence check against `skills/<name>.md`. 4 ACs with sub-clauses, 21 test cases. `blocked_by: []` — 070 is impl-time dep but spec-writing is parallel. Specced by `071-roles@echo-pro-spec-drafting`. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | [2026-05-25-073-onboarding-wizard](../backlog/ready/2026-05-25-073-onboarding-wizard.md) | ECHO Pro onboarding wizard — staged library API for detect-agents / detect-projects / wire / probe (steps 2-5) | 1.5-2.5d | Decision substance of ECHO Pro onboarding per the 2026-05-25 brainstorm — UX-free library 074's `echo init` drives. 7 modules under `src/echo-home/wizard/`: `detect-agents.ts` (config-file + atom-store-activity layered scan with 4-bucket confidence rollup; running-process detection dropped per J3), `detect-projects.ts` (group-by `metadata.repo_root` over 7d ranked by activity; defaults to empty list on fresh install, not throw), `adapter-cache.ts` (the `previous*` JSON cache 072 AC6 deferred — one file per agent at `~/.echo/adapters/<kind>.json`, schema_version: 1, written via 072's `atomicWrite` with `secretSensitive: true`), `render-echo-section.ts` (pure markdown renderer for the BEGIN/END ECHO body 072's markers.ts splices in; includes version + renderedAt fingerprint so 072's conflict detection compares byte-for-byte), `wire.ts` (orchestrator: build AdapterSyncProfile[], call 072's syncAll, persist new previous* only on success+non-conflict, mutate `~/.echo/state/onboarding.json` per 070's schema), `probe.ts` (per-agent best-effort spawn: codex + claude-code automated via `codex exec`/`claude --print`, cursor manual-only per J4 — no V1 headless cursor CLI), `run-wizard.ts` (staged `createWizard()` factory with `detectAgents`/`detectProjects`/`wire`/`probe`/`summary`/`markCompleted` — 074 drives flow + user confirmation). Atom-store access is direct SQLite via `AtomStore` interface (J2 — faster + testable, no daemon dependency for reads). 8 ACs, 45 test cases across 7 test files. 6 design judgment calls (J1-J6) flagged inline for r1 reviewer pushback. `blocked_by: [070, 071, 072]` — full impl-time chain. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | [2026-05-25-072-adapter-sync-engine](../backlog/ready/2026-05-25-072-adapter-sync-engine.md) | ECHO Pro adapter sync engine — merge-with-markers for AGENTS.md/CLAUDE.md + TOML/JSON mutators + idempotent re-sync | 1-2d | The action layer of ECHO Pro onboarding per the 2026-05-25 brainstorm. 6 sub-modules under `src/echo-home/adapters/`: `markers.ts` (merge-with-markers for `~/.codex/AGENTS.md` + `~/.claude/CLAUDE.md` with `<!-- BEGIN ECHO --> ... <!-- END ECHO -->` delimited region; refuses to overwrite user-edited content inside markers, returns typed conflict), `codex-config.ts` (TOML mutator targeting `[mcp_servers.echo]` only; smol-toml lead candidate with `@iarna/toml` fallback documented; builder verifies round-trip against founder's real `~/.codex/config.toml` with 15+ `[projects.*]` tables before adding the dep), `cursor-config.ts` (JSON mutator for `~/.cursor/mcp.json`, path verified on founder's machine), `skill-sync.ts` (overwrite copy `~/.echo/skills/*.md` → `~/.claude/commands/*.md` — fully ECHO-owned per decision archive, flagged for founder review since users may want to hand-tune skill prompts), `role-sync.ts` (first-install copy `assets/echo-roles/*.toml` → `~/.echo/roles/`, conflict-protected for hand-edits), and an orchestrating `syncAll(profiles)` returning `SyncResult` with per-agent outcomes. No transactional rollback — partial failures (per-file atomic via tmp+rename) re-converge on re-run. 7 ACs, 28 test cases across 6 test files. Implementation depends on 070 + 071 having landed; spec writing parallel. Specced by `072-adapter-sync@echo-pro-spec-drafting`. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | [2026-05-22-069-raycast-cold-start-continuity-hero](../backlog/ready/2026-05-22-069-raycast-cold-start-continuity-hero.md) | Raycast cold-start continuity hero — confidence-gated `Continue` row replaces the over-approximating "Open loops · Today" list | 0.5-1d | V1 80% ROI cut of the founder + claude + codex strategist brainstorm (2026-05-22 PDT) on the Raycast landing surface as "first thing opened to check where you left off." Two corrections: (1) `has_open_loop` over-approximates because it counts resolved hints (`src/trace/rank.ts:79-87` against `src/trace/hints.ts:39-95` resolver) — V1 adds `has_unresolved_open_loop` (resolved-false only) as the gate; (2) replaces `EmptyState.tsx:31-49` "Open loops · Today" up-to-three list with a single conditional `Continue` hero row gated on (running session) OR (top cluster with unresolved hint AND 18h freshness AND code/session anchor). New `code_session_anchor` rank signal: repo_root/file_ref/commit artifact OR git source atom OR cluster_id-linked session OR ≥3 distinct apps. Hero text: `Continue: <label-or-USER-preview, 60 chars> · <N> open`. 10-item OoS list explicitly defers dominant-file/repo suffix, additive ranking boosts (failed tool / dirty git / recent commit), adaptive grouping in secondary list, frontmost-app CWD anchoring (launch.ts only sees app name), `has_open_loop` removal, 18h tuning, new MCP tools. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | 2026-05-21-068-tail-mcp-shutdown-banner (stub) | `tail-mcp.sh` surfaces prior shutdown MCP losses on next daemon boot | 0.25d | Sibling stub surfaced by 067 respec against P2. Scope: teach `tools/tail-mcp.sh` to read `<dataDir>/mcp-shutdown.jsonl` on daemon restart and print an operator-visible banner/count for entries rewritten to `killed_during_shutdown`. Full `backlog/ready/` spec file intentionally not created in this turn. |
| HIGH | [2026-05-20-065-raycast-cluster-resume](../backlog/ready/2026-05-20-065-raycast-cluster-resume.md) | Raycast ECHO — cluster-click resume (broken object identity fix; default open prior session over re-asking) | 1-1.5d | Closes a continuity regression in the Raycast wedge: cluster-row clicks unconditionally spawn fresh agent runs even when a session for that cluster has already completed. Founder's exact dogfood report (2026-05-20 21:25 PDT): *"if I exit, everything is gone. Even when the session is done I exit, go back, live call restarts."* Two parallel codex consults — strategist (established role) + senior PM (newly prompted role, role-creation experiment) — converged on: resume-by-default for repeat clicks; explicit `Ask Again from This Cluster` action as the escape hatch (NOT a modal); cluster-row carries an `Answered {time}` chip when a prior session exists; lookup keyed on `cluster_id` only (atom-fingerprint freshness is V1.5+ per PM scope-discipline). Both voices independently flagged the running-session edge case (AC4) and agreed `SessionsList` stays as browse/history while cluster click becomes the deep link into the same sessions object model. PM reframed the bug as "broken object identity, not missing cache" — fix is plumb `cluster_id` onto sessions and route, not add a cache layer. Cost: tokens DOWN (repeated clicks skip agent runs). Storage migration is forward-only + lossless (additive optional `clusterId` field). 10-item OoS list explicitly defers fuzzy topic matching, multi-turn chat continuation, true live-attach to running agents, staleness scoring, daemon-side changes, and errored/cancelled-session "resume." Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | [2026-05-20-064-mcp-compact-view-projection](../backlog/ready/2026-05-20-064-mcp-compact-view-projection.md) | MCP `view: "compact"` projection — substrate-level human-noise filter on `find_clusters` + `get_atoms` | 1-1.5d | Substrate prerequisite for the upcoming Raycast UI redesign. Adds optional `view?: "compact" \| "rich"` (default `"rich"`) to `find_clusters` + `get_atoms` only; Raycast switches to compact. Codex strategist consult on 2026-05-20 14:08 PDT reframed the noise/signal question: the fields claude labelled noise (opaque bubble UUIDs, byte offsets, nested `codex.{sandbox_*, permission_*}` config, debug rank_reasons, UUID-fallback labels, `<!-- ECHO cluster -->` HTML comment) are NOT Raycast-specific — they're substrate leakage. If every future consumer (V1 browser extension, future overlays) re-learns what to hide, ECHO exports capture/runtime internals through its contract. Path A-lite: smallest correct change is a projection knob on the existing tools (codex's exact words), not a new `*_for_display` tool. Default `rich` keeps agents unaffected. Compact projection lives in a single shared module (`src/mcp/wire-shape/compact.ts`) composed with the existing `match.ts` cap projector. AC7 emits `label: null` under compact when the daemon would otherwise emit the UUID-fallback `"discussion about <uuid>"` shape — substrate-side fix for what's currently a per-consumer detect-and-suppress task. 10-item OoS list explicitly defers `search_memories` + `get_atom` compact mode, all Raycast rendering work, deprecation of rich mode, and a new for-display tool. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | [2026-05-19-063-raycast-sessions-as-objects](../backlog/ready/2026-05-19-063-raycast-sessions-as-objects.md) | Raycast ECHO — sessions as objects (D + narrow C persistence model) | 2-3d | UX redesign of the unified `echo` Raycast command following codex strategist consult (journal 2026-05-19 15:20 PDT). Founder named 4 pains during dogfooding: sessions exist but no details, atom counts but no info, navigation unclear, no persistent Q&A. Codex reframed: root pain is "loss of continuity and inspectability," NOT absence of chat. Ships D (sessions list) + narrow C (warm resume) — every ask becomes a checkpointed session object (answer + audit timeline + sources + subprocess log path). Five UI states (Empty / Typing / Live / SessionDetail / SessionsBrowse). ⌘R "Ask again from this" is the chat substitute — forks a new session, does NOT add a turn. Explicit cut list mirrors codex's: no chat threads, no daemon-side conversation memory, no custom session DB beyond LocalStorage + existing tee log files, no daemon endpoint changes. Component-split shrinks echo.tsx 1069 → ≤400 lines (project_v15_cleanup-aligned). Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| MED | [2026-05-18-062-ask-echo-raycast-llm-qa](../backlog/ready/2026-05-18-062-ask-echo-raycast-llm-qa.md) | Ask ECHO — Raycast Q&A command backed by a vendor-agnostic headless agent (codex / claude / custom) consuming ECHO MCP | 1-2d | Adds second Raycast command (⌘⇧A) that turns a typed question into a streaming Q&A view. ECHO does NOT host the LLM loop — agent binary (codex / claude / configurable custom) owns it via subprocess. Sidebar populates from new daemon introspection endpoint `GET /mcp/recent-calls` (loopback-only, 38478) backed by a `registerTool` wrapper + in-memory ring buffer with `args_shape`/`result_shape` redaction allowlist (no raw atom content / queries / paths / IDs). Single-shot by design (Codex warned of "persistent ECHO chat product" failure mode); OoS 1, 2, 3, 15, 16, 17 + AC9 founder-gate enforce. Vendor-agnosticism via agent-profile registry, not LLM SDK — adding a third agent is a profile entry, not new TS. 3 reviewer rounds (15 → 7 → 3 findings, no HIGH after R1, no structural pushback); functionally converged. AC9 gates V1 spec work on ≥5 journal entries (`**Surface:** Ask ECHO` marker) across ≥2 days. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | [2026-05-17-061-doc-leanout](../backlog/ready/2026-05-17-061-doc-leanout.md) | Doc surface leanout — drop committed HTML twins, shard the MCP journal, stub completed backlog items on wiki-promotion | 0.5-1d | **Operating-model hygiene, not product.** Specced after 3 triangulating audits (claude orchestrator's 3 parallel Explore subagents + codex read-only consultee + a prior codex strategist session that independently combined findings) converged on three growth sources: (1) committed HTML twins under `raw/internal/` (~3.0M, all pandoc-regenerated mirrors, no human edits) → AC1 drops + git-ignores + documents local regen; (2) the MCP journal MD monolith (1.1M / 5080 lines, growing ~53 entries/day) → AC2 freezes the current file as `mcp-interactions-journal-archive-through-2026-05-17.md` + starts monthly shards `mcp-interactions-journal-YYYY-MM.md`; (3) `backlog/complete/` accumulation (58 items / 1.8M) → AC4 stubs ONE canonical example (item 001) post-wiki-promotion (bulk migration is OoS; gated on 3+ natural merge-and-cleanup cycles proving the pattern). Plus three hygiene wins: AC3 content-policy "skip zero-MCP-call reviewer ticks" (codex strategist's catch), AC5 remove the stale `.claude/worktrees/laughing-shaw-952357/` worktree (11M, pre-item-050 discipline), AC6 fix the broken launchd recipe in `docs/review-queue-setup.md` + delete `docs/STATUS.md`. AC7 appends the ECHO timezone-offset retrieval bug to `_followups.md` (NOT a separate spec per friction-first stance). 12-item OoS list explicitly forbids bulk migration, reviews-tree archival, new tooling/automation, and the TZ-bug fix. Reviewer roster: `[codex, codex-ops]` (per founder direction 2026-05-17 — claude reviewer paused for usage reasons mid-061 drafting; falls back to the established two-codex pairing that ran 049/050/051/054/056 cleanly). Builder: any agent (mostly delete/edit; no new code). |
| MED | [2026-05-17-059-coord-emit-surface-daemon-rejection](../backlog/ready/2026-05-17-059-coord-emit-surface-daemon-rejection.md) | `coord-emit.sh` distinguishes daemon-rejection from daemon-unreachable (silent-failure observability gap) | 0.25-0.5d | Friction-fix surfaced by founder-driven live test of 057a/057b on 2026-05-17 (journal entry "2026-05-16 16:14 PDT — founder-driven live test of coord layer"). One file + one test extension. Same failure-class as the launchd silent-fail that motivated 057 in the first place. Reviewer roster: `["codex", "codex-ops", "claude"]` — first three-reviewer round; gives the post-056 `claude` binding its empirical-validation data point alongside the established codex / codex-ops pair. Builder: any agent. |
| HIGH | [2026-05-16-057a-coord-substrate-and-observability](../backlog/ready/2026-05-16-057a-coord-substrate-and-observability.md) | Coord layer 057a — substrate + observability (daemon-side `coord_emit` + `coord_status` + deadlines tracker + `wait_for_new_turns` widening; synthetic-emitter testable; ships dormant) | 2-2.5d | Decomposed from 057 (parent in `complete/` at 2026-05-15-057-...; r5 plateau triggered split). Independently shippable; production emission lives in 057b. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | [2026-05-16-057b-coord-active-trigger-and-role-emission](../backlog/ready/2026-05-16-057b-coord-active-trigger-and-role-emission.md) | Coord layer 057b — active trigger + role emission (`coord_invoke` MCP tool + wrapper two-phase emission + skill post-push hooks; activates 057a's dormant substrate) | 2-2.5d | **Blocked by 057a** — claim only after 057a is in `complete/`. Closes today's `_followups.md` HIGH #1 launchd silent-fail end-to-end when combined with 057a. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | [2026-05-15-055-cursor-as-builder-paste-trigger](../backlog/ready/2026-05-15-055-cursor-as-builder-paste-trigger.md) | Cursor-as-builder binding — paste-trigger skill prose for IDE-mode builder (formalizes existing Cursor-domain delegation) | 0.5d | Vendor-agnostic-pivot follow-up to 047. Adds "Binding-specific notes — Cursor's Claude (IDE-mode)" section to `skills/process-backlog.md` + operator doc `docs/cursor-builder-trigger.md`. NO wrapper, NO schema change, NO protocol changes. Closes #1 in the role×vendor matrix gap punch-list. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent (preferably Cursor's Claude for recursive dogfooding). |
| HIGH | [2026-05-15-056-claude-as-reviewer-headless](../backlog/ready/2026-05-15-056-claude-as-reviewer-headless.md) | Claude-as-reviewer headless onboarding — fourth reviewer slug end-to-end (roster + schemas + skill + vendor-agnostic wrapper) | 1-1.5d | Vendor-agnostic-pivot follow-up: closes the 5 gates (roster, 2 schemas, skill, driver) confirmed by live dry-run 2026-05-15. AC5 is the load-bearing substrate change — genericize `_run_reviewer.sh:140` via roster `invoke_command` field so future vendors slot in without wrapper edits. `claude` enters as `required: false`; flip after empirical validation. Closes #2 in the role×vendor matrix gap punch-list. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent. |
| HIGH | [2026-05-14-051-merge-lock-cross-vendor-enforcement](../backlog/ready/2026-05-14-051-merge-lock-cross-vendor-enforcement.md) | Merge-lock cross-vendor enforcement + push-with-retry `--rebase-merges` — interim two-prong fix until 050 ships | 0.5d | Friction-fix spec for the recurring HIGH class that hit twice in one day (2026-05-14 morning 048 collision @ `ec2907f` + 049-evening collision). Two prongs: (a) `tools/review-queue/push-with-retry.sh` switches `git pull --rebase` → `git pull --rebase --rebase-merges` so in-flight merge commits survive rebase; (b) `tools/review-queue/_run_reviewer.sh` (single source of truth for codex-side launchd reviewer ticks per 043 AC3) reads the merge-and-cleanup sentinel-file lock and exits 0 cleanly when present. Explicitly interim — 050 supersedes by deleting the lock convention entirely via worktree isolation. AC1's `--rebase-merges` survives 050; AC2/AC3 are dead code once 050 lands. Reviewer roster: `["codex", "codex-ops"]`. Builder: any agent (~10 lines of shell across two files + two tests). |
| HIGH | [2026-05-14-050-worktree-isolation-for-multi-step-main-writers](../backlog/ready/2026-05-14-050-worktree-isolation-for-multi-step-main-writers.md) | Worktree isolation for multi-step main writers — reviewers + watcher + merger move into ephemeral worktrees; delete the one-sided merge-lock convention | 1-1.5d | Friction-fix spec born from the 2026-05-14 14:02 PDT collision (codex reviewer launchd-tick swept Claude's in-flight 048 merge stage into commit `ec2907f`). Root cause: ECHO's cross-tool peer protocol exists at the skill (prose) layer but not at the substrate (git index) layer — only `merge-and-cleanup` honors the sentinel-file lock convention; reviewers and watcher race the live `.git/index` freely. 050 generalizes the builder-worktree discipline (already in `skills/process-backlog.md`) to reviewers + watcher + merger, deletes the one-sided sentinel-file lock entirely, and introduces no new substrate-lock primitive (no flock, no scheduler) per codex strategist Q1/Q2 cross-tool consult (atoms `3dde6d66` + `f2ef7f5c`). Reviewer roster: two codex (`["codex", "codex-ops"]`) per founder direction "full auto mode with two codex reviewers till converge." Builder: any agent. |
| HIGH | [2026-05-14-049-codex-skill-adapter](../backlog/complete/2026-05-14-049-codex-skill-adapter.md) | Codex skill adapter — superseded by direct `ECHO:*` skill installer | 0.5d | Shipped the original repo-side adapter/symlink path, then superseded during 2026-05-17 Codex dogfooding: canonical Codex import is now `tools/install-echo-codex-skills.sh`, which renders every `skills/*.md` directly into `~/.codex/skills/ECHO:<skill-name>/SKILL.md`. `tools/sync-skills.sh` now maintains Claude command copies only. |
| HIGH | [2026-05-14-048-process-backlog-builder-state-handoff-refresh](../backlog/ready/2026-05-14-048-process-backlog-builder-state-handoff-refresh.md) | Process-backlog builder-state handoff refresh — keep `builder.md` current when items move to `pending_review` | 0.5d | Friction-first follow-up from the 047 merge. Makes `/process-backlog` refresh role-typed builder state in the final handoff commit so reviewers/founder do not catch stale `builder.md` pointers manually. Builder: any agent. |
| HIGH | [2026-05-10-034-cursor-capture-coverage](../backlog/ready/2026-05-10-034-cursor-capture-coverage.md) | Cursor capture coverage — mid-stream bubble cadence + tool-call bubble parsing (M1-1 sub-gaps A+B) | 1.5-2d | Capture-layer gaps. R1+R2 cross-tool review complete (Codex + Cursor, 16 findings dispositioned over 2 rounds, both reviewers' verdicts: claimable). Builder: **Cursor's Claude** per Cursor-domain delegation pattern. |
| HIGH | [2026-05-10-035-tail-session-repo-scoping](../backlog/ready/2026-05-10-035-tail-session-repo-scoping.md) | `tail_session` repo-scoping for Cursor — workspace-aware MRU resolution (M1-1 sub-gap C) | 0.5-1d | Read-side resolver fix. M1-1 sub-gap C fired live 4 times today during 034's own R1+R2 review cycles. Adds `repo_path` parameter to `tail_session` + `QueryFilter.metadata_match` storage extension + workspace.json resolver. Specced in parallel with 034 implementation per founder direction 2026-05-10 22:55 PDT. Builder: any agent (pure MCP-resolver + storage work, not Cursor-domain specific). |
| HIGH | [2026-05-11-036-cursor-multicluster-continuation](../backlog/ready/2026-05-11-036-cursor-multicluster-continuation.md) | Cursor multi-cluster continuation capture — stop silently dropping post-checkpoint assistant bubbles (M1-1 sub-gap D) | 1-1.5d | Closes the V1.5.7 silent fast-forward gap surfaced by 034 AC4 dogfooding on composer 4f02b335 (15 / 26 assistant bubbles dropped, 47.6% capture rate). Emits continuation atoms (Option A from `_followups.md`) carrying `metadata.is_continuation` + `metadata.continuation_of_assistant_bubble_id` join key. Single-file extractor change in `extractCursorTurns`; new shape extends `CursorTurn` interface. Builder: **Cursor's Claude** per Cursor-domain delegation pattern (also acceptable: any agent — pure extractor logic). |
| HIGH | [2026-05-11-037-work-artifact-repo-scoping](../backlog/ready/2026-05-11-037-work-artifact-repo-scoping.md) | Work-artifact (repo) scoping end-to-end — `repo_path` first-class across capture + storage + 4 retrieval tools (RC1) | 1-1.5d | Closes RC1 from today's 3-way (CC + Codex + Cursor) cross-tool root-cause investigation. End-to-end: Cursor extractor writes `metadata.repo_root` (closes 035 freshness gap properly); `repo_root` added to `METADATA_MATCH_KEY_WHITELIST`; `repo_path` parameter uniform across `search_memories`, `find_clusters`, `wait_for_new_turns`, `tail_session` (generalized off Cursor-only branch). 3-way consensus on diagnosis + spec shape; see journal 2026-05-11 14:46/14:56/15:04/15:26 PDT. Builder: any agent. |
| HIGH | [2026-05-11-038-mcp-toolkit-atomicity-refactor](../backlog/ready/2026-05-11-038-mcp-toolkit-atomicity-refactor.md) | MCP toolkit atomicity refactor — subtractive surface reform (RC2) | 1.5-2d | Closes RC2 from today's 3-way investigation. **blocked_by 037.** Adds `echo_resolve_mru` (IDs-only resolver primitive). KILLS `tail_session` (subsumed by `search_memories(source_prefix=X, limit=N)` reverse-chrono + `echo_resolve_mru` composition) and `recent_work_context` (absorbs 031 gated removal — gate well-open since 030 shipped). Unbundles `wait_for_new_turns` bodies (IDs-only contract). DRYs `exclude_metadata_surface: ['fs']` (4 sites → 1 helper). NO renames of `get_atom` / `get_atoms` (Codex round-3 evidence: `get_atom` is the only verbatim path — `get-atom.ts:139`). Net surface 8 → 7 tools (subtractive — matches founder's `v15_cleanup_pause` framing). 4-round strategist convergence on Option D (Cursor) + Codex's `get_atom` save + Cursor's `recent_work_context` fold-in; see journal 2026-05-11 ~16:18-16:40 PDT. Builder: any agent (pure MCP-server refactor; largest risk is the find_clusters / recent_work_context untangle in AC3). |
| HIGH | [2026-05-11-039-cross-tool-review-dispatch-queue](../backlog/ready/2026-05-11-039-cross-tool-review-dispatch-queue.md) | Cross-tool review dispatch queue — file-backed protocol; founder out of dispatch loop (**RC5 / R4-patched — CONVERGED**) | 1.5-2d | Operating-model item. **CONVERGED 2026-05-12 01:18 PDT** after 4 rounds (R1: pushback / 18 findings → R2: proceed_after_patches / 14 → R3: proceed_after_patches / 8 → R4: Cursor `proceed` ZERO + Codex `proceed_after_patches` / 2 LOW). 42 total findings dispositioned; zero residual HIGH/MED at convergence; termination via AC3.5 (c) explicit-waiver (Codex R4 reviewer notes explicitly waived next round; patches mechanical test-wording). **Claim-ready: any agent may now atomically claim and build.** Cycle empirically validated: reviewer differentiation real, zero-ECHO-call reviewer property held, bootstrap-write pattern worked, recursive review-history-as-evolutionary-record property fired (Codex R3 M1 caught AC3.5 state-machine bug by reading prior rounds), journal-as-queue prohibition invariant held, founder escalation never fired across 4 rounds. Builder: any agent (pure protocol + helper scripts + slash-command prompts; the strategist that authored 039 IS acceptable). |
| HIGH | [2026-05-12-040-watcher-state-executable-test](../backlog/ready/2026-05-12-040-watcher-state-executable-test.md) | Watcher post-combine state machine — executable test of AC3.5 (b) (**R3 CONVERGED — claim-ready**) | 0.5d | **CONVERGED 2026-05-12 ~03:00 PDT** after 3 rounds at spec_commit_sha `784698f` (R1: 8 findings / 5 patches → R2: 6 findings / 3 patches → R3: 0 findings, both reviewers `proceed`). **Also: 039 AC6b loop-close gate fired clean** — full live-test ran end-to-end with zero founder→reviewer dispatch messages (Codex sandbox + Cursor YAML emission frictions logged as session-bootstrap / emission-validation defects in `_followups.md`, not dispatch messages). Spec extracts the watcher's (a)/(b)/(c) post-combine prose into `tools/review-queue/dispatch-next-round.py`; AC3 fixtures assert load-bearing transition; explicit branch-specific git block in watcher slash-command; `os.replace` (not `os.link`) for combined.md in-place atomic updates; `--spec-sha` pass-through to request.py. **Claim-ready: any agent may now atomically claim.** |
| HIGH | [2026-05-12-041-reviewer-background-execution](../backlog/ready/2026-05-12-041-reviewer-background-execution.md) | Reviewer background execution — Codex headless via launchd; Cursor accept-degradation; mechanically-enforced emission validation (**R3 CONVERGED — claim-ready**) | 0.5-1d | **CONVERGED 2026-05-12 ~14:46 PDT** after 3 rounds at spec_commit_sha `e8edb29` (R1: 8+1 findings / 5 patches → R2: 6 findings / 5 patches → R3: 0 findings, both reviewers `proceed`). Same 8→6→0 decay shape as 040 — fourth structural-reform-shaped data point. **Closes the 040 founder-activation-friction gap.** Ships: (1) Codex launchd plist + wrapper + log → unattended 10-min ticks after one-time install; (2) Cursor accept-degradation policy explicit; (3) `tools/review-queue/commit-reviewer-response.sh` mechanically-enforced validation gate (Codex amendment); (4) `ECHO_REVIEW_QUEUE_REPO_ROOT` env var contract isolates smoke from production (Codex H2 + Cursor M1 convergent); (5) AC4 invalid-file MV-aside `<reviewer>.md.invalid.<ISO-ts>` unblocks unattended retry (Codex R1 HIGH — would have shipped a footgun); (6) AC5 isolated tmp work + local bare origin with `-b main` (Codex M1: push-with-retry hardcodes `origin main`); (7) AC0 sandbox + flag recipe fixes; (8) scoped `atom_id`→`id` audit. AC8 observational: target ≤1 founder activation per cycle (pre-041 baseline ~5). **Claim-ready: any agent may now atomically claim** (macOS required for launchd verification). |
| HIGH | [2026-05-12-042-reviewer-emission-yaml-validation](../backlog/ready/2026-05-12-042-reviewer-emission-yaml-validation.md) | Reviewer emission YAML validation — yaml.YAMLError → typed ValueError in `_lib.parse_frontmatter` (closes reviewer-side gate) + combine.py defensive parse → `escalated_to_founder: true` combined.md (closes watcher crash path) | 0.5d | **✅ MERGED 2026-05-13 at `4fb517d`.** 3-round cycle in ~50 min wall-time, AC8 = 0 founder activations (the load-bearing dogfooding measurement). See review_notes in `backlog/complete/2026-05-12-042-...md`. |
| HIGH | [2026-05-13-043-per-round-reviewer-roster](../backlog/ready/2026-05-13-043-per-round-reviewer-roster.md) | Per-round reviewer roster — make `request.requested_reviewers` the per-round active set; add `reviewers.json` as runtime metadata; defer non-Codex headless runner abstraction | 1-1.5d | **✅ MERGED 2026-05-13 at `d846c35`.** Generalizes the cross-tool review queue's hardcoded `(codex, cursor)` assumption to N reviewers. See `backlog/complete/2026-05-13-043-...md` for review_notes. 8 review rounds, AC8 = 0 founder activations. |
| HIGH | [2026-05-13-044-reviewer-cycle-infrastructure-debt](../backlog/complete/2026-05-13-044-reviewer-cycle-infrastructure-debt.md) | Reviewer-cycle infrastructure debt — fix the 4 compounding frictions from 043's dogfooding (autostash + direct-invoke wrapper + per-reviewer timeout + single-reviewer auto-disposition) | 1-1.5d | **✅ MERGED 2026-05-13 at `ca51bb2`.** 3-round convergence; AC8=0 founder activations during the autonomous review cycle. 044 closed friction items #1-#4 from 043's enumeration; 045 bundles the recurring frictions surfaced DURING 044's own cycle. See `backlog/complete/2026-05-13-044-...md` review_notes. |
| HIGH | [2026-05-13-045-queue-reliability-friction-cluster](../backlog/complete/2026-05-13-045-queue-reliability-friction-cluster.md) | Queue-reliability friction cluster — recurring frictions surfaced during the 044 cycle | 1-1.5d | **✅ MERGED 2026-05-13 at `ad5ba68`.** 2-round convergence; AC8=0 founder activations. 6 ACs implemented: AC1 reviewer-side pre-link YAML gate (`validate_response_yaml.py` helper) + AC2 smoke gate fail-closed BEFORE production state + AC3 closes `concurrency.test.ts:133` 3-cycle deferral + AC4 prose alignment + AC5 /merge-and-cleanup robustness + AC6 /review-pending sidecar handoff. Mid-cycle skills relocation (commit `6d29f51`) absorbed via pre-merge fixup (builder's `.claude/commands/<X>.md` edits copied to canonical `skills/<X>.md`). Tests: 835/856 pass, 0 fail, 21 skip. See `backlog/complete/2026-05-13-045-...md` review_notes. |
| HIGH | [2026-05-13-046-context-fatigue-via-role-typed-state](../backlog/complete/2026-05-13-046-context-fatigue-via-role-typed-state.md) | Context fatigue via role-typed task-state — role-slot-agnostic orchestration viewed from the friction side (**LAST friction-removal spec before vendor-agnostic pivot**) | 1.5-2d | **✅ MERGED 2026-05-13 at `b419994`.** 5-round cycle decay 9→5→2→1→2 (matches 040-045 shape). 23 findings dispositioned, 12 unique root, zero scope rejects. F1+F2 builder-applied at merge time per R5 `proceed_after_patches` semantic. Empirical baseline for next cycle's PASS test: `raw/internal/dogfooding/role-typed-state-baseline.md` (3 falsifiable conditions). |
| HIGH | [2026-05-13-047-codex-as-builder-binding-adapter](../backlog/ready/2026-05-13-047-codex-as-builder-binding-adapter.md) | Codex-as-builder binding adapter — the missing binding that makes role-slot-agnostic orchestration empirically true (**first post-046 spec; vendor-agnostic pivot begins**) | 1-1.5d | Ships `tools/backlog/run-codex-builder.sh` thin shell wrapper that lets `codex exec` run `skills/process-backlog.md`'s vendor-neutral protocol. One-shot, long-running lifecycle (not periodic). `danger-full-access` sandbox + local lockfile + stable ECHO_AGENT_ID + log rotation, matching `_run_reviewer.sh` shape for shared concerns. Cycle bindings: strategist=claude, reviewers=[codex, cursor], builder of 047 is whichever existing binding claims (chicken-and-egg). AC5 opportunistically measures 046's three baseline PASS conditions (observational, not a hard gate). FAIL on §1 / §3 / §5 → file `048-046-rollback-or-redesign`. Builder: any existing binding (Claude Code or Cursor's Claude). |

*V1.6 successor items map (post-030 friction revisit, 2026-05-10): (1) 029 follow-ups [`_followups.md:127-138`], (2) ✅ **032 M2 reliability** — shipped, (3) ✅ **033 full-atom recovery** — shipped, (4) **034 Cursor capture coverage** — M1-1 sub-gaps A+B, in flight, (5) **035 tail_session repo-scoping** — M1-1 sub-gap C, specced in parallel, (6) hotkey overlay vertical slice, (7) search-ranking / verdict-turn finding (M1-2, **the hardest — saved till the end** per founder direction 2026-05-10 20:08 PDT), (8) subagent dispatch compact/pagination, (9) token-count enrichment per-extractor, (10) 031 deprecation [gated on ≥1 week post-034+035 dogfooding].*

*V2 territory (Wave 4): GitHub adapter, Slack adapter, browser extension upgrade, audit page. Out of scope for V1 per 2026-05-10 founder scope reset.*

---

## 🔒 Claimed (agent owns; in flight on a feature branch)

*(empty — agents move items here via atomic claim)*

| Priority | ID | Title | Claimed By | Branch |
|---|---|---|---|---|
| HIGH | [2026-05-17-060-hotkey-overlay-v0-raycast-dogfood](../backlog/pending_review/2026-05-17-060-hotkey-overlay-v0-raycast-dogfood.md) | V0 hotkey overlay — Raycast extension for founder dogfooding (V1 spec deferred until v0 surfaces friction) | `agent/hotkey-overlay-v0-raycast-dogfood` | Built and pushed at `0857e346064ff7090cd642d7ad0520e9ed43cb61`; verification passed: `npm test`, `npm run typecheck`, `npx ray build`. |

---

## 👀 Pending Review (agent done; founder reviews + merges)

*(empty — agents move items here on completion or escalation; founder reviews each morning)*

| Priority | ID | Title | Branch | Notes |
|---|---|---|---|---|
| | | | | |

---

## ✅ Complete (merged; wiki update may be pending)

**Wave 1 — substrate skeleton.** All five items merged; wiki promoted in commit `7ad5db7` (post-shipment, per the operating model).

| ID | Title | Shipped |
|---|---|---|
| [2026-04-30-001](../backlog/complete/2026-04-30-001-repo-bootstrap.md) | Repo bootstrap | TS/Node + Vitest + ESLint + Prettier scaffold |
| [2026-04-30-002](../backlog/complete/2026-04-30-002-logger.md) | Logger module | Structured JSON-per-line; source-attributed; ECHO_LOG_LEVEL env filter |
| [2026-04-30-003](../backlog/complete/2026-04-30-003-capture-allowlist.md) | Capture allowlist | Empty CAPTURED_SOURCES + Source type + isAllowed* predicates |
| [2026-04-30-004](../backlog/complete/2026-04-30-004-capture-gate.md) | Capture gate | Pure chokepoint function; 28+ test cases; stable rejection codes |
| [2026-04-30-005](../backlog/complete/2026-04-30-005-storage-interface.md) | Storage interface + MemoryStorage | Append-only contract; in-memory impl as test fixture |

**Wave 2 — bring the substrate to life.** Wires the substrate skeleton into a running daemon. 006/007/008 ran in parallel; 009 followed once all three landed. The capture gate's accept-path is exercised in production for the first time at 009. Wiki promoted in commit `44dd2d3` (waves 2 + 3 promoted together).

| ID | Title | Shipped |
|---|---|---|
| [2026-04-30-006](../backlog/complete/2026-04-30-006-capture-pipeline.md) | Capture pipeline (gate → storage wire-up) | Thin async seam `processCandidate(event, storage)`; gates then appends; storage dependency-injected |
| [2026-04-30-007](../backlog/complete/2026-04-30-007-daemon-entry.md) | Daemon entry + lifecycle | Boot/shutdown scaffold; PID lock; signal handling; loopback-only binding |
| [2026-04-30-008](../backlog/complete/2026-04-30-008-sqlite-storage.md) | SQLite storage (better-sqlite3) | WAL mode; migration runner; `source_prefix` filter; default backend (override via `ECHO_STORAGE=memory`) |
| [2026-04-30-009](../backlog/complete/2026-04-30-009-fs-watcher-cursor-and-claude-code.md) | FS watcher — first capture surface | chokidar-backed; allowlisted Cursor + Claude Code paths; gate accept-path live in production |

**Wave 3 — close the loop with MCP.** Adds content extractors, git capture, the MCP server, and the `search_memories` retrieval tool. Lands the V1 spec's killer-demo loop end-to-end: a real Cursor or Claude Code session retrieves context through MCP from the unified store. Wiki promoted in commit `44dd2d3`. Process retrospective in `wiki/operating-model/wave-1-2-3-retrospective.md`.

| ID | Title | Shipped |
|---|---|---|
| [2026-04-30-010](../backlog/complete/2026-04-30-010-cursor-extractor.md) | Cursor extractor (composer chat from globalStorage) | Read-only SQLite parser; user→assistant cluster pairing; `metadata.context` (attached/referenced/deleted files); drift-discipline exhibit (re-claimed after schema-probe correction) |
| [2026-04-30-011](../backlog/complete/2026-04-30-011-claude-code-extractor.md) | Claude Code extractor | Byte-offset JSONL tail; one CaptureEvent per turn pair; nested `{type, message:{role, content}}` shape |
| [2026-04-30-012](../backlog/complete/2026-04-30-012-git-capture.md) | Git capture (commits via refs watch) | Hybrid chokidar + poll watcher; one CaptureEvent per new commit (message + diff) from allowlisted git repos |
| [2026-04-30-013](../backlog/complete/2026-04-30-013-mcp-server-skeleton.md) | MCP server skeleton (HTTP transport, stub tool) | Streamable HTTP/SSE on `127.0.0.1:38478`; `echo_ping` tool; loopback-only |
| [2026-04-30-014](../backlog/complete/2026-04-30-014-mcp-search-memories.md) | MCP `search_memories` tool | Case-insensitive substring + filters (`source_prefix`, `since`, `until`, `limit`); DESC by timestamp; `source_prefix` LIKE-escape fix landed pre-merge; embeddings deferred to V1.5 |
| [2026-04-30-015](../backlog/complete/2026-04-30-015-mcp-integration-test.md) | Cursor + Claude Code MCP integration test | Smoke script (`tools/mcp-integration-smoke.sh`); manual demo verified through real Cursor + Claude Code sessions; Vitest harness deferred to follow-up |

**V1.5 — trace + retrieval reliability + envelope discipline.** Items 016–023 + 025–028 land the recent-work-context tool, edge filtering, open-loop hint resolution, cross-gap reconstruction, the V1.5.2 reliability bundle, the chokidar flake quarantine, MCP best-practices, the `tail_session` tool, stateless transport, and the `format:'skeleton'` envelope mode. Wiki promotion pending.

| ID | Title | Shipped |
|---|---|---|
| [2026-05-06-016](../backlog/complete/2026-05-06-016-read-time-normalizer.md) | Read-time normalizer (capture → normalized atom) | Per-source adapters; deterministic normalization; provenance preserved |
| [2026-05-06-018](../backlog/complete/2026-05-06-018-recent-work-context-tool.md) | `get_recent_work_context` MCP tool | Cluster-shape retrieval; artifact-graph edges; rank/label heuristics |
| [2026-05-07-019](../backlog/complete/2026-05-07-019-trace-edge-filter-and-format.md) | Trace edge-filter + format projection | `cluster.edges[]` filtered to signal-bearing pairs; `format:'minimal'` clip |
| [2026-05-07-020](../backlog/complete/2026-05-07-020-open-loop-resolution-heuristics.md) | Open-loop hint resolution heuristics | `open_loop_hints[].resolved` heuristic with founder-validation pass |
| [2026-05-07-021](../backlog/complete/2026-05-07-021-trace-cross-gap-where-left-off.md) | Cross-gap "where did I leave off" reconstruction | Window-spanning trace; asc-order storage consumer |
| [2026-05-08-022](../backlog/complete/2026-05-08-022-v15-2-trace-retrieval-reliability.md) | V1.5.2 trace + retrieval reliability bundle | Timestamp UTC-Z canonicalization + 152-row migration; storage-cap warning; fs-watcher noise filter; filter-before-slice fix; description clarification; TZ-marker regex broadening |
| [2026-05-08-023](../backlog/complete/2026-05-08-023-chokidar-flake-quarantine.md) | Chokidar flake quarantine | `describe.skip` on `cursor.test.ts` + `daemon/lifecycle.test.ts` flaky blocks; tracking comments; 3 consecutive clean runs |
| [2026-05-08-024](../backlog/complete/2026-05-08-024-fs-watcher-test-quarantine-successor.md) | fs-watcher.test.ts quarantine successor | `describe.skip` on the `startFsWatcher` block; closes the third flaky-test carry-over from item 014 |
| [2026-05-08-025](../backlog/complete/2026-05-08-025-mcp-best-practices.md) | V1.5.3 MCP best-practices bundle | `outputSchema` + `structuredContent` on all tools; `source_app` enum; cost-safer defaults; composite cursor pagination; `readOnlyHint` |
| [2026-05-08-026](../backlog/complete/2026-05-08-026-tail-session-tool.md) | `tail_session` MCP tool | Cheap exact-fetch primitive; `source` or `source_app`; default count=5, max 20 |
| [2026-05-08-027](../backlog/complete/2026-05-08-027-mcp-stateless-transport.md) | MCP stateless transport | `sessionIdGenerator: undefined` + JSON response mode; eliminates stale-session failures after daemon restart |
| [2026-05-08-028](../backlog/complete/2026-05-08-028-rwc-envelope-skeleton-format.md) | `format:'skeleton'` envelope mode | Strips uncapped sub-collections; realistic-density acceptance test; default minimal unchanged |

---

## How to Read This Board

- **Priority HIGH** = on V1 critical path; week-3 substrate gate depends on it
- **Priority MED** = parallel work that compounds (validation, extension)
- **Priority LOW** = nice-to-have; deferrable
- Each item file has frontmatter (status, spec refs, acceptance, files affected, agent-managed fields, review notes) plus a body with What / Why / Acceptance / Out of Scope / After Completion sections

## How to Add Work

During a strategic conversation:

1. Decision is made → spec is captured *inside* a new item file at `backlog/ready/<id>-<slug>.md` (this is the canonical spec until shipment)
2. Add a row to the Ready table on this board
3. Do **not** touch `wiki/` — wiki updates happen only after the item lands in `complete/`

When items land in `complete/`, the next strategist conversation reads each item's "After Completion (Strategist Notes)" section and promotes the shipped decisions into `wiki/`.

## How to Trigger an Agent

Agents use the `/process-backlog` slash command (defined in `.claude/commands/process-backlog.md`).

**Single-item trigger** (claim → ship → stop):

```
/process-backlog
```

The command claims the oldest highest-priority unblocked ready item, creates a worktree, implements, pushes the branch, and moves the item to `pending_review/`. One item per run.

**Batch trigger** (drain the queue):

```
/process-backlog-batch
```

Same workflow, looped: keeps claiming and shipping unblocked items until a hard stop fires (max items, time budget, escalation, no-candidates, or git error). Sequential within the session. Use this for late-night seed-and-walk-away workflows. Hard stops are configurable via env: `ECHO_BATCH_MAX_ITEMS` (default 10), `ECHO_BATCH_TIMEOUT_SECS` (default 21600 = 6h), `ECHO_BATCH_HALT_ON_ESCALATION` (default 1 = strict).

**Parallel runs:** multiple sessions of either command may run simultaneously, each with a distinct `ECHO_AGENT_ID`. The atomic-claim mechanic ensures only one agent ever owns an item; if a push race occurs, the loser picks the next ready item and retries.

**Scheduled trigger** (steady-state): see [`.claude/SETUP.md`](./.claude/SETUP.md). Recommended cadence and parallelism land there.

**Operating manual:** [`docs/AGENT_INSTRUCTIONS.md`](./AGENT_INSTRUCTIONS.md) — required reading for every agent on every run.
