{
  "$schema": "Manifest schema for ECHO project wiki ingestion. The lead agent generates .manifest.json following this structure.",

  "_entry_template": {
    "<folder>/<filename>.md": {
      "status": "shipped | planned",
      "title": "Display name for index.md",
      "link": "filename-without-folder-or-extension (agents use this verbatim in [[wikilinks]])",
      "summary": "One-line description for index.md (written by lead agent, never parsed from page content)",
      "aliases": ["Title Case", "CamelCase", "other common references"],
      "topic": "Topic from taxonomy",
      "subtopic": "Specific area within topic",
      "raw": "path/to/source/file.md (source pages only, omit for principles/surfaces/architecture)"
    }
  },

  "_rules": {
    "filenames": "kebab-case, globally unique across all folders. Prefix with src- if a name would collide elsewhere.",
    "link_field": "Filename only. No folder prefix, no .md extension. Agents copy this into [[link]] verbatim.",
    "status_field": "shipped = the page documents reality that exists (code merged, decision locked, research logged). planned = a V1-locked commitment whose underlying thing is specced but not yet built. The wiki is lagging-doc; aspirational pages must say so explicitly via this field.",
    "summary_field": "Max 120 chars. Written during manifest generation so consolidation never parses page content.",
    "aliases_field": "Must include at least the Title Case version of the filename. Obsidian uses these to resolve [[wikilinks]].",
    "topic_field": "Must match the taxonomy below. Reuse existing topics before creating new ones.",
    "raw_field": "Relative path from project root. Only on pages whose authority is a separate source file (e.g., capture-allowlist mirrors src/capture/sources.ts)."
  },

  "_folders": {
    "product/": "Strategic 'what' — locked V1 scope, target cohort, brand, sequencing decisions",
    "principles/": "Active commitments and disciplines: design, brand, scope-discipline. What we believe and hold.",
    "architecture/": "The durable middle — substrate components and the canonical models (system-architecture, interface-layers)",
    "capture/": "Layer 1 capture surfaces — per-source watchers and extractors (substrate's left edge)",
    "capture/per-app/": "Field-level reference for what each connected app contributes (cursor, claude-code, github, slack, web-extension, ...)",
    "surfaces/": "Layer 3 + Layer 5 — what users and AI clients touch (MCP, hotkey overlay, audit page, browser extension)",
    "research/": "Validation work — wedge / thesis / cohort / funnel research",
    "operating-model/": "Process meta — wave retrospectives, drift audits, and other build-process artifacts"
  },

  "_taxonomy": {
    "Brand & Positioning": ["Brand Promise", "Naming", "Obsession Statement"],
    "Form Factor": ["Ambient Surfaces", "Felt Not Seen", "Hotkey Overlay", "Browser Extension", "MCP Server"],
    "Architecture": ["Layer Stack", "Compose Not Capture", "Layer Above SaaS", "Storage", "Retrieval Composition", "System Architecture", "Substrate", "Capture", "Capture Surfaces", "Per-App Data Collection", "Observability"],
    "V1 Scope": ["Bundle Decision", "Cohort Selection", "Sequencing", "Non-Goals"],
    "Validation": ["Wedge Validation", "Thesis Validation", "Extension Funnel", "User Interviews", "Concierge"],
    "GTM": ["Distribution", "Pricing", "Launch", "Onboarding"],
    "Cohorts": ["Indie AI Builders", "Vibe Coders", "Solo SaaS Founders", "Future Cohorts"],
    "Process": ["Wave Retrospective", "Drift Audit", "Spec Authoring"]
  },

  "_consolidation": {
    "index.md": "Auto-generated by tools/wiki_index.py from .manifest.json. Grouped by folder (in narrative order: product, principles, architecture, capture, capture/per-app, surfaces, research, operating-model), then by topic within folder. Each line: - [[link|title]] — summary, with planned pages tagged inline.",
    "log.md": "Append one entry per operation with timestamp, operation type, and page counts."
  }
}
